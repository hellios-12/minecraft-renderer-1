"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod2) => function __require() {
  return mod2 || (0, cb[__getOwnPropNames(cb)[0]])((mod2 = { exports: {} }).exports, mod2), mod2.exports;
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod2, isNodeMode, target) => (target = mod2 != null ? __create(__getProtoOf(mod2)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod2 || !mod2.__esModule ? __defProp(target, "default", { value: mod2, enumerable: true }) : target,
  mod2
));

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/CommonChunkColumn.js
var require_CommonChunkColumn = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/CommonChunkColumn.js"(exports2, module2) {
    var posKey = (pos) => `${pos.x},${pos.y},${pos.z}`;
    var CommonChunkColumn = class {
      constructor(registry) {
        this.registry = registry;
        this.blockEntities = {};
        this.minY = 0;
      }
      // Some section getters, used by anvil-provider
      getSection(pos) {
        return this.sections[pos.y - this.minY >> 4];
      }
      getSectionAtIndex(index) {
        const minY = this.minY >> 4;
        return this.sections[index - minY];
      }
      // Biomes
      getBiomeId(pos) {
        return this.getBiome(pos);
      }
      setBiomeId(pos, biome) {
        this.setBiome(pos, biome);
      }
      getBiomeData(pos) {
        const biome = this.getBiome(pos);
        return this.registry[biome];
      }
      getBiomeColor(pos) {
        const { color } = this.getBiomeData(pos);
        const r = color >> 16;
        const g = color >> 8 & 255;
        const b = color & 255;
        return { r, g, b };
      }
      setBiomeColor() {
        throw new Error("Cannot change biome color, update the biome instead");
      }
      // Block entities
      getBlockEntity(pos) {
        return this.blockEntities[posKey(pos)];
      }
      setBlockEntity(pos, tag) {
        this.blockEntities[posKey(pos)] = tag;
      }
      removeBlockEntity(pos) {
        delete this.blockEntities[posKey(pos)];
      }
      loadBlockEntities(entities) {
        for (const entity of entities) {
          this.setBlockEntity({ x: entity.x.value, y: entity.y.value, z: entity.z.value }, entity);
        }
      }
    };
    module2.exports = CommonChunkColumn;
  }
});

// ../node_modules/.pnpm/uint4@0.1.2/node_modules/uint4/index.js
var require_uint4 = __commonJS({
  "../node_modules/.pnpm/uint4@0.1.2/node_modules/uint4/index.js"(exports2, module2) {
    var readUInt4BE = function(buffer, cursor) {
      if (cursor % 1)
        return buffer.readUInt8(Math.floor(cursor)) & 15;
      else
        return buffer.readUInt8(cursor) >> 4;
    };
    var writeUInt4BE = function(buffer, value, cursor) {
      if (value >= 16)
        throw new Error("value is out of bounds");
      var byteLoc = Math.floor(cursor);
      if (cursor % 1)
        buffer.writeUInt8(readUInt4BE(buffer, byteLoc) << 4 | value, byteLoc);
      else
        buffer.writeUInt8(value << 4 | readUInt4BE(buffer, cursor), byteLoc);
    };
    var readUInt4LE = function(buffer, cursor) {
      if (cursor % 1)
        return buffer.readUInt8(Math.floor(cursor)) >> 4;
      else
        return buffer.readUInt8(cursor) & 15;
    };
    var writeUInt4LE = function(buffer, value, cursor) {
      if (value >= 16)
        throw new Error("value is out of bounds");
      var byteLoc = Math.floor(cursor);
      if (cursor % 1)
        buffer.writeUInt8(value << 4 | readUInt4LE(buffer, Math.floor(cursor)), byteLoc);
      else
        buffer.writeUInt8(readUInt4LE(buffer, byteLoc) << 4 | value, byteLoc);
    };
    module2.exports.read = readUInt4BE;
    module2.exports.readUInt4 = readUInt4BE;
    module2.exports.write = writeUInt4BE;
    module2.exports.writeUInt4 = writeUInt4BE;
    module2.exports.readUInt4BE = readUInt4BE;
    module2.exports.writeUInt4BE = writeUInt4BE;
    module2.exports.readUInt4LE = readUInt4LE;
    module2.exports.writeUInt4LE = writeUInt4LE;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.8/section.js
var require_section = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.8/section.js"(exports2, module2) {
    var { readUInt4LE, writeUInt4LE } = require_uint4();
    var w = 16;
    var l = 16;
    var sh = 16;
    var getArrayPosition = function(pos) {
      return pos.x + w * (pos.z + l * pos.y);
    };
    var getBlockCursor = function(pos) {
      return getArrayPosition(pos) * 2;
    };
    var getBlockLightCursor = function(pos) {
      return getArrayPosition(pos) * 0.5 + w * l * sh * 2;
    };
    var getSkyLightCursor = function(pos) {
      return getArrayPosition(pos) * 0.5 + w * l * sh / 2 * 5;
    };
    var Section = class _Section {
      constructor(skyLightSent = true) {
        const SECTION_SIZE = _Section.sectionSize(skyLightSent);
        this.data = Buffer.alloc(SECTION_SIZE);
        this.data.fill(0);
      }
      toJson() {
        return this.data.toJSON();
      }
      static fromJson(j) {
        const section = new _Section();
        section.data = Buffer.from(j);
        return section;
      }
      static sectionSize(skyLightSent = true) {
        return w * l * sh * (skyLightSent ? 3 : 5 / 2);
      }
      initialize(iniFunc) {
        const skylight = w * l * sh / 2 * 5;
        const light = w * l * sh * 2;
        let n = 0;
        for (let y = 0; y < sh; y++) {
          for (let z = 0; z < w; z++) {
            for (let x = 0; x < l; x++, n++) {
              const block = iniFunc(x, y, z, n);
              if (block == null) {
                continue;
              }
              this.data.writeUInt16LE(block.type << 4 | block.metadata, n * 2);
              writeUInt4LE(this.data, block.light, n * 0.5 + light);
              writeUInt4LE(this.data, block.skyLight, n * 0.5 + skylight);
            }
          }
        }
      }
      getBiomeColor(pos) {
        return {
          r: 0,
          g: 0,
          b: 0
        };
      }
      setBiomeColor(pos, r, g, b) {
      }
      getBlockStateId(pos) {
        const cursor = getBlockCursor(pos);
        return this.data.readUInt16LE(cursor);
      }
      getBlockType(pos) {
        const cursor = getBlockCursor(pos);
        return this.data.readUInt16LE(cursor) >> 4;
      }
      getBlockData(pos) {
        const cursor = getBlockCursor(pos);
        return this.data.readUInt16LE(cursor) & 15;
      }
      getBlockLight(pos) {
        const cursor = getBlockLightCursor(pos);
        return readUInt4LE(this.data, cursor);
      }
      getSkyLight(pos) {
        const cursor = getSkyLightCursor(pos);
        return readUInt4LE(this.data, cursor);
      }
      setBlockStateId(pos, stateId) {
        const cursor = getBlockCursor(pos);
        this.data.writeUInt16LE(stateId, cursor);
      }
      setBlockType(pos, id) {
        const cursor = getBlockCursor(pos);
        const data = this.getBlockData(pos);
        this.data.writeUInt16LE(id << 4 | data, cursor);
      }
      setBlockData(pos, data) {
        const cursor = getBlockCursor(pos);
        const id = this.getBlockType(pos);
        this.data.writeUInt16LE(id << 4 | data, cursor);
      }
      setBlockLight(pos, light) {
        const cursor = getBlockLightCursor(pos);
        writeUInt4LE(this.data, light, cursor);
      }
      setSkyLight(pos, light) {
        const cursor = getSkyLightCursor(pos);
        writeUInt4LE(this.data, light, cursor);
      }
      dump() {
        return this.data;
      }
      load(data, skyLightSent = true) {
        const SECTION_SIZE = _Section.sectionSize(skyLightSent);
        if (!Buffer.isBuffer(data)) {
          throw new Error("Data must be a buffer");
        }
        if (data.length !== SECTION_SIZE) {
          throw new Error(`Data buffer not correct size (was ${data.length}, expected ${SECTION_SIZE})`);
        }
        this.data = data;
      }
    };
    Section.w = w;
    Section.l = l;
    Section.sh = sh;
    module2.exports = Section;
  }
});

// ../node_modules/.pnpm/vec3@0.1.10/node_modules/vec3/index.js
var require_vec3 = __commonJS({
  "../node_modules/.pnpm/vec3@0.1.10/node_modules/vec3/index.js"(exports2, module2) {
    var re = /\((-?[.\d]+), (-?[.\d]+), (-?[.\d]+)\)/;
    var Vec32 = class _Vec3 {
      constructor(x, y, z) {
        this.x = x;
        this.y = y;
        this.z = z;
      }
      isZero() {
        return this.x === 0 && this.y === 0 && this.z === 0;
      }
      at(id) {
        return this.toArray()[id];
      }
      xz() {
        return [this.x, this.z];
      }
      xy() {
        return [this.x, this.y];
      }
      yz() {
        return [this.y, this.z];
      }
      xzy() {
        return new _Vec3(this.x, this.z, this.y);
      }
      set(x, y, z) {
        this.x = x;
        this.y = y;
        this.z = z;
        return this;
      }
      update(other) {
        this.x = other.x;
        this.y = other.y;
        this.z = other.z;
        return this;
      }
      rounded() {
        return new _Vec3(Math.round(this.x), Math.round(this.y), Math.round(this.z));
      }
      round() {
        this.x = Math.round(this.x);
        this.y = Math.round(this.y);
        this.z = Math.round(this.z);
        return this;
      }
      floored() {
        return new _Vec3(Math.floor(this.x), Math.floor(this.y), Math.floor(this.z));
      }
      floor() {
        this.x = Math.floor(this.x);
        this.y = Math.floor(this.y);
        this.z = Math.floor(this.z);
        return this;
      }
      offset(dx, dy, dz) {
        return new _Vec3(this.x + dx, this.y + dy, this.z + dz);
      }
      translate(dx, dy, dz) {
        this.x += dx;
        this.y += dy;
        this.z += dz;
        return this;
      }
      add(other) {
        this.x += other.x;
        this.y += other.y;
        this.z += other.z;
        return this;
      }
      subtract(other) {
        this.x -= other.x;
        this.y -= other.y;
        this.z -= other.z;
        return this;
      }
      multiply(other) {
        this.x *= other.x;
        this.y *= other.y;
        this.z *= other.z;
        return this;
      }
      divide(other) {
        this.x /= other.x;
        this.y /= other.y;
        this.z /= other.z;
        return this;
      }
      plus(other) {
        return this.offset(other.x, other.y, other.z);
      }
      minus(other) {
        return this.offset(-other.x, -other.y, -other.z);
      }
      scaled(scalar) {
        return new _Vec3(this.x * scalar, this.y * scalar, this.z * scalar);
      }
      abs() {
        return new _Vec3(Math.abs(this.x), Math.abs(this.y), Math.abs(this.z));
      }
      volume() {
        return this.x * this.y * this.z;
      }
      modulus(other) {
        return new _Vec3(
          euclideanMod(this.x, other.x),
          euclideanMod(this.y, other.y),
          euclideanMod(this.z, other.z)
        );
      }
      distanceTo(other) {
        const dx = other.x - this.x;
        const dy = other.y - this.y;
        const dz = other.z - this.z;
        return Math.sqrt(dx * dx + dy * dy + dz * dz);
      }
      distanceSquared(other) {
        const dx = other.x - this.x;
        const dy = other.y - this.y;
        const dz = other.z - this.z;
        return dx * dx + dy * dy + dz * dz;
      }
      equals(other, error = 0) {
        return Math.abs(this.x - other.x) <= error && Math.abs(this.y - other.y) <= error && Math.abs(this.z - other.z) <= error;
      }
      toString() {
        return "(" + this.x + ", " + this.y + ", " + this.z + ")";
      }
      clone() {
        return this.offset(0, 0, 0);
      }
      min(other) {
        return new _Vec3(Math.min(this.x, other.x), Math.min(this.y, other.y), Math.min(this.z, other.z));
      }
      max(other) {
        return new _Vec3(Math.max(this.x, other.x), Math.max(this.y, other.y), Math.max(this.z, other.z));
      }
      norm() {
        return Math.sqrt(this.x * this.x + this.y * this.y + this.z * this.z);
      }
      dot(other) {
        return this.x * other.x + this.y * other.y + this.z * other.z;
      }
      cross(other) {
        return new _Vec3(this.y * other.z - this.z * other.y, this.z * other.x - this.x * other.z, this.x * other.y - this.y * other.x);
      }
      unit() {
        const norm = this.norm();
        if (norm === 0) {
          return this.clone();
        } else {
          return this.scaled(1 / norm);
        }
      }
      normalize() {
        const norm = this.norm();
        if (norm !== 0) {
          this.x /= norm;
          this.y /= norm;
          this.z /= norm;
        }
        return this;
      }
      scale(scalar) {
        this.x *= scalar;
        this.y *= scalar;
        this.z *= scalar;
        return this;
      }
      xyDistanceTo(other) {
        const dx = other.x - this.x;
        const dy = other.y - this.y;
        return Math.sqrt(dx * dx + dy * dy);
      }
      xzDistanceTo(other) {
        const dx = other.x - this.x;
        const dz = other.z - this.z;
        return Math.sqrt(dx * dx + dz * dz);
      }
      yzDistanceTo(other) {
        const dy = other.y - this.y;
        const dz = other.z - this.z;
        return Math.sqrt(dy * dy + dz * dz);
      }
      innerProduct(other) {
        return this.x * other.x + this.y * other.y + this.z * other.z;
      }
      manhattanDistanceTo(other) {
        return Math.abs(other.x - this.x) + Math.abs(other.y - this.y) + Math.abs(other.z - this.z);
      }
      toArray() {
        return [this.x, this.y, this.z];
      }
    };
    function v(x, y, z) {
      if (x == null) {
        return new Vec32(0, 0, 0);
      } else if (Array.isArray(x)) {
        return new Vec32(parseFloat(x[0]), parseFloat(x[1]), parseFloat(x[2]));
      } else if (typeof x === "object") {
        return new Vec32(parseFloat(x.x), parseFloat(x.y), parseFloat(x.z));
      } else if (typeof x === "string" && y == null) {
        const match = x.match(re);
        if (match) {
          return new Vec32(
            parseFloat(match[1]),
            parseFloat(match[2]),
            parseFloat(match[3])
          );
        } else {
          throw new Error("vec3: cannot parse: " + x);
        }
      } else {
        return new Vec32(parseFloat(x), parseFloat(y), parseFloat(z));
      }
    }
    function euclideanMod(numerator, denominator) {
      const result = numerator % denominator;
      return result < 0 ? result + denominator : result;
    }
    module2.exports = v;
    v.Vec3 = Vec32;
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/utils.js
var require_utils = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/utils.js"(exports2, module2) {
    function getField(countField, context) {
      const countFieldArr = countField.split("/");
      let i = 0;
      if (countFieldArr[i] === "") {
        while (context[".."] !== void 0) {
          context = context[".."];
        }
        i++;
      }
      for (; i < countFieldArr.length; i++) {
        context = context[countFieldArr[i]];
      }
      return context;
    }
    function getFieldInfo(fieldInfo) {
      if (typeof fieldInfo === "string") {
        return { type: fieldInfo };
      } else if (Array.isArray(fieldInfo)) {
        return { type: fieldInfo[0], typeArgs: fieldInfo[1] };
      } else if (typeof fieldInfo.type === "string") {
        return fieldInfo;
      } else {
        throw new Error("Not a fieldinfo");
      }
    }
    function getCount(buffer, offset, { count, countType }, rootNode) {
      let c = 0;
      let size = 0;
      if (typeof count === "number") {
        c = count;
      } else if (typeof count !== "undefined") {
        c = getField(count, rootNode);
      } else if (typeof countType !== "undefined") {
        ({ size, value: c } = tryDoc(() => this.read(buffer, offset, getFieldInfo(countType), rootNode), "$count"));
      } else {
        c = 0;
      }
      return { count: c, size };
    }
    function sendCount(len, buffer, offset, { count, countType }, rootNode) {
      if (typeof count !== "undefined" && len !== count) {
      } else if (typeof countType !== "undefined") {
        offset = this.write(len, buffer, offset, getFieldInfo(countType), rootNode);
      } else {
      }
      return offset;
    }
    function calcCount(len, { count, countType }, rootNode) {
      if (typeof count === "undefined" && typeof countType !== "undefined") {
        return tryDoc(() => this.sizeOf(len, getFieldInfo(countType), rootNode), "$count");
      } else {
        return 0;
      }
    }
    function addErrorField(e, field) {
      e.field = e.field ? field + "." + e.field : field;
      throw e;
    }
    function tryCatch2(tryfn, catchfn) {
      try {
        return tryfn();
      } catch (e) {
        catchfn(e);
      }
    }
    function tryDoc(tryfn, field) {
      return tryCatch2(tryfn, (e) => addErrorField(e, field));
    }
    var ExtendableError = class extends Error {
      constructor(message) {
        super(message);
        this.name = this.constructor.name;
        this.message = message;
        if (Error.captureStackTrace != null) {
          Error.captureStackTrace(this, this.constructor.name);
        }
      }
    };
    var PartialReadError2 = class extends ExtendableError {
      constructor(message) {
        super(message);
        this.partialReadError = true;
      }
    };
    module2.exports = {
      getField,
      getFieldInfo,
      addErrorField,
      getCount,
      sendCount,
      calcCount,
      tryCatch: tryCatch2,
      tryDoc,
      PartialReadError: PartialReadError2
    };
  }
});

// ../node_modules/.pnpm/lodash.reduce@4.6.0/node_modules/lodash.reduce/index.js
var require_lodash = __commonJS({
  "../node_modules/.pnpm/lodash.reduce@4.6.0/node_modules/lodash.reduce/index.js"(exports2, module2) {
    var LARGE_ARRAY_SIZE = 200;
    var FUNC_ERROR_TEXT = "Expected a function";
    var HASH_UNDEFINED = "__lodash_hash_undefined__";
    var UNORDERED_COMPARE_FLAG = 1;
    var PARTIAL_COMPARE_FLAG = 2;
    var INFINITY = 1 / 0;
    var MAX_SAFE_INTEGER = 9007199254740991;
    var argsTag = "[object Arguments]";
    var arrayTag = "[object Array]";
    var boolTag = "[object Boolean]";
    var dateTag = "[object Date]";
    var errorTag = "[object Error]";
    var funcTag = "[object Function]";
    var genTag = "[object GeneratorFunction]";
    var mapTag = "[object Map]";
    var numberTag = "[object Number]";
    var objectTag = "[object Object]";
    var promiseTag = "[object Promise]";
    var regexpTag = "[object RegExp]";
    var setTag = "[object Set]";
    var stringTag = "[object String]";
    var symbolTag = "[object Symbol]";
    var weakMapTag = "[object WeakMap]";
    var arrayBufferTag = "[object ArrayBuffer]";
    var dataViewTag = "[object DataView]";
    var float32Tag = "[object Float32Array]";
    var float64Tag = "[object Float64Array]";
    var int8Tag = "[object Int8Array]";
    var int16Tag = "[object Int16Array]";
    var int32Tag = "[object Int32Array]";
    var uint8Tag = "[object Uint8Array]";
    var uint8ClampedTag = "[object Uint8ClampedArray]";
    var uint16Tag = "[object Uint16Array]";
    var uint32Tag = "[object Uint32Array]";
    var reIsDeepProp = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/;
    var reIsPlainProp = /^\w*$/;
    var reLeadingDot = /^\./;
    var rePropName = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g;
    var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;
    var reEscapeChar = /\\(\\)?/g;
    var reIsHostCtor = /^\[object .+?Constructor\]$/;
    var reIsUint = /^(?:0|[1-9]\d*)$/;
    var typedArrayTags = {};
    typedArrayTags[float32Tag] = typedArrayTags[float64Tag] = typedArrayTags[int8Tag] = typedArrayTags[int16Tag] = typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] = typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] = typedArrayTags[uint32Tag] = true;
    typedArrayTags[argsTag] = typedArrayTags[arrayTag] = typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] = typedArrayTags[dataViewTag] = typedArrayTags[dateTag] = typedArrayTags[errorTag] = typedArrayTags[funcTag] = typedArrayTags[mapTag] = typedArrayTags[numberTag] = typedArrayTags[objectTag] = typedArrayTags[regexpTag] = typedArrayTags[setTag] = typedArrayTags[stringTag] = typedArrayTags[weakMapTag] = false;
    var freeGlobal = typeof global == "object" && global && global.Object === Object && global;
    var freeSelf = typeof self == "object" && self && self.Object === Object && self;
    var root = freeGlobal || freeSelf || Function("return this")();
    var freeExports = typeof exports2 == "object" && exports2 && !exports2.nodeType && exports2;
    var freeModule = freeExports && typeof module2 == "object" && module2 && !module2.nodeType && module2;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var freeProcess = moduleExports && freeGlobal.process;
    var nodeUtil = function() {
      try {
        return freeProcess && freeProcess.binding("util");
      } catch (e) {
      }
    }();
    var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;
    function arrayReduce(array, iteratee, accumulator, initAccum) {
      var index = -1, length = array ? array.length : 0;
      if (initAccum && length) {
        accumulator = array[++index];
      }
      while (++index < length) {
        accumulator = iteratee(accumulator, array[index], index, array);
      }
      return accumulator;
    }
    function arraySome(array, predicate) {
      var index = -1, length = array ? array.length : 0;
      while (++index < length) {
        if (predicate(array[index], index, array)) {
          return true;
        }
      }
      return false;
    }
    function baseProperty(key) {
      return function(object) {
        return object == null ? void 0 : object[key];
      };
    }
    function baseReduce(collection, iteratee, accumulator, initAccum, eachFunc) {
      eachFunc(collection, function(value, index, collection2) {
        accumulator = initAccum ? (initAccum = false, value) : iteratee(accumulator, value, index, collection2);
      });
      return accumulator;
    }
    function baseTimes(n, iteratee) {
      var index = -1, result = Array(n);
      while (++index < n) {
        result[index] = iteratee(index);
      }
      return result;
    }
    function baseUnary(func) {
      return function(value) {
        return func(value);
      };
    }
    function getValue(object, key) {
      return object == null ? void 0 : object[key];
    }
    function isHostObject(value) {
      var result = false;
      if (value != null && typeof value.toString != "function") {
        try {
          result = !!(value + "");
        } catch (e) {
        }
      }
      return result;
    }
    function mapToArray(map) {
      var index = -1, result = Array(map.size);
      map.forEach(function(value, key) {
        result[++index] = [key, value];
      });
      return result;
    }
    function overArg(func, transform) {
      return function(arg) {
        return func(transform(arg));
      };
    }
    function setToArray(set) {
      var index = -1, result = Array(set.size);
      set.forEach(function(value) {
        result[++index] = value;
      });
      return result;
    }
    var arrayProto = Array.prototype;
    var funcProto = Function.prototype;
    var objectProto = Object.prototype;
    var coreJsData = root["__core-js_shared__"];
    var maskSrcKey = function() {
      var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || "");
      return uid ? "Symbol(src)_1." + uid : "";
    }();
    var funcToString = funcProto.toString;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var objectToString = objectProto.toString;
    var reIsNative = RegExp(
      "^" + funcToString.call(hasOwnProperty).replace(reRegExpChar, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
    );
    var Symbol2 = root.Symbol;
    var Uint8Array2 = root.Uint8Array;
    var propertyIsEnumerable = objectProto.propertyIsEnumerable;
    var splice = arrayProto.splice;
    var nativeKeys = overArg(Object.keys, Object);
    var DataView2 = getNative(root, "DataView");
    var Map2 = getNative(root, "Map");
    var Promise2 = getNative(root, "Promise");
    var Set2 = getNative(root, "Set");
    var WeakMap2 = getNative(root, "WeakMap");
    var nativeCreate = getNative(Object, "create");
    var dataViewCtorString = toSource(DataView2);
    var mapCtorString = toSource(Map2);
    var promiseCtorString = toSource(Promise2);
    var setCtorString = toSource(Set2);
    var weakMapCtorString = toSource(WeakMap2);
    var symbolProto = Symbol2 ? Symbol2.prototype : void 0;
    var symbolValueOf = symbolProto ? symbolProto.valueOf : void 0;
    var symbolToString = symbolProto ? symbolProto.toString : void 0;
    function Hash(entries) {
      var index = -1, length = entries ? entries.length : 0;
      this.clear();
      while (++index < length) {
        var entry = entries[index];
        this.set(entry[0], entry[1]);
      }
    }
    function hashClear() {
      this.__data__ = nativeCreate ? nativeCreate(null) : {};
    }
    function hashDelete(key) {
      return this.has(key) && delete this.__data__[key];
    }
    function hashGet(key) {
      var data = this.__data__;
      if (nativeCreate) {
        var result = data[key];
        return result === HASH_UNDEFINED ? void 0 : result;
      }
      return hasOwnProperty.call(data, key) ? data[key] : void 0;
    }
    function hashHas(key) {
      var data = this.__data__;
      return nativeCreate ? data[key] !== void 0 : hasOwnProperty.call(data, key);
    }
    function hashSet(key, value) {
      var data = this.__data__;
      data[key] = nativeCreate && value === void 0 ? HASH_UNDEFINED : value;
      return this;
    }
    Hash.prototype.clear = hashClear;
    Hash.prototype["delete"] = hashDelete;
    Hash.prototype.get = hashGet;
    Hash.prototype.has = hashHas;
    Hash.prototype.set = hashSet;
    function ListCache(entries) {
      var index = -1, length = entries ? entries.length : 0;
      this.clear();
      while (++index < length) {
        var entry = entries[index];
        this.set(entry[0], entry[1]);
      }
    }
    function listCacheClear() {
      this.__data__ = [];
    }
    function listCacheDelete(key) {
      var data = this.__data__, index = assocIndexOf(data, key);
      if (index < 0) {
        return false;
      }
      var lastIndex = data.length - 1;
      if (index == lastIndex) {
        data.pop();
      } else {
        splice.call(data, index, 1);
      }
      return true;
    }
    function listCacheGet(key) {
      var data = this.__data__, index = assocIndexOf(data, key);
      return index < 0 ? void 0 : data[index][1];
    }
    function listCacheHas(key) {
      return assocIndexOf(this.__data__, key) > -1;
    }
    function listCacheSet(key, value) {
      var data = this.__data__, index = assocIndexOf(data, key);
      if (index < 0) {
        data.push([key, value]);
      } else {
        data[index][1] = value;
      }
      return this;
    }
    ListCache.prototype.clear = listCacheClear;
    ListCache.prototype["delete"] = listCacheDelete;
    ListCache.prototype.get = listCacheGet;
    ListCache.prototype.has = listCacheHas;
    ListCache.prototype.set = listCacheSet;
    function MapCache(entries) {
      var index = -1, length = entries ? entries.length : 0;
      this.clear();
      while (++index < length) {
        var entry = entries[index];
        this.set(entry[0], entry[1]);
      }
    }
    function mapCacheClear() {
      this.__data__ = {
        "hash": new Hash(),
        "map": new (Map2 || ListCache)(),
        "string": new Hash()
      };
    }
    function mapCacheDelete(key) {
      return getMapData(this, key)["delete"](key);
    }
    function mapCacheGet(key) {
      return getMapData(this, key).get(key);
    }
    function mapCacheHas(key) {
      return getMapData(this, key).has(key);
    }
    function mapCacheSet(key, value) {
      getMapData(this, key).set(key, value);
      return this;
    }
    MapCache.prototype.clear = mapCacheClear;
    MapCache.prototype["delete"] = mapCacheDelete;
    MapCache.prototype.get = mapCacheGet;
    MapCache.prototype.has = mapCacheHas;
    MapCache.prototype.set = mapCacheSet;
    function SetCache(values) {
      var index = -1, length = values ? values.length : 0;
      this.__data__ = new MapCache();
      while (++index < length) {
        this.add(values[index]);
      }
    }
    function setCacheAdd(value) {
      this.__data__.set(value, HASH_UNDEFINED);
      return this;
    }
    function setCacheHas(value) {
      return this.__data__.has(value);
    }
    SetCache.prototype.add = SetCache.prototype.push = setCacheAdd;
    SetCache.prototype.has = setCacheHas;
    function Stack(entries) {
      this.__data__ = new ListCache(entries);
    }
    function stackClear() {
      this.__data__ = new ListCache();
    }
    function stackDelete(key) {
      return this.__data__["delete"](key);
    }
    function stackGet(key) {
      return this.__data__.get(key);
    }
    function stackHas(key) {
      return this.__data__.has(key);
    }
    function stackSet(key, value) {
      var cache = this.__data__;
      if (cache instanceof ListCache) {
        var pairs = cache.__data__;
        if (!Map2 || pairs.length < LARGE_ARRAY_SIZE - 1) {
          pairs.push([key, value]);
          return this;
        }
        cache = this.__data__ = new MapCache(pairs);
      }
      cache.set(key, value);
      return this;
    }
    Stack.prototype.clear = stackClear;
    Stack.prototype["delete"] = stackDelete;
    Stack.prototype.get = stackGet;
    Stack.prototype.has = stackHas;
    Stack.prototype.set = stackSet;
    function arrayLikeKeys(value, inherited) {
      var result = isArray(value) || isArguments(value) ? baseTimes(value.length, String) : [];
      var length = result.length, skipIndexes = !!length;
      for (var key in value) {
        if ((inherited || hasOwnProperty.call(value, key)) && !(skipIndexes && (key == "length" || isIndex(key, length)))) {
          result.push(key);
        }
      }
      return result;
    }
    function assocIndexOf(array, key) {
      var length = array.length;
      while (length--) {
        if (eq(array[length][0], key)) {
          return length;
        }
      }
      return -1;
    }
    var baseEach = createBaseEach(baseForOwn);
    var baseFor = createBaseFor();
    function baseForOwn(object, iteratee) {
      return object && baseFor(object, iteratee, keys);
    }
    function baseGet(object, path) {
      path = isKey(path, object) ? [path] : castPath(path);
      var index = 0, length = path.length;
      while (object != null && index < length) {
        object = object[toKey(path[index++])];
      }
      return index && index == length ? object : void 0;
    }
    function baseGetTag(value) {
      return objectToString.call(value);
    }
    function baseHasIn(object, key) {
      return object != null && key in Object(object);
    }
    function baseIsEqual(value, other, customizer, bitmask, stack) {
      if (value === other) {
        return true;
      }
      if (value == null || other == null || !isObject(value) && !isObjectLike(other)) {
        return value !== value && other !== other;
      }
      return baseIsEqualDeep(value, other, baseIsEqual, customizer, bitmask, stack);
    }
    function baseIsEqualDeep(object, other, equalFunc, customizer, bitmask, stack) {
      var objIsArr = isArray(object), othIsArr = isArray(other), objTag = arrayTag, othTag = arrayTag;
      if (!objIsArr) {
        objTag = getTag(object);
        objTag = objTag == argsTag ? objectTag : objTag;
      }
      if (!othIsArr) {
        othTag = getTag(other);
        othTag = othTag == argsTag ? objectTag : othTag;
      }
      var objIsObj = objTag == objectTag && !isHostObject(object), othIsObj = othTag == objectTag && !isHostObject(other), isSameTag = objTag == othTag;
      if (isSameTag && !objIsObj) {
        stack || (stack = new Stack());
        return objIsArr || isTypedArray(object) ? equalArrays(object, other, equalFunc, customizer, bitmask, stack) : equalByTag(object, other, objTag, equalFunc, customizer, bitmask, stack);
      }
      if (!(bitmask & PARTIAL_COMPARE_FLAG)) {
        var objIsWrapped = objIsObj && hasOwnProperty.call(object, "__wrapped__"), othIsWrapped = othIsObj && hasOwnProperty.call(other, "__wrapped__");
        if (objIsWrapped || othIsWrapped) {
          var objUnwrapped = objIsWrapped ? object.value() : object, othUnwrapped = othIsWrapped ? other.value() : other;
          stack || (stack = new Stack());
          return equalFunc(objUnwrapped, othUnwrapped, customizer, bitmask, stack);
        }
      }
      if (!isSameTag) {
        return false;
      }
      stack || (stack = new Stack());
      return equalObjects(object, other, equalFunc, customizer, bitmask, stack);
    }
    function baseIsMatch(object, source, matchData, customizer) {
      var index = matchData.length, length = index, noCustomizer = !customizer;
      if (object == null) {
        return !length;
      }
      object = Object(object);
      while (index--) {
        var data = matchData[index];
        if (noCustomizer && data[2] ? data[1] !== object[data[0]] : !(data[0] in object)) {
          return false;
        }
      }
      while (++index < length) {
        data = matchData[index];
        var key = data[0], objValue = object[key], srcValue = data[1];
        if (noCustomizer && data[2]) {
          if (objValue === void 0 && !(key in object)) {
            return false;
          }
        } else {
          var stack = new Stack();
          if (customizer) {
            var result = customizer(objValue, srcValue, key, object, source, stack);
          }
          if (!(result === void 0 ? baseIsEqual(srcValue, objValue, customizer, UNORDERED_COMPARE_FLAG | PARTIAL_COMPARE_FLAG, stack) : result)) {
            return false;
          }
        }
      }
      return true;
    }
    function baseIsNative(value) {
      if (!isObject(value) || isMasked(value)) {
        return false;
      }
      var pattern = isFunction(value) || isHostObject(value) ? reIsNative : reIsHostCtor;
      return pattern.test(toSource(value));
    }
    function baseIsTypedArray(value) {
      return isObjectLike(value) && isLength(value.length) && !!typedArrayTags[objectToString.call(value)];
    }
    function baseIteratee(value) {
      if (typeof value == "function") {
        return value;
      }
      if (value == null) {
        return identity;
      }
      if (typeof value == "object") {
        return isArray(value) ? baseMatchesProperty(value[0], value[1]) : baseMatches(value);
      }
      return property(value);
    }
    function baseKeys(object) {
      if (!isPrototype(object)) {
        return nativeKeys(object);
      }
      var result = [];
      for (var key in Object(object)) {
        if (hasOwnProperty.call(object, key) && key != "constructor") {
          result.push(key);
        }
      }
      return result;
    }
    function baseMatches(source) {
      var matchData = getMatchData(source);
      if (matchData.length == 1 && matchData[0][2]) {
        return matchesStrictComparable(matchData[0][0], matchData[0][1]);
      }
      return function(object) {
        return object === source || baseIsMatch(object, source, matchData);
      };
    }
    function baseMatchesProperty(path, srcValue) {
      if (isKey(path) && isStrictComparable(srcValue)) {
        return matchesStrictComparable(toKey(path), srcValue);
      }
      return function(object) {
        var objValue = get(object, path);
        return objValue === void 0 && objValue === srcValue ? hasIn(object, path) : baseIsEqual(srcValue, objValue, void 0, UNORDERED_COMPARE_FLAG | PARTIAL_COMPARE_FLAG);
      };
    }
    function basePropertyDeep(path) {
      return function(object) {
        return baseGet(object, path);
      };
    }
    function baseToString(value) {
      if (typeof value == "string") {
        return value;
      }
      if (isSymbol(value)) {
        return symbolToString ? symbolToString.call(value) : "";
      }
      var result = value + "";
      return result == "0" && 1 / value == -INFINITY ? "-0" : result;
    }
    function castPath(value) {
      return isArray(value) ? value : stringToPath(value);
    }
    function createBaseEach(eachFunc, fromRight) {
      return function(collection, iteratee) {
        if (collection == null) {
          return collection;
        }
        if (!isArrayLike(collection)) {
          return eachFunc(collection, iteratee);
        }
        var length = collection.length, index = fromRight ? length : -1, iterable = Object(collection);
        while (fromRight ? index-- : ++index < length) {
          if (iteratee(iterable[index], index, iterable) === false) {
            break;
          }
        }
        return collection;
      };
    }
    function createBaseFor(fromRight) {
      return function(object, iteratee, keysFunc) {
        var index = -1, iterable = Object(object), props = keysFunc(object), length = props.length;
        while (length--) {
          var key = props[fromRight ? length : ++index];
          if (iteratee(iterable[key], key, iterable) === false) {
            break;
          }
        }
        return object;
      };
    }
    function equalArrays(array, other, equalFunc, customizer, bitmask, stack) {
      var isPartial = bitmask & PARTIAL_COMPARE_FLAG, arrLength = array.length, othLength = other.length;
      if (arrLength != othLength && !(isPartial && othLength > arrLength)) {
        return false;
      }
      var stacked = stack.get(array);
      if (stacked && stack.get(other)) {
        return stacked == other;
      }
      var index = -1, result = true, seen = bitmask & UNORDERED_COMPARE_FLAG ? new SetCache() : void 0;
      stack.set(array, other);
      stack.set(other, array);
      while (++index < arrLength) {
        var arrValue = array[index], othValue = other[index];
        if (customizer) {
          var compared = isPartial ? customizer(othValue, arrValue, index, other, array, stack) : customizer(arrValue, othValue, index, array, other, stack);
        }
        if (compared !== void 0) {
          if (compared) {
            continue;
          }
          result = false;
          break;
        }
        if (seen) {
          if (!arraySome(other, function(othValue2, othIndex) {
            if (!seen.has(othIndex) && (arrValue === othValue2 || equalFunc(arrValue, othValue2, customizer, bitmask, stack))) {
              return seen.add(othIndex);
            }
          })) {
            result = false;
            break;
          }
        } else if (!(arrValue === othValue || equalFunc(arrValue, othValue, customizer, bitmask, stack))) {
          result = false;
          break;
        }
      }
      stack["delete"](array);
      stack["delete"](other);
      return result;
    }
    function equalByTag(object, other, tag, equalFunc, customizer, bitmask, stack) {
      switch (tag) {
        case dataViewTag:
          if (object.byteLength != other.byteLength || object.byteOffset != other.byteOffset) {
            return false;
          }
          object = object.buffer;
          other = other.buffer;
        case arrayBufferTag:
          if (object.byteLength != other.byteLength || !equalFunc(new Uint8Array2(object), new Uint8Array2(other))) {
            return false;
          }
          return true;
        case boolTag:
        case dateTag:
        case numberTag:
          return eq(+object, +other);
        case errorTag:
          return object.name == other.name && object.message == other.message;
        case regexpTag:
        case stringTag:
          return object == other + "";
        case mapTag:
          var convert = mapToArray;
        case setTag:
          var isPartial = bitmask & PARTIAL_COMPARE_FLAG;
          convert || (convert = setToArray);
          if (object.size != other.size && !isPartial) {
            return false;
          }
          var stacked = stack.get(object);
          if (stacked) {
            return stacked == other;
          }
          bitmask |= UNORDERED_COMPARE_FLAG;
          stack.set(object, other);
          var result = equalArrays(convert(object), convert(other), equalFunc, customizer, bitmask, stack);
          stack["delete"](object);
          return result;
        case symbolTag:
          if (symbolValueOf) {
            return symbolValueOf.call(object) == symbolValueOf.call(other);
          }
      }
      return false;
    }
    function equalObjects(object, other, equalFunc, customizer, bitmask, stack) {
      var isPartial = bitmask & PARTIAL_COMPARE_FLAG, objProps = keys(object), objLength = objProps.length, othProps = keys(other), othLength = othProps.length;
      if (objLength != othLength && !isPartial) {
        return false;
      }
      var index = objLength;
      while (index--) {
        var key = objProps[index];
        if (!(isPartial ? key in other : hasOwnProperty.call(other, key))) {
          return false;
        }
      }
      var stacked = stack.get(object);
      if (stacked && stack.get(other)) {
        return stacked == other;
      }
      var result = true;
      stack.set(object, other);
      stack.set(other, object);
      var skipCtor = isPartial;
      while (++index < objLength) {
        key = objProps[index];
        var objValue = object[key], othValue = other[key];
        if (customizer) {
          var compared = isPartial ? customizer(othValue, objValue, key, other, object, stack) : customizer(objValue, othValue, key, object, other, stack);
        }
        if (!(compared === void 0 ? objValue === othValue || equalFunc(objValue, othValue, customizer, bitmask, stack) : compared)) {
          result = false;
          break;
        }
        skipCtor || (skipCtor = key == "constructor");
      }
      if (result && !skipCtor) {
        var objCtor = object.constructor, othCtor = other.constructor;
        if (objCtor != othCtor && ("constructor" in object && "constructor" in other) && !(typeof objCtor == "function" && objCtor instanceof objCtor && typeof othCtor == "function" && othCtor instanceof othCtor)) {
          result = false;
        }
      }
      stack["delete"](object);
      stack["delete"](other);
      return result;
    }
    function getMapData(map, key) {
      var data = map.__data__;
      return isKeyable(key) ? data[typeof key == "string" ? "string" : "hash"] : data.map;
    }
    function getMatchData(object) {
      var result = keys(object), length = result.length;
      while (length--) {
        var key = result[length], value = object[key];
        result[length] = [key, value, isStrictComparable(value)];
      }
      return result;
    }
    function getNative(object, key) {
      var value = getValue(object, key);
      return baseIsNative(value) ? value : void 0;
    }
    var getTag = baseGetTag;
    if (DataView2 && getTag(new DataView2(new ArrayBuffer(1))) != dataViewTag || Map2 && getTag(new Map2()) != mapTag || Promise2 && getTag(Promise2.resolve()) != promiseTag || Set2 && getTag(new Set2()) != setTag || WeakMap2 && getTag(new WeakMap2()) != weakMapTag) {
      getTag = function(value) {
        var result = objectToString.call(value), Ctor = result == objectTag ? value.constructor : void 0, ctorString = Ctor ? toSource(Ctor) : void 0;
        if (ctorString) {
          switch (ctorString) {
            case dataViewCtorString:
              return dataViewTag;
            case mapCtorString:
              return mapTag;
            case promiseCtorString:
              return promiseTag;
            case setCtorString:
              return setTag;
            case weakMapCtorString:
              return weakMapTag;
          }
        }
        return result;
      };
    }
    function hasPath(object, path, hasFunc) {
      path = isKey(path, object) ? [path] : castPath(path);
      var result, index = -1, length = path.length;
      while (++index < length) {
        var key = toKey(path[index]);
        if (!(result = object != null && hasFunc(object, key))) {
          break;
        }
        object = object[key];
      }
      if (result) {
        return result;
      }
      var length = object ? object.length : 0;
      return !!length && isLength(length) && isIndex(key, length) && (isArray(object) || isArguments(object));
    }
    function isIndex(value, length) {
      length = length == null ? MAX_SAFE_INTEGER : length;
      return !!length && (typeof value == "number" || reIsUint.test(value)) && (value > -1 && value % 1 == 0 && value < length);
    }
    function isKey(value, object) {
      if (isArray(value)) {
        return false;
      }
      var type = typeof value;
      if (type == "number" || type == "symbol" || type == "boolean" || value == null || isSymbol(value)) {
        return true;
      }
      return reIsPlainProp.test(value) || !reIsDeepProp.test(value) || object != null && value in Object(object);
    }
    function isKeyable(value) {
      var type = typeof value;
      return type == "string" || type == "number" || type == "symbol" || type == "boolean" ? value !== "__proto__" : value === null;
    }
    function isMasked(func) {
      return !!maskSrcKey && maskSrcKey in func;
    }
    function isPrototype(value) {
      var Ctor = value && value.constructor, proto = typeof Ctor == "function" && Ctor.prototype || objectProto;
      return value === proto;
    }
    function isStrictComparable(value) {
      return value === value && !isObject(value);
    }
    function matchesStrictComparable(key, srcValue) {
      return function(object) {
        if (object == null) {
          return false;
        }
        return object[key] === srcValue && (srcValue !== void 0 || key in Object(object));
      };
    }
    var stringToPath = memoize(function(string) {
      string = toString(string);
      var result = [];
      if (reLeadingDot.test(string)) {
        result.push("");
      }
      string.replace(rePropName, function(match, number, quote, string2) {
        result.push(quote ? string2.replace(reEscapeChar, "$1") : number || match);
      });
      return result;
    });
    function toKey(value) {
      if (typeof value == "string" || isSymbol(value)) {
        return value;
      }
      var result = value + "";
      return result == "0" && 1 / value == -INFINITY ? "-0" : result;
    }
    function toSource(func) {
      if (func != null) {
        try {
          return funcToString.call(func);
        } catch (e) {
        }
        try {
          return func + "";
        } catch (e) {
        }
      }
      return "";
    }
    function reduce(collection, iteratee, accumulator) {
      var func = isArray(collection) ? arrayReduce : baseReduce, initAccum = arguments.length < 3;
      return func(collection, baseIteratee(iteratee, 4), accumulator, initAccum, baseEach);
    }
    function memoize(func, resolver) {
      if (typeof func != "function" || resolver && typeof resolver != "function") {
        throw new TypeError(FUNC_ERROR_TEXT);
      }
      var memoized = function() {
        var args = arguments, key = resolver ? resolver.apply(this, args) : args[0], cache = memoized.cache;
        if (cache.has(key)) {
          return cache.get(key);
        }
        var result = func.apply(this, args);
        memoized.cache = cache.set(key, result);
        return result;
      };
      memoized.cache = new (memoize.Cache || MapCache)();
      return memoized;
    }
    memoize.Cache = MapCache;
    function eq(value, other) {
      return value === other || value !== value && other !== other;
    }
    function isArguments(value) {
      return isArrayLikeObject(value) && hasOwnProperty.call(value, "callee") && (!propertyIsEnumerable.call(value, "callee") || objectToString.call(value) == argsTag);
    }
    var isArray = Array.isArray;
    function isArrayLike(value) {
      return value != null && isLength(value.length) && !isFunction(value);
    }
    function isArrayLikeObject(value) {
      return isObjectLike(value) && isArrayLike(value);
    }
    function isFunction(value) {
      var tag = isObject(value) ? objectToString.call(value) : "";
      return tag == funcTag || tag == genTag;
    }
    function isLength(value) {
      return typeof value == "number" && value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
    }
    function isObject(value) {
      var type = typeof value;
      return !!value && (type == "object" || type == "function");
    }
    function isObjectLike(value) {
      return !!value && typeof value == "object";
    }
    function isSymbol(value) {
      return typeof value == "symbol" || isObjectLike(value) && objectToString.call(value) == symbolTag;
    }
    var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;
    function toString(value) {
      return value == null ? "" : baseToString(value);
    }
    function get(object, path, defaultValue) {
      var result = object == null ? void 0 : baseGet(object, path);
      return result === void 0 ? defaultValue : result;
    }
    function hasIn(object, path) {
      return object != null && hasPath(object, path, baseHasIn);
    }
    function keys(object) {
      return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
    }
    function identity(value) {
      return value;
    }
    function property(path) {
      return isKey(path) ? baseProperty(toKey(path)) : basePropertyDeep(path);
    }
    module2.exports = reduce;
  }
});

// ../node_modules/.pnpm/uri-js@4.4.1/node_modules/uri-js/dist/es5/uri.all.js
var require_uri_all = __commonJS({
  "../node_modules/.pnpm/uri-js@4.4.1/node_modules/uri-js/dist/es5/uri.all.js"(exports2, module2) {
    (function(global2, factory) {
      typeof exports2 === "object" && typeof module2 !== "undefined" ? factory(exports2) : typeof define === "function" && define.amd ? define(["exports"], factory) : factory(global2.URI = global2.URI || {});
    })(exports2, function(exports3) {
      "use strict";
      function merge() {
        for (var _len = arguments.length, sets = Array(_len), _key = 0; _key < _len; _key++) {
          sets[_key] = arguments[_key];
        }
        if (sets.length > 1) {
          sets[0] = sets[0].slice(0, -1);
          var xl = sets.length - 1;
          for (var x = 1; x < xl; ++x) {
            sets[x] = sets[x].slice(1, -1);
          }
          sets[xl] = sets[xl].slice(1);
          return sets.join("");
        } else {
          return sets[0];
        }
      }
      function subexp(str) {
        return "(?:" + str + ")";
      }
      function typeOf(o) {
        return o === void 0 ? "undefined" : o === null ? "null" : Object.prototype.toString.call(o).split(" ").pop().split("]").shift().toLowerCase();
      }
      function toUpperCase(str) {
        return str.toUpperCase();
      }
      function toArray(obj) {
        return obj !== void 0 && obj !== null ? obj instanceof Array ? obj : typeof obj.length !== "number" || obj.split || obj.setInterval || obj.call ? [obj] : Array.prototype.slice.call(obj) : [];
      }
      function assign(target, source) {
        var obj = target;
        if (source) {
          for (var key in source) {
            obj[key] = source[key];
          }
        }
        return obj;
      }
      function buildExps(isIRI2) {
        var ALPHA$$ = "[A-Za-z]", CR$ = "[\\x0D]", DIGIT$$ = "[0-9]", DQUOTE$$ = "[\\x22]", HEXDIG$$2 = merge(DIGIT$$, "[A-Fa-f]"), LF$$ = "[\\x0A]", SP$$ = "[\\x20]", PCT_ENCODED$2 = subexp(subexp("%[EFef]" + HEXDIG$$2 + "%" + HEXDIG$$2 + HEXDIG$$2 + "%" + HEXDIG$$2 + HEXDIG$$2) + "|" + subexp("%[89A-Fa-f]" + HEXDIG$$2 + "%" + HEXDIG$$2 + HEXDIG$$2) + "|" + subexp("%" + HEXDIG$$2 + HEXDIG$$2)), GEN_DELIMS$$ = "[\\:\\/\\?\\#\\[\\]\\@]", SUB_DELIMS$$ = "[\\!\\$\\&\\'\\(\\)\\*\\+\\,\\;\\=]", RESERVED$$ = merge(GEN_DELIMS$$, SUB_DELIMS$$), UCSCHAR$$ = isIRI2 ? "[\\xA0-\\u200D\\u2010-\\u2029\\u202F-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFEF]" : "[]", IPRIVATE$$ = isIRI2 ? "[\\uE000-\\uF8FF]" : "[]", UNRESERVED$$2 = merge(ALPHA$$, DIGIT$$, "[\\-\\.\\_\\~]", UCSCHAR$$), SCHEME$ = subexp(ALPHA$$ + merge(ALPHA$$, DIGIT$$, "[\\+\\-\\.]") + "*"), USERINFO$ = subexp(subexp(PCT_ENCODED$2 + "|" + merge(UNRESERVED$$2, SUB_DELIMS$$, "[\\:]")) + "*"), DEC_OCTET$ = subexp(subexp("25[0-5]") + "|" + subexp("2[0-4]" + DIGIT$$) + "|" + subexp("1" + DIGIT$$ + DIGIT$$) + "|" + subexp("[1-9]" + DIGIT$$) + "|" + DIGIT$$), DEC_OCTET_RELAXED$ = subexp(subexp("25[0-5]") + "|" + subexp("2[0-4]" + DIGIT$$) + "|" + subexp("1" + DIGIT$$ + DIGIT$$) + "|" + subexp("0?[1-9]" + DIGIT$$) + "|0?0?" + DIGIT$$), IPV4ADDRESS$ = subexp(DEC_OCTET_RELAXED$ + "\\." + DEC_OCTET_RELAXED$ + "\\." + DEC_OCTET_RELAXED$ + "\\." + DEC_OCTET_RELAXED$), H16$ = subexp(HEXDIG$$2 + "{1,4}"), LS32$ = subexp(subexp(H16$ + "\\:" + H16$) + "|" + IPV4ADDRESS$), IPV6ADDRESS1$ = subexp(subexp(H16$ + "\\:") + "{6}" + LS32$), IPV6ADDRESS2$ = subexp("\\:\\:" + subexp(H16$ + "\\:") + "{5}" + LS32$), IPV6ADDRESS3$ = subexp(subexp(H16$) + "?\\:\\:" + subexp(H16$ + "\\:") + "{4}" + LS32$), IPV6ADDRESS4$ = subexp(subexp(subexp(H16$ + "\\:") + "{0,1}" + H16$) + "?\\:\\:" + subexp(H16$ + "\\:") + "{3}" + LS32$), IPV6ADDRESS5$ = subexp(subexp(subexp(H16$ + "\\:") + "{0,2}" + H16$) + "?\\:\\:" + subexp(H16$ + "\\:") + "{2}" + LS32$), IPV6ADDRESS6$ = subexp(subexp(subexp(H16$ + "\\:") + "{0,3}" + H16$) + "?\\:\\:" + H16$ + "\\:" + LS32$), IPV6ADDRESS7$ = subexp(subexp(subexp(H16$ + "\\:") + "{0,4}" + H16$) + "?\\:\\:" + LS32$), IPV6ADDRESS8$ = subexp(subexp(subexp(H16$ + "\\:") + "{0,5}" + H16$) + "?\\:\\:" + H16$), IPV6ADDRESS9$ = subexp(subexp(subexp(H16$ + "\\:") + "{0,6}" + H16$) + "?\\:\\:"), IPV6ADDRESS$ = subexp([IPV6ADDRESS1$, IPV6ADDRESS2$, IPV6ADDRESS3$, IPV6ADDRESS4$, IPV6ADDRESS5$, IPV6ADDRESS6$, IPV6ADDRESS7$, IPV6ADDRESS8$, IPV6ADDRESS9$].join("|")), ZONEID$ = subexp(subexp(UNRESERVED$$2 + "|" + PCT_ENCODED$2) + "+"), IPV6ADDRZ$ = subexp(IPV6ADDRESS$ + "\\%25" + ZONEID$), IPV6ADDRZ_RELAXED$ = subexp(IPV6ADDRESS$ + subexp("\\%25|\\%(?!" + HEXDIG$$2 + "{2})") + ZONEID$), IPVFUTURE$ = subexp("[vV]" + HEXDIG$$2 + "+\\." + merge(UNRESERVED$$2, SUB_DELIMS$$, "[\\:]") + "+"), IP_LITERAL$ = subexp("\\[" + subexp(IPV6ADDRZ_RELAXED$ + "|" + IPV6ADDRESS$ + "|" + IPVFUTURE$) + "\\]"), REG_NAME$ = subexp(subexp(PCT_ENCODED$2 + "|" + merge(UNRESERVED$$2, SUB_DELIMS$$)) + "*"), HOST$ = subexp(IP_LITERAL$ + "|" + IPV4ADDRESS$ + "(?!" + REG_NAME$ + ")|" + REG_NAME$), PORT$ = subexp(DIGIT$$ + "*"), AUTHORITY$ = subexp(subexp(USERINFO$ + "@") + "?" + HOST$ + subexp("\\:" + PORT$) + "?"), PCHAR$ = subexp(PCT_ENCODED$2 + "|" + merge(UNRESERVED$$2, SUB_DELIMS$$, "[\\:\\@]")), SEGMENT$ = subexp(PCHAR$ + "*"), SEGMENT_NZ$ = subexp(PCHAR$ + "+"), SEGMENT_NZ_NC$ = subexp(subexp(PCT_ENCODED$2 + "|" + merge(UNRESERVED$$2, SUB_DELIMS$$, "[\\@]")) + "+"), PATH_ABEMPTY$ = subexp(subexp("\\/" + SEGMENT$) + "*"), PATH_ABSOLUTE$ = subexp("\\/" + subexp(SEGMENT_NZ$ + PATH_ABEMPTY$) + "?"), PATH_NOSCHEME$ = subexp(SEGMENT_NZ_NC$ + PATH_ABEMPTY$), PATH_ROOTLESS$ = subexp(SEGMENT_NZ$ + PATH_ABEMPTY$), PATH_EMPTY$ = "(?!" + PCHAR$ + ")", PATH$ = subexp(PATH_ABEMPTY$ + "|" + PATH_ABSOLUTE$ + "|" + PATH_NOSCHEME$ + "|" + PATH_ROOTLESS$ + "|" + PATH_EMPTY$), QUERY$ = subexp(subexp(PCHAR$ + "|" + merge("[\\/\\?]", IPRIVATE$$)) + "*"), FRAGMENT$ = subexp(subexp(PCHAR$ + "|[\\/\\?]") + "*"), HIER_PART$ = subexp(subexp("\\/\\/" + AUTHORITY$ + PATH_ABEMPTY$) + "|" + PATH_ABSOLUTE$ + "|" + PATH_ROOTLESS$ + "|" + PATH_EMPTY$), URI$ = subexp(SCHEME$ + "\\:" + HIER_PART$ + subexp("\\?" + QUERY$) + "?" + subexp("\\#" + FRAGMENT$) + "?"), RELATIVE_PART$ = subexp(subexp("\\/\\/" + AUTHORITY$ + PATH_ABEMPTY$) + "|" + PATH_ABSOLUTE$ + "|" + PATH_NOSCHEME$ + "|" + PATH_EMPTY$), RELATIVE$ = subexp(RELATIVE_PART$ + subexp("\\?" + QUERY$) + "?" + subexp("\\#" + FRAGMENT$) + "?"), URI_REFERENCE$ = subexp(URI$ + "|" + RELATIVE$), ABSOLUTE_URI$ = subexp(SCHEME$ + "\\:" + HIER_PART$ + subexp("\\?" + QUERY$) + "?"), GENERIC_REF$ = "^(" + SCHEME$ + ")\\:" + subexp(subexp("\\/\\/(" + subexp("(" + USERINFO$ + ")@") + "?(" + HOST$ + ")" + subexp("\\:(" + PORT$ + ")") + "?)") + "?(" + PATH_ABEMPTY$ + "|" + PATH_ABSOLUTE$ + "|" + PATH_ROOTLESS$ + "|" + PATH_EMPTY$ + ")") + subexp("\\?(" + QUERY$ + ")") + "?" + subexp("\\#(" + FRAGMENT$ + ")") + "?$", RELATIVE_REF$ = "^(){0}" + subexp(subexp("\\/\\/(" + subexp("(" + USERINFO$ + ")@") + "?(" + HOST$ + ")" + subexp("\\:(" + PORT$ + ")") + "?)") + "?(" + PATH_ABEMPTY$ + "|" + PATH_ABSOLUTE$ + "|" + PATH_NOSCHEME$ + "|" + PATH_EMPTY$ + ")") + subexp("\\?(" + QUERY$ + ")") + "?" + subexp("\\#(" + FRAGMENT$ + ")") + "?$", ABSOLUTE_REF$ = "^(" + SCHEME$ + ")\\:" + subexp(subexp("\\/\\/(" + subexp("(" + USERINFO$ + ")@") + "?(" + HOST$ + ")" + subexp("\\:(" + PORT$ + ")") + "?)") + "?(" + PATH_ABEMPTY$ + "|" + PATH_ABSOLUTE$ + "|" + PATH_ROOTLESS$ + "|" + PATH_EMPTY$ + ")") + subexp("\\?(" + QUERY$ + ")") + "?$", SAMEDOC_REF$ = "^" + subexp("\\#(" + FRAGMENT$ + ")") + "?$", AUTHORITY_REF$ = "^" + subexp("(" + USERINFO$ + ")@") + "?(" + HOST$ + ")" + subexp("\\:(" + PORT$ + ")") + "?$";
        return {
          NOT_SCHEME: new RegExp(merge("[^]", ALPHA$$, DIGIT$$, "[\\+\\-\\.]"), "g"),
          NOT_USERINFO: new RegExp(merge("[^\\%\\:]", UNRESERVED$$2, SUB_DELIMS$$), "g"),
          NOT_HOST: new RegExp(merge("[^\\%\\[\\]\\:]", UNRESERVED$$2, SUB_DELIMS$$), "g"),
          NOT_PATH: new RegExp(merge("[^\\%\\/\\:\\@]", UNRESERVED$$2, SUB_DELIMS$$), "g"),
          NOT_PATH_NOSCHEME: new RegExp(merge("[^\\%\\/\\@]", UNRESERVED$$2, SUB_DELIMS$$), "g"),
          NOT_QUERY: new RegExp(merge("[^\\%]", UNRESERVED$$2, SUB_DELIMS$$, "[\\:\\@\\/\\?]", IPRIVATE$$), "g"),
          NOT_FRAGMENT: new RegExp(merge("[^\\%]", UNRESERVED$$2, SUB_DELIMS$$, "[\\:\\@\\/\\?]"), "g"),
          ESCAPE: new RegExp(merge("[^]", UNRESERVED$$2, SUB_DELIMS$$), "g"),
          UNRESERVED: new RegExp(UNRESERVED$$2, "g"),
          OTHER_CHARS: new RegExp(merge("[^\\%]", UNRESERVED$$2, RESERVED$$), "g"),
          PCT_ENCODED: new RegExp(PCT_ENCODED$2, "g"),
          IPV4ADDRESS: new RegExp("^(" + IPV4ADDRESS$ + ")$"),
          IPV6ADDRESS: new RegExp("^\\[?(" + IPV6ADDRESS$ + ")" + subexp(subexp("\\%25|\\%(?!" + HEXDIG$$2 + "{2})") + "(" + ZONEID$ + ")") + "?\\]?$")
          //RFC 6874, with relaxed parsing rules
        };
      }
      var URI_PROTOCOL = buildExps(false);
      var IRI_PROTOCOL = buildExps(true);
      var slicedToArray = /* @__PURE__ */ function() {
        function sliceIterator(arr, i) {
          var _arr = [];
          var _n = true;
          var _d = false;
          var _e = void 0;
          try {
            for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) {
              _arr.push(_s.value);
              if (i && _arr.length === i)
                break;
            }
          } catch (err) {
            _d = true;
            _e = err;
          } finally {
            try {
              if (!_n && _i["return"])
                _i["return"]();
            } finally {
              if (_d)
                throw _e;
            }
          }
          return _arr;
        }
        return function(arr, i) {
          if (Array.isArray(arr)) {
            return arr;
          } else if (Symbol.iterator in Object(arr)) {
            return sliceIterator(arr, i);
          } else {
            throw new TypeError("Invalid attempt to destructure non-iterable instance");
          }
        };
      }();
      var toConsumableArray = function(arr) {
        if (Array.isArray(arr)) {
          for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++)
            arr2[i] = arr[i];
          return arr2;
        } else {
          return Array.from(arr);
        }
      };
      var maxInt = 2147483647;
      var base = 36;
      var tMin = 1;
      var tMax = 26;
      var skew = 38;
      var damp = 700;
      var initialBias = 72;
      var initialN = 128;
      var delimiter = "-";
      var regexPunycode = /^xn--/;
      var regexNonASCII = /[^\0-\x7E]/;
      var regexSeparators = /[\x2E\u3002\uFF0E\uFF61]/g;
      var errors = {
        "overflow": "Overflow: input needs wider integers to process",
        "not-basic": "Illegal input >= 0x80 (not a basic code point)",
        "invalid-input": "Invalid input"
      };
      var baseMinusTMin = base - tMin;
      var floor = Math.floor;
      var stringFromCharCode = String.fromCharCode;
      function error$1(type) {
        throw new RangeError(errors[type]);
      }
      function map(array, fn) {
        var result = [];
        var length = array.length;
        while (length--) {
          result[length] = fn(array[length]);
        }
        return result;
      }
      function mapDomain(string, fn) {
        var parts = string.split("@");
        var result = "";
        if (parts.length > 1) {
          result = parts[0] + "@";
          string = parts[1];
        }
        string = string.replace(regexSeparators, ".");
        var labels = string.split(".");
        var encoded = map(labels, fn).join(".");
        return result + encoded;
      }
      function ucs2decode(string) {
        var output = [];
        var counter = 0;
        var length = string.length;
        while (counter < length) {
          var value = string.charCodeAt(counter++);
          if (value >= 55296 && value <= 56319 && counter < length) {
            var extra = string.charCodeAt(counter++);
            if ((extra & 64512) == 56320) {
              output.push(((value & 1023) << 10) + (extra & 1023) + 65536);
            } else {
              output.push(value);
              counter--;
            }
          } else {
            output.push(value);
          }
        }
        return output;
      }
      var ucs2encode = function ucs2encode2(array) {
        return String.fromCodePoint.apply(String, toConsumableArray(array));
      };
      var basicToDigit = function basicToDigit2(codePoint) {
        if (codePoint - 48 < 10) {
          return codePoint - 22;
        }
        if (codePoint - 65 < 26) {
          return codePoint - 65;
        }
        if (codePoint - 97 < 26) {
          return codePoint - 97;
        }
        return base;
      };
      var digitToBasic = function digitToBasic2(digit, flag) {
        return digit + 22 + 75 * (digit < 26) - ((flag != 0) << 5);
      };
      var adapt = function adapt2(delta, numPoints, firstTime) {
        var k = 0;
        delta = firstTime ? floor(delta / damp) : delta >> 1;
        delta += floor(delta / numPoints);
        for (
          ;
          /* no initialization */
          delta > baseMinusTMin * tMax >> 1;
          k += base
        ) {
          delta = floor(delta / baseMinusTMin);
        }
        return floor(k + (baseMinusTMin + 1) * delta / (delta + skew));
      };
      var decode = function decode2(input) {
        var output = [];
        var inputLength = input.length;
        var i = 0;
        var n = initialN;
        var bias = initialBias;
        var basic = input.lastIndexOf(delimiter);
        if (basic < 0) {
          basic = 0;
        }
        for (var j = 0; j < basic; ++j) {
          if (input.charCodeAt(j) >= 128) {
            error$1("not-basic");
          }
          output.push(input.charCodeAt(j));
        }
        for (var index = basic > 0 ? basic + 1 : 0; index < inputLength; ) {
          var oldi = i;
          for (
            var w = 1, k = base;
            ;
            /* no condition */
            k += base
          ) {
            if (index >= inputLength) {
              error$1("invalid-input");
            }
            var digit = basicToDigit(input.charCodeAt(index++));
            if (digit >= base || digit > floor((maxInt - i) / w)) {
              error$1("overflow");
            }
            i += digit * w;
            var t = k <= bias ? tMin : k >= bias + tMax ? tMax : k - bias;
            if (digit < t) {
              break;
            }
            var baseMinusT = base - t;
            if (w > floor(maxInt / baseMinusT)) {
              error$1("overflow");
            }
            w *= baseMinusT;
          }
          var out = output.length + 1;
          bias = adapt(i - oldi, out, oldi == 0);
          if (floor(i / out) > maxInt - n) {
            error$1("overflow");
          }
          n += floor(i / out);
          i %= out;
          output.splice(i++, 0, n);
        }
        return String.fromCodePoint.apply(String, output);
      };
      var encode = function encode2(input) {
        var output = [];
        input = ucs2decode(input);
        var inputLength = input.length;
        var n = initialN;
        var delta = 0;
        var bias = initialBias;
        var _iteratorNormalCompletion = true;
        var _didIteratorError = false;
        var _iteratorError = void 0;
        try {
          for (var _iterator = input[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
            var _currentValue2 = _step.value;
            if (_currentValue2 < 128) {
              output.push(stringFromCharCode(_currentValue2));
            }
          }
        } catch (err) {
          _didIteratorError = true;
          _iteratorError = err;
        } finally {
          try {
            if (!_iteratorNormalCompletion && _iterator.return) {
              _iterator.return();
            }
          } finally {
            if (_didIteratorError) {
              throw _iteratorError;
            }
          }
        }
        var basicLength = output.length;
        var handledCPCount = basicLength;
        if (basicLength) {
          output.push(delimiter);
        }
        while (handledCPCount < inputLength) {
          var m = maxInt;
          var _iteratorNormalCompletion2 = true;
          var _didIteratorError2 = false;
          var _iteratorError2 = void 0;
          try {
            for (var _iterator2 = input[Symbol.iterator](), _step2; !(_iteratorNormalCompletion2 = (_step2 = _iterator2.next()).done); _iteratorNormalCompletion2 = true) {
              var currentValue = _step2.value;
              if (currentValue >= n && currentValue < m) {
                m = currentValue;
              }
            }
          } catch (err) {
            _didIteratorError2 = true;
            _iteratorError2 = err;
          } finally {
            try {
              if (!_iteratorNormalCompletion2 && _iterator2.return) {
                _iterator2.return();
              }
            } finally {
              if (_didIteratorError2) {
                throw _iteratorError2;
              }
            }
          }
          var handledCPCountPlusOne = handledCPCount + 1;
          if (m - n > floor((maxInt - delta) / handledCPCountPlusOne)) {
            error$1("overflow");
          }
          delta += (m - n) * handledCPCountPlusOne;
          n = m;
          var _iteratorNormalCompletion3 = true;
          var _didIteratorError3 = false;
          var _iteratorError3 = void 0;
          try {
            for (var _iterator3 = input[Symbol.iterator](), _step3; !(_iteratorNormalCompletion3 = (_step3 = _iterator3.next()).done); _iteratorNormalCompletion3 = true) {
              var _currentValue = _step3.value;
              if (_currentValue < n && ++delta > maxInt) {
                error$1("overflow");
              }
              if (_currentValue == n) {
                var q = delta;
                for (
                  var k = base;
                  ;
                  /* no condition */
                  k += base
                ) {
                  var t = k <= bias ? tMin : k >= bias + tMax ? tMax : k - bias;
                  if (q < t) {
                    break;
                  }
                  var qMinusT = q - t;
                  var baseMinusT = base - t;
                  output.push(stringFromCharCode(digitToBasic(t + qMinusT % baseMinusT, 0)));
                  q = floor(qMinusT / baseMinusT);
                }
                output.push(stringFromCharCode(digitToBasic(q, 0)));
                bias = adapt(delta, handledCPCountPlusOne, handledCPCount == basicLength);
                delta = 0;
                ++handledCPCount;
              }
            }
          } catch (err) {
            _didIteratorError3 = true;
            _iteratorError3 = err;
          } finally {
            try {
              if (!_iteratorNormalCompletion3 && _iterator3.return) {
                _iterator3.return();
              }
            } finally {
              if (_didIteratorError3) {
                throw _iteratorError3;
              }
            }
          }
          ++delta;
          ++n;
        }
        return output.join("");
      };
      var toUnicode = function toUnicode2(input) {
        return mapDomain(input, function(string) {
          return regexPunycode.test(string) ? decode(string.slice(4).toLowerCase()) : string;
        });
      };
      var toASCII = function toASCII2(input) {
        return mapDomain(input, function(string) {
          return regexNonASCII.test(string) ? "xn--" + encode(string) : string;
        });
      };
      var punycode = {
        /**
         * A string representing the current Punycode.js version number.
         * @memberOf punycode
         * @type String
         */
        "version": "2.1.0",
        /**
         * An object of methods to convert from JavaScript's internal character
         * representation (UCS-2) to Unicode code points, and back.
         * @see <https://mathiasbynens.be/notes/javascript-encoding>
         * @memberOf punycode
         * @type Object
         */
        "ucs2": {
          "decode": ucs2decode,
          "encode": ucs2encode
        },
        "decode": decode,
        "encode": encode,
        "toASCII": toASCII,
        "toUnicode": toUnicode
      };
      var SCHEMES = {};
      function pctEncChar(chr) {
        var c = chr.charCodeAt(0);
        var e = void 0;
        if (c < 16)
          e = "%0" + c.toString(16).toUpperCase();
        else if (c < 128)
          e = "%" + c.toString(16).toUpperCase();
        else if (c < 2048)
          e = "%" + (c >> 6 | 192).toString(16).toUpperCase() + "%" + (c & 63 | 128).toString(16).toUpperCase();
        else
          e = "%" + (c >> 12 | 224).toString(16).toUpperCase() + "%" + (c >> 6 & 63 | 128).toString(16).toUpperCase() + "%" + (c & 63 | 128).toString(16).toUpperCase();
        return e;
      }
      function pctDecChars(str) {
        var newStr = "";
        var i = 0;
        var il = str.length;
        while (i < il) {
          var c = parseInt(str.substr(i + 1, 2), 16);
          if (c < 128) {
            newStr += String.fromCharCode(c);
            i += 3;
          } else if (c >= 194 && c < 224) {
            if (il - i >= 6) {
              var c2 = parseInt(str.substr(i + 4, 2), 16);
              newStr += String.fromCharCode((c & 31) << 6 | c2 & 63);
            } else {
              newStr += str.substr(i, 6);
            }
            i += 6;
          } else if (c >= 224) {
            if (il - i >= 9) {
              var _c = parseInt(str.substr(i + 4, 2), 16);
              var c3 = parseInt(str.substr(i + 7, 2), 16);
              newStr += String.fromCharCode((c & 15) << 12 | (_c & 63) << 6 | c3 & 63);
            } else {
              newStr += str.substr(i, 9);
            }
            i += 9;
          } else {
            newStr += str.substr(i, 3);
            i += 3;
          }
        }
        return newStr;
      }
      function _normalizeComponentEncoding(components, protocol) {
        function decodeUnreserved2(str) {
          var decStr = pctDecChars(str);
          return !decStr.match(protocol.UNRESERVED) ? str : decStr;
        }
        if (components.scheme)
          components.scheme = String(components.scheme).replace(protocol.PCT_ENCODED, decodeUnreserved2).toLowerCase().replace(protocol.NOT_SCHEME, "");
        if (components.userinfo !== void 0)
          components.userinfo = String(components.userinfo).replace(protocol.PCT_ENCODED, decodeUnreserved2).replace(protocol.NOT_USERINFO, pctEncChar).replace(protocol.PCT_ENCODED, toUpperCase);
        if (components.host !== void 0)
          components.host = String(components.host).replace(protocol.PCT_ENCODED, decodeUnreserved2).toLowerCase().replace(protocol.NOT_HOST, pctEncChar).replace(protocol.PCT_ENCODED, toUpperCase);
        if (components.path !== void 0)
          components.path = String(components.path).replace(protocol.PCT_ENCODED, decodeUnreserved2).replace(components.scheme ? protocol.NOT_PATH : protocol.NOT_PATH_NOSCHEME, pctEncChar).replace(protocol.PCT_ENCODED, toUpperCase);
        if (components.query !== void 0)
          components.query = String(components.query).replace(protocol.PCT_ENCODED, decodeUnreserved2).replace(protocol.NOT_QUERY, pctEncChar).replace(protocol.PCT_ENCODED, toUpperCase);
        if (components.fragment !== void 0)
          components.fragment = String(components.fragment).replace(protocol.PCT_ENCODED, decodeUnreserved2).replace(protocol.NOT_FRAGMENT, pctEncChar).replace(protocol.PCT_ENCODED, toUpperCase);
        return components;
      }
      function _stripLeadingZeros(str) {
        return str.replace(/^0*(.*)/, "$1") || "0";
      }
      function _normalizeIPv4(host, protocol) {
        var matches = host.match(protocol.IPV4ADDRESS) || [];
        var _matches = slicedToArray(matches, 2), address = _matches[1];
        if (address) {
          return address.split(".").map(_stripLeadingZeros).join(".");
        } else {
          return host;
        }
      }
      function _normalizeIPv6(host, protocol) {
        var matches = host.match(protocol.IPV6ADDRESS) || [];
        var _matches2 = slicedToArray(matches, 3), address = _matches2[1], zone = _matches2[2];
        if (address) {
          var _address$toLowerCase$ = address.toLowerCase().split("::").reverse(), _address$toLowerCase$2 = slicedToArray(_address$toLowerCase$, 2), last = _address$toLowerCase$2[0], first = _address$toLowerCase$2[1];
          var firstFields = first ? first.split(":").map(_stripLeadingZeros) : [];
          var lastFields = last.split(":").map(_stripLeadingZeros);
          var isLastFieldIPv4Address = protocol.IPV4ADDRESS.test(lastFields[lastFields.length - 1]);
          var fieldCount = isLastFieldIPv4Address ? 7 : 8;
          var lastFieldsStart = lastFields.length - fieldCount;
          var fields = Array(fieldCount);
          for (var x = 0; x < fieldCount; ++x) {
            fields[x] = firstFields[x] || lastFields[lastFieldsStart + x] || "";
          }
          if (isLastFieldIPv4Address) {
            fields[fieldCount - 1] = _normalizeIPv4(fields[fieldCount - 1], protocol);
          }
          var allZeroFields = fields.reduce(function(acc, field, index) {
            if (!field || field === "0") {
              var lastLongest = acc[acc.length - 1];
              if (lastLongest && lastLongest.index + lastLongest.length === index) {
                lastLongest.length++;
              } else {
                acc.push({ index, length: 1 });
              }
            }
            return acc;
          }, []);
          var longestZeroFields = allZeroFields.sort(function(a, b) {
            return b.length - a.length;
          })[0];
          var newHost = void 0;
          if (longestZeroFields && longestZeroFields.length > 1) {
            var newFirst = fields.slice(0, longestZeroFields.index);
            var newLast = fields.slice(longestZeroFields.index + longestZeroFields.length);
            newHost = newFirst.join(":") + "::" + newLast.join(":");
          } else {
            newHost = fields.join(":");
          }
          if (zone) {
            newHost += "%" + zone;
          }
          return newHost;
        } else {
          return host;
        }
      }
      var URI_PARSE = /^(?:([^:\/?#]+):)?(?:\/\/((?:([^\/?#@]*)@)?(\[[^\/?#\]]+\]|[^\/?#:]*)(?:\:(\d*))?))?([^?#]*)(?:\?([^#]*))?(?:#((?:.|\n|\r)*))?/i;
      var NO_MATCH_IS_UNDEFINED = "".match(/(){0}/)[1] === void 0;
      function parse(uriString) {
        var options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
        var components = {};
        var protocol = options.iri !== false ? IRI_PROTOCOL : URI_PROTOCOL;
        if (options.reference === "suffix")
          uriString = (options.scheme ? options.scheme + ":" : "") + "//" + uriString;
        var matches = uriString.match(URI_PARSE);
        if (matches) {
          if (NO_MATCH_IS_UNDEFINED) {
            components.scheme = matches[1];
            components.userinfo = matches[3];
            components.host = matches[4];
            components.port = parseInt(matches[5], 10);
            components.path = matches[6] || "";
            components.query = matches[7];
            components.fragment = matches[8];
            if (isNaN(components.port)) {
              components.port = matches[5];
            }
          } else {
            components.scheme = matches[1] || void 0;
            components.userinfo = uriString.indexOf("@") !== -1 ? matches[3] : void 0;
            components.host = uriString.indexOf("//") !== -1 ? matches[4] : void 0;
            components.port = parseInt(matches[5], 10);
            components.path = matches[6] || "";
            components.query = uriString.indexOf("?") !== -1 ? matches[7] : void 0;
            components.fragment = uriString.indexOf("#") !== -1 ? matches[8] : void 0;
            if (isNaN(components.port)) {
              components.port = uriString.match(/\/\/(?:.|\n)*\:(?:\/|\?|\#|$)/) ? matches[4] : void 0;
            }
          }
          if (components.host) {
            components.host = _normalizeIPv6(_normalizeIPv4(components.host, protocol), protocol);
          }
          if (components.scheme === void 0 && components.userinfo === void 0 && components.host === void 0 && components.port === void 0 && !components.path && components.query === void 0) {
            components.reference = "same-document";
          } else if (components.scheme === void 0) {
            components.reference = "relative";
          } else if (components.fragment === void 0) {
            components.reference = "absolute";
          } else {
            components.reference = "uri";
          }
          if (options.reference && options.reference !== "suffix" && options.reference !== components.reference) {
            components.error = components.error || "URI is not a " + options.reference + " reference.";
          }
          var schemeHandler = SCHEMES[(options.scheme || components.scheme || "").toLowerCase()];
          if (!options.unicodeSupport && (!schemeHandler || !schemeHandler.unicodeSupport)) {
            if (components.host && (options.domainHost || schemeHandler && schemeHandler.domainHost)) {
              try {
                components.host = punycode.toASCII(components.host.replace(protocol.PCT_ENCODED, pctDecChars).toLowerCase());
              } catch (e) {
                components.error = components.error || "Host's domain name can not be converted to ASCII via punycode: " + e;
              }
            }
            _normalizeComponentEncoding(components, URI_PROTOCOL);
          } else {
            _normalizeComponentEncoding(components, protocol);
          }
          if (schemeHandler && schemeHandler.parse) {
            schemeHandler.parse(components, options);
          }
        } else {
          components.error = components.error || "URI can not be parsed.";
        }
        return components;
      }
      function _recomposeAuthority(components, options) {
        var protocol = options.iri !== false ? IRI_PROTOCOL : URI_PROTOCOL;
        var uriTokens = [];
        if (components.userinfo !== void 0) {
          uriTokens.push(components.userinfo);
          uriTokens.push("@");
        }
        if (components.host !== void 0) {
          uriTokens.push(_normalizeIPv6(_normalizeIPv4(String(components.host), protocol), protocol).replace(protocol.IPV6ADDRESS, function(_, $1, $2) {
            return "[" + $1 + ($2 ? "%25" + $2 : "") + "]";
          }));
        }
        if (typeof components.port === "number" || typeof components.port === "string") {
          uriTokens.push(":");
          uriTokens.push(String(components.port));
        }
        return uriTokens.length ? uriTokens.join("") : void 0;
      }
      var RDS1 = /^\.\.?\//;
      var RDS2 = /^\/\.(\/|$)/;
      var RDS3 = /^\/\.\.(\/|$)/;
      var RDS5 = /^\/?(?:.|\n)*?(?=\/|$)/;
      function removeDotSegments(input) {
        var output = [];
        while (input.length) {
          if (input.match(RDS1)) {
            input = input.replace(RDS1, "");
          } else if (input.match(RDS2)) {
            input = input.replace(RDS2, "/");
          } else if (input.match(RDS3)) {
            input = input.replace(RDS3, "/");
            output.pop();
          } else if (input === "." || input === "..") {
            input = "";
          } else {
            var im = input.match(RDS5);
            if (im) {
              var s = im[0];
              input = input.slice(s.length);
              output.push(s);
            } else {
              throw new Error("Unexpected dot segment condition");
            }
          }
        }
        return output.join("");
      }
      function serialize(components) {
        var options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
        var protocol = options.iri ? IRI_PROTOCOL : URI_PROTOCOL;
        var uriTokens = [];
        var schemeHandler = SCHEMES[(options.scheme || components.scheme || "").toLowerCase()];
        if (schemeHandler && schemeHandler.serialize)
          schemeHandler.serialize(components, options);
        if (components.host) {
          if (protocol.IPV6ADDRESS.test(components.host)) {
          } else if (options.domainHost || schemeHandler && schemeHandler.domainHost) {
            try {
              components.host = !options.iri ? punycode.toASCII(components.host.replace(protocol.PCT_ENCODED, pctDecChars).toLowerCase()) : punycode.toUnicode(components.host);
            } catch (e) {
              components.error = components.error || "Host's domain name can not be converted to " + (!options.iri ? "ASCII" : "Unicode") + " via punycode: " + e;
            }
          }
        }
        _normalizeComponentEncoding(components, protocol);
        if (options.reference !== "suffix" && components.scheme) {
          uriTokens.push(components.scheme);
          uriTokens.push(":");
        }
        var authority = _recomposeAuthority(components, options);
        if (authority !== void 0) {
          if (options.reference !== "suffix") {
            uriTokens.push("//");
          }
          uriTokens.push(authority);
          if (components.path && components.path.charAt(0) !== "/") {
            uriTokens.push("/");
          }
        }
        if (components.path !== void 0) {
          var s = components.path;
          if (!options.absolutePath && (!schemeHandler || !schemeHandler.absolutePath)) {
            s = removeDotSegments(s);
          }
          if (authority === void 0) {
            s = s.replace(/^\/\//, "/%2F");
          }
          uriTokens.push(s);
        }
        if (components.query !== void 0) {
          uriTokens.push("?");
          uriTokens.push(components.query);
        }
        if (components.fragment !== void 0) {
          uriTokens.push("#");
          uriTokens.push(components.fragment);
        }
        return uriTokens.join("");
      }
      function resolveComponents(base2, relative) {
        var options = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
        var skipNormalization = arguments[3];
        var target = {};
        if (!skipNormalization) {
          base2 = parse(serialize(base2, options), options);
          relative = parse(serialize(relative, options), options);
        }
        options = options || {};
        if (!options.tolerant && relative.scheme) {
          target.scheme = relative.scheme;
          target.userinfo = relative.userinfo;
          target.host = relative.host;
          target.port = relative.port;
          target.path = removeDotSegments(relative.path || "");
          target.query = relative.query;
        } else {
          if (relative.userinfo !== void 0 || relative.host !== void 0 || relative.port !== void 0) {
            target.userinfo = relative.userinfo;
            target.host = relative.host;
            target.port = relative.port;
            target.path = removeDotSegments(relative.path || "");
            target.query = relative.query;
          } else {
            if (!relative.path) {
              target.path = base2.path;
              if (relative.query !== void 0) {
                target.query = relative.query;
              } else {
                target.query = base2.query;
              }
            } else {
              if (relative.path.charAt(0) === "/") {
                target.path = removeDotSegments(relative.path);
              } else {
                if ((base2.userinfo !== void 0 || base2.host !== void 0 || base2.port !== void 0) && !base2.path) {
                  target.path = "/" + relative.path;
                } else if (!base2.path) {
                  target.path = relative.path;
                } else {
                  target.path = base2.path.slice(0, base2.path.lastIndexOf("/") + 1) + relative.path;
                }
                target.path = removeDotSegments(target.path);
              }
              target.query = relative.query;
            }
            target.userinfo = base2.userinfo;
            target.host = base2.host;
            target.port = base2.port;
          }
          target.scheme = base2.scheme;
        }
        target.fragment = relative.fragment;
        return target;
      }
      function resolve(baseURI, relativeURI, options) {
        var schemelessOptions = assign({ scheme: "null" }, options);
        return serialize(resolveComponents(parse(baseURI, schemelessOptions), parse(relativeURI, schemelessOptions), schemelessOptions, true), schemelessOptions);
      }
      function normalize(uri, options) {
        if (typeof uri === "string") {
          uri = serialize(parse(uri, options), options);
        } else if (typeOf(uri) === "object") {
          uri = parse(serialize(uri, options), options);
        }
        return uri;
      }
      function equal(uriA, uriB, options) {
        if (typeof uriA === "string") {
          uriA = serialize(parse(uriA, options), options);
        } else if (typeOf(uriA) === "object") {
          uriA = serialize(uriA, options);
        }
        if (typeof uriB === "string") {
          uriB = serialize(parse(uriB, options), options);
        } else if (typeOf(uriB) === "object") {
          uriB = serialize(uriB, options);
        }
        return uriA === uriB;
      }
      function escapeComponent(str, options) {
        return str && str.toString().replace(!options || !options.iri ? URI_PROTOCOL.ESCAPE : IRI_PROTOCOL.ESCAPE, pctEncChar);
      }
      function unescapeComponent(str, options) {
        return str && str.toString().replace(!options || !options.iri ? URI_PROTOCOL.PCT_ENCODED : IRI_PROTOCOL.PCT_ENCODED, pctDecChars);
      }
      var handler = {
        scheme: "http",
        domainHost: true,
        parse: function parse2(components, options) {
          if (!components.host) {
            components.error = components.error || "HTTP URIs must have a host.";
          }
          return components;
        },
        serialize: function serialize2(components, options) {
          var secure = String(components.scheme).toLowerCase() === "https";
          if (components.port === (secure ? 443 : 80) || components.port === "") {
            components.port = void 0;
          }
          if (!components.path) {
            components.path = "/";
          }
          return components;
        }
      };
      var handler$1 = {
        scheme: "https",
        domainHost: handler.domainHost,
        parse: handler.parse,
        serialize: handler.serialize
      };
      function isSecure(wsComponents) {
        return typeof wsComponents.secure === "boolean" ? wsComponents.secure : String(wsComponents.scheme).toLowerCase() === "wss";
      }
      var handler$2 = {
        scheme: "ws",
        domainHost: true,
        parse: function parse2(components, options) {
          var wsComponents = components;
          wsComponents.secure = isSecure(wsComponents);
          wsComponents.resourceName = (wsComponents.path || "/") + (wsComponents.query ? "?" + wsComponents.query : "");
          wsComponents.path = void 0;
          wsComponents.query = void 0;
          return wsComponents;
        },
        serialize: function serialize2(wsComponents, options) {
          if (wsComponents.port === (isSecure(wsComponents) ? 443 : 80) || wsComponents.port === "") {
            wsComponents.port = void 0;
          }
          if (typeof wsComponents.secure === "boolean") {
            wsComponents.scheme = wsComponents.secure ? "wss" : "ws";
            wsComponents.secure = void 0;
          }
          if (wsComponents.resourceName) {
            var _wsComponents$resourc = wsComponents.resourceName.split("?"), _wsComponents$resourc2 = slicedToArray(_wsComponents$resourc, 2), path = _wsComponents$resourc2[0], query = _wsComponents$resourc2[1];
            wsComponents.path = path && path !== "/" ? path : void 0;
            wsComponents.query = query;
            wsComponents.resourceName = void 0;
          }
          wsComponents.fragment = void 0;
          return wsComponents;
        }
      };
      var handler$3 = {
        scheme: "wss",
        domainHost: handler$2.domainHost,
        parse: handler$2.parse,
        serialize: handler$2.serialize
      };
      var O = {};
      var isIRI = true;
      var UNRESERVED$$ = "[A-Za-z0-9\\-\\.\\_\\~" + (isIRI ? "\\xA0-\\u200D\\u2010-\\u2029\\u202F-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFEF" : "") + "]";
      var HEXDIG$$ = "[0-9A-Fa-f]";
      var PCT_ENCODED$ = subexp(subexp("%[EFef]" + HEXDIG$$ + "%" + HEXDIG$$ + HEXDIG$$ + "%" + HEXDIG$$ + HEXDIG$$) + "|" + subexp("%[89A-Fa-f]" + HEXDIG$$ + "%" + HEXDIG$$ + HEXDIG$$) + "|" + subexp("%" + HEXDIG$$ + HEXDIG$$));
      var ATEXT$$ = "[A-Za-z0-9\\!\\$\\%\\'\\*\\+\\-\\^\\_\\`\\{\\|\\}\\~]";
      var QTEXT$$ = "[\\!\\$\\%\\'\\(\\)\\*\\+\\,\\-\\.0-9\\<\\>A-Z\\x5E-\\x7E]";
      var VCHAR$$ = merge(QTEXT$$, '[\\"\\\\]');
      var SOME_DELIMS$$ = "[\\!\\$\\'\\(\\)\\*\\+\\,\\;\\:\\@]";
      var UNRESERVED = new RegExp(UNRESERVED$$, "g");
      var PCT_ENCODED = new RegExp(PCT_ENCODED$, "g");
      var NOT_LOCAL_PART = new RegExp(merge("[^]", ATEXT$$, "[\\.]", '[\\"]', VCHAR$$), "g");
      var NOT_HFNAME = new RegExp(merge("[^]", UNRESERVED$$, SOME_DELIMS$$), "g");
      var NOT_HFVALUE = NOT_HFNAME;
      function decodeUnreserved(str) {
        var decStr = pctDecChars(str);
        return !decStr.match(UNRESERVED) ? str : decStr;
      }
      var handler$4 = {
        scheme: "mailto",
        parse: function parse$$1(components, options) {
          var mailtoComponents = components;
          var to = mailtoComponents.to = mailtoComponents.path ? mailtoComponents.path.split(",") : [];
          mailtoComponents.path = void 0;
          if (mailtoComponents.query) {
            var unknownHeaders = false;
            var headers = {};
            var hfields = mailtoComponents.query.split("&");
            for (var x = 0, xl = hfields.length; x < xl; ++x) {
              var hfield = hfields[x].split("=");
              switch (hfield[0]) {
                case "to":
                  var toAddrs = hfield[1].split(",");
                  for (var _x = 0, _xl = toAddrs.length; _x < _xl; ++_x) {
                    to.push(toAddrs[_x]);
                  }
                  break;
                case "subject":
                  mailtoComponents.subject = unescapeComponent(hfield[1], options);
                  break;
                case "body":
                  mailtoComponents.body = unescapeComponent(hfield[1], options);
                  break;
                default:
                  unknownHeaders = true;
                  headers[unescapeComponent(hfield[0], options)] = unescapeComponent(hfield[1], options);
                  break;
              }
            }
            if (unknownHeaders)
              mailtoComponents.headers = headers;
          }
          mailtoComponents.query = void 0;
          for (var _x2 = 0, _xl2 = to.length; _x2 < _xl2; ++_x2) {
            var addr = to[_x2].split("@");
            addr[0] = unescapeComponent(addr[0]);
            if (!options.unicodeSupport) {
              try {
                addr[1] = punycode.toASCII(unescapeComponent(addr[1], options).toLowerCase());
              } catch (e) {
                mailtoComponents.error = mailtoComponents.error || "Email address's domain name can not be converted to ASCII via punycode: " + e;
              }
            } else {
              addr[1] = unescapeComponent(addr[1], options).toLowerCase();
            }
            to[_x2] = addr.join("@");
          }
          return mailtoComponents;
        },
        serialize: function serialize$$1(mailtoComponents, options) {
          var components = mailtoComponents;
          var to = toArray(mailtoComponents.to);
          if (to) {
            for (var x = 0, xl = to.length; x < xl; ++x) {
              var toAddr = String(to[x]);
              var atIdx = toAddr.lastIndexOf("@");
              var localPart = toAddr.slice(0, atIdx).replace(PCT_ENCODED, decodeUnreserved).replace(PCT_ENCODED, toUpperCase).replace(NOT_LOCAL_PART, pctEncChar);
              var domain = toAddr.slice(atIdx + 1);
              try {
                domain = !options.iri ? punycode.toASCII(unescapeComponent(domain, options).toLowerCase()) : punycode.toUnicode(domain);
              } catch (e) {
                components.error = components.error || "Email address's domain name can not be converted to " + (!options.iri ? "ASCII" : "Unicode") + " via punycode: " + e;
              }
              to[x] = localPart + "@" + domain;
            }
            components.path = to.join(",");
          }
          var headers = mailtoComponents.headers = mailtoComponents.headers || {};
          if (mailtoComponents.subject)
            headers["subject"] = mailtoComponents.subject;
          if (mailtoComponents.body)
            headers["body"] = mailtoComponents.body;
          var fields = [];
          for (var name in headers) {
            if (headers[name] !== O[name]) {
              fields.push(name.replace(PCT_ENCODED, decodeUnreserved).replace(PCT_ENCODED, toUpperCase).replace(NOT_HFNAME, pctEncChar) + "=" + headers[name].replace(PCT_ENCODED, decodeUnreserved).replace(PCT_ENCODED, toUpperCase).replace(NOT_HFVALUE, pctEncChar));
            }
          }
          if (fields.length) {
            components.query = fields.join("&");
          }
          return components;
        }
      };
      var URN_PARSE = /^([^\:]+)\:(.*)/;
      var handler$5 = {
        scheme: "urn",
        parse: function parse$$1(components, options) {
          var matches = components.path && components.path.match(URN_PARSE);
          var urnComponents = components;
          if (matches) {
            var scheme = options.scheme || urnComponents.scheme || "urn";
            var nid = matches[1].toLowerCase();
            var nss = matches[2];
            var urnScheme = scheme + ":" + (options.nid || nid);
            var schemeHandler = SCHEMES[urnScheme];
            urnComponents.nid = nid;
            urnComponents.nss = nss;
            urnComponents.path = void 0;
            if (schemeHandler) {
              urnComponents = schemeHandler.parse(urnComponents, options);
            }
          } else {
            urnComponents.error = urnComponents.error || "URN can not be parsed.";
          }
          return urnComponents;
        },
        serialize: function serialize$$1(urnComponents, options) {
          var scheme = options.scheme || urnComponents.scheme || "urn";
          var nid = urnComponents.nid;
          var urnScheme = scheme + ":" + (options.nid || nid);
          var schemeHandler = SCHEMES[urnScheme];
          if (schemeHandler) {
            urnComponents = schemeHandler.serialize(urnComponents, options);
          }
          var uriComponents = urnComponents;
          var nss = urnComponents.nss;
          uriComponents.path = (nid || options.nid) + ":" + nss;
          return uriComponents;
        }
      };
      var UUID = /^[0-9A-Fa-f]{8}(?:\-[0-9A-Fa-f]{4}){3}\-[0-9A-Fa-f]{12}$/;
      var handler$6 = {
        scheme: "urn:uuid",
        parse: function parse2(urnComponents, options) {
          var uuidComponents = urnComponents;
          uuidComponents.uuid = uuidComponents.nss;
          uuidComponents.nss = void 0;
          if (!options.tolerant && (!uuidComponents.uuid || !uuidComponents.uuid.match(UUID))) {
            uuidComponents.error = uuidComponents.error || "UUID is not valid.";
          }
          return uuidComponents;
        },
        serialize: function serialize2(uuidComponents, options) {
          var urnComponents = uuidComponents;
          urnComponents.nss = (uuidComponents.uuid || "").toLowerCase();
          return urnComponents;
        }
      };
      SCHEMES[handler.scheme] = handler;
      SCHEMES[handler$1.scheme] = handler$1;
      SCHEMES[handler$2.scheme] = handler$2;
      SCHEMES[handler$3.scheme] = handler$3;
      SCHEMES[handler$4.scheme] = handler$4;
      SCHEMES[handler$5.scheme] = handler$5;
      SCHEMES[handler$6.scheme] = handler$6;
      exports3.SCHEMES = SCHEMES;
      exports3.pctEncChar = pctEncChar;
      exports3.pctDecChars = pctDecChars;
      exports3.parse = parse;
      exports3.removeDotSegments = removeDotSegments;
      exports3.serialize = serialize;
      exports3.resolveComponents = resolveComponents;
      exports3.resolve = resolve;
      exports3.normalize = normalize;
      exports3.equal = equal;
      exports3.escapeComponent = escapeComponent;
      exports3.unescapeComponent = unescapeComponent;
      Object.defineProperty(exports3, "__esModule", { value: true });
    });
  }
});

// ../node_modules/.pnpm/fast-deep-equal@3.1.3/node_modules/fast-deep-equal/index.js
var require_fast_deep_equal = __commonJS({
  "../node_modules/.pnpm/fast-deep-equal@3.1.3/node_modules/fast-deep-equal/index.js"(exports2, module2) {
    "use strict";
    module2.exports = function equal(a, b) {
      if (a === b)
        return true;
      if (a && b && typeof a == "object" && typeof b == "object") {
        if (a.constructor !== b.constructor)
          return false;
        var length, i, keys;
        if (Array.isArray(a)) {
          length = a.length;
          if (length != b.length)
            return false;
          for (i = length; i-- !== 0; )
            if (!equal(a[i], b[i]))
              return false;
          return true;
        }
        if (a.constructor === RegExp)
          return a.source === b.source && a.flags === b.flags;
        if (a.valueOf !== Object.prototype.valueOf)
          return a.valueOf() === b.valueOf();
        if (a.toString !== Object.prototype.toString)
          return a.toString() === b.toString();
        keys = Object.keys(a);
        length = keys.length;
        if (length !== Object.keys(b).length)
          return false;
        for (i = length; i-- !== 0; )
          if (!Object.prototype.hasOwnProperty.call(b, keys[i]))
            return false;
        for (i = length; i-- !== 0; ) {
          var key = keys[i];
          if (!equal(a[key], b[key]))
            return false;
        }
        return true;
      }
      return a !== a && b !== b;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/ucs2length.js
var require_ucs2length = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/ucs2length.js"(exports2, module2) {
    "use strict";
    module2.exports = function ucs2length(str) {
      var length = 0, len = str.length, pos = 0, value;
      while (pos < len) {
        length++;
        value = str.charCodeAt(pos++);
        if (value >= 55296 && value <= 56319 && pos < len) {
          value = str.charCodeAt(pos);
          if ((value & 64512) == 56320)
            pos++;
        }
      }
      return length;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/util.js
var require_util = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/util.js"(exports2, module2) {
    "use strict";
    module2.exports = {
      copy,
      checkDataType,
      checkDataTypes,
      coerceToTypes,
      toHash,
      getProperty,
      escapeQuotes,
      equal: require_fast_deep_equal(),
      ucs2length: require_ucs2length(),
      varOccurences,
      varReplace,
      schemaHasRules,
      schemaHasRulesExcept,
      schemaUnknownRules,
      toQuotedString,
      getPathExpr,
      getPath,
      getData,
      unescapeFragment,
      unescapeJsonPointer,
      escapeFragment,
      escapeJsonPointer
    };
    function copy(o, to) {
      to = to || {};
      for (var key in o)
        to[key] = o[key];
      return to;
    }
    function checkDataType(dataType, data, strictNumbers, negate) {
      var EQUAL = negate ? " !== " : " === ", AND = negate ? " || " : " && ", OK = negate ? "!" : "", NOT = negate ? "" : "!";
      switch (dataType) {
        case "null":
          return data + EQUAL + "null";
        case "array":
          return OK + "Array.isArray(" + data + ")";
        case "object":
          return "(" + OK + data + AND + "typeof " + data + EQUAL + '"object"' + AND + NOT + "Array.isArray(" + data + "))";
        case "integer":
          return "(typeof " + data + EQUAL + '"number"' + AND + NOT + "(" + data + " % 1)" + AND + data + EQUAL + data + (strictNumbers ? AND + OK + "isFinite(" + data + ")" : "") + ")";
        case "number":
          return "(typeof " + data + EQUAL + '"' + dataType + '"' + (strictNumbers ? AND + OK + "isFinite(" + data + ")" : "") + ")";
        default:
          return "typeof " + data + EQUAL + '"' + dataType + '"';
      }
    }
    function checkDataTypes(dataTypes, data, strictNumbers) {
      switch (dataTypes.length) {
        case 1:
          return checkDataType(dataTypes[0], data, strictNumbers, true);
        default:
          var code2 = "";
          var types = toHash(dataTypes);
          if (types.array && types.object) {
            code2 = types.null ? "(" : "(!" + data + " || ";
            code2 += "typeof " + data + ' !== "object")';
            delete types.null;
            delete types.array;
            delete types.object;
          }
          if (types.number)
            delete types.integer;
          for (var t in types)
            code2 += (code2 ? " && " : "") + checkDataType(t, data, strictNumbers, true);
          return code2;
      }
    }
    var COERCE_TO_TYPES = toHash(["string", "number", "integer", "boolean", "null"]);
    function coerceToTypes(optionCoerceTypes, dataTypes) {
      if (Array.isArray(dataTypes)) {
        var types = [];
        for (var i = 0; i < dataTypes.length; i++) {
          var t = dataTypes[i];
          if (COERCE_TO_TYPES[t])
            types[types.length] = t;
          else if (optionCoerceTypes === "array" && t === "array")
            types[types.length] = t;
        }
        if (types.length)
          return types;
      } else if (COERCE_TO_TYPES[dataTypes]) {
        return [dataTypes];
      } else if (optionCoerceTypes === "array" && dataTypes === "array") {
        return ["array"];
      }
    }
    function toHash(arr) {
      var hash = {};
      for (var i = 0; i < arr.length; i++)
        hash[arr[i]] = true;
      return hash;
    }
    var IDENTIFIER = /^[a-z$_][a-z$_0-9]*$/i;
    var SINGLE_QUOTE = /'|\\/g;
    function getProperty(key) {
      return typeof key == "number" ? "[" + key + "]" : IDENTIFIER.test(key) ? "." + key : "['" + escapeQuotes(key) + "']";
    }
    function escapeQuotes(str) {
      return str.replace(SINGLE_QUOTE, "\\$&").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/\f/g, "\\f").replace(/\t/g, "\\t");
    }
    function varOccurences(str, dataVar) {
      dataVar += "[^0-9]";
      var matches = str.match(new RegExp(dataVar, "g"));
      return matches ? matches.length : 0;
    }
    function varReplace(str, dataVar, expr) {
      dataVar += "([^0-9])";
      expr = expr.replace(/\$/g, "$$$$");
      return str.replace(new RegExp(dataVar, "g"), expr + "$1");
    }
    function schemaHasRules(schema, rules) {
      if (typeof schema == "boolean")
        return !schema;
      for (var key in schema)
        if (rules[key])
          return true;
    }
    function schemaHasRulesExcept(schema, rules, exceptKeyword) {
      if (typeof schema == "boolean")
        return !schema && exceptKeyword != "not";
      for (var key in schema)
        if (key != exceptKeyword && rules[key])
          return true;
    }
    function schemaUnknownRules(schema, rules) {
      if (typeof schema == "boolean")
        return;
      for (var key in schema)
        if (!rules[key])
          return key;
    }
    function toQuotedString(str) {
      return "'" + escapeQuotes(str) + "'";
    }
    function getPathExpr(currentPath, expr, jsonPointers, isNumber) {
      var path = jsonPointers ? "'/' + " + expr + (isNumber ? "" : ".replace(/~/g, '~0').replace(/\\//g, '~1')") : isNumber ? "'[' + " + expr + " + ']'" : "'[\\'' + " + expr + " + '\\']'";
      return joinPaths(currentPath, path);
    }
    function getPath(currentPath, prop, jsonPointers) {
      var path = jsonPointers ? toQuotedString("/" + escapeJsonPointer(prop)) : toQuotedString(getProperty(prop));
      return joinPaths(currentPath, path);
    }
    var JSON_POINTER = /^\/(?:[^~]|~0|~1)*$/;
    var RELATIVE_JSON_POINTER = /^([0-9]+)(#|\/(?:[^~]|~0|~1)*)?$/;
    function getData($data, lvl, paths) {
      var up, jsonPointer, data, matches;
      if ($data === "")
        return "rootData";
      if ($data[0] == "/") {
        if (!JSON_POINTER.test($data))
          throw new Error("Invalid JSON-pointer: " + $data);
        jsonPointer = $data;
        data = "rootData";
      } else {
        matches = $data.match(RELATIVE_JSON_POINTER);
        if (!matches)
          throw new Error("Invalid JSON-pointer: " + $data);
        up = +matches[1];
        jsonPointer = matches[2];
        if (jsonPointer == "#") {
          if (up >= lvl)
            throw new Error("Cannot access property/index " + up + " levels up, current level is " + lvl);
          return paths[lvl - up];
        }
        if (up > lvl)
          throw new Error("Cannot access data " + up + " levels up, current level is " + lvl);
        data = "data" + (lvl - up || "");
        if (!jsonPointer)
          return data;
      }
      var expr = data;
      var segments = jsonPointer.split("/");
      for (var i = 0; i < segments.length; i++) {
        var segment = segments[i];
        if (segment) {
          data += getProperty(unescapeJsonPointer(segment));
          expr += " && " + data;
        }
      }
      return expr;
    }
    function joinPaths(a, b) {
      if (a == '""')
        return b;
      return (a + " + " + b).replace(/([^\\])' \+ '/g, "$1");
    }
    function unescapeFragment(str) {
      return unescapeJsonPointer(decodeURIComponent(str));
    }
    function escapeFragment(str) {
      return encodeURIComponent(escapeJsonPointer(str));
    }
    function escapeJsonPointer(str) {
      return str.replace(/~/g, "~0").replace(/\//g, "~1");
    }
    function unescapeJsonPointer(str) {
      return str.replace(/~1/g, "/").replace(/~0/g, "~");
    }
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/schema_obj.js
var require_schema_obj = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/schema_obj.js"(exports2, module2) {
    "use strict";
    var util = require_util();
    module2.exports = SchemaObject;
    function SchemaObject(obj) {
      util.copy(obj, this);
    }
  }
});

// ../node_modules/.pnpm/json-schema-traverse@0.4.1/node_modules/json-schema-traverse/index.js
var require_json_schema_traverse = __commonJS({
  "../node_modules/.pnpm/json-schema-traverse@0.4.1/node_modules/json-schema-traverse/index.js"(exports2, module2) {
    "use strict";
    var traverse = module2.exports = function(schema, opts, cb) {
      if (typeof opts == "function") {
        cb = opts;
        opts = {};
      }
      cb = opts.cb || cb;
      var pre = typeof cb == "function" ? cb : cb.pre || function() {
      };
      var post = cb.post || function() {
      };
      _traverse(opts, pre, post, schema, "", schema);
    };
    traverse.keywords = {
      additionalItems: true,
      items: true,
      contains: true,
      additionalProperties: true,
      propertyNames: true,
      not: true
    };
    traverse.arrayKeywords = {
      items: true,
      allOf: true,
      anyOf: true,
      oneOf: true
    };
    traverse.propsKeywords = {
      definitions: true,
      properties: true,
      patternProperties: true,
      dependencies: true
    };
    traverse.skipKeywords = {
      default: true,
      enum: true,
      const: true,
      required: true,
      maximum: true,
      minimum: true,
      exclusiveMaximum: true,
      exclusiveMinimum: true,
      multipleOf: true,
      maxLength: true,
      minLength: true,
      pattern: true,
      format: true,
      maxItems: true,
      minItems: true,
      uniqueItems: true,
      maxProperties: true,
      minProperties: true
    };
    function _traverse(opts, pre, post, schema, jsonPtr, rootSchema, parentJsonPtr, parentKeyword, parentSchema, keyIndex) {
      if (schema && typeof schema == "object" && !Array.isArray(schema)) {
        pre(schema, jsonPtr, rootSchema, parentJsonPtr, parentKeyword, parentSchema, keyIndex);
        for (var key in schema) {
          var sch = schema[key];
          if (Array.isArray(sch)) {
            if (key in traverse.arrayKeywords) {
              for (var i = 0; i < sch.length; i++)
                _traverse(opts, pre, post, sch[i], jsonPtr + "/" + key + "/" + i, rootSchema, jsonPtr, key, schema, i);
            }
          } else if (key in traverse.propsKeywords) {
            if (sch && typeof sch == "object") {
              for (var prop in sch)
                _traverse(opts, pre, post, sch[prop], jsonPtr + "/" + key + "/" + escapeJsonPtr(prop), rootSchema, jsonPtr, key, schema, prop);
            }
          } else if (key in traverse.keywords || opts.allKeys && !(key in traverse.skipKeywords)) {
            _traverse(opts, pre, post, sch, jsonPtr + "/" + key, rootSchema, jsonPtr, key, schema);
          }
        }
        post(schema, jsonPtr, rootSchema, parentJsonPtr, parentKeyword, parentSchema, keyIndex);
      }
    }
    function escapeJsonPtr(str) {
      return str.replace(/~/g, "~0").replace(/\//g, "~1");
    }
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/resolve.js
var require_resolve = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/resolve.js"(exports2, module2) {
    "use strict";
    var URI = require_uri_all();
    var equal = require_fast_deep_equal();
    var util = require_util();
    var SchemaObject = require_schema_obj();
    var traverse = require_json_schema_traverse();
    module2.exports = resolve;
    resolve.normalizeId = normalizeId;
    resolve.fullPath = getFullPath;
    resolve.url = resolveUrl;
    resolve.ids = resolveIds;
    resolve.inlineRef = inlineRef;
    resolve.schema = resolveSchema;
    function resolve(compile, root, ref) {
      var refVal = this._refs[ref];
      if (typeof refVal == "string") {
        if (this._refs[refVal])
          refVal = this._refs[refVal];
        else
          return resolve.call(this, compile, root, refVal);
      }
      refVal = refVal || this._schemas[ref];
      if (refVal instanceof SchemaObject) {
        return inlineRef(refVal.schema, this._opts.inlineRefs) ? refVal.schema : refVal.validate || this._compile(refVal);
      }
      var res = resolveSchema.call(this, root, ref);
      var schema, v, baseId;
      if (res) {
        schema = res.schema;
        root = res.root;
        baseId = res.baseId;
      }
      if (schema instanceof SchemaObject) {
        v = schema.validate || compile.call(this, schema.schema, root, void 0, baseId);
      } else if (schema !== void 0) {
        v = inlineRef(schema, this._opts.inlineRefs) ? schema : compile.call(this, schema, root, void 0, baseId);
      }
      return v;
    }
    function resolveSchema(root, ref) {
      var p = URI.parse(ref), refPath = _getFullPath(p), baseId = getFullPath(this._getId(root.schema));
      if (Object.keys(root.schema).length === 0 || refPath !== baseId) {
        var id = normalizeId(refPath);
        var refVal = this._refs[id];
        if (typeof refVal == "string") {
          return resolveRecursive.call(this, root, refVal, p);
        } else if (refVal instanceof SchemaObject) {
          if (!refVal.validate)
            this._compile(refVal);
          root = refVal;
        } else {
          refVal = this._schemas[id];
          if (refVal instanceof SchemaObject) {
            if (!refVal.validate)
              this._compile(refVal);
            if (id == normalizeId(ref))
              return { schema: refVal, root, baseId };
            root = refVal;
          } else {
            return;
          }
        }
        if (!root.schema)
          return;
        baseId = getFullPath(this._getId(root.schema));
      }
      return getJsonPointer.call(this, p, baseId, root.schema, root);
    }
    function resolveRecursive(root, ref, parsedRef) {
      var res = resolveSchema.call(this, root, ref);
      if (res) {
        var schema = res.schema;
        var baseId = res.baseId;
        root = res.root;
        var id = this._getId(schema);
        if (id)
          baseId = resolveUrl(baseId, id);
        return getJsonPointer.call(this, parsedRef, baseId, schema, root);
      }
    }
    var PREVENT_SCOPE_CHANGE = util.toHash(["properties", "patternProperties", "enum", "dependencies", "definitions"]);
    function getJsonPointer(parsedRef, baseId, schema, root) {
      parsedRef.fragment = parsedRef.fragment || "";
      if (parsedRef.fragment.slice(0, 1) != "/")
        return;
      var parts = parsedRef.fragment.split("/");
      for (var i = 1; i < parts.length; i++) {
        var part = parts[i];
        if (part) {
          part = util.unescapeFragment(part);
          schema = schema[part];
          if (schema === void 0)
            break;
          var id;
          if (!PREVENT_SCOPE_CHANGE[part]) {
            id = this._getId(schema);
            if (id)
              baseId = resolveUrl(baseId, id);
            if (schema.$ref) {
              var $ref = resolveUrl(baseId, schema.$ref);
              var res = resolveSchema.call(this, root, $ref);
              if (res) {
                schema = res.schema;
                root = res.root;
                baseId = res.baseId;
              }
            }
          }
        }
      }
      if (schema !== void 0 && schema !== root.schema)
        return { schema, root, baseId };
    }
    var SIMPLE_INLINED = util.toHash([
      "type",
      "format",
      "pattern",
      "maxLength",
      "minLength",
      "maxProperties",
      "minProperties",
      "maxItems",
      "minItems",
      "maximum",
      "minimum",
      "uniqueItems",
      "multipleOf",
      "required",
      "enum"
    ]);
    function inlineRef(schema, limit) {
      if (limit === false)
        return false;
      if (limit === void 0 || limit === true)
        return checkNoRef(schema);
      else if (limit)
        return countKeys(schema) <= limit;
    }
    function checkNoRef(schema) {
      var item;
      if (Array.isArray(schema)) {
        for (var i = 0; i < schema.length; i++) {
          item = schema[i];
          if (typeof item == "object" && !checkNoRef(item))
            return false;
        }
      } else {
        for (var key in schema) {
          if (key == "$ref")
            return false;
          item = schema[key];
          if (typeof item == "object" && !checkNoRef(item))
            return false;
        }
      }
      return true;
    }
    function countKeys(schema) {
      var count = 0, item;
      if (Array.isArray(schema)) {
        for (var i = 0; i < schema.length; i++) {
          item = schema[i];
          if (typeof item == "object")
            count += countKeys(item);
          if (count == Infinity)
            return Infinity;
        }
      } else {
        for (var key in schema) {
          if (key == "$ref")
            return Infinity;
          if (SIMPLE_INLINED[key]) {
            count++;
          } else {
            item = schema[key];
            if (typeof item == "object")
              count += countKeys(item) + 1;
            if (count == Infinity)
              return Infinity;
          }
        }
      }
      return count;
    }
    function getFullPath(id, normalize) {
      if (normalize !== false)
        id = normalizeId(id);
      var p = URI.parse(id);
      return _getFullPath(p);
    }
    function _getFullPath(p) {
      return URI.serialize(p).split("#")[0] + "#";
    }
    var TRAILING_SLASH_HASH = /#\/?$/;
    function normalizeId(id) {
      return id ? id.replace(TRAILING_SLASH_HASH, "") : "";
    }
    function resolveUrl(baseId, id) {
      id = normalizeId(id);
      return URI.resolve(baseId, id);
    }
    function resolveIds(schema) {
      var schemaId = normalizeId(this._getId(schema));
      var baseIds = { "": schemaId };
      var fullPaths = { "": getFullPath(schemaId, false) };
      var localRefs = {};
      var self2 = this;
      traverse(schema, { allKeys: true }, function(sch, jsonPtr, rootSchema, parentJsonPtr, parentKeyword, parentSchema, keyIndex) {
        if (jsonPtr === "")
          return;
        var id = self2._getId(sch);
        var baseId = baseIds[parentJsonPtr];
        var fullPath = fullPaths[parentJsonPtr] + "/" + parentKeyword;
        if (keyIndex !== void 0)
          fullPath += "/" + (typeof keyIndex == "number" ? keyIndex : util.escapeFragment(keyIndex));
        if (typeof id == "string") {
          id = baseId = normalizeId(baseId ? URI.resolve(baseId, id) : id);
          var refVal = self2._refs[id];
          if (typeof refVal == "string")
            refVal = self2._refs[refVal];
          if (refVal && refVal.schema) {
            if (!equal(sch, refVal.schema))
              throw new Error('id "' + id + '" resolves to more than one schema');
          } else if (id != normalizeId(fullPath)) {
            if (id[0] == "#") {
              if (localRefs[id] && !equal(sch, localRefs[id]))
                throw new Error('id "' + id + '" resolves to more than one schema');
              localRefs[id] = sch;
            } else {
              self2._refs[id] = fullPath;
            }
          }
        }
        baseIds[jsonPtr] = baseId;
        fullPaths[jsonPtr] = fullPath;
      });
      return localRefs;
    }
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/error_classes.js
var require_error_classes = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/error_classes.js"(exports2, module2) {
    "use strict";
    var resolve = require_resolve();
    module2.exports = {
      Validation: errorSubclass(ValidationError),
      MissingRef: errorSubclass(MissingRefError)
    };
    function ValidationError(errors) {
      this.message = "validation failed";
      this.errors = errors;
      this.ajv = this.validation = true;
    }
    MissingRefError.message = function(baseId, ref) {
      return "can't resolve reference " + ref + " from id " + baseId;
    };
    function MissingRefError(baseId, ref, message) {
      this.message = message || MissingRefError.message(baseId, ref);
      this.missingRef = resolve.url(baseId, ref);
      this.missingSchema = resolve.normalizeId(resolve.fullPath(this.missingRef));
    }
    function errorSubclass(Subclass) {
      Subclass.prototype = Object.create(Error.prototype);
      Subclass.prototype.constructor = Subclass;
      return Subclass;
    }
  }
});

// ../node_modules/.pnpm/fast-json-stable-stringify@2.1.0/node_modules/fast-json-stable-stringify/index.js
var require_fast_json_stable_stringify = __commonJS({
  "../node_modules/.pnpm/fast-json-stable-stringify@2.1.0/node_modules/fast-json-stable-stringify/index.js"(exports2, module2) {
    "use strict";
    module2.exports = function(data, opts) {
      if (!opts)
        opts = {};
      if (typeof opts === "function")
        opts = { cmp: opts };
      var cycles = typeof opts.cycles === "boolean" ? opts.cycles : false;
      var cmp = opts.cmp && /* @__PURE__ */ function(f) {
        return function(node) {
          return function(a, b) {
            var aobj = { key: a, value: node[a] };
            var bobj = { key: b, value: node[b] };
            return f(aobj, bobj);
          };
        };
      }(opts.cmp);
      var seen = [];
      return function stringify(node) {
        if (node && node.toJSON && typeof node.toJSON === "function") {
          node = node.toJSON();
        }
        if (node === void 0)
          return;
        if (typeof node == "number")
          return isFinite(node) ? "" + node : "null";
        if (typeof node !== "object")
          return JSON.stringify(node);
        var i, out;
        if (Array.isArray(node)) {
          out = "[";
          for (i = 0; i < node.length; i++) {
            if (i)
              out += ",";
            out += stringify(node[i]) || "null";
          }
          return out + "]";
        }
        if (node === null)
          return "null";
        if (seen.indexOf(node) !== -1) {
          if (cycles)
            return JSON.stringify("__cycle__");
          throw new TypeError("Converting circular structure to JSON");
        }
        var seenIndex = seen.push(node) - 1;
        var keys = Object.keys(node).sort(cmp && cmp(node));
        out = "";
        for (i = 0; i < keys.length; i++) {
          var key = keys[i];
          var value = stringify(node[key]);
          if (!value)
            continue;
          if (out)
            out += ",";
          out += JSON.stringify(key) + ":" + value;
        }
        seen.splice(seenIndex, 1);
        return "{" + out + "}";
      }(data);
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/validate.js
var require_validate = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/validate.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_validate(it, $keyword, $ruleType) {
      var out = "";
      var $async = it.schema.$async === true, $refKeywords = it.util.schemaHasRulesExcept(it.schema, it.RULES.all, "$ref"), $id = it.self._getId(it.schema);
      if (it.opts.strictKeywords) {
        var $unknownKwd = it.util.schemaUnknownRules(it.schema, it.RULES.keywords);
        if ($unknownKwd) {
          var $keywordsMsg = "unknown keyword: " + $unknownKwd;
          if (it.opts.strictKeywords === "log")
            it.logger.warn($keywordsMsg);
          else
            throw new Error($keywordsMsg);
        }
      }
      if (it.isTop) {
        out += " var validate = ";
        if ($async) {
          it.async = true;
          out += "async ";
        }
        out += "function(data, dataPath, parentData, parentDataProperty, rootData) { 'use strict'; ";
        if ($id && (it.opts.sourceCode || it.opts.processCode)) {
          out += " " + ("/*# sourceURL=" + $id + " */") + " ";
        }
      }
      if (typeof it.schema == "boolean" || !($refKeywords || it.schema.$ref)) {
        var $keyword = "false schema";
        var $lvl = it.level;
        var $dataLvl = it.dataLevel;
        var $schema = it.schema[$keyword];
        var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
        var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
        var $breakOnError = !it.opts.allErrors;
        var $errorKeyword;
        var $data = "data" + ($dataLvl || "");
        var $valid = "valid" + $lvl;
        if (it.schema === false) {
          if (it.isTop) {
            $breakOnError = true;
          } else {
            out += " var " + $valid + " = false; ";
          }
          var $$outStack = $$outStack || [];
          $$outStack.push(out);
          out = "";
          if (it.createErrors !== false) {
            out += " { keyword: '" + ($errorKeyword || "false schema") + "' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: {} ";
            if (it.opts.messages !== false) {
              out += " , message: 'boolean schema is false' ";
            }
            if (it.opts.verbose) {
              out += " , schema: false , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
            }
            out += " } ";
          } else {
            out += " {} ";
          }
          var __err = out;
          out = $$outStack.pop();
          if (!it.compositeRule && $breakOnError) {
            if (it.async) {
              out += " throw new ValidationError([" + __err + "]); ";
            } else {
              out += " validate.errors = [" + __err + "]; return false; ";
            }
          } else {
            out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
          }
        } else {
          if (it.isTop) {
            if ($async) {
              out += " return data; ";
            } else {
              out += " validate.errors = null; return true; ";
            }
          } else {
            out += " var " + $valid + " = true; ";
          }
        }
        if (it.isTop) {
          out += " }; return validate; ";
        }
        return out;
      }
      if (it.isTop) {
        var $top = it.isTop, $lvl = it.level = 0, $dataLvl = it.dataLevel = 0, $data = "data";
        it.rootId = it.resolve.fullPath(it.self._getId(it.root.schema));
        it.baseId = it.baseId || it.rootId;
        delete it.isTop;
        it.dataPathArr = [""];
        if (it.schema.default !== void 0 && it.opts.useDefaults && it.opts.strictDefaults) {
          var $defaultMsg = "default is ignored in the schema root";
          if (it.opts.strictDefaults === "log")
            it.logger.warn($defaultMsg);
          else
            throw new Error($defaultMsg);
        }
        out += " var vErrors = null; ";
        out += " var errors = 0;     ";
        out += " if (rootData === undefined) rootData = data; ";
      } else {
        var $lvl = it.level, $dataLvl = it.dataLevel, $data = "data" + ($dataLvl || "");
        if ($id)
          it.baseId = it.resolve.url(it.baseId, $id);
        if ($async && !it.async)
          throw new Error("async schema in sync schema");
        out += " var errs_" + $lvl + " = errors;";
      }
      var $valid = "valid" + $lvl, $breakOnError = !it.opts.allErrors, $closingBraces1 = "", $closingBraces2 = "";
      var $errorKeyword;
      var $typeSchema = it.schema.type, $typeIsArray = Array.isArray($typeSchema);
      if ($typeSchema && it.opts.nullable && it.schema.nullable === true) {
        if ($typeIsArray) {
          if ($typeSchema.indexOf("null") == -1)
            $typeSchema = $typeSchema.concat("null");
        } else if ($typeSchema != "null") {
          $typeSchema = [$typeSchema, "null"];
          $typeIsArray = true;
        }
      }
      if ($typeIsArray && $typeSchema.length == 1) {
        $typeSchema = $typeSchema[0];
        $typeIsArray = false;
      }
      if (it.schema.$ref && $refKeywords) {
        if (it.opts.extendRefs == "fail") {
          throw new Error('$ref: validation keywords used in schema at path "' + it.errSchemaPath + '" (see option extendRefs)');
        } else if (it.opts.extendRefs !== true) {
          $refKeywords = false;
          it.logger.warn('$ref: keywords ignored in schema at path "' + it.errSchemaPath + '"');
        }
      }
      if (it.schema.$comment && it.opts.$comment) {
        out += " " + it.RULES.all.$comment.code(it, "$comment");
      }
      if ($typeSchema) {
        if (it.opts.coerceTypes) {
          var $coerceToTypes = it.util.coerceToTypes(it.opts.coerceTypes, $typeSchema);
        }
        var $rulesGroup = it.RULES.types[$typeSchema];
        if ($coerceToTypes || $typeIsArray || $rulesGroup === true || $rulesGroup && !$shouldUseGroup($rulesGroup)) {
          var $schemaPath = it.schemaPath + ".type", $errSchemaPath = it.errSchemaPath + "/type";
          var $schemaPath = it.schemaPath + ".type", $errSchemaPath = it.errSchemaPath + "/type", $method = $typeIsArray ? "checkDataTypes" : "checkDataType";
          out += " if (" + it.util[$method]($typeSchema, $data, it.opts.strictNumbers, true) + ") { ";
          if ($coerceToTypes) {
            var $dataType = "dataType" + $lvl, $coerced = "coerced" + $lvl;
            out += " var " + $dataType + " = typeof " + $data + "; var " + $coerced + " = undefined; ";
            if (it.opts.coerceTypes == "array") {
              out += " if (" + $dataType + " == 'object' && Array.isArray(" + $data + ") && " + $data + ".length == 1) { " + $data + " = " + $data + "[0]; " + $dataType + " = typeof " + $data + "; if (" + it.util.checkDataType(it.schema.type, $data, it.opts.strictNumbers) + ") " + $coerced + " = " + $data + "; } ";
            }
            out += " if (" + $coerced + " !== undefined) ; ";
            var arr1 = $coerceToTypes;
            if (arr1) {
              var $type, $i = -1, l1 = arr1.length - 1;
              while ($i < l1) {
                $type = arr1[$i += 1];
                if ($type == "string") {
                  out += " else if (" + $dataType + " == 'number' || " + $dataType + " == 'boolean') " + $coerced + " = '' + " + $data + "; else if (" + $data + " === null) " + $coerced + " = ''; ";
                } else if ($type == "number" || $type == "integer") {
                  out += " else if (" + $dataType + " == 'boolean' || " + $data + " === null || (" + $dataType + " == 'string' && " + $data + " && " + $data + " == +" + $data + " ";
                  if ($type == "integer") {
                    out += " && !(" + $data + " % 1)";
                  }
                  out += ")) " + $coerced + " = +" + $data + "; ";
                } else if ($type == "boolean") {
                  out += " else if (" + $data + " === 'false' || " + $data + " === 0 || " + $data + " === null) " + $coerced + " = false; else if (" + $data + " === 'true' || " + $data + " === 1) " + $coerced + " = true; ";
                } else if ($type == "null") {
                  out += " else if (" + $data + " === '' || " + $data + " === 0 || " + $data + " === false) " + $coerced + " = null; ";
                } else if (it.opts.coerceTypes == "array" && $type == "array") {
                  out += " else if (" + $dataType + " == 'string' || " + $dataType + " == 'number' || " + $dataType + " == 'boolean' || " + $data + " == null) " + $coerced + " = [" + $data + "]; ";
                }
              }
            }
            out += " else {   ";
            var $$outStack = $$outStack || [];
            $$outStack.push(out);
            out = "";
            if (it.createErrors !== false) {
              out += " { keyword: '" + ($errorKeyword || "type") + "' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { type: '";
              if ($typeIsArray) {
                out += "" + $typeSchema.join(",");
              } else {
                out += "" + $typeSchema;
              }
              out += "' } ";
              if (it.opts.messages !== false) {
                out += " , message: 'should be ";
                if ($typeIsArray) {
                  out += "" + $typeSchema.join(",");
                } else {
                  out += "" + $typeSchema;
                }
                out += "' ";
              }
              if (it.opts.verbose) {
                out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
              }
              out += " } ";
            } else {
              out += " {} ";
            }
            var __err = out;
            out = $$outStack.pop();
            if (!it.compositeRule && $breakOnError) {
              if (it.async) {
                out += " throw new ValidationError([" + __err + "]); ";
              } else {
                out += " validate.errors = [" + __err + "]; return false; ";
              }
            } else {
              out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
            }
            out += " } if (" + $coerced + " !== undefined) {  ";
            var $parentData = $dataLvl ? "data" + ($dataLvl - 1 || "") : "parentData", $parentDataProperty = $dataLvl ? it.dataPathArr[$dataLvl] : "parentDataProperty";
            out += " " + $data + " = " + $coerced + "; ";
            if (!$dataLvl) {
              out += "if (" + $parentData + " !== undefined)";
            }
            out += " " + $parentData + "[" + $parentDataProperty + "] = " + $coerced + "; } ";
          } else {
            var $$outStack = $$outStack || [];
            $$outStack.push(out);
            out = "";
            if (it.createErrors !== false) {
              out += " { keyword: '" + ($errorKeyword || "type") + "' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { type: '";
              if ($typeIsArray) {
                out += "" + $typeSchema.join(",");
              } else {
                out += "" + $typeSchema;
              }
              out += "' } ";
              if (it.opts.messages !== false) {
                out += " , message: 'should be ";
                if ($typeIsArray) {
                  out += "" + $typeSchema.join(",");
                } else {
                  out += "" + $typeSchema;
                }
                out += "' ";
              }
              if (it.opts.verbose) {
                out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
              }
              out += " } ";
            } else {
              out += " {} ";
            }
            var __err = out;
            out = $$outStack.pop();
            if (!it.compositeRule && $breakOnError) {
              if (it.async) {
                out += " throw new ValidationError([" + __err + "]); ";
              } else {
                out += " validate.errors = [" + __err + "]; return false; ";
              }
            } else {
              out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
            }
          }
          out += " } ";
        }
      }
      if (it.schema.$ref && !$refKeywords) {
        out += " " + it.RULES.all.$ref.code(it, "$ref") + " ";
        if ($breakOnError) {
          out += " } if (errors === ";
          if ($top) {
            out += "0";
          } else {
            out += "errs_" + $lvl;
          }
          out += ") { ";
          $closingBraces2 += "}";
        }
      } else {
        var arr2 = it.RULES;
        if (arr2) {
          var $rulesGroup, i2 = -1, l2 = arr2.length - 1;
          while (i2 < l2) {
            $rulesGroup = arr2[i2 += 1];
            if ($shouldUseGroup($rulesGroup)) {
              if ($rulesGroup.type) {
                out += " if (" + it.util.checkDataType($rulesGroup.type, $data, it.opts.strictNumbers) + ") { ";
              }
              if (it.opts.useDefaults) {
                if ($rulesGroup.type == "object" && it.schema.properties) {
                  var $schema = it.schema.properties, $schemaKeys = Object.keys($schema);
                  var arr3 = $schemaKeys;
                  if (arr3) {
                    var $propertyKey, i3 = -1, l3 = arr3.length - 1;
                    while (i3 < l3) {
                      $propertyKey = arr3[i3 += 1];
                      var $sch = $schema[$propertyKey];
                      if ($sch.default !== void 0) {
                        var $passData = $data + it.util.getProperty($propertyKey);
                        if (it.compositeRule) {
                          if (it.opts.strictDefaults) {
                            var $defaultMsg = "default is ignored for: " + $passData;
                            if (it.opts.strictDefaults === "log")
                              it.logger.warn($defaultMsg);
                            else
                              throw new Error($defaultMsg);
                          }
                        } else {
                          out += " if (" + $passData + " === undefined ";
                          if (it.opts.useDefaults == "empty") {
                            out += " || " + $passData + " === null || " + $passData + " === '' ";
                          }
                          out += " ) " + $passData + " = ";
                          if (it.opts.useDefaults == "shared") {
                            out += " " + it.useDefault($sch.default) + " ";
                          } else {
                            out += " " + JSON.stringify($sch.default) + " ";
                          }
                          out += "; ";
                        }
                      }
                    }
                  }
                } else if ($rulesGroup.type == "array" && Array.isArray(it.schema.items)) {
                  var arr4 = it.schema.items;
                  if (arr4) {
                    var $sch, $i = -1, l4 = arr4.length - 1;
                    while ($i < l4) {
                      $sch = arr4[$i += 1];
                      if ($sch.default !== void 0) {
                        var $passData = $data + "[" + $i + "]";
                        if (it.compositeRule) {
                          if (it.opts.strictDefaults) {
                            var $defaultMsg = "default is ignored for: " + $passData;
                            if (it.opts.strictDefaults === "log")
                              it.logger.warn($defaultMsg);
                            else
                              throw new Error($defaultMsg);
                          }
                        } else {
                          out += " if (" + $passData + " === undefined ";
                          if (it.opts.useDefaults == "empty") {
                            out += " || " + $passData + " === null || " + $passData + " === '' ";
                          }
                          out += " ) " + $passData + " = ";
                          if (it.opts.useDefaults == "shared") {
                            out += " " + it.useDefault($sch.default) + " ";
                          } else {
                            out += " " + JSON.stringify($sch.default) + " ";
                          }
                          out += "; ";
                        }
                      }
                    }
                  }
                }
              }
              var arr5 = $rulesGroup.rules;
              if (arr5) {
                var $rule, i5 = -1, l5 = arr5.length - 1;
                while (i5 < l5) {
                  $rule = arr5[i5 += 1];
                  if ($shouldUseRule($rule)) {
                    var $code = $rule.code(it, $rule.keyword, $rulesGroup.type);
                    if ($code) {
                      out += " " + $code + " ";
                      if ($breakOnError) {
                        $closingBraces1 += "}";
                      }
                    }
                  }
                }
              }
              if ($breakOnError) {
                out += " " + $closingBraces1 + " ";
                $closingBraces1 = "";
              }
              if ($rulesGroup.type) {
                out += " } ";
                if ($typeSchema && $typeSchema === $rulesGroup.type && !$coerceToTypes) {
                  out += " else { ";
                  var $schemaPath = it.schemaPath + ".type", $errSchemaPath = it.errSchemaPath + "/type";
                  var $$outStack = $$outStack || [];
                  $$outStack.push(out);
                  out = "";
                  if (it.createErrors !== false) {
                    out += " { keyword: '" + ($errorKeyword || "type") + "' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { type: '";
                    if ($typeIsArray) {
                      out += "" + $typeSchema.join(",");
                    } else {
                      out += "" + $typeSchema;
                    }
                    out += "' } ";
                    if (it.opts.messages !== false) {
                      out += " , message: 'should be ";
                      if ($typeIsArray) {
                        out += "" + $typeSchema.join(",");
                      } else {
                        out += "" + $typeSchema;
                      }
                      out += "' ";
                    }
                    if (it.opts.verbose) {
                      out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
                    }
                    out += " } ";
                  } else {
                    out += " {} ";
                  }
                  var __err = out;
                  out = $$outStack.pop();
                  if (!it.compositeRule && $breakOnError) {
                    if (it.async) {
                      out += " throw new ValidationError([" + __err + "]); ";
                    } else {
                      out += " validate.errors = [" + __err + "]; return false; ";
                    }
                  } else {
                    out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
                  }
                  out += " } ";
                }
              }
              if ($breakOnError) {
                out += " if (errors === ";
                if ($top) {
                  out += "0";
                } else {
                  out += "errs_" + $lvl;
                }
                out += ") { ";
                $closingBraces2 += "}";
              }
            }
          }
        }
      }
      if ($breakOnError) {
        out += " " + $closingBraces2 + " ";
      }
      if ($top) {
        if ($async) {
          out += " if (errors === 0) return data;           ";
          out += " else throw new ValidationError(vErrors); ";
        } else {
          out += " validate.errors = vErrors; ";
          out += " return errors === 0;       ";
        }
        out += " }; return validate;";
      } else {
        out += " var " + $valid + " = errors === errs_" + $lvl + ";";
      }
      function $shouldUseGroup($rulesGroup2) {
        var rules = $rulesGroup2.rules;
        for (var i = 0; i < rules.length; i++)
          if ($shouldUseRule(rules[i]))
            return true;
      }
      function $shouldUseRule($rule2) {
        return it.schema[$rule2.keyword] !== void 0 || $rule2.implements && $ruleImplementsSomeKeyword($rule2);
      }
      function $ruleImplementsSomeKeyword($rule2) {
        var impl = $rule2.implements;
        for (var i = 0; i < impl.length; i++)
          if (it.schema[impl[i]] !== void 0)
            return true;
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/index.js
var require_compile = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/index.js"(exports2, module2) {
    "use strict";
    var resolve = require_resolve();
    var util = require_util();
    var errorClasses = require_error_classes();
    var stableStringify = require_fast_json_stable_stringify();
    var validateGenerator = require_validate();
    var ucs2length = util.ucs2length;
    var equal = require_fast_deep_equal();
    var ValidationError = errorClasses.Validation;
    module2.exports = compile;
    function compile(schema, root, localRefs, baseId) {
      var self2 = this, opts = this._opts, refVal = [void 0], refs = {}, patterns = [], patternsHash = {}, defaults = [], defaultsHash = {}, customRules = [];
      root = root || { schema, refVal, refs };
      var c = checkCompiling.call(this, schema, root, baseId);
      var compilation = this._compilations[c.index];
      if (c.compiling)
        return compilation.callValidate = callValidate;
      var formats = this._formats;
      var RULES = this.RULES;
      try {
        var v = localCompile(schema, root, localRefs, baseId);
        compilation.validate = v;
        var cv = compilation.callValidate;
        if (cv) {
          cv.schema = v.schema;
          cv.errors = null;
          cv.refs = v.refs;
          cv.refVal = v.refVal;
          cv.root = v.root;
          cv.$async = v.$async;
          if (opts.sourceCode)
            cv.source = v.source;
        }
        return v;
      } finally {
        endCompiling.call(this, schema, root, baseId);
      }
      function callValidate() {
        var validate = compilation.validate;
        var result = validate.apply(this, arguments);
        callValidate.errors = validate.errors;
        return result;
      }
      function localCompile(_schema, _root, localRefs2, baseId2) {
        var isRoot = !_root || _root && _root.schema == _schema;
        if (_root.schema != root.schema)
          return compile.call(self2, _schema, _root, localRefs2, baseId2);
        var $async = _schema.$async === true;
        var sourceCode = validateGenerator({
          isTop: true,
          schema: _schema,
          isRoot,
          baseId: baseId2,
          root: _root,
          schemaPath: "",
          errSchemaPath: "#",
          errorPath: '""',
          MissingRefError: errorClasses.MissingRef,
          RULES,
          validate: validateGenerator,
          util,
          resolve,
          resolveRef,
          usePattern,
          useDefault,
          useCustomRule,
          opts,
          formats,
          logger: self2.logger,
          self: self2
        });
        sourceCode = vars(refVal, refValCode) + vars(patterns, patternCode) + vars(defaults, defaultCode) + vars(customRules, customRuleCode) + sourceCode;
        if (opts.processCode)
          sourceCode = opts.processCode(sourceCode, _schema);
        var validate;
        try {
          var makeValidate = new Function(
            "self",
            "RULES",
            "formats",
            "root",
            "refVal",
            "defaults",
            "customRules",
            "equal",
            "ucs2length",
            "ValidationError",
            sourceCode
          );
          validate = makeValidate(
            self2,
            RULES,
            formats,
            root,
            refVal,
            defaults,
            customRules,
            equal,
            ucs2length,
            ValidationError
          );
          refVal[0] = validate;
        } catch (e) {
          self2.logger.error("Error compiling schema, function code:", sourceCode);
          throw e;
        }
        validate.schema = _schema;
        validate.errors = null;
        validate.refs = refs;
        validate.refVal = refVal;
        validate.root = isRoot ? validate : _root;
        if ($async)
          validate.$async = true;
        if (opts.sourceCode === true) {
          validate.source = {
            code: sourceCode,
            patterns,
            defaults
          };
        }
        return validate;
      }
      function resolveRef(baseId2, ref, isRoot) {
        ref = resolve.url(baseId2, ref);
        var refIndex = refs[ref];
        var _refVal, refCode;
        if (refIndex !== void 0) {
          _refVal = refVal[refIndex];
          refCode = "refVal[" + refIndex + "]";
          return resolvedRef(_refVal, refCode);
        }
        if (!isRoot && root.refs) {
          var rootRefId = root.refs[ref];
          if (rootRefId !== void 0) {
            _refVal = root.refVal[rootRefId];
            refCode = addLocalRef(ref, _refVal);
            return resolvedRef(_refVal, refCode);
          }
        }
        refCode = addLocalRef(ref);
        var v2 = resolve.call(self2, localCompile, root, ref);
        if (v2 === void 0) {
          var localSchema = localRefs && localRefs[ref];
          if (localSchema) {
            v2 = resolve.inlineRef(localSchema, opts.inlineRefs) ? localSchema : compile.call(self2, localSchema, root, localRefs, baseId2);
          }
        }
        if (v2 === void 0) {
          removeLocalRef(ref);
        } else {
          replaceLocalRef(ref, v2);
          return resolvedRef(v2, refCode);
        }
      }
      function addLocalRef(ref, v2) {
        var refId = refVal.length;
        refVal[refId] = v2;
        refs[ref] = refId;
        return "refVal" + refId;
      }
      function removeLocalRef(ref) {
        delete refs[ref];
      }
      function replaceLocalRef(ref, v2) {
        var refId = refs[ref];
        refVal[refId] = v2;
      }
      function resolvedRef(refVal2, code2) {
        return typeof refVal2 == "object" || typeof refVal2 == "boolean" ? { code: code2, schema: refVal2, inline: true } : { code: code2, $async: refVal2 && !!refVal2.$async };
      }
      function usePattern(regexStr) {
        var index = patternsHash[regexStr];
        if (index === void 0) {
          index = patternsHash[regexStr] = patterns.length;
          patterns[index] = regexStr;
        }
        return "pattern" + index;
      }
      function useDefault(value) {
        switch (typeof value) {
          case "boolean":
          case "number":
            return "" + value;
          case "string":
            return util.toQuotedString(value);
          case "object":
            if (value === null)
              return "null";
            var valueStr = stableStringify(value);
            var index = defaultsHash[valueStr];
            if (index === void 0) {
              index = defaultsHash[valueStr] = defaults.length;
              defaults[index] = value;
            }
            return "default" + index;
        }
      }
      function useCustomRule(rule, schema2, parentSchema, it) {
        if (self2._opts.validateSchema !== false) {
          var deps = rule.definition.dependencies;
          if (deps && !deps.every(function(keyword) {
            return Object.prototype.hasOwnProperty.call(parentSchema, keyword);
          }))
            throw new Error("parent schema must have all required keywords: " + deps.join(","));
          var validateSchema = rule.definition.validateSchema;
          if (validateSchema) {
            var valid = validateSchema(schema2);
            if (!valid) {
              var message = "keyword schema is invalid: " + self2.errorsText(validateSchema.errors);
              if (self2._opts.validateSchema == "log")
                self2.logger.error(message);
              else
                throw new Error(message);
            }
          }
        }
        var compile2 = rule.definition.compile, inline = rule.definition.inline, macro = rule.definition.macro;
        var validate;
        if (compile2) {
          validate = compile2.call(self2, schema2, parentSchema, it);
        } else if (macro) {
          validate = macro.call(self2, schema2, parentSchema, it);
          if (opts.validateSchema !== false)
            self2.validateSchema(validate, true);
        } else if (inline) {
          validate = inline.call(self2, it, rule.keyword, schema2, parentSchema);
        } else {
          validate = rule.definition.validate;
          if (!validate)
            return;
        }
        if (validate === void 0)
          throw new Error('custom keyword "' + rule.keyword + '"failed to compile');
        var index = customRules.length;
        customRules[index] = validate;
        return {
          code: "customRule" + index,
          validate
        };
      }
    }
    function checkCompiling(schema, root, baseId) {
      var index = compIndex.call(this, schema, root, baseId);
      if (index >= 0)
        return { index, compiling: true };
      index = this._compilations.length;
      this._compilations[index] = {
        schema,
        root,
        baseId
      };
      return { index, compiling: false };
    }
    function endCompiling(schema, root, baseId) {
      var i = compIndex.call(this, schema, root, baseId);
      if (i >= 0)
        this._compilations.splice(i, 1);
    }
    function compIndex(schema, root, baseId) {
      for (var i = 0; i < this._compilations.length; i++) {
        var c = this._compilations[i];
        if (c.schema == schema && c.root == root && c.baseId == baseId)
          return i;
      }
      return -1;
    }
    function patternCode(i, patterns) {
      return "var pattern" + i + " = new RegExp(" + util.toQuotedString(patterns[i]) + ");";
    }
    function defaultCode(i) {
      return "var default" + i + " = defaults[" + i + "];";
    }
    function refValCode(i, refVal) {
      return refVal[i] === void 0 ? "" : "var refVal" + i + " = refVal[" + i + "];";
    }
    function customRuleCode(i) {
      return "var customRule" + i + " = customRules[" + i + "];";
    }
    function vars(arr, statement) {
      if (!arr.length)
        return "";
      var code2 = "";
      for (var i = 0; i < arr.length; i++)
        code2 += statement(i, arr);
      return code2;
    }
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/cache.js
var require_cache = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/cache.js"(exports2, module2) {
    "use strict";
    var Cache = module2.exports = function Cache2() {
      this._cache = {};
    };
    Cache.prototype.put = function Cache_put(key, value) {
      this._cache[key] = value;
    };
    Cache.prototype.get = function Cache_get(key) {
      return this._cache[key];
    };
    Cache.prototype.del = function Cache_del(key) {
      delete this._cache[key];
    };
    Cache.prototype.clear = function Cache_clear() {
      this._cache = {};
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/formats.js
var require_formats = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/formats.js"(exports2, module2) {
    "use strict";
    var util = require_util();
    var DATE = /^(\d\d\d\d)-(\d\d)-(\d\d)$/;
    var DAYS = [0, 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31];
    var TIME = /^(\d\d):(\d\d):(\d\d)(\.\d+)?(z|[+-]\d\d(?::?\d\d)?)?$/i;
    var HOSTNAME = /^(?=.{1,253}\.?$)[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?(?:\.[a-z0-9](?:[-0-9a-z]{0,61}[0-9a-z])?)*\.?$/i;
    var URI = /^(?:[a-z][a-z0-9+\-.]*:)(?:\/?\/(?:(?:[a-z0-9\-._~!$&'()*+,;=:]|%[0-9a-f]{2})*@)?(?:\[(?:(?:(?:(?:[0-9a-f]{1,4}:){6}|::(?:[0-9a-f]{1,4}:){5}|(?:[0-9a-f]{1,4})?::(?:[0-9a-f]{1,4}:){4}|(?:(?:[0-9a-f]{1,4}:){0,1}[0-9a-f]{1,4})?::(?:[0-9a-f]{1,4}:){3}|(?:(?:[0-9a-f]{1,4}:){0,2}[0-9a-f]{1,4})?::(?:[0-9a-f]{1,4}:){2}|(?:(?:[0-9a-f]{1,4}:){0,3}[0-9a-f]{1,4})?::[0-9a-f]{1,4}:|(?:(?:[0-9a-f]{1,4}:){0,4}[0-9a-f]{1,4})?::)(?:[0-9a-f]{1,4}:[0-9a-f]{1,4}|(?:(?:25[0-5]|2[0-4]\d|[01]?\d\d?)\.){3}(?:25[0-5]|2[0-4]\d|[01]?\d\d?))|(?:(?:[0-9a-f]{1,4}:){0,5}[0-9a-f]{1,4})?::[0-9a-f]{1,4}|(?:(?:[0-9a-f]{1,4}:){0,6}[0-9a-f]{1,4})?::)|[Vv][0-9a-f]+\.[a-z0-9\-._~!$&'()*+,;=:]+)\]|(?:(?:25[0-5]|2[0-4]\d|[01]?\d\d?)\.){3}(?:25[0-5]|2[0-4]\d|[01]?\d\d?)|(?:[a-z0-9\-._~!$&'()*+,;=]|%[0-9a-f]{2})*)(?::\d*)?(?:\/(?:[a-z0-9\-._~!$&'()*+,;=:@]|%[0-9a-f]{2})*)*|\/(?:(?:[a-z0-9\-._~!$&'()*+,;=:@]|%[0-9a-f]{2})+(?:\/(?:[a-z0-9\-._~!$&'()*+,;=:@]|%[0-9a-f]{2})*)*)?|(?:[a-z0-9\-._~!$&'()*+,;=:@]|%[0-9a-f]{2})+(?:\/(?:[a-z0-9\-._~!$&'()*+,;=:@]|%[0-9a-f]{2})*)*)(?:\?(?:[a-z0-9\-._~!$&'()*+,;=:@/?]|%[0-9a-f]{2})*)?(?:#(?:[a-z0-9\-._~!$&'()*+,;=:@/?]|%[0-9a-f]{2})*)?$/i;
    var URIREF = /^(?:[a-z][a-z0-9+\-.]*:)?(?:\/?\/(?:(?:[a-z0-9\-._~!$&'()*+,;=:]|%[0-9a-f]{2})*@)?(?:\[(?:(?:(?:(?:[0-9a-f]{1,4}:){6}|::(?:[0-9a-f]{1,4}:){5}|(?:[0-9a-f]{1,4})?::(?:[0-9a-f]{1,4}:){4}|(?:(?:[0-9a-f]{1,4}:){0,1}[0-9a-f]{1,4})?::(?:[0-9a-f]{1,4}:){3}|(?:(?:[0-9a-f]{1,4}:){0,2}[0-9a-f]{1,4})?::(?:[0-9a-f]{1,4}:){2}|(?:(?:[0-9a-f]{1,4}:){0,3}[0-9a-f]{1,4})?::[0-9a-f]{1,4}:|(?:(?:[0-9a-f]{1,4}:){0,4}[0-9a-f]{1,4})?::)(?:[0-9a-f]{1,4}:[0-9a-f]{1,4}|(?:(?:25[0-5]|2[0-4]\d|[01]?\d\d?)\.){3}(?:25[0-5]|2[0-4]\d|[01]?\d\d?))|(?:(?:[0-9a-f]{1,4}:){0,5}[0-9a-f]{1,4})?::[0-9a-f]{1,4}|(?:(?:[0-9a-f]{1,4}:){0,6}[0-9a-f]{1,4})?::)|[Vv][0-9a-f]+\.[a-z0-9\-._~!$&'()*+,;=:]+)\]|(?:(?:25[0-5]|2[0-4]\d|[01]?\d\d?)\.){3}(?:25[0-5]|2[0-4]\d|[01]?\d\d?)|(?:[a-z0-9\-._~!$&'"()*+,;=]|%[0-9a-f]{2})*)(?::\d*)?(?:\/(?:[a-z0-9\-._~!$&'"()*+,;=:@]|%[0-9a-f]{2})*)*|\/(?:(?:[a-z0-9\-._~!$&'"()*+,;=:@]|%[0-9a-f]{2})+(?:\/(?:[a-z0-9\-._~!$&'"()*+,;=:@]|%[0-9a-f]{2})*)*)?|(?:[a-z0-9\-._~!$&'"()*+,;=:@]|%[0-9a-f]{2})+(?:\/(?:[a-z0-9\-._~!$&'"()*+,;=:@]|%[0-9a-f]{2})*)*)?(?:\?(?:[a-z0-9\-._~!$&'"()*+,;=:@/?]|%[0-9a-f]{2})*)?(?:#(?:[a-z0-9\-._~!$&'"()*+,;=:@/?]|%[0-9a-f]{2})*)?$/i;
    var URITEMPLATE = /^(?:(?:[^\x00-\x20"'<>%\\^`{|}]|%[0-9a-f]{2})|\{[+#./;?&=,!@|]?(?:[a-z0-9_]|%[0-9a-f]{2})+(?::[1-9][0-9]{0,3}|\*)?(?:,(?:[a-z0-9_]|%[0-9a-f]{2})+(?::[1-9][0-9]{0,3}|\*)?)*\})*$/i;
    var URL = /^(?:(?:http[s\u017F]?|ftp):\/\/)(?:(?:[\0-\x08\x0E-\x1F!-\x9F\xA1-\u167F\u1681-\u1FFF\u200B-\u2027\u202A-\u202E\u2030-\u205E\u2060-\u2FFF\u3001-\uD7FF\uE000-\uFEFE\uFF00-\uFFFF]|[\uD800-\uDBFF][\uDC00-\uDFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF])+(?::(?:[\0-\x08\x0E-\x1F!-\x9F\xA1-\u167F\u1681-\u1FFF\u200B-\u2027\u202A-\u202E\u2030-\u205E\u2060-\u2FFF\u3001-\uD7FF\uE000-\uFEFE\uFF00-\uFFFF]|[\uD800-\uDBFF][\uDC00-\uDFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF])*)?@)?(?:(?!10(?:\.[0-9]{1,3}){3})(?!127(?:\.[0-9]{1,3}){3})(?!169\.254(?:\.[0-9]{1,3}){2})(?!192\.168(?:\.[0-9]{1,3}){2})(?!172\.(?:1[6-9]|2[0-9]|3[01])(?:\.[0-9]{1,3}){2})(?:[1-9][0-9]?|1[0-9][0-9]|2[01][0-9]|22[0-3])(?:\.(?:1?[0-9]{1,2}|2[0-4][0-9]|25[0-5])){2}(?:\.(?:[1-9][0-9]?|1[0-9][0-9]|2[0-4][0-9]|25[0-4]))|(?:(?:(?:[0-9a-z\xA1-\uD7FF\uE000-\uFFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF])+-)*(?:[0-9a-z\xA1-\uD7FF\uE000-\uFFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF])+)(?:\.(?:(?:[0-9a-z\xA1-\uD7FF\uE000-\uFFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF])+-)*(?:[0-9a-z\xA1-\uD7FF\uE000-\uFFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF])+)*(?:\.(?:(?:[a-z\xA1-\uD7FF\uE000-\uFFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF]){2,})))(?::[0-9]{2,5})?(?:\/(?:[\0-\x08\x0E-\x1F!-\x9F\xA1-\u167F\u1681-\u1FFF\u200B-\u2027\u202A-\u202E\u2030-\u205E\u2060-\u2FFF\u3001-\uD7FF\uE000-\uFEFE\uFF00-\uFFFF]|[\uD800-\uDBFF][\uDC00-\uDFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF])*)?$/i;
    var UUID = /^(?:urn:uuid:)?[0-9a-f]{8}-(?:[0-9a-f]{4}-){3}[0-9a-f]{12}$/i;
    var JSON_POINTER = /^(?:\/(?:[^~/]|~0|~1)*)*$/;
    var JSON_POINTER_URI_FRAGMENT = /^#(?:\/(?:[a-z0-9_\-.!$&'()*+,;:=@]|%[0-9a-f]{2}|~0|~1)*)*$/i;
    var RELATIVE_JSON_POINTER = /^(?:0|[1-9][0-9]*)(?:#|(?:\/(?:[^~/]|~0|~1)*)*)$/;
    module2.exports = formats;
    function formats(mode) {
      mode = mode == "full" ? "full" : "fast";
      return util.copy(formats[mode]);
    }
    formats.fast = {
      // date: http://tools.ietf.org/html/rfc3339#section-5.6
      date: /^\d\d\d\d-[0-1]\d-[0-3]\d$/,
      // date-time: http://tools.ietf.org/html/rfc3339#section-5.6
      time: /^(?:[0-2]\d:[0-5]\d:[0-5]\d|23:59:60)(?:\.\d+)?(?:z|[+-]\d\d(?::?\d\d)?)?$/i,
      "date-time": /^\d\d\d\d-[0-1]\d-[0-3]\d[t\s](?:[0-2]\d:[0-5]\d:[0-5]\d|23:59:60)(?:\.\d+)?(?:z|[+-]\d\d(?::?\d\d)?)$/i,
      // uri: https://github.com/mafintosh/is-my-json-valid/blob/master/formats.js
      uri: /^(?:[a-z][a-z0-9+\-.]*:)(?:\/?\/)?[^\s]*$/i,
      "uri-reference": /^(?:(?:[a-z][a-z0-9+\-.]*:)?\/?\/)?(?:[^\\\s#][^\s#]*)?(?:#[^\\\s]*)?$/i,
      "uri-template": URITEMPLATE,
      url: URL,
      // email (sources from jsen validator):
      // http://stackoverflow.com/questions/201323/using-a-regular-expression-to-validate-an-email-address#answer-8829363
      // http://www.w3.org/TR/html5/forms.html#valid-e-mail-address (search for 'willful violation')
      email: /^[a-z0-9.!#$%&'*+/=?^_`{|}~-]+@[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?(?:\.[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?)*$/i,
      hostname: HOSTNAME,
      // optimized https://www.safaribooksonline.com/library/view/regular-expressions-cookbook/9780596802837/ch07s16.html
      ipv4: /^(?:(?:25[0-5]|2[0-4]\d|[01]?\d\d?)\.){3}(?:25[0-5]|2[0-4]\d|[01]?\d\d?)$/,
      // optimized http://stackoverflow.com/questions/53497/regular-expression-that-matches-valid-ipv6-addresses
      ipv6: /^\s*(?:(?:(?:[0-9a-f]{1,4}:){7}(?:[0-9a-f]{1,4}|:))|(?:(?:[0-9a-f]{1,4}:){6}(?::[0-9a-f]{1,4}|(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3})|:))|(?:(?:[0-9a-f]{1,4}:){5}(?:(?:(?::[0-9a-f]{1,4}){1,2})|:(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3})|:))|(?:(?:[0-9a-f]{1,4}:){4}(?:(?:(?::[0-9a-f]{1,4}){1,3})|(?:(?::[0-9a-f]{1,4})?:(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(?:(?:[0-9a-f]{1,4}:){3}(?:(?:(?::[0-9a-f]{1,4}){1,4})|(?:(?::[0-9a-f]{1,4}){0,2}:(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(?:(?:[0-9a-f]{1,4}:){2}(?:(?:(?::[0-9a-f]{1,4}){1,5})|(?:(?::[0-9a-f]{1,4}){0,3}:(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(?:(?:[0-9a-f]{1,4}:){1}(?:(?:(?::[0-9a-f]{1,4}){1,6})|(?:(?::[0-9a-f]{1,4}){0,4}:(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(?::(?:(?:(?::[0-9a-f]{1,4}){1,7})|(?:(?::[0-9a-f]{1,4}){0,5}:(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:)))(?:%.+)?\s*$/i,
      regex,
      // uuid: http://tools.ietf.org/html/rfc4122
      uuid: UUID,
      // JSON-pointer: https://tools.ietf.org/html/rfc6901
      // uri fragment: https://tools.ietf.org/html/rfc3986#appendix-A
      "json-pointer": JSON_POINTER,
      "json-pointer-uri-fragment": JSON_POINTER_URI_FRAGMENT,
      // relative JSON-pointer: http://tools.ietf.org/html/draft-luff-relative-json-pointer-00
      "relative-json-pointer": RELATIVE_JSON_POINTER
    };
    formats.full = {
      date,
      time,
      "date-time": date_time,
      uri,
      "uri-reference": URIREF,
      "uri-template": URITEMPLATE,
      url: URL,
      email: /^[a-z0-9!#$%&'*+/=?^_`{|}~-]+(?:\.[a-z0-9!#$%&'*+/=?^_`{|}~-]+)*@(?:[a-z0-9](?:[a-z0-9-]*[a-z0-9])?\.)+[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$/i,
      hostname: HOSTNAME,
      ipv4: /^(?:(?:25[0-5]|2[0-4]\d|[01]?\d\d?)\.){3}(?:25[0-5]|2[0-4]\d|[01]?\d\d?)$/,
      ipv6: /^\s*(?:(?:(?:[0-9a-f]{1,4}:){7}(?:[0-9a-f]{1,4}|:))|(?:(?:[0-9a-f]{1,4}:){6}(?::[0-9a-f]{1,4}|(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3})|:))|(?:(?:[0-9a-f]{1,4}:){5}(?:(?:(?::[0-9a-f]{1,4}){1,2})|:(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3})|:))|(?:(?:[0-9a-f]{1,4}:){4}(?:(?:(?::[0-9a-f]{1,4}){1,3})|(?:(?::[0-9a-f]{1,4})?:(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(?:(?:[0-9a-f]{1,4}:){3}(?:(?:(?::[0-9a-f]{1,4}){1,4})|(?:(?::[0-9a-f]{1,4}){0,2}:(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(?:(?:[0-9a-f]{1,4}:){2}(?:(?:(?::[0-9a-f]{1,4}){1,5})|(?:(?::[0-9a-f]{1,4}){0,3}:(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(?:(?:[0-9a-f]{1,4}:){1}(?:(?:(?::[0-9a-f]{1,4}){1,6})|(?:(?::[0-9a-f]{1,4}){0,4}:(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(?::(?:(?:(?::[0-9a-f]{1,4}){1,7})|(?:(?::[0-9a-f]{1,4}){0,5}:(?:(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:)))(?:%.+)?\s*$/i,
      regex,
      uuid: UUID,
      "json-pointer": JSON_POINTER,
      "json-pointer-uri-fragment": JSON_POINTER_URI_FRAGMENT,
      "relative-json-pointer": RELATIVE_JSON_POINTER
    };
    function isLeapYear(year) {
      return year % 4 === 0 && (year % 100 !== 0 || year % 400 === 0);
    }
    function date(str) {
      var matches = str.match(DATE);
      if (!matches)
        return false;
      var year = +matches[1];
      var month = +matches[2];
      var day = +matches[3];
      return month >= 1 && month <= 12 && day >= 1 && day <= (month == 2 && isLeapYear(year) ? 29 : DAYS[month]);
    }
    function time(str, full) {
      var matches = str.match(TIME);
      if (!matches)
        return false;
      var hour = matches[1];
      var minute = matches[2];
      var second = matches[3];
      var timeZone = matches[5];
      return (hour <= 23 && minute <= 59 && second <= 59 || hour == 23 && minute == 59 && second == 60) && (!full || timeZone);
    }
    var DATE_TIME_SEPARATOR = /t|\s/i;
    function date_time(str) {
      var dateTime = str.split(DATE_TIME_SEPARATOR);
      return dateTime.length == 2 && date(dateTime[0]) && time(dateTime[1], true);
    }
    var NOT_URI_FRAGMENT = /\/|:/;
    function uri(str) {
      return NOT_URI_FRAGMENT.test(str) && URI.test(str);
    }
    var Z_ANCHOR = /[^\\]\\Z/;
    function regex(str) {
      if (Z_ANCHOR.test(str))
        return false;
      try {
        new RegExp(str);
        return true;
      } catch (e) {
        return false;
      }
    }
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/ref.js
var require_ref = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/ref.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_ref(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $valid = "valid" + $lvl;
      var $async, $refCode;
      if ($schema == "#" || $schema == "#/") {
        if (it.isRoot) {
          $async = it.async;
          $refCode = "validate";
        } else {
          $async = it.root.schema.$async === true;
          $refCode = "root.refVal[0]";
        }
      } else {
        var $refVal = it.resolveRef(it.baseId, $schema, it.isRoot);
        if ($refVal === void 0) {
          var $message = it.MissingRefError.message(it.baseId, $schema);
          if (it.opts.missingRefs == "fail") {
            it.logger.error($message);
            var $$outStack = $$outStack || [];
            $$outStack.push(out);
            out = "";
            if (it.createErrors !== false) {
              out += " { keyword: '$ref' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { ref: '" + it.util.escapeQuotes($schema) + "' } ";
              if (it.opts.messages !== false) {
                out += " , message: 'can\\'t resolve reference " + it.util.escapeQuotes($schema) + "' ";
              }
              if (it.opts.verbose) {
                out += " , schema: " + it.util.toQuotedString($schema) + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
              }
              out += " } ";
            } else {
              out += " {} ";
            }
            var __err = out;
            out = $$outStack.pop();
            if (!it.compositeRule && $breakOnError) {
              if (it.async) {
                out += " throw new ValidationError([" + __err + "]); ";
              } else {
                out += " validate.errors = [" + __err + "]; return false; ";
              }
            } else {
              out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
            }
            if ($breakOnError) {
              out += " if (false) { ";
            }
          } else if (it.opts.missingRefs == "ignore") {
            it.logger.warn($message);
            if ($breakOnError) {
              out += " if (true) { ";
            }
          } else {
            throw new it.MissingRefError(it.baseId, $schema, $message);
          }
        } else if ($refVal.inline) {
          var $it = it.util.copy(it);
          $it.level++;
          var $nextValid = "valid" + $it.level;
          $it.schema = $refVal.schema;
          $it.schemaPath = "";
          $it.errSchemaPath = $schema;
          var $code = it.validate($it).replace(/validate\.schema/g, $refVal.code);
          out += " " + $code + " ";
          if ($breakOnError) {
            out += " if (" + $nextValid + ") { ";
          }
        } else {
          $async = $refVal.$async === true || it.async && $refVal.$async !== false;
          $refCode = $refVal.code;
        }
      }
      if ($refCode) {
        var $$outStack = $$outStack || [];
        $$outStack.push(out);
        out = "";
        if (it.opts.passContext) {
          out += " " + $refCode + ".call(this, ";
        } else {
          out += " " + $refCode + "( ";
        }
        out += " " + $data + ", (dataPath || '')";
        if (it.errorPath != '""') {
          out += " + " + it.errorPath;
        }
        var $parentData = $dataLvl ? "data" + ($dataLvl - 1 || "") : "parentData", $parentDataProperty = $dataLvl ? it.dataPathArr[$dataLvl] : "parentDataProperty";
        out += " , " + $parentData + " , " + $parentDataProperty + ", rootData)  ";
        var __callValidate = out;
        out = $$outStack.pop();
        if ($async) {
          if (!it.async)
            throw new Error("async schema referenced by sync schema");
          if ($breakOnError) {
            out += " var " + $valid + "; ";
          }
          out += " try { await " + __callValidate + "; ";
          if ($breakOnError) {
            out += " " + $valid + " = true; ";
          }
          out += " } catch (e) { if (!(e instanceof ValidationError)) throw e; if (vErrors === null) vErrors = e.errors; else vErrors = vErrors.concat(e.errors); errors = vErrors.length; ";
          if ($breakOnError) {
            out += " " + $valid + " = false; ";
          }
          out += " } ";
          if ($breakOnError) {
            out += " if (" + $valid + ") { ";
          }
        } else {
          out += " if (!" + __callValidate + ") { if (vErrors === null) vErrors = " + $refCode + ".errors; else vErrors = vErrors.concat(" + $refCode + ".errors); errors = vErrors.length; } ";
          if ($breakOnError) {
            out += " else { ";
          }
        }
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/allOf.js
var require_allOf = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/allOf.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_allOf(it, $keyword, $ruleType) {
      var out = " ";
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $it = it.util.copy(it);
      var $closingBraces = "";
      $it.level++;
      var $nextValid = "valid" + $it.level;
      var $currentBaseId = $it.baseId, $allSchemasEmpty = true;
      var arr1 = $schema;
      if (arr1) {
        var $sch, $i = -1, l1 = arr1.length - 1;
        while ($i < l1) {
          $sch = arr1[$i += 1];
          if (it.opts.strictKeywords ? typeof $sch == "object" && Object.keys($sch).length > 0 || $sch === false : it.util.schemaHasRules($sch, it.RULES.all)) {
            $allSchemasEmpty = false;
            $it.schema = $sch;
            $it.schemaPath = $schemaPath + "[" + $i + "]";
            $it.errSchemaPath = $errSchemaPath + "/" + $i;
            out += "  " + it.validate($it) + " ";
            $it.baseId = $currentBaseId;
            if ($breakOnError) {
              out += " if (" + $nextValid + ") { ";
              $closingBraces += "}";
            }
          }
        }
      }
      if ($breakOnError) {
        if ($allSchemasEmpty) {
          out += " if (true) { ";
        } else {
          out += " " + $closingBraces.slice(0, -1) + " ";
        }
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/anyOf.js
var require_anyOf = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/anyOf.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_anyOf(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $valid = "valid" + $lvl;
      var $errs = "errs__" + $lvl;
      var $it = it.util.copy(it);
      var $closingBraces = "";
      $it.level++;
      var $nextValid = "valid" + $it.level;
      var $noEmptySchema = $schema.every(function($sch2) {
        return it.opts.strictKeywords ? typeof $sch2 == "object" && Object.keys($sch2).length > 0 || $sch2 === false : it.util.schemaHasRules($sch2, it.RULES.all);
      });
      if ($noEmptySchema) {
        var $currentBaseId = $it.baseId;
        out += " var " + $errs + " = errors; var " + $valid + " = false;  ";
        var $wasComposite = it.compositeRule;
        it.compositeRule = $it.compositeRule = true;
        var arr1 = $schema;
        if (arr1) {
          var $sch, $i = -1, l1 = arr1.length - 1;
          while ($i < l1) {
            $sch = arr1[$i += 1];
            $it.schema = $sch;
            $it.schemaPath = $schemaPath + "[" + $i + "]";
            $it.errSchemaPath = $errSchemaPath + "/" + $i;
            out += "  " + it.validate($it) + " ";
            $it.baseId = $currentBaseId;
            out += " " + $valid + " = " + $valid + " || " + $nextValid + "; if (!" + $valid + ") { ";
            $closingBraces += "}";
          }
        }
        it.compositeRule = $it.compositeRule = $wasComposite;
        out += " " + $closingBraces + " if (!" + $valid + ") {   var err =   ";
        if (it.createErrors !== false) {
          out += " { keyword: 'anyOf' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: {} ";
          if (it.opts.messages !== false) {
            out += " , message: 'should match some schema in anyOf' ";
          }
          if (it.opts.verbose) {
            out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
          }
          out += " } ";
        } else {
          out += " {} ";
        }
        out += ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
        if (!it.compositeRule && $breakOnError) {
          if (it.async) {
            out += " throw new ValidationError(vErrors); ";
          } else {
            out += " validate.errors = vErrors; return false; ";
          }
        }
        out += " } else {  errors = " + $errs + "; if (vErrors !== null) { if (" + $errs + ") vErrors.length = " + $errs + "; else vErrors = null; } ";
        if (it.opts.allErrors) {
          out += " } ";
        }
      } else {
        if ($breakOnError) {
          out += " if (true) { ";
        }
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/comment.js
var require_comment = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/comment.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_comment(it, $keyword, $ruleType) {
      var out = " ";
      var $schema = it.schema[$keyword];
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $comment = it.util.toQuotedString($schema);
      if (it.opts.$comment === true) {
        out += " console.log(" + $comment + ");";
      } else if (typeof it.opts.$comment == "function") {
        out += " self._opts.$comment(" + $comment + ", " + it.util.toQuotedString($errSchemaPath) + ", validate.root.schema);";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/const.js
var require_const = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/const.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_const(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $valid = "valid" + $lvl;
      var $isData = it.opts.$data && $schema && $schema.$data, $schemaValue;
      if ($isData) {
        out += " var schema" + $lvl + " = " + it.util.getData($schema.$data, $dataLvl, it.dataPathArr) + "; ";
        $schemaValue = "schema" + $lvl;
      } else {
        $schemaValue = $schema;
      }
      if (!$isData) {
        out += " var schema" + $lvl + " = validate.schema" + $schemaPath + ";";
      }
      out += "var " + $valid + " = equal(" + $data + ", schema" + $lvl + "); if (!" + $valid + ") {   ";
      var $$outStack = $$outStack || [];
      $$outStack.push(out);
      out = "";
      if (it.createErrors !== false) {
        out += " { keyword: 'const' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { allowedValue: schema" + $lvl + " } ";
        if (it.opts.messages !== false) {
          out += " , message: 'should be equal to constant' ";
        }
        if (it.opts.verbose) {
          out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
        }
        out += " } ";
      } else {
        out += " {} ";
      }
      var __err = out;
      out = $$outStack.pop();
      if (!it.compositeRule && $breakOnError) {
        if (it.async) {
          out += " throw new ValidationError([" + __err + "]); ";
        } else {
          out += " validate.errors = [" + __err + "]; return false; ";
        }
      } else {
        out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
      }
      out += " }";
      if ($breakOnError) {
        out += " else { ";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/contains.js
var require_contains = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/contains.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_contains(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $valid = "valid" + $lvl;
      var $errs = "errs__" + $lvl;
      var $it = it.util.copy(it);
      var $closingBraces = "";
      $it.level++;
      var $nextValid = "valid" + $it.level;
      var $idx = "i" + $lvl, $dataNxt = $it.dataLevel = it.dataLevel + 1, $nextData = "data" + $dataNxt, $currentBaseId = it.baseId, $nonEmptySchema = it.opts.strictKeywords ? typeof $schema == "object" && Object.keys($schema).length > 0 || $schema === false : it.util.schemaHasRules($schema, it.RULES.all);
      out += "var " + $errs + " = errors;var " + $valid + ";";
      if ($nonEmptySchema) {
        var $wasComposite = it.compositeRule;
        it.compositeRule = $it.compositeRule = true;
        $it.schema = $schema;
        $it.schemaPath = $schemaPath;
        $it.errSchemaPath = $errSchemaPath;
        out += " var " + $nextValid + " = false; for (var " + $idx + " = 0; " + $idx + " < " + $data + ".length; " + $idx + "++) { ";
        $it.errorPath = it.util.getPathExpr(it.errorPath, $idx, it.opts.jsonPointers, true);
        var $passData = $data + "[" + $idx + "]";
        $it.dataPathArr[$dataNxt] = $idx;
        var $code = it.validate($it);
        $it.baseId = $currentBaseId;
        if (it.util.varOccurences($code, $nextData) < 2) {
          out += " " + it.util.varReplace($code, $nextData, $passData) + " ";
        } else {
          out += " var " + $nextData + " = " + $passData + "; " + $code + " ";
        }
        out += " if (" + $nextValid + ") break; }  ";
        it.compositeRule = $it.compositeRule = $wasComposite;
        out += " " + $closingBraces + " if (!" + $nextValid + ") {";
      } else {
        out += " if (" + $data + ".length == 0) {";
      }
      var $$outStack = $$outStack || [];
      $$outStack.push(out);
      out = "";
      if (it.createErrors !== false) {
        out += " { keyword: 'contains' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: {} ";
        if (it.opts.messages !== false) {
          out += " , message: 'should contain a valid item' ";
        }
        if (it.opts.verbose) {
          out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
        }
        out += " } ";
      } else {
        out += " {} ";
      }
      var __err = out;
      out = $$outStack.pop();
      if (!it.compositeRule && $breakOnError) {
        if (it.async) {
          out += " throw new ValidationError([" + __err + "]); ";
        } else {
          out += " validate.errors = [" + __err + "]; return false; ";
        }
      } else {
        out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
      }
      out += " } else { ";
      if ($nonEmptySchema) {
        out += "  errors = " + $errs + "; if (vErrors !== null) { if (" + $errs + ") vErrors.length = " + $errs + "; else vErrors = null; } ";
      }
      if (it.opts.allErrors) {
        out += " } ";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/dependencies.js
var require_dependencies = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/dependencies.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_dependencies(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $errs = "errs__" + $lvl;
      var $it = it.util.copy(it);
      var $closingBraces = "";
      $it.level++;
      var $nextValid = "valid" + $it.level;
      var $schemaDeps = {}, $propertyDeps = {}, $ownProperties = it.opts.ownProperties;
      for ($property in $schema) {
        if ($property == "__proto__")
          continue;
        var $sch = $schema[$property];
        var $deps = Array.isArray($sch) ? $propertyDeps : $schemaDeps;
        $deps[$property] = $sch;
      }
      out += "var " + $errs + " = errors;";
      var $currentErrorPath = it.errorPath;
      out += "var missing" + $lvl + ";";
      for (var $property in $propertyDeps) {
        $deps = $propertyDeps[$property];
        if ($deps.length) {
          out += " if ( " + $data + it.util.getProperty($property) + " !== undefined ";
          if ($ownProperties) {
            out += " && Object.prototype.hasOwnProperty.call(" + $data + ", '" + it.util.escapeQuotes($property) + "') ";
          }
          if ($breakOnError) {
            out += " && ( ";
            var arr1 = $deps;
            if (arr1) {
              var $propertyKey, $i = -1, l1 = arr1.length - 1;
              while ($i < l1) {
                $propertyKey = arr1[$i += 1];
                if ($i) {
                  out += " || ";
                }
                var $prop = it.util.getProperty($propertyKey), $useData = $data + $prop;
                out += " ( ( " + $useData + " === undefined ";
                if ($ownProperties) {
                  out += " || ! Object.prototype.hasOwnProperty.call(" + $data + ", '" + it.util.escapeQuotes($propertyKey) + "') ";
                }
                out += ") && (missing" + $lvl + " = " + it.util.toQuotedString(it.opts.jsonPointers ? $propertyKey : $prop) + ") ) ";
              }
            }
            out += ")) {  ";
            var $propertyPath = "missing" + $lvl, $missingProperty = "' + " + $propertyPath + " + '";
            if (it.opts._errorDataPathProperty) {
              it.errorPath = it.opts.jsonPointers ? it.util.getPathExpr($currentErrorPath, $propertyPath, true) : $currentErrorPath + " + " + $propertyPath;
            }
            var $$outStack = $$outStack || [];
            $$outStack.push(out);
            out = "";
            if (it.createErrors !== false) {
              out += " { keyword: 'dependencies' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { property: '" + it.util.escapeQuotes($property) + "', missingProperty: '" + $missingProperty + "', depsCount: " + $deps.length + ", deps: '" + it.util.escapeQuotes($deps.length == 1 ? $deps[0] : $deps.join(", ")) + "' } ";
              if (it.opts.messages !== false) {
                out += " , message: 'should have ";
                if ($deps.length == 1) {
                  out += "property " + it.util.escapeQuotes($deps[0]);
                } else {
                  out += "properties " + it.util.escapeQuotes($deps.join(", "));
                }
                out += " when property " + it.util.escapeQuotes($property) + " is present' ";
              }
              if (it.opts.verbose) {
                out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
              }
              out += " } ";
            } else {
              out += " {} ";
            }
            var __err = out;
            out = $$outStack.pop();
            if (!it.compositeRule && $breakOnError) {
              if (it.async) {
                out += " throw new ValidationError([" + __err + "]); ";
              } else {
                out += " validate.errors = [" + __err + "]; return false; ";
              }
            } else {
              out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
            }
          } else {
            out += " ) { ";
            var arr2 = $deps;
            if (arr2) {
              var $propertyKey, i2 = -1, l2 = arr2.length - 1;
              while (i2 < l2) {
                $propertyKey = arr2[i2 += 1];
                var $prop = it.util.getProperty($propertyKey), $missingProperty = it.util.escapeQuotes($propertyKey), $useData = $data + $prop;
                if (it.opts._errorDataPathProperty) {
                  it.errorPath = it.util.getPath($currentErrorPath, $propertyKey, it.opts.jsonPointers);
                }
                out += " if ( " + $useData + " === undefined ";
                if ($ownProperties) {
                  out += " || ! Object.prototype.hasOwnProperty.call(" + $data + ", '" + it.util.escapeQuotes($propertyKey) + "') ";
                }
                out += ") {  var err =   ";
                if (it.createErrors !== false) {
                  out += " { keyword: 'dependencies' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { property: '" + it.util.escapeQuotes($property) + "', missingProperty: '" + $missingProperty + "', depsCount: " + $deps.length + ", deps: '" + it.util.escapeQuotes($deps.length == 1 ? $deps[0] : $deps.join(", ")) + "' } ";
                  if (it.opts.messages !== false) {
                    out += " , message: 'should have ";
                    if ($deps.length == 1) {
                      out += "property " + it.util.escapeQuotes($deps[0]);
                    } else {
                      out += "properties " + it.util.escapeQuotes($deps.join(", "));
                    }
                    out += " when property " + it.util.escapeQuotes($property) + " is present' ";
                  }
                  if (it.opts.verbose) {
                    out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
                  }
                  out += " } ";
                } else {
                  out += " {} ";
                }
                out += ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; } ";
              }
            }
          }
          out += " }   ";
          if ($breakOnError) {
            $closingBraces += "}";
            out += " else { ";
          }
        }
      }
      it.errorPath = $currentErrorPath;
      var $currentBaseId = $it.baseId;
      for (var $property in $schemaDeps) {
        var $sch = $schemaDeps[$property];
        if (it.opts.strictKeywords ? typeof $sch == "object" && Object.keys($sch).length > 0 || $sch === false : it.util.schemaHasRules($sch, it.RULES.all)) {
          out += " " + $nextValid + " = true; if ( " + $data + it.util.getProperty($property) + " !== undefined ";
          if ($ownProperties) {
            out += " && Object.prototype.hasOwnProperty.call(" + $data + ", '" + it.util.escapeQuotes($property) + "') ";
          }
          out += ") { ";
          $it.schema = $sch;
          $it.schemaPath = $schemaPath + it.util.getProperty($property);
          $it.errSchemaPath = $errSchemaPath + "/" + it.util.escapeFragment($property);
          out += "  " + it.validate($it) + " ";
          $it.baseId = $currentBaseId;
          out += " }  ";
          if ($breakOnError) {
            out += " if (" + $nextValid + ") { ";
            $closingBraces += "}";
          }
        }
      }
      if ($breakOnError) {
        out += "   " + $closingBraces + " if (" + $errs + " == errors) {";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/enum.js
var require_enum = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/enum.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_enum(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $valid = "valid" + $lvl;
      var $isData = it.opts.$data && $schema && $schema.$data, $schemaValue;
      if ($isData) {
        out += " var schema" + $lvl + " = " + it.util.getData($schema.$data, $dataLvl, it.dataPathArr) + "; ";
        $schemaValue = "schema" + $lvl;
      } else {
        $schemaValue = $schema;
      }
      var $i = "i" + $lvl, $vSchema = "schema" + $lvl;
      if (!$isData) {
        out += " var " + $vSchema + " = validate.schema" + $schemaPath + ";";
      }
      out += "var " + $valid + ";";
      if ($isData) {
        out += " if (schema" + $lvl + " === undefined) " + $valid + " = true; else if (!Array.isArray(schema" + $lvl + ")) " + $valid + " = false; else {";
      }
      out += "" + $valid + " = false;for (var " + $i + "=0; " + $i + "<" + $vSchema + ".length; " + $i + "++) if (equal(" + $data + ", " + $vSchema + "[" + $i + "])) { " + $valid + " = true; break; }";
      if ($isData) {
        out += "  }  ";
      }
      out += " if (!" + $valid + ") {   ";
      var $$outStack = $$outStack || [];
      $$outStack.push(out);
      out = "";
      if (it.createErrors !== false) {
        out += " { keyword: 'enum' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { allowedValues: schema" + $lvl + " } ";
        if (it.opts.messages !== false) {
          out += " , message: 'should be equal to one of the allowed values' ";
        }
        if (it.opts.verbose) {
          out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
        }
        out += " } ";
      } else {
        out += " {} ";
      }
      var __err = out;
      out = $$outStack.pop();
      if (!it.compositeRule && $breakOnError) {
        if (it.async) {
          out += " throw new ValidationError([" + __err + "]); ";
        } else {
          out += " validate.errors = [" + __err + "]; return false; ";
        }
      } else {
        out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
      }
      out += " }";
      if ($breakOnError) {
        out += " else { ";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/format.js
var require_format = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/format.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_format(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      if (it.opts.format === false) {
        if ($breakOnError) {
          out += " if (true) { ";
        }
        return out;
      }
      var $isData = it.opts.$data && $schema && $schema.$data, $schemaValue;
      if ($isData) {
        out += " var schema" + $lvl + " = " + it.util.getData($schema.$data, $dataLvl, it.dataPathArr) + "; ";
        $schemaValue = "schema" + $lvl;
      } else {
        $schemaValue = $schema;
      }
      var $unknownFormats = it.opts.unknownFormats, $allowUnknown = Array.isArray($unknownFormats);
      if ($isData) {
        var $format = "format" + $lvl, $isObject = "isObject" + $lvl, $formatType = "formatType" + $lvl;
        out += " var " + $format + " = formats[" + $schemaValue + "]; var " + $isObject + " = typeof " + $format + " == 'object' && !(" + $format + " instanceof RegExp) && " + $format + ".validate; var " + $formatType + " = " + $isObject + " && " + $format + ".type || 'string'; if (" + $isObject + ") { ";
        if (it.async) {
          out += " var async" + $lvl + " = " + $format + ".async; ";
        }
        out += " " + $format + " = " + $format + ".validate; } if (  ";
        if ($isData) {
          out += " (" + $schemaValue + " !== undefined && typeof " + $schemaValue + " != 'string') || ";
        }
        out += " (";
        if ($unknownFormats != "ignore") {
          out += " (" + $schemaValue + " && !" + $format + " ";
          if ($allowUnknown) {
            out += " && self._opts.unknownFormats.indexOf(" + $schemaValue + ") == -1 ";
          }
          out += ") || ";
        }
        out += " (" + $format + " && " + $formatType + " == '" + $ruleType + "' && !(typeof " + $format + " == 'function' ? ";
        if (it.async) {
          out += " (async" + $lvl + " ? await " + $format + "(" + $data + ") : " + $format + "(" + $data + ")) ";
        } else {
          out += " " + $format + "(" + $data + ") ";
        }
        out += " : " + $format + ".test(" + $data + "))))) {";
      } else {
        var $format = it.formats[$schema];
        if (!$format) {
          if ($unknownFormats == "ignore") {
            it.logger.warn('unknown format "' + $schema + '" ignored in schema at path "' + it.errSchemaPath + '"');
            if ($breakOnError) {
              out += " if (true) { ";
            }
            return out;
          } else if ($allowUnknown && $unknownFormats.indexOf($schema) >= 0) {
            if ($breakOnError) {
              out += " if (true) { ";
            }
            return out;
          } else {
            throw new Error('unknown format "' + $schema + '" is used in schema at path "' + it.errSchemaPath + '"');
          }
        }
        var $isObject = typeof $format == "object" && !($format instanceof RegExp) && $format.validate;
        var $formatType = $isObject && $format.type || "string";
        if ($isObject) {
          var $async = $format.async === true;
          $format = $format.validate;
        }
        if ($formatType != $ruleType) {
          if ($breakOnError) {
            out += " if (true) { ";
          }
          return out;
        }
        if ($async) {
          if (!it.async)
            throw new Error("async format in sync schema");
          var $formatRef = "formats" + it.util.getProperty($schema) + ".validate";
          out += " if (!(await " + $formatRef + "(" + $data + "))) { ";
        } else {
          out += " if (! ";
          var $formatRef = "formats" + it.util.getProperty($schema);
          if ($isObject)
            $formatRef += ".validate";
          if (typeof $format == "function") {
            out += " " + $formatRef + "(" + $data + ") ";
          } else {
            out += " " + $formatRef + ".test(" + $data + ") ";
          }
          out += ") { ";
        }
      }
      var $$outStack = $$outStack || [];
      $$outStack.push(out);
      out = "";
      if (it.createErrors !== false) {
        out += " { keyword: 'format' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { format:  ";
        if ($isData) {
          out += "" + $schemaValue;
        } else {
          out += "" + it.util.toQuotedString($schema);
        }
        out += "  } ";
        if (it.opts.messages !== false) {
          out += ` , message: 'should match format "`;
          if ($isData) {
            out += "' + " + $schemaValue + " + '";
          } else {
            out += "" + it.util.escapeQuotes($schema);
          }
          out += `"' `;
        }
        if (it.opts.verbose) {
          out += " , schema:  ";
          if ($isData) {
            out += "validate.schema" + $schemaPath;
          } else {
            out += "" + it.util.toQuotedString($schema);
          }
          out += "         , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
        }
        out += " } ";
      } else {
        out += " {} ";
      }
      var __err = out;
      out = $$outStack.pop();
      if (!it.compositeRule && $breakOnError) {
        if (it.async) {
          out += " throw new ValidationError([" + __err + "]); ";
        } else {
          out += " validate.errors = [" + __err + "]; return false; ";
        }
      } else {
        out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
      }
      out += " } ";
      if ($breakOnError) {
        out += " else { ";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/if.js
var require_if = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/if.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_if(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $valid = "valid" + $lvl;
      var $errs = "errs__" + $lvl;
      var $it = it.util.copy(it);
      $it.level++;
      var $nextValid = "valid" + $it.level;
      var $thenSch = it.schema["then"], $elseSch = it.schema["else"], $thenPresent = $thenSch !== void 0 && (it.opts.strictKeywords ? typeof $thenSch == "object" && Object.keys($thenSch).length > 0 || $thenSch === false : it.util.schemaHasRules($thenSch, it.RULES.all)), $elsePresent = $elseSch !== void 0 && (it.opts.strictKeywords ? typeof $elseSch == "object" && Object.keys($elseSch).length > 0 || $elseSch === false : it.util.schemaHasRules($elseSch, it.RULES.all)), $currentBaseId = $it.baseId;
      if ($thenPresent || $elsePresent) {
        var $ifClause;
        $it.createErrors = false;
        $it.schema = $schema;
        $it.schemaPath = $schemaPath;
        $it.errSchemaPath = $errSchemaPath;
        out += " var " + $errs + " = errors; var " + $valid + " = true;  ";
        var $wasComposite = it.compositeRule;
        it.compositeRule = $it.compositeRule = true;
        out += "  " + it.validate($it) + " ";
        $it.baseId = $currentBaseId;
        $it.createErrors = true;
        out += "  errors = " + $errs + "; if (vErrors !== null) { if (" + $errs + ") vErrors.length = " + $errs + "; else vErrors = null; }  ";
        it.compositeRule = $it.compositeRule = $wasComposite;
        if ($thenPresent) {
          out += " if (" + $nextValid + ") {  ";
          $it.schema = it.schema["then"];
          $it.schemaPath = it.schemaPath + ".then";
          $it.errSchemaPath = it.errSchemaPath + "/then";
          out += "  " + it.validate($it) + " ";
          $it.baseId = $currentBaseId;
          out += " " + $valid + " = " + $nextValid + "; ";
          if ($thenPresent && $elsePresent) {
            $ifClause = "ifClause" + $lvl;
            out += " var " + $ifClause + " = 'then'; ";
          } else {
            $ifClause = "'then'";
          }
          out += " } ";
          if ($elsePresent) {
            out += " else { ";
          }
        } else {
          out += " if (!" + $nextValid + ") { ";
        }
        if ($elsePresent) {
          $it.schema = it.schema["else"];
          $it.schemaPath = it.schemaPath + ".else";
          $it.errSchemaPath = it.errSchemaPath + "/else";
          out += "  " + it.validate($it) + " ";
          $it.baseId = $currentBaseId;
          out += " " + $valid + " = " + $nextValid + "; ";
          if ($thenPresent && $elsePresent) {
            $ifClause = "ifClause" + $lvl;
            out += " var " + $ifClause + " = 'else'; ";
          } else {
            $ifClause = "'else'";
          }
          out += " } ";
        }
        out += " if (!" + $valid + ") {   var err =   ";
        if (it.createErrors !== false) {
          out += " { keyword: 'if' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { failingKeyword: " + $ifClause + " } ";
          if (it.opts.messages !== false) {
            out += ` , message: 'should match "' + ` + $ifClause + ` + '" schema' `;
          }
          if (it.opts.verbose) {
            out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
          }
          out += " } ";
        } else {
          out += " {} ";
        }
        out += ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
        if (!it.compositeRule && $breakOnError) {
          if (it.async) {
            out += " throw new ValidationError(vErrors); ";
          } else {
            out += " validate.errors = vErrors; return false; ";
          }
        }
        out += " }   ";
        if ($breakOnError) {
          out += " else { ";
        }
      } else {
        if ($breakOnError) {
          out += " if (true) { ";
        }
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/items.js
var require_items = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/items.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_items(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $valid = "valid" + $lvl;
      var $errs = "errs__" + $lvl;
      var $it = it.util.copy(it);
      var $closingBraces = "";
      $it.level++;
      var $nextValid = "valid" + $it.level;
      var $idx = "i" + $lvl, $dataNxt = $it.dataLevel = it.dataLevel + 1, $nextData = "data" + $dataNxt, $currentBaseId = it.baseId;
      out += "var " + $errs + " = errors;var " + $valid + ";";
      if (Array.isArray($schema)) {
        var $additionalItems = it.schema.additionalItems;
        if ($additionalItems === false) {
          out += " " + $valid + " = " + $data + ".length <= " + $schema.length + "; ";
          var $currErrSchemaPath = $errSchemaPath;
          $errSchemaPath = it.errSchemaPath + "/additionalItems";
          out += "  if (!" + $valid + ") {   ";
          var $$outStack = $$outStack || [];
          $$outStack.push(out);
          out = "";
          if (it.createErrors !== false) {
            out += " { keyword: 'additionalItems' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { limit: " + $schema.length + " } ";
            if (it.opts.messages !== false) {
              out += " , message: 'should NOT have more than " + $schema.length + " items' ";
            }
            if (it.opts.verbose) {
              out += " , schema: false , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
            }
            out += " } ";
          } else {
            out += " {} ";
          }
          var __err = out;
          out = $$outStack.pop();
          if (!it.compositeRule && $breakOnError) {
            if (it.async) {
              out += " throw new ValidationError([" + __err + "]); ";
            } else {
              out += " validate.errors = [" + __err + "]; return false; ";
            }
          } else {
            out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
          }
          out += " } ";
          $errSchemaPath = $currErrSchemaPath;
          if ($breakOnError) {
            $closingBraces += "}";
            out += " else { ";
          }
        }
        var arr1 = $schema;
        if (arr1) {
          var $sch, $i = -1, l1 = arr1.length - 1;
          while ($i < l1) {
            $sch = arr1[$i += 1];
            if (it.opts.strictKeywords ? typeof $sch == "object" && Object.keys($sch).length > 0 || $sch === false : it.util.schemaHasRules($sch, it.RULES.all)) {
              out += " " + $nextValid + " = true; if (" + $data + ".length > " + $i + ") { ";
              var $passData = $data + "[" + $i + "]";
              $it.schema = $sch;
              $it.schemaPath = $schemaPath + "[" + $i + "]";
              $it.errSchemaPath = $errSchemaPath + "/" + $i;
              $it.errorPath = it.util.getPathExpr(it.errorPath, $i, it.opts.jsonPointers, true);
              $it.dataPathArr[$dataNxt] = $i;
              var $code = it.validate($it);
              $it.baseId = $currentBaseId;
              if (it.util.varOccurences($code, $nextData) < 2) {
                out += " " + it.util.varReplace($code, $nextData, $passData) + " ";
              } else {
                out += " var " + $nextData + " = " + $passData + "; " + $code + " ";
              }
              out += " }  ";
              if ($breakOnError) {
                out += " if (" + $nextValid + ") { ";
                $closingBraces += "}";
              }
            }
          }
        }
        if (typeof $additionalItems == "object" && (it.opts.strictKeywords ? typeof $additionalItems == "object" && Object.keys($additionalItems).length > 0 || $additionalItems === false : it.util.schemaHasRules($additionalItems, it.RULES.all))) {
          $it.schema = $additionalItems;
          $it.schemaPath = it.schemaPath + ".additionalItems";
          $it.errSchemaPath = it.errSchemaPath + "/additionalItems";
          out += " " + $nextValid + " = true; if (" + $data + ".length > " + $schema.length + ") {  for (var " + $idx + " = " + $schema.length + "; " + $idx + " < " + $data + ".length; " + $idx + "++) { ";
          $it.errorPath = it.util.getPathExpr(it.errorPath, $idx, it.opts.jsonPointers, true);
          var $passData = $data + "[" + $idx + "]";
          $it.dataPathArr[$dataNxt] = $idx;
          var $code = it.validate($it);
          $it.baseId = $currentBaseId;
          if (it.util.varOccurences($code, $nextData) < 2) {
            out += " " + it.util.varReplace($code, $nextData, $passData) + " ";
          } else {
            out += " var " + $nextData + " = " + $passData + "; " + $code + " ";
          }
          if ($breakOnError) {
            out += " if (!" + $nextValid + ") break; ";
          }
          out += " } }  ";
          if ($breakOnError) {
            out += " if (" + $nextValid + ") { ";
            $closingBraces += "}";
          }
        }
      } else if (it.opts.strictKeywords ? typeof $schema == "object" && Object.keys($schema).length > 0 || $schema === false : it.util.schemaHasRules($schema, it.RULES.all)) {
        $it.schema = $schema;
        $it.schemaPath = $schemaPath;
        $it.errSchemaPath = $errSchemaPath;
        out += "  for (var " + $idx + " = 0; " + $idx + " < " + $data + ".length; " + $idx + "++) { ";
        $it.errorPath = it.util.getPathExpr(it.errorPath, $idx, it.opts.jsonPointers, true);
        var $passData = $data + "[" + $idx + "]";
        $it.dataPathArr[$dataNxt] = $idx;
        var $code = it.validate($it);
        $it.baseId = $currentBaseId;
        if (it.util.varOccurences($code, $nextData) < 2) {
          out += " " + it.util.varReplace($code, $nextData, $passData) + " ";
        } else {
          out += " var " + $nextData + " = " + $passData + "; " + $code + " ";
        }
        if ($breakOnError) {
          out += " if (!" + $nextValid + ") break; ";
        }
        out += " }";
      }
      if ($breakOnError) {
        out += " " + $closingBraces + " if (" + $errs + " == errors) {";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/_limit.js
var require_limit = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/_limit.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate__limit(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $errorKeyword;
      var $data = "data" + ($dataLvl || "");
      var $isData = it.opts.$data && $schema && $schema.$data, $schemaValue;
      if ($isData) {
        out += " var schema" + $lvl + " = " + it.util.getData($schema.$data, $dataLvl, it.dataPathArr) + "; ";
        $schemaValue = "schema" + $lvl;
      } else {
        $schemaValue = $schema;
      }
      var $isMax = $keyword == "maximum", $exclusiveKeyword = $isMax ? "exclusiveMaximum" : "exclusiveMinimum", $schemaExcl = it.schema[$exclusiveKeyword], $isDataExcl = it.opts.$data && $schemaExcl && $schemaExcl.$data, $op = $isMax ? "<" : ">", $notOp = $isMax ? ">" : "<", $errorKeyword = void 0;
      if (!($isData || typeof $schema == "number" || $schema === void 0)) {
        throw new Error($keyword + " must be number");
      }
      if (!($isDataExcl || $schemaExcl === void 0 || typeof $schemaExcl == "number" || typeof $schemaExcl == "boolean")) {
        throw new Error($exclusiveKeyword + " must be number or boolean");
      }
      if ($isDataExcl) {
        var $schemaValueExcl = it.util.getData($schemaExcl.$data, $dataLvl, it.dataPathArr), $exclusive = "exclusive" + $lvl, $exclType = "exclType" + $lvl, $exclIsNumber = "exclIsNumber" + $lvl, $opExpr = "op" + $lvl, $opStr = "' + " + $opExpr + " + '";
        out += " var schemaExcl" + $lvl + " = " + $schemaValueExcl + "; ";
        $schemaValueExcl = "schemaExcl" + $lvl;
        out += " var " + $exclusive + "; var " + $exclType + " = typeof " + $schemaValueExcl + "; if (" + $exclType + " != 'boolean' && " + $exclType + " != 'undefined' && " + $exclType + " != 'number') { ";
        var $errorKeyword = $exclusiveKeyword;
        var $$outStack = $$outStack || [];
        $$outStack.push(out);
        out = "";
        if (it.createErrors !== false) {
          out += " { keyword: '" + ($errorKeyword || "_exclusiveLimit") + "' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: {} ";
          if (it.opts.messages !== false) {
            out += " , message: '" + $exclusiveKeyword + " should be boolean' ";
          }
          if (it.opts.verbose) {
            out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
          }
          out += " } ";
        } else {
          out += " {} ";
        }
        var __err = out;
        out = $$outStack.pop();
        if (!it.compositeRule && $breakOnError) {
          if (it.async) {
            out += " throw new ValidationError([" + __err + "]); ";
          } else {
            out += " validate.errors = [" + __err + "]; return false; ";
          }
        } else {
          out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
        }
        out += " } else if ( ";
        if ($isData) {
          out += " (" + $schemaValue + " !== undefined && typeof " + $schemaValue + " != 'number') || ";
        }
        out += " " + $exclType + " == 'number' ? ( (" + $exclusive + " = " + $schemaValue + " === undefined || " + $schemaValueExcl + " " + $op + "= " + $schemaValue + ") ? " + $data + " " + $notOp + "= " + $schemaValueExcl + " : " + $data + " " + $notOp + " " + $schemaValue + " ) : ( (" + $exclusive + " = " + $schemaValueExcl + " === true) ? " + $data + " " + $notOp + "= " + $schemaValue + " : " + $data + " " + $notOp + " " + $schemaValue + " ) || " + $data + " !== " + $data + ") { var op" + $lvl + " = " + $exclusive + " ? '" + $op + "' : '" + $op + "='; ";
        if ($schema === void 0) {
          $errorKeyword = $exclusiveKeyword;
          $errSchemaPath = it.errSchemaPath + "/" + $exclusiveKeyword;
          $schemaValue = $schemaValueExcl;
          $isData = $isDataExcl;
        }
      } else {
        var $exclIsNumber = typeof $schemaExcl == "number", $opStr = $op;
        if ($exclIsNumber && $isData) {
          var $opExpr = "'" + $opStr + "'";
          out += " if ( ";
          if ($isData) {
            out += " (" + $schemaValue + " !== undefined && typeof " + $schemaValue + " != 'number') || ";
          }
          out += " ( " + $schemaValue + " === undefined || " + $schemaExcl + " " + $op + "= " + $schemaValue + " ? " + $data + " " + $notOp + "= " + $schemaExcl + " : " + $data + " " + $notOp + " " + $schemaValue + " ) || " + $data + " !== " + $data + ") { ";
        } else {
          if ($exclIsNumber && $schema === void 0) {
            $exclusive = true;
            $errorKeyword = $exclusiveKeyword;
            $errSchemaPath = it.errSchemaPath + "/" + $exclusiveKeyword;
            $schemaValue = $schemaExcl;
            $notOp += "=";
          } else {
            if ($exclIsNumber)
              $schemaValue = Math[$isMax ? "min" : "max"]($schemaExcl, $schema);
            if ($schemaExcl === ($exclIsNumber ? $schemaValue : true)) {
              $exclusive = true;
              $errorKeyword = $exclusiveKeyword;
              $errSchemaPath = it.errSchemaPath + "/" + $exclusiveKeyword;
              $notOp += "=";
            } else {
              $exclusive = false;
              $opStr += "=";
            }
          }
          var $opExpr = "'" + $opStr + "'";
          out += " if ( ";
          if ($isData) {
            out += " (" + $schemaValue + " !== undefined && typeof " + $schemaValue + " != 'number') || ";
          }
          out += " " + $data + " " + $notOp + " " + $schemaValue + " || " + $data + " !== " + $data + ") { ";
        }
      }
      $errorKeyword = $errorKeyword || $keyword;
      var $$outStack = $$outStack || [];
      $$outStack.push(out);
      out = "";
      if (it.createErrors !== false) {
        out += " { keyword: '" + ($errorKeyword || "_limit") + "' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { comparison: " + $opExpr + ", limit: " + $schemaValue + ", exclusive: " + $exclusive + " } ";
        if (it.opts.messages !== false) {
          out += " , message: 'should be " + $opStr + " ";
          if ($isData) {
            out += "' + " + $schemaValue;
          } else {
            out += "" + $schemaValue + "'";
          }
        }
        if (it.opts.verbose) {
          out += " , schema:  ";
          if ($isData) {
            out += "validate.schema" + $schemaPath;
          } else {
            out += "" + $schema;
          }
          out += "         , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
        }
        out += " } ";
      } else {
        out += " {} ";
      }
      var __err = out;
      out = $$outStack.pop();
      if (!it.compositeRule && $breakOnError) {
        if (it.async) {
          out += " throw new ValidationError([" + __err + "]); ";
        } else {
          out += " validate.errors = [" + __err + "]; return false; ";
        }
      } else {
        out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
      }
      out += " } ";
      if ($breakOnError) {
        out += " else { ";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/_limitItems.js
var require_limitItems = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/_limitItems.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate__limitItems(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $errorKeyword;
      var $data = "data" + ($dataLvl || "");
      var $isData = it.opts.$data && $schema && $schema.$data, $schemaValue;
      if ($isData) {
        out += " var schema" + $lvl + " = " + it.util.getData($schema.$data, $dataLvl, it.dataPathArr) + "; ";
        $schemaValue = "schema" + $lvl;
      } else {
        $schemaValue = $schema;
      }
      if (!($isData || typeof $schema == "number")) {
        throw new Error($keyword + " must be number");
      }
      var $op = $keyword == "maxItems" ? ">" : "<";
      out += "if ( ";
      if ($isData) {
        out += " (" + $schemaValue + " !== undefined && typeof " + $schemaValue + " != 'number') || ";
      }
      out += " " + $data + ".length " + $op + " " + $schemaValue + ") { ";
      var $errorKeyword = $keyword;
      var $$outStack = $$outStack || [];
      $$outStack.push(out);
      out = "";
      if (it.createErrors !== false) {
        out += " { keyword: '" + ($errorKeyword || "_limitItems") + "' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { limit: " + $schemaValue + " } ";
        if (it.opts.messages !== false) {
          out += " , message: 'should NOT have ";
          if ($keyword == "maxItems") {
            out += "more";
          } else {
            out += "fewer";
          }
          out += " than ";
          if ($isData) {
            out += "' + " + $schemaValue + " + '";
          } else {
            out += "" + $schema;
          }
          out += " items' ";
        }
        if (it.opts.verbose) {
          out += " , schema:  ";
          if ($isData) {
            out += "validate.schema" + $schemaPath;
          } else {
            out += "" + $schema;
          }
          out += "         , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
        }
        out += " } ";
      } else {
        out += " {} ";
      }
      var __err = out;
      out = $$outStack.pop();
      if (!it.compositeRule && $breakOnError) {
        if (it.async) {
          out += " throw new ValidationError([" + __err + "]); ";
        } else {
          out += " validate.errors = [" + __err + "]; return false; ";
        }
      } else {
        out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
      }
      out += "} ";
      if ($breakOnError) {
        out += " else { ";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/_limitLength.js
var require_limitLength = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/_limitLength.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate__limitLength(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $errorKeyword;
      var $data = "data" + ($dataLvl || "");
      var $isData = it.opts.$data && $schema && $schema.$data, $schemaValue;
      if ($isData) {
        out += " var schema" + $lvl + " = " + it.util.getData($schema.$data, $dataLvl, it.dataPathArr) + "; ";
        $schemaValue = "schema" + $lvl;
      } else {
        $schemaValue = $schema;
      }
      if (!($isData || typeof $schema == "number")) {
        throw new Error($keyword + " must be number");
      }
      var $op = $keyword == "maxLength" ? ">" : "<";
      out += "if ( ";
      if ($isData) {
        out += " (" + $schemaValue + " !== undefined && typeof " + $schemaValue + " != 'number') || ";
      }
      if (it.opts.unicode === false) {
        out += " " + $data + ".length ";
      } else {
        out += " ucs2length(" + $data + ") ";
      }
      out += " " + $op + " " + $schemaValue + ") { ";
      var $errorKeyword = $keyword;
      var $$outStack = $$outStack || [];
      $$outStack.push(out);
      out = "";
      if (it.createErrors !== false) {
        out += " { keyword: '" + ($errorKeyword || "_limitLength") + "' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { limit: " + $schemaValue + " } ";
        if (it.opts.messages !== false) {
          out += " , message: 'should NOT be ";
          if ($keyword == "maxLength") {
            out += "longer";
          } else {
            out += "shorter";
          }
          out += " than ";
          if ($isData) {
            out += "' + " + $schemaValue + " + '";
          } else {
            out += "" + $schema;
          }
          out += " characters' ";
        }
        if (it.opts.verbose) {
          out += " , schema:  ";
          if ($isData) {
            out += "validate.schema" + $schemaPath;
          } else {
            out += "" + $schema;
          }
          out += "         , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
        }
        out += " } ";
      } else {
        out += " {} ";
      }
      var __err = out;
      out = $$outStack.pop();
      if (!it.compositeRule && $breakOnError) {
        if (it.async) {
          out += " throw new ValidationError([" + __err + "]); ";
        } else {
          out += " validate.errors = [" + __err + "]; return false; ";
        }
      } else {
        out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
      }
      out += "} ";
      if ($breakOnError) {
        out += " else { ";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/_limitProperties.js
var require_limitProperties = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/_limitProperties.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate__limitProperties(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $errorKeyword;
      var $data = "data" + ($dataLvl || "");
      var $isData = it.opts.$data && $schema && $schema.$data, $schemaValue;
      if ($isData) {
        out += " var schema" + $lvl + " = " + it.util.getData($schema.$data, $dataLvl, it.dataPathArr) + "; ";
        $schemaValue = "schema" + $lvl;
      } else {
        $schemaValue = $schema;
      }
      if (!($isData || typeof $schema == "number")) {
        throw new Error($keyword + " must be number");
      }
      var $op = $keyword == "maxProperties" ? ">" : "<";
      out += "if ( ";
      if ($isData) {
        out += " (" + $schemaValue + " !== undefined && typeof " + $schemaValue + " != 'number') || ";
      }
      out += " Object.keys(" + $data + ").length " + $op + " " + $schemaValue + ") { ";
      var $errorKeyword = $keyword;
      var $$outStack = $$outStack || [];
      $$outStack.push(out);
      out = "";
      if (it.createErrors !== false) {
        out += " { keyword: '" + ($errorKeyword || "_limitProperties") + "' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { limit: " + $schemaValue + " } ";
        if (it.opts.messages !== false) {
          out += " , message: 'should NOT have ";
          if ($keyword == "maxProperties") {
            out += "more";
          } else {
            out += "fewer";
          }
          out += " than ";
          if ($isData) {
            out += "' + " + $schemaValue + " + '";
          } else {
            out += "" + $schema;
          }
          out += " properties' ";
        }
        if (it.opts.verbose) {
          out += " , schema:  ";
          if ($isData) {
            out += "validate.schema" + $schemaPath;
          } else {
            out += "" + $schema;
          }
          out += "         , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
        }
        out += " } ";
      } else {
        out += " {} ";
      }
      var __err = out;
      out = $$outStack.pop();
      if (!it.compositeRule && $breakOnError) {
        if (it.async) {
          out += " throw new ValidationError([" + __err + "]); ";
        } else {
          out += " validate.errors = [" + __err + "]; return false; ";
        }
      } else {
        out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
      }
      out += "} ";
      if ($breakOnError) {
        out += " else { ";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/multipleOf.js
var require_multipleOf = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/multipleOf.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_multipleOf(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $isData = it.opts.$data && $schema && $schema.$data, $schemaValue;
      if ($isData) {
        out += " var schema" + $lvl + " = " + it.util.getData($schema.$data, $dataLvl, it.dataPathArr) + "; ";
        $schemaValue = "schema" + $lvl;
      } else {
        $schemaValue = $schema;
      }
      if (!($isData || typeof $schema == "number")) {
        throw new Error($keyword + " must be number");
      }
      out += "var division" + $lvl + ";if (";
      if ($isData) {
        out += " " + $schemaValue + " !== undefined && ( typeof " + $schemaValue + " != 'number' || ";
      }
      out += " (division" + $lvl + " = " + $data + " / " + $schemaValue + ", ";
      if (it.opts.multipleOfPrecision) {
        out += " Math.abs(Math.round(division" + $lvl + ") - division" + $lvl + ") > 1e-" + it.opts.multipleOfPrecision + " ";
      } else {
        out += " division" + $lvl + " !== parseInt(division" + $lvl + ") ";
      }
      out += " ) ";
      if ($isData) {
        out += "  )  ";
      }
      out += " ) {   ";
      var $$outStack = $$outStack || [];
      $$outStack.push(out);
      out = "";
      if (it.createErrors !== false) {
        out += " { keyword: 'multipleOf' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { multipleOf: " + $schemaValue + " } ";
        if (it.opts.messages !== false) {
          out += " , message: 'should be multiple of ";
          if ($isData) {
            out += "' + " + $schemaValue;
          } else {
            out += "" + $schemaValue + "'";
          }
        }
        if (it.opts.verbose) {
          out += " , schema:  ";
          if ($isData) {
            out += "validate.schema" + $schemaPath;
          } else {
            out += "" + $schema;
          }
          out += "         , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
        }
        out += " } ";
      } else {
        out += " {} ";
      }
      var __err = out;
      out = $$outStack.pop();
      if (!it.compositeRule && $breakOnError) {
        if (it.async) {
          out += " throw new ValidationError([" + __err + "]); ";
        } else {
          out += " validate.errors = [" + __err + "]; return false; ";
        }
      } else {
        out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
      }
      out += "} ";
      if ($breakOnError) {
        out += " else { ";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/not.js
var require_not = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/not.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_not(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $errs = "errs__" + $lvl;
      var $it = it.util.copy(it);
      $it.level++;
      var $nextValid = "valid" + $it.level;
      if (it.opts.strictKeywords ? typeof $schema == "object" && Object.keys($schema).length > 0 || $schema === false : it.util.schemaHasRules($schema, it.RULES.all)) {
        $it.schema = $schema;
        $it.schemaPath = $schemaPath;
        $it.errSchemaPath = $errSchemaPath;
        out += " var " + $errs + " = errors;  ";
        var $wasComposite = it.compositeRule;
        it.compositeRule = $it.compositeRule = true;
        $it.createErrors = false;
        var $allErrorsOption;
        if ($it.opts.allErrors) {
          $allErrorsOption = $it.opts.allErrors;
          $it.opts.allErrors = false;
        }
        out += " " + it.validate($it) + " ";
        $it.createErrors = true;
        if ($allErrorsOption)
          $it.opts.allErrors = $allErrorsOption;
        it.compositeRule = $it.compositeRule = $wasComposite;
        out += " if (" + $nextValid + ") {   ";
        var $$outStack = $$outStack || [];
        $$outStack.push(out);
        out = "";
        if (it.createErrors !== false) {
          out += " { keyword: 'not' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: {} ";
          if (it.opts.messages !== false) {
            out += " , message: 'should NOT be valid' ";
          }
          if (it.opts.verbose) {
            out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
          }
          out += " } ";
        } else {
          out += " {} ";
        }
        var __err = out;
        out = $$outStack.pop();
        if (!it.compositeRule && $breakOnError) {
          if (it.async) {
            out += " throw new ValidationError([" + __err + "]); ";
          } else {
            out += " validate.errors = [" + __err + "]; return false; ";
          }
        } else {
          out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
        }
        out += " } else {  errors = " + $errs + "; if (vErrors !== null) { if (" + $errs + ") vErrors.length = " + $errs + "; else vErrors = null; } ";
        if (it.opts.allErrors) {
          out += " } ";
        }
      } else {
        out += "  var err =   ";
        if (it.createErrors !== false) {
          out += " { keyword: 'not' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: {} ";
          if (it.opts.messages !== false) {
            out += " , message: 'should NOT be valid' ";
          }
          if (it.opts.verbose) {
            out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
          }
          out += " } ";
        } else {
          out += " {} ";
        }
        out += ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
        if ($breakOnError) {
          out += " if (false) { ";
        }
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/oneOf.js
var require_oneOf = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/oneOf.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_oneOf(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $valid = "valid" + $lvl;
      var $errs = "errs__" + $lvl;
      var $it = it.util.copy(it);
      var $closingBraces = "";
      $it.level++;
      var $nextValid = "valid" + $it.level;
      var $currentBaseId = $it.baseId, $prevValid = "prevValid" + $lvl, $passingSchemas = "passingSchemas" + $lvl;
      out += "var " + $errs + " = errors , " + $prevValid + " = false , " + $valid + " = false , " + $passingSchemas + " = null; ";
      var $wasComposite = it.compositeRule;
      it.compositeRule = $it.compositeRule = true;
      var arr1 = $schema;
      if (arr1) {
        var $sch, $i = -1, l1 = arr1.length - 1;
        while ($i < l1) {
          $sch = arr1[$i += 1];
          if (it.opts.strictKeywords ? typeof $sch == "object" && Object.keys($sch).length > 0 || $sch === false : it.util.schemaHasRules($sch, it.RULES.all)) {
            $it.schema = $sch;
            $it.schemaPath = $schemaPath + "[" + $i + "]";
            $it.errSchemaPath = $errSchemaPath + "/" + $i;
            out += "  " + it.validate($it) + " ";
            $it.baseId = $currentBaseId;
          } else {
            out += " var " + $nextValid + " = true; ";
          }
          if ($i) {
            out += " if (" + $nextValid + " && " + $prevValid + ") { " + $valid + " = false; " + $passingSchemas + " = [" + $passingSchemas + ", " + $i + "]; } else { ";
            $closingBraces += "}";
          }
          out += " if (" + $nextValid + ") { " + $valid + " = " + $prevValid + " = true; " + $passingSchemas + " = " + $i + "; }";
        }
      }
      it.compositeRule = $it.compositeRule = $wasComposite;
      out += "" + $closingBraces + "if (!" + $valid + ") {   var err =   ";
      if (it.createErrors !== false) {
        out += " { keyword: 'oneOf' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { passingSchemas: " + $passingSchemas + " } ";
        if (it.opts.messages !== false) {
          out += " , message: 'should match exactly one schema in oneOf' ";
        }
        if (it.opts.verbose) {
          out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
        }
        out += " } ";
      } else {
        out += " {} ";
      }
      out += ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
      if (!it.compositeRule && $breakOnError) {
        if (it.async) {
          out += " throw new ValidationError(vErrors); ";
        } else {
          out += " validate.errors = vErrors; return false; ";
        }
      }
      out += "} else {  errors = " + $errs + "; if (vErrors !== null) { if (" + $errs + ") vErrors.length = " + $errs + "; else vErrors = null; }";
      if (it.opts.allErrors) {
        out += " } ";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/pattern.js
var require_pattern = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/pattern.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_pattern(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $isData = it.opts.$data && $schema && $schema.$data, $schemaValue;
      if ($isData) {
        out += " var schema" + $lvl + " = " + it.util.getData($schema.$data, $dataLvl, it.dataPathArr) + "; ";
        $schemaValue = "schema" + $lvl;
      } else {
        $schemaValue = $schema;
      }
      var $regexp = $isData ? "(new RegExp(" + $schemaValue + "))" : it.usePattern($schema);
      out += "if ( ";
      if ($isData) {
        out += " (" + $schemaValue + " !== undefined && typeof " + $schemaValue + " != 'string') || ";
      }
      out += " !" + $regexp + ".test(" + $data + ") ) {   ";
      var $$outStack = $$outStack || [];
      $$outStack.push(out);
      out = "";
      if (it.createErrors !== false) {
        out += " { keyword: 'pattern' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { pattern:  ";
        if ($isData) {
          out += "" + $schemaValue;
        } else {
          out += "" + it.util.toQuotedString($schema);
        }
        out += "  } ";
        if (it.opts.messages !== false) {
          out += ` , message: 'should match pattern "`;
          if ($isData) {
            out += "' + " + $schemaValue + " + '";
          } else {
            out += "" + it.util.escapeQuotes($schema);
          }
          out += `"' `;
        }
        if (it.opts.verbose) {
          out += " , schema:  ";
          if ($isData) {
            out += "validate.schema" + $schemaPath;
          } else {
            out += "" + it.util.toQuotedString($schema);
          }
          out += "         , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
        }
        out += " } ";
      } else {
        out += " {} ";
      }
      var __err = out;
      out = $$outStack.pop();
      if (!it.compositeRule && $breakOnError) {
        if (it.async) {
          out += " throw new ValidationError([" + __err + "]); ";
        } else {
          out += " validate.errors = [" + __err + "]; return false; ";
        }
      } else {
        out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
      }
      out += "} ";
      if ($breakOnError) {
        out += " else { ";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/properties.js
var require_properties = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/properties.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_properties(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $errs = "errs__" + $lvl;
      var $it = it.util.copy(it);
      var $closingBraces = "";
      $it.level++;
      var $nextValid = "valid" + $it.level;
      var $key = "key" + $lvl, $idx = "idx" + $lvl, $dataNxt = $it.dataLevel = it.dataLevel + 1, $nextData = "data" + $dataNxt, $dataProperties = "dataProperties" + $lvl;
      var $schemaKeys = Object.keys($schema || {}).filter(notProto), $pProperties = it.schema.patternProperties || {}, $pPropertyKeys = Object.keys($pProperties).filter(notProto), $aProperties = it.schema.additionalProperties, $someProperties = $schemaKeys.length || $pPropertyKeys.length, $noAdditional = $aProperties === false, $additionalIsSchema = typeof $aProperties == "object" && Object.keys($aProperties).length, $removeAdditional = it.opts.removeAdditional, $checkAdditional = $noAdditional || $additionalIsSchema || $removeAdditional, $ownProperties = it.opts.ownProperties, $currentBaseId = it.baseId;
      var $required = it.schema.required;
      if ($required && !(it.opts.$data && $required.$data) && $required.length < it.opts.loopRequired) {
        var $requiredHash = it.util.toHash($required);
      }
      function notProto(p) {
        return p !== "__proto__";
      }
      out += "var " + $errs + " = errors;var " + $nextValid + " = true;";
      if ($ownProperties) {
        out += " var " + $dataProperties + " = undefined;";
      }
      if ($checkAdditional) {
        if ($ownProperties) {
          out += " " + $dataProperties + " = " + $dataProperties + " || Object.keys(" + $data + "); for (var " + $idx + "=0; " + $idx + "<" + $dataProperties + ".length; " + $idx + "++) { var " + $key + " = " + $dataProperties + "[" + $idx + "]; ";
        } else {
          out += " for (var " + $key + " in " + $data + ") { ";
        }
        if ($someProperties) {
          out += " var isAdditional" + $lvl + " = !(false ";
          if ($schemaKeys.length) {
            if ($schemaKeys.length > 8) {
              out += " || validate.schema" + $schemaPath + ".hasOwnProperty(" + $key + ") ";
            } else {
              var arr1 = $schemaKeys;
              if (arr1) {
                var $propertyKey, i1 = -1, l1 = arr1.length - 1;
                while (i1 < l1) {
                  $propertyKey = arr1[i1 += 1];
                  out += " || " + $key + " == " + it.util.toQuotedString($propertyKey) + " ";
                }
              }
            }
          }
          if ($pPropertyKeys.length) {
            var arr2 = $pPropertyKeys;
            if (arr2) {
              var $pProperty, $i = -1, l2 = arr2.length - 1;
              while ($i < l2) {
                $pProperty = arr2[$i += 1];
                out += " || " + it.usePattern($pProperty) + ".test(" + $key + ") ";
              }
            }
          }
          out += " ); if (isAdditional" + $lvl + ") { ";
        }
        if ($removeAdditional == "all") {
          out += " delete " + $data + "[" + $key + "]; ";
        } else {
          var $currentErrorPath = it.errorPath;
          var $additionalProperty = "' + " + $key + " + '";
          if (it.opts._errorDataPathProperty) {
            it.errorPath = it.util.getPathExpr(it.errorPath, $key, it.opts.jsonPointers);
          }
          if ($noAdditional) {
            if ($removeAdditional) {
              out += " delete " + $data + "[" + $key + "]; ";
            } else {
              out += " " + $nextValid + " = false; ";
              var $currErrSchemaPath = $errSchemaPath;
              $errSchemaPath = it.errSchemaPath + "/additionalProperties";
              var $$outStack = $$outStack || [];
              $$outStack.push(out);
              out = "";
              if (it.createErrors !== false) {
                out += " { keyword: 'additionalProperties' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { additionalProperty: '" + $additionalProperty + "' } ";
                if (it.opts.messages !== false) {
                  out += " , message: '";
                  if (it.opts._errorDataPathProperty) {
                    out += "is an invalid additional property";
                  } else {
                    out += "should NOT have additional properties";
                  }
                  out += "' ";
                }
                if (it.opts.verbose) {
                  out += " , schema: false , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
                }
                out += " } ";
              } else {
                out += " {} ";
              }
              var __err = out;
              out = $$outStack.pop();
              if (!it.compositeRule && $breakOnError) {
                if (it.async) {
                  out += " throw new ValidationError([" + __err + "]); ";
                } else {
                  out += " validate.errors = [" + __err + "]; return false; ";
                }
              } else {
                out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
              }
              $errSchemaPath = $currErrSchemaPath;
              if ($breakOnError) {
                out += " break; ";
              }
            }
          } else if ($additionalIsSchema) {
            if ($removeAdditional == "failing") {
              out += " var " + $errs + " = errors;  ";
              var $wasComposite = it.compositeRule;
              it.compositeRule = $it.compositeRule = true;
              $it.schema = $aProperties;
              $it.schemaPath = it.schemaPath + ".additionalProperties";
              $it.errSchemaPath = it.errSchemaPath + "/additionalProperties";
              $it.errorPath = it.opts._errorDataPathProperty ? it.errorPath : it.util.getPathExpr(it.errorPath, $key, it.opts.jsonPointers);
              var $passData = $data + "[" + $key + "]";
              $it.dataPathArr[$dataNxt] = $key;
              var $code = it.validate($it);
              $it.baseId = $currentBaseId;
              if (it.util.varOccurences($code, $nextData) < 2) {
                out += " " + it.util.varReplace($code, $nextData, $passData) + " ";
              } else {
                out += " var " + $nextData + " = " + $passData + "; " + $code + " ";
              }
              out += " if (!" + $nextValid + ") { errors = " + $errs + "; if (validate.errors !== null) { if (errors) validate.errors.length = errors; else validate.errors = null; } delete " + $data + "[" + $key + "]; }  ";
              it.compositeRule = $it.compositeRule = $wasComposite;
            } else {
              $it.schema = $aProperties;
              $it.schemaPath = it.schemaPath + ".additionalProperties";
              $it.errSchemaPath = it.errSchemaPath + "/additionalProperties";
              $it.errorPath = it.opts._errorDataPathProperty ? it.errorPath : it.util.getPathExpr(it.errorPath, $key, it.opts.jsonPointers);
              var $passData = $data + "[" + $key + "]";
              $it.dataPathArr[$dataNxt] = $key;
              var $code = it.validate($it);
              $it.baseId = $currentBaseId;
              if (it.util.varOccurences($code, $nextData) < 2) {
                out += " " + it.util.varReplace($code, $nextData, $passData) + " ";
              } else {
                out += " var " + $nextData + " = " + $passData + "; " + $code + " ";
              }
              if ($breakOnError) {
                out += " if (!" + $nextValid + ") break; ";
              }
            }
          }
          it.errorPath = $currentErrorPath;
        }
        if ($someProperties) {
          out += " } ";
        }
        out += " }  ";
        if ($breakOnError) {
          out += " if (" + $nextValid + ") { ";
          $closingBraces += "}";
        }
      }
      var $useDefaults = it.opts.useDefaults && !it.compositeRule;
      if ($schemaKeys.length) {
        var arr3 = $schemaKeys;
        if (arr3) {
          var $propertyKey, i3 = -1, l3 = arr3.length - 1;
          while (i3 < l3) {
            $propertyKey = arr3[i3 += 1];
            var $sch = $schema[$propertyKey];
            if (it.opts.strictKeywords ? typeof $sch == "object" && Object.keys($sch).length > 0 || $sch === false : it.util.schemaHasRules($sch, it.RULES.all)) {
              var $prop = it.util.getProperty($propertyKey), $passData = $data + $prop, $hasDefault = $useDefaults && $sch.default !== void 0;
              $it.schema = $sch;
              $it.schemaPath = $schemaPath + $prop;
              $it.errSchemaPath = $errSchemaPath + "/" + it.util.escapeFragment($propertyKey);
              $it.errorPath = it.util.getPath(it.errorPath, $propertyKey, it.opts.jsonPointers);
              $it.dataPathArr[$dataNxt] = it.util.toQuotedString($propertyKey);
              var $code = it.validate($it);
              $it.baseId = $currentBaseId;
              if (it.util.varOccurences($code, $nextData) < 2) {
                $code = it.util.varReplace($code, $nextData, $passData);
                var $useData = $passData;
              } else {
                var $useData = $nextData;
                out += " var " + $nextData + " = " + $passData + "; ";
              }
              if ($hasDefault) {
                out += " " + $code + " ";
              } else {
                if ($requiredHash && $requiredHash[$propertyKey]) {
                  out += " if ( " + $useData + " === undefined ";
                  if ($ownProperties) {
                    out += " || ! Object.prototype.hasOwnProperty.call(" + $data + ", '" + it.util.escapeQuotes($propertyKey) + "') ";
                  }
                  out += ") { " + $nextValid + " = false; ";
                  var $currentErrorPath = it.errorPath, $currErrSchemaPath = $errSchemaPath, $missingProperty = it.util.escapeQuotes($propertyKey);
                  if (it.opts._errorDataPathProperty) {
                    it.errorPath = it.util.getPath($currentErrorPath, $propertyKey, it.opts.jsonPointers);
                  }
                  $errSchemaPath = it.errSchemaPath + "/required";
                  var $$outStack = $$outStack || [];
                  $$outStack.push(out);
                  out = "";
                  if (it.createErrors !== false) {
                    out += " { keyword: 'required' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { missingProperty: '" + $missingProperty + "' } ";
                    if (it.opts.messages !== false) {
                      out += " , message: '";
                      if (it.opts._errorDataPathProperty) {
                        out += "is a required property";
                      } else {
                        out += "should have required property \\'" + $missingProperty + "\\'";
                      }
                      out += "' ";
                    }
                    if (it.opts.verbose) {
                      out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
                    }
                    out += " } ";
                  } else {
                    out += " {} ";
                  }
                  var __err = out;
                  out = $$outStack.pop();
                  if (!it.compositeRule && $breakOnError) {
                    if (it.async) {
                      out += " throw new ValidationError([" + __err + "]); ";
                    } else {
                      out += " validate.errors = [" + __err + "]; return false; ";
                    }
                  } else {
                    out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
                  }
                  $errSchemaPath = $currErrSchemaPath;
                  it.errorPath = $currentErrorPath;
                  out += " } else { ";
                } else {
                  if ($breakOnError) {
                    out += " if ( " + $useData + " === undefined ";
                    if ($ownProperties) {
                      out += " || ! Object.prototype.hasOwnProperty.call(" + $data + ", '" + it.util.escapeQuotes($propertyKey) + "') ";
                    }
                    out += ") { " + $nextValid + " = true; } else { ";
                  } else {
                    out += " if (" + $useData + " !== undefined ";
                    if ($ownProperties) {
                      out += " &&   Object.prototype.hasOwnProperty.call(" + $data + ", '" + it.util.escapeQuotes($propertyKey) + "') ";
                    }
                    out += " ) { ";
                  }
                }
                out += " " + $code + " } ";
              }
            }
            if ($breakOnError) {
              out += " if (" + $nextValid + ") { ";
              $closingBraces += "}";
            }
          }
        }
      }
      if ($pPropertyKeys.length) {
        var arr4 = $pPropertyKeys;
        if (arr4) {
          var $pProperty, i4 = -1, l4 = arr4.length - 1;
          while (i4 < l4) {
            $pProperty = arr4[i4 += 1];
            var $sch = $pProperties[$pProperty];
            if (it.opts.strictKeywords ? typeof $sch == "object" && Object.keys($sch).length > 0 || $sch === false : it.util.schemaHasRules($sch, it.RULES.all)) {
              $it.schema = $sch;
              $it.schemaPath = it.schemaPath + ".patternProperties" + it.util.getProperty($pProperty);
              $it.errSchemaPath = it.errSchemaPath + "/patternProperties/" + it.util.escapeFragment($pProperty);
              if ($ownProperties) {
                out += " " + $dataProperties + " = " + $dataProperties + " || Object.keys(" + $data + "); for (var " + $idx + "=0; " + $idx + "<" + $dataProperties + ".length; " + $idx + "++) { var " + $key + " = " + $dataProperties + "[" + $idx + "]; ";
              } else {
                out += " for (var " + $key + " in " + $data + ") { ";
              }
              out += " if (" + it.usePattern($pProperty) + ".test(" + $key + ")) { ";
              $it.errorPath = it.util.getPathExpr(it.errorPath, $key, it.opts.jsonPointers);
              var $passData = $data + "[" + $key + "]";
              $it.dataPathArr[$dataNxt] = $key;
              var $code = it.validate($it);
              $it.baseId = $currentBaseId;
              if (it.util.varOccurences($code, $nextData) < 2) {
                out += " " + it.util.varReplace($code, $nextData, $passData) + " ";
              } else {
                out += " var " + $nextData + " = " + $passData + "; " + $code + " ";
              }
              if ($breakOnError) {
                out += " if (!" + $nextValid + ") break; ";
              }
              out += " } ";
              if ($breakOnError) {
                out += " else " + $nextValid + " = true; ";
              }
              out += " }  ";
              if ($breakOnError) {
                out += " if (" + $nextValid + ") { ";
                $closingBraces += "}";
              }
            }
          }
        }
      }
      if ($breakOnError) {
        out += " " + $closingBraces + " if (" + $errs + " == errors) {";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/propertyNames.js
var require_propertyNames = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/propertyNames.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_propertyNames(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $errs = "errs__" + $lvl;
      var $it = it.util.copy(it);
      var $closingBraces = "";
      $it.level++;
      var $nextValid = "valid" + $it.level;
      out += "var " + $errs + " = errors;";
      if (it.opts.strictKeywords ? typeof $schema == "object" && Object.keys($schema).length > 0 || $schema === false : it.util.schemaHasRules($schema, it.RULES.all)) {
        $it.schema = $schema;
        $it.schemaPath = $schemaPath;
        $it.errSchemaPath = $errSchemaPath;
        var $key = "key" + $lvl, $idx = "idx" + $lvl, $i = "i" + $lvl, $invalidName = "' + " + $key + " + '", $dataNxt = $it.dataLevel = it.dataLevel + 1, $nextData = "data" + $dataNxt, $dataProperties = "dataProperties" + $lvl, $ownProperties = it.opts.ownProperties, $currentBaseId = it.baseId;
        if ($ownProperties) {
          out += " var " + $dataProperties + " = undefined; ";
        }
        if ($ownProperties) {
          out += " " + $dataProperties + " = " + $dataProperties + " || Object.keys(" + $data + "); for (var " + $idx + "=0; " + $idx + "<" + $dataProperties + ".length; " + $idx + "++) { var " + $key + " = " + $dataProperties + "[" + $idx + "]; ";
        } else {
          out += " for (var " + $key + " in " + $data + ") { ";
        }
        out += " var startErrs" + $lvl + " = errors; ";
        var $passData = $key;
        var $wasComposite = it.compositeRule;
        it.compositeRule = $it.compositeRule = true;
        var $code = it.validate($it);
        $it.baseId = $currentBaseId;
        if (it.util.varOccurences($code, $nextData) < 2) {
          out += " " + it.util.varReplace($code, $nextData, $passData) + " ";
        } else {
          out += " var " + $nextData + " = " + $passData + "; " + $code + " ";
        }
        it.compositeRule = $it.compositeRule = $wasComposite;
        out += " if (!" + $nextValid + ") { for (var " + $i + "=startErrs" + $lvl + "; " + $i + "<errors; " + $i + "++) { vErrors[" + $i + "].propertyName = " + $key + "; }   var err =   ";
        if (it.createErrors !== false) {
          out += " { keyword: 'propertyNames' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { propertyName: '" + $invalidName + "' } ";
          if (it.opts.messages !== false) {
            out += " , message: 'property name \\'" + $invalidName + "\\' is invalid' ";
          }
          if (it.opts.verbose) {
            out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
          }
          out += " } ";
        } else {
          out += " {} ";
        }
        out += ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
        if (!it.compositeRule && $breakOnError) {
          if (it.async) {
            out += " throw new ValidationError(vErrors); ";
          } else {
            out += " validate.errors = vErrors; return false; ";
          }
        }
        if ($breakOnError) {
          out += " break; ";
        }
        out += " } }";
      }
      if ($breakOnError) {
        out += " " + $closingBraces + " if (" + $errs + " == errors) {";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/required.js
var require_required = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/required.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_required(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $valid = "valid" + $lvl;
      var $isData = it.opts.$data && $schema && $schema.$data, $schemaValue;
      if ($isData) {
        out += " var schema" + $lvl + " = " + it.util.getData($schema.$data, $dataLvl, it.dataPathArr) + "; ";
        $schemaValue = "schema" + $lvl;
      } else {
        $schemaValue = $schema;
      }
      var $vSchema = "schema" + $lvl;
      if (!$isData) {
        if ($schema.length < it.opts.loopRequired && it.schema.properties && Object.keys(it.schema.properties).length) {
          var $required = [];
          var arr1 = $schema;
          if (arr1) {
            var $property, i1 = -1, l1 = arr1.length - 1;
            while (i1 < l1) {
              $property = arr1[i1 += 1];
              var $propertySch = it.schema.properties[$property];
              if (!($propertySch && (it.opts.strictKeywords ? typeof $propertySch == "object" && Object.keys($propertySch).length > 0 || $propertySch === false : it.util.schemaHasRules($propertySch, it.RULES.all)))) {
                $required[$required.length] = $property;
              }
            }
          }
        } else {
          var $required = $schema;
        }
      }
      if ($isData || $required.length) {
        var $currentErrorPath = it.errorPath, $loopRequired = $isData || $required.length >= it.opts.loopRequired, $ownProperties = it.opts.ownProperties;
        if ($breakOnError) {
          out += " var missing" + $lvl + "; ";
          if ($loopRequired) {
            if (!$isData) {
              out += " var " + $vSchema + " = validate.schema" + $schemaPath + "; ";
            }
            var $i = "i" + $lvl, $propertyPath = "schema" + $lvl + "[" + $i + "]", $missingProperty = "' + " + $propertyPath + " + '";
            if (it.opts._errorDataPathProperty) {
              it.errorPath = it.util.getPathExpr($currentErrorPath, $propertyPath, it.opts.jsonPointers);
            }
            out += " var " + $valid + " = true; ";
            if ($isData) {
              out += " if (schema" + $lvl + " === undefined) " + $valid + " = true; else if (!Array.isArray(schema" + $lvl + ")) " + $valid + " = false; else {";
            }
            out += " for (var " + $i + " = 0; " + $i + " < " + $vSchema + ".length; " + $i + "++) { " + $valid + " = " + $data + "[" + $vSchema + "[" + $i + "]] !== undefined ";
            if ($ownProperties) {
              out += " &&   Object.prototype.hasOwnProperty.call(" + $data + ", " + $vSchema + "[" + $i + "]) ";
            }
            out += "; if (!" + $valid + ") break; } ";
            if ($isData) {
              out += "  }  ";
            }
            out += "  if (!" + $valid + ") {   ";
            var $$outStack = $$outStack || [];
            $$outStack.push(out);
            out = "";
            if (it.createErrors !== false) {
              out += " { keyword: 'required' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { missingProperty: '" + $missingProperty + "' } ";
              if (it.opts.messages !== false) {
                out += " , message: '";
                if (it.opts._errorDataPathProperty) {
                  out += "is a required property";
                } else {
                  out += "should have required property \\'" + $missingProperty + "\\'";
                }
                out += "' ";
              }
              if (it.opts.verbose) {
                out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
              }
              out += " } ";
            } else {
              out += " {} ";
            }
            var __err = out;
            out = $$outStack.pop();
            if (!it.compositeRule && $breakOnError) {
              if (it.async) {
                out += " throw new ValidationError([" + __err + "]); ";
              } else {
                out += " validate.errors = [" + __err + "]; return false; ";
              }
            } else {
              out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
            }
            out += " } else { ";
          } else {
            out += " if ( ";
            var arr2 = $required;
            if (arr2) {
              var $propertyKey, $i = -1, l2 = arr2.length - 1;
              while ($i < l2) {
                $propertyKey = arr2[$i += 1];
                if ($i) {
                  out += " || ";
                }
                var $prop = it.util.getProperty($propertyKey), $useData = $data + $prop;
                out += " ( ( " + $useData + " === undefined ";
                if ($ownProperties) {
                  out += " || ! Object.prototype.hasOwnProperty.call(" + $data + ", '" + it.util.escapeQuotes($propertyKey) + "') ";
                }
                out += ") && (missing" + $lvl + " = " + it.util.toQuotedString(it.opts.jsonPointers ? $propertyKey : $prop) + ") ) ";
              }
            }
            out += ") {  ";
            var $propertyPath = "missing" + $lvl, $missingProperty = "' + " + $propertyPath + " + '";
            if (it.opts._errorDataPathProperty) {
              it.errorPath = it.opts.jsonPointers ? it.util.getPathExpr($currentErrorPath, $propertyPath, true) : $currentErrorPath + " + " + $propertyPath;
            }
            var $$outStack = $$outStack || [];
            $$outStack.push(out);
            out = "";
            if (it.createErrors !== false) {
              out += " { keyword: 'required' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { missingProperty: '" + $missingProperty + "' } ";
              if (it.opts.messages !== false) {
                out += " , message: '";
                if (it.opts._errorDataPathProperty) {
                  out += "is a required property";
                } else {
                  out += "should have required property \\'" + $missingProperty + "\\'";
                }
                out += "' ";
              }
              if (it.opts.verbose) {
                out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
              }
              out += " } ";
            } else {
              out += " {} ";
            }
            var __err = out;
            out = $$outStack.pop();
            if (!it.compositeRule && $breakOnError) {
              if (it.async) {
                out += " throw new ValidationError([" + __err + "]); ";
              } else {
                out += " validate.errors = [" + __err + "]; return false; ";
              }
            } else {
              out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
            }
            out += " } else { ";
          }
        } else {
          if ($loopRequired) {
            if (!$isData) {
              out += " var " + $vSchema + " = validate.schema" + $schemaPath + "; ";
            }
            var $i = "i" + $lvl, $propertyPath = "schema" + $lvl + "[" + $i + "]", $missingProperty = "' + " + $propertyPath + " + '";
            if (it.opts._errorDataPathProperty) {
              it.errorPath = it.util.getPathExpr($currentErrorPath, $propertyPath, it.opts.jsonPointers);
            }
            if ($isData) {
              out += " if (" + $vSchema + " && !Array.isArray(" + $vSchema + ")) {  var err =   ";
              if (it.createErrors !== false) {
                out += " { keyword: 'required' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { missingProperty: '" + $missingProperty + "' } ";
                if (it.opts.messages !== false) {
                  out += " , message: '";
                  if (it.opts._errorDataPathProperty) {
                    out += "is a required property";
                  } else {
                    out += "should have required property \\'" + $missingProperty + "\\'";
                  }
                  out += "' ";
                }
                if (it.opts.verbose) {
                  out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
                }
                out += " } ";
              } else {
                out += " {} ";
              }
              out += ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; } else if (" + $vSchema + " !== undefined) { ";
            }
            out += " for (var " + $i + " = 0; " + $i + " < " + $vSchema + ".length; " + $i + "++) { if (" + $data + "[" + $vSchema + "[" + $i + "]] === undefined ";
            if ($ownProperties) {
              out += " || ! Object.prototype.hasOwnProperty.call(" + $data + ", " + $vSchema + "[" + $i + "]) ";
            }
            out += ") {  var err =   ";
            if (it.createErrors !== false) {
              out += " { keyword: 'required' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { missingProperty: '" + $missingProperty + "' } ";
              if (it.opts.messages !== false) {
                out += " , message: '";
                if (it.opts._errorDataPathProperty) {
                  out += "is a required property";
                } else {
                  out += "should have required property \\'" + $missingProperty + "\\'";
                }
                out += "' ";
              }
              if (it.opts.verbose) {
                out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
              }
              out += " } ";
            } else {
              out += " {} ";
            }
            out += ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; } } ";
            if ($isData) {
              out += "  }  ";
            }
          } else {
            var arr3 = $required;
            if (arr3) {
              var $propertyKey, i3 = -1, l3 = arr3.length - 1;
              while (i3 < l3) {
                $propertyKey = arr3[i3 += 1];
                var $prop = it.util.getProperty($propertyKey), $missingProperty = it.util.escapeQuotes($propertyKey), $useData = $data + $prop;
                if (it.opts._errorDataPathProperty) {
                  it.errorPath = it.util.getPath($currentErrorPath, $propertyKey, it.opts.jsonPointers);
                }
                out += " if ( " + $useData + " === undefined ";
                if ($ownProperties) {
                  out += " || ! Object.prototype.hasOwnProperty.call(" + $data + ", '" + it.util.escapeQuotes($propertyKey) + "') ";
                }
                out += ") {  var err =   ";
                if (it.createErrors !== false) {
                  out += " { keyword: 'required' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { missingProperty: '" + $missingProperty + "' } ";
                  if (it.opts.messages !== false) {
                    out += " , message: '";
                    if (it.opts._errorDataPathProperty) {
                      out += "is a required property";
                    } else {
                      out += "should have required property \\'" + $missingProperty + "\\'";
                    }
                    out += "' ";
                  }
                  if (it.opts.verbose) {
                    out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
                  }
                  out += " } ";
                } else {
                  out += " {} ";
                }
                out += ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; } ";
              }
            }
          }
        }
        it.errorPath = $currentErrorPath;
      } else if ($breakOnError) {
        out += " if (true) {";
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/uniqueItems.js
var require_uniqueItems = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/uniqueItems.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_uniqueItems(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $data = "data" + ($dataLvl || "");
      var $valid = "valid" + $lvl;
      var $isData = it.opts.$data && $schema && $schema.$data, $schemaValue;
      if ($isData) {
        out += " var schema" + $lvl + " = " + it.util.getData($schema.$data, $dataLvl, it.dataPathArr) + "; ";
        $schemaValue = "schema" + $lvl;
      } else {
        $schemaValue = $schema;
      }
      if (($schema || $isData) && it.opts.uniqueItems !== false) {
        if ($isData) {
          out += " var " + $valid + "; if (" + $schemaValue + " === false || " + $schemaValue + " === undefined) " + $valid + " = true; else if (typeof " + $schemaValue + " != 'boolean') " + $valid + " = false; else { ";
        }
        out += " var i = " + $data + ".length , " + $valid + " = true , j; if (i > 1) { ";
        var $itemType = it.schema.items && it.schema.items.type, $typeIsArray = Array.isArray($itemType);
        if (!$itemType || $itemType == "object" || $itemType == "array" || $typeIsArray && ($itemType.indexOf("object") >= 0 || $itemType.indexOf("array") >= 0)) {
          out += " outer: for (;i--;) { for (j = i; j--;) { if (equal(" + $data + "[i], " + $data + "[j])) { " + $valid + " = false; break outer; } } } ";
        } else {
          out += " var itemIndices = {}, item; for (;i--;) { var item = " + $data + "[i]; ";
          var $method = "checkDataType" + ($typeIsArray ? "s" : "");
          out += " if (" + it.util[$method]($itemType, "item", it.opts.strictNumbers, true) + ") continue; ";
          if ($typeIsArray) {
            out += ` if (typeof item == 'string') item = '"' + item; `;
          }
          out += " if (typeof itemIndices[item] == 'number') { " + $valid + " = false; j = itemIndices[item]; break; } itemIndices[item] = i; } ";
        }
        out += " } ";
        if ($isData) {
          out += "  }  ";
        }
        out += " if (!" + $valid + ") {   ";
        var $$outStack = $$outStack || [];
        $$outStack.push(out);
        out = "";
        if (it.createErrors !== false) {
          out += " { keyword: 'uniqueItems' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { i: i, j: j } ";
          if (it.opts.messages !== false) {
            out += " , message: 'should NOT have duplicate items (items ## ' + j + ' and ' + i + ' are identical)' ";
          }
          if (it.opts.verbose) {
            out += " , schema:  ";
            if ($isData) {
              out += "validate.schema" + $schemaPath;
            } else {
              out += "" + $schema;
            }
            out += "         , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
          }
          out += " } ";
        } else {
          out += " {} ";
        }
        var __err = out;
        out = $$outStack.pop();
        if (!it.compositeRule && $breakOnError) {
          if (it.async) {
            out += " throw new ValidationError([" + __err + "]); ";
          } else {
            out += " validate.errors = [" + __err + "]; return false; ";
          }
        } else {
          out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
        }
        out += " } ";
        if ($breakOnError) {
          out += " else { ";
        }
      } else {
        if ($breakOnError) {
          out += " if (true) { ";
        }
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/index.js
var require_dotjs = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/index.js"(exports2, module2) {
    "use strict";
    module2.exports = {
      "$ref": require_ref(),
      allOf: require_allOf(),
      anyOf: require_anyOf(),
      "$comment": require_comment(),
      const: require_const(),
      contains: require_contains(),
      dependencies: require_dependencies(),
      "enum": require_enum(),
      format: require_format(),
      "if": require_if(),
      items: require_items(),
      maximum: require_limit(),
      minimum: require_limit(),
      maxItems: require_limitItems(),
      minItems: require_limitItems(),
      maxLength: require_limitLength(),
      minLength: require_limitLength(),
      maxProperties: require_limitProperties(),
      minProperties: require_limitProperties(),
      multipleOf: require_multipleOf(),
      not: require_not(),
      oneOf: require_oneOf(),
      pattern: require_pattern(),
      properties: require_properties(),
      propertyNames: require_propertyNames(),
      required: require_required(),
      uniqueItems: require_uniqueItems(),
      validate: require_validate()
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/rules.js
var require_rules = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/rules.js"(exports2, module2) {
    "use strict";
    var ruleModules = require_dotjs();
    var toHash = require_util().toHash;
    module2.exports = function rules() {
      var RULES = [
        {
          type: "number",
          rules: [
            { "maximum": ["exclusiveMaximum"] },
            { "minimum": ["exclusiveMinimum"] },
            "multipleOf",
            "format"
          ]
        },
        {
          type: "string",
          rules: ["maxLength", "minLength", "pattern", "format"]
        },
        {
          type: "array",
          rules: ["maxItems", "minItems", "items", "contains", "uniqueItems"]
        },
        {
          type: "object",
          rules: [
            "maxProperties",
            "minProperties",
            "required",
            "dependencies",
            "propertyNames",
            { "properties": ["additionalProperties", "patternProperties"] }
          ]
        },
        { rules: ["$ref", "const", "enum", "not", "anyOf", "oneOf", "allOf", "if"] }
      ];
      var ALL = ["type", "$comment"];
      var KEYWORDS = [
        "$schema",
        "$id",
        "id",
        "$data",
        "$async",
        "title",
        "description",
        "default",
        "definitions",
        "examples",
        "readOnly",
        "writeOnly",
        "contentMediaType",
        "contentEncoding",
        "additionalItems",
        "then",
        "else"
      ];
      var TYPES = ["number", "integer", "string", "array", "object", "boolean", "null"];
      RULES.all = toHash(ALL);
      RULES.types = toHash(TYPES);
      RULES.forEach(function(group) {
        group.rules = group.rules.map(function(keyword) {
          var implKeywords;
          if (typeof keyword == "object") {
            var key = Object.keys(keyword)[0];
            implKeywords = keyword[key];
            keyword = key;
            implKeywords.forEach(function(k) {
              ALL.push(k);
              RULES.all[k] = true;
            });
          }
          ALL.push(keyword);
          var rule = RULES.all[keyword] = {
            keyword,
            code: ruleModules[keyword],
            implements: implKeywords
          };
          return rule;
        });
        RULES.all.$comment = {
          keyword: "$comment",
          code: ruleModules.$comment
        };
        if (group.type)
          RULES.types[group.type] = group;
      });
      RULES.keywords = toHash(ALL.concat(KEYWORDS));
      RULES.custom = {};
      return RULES;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/data.js
var require_data = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/data.js"(exports2, module2) {
    "use strict";
    var KEYWORDS = [
      "multipleOf",
      "maximum",
      "exclusiveMaximum",
      "minimum",
      "exclusiveMinimum",
      "maxLength",
      "minLength",
      "pattern",
      "additionalItems",
      "maxItems",
      "minItems",
      "uniqueItems",
      "maxProperties",
      "minProperties",
      "required",
      "additionalProperties",
      "enum",
      "format",
      "const"
    ];
    module2.exports = function(metaSchema, keywordsJsonPointers) {
      for (var i = 0; i < keywordsJsonPointers.length; i++) {
        metaSchema = JSON.parse(JSON.stringify(metaSchema));
        var segments = keywordsJsonPointers[i].split("/");
        var keywords = metaSchema;
        var j;
        for (j = 1; j < segments.length; j++)
          keywords = keywords[segments[j]];
        for (j = 0; j < KEYWORDS.length; j++) {
          var key = KEYWORDS[j];
          var schema = keywords[key];
          if (schema) {
            keywords[key] = {
              anyOf: [
                schema,
                { $ref: "https://raw.githubusercontent.com/ajv-validator/ajv/master/lib/refs/data.json#" }
              ]
            };
          }
        }
      }
      return metaSchema;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/async.js
var require_async = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/compile/async.js"(exports2, module2) {
    "use strict";
    var MissingRefError = require_error_classes().MissingRef;
    module2.exports = compileAsync;
    function compileAsync(schema, meta, callback) {
      var self2 = this;
      if (typeof this._opts.loadSchema != "function")
        throw new Error("options.loadSchema should be a function");
      if (typeof meta == "function") {
        callback = meta;
        meta = void 0;
      }
      var p = loadMetaSchemaOf(schema).then(function() {
        var schemaObj = self2._addSchema(schema, void 0, meta);
        return schemaObj.validate || _compileAsync(schemaObj);
      });
      if (callback) {
        p.then(
          function(v) {
            callback(null, v);
          },
          callback
        );
      }
      return p;
      function loadMetaSchemaOf(sch) {
        var $schema = sch.$schema;
        return $schema && !self2.getSchema($schema) ? compileAsync.call(self2, { $ref: $schema }, true) : Promise.resolve();
      }
      function _compileAsync(schemaObj) {
        try {
          return self2._compile(schemaObj);
        } catch (e) {
          if (e instanceof MissingRefError)
            return loadMissingSchema(e);
          throw e;
        }
        function loadMissingSchema(e) {
          var ref = e.missingSchema;
          if (added(ref))
            throw new Error("Schema " + ref + " is loaded but " + e.missingRef + " cannot be resolved");
          var schemaPromise = self2._loadingSchemas[ref];
          if (!schemaPromise) {
            schemaPromise = self2._loadingSchemas[ref] = self2._opts.loadSchema(ref);
            schemaPromise.then(removePromise, removePromise);
          }
          return schemaPromise.then(function(sch) {
            if (!added(ref)) {
              return loadMetaSchemaOf(sch).then(function() {
                if (!added(ref))
                  self2.addSchema(sch, ref, void 0, meta);
              });
            }
          }).then(function() {
            return _compileAsync(schemaObj);
          });
          function removePromise() {
            delete self2._loadingSchemas[ref];
          }
          function added(ref2) {
            return self2._refs[ref2] || self2._schemas[ref2];
          }
        }
      }
    }
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/custom.js
var require_custom = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/dotjs/custom.js"(exports2, module2) {
    "use strict";
    module2.exports = function generate_custom(it, $keyword, $ruleType) {
      var out = " ";
      var $lvl = it.level;
      var $dataLvl = it.dataLevel;
      var $schema = it.schema[$keyword];
      var $schemaPath = it.schemaPath + it.util.getProperty($keyword);
      var $errSchemaPath = it.errSchemaPath + "/" + $keyword;
      var $breakOnError = !it.opts.allErrors;
      var $errorKeyword;
      var $data = "data" + ($dataLvl || "");
      var $valid = "valid" + $lvl;
      var $errs = "errs__" + $lvl;
      var $isData = it.opts.$data && $schema && $schema.$data, $schemaValue;
      if ($isData) {
        out += " var schema" + $lvl + " = " + it.util.getData($schema.$data, $dataLvl, it.dataPathArr) + "; ";
        $schemaValue = "schema" + $lvl;
      } else {
        $schemaValue = $schema;
      }
      var $rule = this, $definition = "definition" + $lvl, $rDef = $rule.definition, $closingBraces = "";
      var $compile, $inline, $macro, $ruleValidate, $validateCode;
      if ($isData && $rDef.$data) {
        $validateCode = "keywordValidate" + $lvl;
        var $validateSchema = $rDef.validateSchema;
        out += " var " + $definition + " = RULES.custom['" + $keyword + "'].definition; var " + $validateCode + " = " + $definition + ".validate;";
      } else {
        $ruleValidate = it.useCustomRule($rule, $schema, it.schema, it);
        if (!$ruleValidate)
          return;
        $schemaValue = "validate.schema" + $schemaPath;
        $validateCode = $ruleValidate.code;
        $compile = $rDef.compile;
        $inline = $rDef.inline;
        $macro = $rDef.macro;
      }
      var $ruleErrs = $validateCode + ".errors", $i = "i" + $lvl, $ruleErr = "ruleErr" + $lvl, $asyncKeyword = $rDef.async;
      if ($asyncKeyword && !it.async)
        throw new Error("async keyword in sync schema");
      if (!($inline || $macro)) {
        out += "" + $ruleErrs + " = null;";
      }
      out += "var " + $errs + " = errors;var " + $valid + ";";
      if ($isData && $rDef.$data) {
        $closingBraces += "}";
        out += " if (" + $schemaValue + " === undefined) { " + $valid + " = true; } else { ";
        if ($validateSchema) {
          $closingBraces += "}";
          out += " " + $valid + " = " + $definition + ".validateSchema(" + $schemaValue + "); if (" + $valid + ") { ";
        }
      }
      if ($inline) {
        if ($rDef.statements) {
          out += " " + $ruleValidate.validate + " ";
        } else {
          out += " " + $valid + " = " + $ruleValidate.validate + "; ";
        }
      } else if ($macro) {
        var $it = it.util.copy(it);
        var $closingBraces = "";
        $it.level++;
        var $nextValid = "valid" + $it.level;
        $it.schema = $ruleValidate.validate;
        $it.schemaPath = "";
        var $wasComposite = it.compositeRule;
        it.compositeRule = $it.compositeRule = true;
        var $code = it.validate($it).replace(/validate\.schema/g, $validateCode);
        it.compositeRule = $it.compositeRule = $wasComposite;
        out += " " + $code;
      } else {
        var $$outStack = $$outStack || [];
        $$outStack.push(out);
        out = "";
        out += "  " + $validateCode + ".call( ";
        if (it.opts.passContext) {
          out += "this";
        } else {
          out += "self";
        }
        if ($compile || $rDef.schema === false) {
          out += " , " + $data + " ";
        } else {
          out += " , " + $schemaValue + " , " + $data + " , validate.schema" + it.schemaPath + " ";
        }
        out += " , (dataPath || '')";
        if (it.errorPath != '""') {
          out += " + " + it.errorPath;
        }
        var $parentData = $dataLvl ? "data" + ($dataLvl - 1 || "") : "parentData", $parentDataProperty = $dataLvl ? it.dataPathArr[$dataLvl] : "parentDataProperty";
        out += " , " + $parentData + " , " + $parentDataProperty + " , rootData )  ";
        var def_callRuleValidate = out;
        out = $$outStack.pop();
        if ($rDef.errors === false) {
          out += " " + $valid + " = ";
          if ($asyncKeyword) {
            out += "await ";
          }
          out += "" + def_callRuleValidate + "; ";
        } else {
          if ($asyncKeyword) {
            $ruleErrs = "customErrors" + $lvl;
            out += " var " + $ruleErrs + " = null; try { " + $valid + " = await " + def_callRuleValidate + "; } catch (e) { " + $valid + " = false; if (e instanceof ValidationError) " + $ruleErrs + " = e.errors; else throw e; } ";
          } else {
            out += " " + $ruleErrs + " = null; " + $valid + " = " + def_callRuleValidate + "; ";
          }
        }
      }
      if ($rDef.modifying) {
        out += " if (" + $parentData + ") " + $data + " = " + $parentData + "[" + $parentDataProperty + "];";
      }
      out += "" + $closingBraces;
      if ($rDef.valid) {
        if ($breakOnError) {
          out += " if (true) { ";
        }
      } else {
        out += " if ( ";
        if ($rDef.valid === void 0) {
          out += " !";
          if ($macro) {
            out += "" + $nextValid;
          } else {
            out += "" + $valid;
          }
        } else {
          out += " " + !$rDef.valid + " ";
        }
        out += ") { ";
        $errorKeyword = $rule.keyword;
        var $$outStack = $$outStack || [];
        $$outStack.push(out);
        out = "";
        var $$outStack = $$outStack || [];
        $$outStack.push(out);
        out = "";
        if (it.createErrors !== false) {
          out += " { keyword: '" + ($errorKeyword || "custom") + "' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { keyword: '" + $rule.keyword + "' } ";
          if (it.opts.messages !== false) {
            out += ` , message: 'should pass "` + $rule.keyword + `" keyword validation' `;
          }
          if (it.opts.verbose) {
            out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
          }
          out += " } ";
        } else {
          out += " {} ";
        }
        var __err = out;
        out = $$outStack.pop();
        if (!it.compositeRule && $breakOnError) {
          if (it.async) {
            out += " throw new ValidationError([" + __err + "]); ";
          } else {
            out += " validate.errors = [" + __err + "]; return false; ";
          }
        } else {
          out += " var err = " + __err + ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
        }
        var def_customError = out;
        out = $$outStack.pop();
        if ($inline) {
          if ($rDef.errors) {
            if ($rDef.errors != "full") {
              out += "  for (var " + $i + "=" + $errs + "; " + $i + "<errors; " + $i + "++) { var " + $ruleErr + " = vErrors[" + $i + "]; if (" + $ruleErr + ".dataPath === undefined) " + $ruleErr + ".dataPath = (dataPath || '') + " + it.errorPath + "; if (" + $ruleErr + ".schemaPath === undefined) { " + $ruleErr + '.schemaPath = "' + $errSchemaPath + '"; } ';
              if (it.opts.verbose) {
                out += " " + $ruleErr + ".schema = " + $schemaValue + "; " + $ruleErr + ".data = " + $data + "; ";
              }
              out += " } ";
            }
          } else {
            if ($rDef.errors === false) {
              out += " " + def_customError + " ";
            } else {
              out += " if (" + $errs + " == errors) { " + def_customError + " } else {  for (var " + $i + "=" + $errs + "; " + $i + "<errors; " + $i + "++) { var " + $ruleErr + " = vErrors[" + $i + "]; if (" + $ruleErr + ".dataPath === undefined) " + $ruleErr + ".dataPath = (dataPath || '') + " + it.errorPath + "; if (" + $ruleErr + ".schemaPath === undefined) { " + $ruleErr + '.schemaPath = "' + $errSchemaPath + '"; } ';
              if (it.opts.verbose) {
                out += " " + $ruleErr + ".schema = " + $schemaValue + "; " + $ruleErr + ".data = " + $data + "; ";
              }
              out += " } } ";
            }
          }
        } else if ($macro) {
          out += "   var err =   ";
          if (it.createErrors !== false) {
            out += " { keyword: '" + ($errorKeyword || "custom") + "' , dataPath: (dataPath || '') + " + it.errorPath + " , schemaPath: " + it.util.toQuotedString($errSchemaPath) + " , params: { keyword: '" + $rule.keyword + "' } ";
            if (it.opts.messages !== false) {
              out += ` , message: 'should pass "` + $rule.keyword + `" keyword validation' `;
            }
            if (it.opts.verbose) {
              out += " , schema: validate.schema" + $schemaPath + " , parentSchema: validate.schema" + it.schemaPath + " , data: " + $data + " ";
            }
            out += " } ";
          } else {
            out += " {} ";
          }
          out += ";  if (vErrors === null) vErrors = [err]; else vErrors.push(err); errors++; ";
          if (!it.compositeRule && $breakOnError) {
            if (it.async) {
              out += " throw new ValidationError(vErrors); ";
            } else {
              out += " validate.errors = vErrors; return false; ";
            }
          }
        } else {
          if ($rDef.errors === false) {
            out += " " + def_customError + " ";
          } else {
            out += " if (Array.isArray(" + $ruleErrs + ")) { if (vErrors === null) vErrors = " + $ruleErrs + "; else vErrors = vErrors.concat(" + $ruleErrs + "); errors = vErrors.length;  for (var " + $i + "=" + $errs + "; " + $i + "<errors; " + $i + "++) { var " + $ruleErr + " = vErrors[" + $i + "]; if (" + $ruleErr + ".dataPath === undefined) " + $ruleErr + ".dataPath = (dataPath || '') + " + it.errorPath + ";  " + $ruleErr + '.schemaPath = "' + $errSchemaPath + '";  ';
            if (it.opts.verbose) {
              out += " " + $ruleErr + ".schema = " + $schemaValue + "; " + $ruleErr + ".data = " + $data + "; ";
            }
            out += " } } else { " + def_customError + " } ";
          }
        }
        out += " } ";
        if ($breakOnError) {
          out += " else { ";
        }
      }
      return out;
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/refs/json-schema-draft-07.json
var require_json_schema_draft_07 = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/refs/json-schema-draft-07.json"(exports2, module2) {
    module2.exports = {
      $schema: "http://json-schema.org/draft-07/schema#",
      $id: "http://json-schema.org/draft-07/schema#",
      title: "Core schema meta-schema",
      definitions: {
        schemaArray: {
          type: "array",
          minItems: 1,
          items: { $ref: "#" }
        },
        nonNegativeInteger: {
          type: "integer",
          minimum: 0
        },
        nonNegativeIntegerDefault0: {
          allOf: [
            { $ref: "#/definitions/nonNegativeInteger" },
            { default: 0 }
          ]
        },
        simpleTypes: {
          enum: [
            "array",
            "boolean",
            "integer",
            "null",
            "number",
            "object",
            "string"
          ]
        },
        stringArray: {
          type: "array",
          items: { type: "string" },
          uniqueItems: true,
          default: []
        }
      },
      type: ["object", "boolean"],
      properties: {
        $id: {
          type: "string",
          format: "uri-reference"
        },
        $schema: {
          type: "string",
          format: "uri"
        },
        $ref: {
          type: "string",
          format: "uri-reference"
        },
        $comment: {
          type: "string"
        },
        title: {
          type: "string"
        },
        description: {
          type: "string"
        },
        default: true,
        readOnly: {
          type: "boolean",
          default: false
        },
        examples: {
          type: "array",
          items: true
        },
        multipleOf: {
          type: "number",
          exclusiveMinimum: 0
        },
        maximum: {
          type: "number"
        },
        exclusiveMaximum: {
          type: "number"
        },
        minimum: {
          type: "number"
        },
        exclusiveMinimum: {
          type: "number"
        },
        maxLength: { $ref: "#/definitions/nonNegativeInteger" },
        minLength: { $ref: "#/definitions/nonNegativeIntegerDefault0" },
        pattern: {
          type: "string",
          format: "regex"
        },
        additionalItems: { $ref: "#" },
        items: {
          anyOf: [
            { $ref: "#" },
            { $ref: "#/definitions/schemaArray" }
          ],
          default: true
        },
        maxItems: { $ref: "#/definitions/nonNegativeInteger" },
        minItems: { $ref: "#/definitions/nonNegativeIntegerDefault0" },
        uniqueItems: {
          type: "boolean",
          default: false
        },
        contains: { $ref: "#" },
        maxProperties: { $ref: "#/definitions/nonNegativeInteger" },
        minProperties: { $ref: "#/definitions/nonNegativeIntegerDefault0" },
        required: { $ref: "#/definitions/stringArray" },
        additionalProperties: { $ref: "#" },
        definitions: {
          type: "object",
          additionalProperties: { $ref: "#" },
          default: {}
        },
        properties: {
          type: "object",
          additionalProperties: { $ref: "#" },
          default: {}
        },
        patternProperties: {
          type: "object",
          additionalProperties: { $ref: "#" },
          propertyNames: { format: "regex" },
          default: {}
        },
        dependencies: {
          type: "object",
          additionalProperties: {
            anyOf: [
              { $ref: "#" },
              { $ref: "#/definitions/stringArray" }
            ]
          }
        },
        propertyNames: { $ref: "#" },
        const: true,
        enum: {
          type: "array",
          items: true,
          minItems: 1,
          uniqueItems: true
        },
        type: {
          anyOf: [
            { $ref: "#/definitions/simpleTypes" },
            {
              type: "array",
              items: { $ref: "#/definitions/simpleTypes" },
              minItems: 1,
              uniqueItems: true
            }
          ]
        },
        format: { type: "string" },
        contentMediaType: { type: "string" },
        contentEncoding: { type: "string" },
        if: { $ref: "#" },
        then: { $ref: "#" },
        else: { $ref: "#" },
        allOf: { $ref: "#/definitions/schemaArray" },
        anyOf: { $ref: "#/definitions/schemaArray" },
        oneOf: { $ref: "#/definitions/schemaArray" },
        not: { $ref: "#" }
      },
      default: true
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/definition_schema.js
var require_definition_schema = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/definition_schema.js"(exports2, module2) {
    "use strict";
    var metaSchema = require_json_schema_draft_07();
    module2.exports = {
      $id: "https://github.com/ajv-validator/ajv/blob/master/lib/definition_schema.js",
      definitions: {
        simpleTypes: metaSchema.definitions.simpleTypes
      },
      type: "object",
      dependencies: {
        schema: ["validate"],
        $data: ["validate"],
        statements: ["inline"],
        valid: { not: { required: ["macro"] } }
      },
      properties: {
        type: metaSchema.properties.type,
        schema: { type: "boolean" },
        statements: { type: "boolean" },
        dependencies: {
          type: "array",
          items: { type: "string" }
        },
        metaSchema: { type: "object" },
        modifying: { type: "boolean" },
        valid: { type: "boolean" },
        $data: { type: "boolean" },
        async: { type: "boolean" },
        errors: {
          anyOf: [
            { type: "boolean" },
            { const: "full" }
          ]
        }
      }
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/keyword.js
var require_keyword = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/keyword.js"(exports2, module2) {
    "use strict";
    var IDENTIFIER = /^[a-z_$][a-z0-9_$-]*$/i;
    var customRuleCode = require_custom();
    var definitionSchema = require_definition_schema();
    module2.exports = {
      add: addKeyword,
      get: getKeyword,
      remove: removeKeyword,
      validate: validateKeyword
    };
    function addKeyword(keyword, definition) {
      var RULES = this.RULES;
      if (RULES.keywords[keyword])
        throw new Error("Keyword " + keyword + " is already defined");
      if (!IDENTIFIER.test(keyword))
        throw new Error("Keyword " + keyword + " is not a valid identifier");
      if (definition) {
        this.validateKeyword(definition, true);
        var dataType = definition.type;
        if (Array.isArray(dataType)) {
          for (var i = 0; i < dataType.length; i++)
            _addRule(keyword, dataType[i], definition);
        } else {
          _addRule(keyword, dataType, definition);
        }
        var metaSchema = definition.metaSchema;
        if (metaSchema) {
          if (definition.$data && this._opts.$data) {
            metaSchema = {
              anyOf: [
                metaSchema,
                { "$ref": "https://raw.githubusercontent.com/ajv-validator/ajv/master/lib/refs/data.json#" }
              ]
            };
          }
          definition.validateSchema = this.compile(metaSchema, true);
        }
      }
      RULES.keywords[keyword] = RULES.all[keyword] = true;
      function _addRule(keyword2, dataType2, definition2) {
        var ruleGroup;
        for (var i2 = 0; i2 < RULES.length; i2++) {
          var rg = RULES[i2];
          if (rg.type == dataType2) {
            ruleGroup = rg;
            break;
          }
        }
        if (!ruleGroup) {
          ruleGroup = { type: dataType2, rules: [] };
          RULES.push(ruleGroup);
        }
        var rule = {
          keyword: keyword2,
          definition: definition2,
          custom: true,
          code: customRuleCode,
          implements: definition2.implements
        };
        ruleGroup.rules.push(rule);
        RULES.custom[keyword2] = rule;
      }
      return this;
    }
    function getKeyword(keyword) {
      var rule = this.RULES.custom[keyword];
      return rule ? rule.definition : this.RULES.keywords[keyword] || false;
    }
    function removeKeyword(keyword) {
      var RULES = this.RULES;
      delete RULES.keywords[keyword];
      delete RULES.all[keyword];
      delete RULES.custom[keyword];
      for (var i = 0; i < RULES.length; i++) {
        var rules = RULES[i].rules;
        for (var j = 0; j < rules.length; j++) {
          if (rules[j].keyword == keyword) {
            rules.splice(j, 1);
            break;
          }
        }
      }
      return this;
    }
    function validateKeyword(definition, throwError) {
      validateKeyword.errors = null;
      var v = this._validateKeyword = this._validateKeyword || this.compile(definitionSchema, true);
      if (v(definition))
        return true;
      validateKeyword.errors = v.errors;
      if (throwError)
        throw new Error("custom keyword definition is invalid: " + this.errorsText(v.errors));
      else
        return false;
    }
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/refs/data.json
var require_data2 = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/refs/data.json"(exports2, module2) {
    module2.exports = {
      $schema: "http://json-schema.org/draft-07/schema#",
      $id: "https://raw.githubusercontent.com/ajv-validator/ajv/master/lib/refs/data.json#",
      description: "Meta-schema for $data reference (JSON Schema extension proposal)",
      type: "object",
      required: ["$data"],
      properties: {
        $data: {
          type: "string",
          anyOf: [
            { format: "relative-json-pointer" },
            { format: "json-pointer" }
          ]
        }
      },
      additionalProperties: false
    };
  }
});

// ../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/ajv.js
var require_ajv = __commonJS({
  "../node_modules/.pnpm/ajv@6.12.6/node_modules/ajv/lib/ajv.js"(exports2, module2) {
    "use strict";
    var compileSchema = require_compile();
    var resolve = require_resolve();
    var Cache = require_cache();
    var SchemaObject = require_schema_obj();
    var stableStringify = require_fast_json_stable_stringify();
    var formats = require_formats();
    var rules = require_rules();
    var $dataMetaSchema = require_data();
    var util = require_util();
    module2.exports = Ajv;
    Ajv.prototype.validate = validate;
    Ajv.prototype.compile = compile;
    Ajv.prototype.addSchema = addSchema;
    Ajv.prototype.addMetaSchema = addMetaSchema;
    Ajv.prototype.validateSchema = validateSchema;
    Ajv.prototype.getSchema = getSchema;
    Ajv.prototype.removeSchema = removeSchema;
    Ajv.prototype.addFormat = addFormat;
    Ajv.prototype.errorsText = errorsText;
    Ajv.prototype._addSchema = _addSchema;
    Ajv.prototype._compile = _compile;
    Ajv.prototype.compileAsync = require_async();
    var customKeyword = require_keyword();
    Ajv.prototype.addKeyword = customKeyword.add;
    Ajv.prototype.getKeyword = customKeyword.get;
    Ajv.prototype.removeKeyword = customKeyword.remove;
    Ajv.prototype.validateKeyword = customKeyword.validate;
    var errorClasses = require_error_classes();
    Ajv.ValidationError = errorClasses.Validation;
    Ajv.MissingRefError = errorClasses.MissingRef;
    Ajv.$dataMetaSchema = $dataMetaSchema;
    var META_SCHEMA_ID = "http://json-schema.org/draft-07/schema";
    var META_IGNORE_OPTIONS = ["removeAdditional", "useDefaults", "coerceTypes", "strictDefaults"];
    var META_SUPPORT_DATA = ["/properties"];
    function Ajv(opts) {
      if (!(this instanceof Ajv))
        return new Ajv(opts);
      opts = this._opts = util.copy(opts) || {};
      setLogger(this);
      this._schemas = {};
      this._refs = {};
      this._fragments = {};
      this._formats = formats(opts.format);
      this._cache = opts.cache || new Cache();
      this._loadingSchemas = {};
      this._compilations = [];
      this.RULES = rules();
      this._getId = chooseGetId(opts);
      opts.loopRequired = opts.loopRequired || Infinity;
      if (opts.errorDataPath == "property")
        opts._errorDataPathProperty = true;
      if (opts.serialize === void 0)
        opts.serialize = stableStringify;
      this._metaOpts = getMetaSchemaOptions(this);
      if (opts.formats)
        addInitialFormats(this);
      if (opts.keywords)
        addInitialKeywords(this);
      addDefaultMetaSchema(this);
      if (typeof opts.meta == "object")
        this.addMetaSchema(opts.meta);
      if (opts.nullable)
        this.addKeyword("nullable", { metaSchema: { type: "boolean" } });
      addInitialSchemas(this);
    }
    function validate(schemaKeyRef, data) {
      var v;
      if (typeof schemaKeyRef == "string") {
        v = this.getSchema(schemaKeyRef);
        if (!v)
          throw new Error('no schema with key or ref "' + schemaKeyRef + '"');
      } else {
        var schemaObj = this._addSchema(schemaKeyRef);
        v = schemaObj.validate || this._compile(schemaObj);
      }
      var valid = v(data);
      if (v.$async !== true)
        this.errors = v.errors;
      return valid;
    }
    function compile(schema, _meta) {
      var schemaObj = this._addSchema(schema, void 0, _meta);
      return schemaObj.validate || this._compile(schemaObj);
    }
    function addSchema(schema, key, _skipValidation, _meta) {
      if (Array.isArray(schema)) {
        for (var i = 0; i < schema.length; i++)
          this.addSchema(schema[i], void 0, _skipValidation, _meta);
        return this;
      }
      var id = this._getId(schema);
      if (id !== void 0 && typeof id != "string")
        throw new Error("schema id must be string");
      key = resolve.normalizeId(key || id);
      checkUnique(this, key);
      this._schemas[key] = this._addSchema(schema, _skipValidation, _meta, true);
      return this;
    }
    function addMetaSchema(schema, key, skipValidation) {
      this.addSchema(schema, key, skipValidation, true);
      return this;
    }
    function validateSchema(schema, throwOrLogError) {
      var $schema = schema.$schema;
      if ($schema !== void 0 && typeof $schema != "string")
        throw new Error("$schema must be a string");
      $schema = $schema || this._opts.defaultMeta || defaultMeta(this);
      if (!$schema) {
        this.logger.warn("meta-schema not available");
        this.errors = null;
        return true;
      }
      var valid = this.validate($schema, schema);
      if (!valid && throwOrLogError) {
        var message = "schema is invalid: " + this.errorsText();
        if (this._opts.validateSchema == "log")
          this.logger.error(message);
        else
          throw new Error(message);
      }
      return valid;
    }
    function defaultMeta(self2) {
      var meta = self2._opts.meta;
      self2._opts.defaultMeta = typeof meta == "object" ? self2._getId(meta) || meta : self2.getSchema(META_SCHEMA_ID) ? META_SCHEMA_ID : void 0;
      return self2._opts.defaultMeta;
    }
    function getSchema(keyRef) {
      var schemaObj = _getSchemaObj(this, keyRef);
      switch (typeof schemaObj) {
        case "object":
          return schemaObj.validate || this._compile(schemaObj);
        case "string":
          return this.getSchema(schemaObj);
        case "undefined":
          return _getSchemaFragment(this, keyRef);
      }
    }
    function _getSchemaFragment(self2, ref) {
      var res = resolve.schema.call(self2, { schema: {} }, ref);
      if (res) {
        var schema = res.schema, root = res.root, baseId = res.baseId;
        var v = compileSchema.call(self2, schema, root, void 0, baseId);
        self2._fragments[ref] = new SchemaObject({
          ref,
          fragment: true,
          schema,
          root,
          baseId,
          validate: v
        });
        return v;
      }
    }
    function _getSchemaObj(self2, keyRef) {
      keyRef = resolve.normalizeId(keyRef);
      return self2._schemas[keyRef] || self2._refs[keyRef] || self2._fragments[keyRef];
    }
    function removeSchema(schemaKeyRef) {
      if (schemaKeyRef instanceof RegExp) {
        _removeAllSchemas(this, this._schemas, schemaKeyRef);
        _removeAllSchemas(this, this._refs, schemaKeyRef);
        return this;
      }
      switch (typeof schemaKeyRef) {
        case "undefined":
          _removeAllSchemas(this, this._schemas);
          _removeAllSchemas(this, this._refs);
          this._cache.clear();
          return this;
        case "string":
          var schemaObj = _getSchemaObj(this, schemaKeyRef);
          if (schemaObj)
            this._cache.del(schemaObj.cacheKey);
          delete this._schemas[schemaKeyRef];
          delete this._refs[schemaKeyRef];
          return this;
        case "object":
          var serialize = this._opts.serialize;
          var cacheKey = serialize ? serialize(schemaKeyRef) : schemaKeyRef;
          this._cache.del(cacheKey);
          var id = this._getId(schemaKeyRef);
          if (id) {
            id = resolve.normalizeId(id);
            delete this._schemas[id];
            delete this._refs[id];
          }
      }
      return this;
    }
    function _removeAllSchemas(self2, schemas, regex) {
      for (var keyRef in schemas) {
        var schemaObj = schemas[keyRef];
        if (!schemaObj.meta && (!regex || regex.test(keyRef))) {
          self2._cache.del(schemaObj.cacheKey);
          delete schemas[keyRef];
        }
      }
    }
    function _addSchema(schema, skipValidation, meta, shouldAddSchema) {
      if (typeof schema != "object" && typeof schema != "boolean")
        throw new Error("schema should be object or boolean");
      var serialize = this._opts.serialize;
      var cacheKey = serialize ? serialize(schema) : schema;
      var cached = this._cache.get(cacheKey);
      if (cached)
        return cached;
      shouldAddSchema = shouldAddSchema || this._opts.addUsedSchema !== false;
      var id = resolve.normalizeId(this._getId(schema));
      if (id && shouldAddSchema)
        checkUnique(this, id);
      var willValidate = this._opts.validateSchema !== false && !skipValidation;
      var recursiveMeta;
      if (willValidate && !(recursiveMeta = id && id == resolve.normalizeId(schema.$schema)))
        this.validateSchema(schema, true);
      var localRefs = resolve.ids.call(this, schema);
      var schemaObj = new SchemaObject({
        id,
        schema,
        localRefs,
        cacheKey,
        meta
      });
      if (id[0] != "#" && shouldAddSchema)
        this._refs[id] = schemaObj;
      this._cache.put(cacheKey, schemaObj);
      if (willValidate && recursiveMeta)
        this.validateSchema(schema, true);
      return schemaObj;
    }
    function _compile(schemaObj, root) {
      if (schemaObj.compiling) {
        schemaObj.validate = callValidate;
        callValidate.schema = schemaObj.schema;
        callValidate.errors = null;
        callValidate.root = root ? root : callValidate;
        if (schemaObj.schema.$async === true)
          callValidate.$async = true;
        return callValidate;
      }
      schemaObj.compiling = true;
      var currentOpts;
      if (schemaObj.meta) {
        currentOpts = this._opts;
        this._opts = this._metaOpts;
      }
      var v;
      try {
        v = compileSchema.call(this, schemaObj.schema, root, schemaObj.localRefs);
      } catch (e) {
        delete schemaObj.validate;
        throw e;
      } finally {
        schemaObj.compiling = false;
        if (schemaObj.meta)
          this._opts = currentOpts;
      }
      schemaObj.validate = v;
      schemaObj.refs = v.refs;
      schemaObj.refVal = v.refVal;
      schemaObj.root = v.root;
      return v;
      function callValidate() {
        var _validate = schemaObj.validate;
        var result = _validate.apply(this, arguments);
        callValidate.errors = _validate.errors;
        return result;
      }
    }
    function chooseGetId(opts) {
      switch (opts.schemaId) {
        case "auto":
          return _get$IdOrId;
        case "id":
          return _getId;
        default:
          return _get$Id;
      }
    }
    function _getId(schema) {
      if (schema.$id)
        this.logger.warn("schema $id ignored", schema.$id);
      return schema.id;
    }
    function _get$Id(schema) {
      if (schema.id)
        this.logger.warn("schema id ignored", schema.id);
      return schema.$id;
    }
    function _get$IdOrId(schema) {
      if (schema.$id && schema.id && schema.$id != schema.id)
        throw new Error("schema $id is different from id");
      return schema.$id || schema.id;
    }
    function errorsText(errors, options) {
      errors = errors || this.errors;
      if (!errors)
        return "No errors";
      options = options || {};
      var separator = options.separator === void 0 ? ", " : options.separator;
      var dataVar = options.dataVar === void 0 ? "data" : options.dataVar;
      var text = "";
      for (var i = 0; i < errors.length; i++) {
        var e = errors[i];
        if (e)
          text += dataVar + e.dataPath + " " + e.message + separator;
      }
      return text.slice(0, -separator.length);
    }
    function addFormat(name, format) {
      if (typeof format == "string")
        format = new RegExp(format);
      this._formats[name] = format;
      return this;
    }
    function addDefaultMetaSchema(self2) {
      var $dataSchema;
      if (self2._opts.$data) {
        $dataSchema = require_data2();
        self2.addMetaSchema($dataSchema, $dataSchema.$id, true);
      }
      if (self2._opts.meta === false)
        return;
      var metaSchema = require_json_schema_draft_07();
      if (self2._opts.$data)
        metaSchema = $dataMetaSchema(metaSchema, META_SUPPORT_DATA);
      self2.addMetaSchema(metaSchema, META_SCHEMA_ID, true);
      self2._refs["http://json-schema.org/schema"] = META_SCHEMA_ID;
    }
    function addInitialSchemas(self2) {
      var optsSchemas = self2._opts.schemas;
      if (!optsSchemas)
        return;
      if (Array.isArray(optsSchemas))
        self2.addSchema(optsSchemas);
      else
        for (var key in optsSchemas)
          self2.addSchema(optsSchemas[key], key);
    }
    function addInitialFormats(self2) {
      for (var name in self2._opts.formats) {
        var format = self2._opts.formats[name];
        self2.addFormat(name, format);
      }
    }
    function addInitialKeywords(self2) {
      for (var name in self2._opts.keywords) {
        var keyword = self2._opts.keywords[name];
        self2.addKeyword(name, keyword);
      }
    }
    function checkUnique(self2, id) {
      if (self2._schemas[id] || self2._refs[id])
        throw new Error('schema with key or id "' + id + '" already exists');
    }
    function getMetaSchemaOptions(self2) {
      var metaOpts = util.copy(self2._opts);
      for (var i = 0; i < META_IGNORE_OPTIONS.length; i++)
        delete metaOpts[META_IGNORE_OPTIONS[i]];
      return metaOpts;
    }
    function setLogger(self2) {
      var logger = self2._opts.logger;
      if (logger === false) {
        self2.logger = { log: noop, warn: noop, error: noop };
      } else {
        if (logger === void 0)
          logger = console;
        if (!(typeof logger == "object" && logger.log && logger.warn && logger.error))
          throw new Error("logger must implement log, warn and error methods");
        self2.logger = logger;
      }
    }
    function noop() {
    }
  }
});

// ../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/definitions.json
var require_definitions = __commonJS({
  "../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/definitions.json"(exports2, module2) {
    module2.exports = {
      title: "definitions",
      definitions: {
        contextualizedFieldName: {
          type: "string",
          pattern: "^(this\\.)?.+$"
        },
        dataTypeArgsCount: {
          oneOf: [
            {
              $ref: "#/definitions/contextualizedFieldName"
            },
            {
              type: "number"
            }
          ]
        },
        fieldName: {
          type: "string",
          pattern: "^[a-zA-Z0-9_]+$"
        }
      },
      type: "object"
    };
  }
});

// ../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/protocol_schema.json
var require_protocol_schema = __commonJS({
  "../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/protocol_schema.json"(exports2, module2) {
    module2.exports = {
      title: "protocol",
      type: "object",
      properties: {
        types: {
          type: "object",
          patternProperties: {
            "^[0-9a-zA-Z_]+$": {
              oneOf: [
                {
                  type: "string"
                },
                {
                  type: "array",
                  items: [
                    {
                      type: "string"
                    },
                    {
                      oneOf: [
                        {
                          type: "object"
                        },
                        {
                          type: "array"
                        }
                      ]
                    }
                  ]
                }
              ]
            }
          },
          additionalProperties: false
        }
      },
      patternProperties: {
        "^(?!types)[a-zA-Z_]+$": {
          $ref: "#"
        }
      },
      additionalProperties: false
    };
  }
});

// ../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/numeric.json
var require_numeric = __commonJS({
  "../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/numeric.json"(exports2, module2) {
    module2.exports = {
      i8: {
        enum: ["i8"]
      },
      u8: {
        enum: ["u8"]
      },
      i16: {
        enum: ["i16"]
      },
      u16: {
        enum: ["u16"]
      },
      i32: {
        enum: ["i32"]
      },
      u32: {
        enum: ["u32"]
      },
      f32: {
        enum: ["f32"]
      },
      f64: {
        enum: ["f64"]
      },
      li8: {
        enum: ["li8"]
      },
      lu8: {
        enum: ["lu8"]
      },
      li16: {
        enum: ["li16"]
      },
      lu16: {
        enum: ["lu16"]
      },
      li32: {
        enum: ["li32"]
      },
      lu32: {
        enum: ["lu32"]
      },
      lf32: {
        enum: ["lf32"]
      },
      lf64: {
        enum: ["lf64"]
      },
      i64: {
        enum: ["i64"]
      },
      li64: {
        enum: ["li64"]
      },
      u64: {
        enum: ["u64"]
      },
      lu64: {
        enum: ["lu64"]
      },
      varint: {
        enum: ["varint"]
      },
      varint64: {
        enum: ["varint64"]
      },
      varint128: {
        enum: ["varint128"]
      },
      zigzag32: {
        enum: ["zigzag32"]
      },
      zigzag64: {
        enum: ["zigzag64"]
      },
      int: {
        title: "int",
        type: "array",
        items: [
          {
            enum: ["int"]
          },
          {
            type: "array",
            items: {
              type: "object",
              properties: {
                size: {
                  type: "number"
                }
              },
              required: ["size"],
              additionalProperties: false
            },
            additionalItems: false
          }
        ],
        additionalItems: false
      },
      lint: {
        title: "lint",
        type: "array",
        items: [
          {
            enum: ["lint"]
          },
          {
            type: "array",
            items: {
              type: "object",
              properties: {
                size: {
                  type: "number"
                }
              },
              required: ["size"],
              additionalProperties: false
            },
            additionalItems: false
          }
        ],
        additionalItems: false
      }
    };
  }
});

// ../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/utils.json
var require_utils2 = __commonJS({
  "../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/utils.json"(exports2, module2) {
    module2.exports = {
      pstring: {
        title: "pstring",
        type: "array",
        items: [
          {
            enum: ["pstring"]
          },
          {
            oneOf: [
              {
                type: "object",
                properties: {
                  countType: {
                    $ref: "dataType"
                  },
                  encoding: {
                    type: "string"
                  }
                },
                additionalProperties: false,
                required: ["countType"]
              },
              {
                type: "object",
                properties: {
                  count: {
                    $ref: "definitions#/definitions/dataTypeArgsCount"
                  },
                  encoding: {
                    type: "string"
                  }
                },
                additionalProperties: false,
                required: ["count"]
              }
            ]
          }
        ],
        additionalItems: false
      },
      buffer: {
        title: "buffer",
        type: "array",
        items: [
          {
            enum: ["buffer"]
          },
          {
            oneOf: [
              {
                type: "object",
                properties: {
                  countType: {
                    $ref: "dataType"
                  }
                },
                additionalProperties: false,
                required: ["countType"]
              },
              {
                type: "object",
                properties: {
                  count: {
                    $ref: "definitions#/definitions/dataTypeArgsCount"
                  }
                },
                additionalProperties: false,
                required: ["count"]
              }
            ]
          }
        ]
      },
      bitfield: {
        title: "bitfield",
        type: "array",
        items: [
          {
            enum: ["bitfield"]
          },
          {
            type: "array",
            items: {
              type: "object",
              properties: {
                name: {
                  $ref: "definitions#/definitions/fieldName"
                },
                size: {
                  type: "number"
                },
                signed: {
                  type: "boolean"
                }
              },
              required: ["name", "size", "signed"],
              additionalProperties: false
            },
            additionalItems: false
          }
        ],
        additionalItems: false
      },
      bitflags: {
        title: "bitflags",
        type: "array",
        items: [
          {
            enum: ["bitflags"]
          },
          {
            type: "object",
            additionalItems: false
          }
        ],
        additionalItems: false
      },
      mapper: {
        title: "mapper",
        type: "array",
        items: [
          {
            enum: ["mapper"]
          },
          {
            type: "object",
            properties: {
              type: {
                $ref: "dataType"
              },
              mappings: {
                type: "object",
                patternProperties: {
                  "^[-a-zA-Z0-9 _]+$": {
                    type: "string"
                  }
                },
                additionalProperties: false
              }
            },
            required: ["type", "mappings"],
            additionalProperties: false
          }
        ],
        additionalItems: false
      }
    };
  }
});

// ../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/structures.json
var require_structures = __commonJS({
  "../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/structures.json"(exports2, module2) {
    module2.exports = {
      array: {
        title: "array",
        type: "array",
        items: [
          {
            enum: ["array"]
          },
          {
            oneOf: [
              {
                type: "object",
                properties: {
                  type: {
                    $ref: "dataType"
                  },
                  countType: {
                    $ref: "dataType"
                  }
                },
                additionalProperties: false,
                required: ["type", "countType"]
              },
              {
                type: "object",
                properties: {
                  type: {
                    $ref: "dataType"
                  },
                  count: {
                    $ref: "definitions#/definitions/dataTypeArgsCount"
                  }
                },
                additionalProperties: false,
                required: ["type", "count"]
              }
            ]
          }
        ],
        additionalItems: false
      },
      count: {
        title: "count",
        type: "array",
        items: [
          {
            enum: ["count"]
          },
          {
            type: "object",
            properties: {
              countFor: {
                $ref: "definitions#/definitions/contextualizedFieldName"
              },
              type: {
                $ref: "dataType"
              }
            },
            required: ["countFor", "type"],
            additionalProperties: false
          }
        ],
        additionalItems: false
      },
      container: {
        title: "container",
        type: "array",
        items: [
          {
            enum: ["container"]
          },
          {
            type: "array",
            items: {
              type: "object",
              properties: {
                anon: {
                  type: "boolean"
                },
                name: {
                  $ref: "definitions#/definitions/fieldName"
                },
                type: {
                  $ref: "dataType"
                }
              },
              oneOf: [
                {
                  required: ["anon"]
                },
                {
                  required: ["name"]
                }
              ],
              required: ["type"],
              additionalProperties: false
            },
            additionalItems: false
          }
        ],
        additionalItems: false
      }
    };
  }
});

// ../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/conditional.json
var require_conditional = __commonJS({
  "../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/conditional.json"(exports2, module2) {
    module2.exports = {
      switch: {
        title: "switch",
        type: "array",
        items: [
          {
            enum: ["switch"]
          },
          {
            type: "object",
            properties: {
              compareTo: {
                $ref: "definitions#/definitions/contextualizedFieldName"
              },
              compareToValue: {
                type: "string"
              },
              fields: {
                type: "object",
                patternProperties: {
                  "^[-a-zA-Z0-9 _:/]+$": {
                    $ref: "dataType"
                  }
                },
                additionalProperties: false
              },
              default: {
                $ref: "dataType"
              }
            },
            oneOf: [
              {
                required: ["compareTo", "fields"]
              },
              {
                required: ["compareToValue", "fields"]
              }
            ],
            additionalProperties: false
          }
        ],
        additionalItems: false
      },
      option: {
        title: "option",
        type: "array",
        items: [
          {
            enum: ["option"]
          },
          {
            $ref: "dataType"
          }
        ],
        additionalItems: false
      }
    };
  }
});

// ../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/primitives.json
var require_primitives = __commonJS({
  "../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/ProtoDef/schemas/primitives.json"(exports2, module2) {
    module2.exports = {
      bool: {
        enum: ["bool"]
      },
      cstring: {
        title: "cstring",
        type: "array",
        items: [
          {
            enum: ["cstring"]
          },
          {
            type: "object",
            properties: {
              encoding: {
                type: "string"
              }
            },
            additionalProperties: false,
            required: []
          }
        ],
        additionalItems: false
      },
      void: {
        enum: ["void"]
      }
    };
  }
});

// ../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/index.js
var require_protodef_validator = __commonJS({
  "../node_modules/.pnpm/protodef-validator@1.4.0/node_modules/protodef-validator/index.js"(exports2, module2) {
    var Ajv = require_ajv();
    var assert = require("assert");
    var Validator = class _Validator {
      constructor(typesSchemas) {
        this.createAjvInstance(typesSchemas);
        this.addDefaultTypes();
      }
      createAjvInstance(typesSchemas) {
        this.typesSchemas = {};
        this.compiled = false;
        this.ajv = new Ajv({ verbose: true });
        this.ajv.addSchema(require_definitions(), "definitions");
        this.ajv.addSchema(require_protocol_schema(), "protocol");
        if (typesSchemas) {
          Object.keys(typesSchemas).forEach((s) => this.addType(s, typesSchemas[s]));
        }
      }
      addDefaultTypes() {
        this.addTypes(require_numeric());
        this.addTypes(require_utils2());
        this.addTypes(require_structures());
        this.addTypes(require_conditional());
        this.addTypes(require_primitives());
      }
      addTypes(schemas) {
        Object.keys(schemas).forEach((name) => this.addType(name, schemas[name]));
      }
      typeToSchemaName(name) {
        return name.replace("|", "_");
      }
      addType(name, schema) {
        const schemaName = this.typeToSchemaName(name);
        if (this.typesSchemas[schemaName] != void 0)
          return;
        if (!schema) {
          schema = {
            "oneOf": [
              { "enum": [name] },
              {
                "type": "array",
                "items": [
                  { "enum": [name] },
                  { "oneOf": [{ "type": "object" }, { "type": "array" }] }
                ]
              }
            ]
          };
        }
        this.typesSchemas[schemaName] = schema;
        if (this.compiled)
          this.createAjvInstance(this.typesSchemas);
        else {
          this.ajv.addSchema(schema, schemaName);
        }
        this.ajv.removeSchema("dataType");
        this.ajv.addSchema({
          "title": "dataType",
          "oneOf": [{ "enum": ["native"] }].concat(Object.keys(this.typesSchemas).map((name2) => ({ "$ref": this.typeToSchemaName(name2) })))
        }, "dataType");
      }
      validateType(type) {
        let valid = this.ajv.validate("dataType", type);
        this.compiled = true;
        if (!valid) {
          console.log(JSON.stringify(this.ajv.errors[0], null, 2));
          if (this.ajv.errors[0]["parentSchema"]["title"] == "dataType") {
            this.validateTypeGoingInside(this.ajv.errors[0]["data"]);
          }
          throw new Error("validation error");
        }
      }
      validateTypeGoingInside(type) {
        if (Array.isArray(type)) {
          assert.ok(this.typesSchemas[this.typeToSchemaName(type[0])] != void 0, type + " is an undefined type");
          let valid = this.ajv.validate(type[0], type);
          this.compiled = true;
          if (!valid) {
            console.log(JSON.stringify(this.ajv.errors[0], null, 2));
            if (this.ajv.errors[0]["parentSchema"]["title"] == "dataType") {
              this.validateTypeGoingInside(this.ajv.errors[0]["data"]);
            }
            throw new Error("validation error");
          }
        } else {
          if (type == "native")
            return;
          assert.ok(this.typesSchemas[this.typeToSchemaName(type)] != void 0, type + " is an undefined type");
        }
      }
      validateProtocol(protocol) {
        let valid = this.ajv.validate("protocol", protocol);
        assert.ok(valid, JSON.stringify(this.ajv.errors, null, 2));
        function validateTypes(p, originalValidator, path) {
          const v = new _Validator(originalValidator.typesSchemas);
          Object.keys(p).forEach((k) => {
            if (k == "types") {
              Object.keys(p[k]).forEach((typeName) => v.addType(typeName));
              Object.keys(p[k]).forEach((typeName) => {
                try {
                  v.validateType(p[k][typeName], path + "." + k + "." + typeName);
                } catch (e) {
                  throw new Error("Error at " + path + "." + k + "." + typeName);
                }
              });
            } else {
              validateTypes(p[k], v, path + "." + k);
            }
          });
        }
        validateTypes(protocol, this, "root");
      }
    };
    module2.exports = Validator;
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/ProtoDef/schemas/numeric.json
var require_numeric2 = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/ProtoDef/schemas/numeric.json"(exports2, module2) {
    module2.exports = {
      i8: {
        enum: ["i8"]
      },
      u8: {
        enum: ["u8"]
      },
      i16: {
        enum: ["i16"]
      },
      u16: {
        enum: ["u16"]
      },
      i32: {
        enum: ["i32"]
      },
      u32: {
        enum: ["u32"]
      },
      f32: {
        enum: ["f32"]
      },
      f64: {
        enum: ["f64"]
      },
      li8: {
        enum: ["li8"]
      },
      lu8: {
        enum: ["lu8"]
      },
      li16: {
        enum: ["li16"]
      },
      lu16: {
        enum: ["lu16"]
      },
      li32: {
        enum: ["li32"]
      },
      lu32: {
        enum: ["lu32"]
      },
      lf32: {
        enum: ["lf32"]
      },
      lf64: {
        enum: ["lf64"]
      },
      i64: {
        enum: ["i64"]
      },
      li64: {
        enum: ["li64"]
      },
      u64: {
        enum: ["u64"]
      },
      lu64: {
        enum: ["lu64"]
      },
      varint: {
        enum: ["varint"]
      },
      varint64: {
        enum: ["varint64"]
      },
      varint128: {
        enum: ["varint128"]
      },
      zigzag32: {
        enum: ["zigzag32"]
      },
      zigzag64: {
        enum: ["zigzag64"]
      },
      int: {
        title: "int",
        type: "array",
        items: [
          {
            enum: ["int"]
          },
          {
            type: "array",
            items: {
              type: "object",
              properties: {
                size: {
                  type: "number"
                }
              },
              required: ["size"],
              additionalProperties: false
            },
            additionalItems: false
          }
        ],
        additionalItems: false
      },
      lint: {
        title: "lint",
        type: "array",
        items: [
          {
            enum: ["lint"]
          },
          {
            type: "array",
            items: {
              type: "object",
              properties: {
                size: {
                  type: "number"
                }
              },
              required: ["size"],
              additionalProperties: false
            },
            additionalItems: false
          }
        ],
        additionalItems: false
      }
    };
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/numeric.js
var require_numeric3 = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/numeric.js"(exports2, module2) {
    var { PartialReadError: PartialReadError2 } = require_utils();
    var SignedBigInt = class extends Array {
      valueOf() {
        return BigInt.asIntN(64, BigInt(this[0]) << 32n) | BigInt.asUintN(32, BigInt(this[1]));
      }
      toString() {
        return this.valueOf().toString();
      }
      [Symbol.for("nodejs.util.inspect.custom")]() {
        return this.valueOf();
      }
    };
    var UnsignedBigInt = class extends Array {
      valueOf() {
        return BigInt.asUintN(64, BigInt(this[0]) << 32n) | BigInt.asUintN(32, BigInt(this[1]));
      }
      toString() {
        return this.valueOf().toString();
      }
      [Symbol.for("nodejs.util.inspect.custom")]() {
        return this.valueOf();
      }
    };
    function readI64(buffer, offset) {
      if (offset + 8 > buffer.length) {
        throw new PartialReadError2();
      }
      return {
        value: new SignedBigInt(buffer.readInt32BE(offset), buffer.readInt32BE(offset + 4)),
        size: 8
      };
    }
    function writeI64(value, buffer, offset) {
      if (typeof value === "bigint") {
        buffer.writeBigInt64BE(value, offset);
      } else {
        buffer.writeInt32BE(value[0], offset);
        buffer.writeInt32BE(value[1], offset + 4);
      }
      return offset + 8;
    }
    function readLI64(buffer, offset) {
      if (offset + 8 > buffer.length) {
        throw new PartialReadError2();
      }
      return {
        value: new SignedBigInt(buffer.readInt32LE(offset + 4), buffer.readInt32LE(offset)),
        size: 8
      };
    }
    function writeLI64(value, buffer, offset) {
      if (typeof value === "bigint") {
        buffer.writeBigInt64LE(value, offset);
      } else {
        buffer.writeInt32LE(value[0], offset + 4);
        buffer.writeInt32LE(value[1], offset);
      }
      return offset + 8;
    }
    function readU64(buffer, offset) {
      if (offset + 8 > buffer.length) {
        throw new PartialReadError2();
      }
      return {
        value: new UnsignedBigInt(buffer.readUInt32BE(offset), buffer.readUInt32BE(offset + 4)),
        size: 8
      };
    }
    function writeU64(value, buffer, offset) {
      if (typeof value === "bigint") {
        buffer.writeBigUInt64BE(value, offset);
      } else {
        buffer.writeUInt32BE(value[0], offset);
        buffer.writeUInt32BE(value[1], offset + 4);
      }
      return offset + 8;
    }
    function readLU64(buffer, offset) {
      if (offset + 8 > buffer.length) {
        throw new PartialReadError2();
      }
      return {
        value: new UnsignedBigInt(buffer.readUInt32LE(offset + 4), buffer.readUInt32LE(offset)),
        size: 8
      };
    }
    function writeLU64(value, buffer, offset) {
      if (typeof value === "bigint") {
        buffer.writeBigUInt64LE(value, offset);
      } else {
        buffer.writeUInt32LE(value[0], offset + 4);
        buffer.writeUInt32LE(value[1], offset);
      }
      return offset + 8;
    }
    function generateFunctions(bufferReader, bufferWriter, size, schema) {
      const reader = (buffer, offset) => {
        if (offset + size > buffer.length) {
          throw new PartialReadError2();
        }
        const value = buffer[bufferReader](offset);
        return {
          value,
          size
        };
      };
      const writer = (value, buffer, offset) => {
        buffer[bufferWriter](value, offset);
        return offset + size;
      };
      return [reader, writer, size, schema];
    }
    var nums = {
      i8: ["readInt8", "writeInt8", 1],
      u8: ["readUInt8", "writeUInt8", 1],
      i16: ["readInt16BE", "writeInt16BE", 2],
      u16: ["readUInt16BE", "writeUInt16BE", 2],
      i32: ["readInt32BE", "writeInt32BE", 4],
      u32: ["readUInt32BE", "writeUInt32BE", 4],
      f32: ["readFloatBE", "writeFloatBE", 4],
      f64: ["readDoubleBE", "writeDoubleBE", 8],
      li8: ["readInt8", "writeInt8", 1],
      lu8: ["readUInt8", "writeUInt8", 1],
      li16: ["readInt16LE", "writeInt16LE", 2],
      lu16: ["readUInt16LE", "writeUInt16LE", 2],
      li32: ["readInt32LE", "writeInt32LE", 4],
      lu32: ["readUInt32LE", "writeUInt32LE", 4],
      lf32: ["readFloatLE", "writeFloatLE", 4],
      lf64: ["readDoubleLE", "writeDoubleLE", 8]
    };
    var types = Object.keys(nums).reduce((types2, num) => {
      types2[num] = generateFunctions(nums[num][0], nums[num][1], nums[num][2], require_numeric2()[num]);
      return types2;
    }, {});
    types.i64 = [readI64, writeI64, 8, require_numeric2().i64];
    types.li64 = [readLI64, writeLI64, 8, require_numeric2().li64];
    types.u64 = [readU64, writeU64, 8, require_numeric2().u64];
    types.lu64 = [readLU64, writeLU64, 8, require_numeric2().lu64];
    module2.exports = types;
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/ProtoDef/schemas/utils.json
var require_utils3 = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/ProtoDef/schemas/utils.json"(exports2, module2) {
    module2.exports = {
      pstring: {
        title: "pstring",
        type: "array",
        items: [
          {
            enum: ["pstring"]
          },
          {
            oneOf: [
              {
                type: "object",
                properties: {
                  countType: {
                    $ref: "dataType"
                  },
                  encoding: {
                    type: "string"
                  }
                },
                additionalProperties: false,
                required: ["countType"]
              },
              {
                type: "object",
                properties: {
                  count: {
                    $ref: "definitions#/definitions/dataTypeArgsCount"
                  },
                  encoding: {
                    type: "string"
                  }
                },
                additionalProperties: false,
                required: ["count"]
              }
            ]
          }
        ],
        additionalItems: false
      },
      buffer: {
        title: "buffer",
        type: "array",
        items: [
          {
            enum: ["buffer"]
          },
          {
            oneOf: [
              {
                type: "object",
                properties: {
                  countType: {
                    $ref: "dataType"
                  }
                },
                additionalProperties: false,
                required: ["countType"]
              },
              {
                type: "object",
                properties: {
                  count: {
                    $ref: "definitions#/definitions/dataTypeArgsCount"
                  }
                },
                additionalProperties: false,
                required: ["count"]
              }
            ]
          }
        ]
      },
      bitfield: {
        title: "bitfield",
        type: "array",
        items: [
          {
            enum: ["bitfield"]
          },
          {
            type: "array",
            items: {
              type: "object",
              properties: {
                name: {
                  $ref: "definitions#/definitions/fieldName"
                },
                size: {
                  type: "number"
                },
                signed: {
                  type: "boolean"
                }
              },
              required: ["name", "size", "signed"],
              additionalProperties: false
            },
            additionalItems: false
          }
        ],
        additionalItems: false
      },
      bitflags: {
        title: "bitflags",
        type: "array",
        items: [
          {
            enum: ["bitflags"]
          },
          {
            type: "object",
            additionalItems: false
          }
        ],
        additionalItems: false
      },
      mapper: {
        title: "mapper",
        type: "array",
        items: [
          {
            enum: ["mapper"]
          },
          {
            type: "object",
            properties: {
              type: {
                $ref: "dataType"
              },
              mappings: {
                type: "object",
                patternProperties: {
                  "^[-a-zA-Z0-9 _]+$": {
                    type: "string"
                  }
                },
                additionalProperties: false
              }
            },
            required: ["type", "mappings"],
            additionalProperties: false
          }
        ],
        additionalItems: false
      }
    };
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/varint.js
var require_varint = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/varint.js"(exports2, module2) {
    var { PartialReadError: PartialReadError2 } = require_utils();
    module2.exports = {
      varint: [readVarInt, writeVarInt, sizeOfVarInt, require_numeric2().varint],
      varint64: [readVarLong, writeVarLong, sizeOfVarLong, require_numeric2().varint64],
      varint128: [readVarLong128, writeVarLong, sizeOfVarLong, require_numeric2().varint128],
      zigzag32: [readSignedVarInt, writeSignedVarInt, sizeOfSignedVarInt, require_numeric2().zigzag32],
      zigzag64: [readSignedVarLong, writeSignedVarLong, sizeOfSignedVarLong, require_numeric2().zigzag64]
    };
    function readVarInt(buffer, offset) {
      let result = 0;
      let shift = 0;
      let cursor = offset;
      while (true) {
        if (cursor >= buffer.length)
          throw new PartialReadError2("Unexpected buffer end while reading VarInt");
        const byte = buffer.readUInt8(cursor);
        result |= (byte & 127) << shift;
        cursor++;
        if (!(byte & 128)) {
          return { value: result, size: cursor - offset };
        }
        shift += 7;
        if (shift > 64)
          throw new PartialReadError2(`varint is too big: ${shift}`);
      }
    }
    function sizeOfVarInt(value) {
      let cursor = 0;
      while (value & ~127) {
        value >>>= 7;
        cursor++;
      }
      return cursor + 1;
    }
    function writeVarInt(value, buffer, offset) {
      let cursor = 0;
      while (value & ~127) {
        buffer.writeUInt8(value & 255 | 128, offset + cursor);
        cursor++;
        value >>>= 7;
      }
      buffer.writeUInt8(value, offset + cursor);
      return offset + cursor + 1;
    }
    function readVarLong(buffer, offset) {
      let result = 0n;
      let shift = 0n;
      let cursor = offset;
      while (true) {
        if (cursor >= buffer.length)
          throw new PartialReadError2("Unexpected buffer end while reading VarLong");
        const byte = buffer.readUInt8(cursor);
        result |= (BigInt(byte) & 0x7Fn) << shift;
        cursor++;
        if (!(byte & 128)) {
          return { value: result, size: cursor - offset };
        }
        shift += 7n;
        if (shift > 63n)
          throw new Error(`varint is too big: ${shift}`);
      }
    }
    function readVarLong128(buffer, offset) {
      let result = 0n;
      let shift = 0n;
      let cursor = offset;
      while (true) {
        if (cursor >= buffer.length)
          throw new PartialReadError2("Unexpected buffer end while reading VarLong");
        const byte = buffer.readUInt8(cursor);
        result |= (BigInt(byte) & 0x7Fn) << shift;
        cursor++;
        if (!(byte & 128)) {
          return { value: result, size: cursor - offset };
        }
        shift += 7n;
        if (shift > 127n)
          throw new Error(`varint is too big: ${shift}`);
      }
    }
    function sizeOfVarLong(value) {
      value = BigInt(value);
      let size = 0;
      do {
        value >>= 7n;
        size++;
      } while (value !== 0n);
      return size;
    }
    function writeVarLong(value, buffer, offset) {
      value = BigInt(value);
      let cursor = offset;
      do {
        const byte = value & 0x7Fn;
        value >>= 7n;
        buffer.writeUInt8(Number(byte) | (value ? 128 : 0), cursor++);
      } while (value);
      return cursor;
    }
    function readSignedVarInt(buffer, offset) {
      const { value, size } = readVarInt(buffer, offset);
      return { value: value >>> 1 ^ -(value & 1), size };
    }
    function sizeOfSignedVarInt(value) {
      return sizeOfVarInt(value << 1 ^ value >> 31);
    }
    function writeSignedVarInt(value, buffer, offset) {
      return writeVarInt(value << 1 ^ value >> 31, buffer, offset);
    }
    function readSignedVarLong(buffer, offset) {
      const { value, size } = readVarLong(buffer, offset);
      return { value: value >> 1n ^ -(value & 1n), size };
    }
    function sizeOfSignedVarLong(value) {
      return sizeOfVarLong(BigInt(value) << 1n ^ BigInt(value) >> 63n);
    }
    function writeSignedVarLong(value, buffer, offset) {
      return writeVarLong(BigInt(value) << 1n ^ BigInt(value) >> 63n, buffer, offset);
    }
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/utils.js
var require_utils4 = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/utils.js"(exports2, module2) {
    var { getCount, sendCount, calcCount, PartialReadError: PartialReadError2 } = require_utils();
    module2.exports = {
      bool: [readBool, writeBool, 1, require_utils3().bool],
      pstring: [readPString, writePString, sizeOfPString, require_utils3().pstring],
      buffer: [readBuffer, writeBuffer, sizeOfBuffer, require_utils3().buffer],
      void: [readVoid, writeVoid, 0, require_utils3().void],
      bitfield: [readBitField, writeBitField, sizeOfBitField, require_utils3().bitfield],
      bitflags: [readBitflags, writeBitflags, sizeOfBitflags, require_utils3().bitflags],
      cstring: [readCString, writeCString, sizeOfCString, require_utils3().cstring],
      mapper: [readMapper, writeMapper, sizeOfMapper, require_utils3().mapper],
      ...require_varint()
    };
    function mapperEquality(a, b) {
      return a === b || parseInt(a) === parseInt(b);
    }
    function readMapper(buffer, offset, { type, mappings }, rootNode) {
      const { size, value } = this.read(buffer, offset, type, rootNode);
      let mappedValue = null;
      const keys = Object.keys(mappings);
      for (let i = 0; i < keys.length; i++) {
        if (mapperEquality(keys[i], value)) {
          mappedValue = mappings[keys[i]];
          break;
        }
      }
      if (mappedValue == null)
        throw new Error(value + " is not in the mappings value");
      return {
        size,
        value: mappedValue
      };
    }
    function writeMapper(value, buffer, offset, { type, mappings }, rootNode) {
      const keys = Object.keys(mappings);
      let mappedValue = null;
      for (let i = 0; i < keys.length; i++) {
        if (mapperEquality(mappings[keys[i]], value)) {
          mappedValue = keys[i];
          break;
        }
      }
      if (mappedValue == null)
        throw new Error(value + " is not in the mappings value");
      return this.write(mappedValue, buffer, offset, type, rootNode);
    }
    function sizeOfMapper(value, { type, mappings }, rootNode) {
      const keys = Object.keys(mappings);
      let mappedValue = null;
      for (let i = 0; i < keys.length; i++) {
        if (mapperEquality(mappings[keys[i]], value)) {
          mappedValue = keys[i];
          break;
        }
      }
      if (mappedValue == null)
        throw new Error(value + " is not in the mappings value");
      return this.sizeOf(mappedValue, type, rootNode);
    }
    function readPString(buffer, offset, typeArgs, rootNode) {
      const { size, count } = getCount.call(this, buffer, offset, typeArgs, rootNode);
      const cursor = offset + size;
      const strEnd = cursor + count;
      if (strEnd > buffer.length) {
        throw new PartialReadError2("Missing characters in string, found size is " + buffer.length + " expected size was " + strEnd);
      }
      return {
        value: buffer.toString(typeArgs.encoding || "utf8", cursor, strEnd),
        size: strEnd - offset
      };
    }
    function writePString(value, buffer, offset, typeArgs, rootNode) {
      const length = Buffer.byteLength(value, "utf8");
      offset = sendCount.call(this, length, buffer, offset, typeArgs, rootNode);
      buffer.write(value, offset, length, typeArgs.encoding || "utf8");
      return offset + length;
    }
    function sizeOfPString(value, typeArgs, rootNode) {
      const length = Buffer.byteLength(value, typeArgs.encoding || "utf8");
      const size = calcCount.call(this, length, typeArgs, rootNode);
      return size + length;
    }
    function readBool(buffer, offset) {
      if (offset + 1 > buffer.length)
        throw new PartialReadError2();
      const value = buffer.readInt8(offset);
      return {
        value: !!value,
        size: 1
      };
    }
    function writeBool(value, buffer, offset) {
      buffer.writeInt8(+value, offset);
      return offset + 1;
    }
    function readBuffer(buffer, offset, typeArgs, rootNode) {
      const { size, count } = getCount.call(this, buffer, offset, typeArgs, rootNode);
      offset += size;
      if (offset + count > buffer.length)
        throw new PartialReadError2();
      return {
        value: buffer.slice(offset, offset + count),
        size: size + count
      };
    }
    function writeBuffer(value, buffer, offset, typeArgs, rootNode) {
      if (!(value instanceof Buffer))
        value = Buffer.from(value);
      offset = sendCount.call(this, value.length, buffer, offset, typeArgs, rootNode);
      value.copy(buffer, offset);
      return offset + value.length;
    }
    function sizeOfBuffer(value, typeArgs, rootNode) {
      if (!(value instanceof Buffer))
        value = Buffer.from(value);
      const size = calcCount.call(this, value.length, typeArgs, rootNode);
      return size + value.length;
    }
    function readVoid() {
      return {
        value: void 0,
        size: 0
      };
    }
    function writeVoid(value, buffer, offset) {
      return offset;
    }
    function generateBitMask(n) {
      return (1 << n) - 1;
    }
    function readBitField(buffer, offset, typeArgs) {
      const beginOffset = offset;
      let curVal = null;
      let bits = 0;
      const results = {};
      results.value = typeArgs.reduce((acc, { size, signed, name }) => {
        let currentSize = size;
        let val = 0;
        while (currentSize > 0) {
          if (bits === 0) {
            if (buffer.length < offset + 1) {
              throw new PartialReadError2();
            }
            curVal = buffer[offset++];
            bits = 8;
          }
          const bitsToRead = Math.min(currentSize, bits);
          val = val << bitsToRead | (curVal & generateBitMask(bits)) >> bits - bitsToRead;
          bits -= bitsToRead;
          currentSize -= bitsToRead;
        }
        if (signed && val >= 1 << size - 1) {
          val -= 1 << size;
        }
        acc[name] = val;
        return acc;
      }, {});
      results.size = offset - beginOffset;
      return results;
    }
    function writeBitField(value, buffer, offset, typeArgs) {
      let toWrite = 0;
      let bits = 0;
      typeArgs.forEach(({ size, signed, name }) => {
        const val = value[name];
        if (!signed && val < 0 || signed && val < -(1 << size - 1)) {
          throw new Error(value + " < " + signed ? -(1 << size - 1) : 0);
        } else if (!signed && val >= 1 << size || signed && val >= (1 << size - 1) - 1) {
          throw new Error(value + " >= " + signed ? 1 << size : (1 << size - 1) - 1);
        }
        while (size > 0) {
          const writeBits = Math.min(8 - bits, size);
          toWrite = toWrite << writeBits | val >> size - writeBits & generateBitMask(writeBits);
          size -= writeBits;
          bits += writeBits;
          if (bits === 8) {
            buffer[offset++] = toWrite;
            bits = 0;
            toWrite = 0;
          }
        }
      });
      if (bits !== 0) {
        buffer[offset++] = toWrite << 8 - bits;
      }
      return offset;
    }
    function sizeOfBitField(value, typeArgs) {
      return Math.ceil(typeArgs.reduce((acc, { size }) => {
        return acc + size;
      }, 0) / 8);
    }
    function readCString(buffer, offset, typeArgs) {
      let size = 0;
      while (offset + size < buffer.length && buffer[offset + size] !== 0) {
        size++;
      }
      if (buffer.length < offset + size + 1) {
        throw new PartialReadError2();
      }
      return {
        value: buffer.toString(typeArgs?.encoding || "utf8", offset, offset + size),
        size: size + 1
      };
    }
    function writeCString(value, buffer, offset, typeArgs) {
      const length = Buffer.byteLength(value, typeArgs?.encoding || "utf8");
      buffer.write(value, offset, length, typeArgs?.encoding || "utf8");
      offset += length;
      buffer.writeInt8(0, offset);
      return offset + 1;
    }
    function sizeOfCString(value) {
      const length = Buffer.byteLength(value, "utf8");
      return length + 1;
    }
    function readBitflags(buffer, offset, { type, flags, shift, big }, rootNode) {
      const { size, value } = this.read(buffer, offset, type, rootNode);
      let f = {};
      if (Array.isArray(flags)) {
        for (const [k, v] of Object.entries(flags)) {
          f[v] = big ? 1n << BigInt(k) : 1 << k;
        }
      } else if (shift) {
        for (const k in flags) {
          f[k] = big ? 1n << BigInt(flags[k]) : 1 << flags[k];
        }
      } else {
        f = flags;
      }
      const result = { _value: value };
      for (const key in f) {
        result[key] = (value & f[key]) === f[key];
      }
      return { value: result, size };
    }
    function writeBitflags(value, buffer, offset, { type, flags, shift, big }, rootNode) {
      let f = {};
      if (Array.isArray(flags)) {
        for (const [k, v] of Object.entries(flags)) {
          f[v] = big ? 1n << BigInt(k) : 1 << k;
        }
      } else if (shift) {
        for (const k in flags) {
          f[k] = big ? 1n << BigInt(flags[k]) : 1 << flags[k];
        }
      } else {
        f = flags;
      }
      let val = value._value || (big ? 0n : 0);
      for (const key in f) {
        if (value[key])
          val |= f[key];
      }
      return this.write(val, buffer, offset, type, rootNode);
    }
    function sizeOfBitflags(value, { type, flags, shift, big }, rootNode) {
      if (!value)
        throw new Error("Missing field");
      let f = {};
      if (Array.isArray(flags)) {
        for (const [k, v] of Object.entries(flags)) {
          f[v] = big ? 1n << BigInt(k) : 1 << k;
        }
      } else if (shift) {
        for (const k in flags) {
          f[k] = big ? 1n << BigInt(flags[k]) : 1 << flags[k];
        }
      } else {
        f = flags;
      }
      let mappedValue = value._value || (big ? 0n : 0);
      for (const key in f) {
        if (value[key])
          mappedValue |= f[key];
      }
      return this.sizeOf(mappedValue, type, rootNode);
    }
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/ProtoDef/schemas/structures.json
var require_structures2 = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/ProtoDef/schemas/structures.json"(exports2, module2) {
    module2.exports = {
      array: {
        title: "array",
        type: "array",
        items: [
          {
            enum: ["array"]
          },
          {
            oneOf: [
              {
                type: "object",
                properties: {
                  type: {
                    $ref: "dataType"
                  },
                  countType: {
                    $ref: "dataType"
                  }
                },
                additionalProperties: false,
                required: ["type", "countType"]
              },
              {
                type: "object",
                properties: {
                  type: {
                    $ref: "dataType"
                  },
                  count: {
                    $ref: "definitions#/definitions/dataTypeArgsCount"
                  }
                },
                additionalProperties: false,
                required: ["type", "count"]
              }
            ]
          }
        ],
        additionalItems: false
      },
      count: {
        title: "count",
        type: "array",
        items: [
          {
            enum: ["count"]
          },
          {
            type: "object",
            properties: {
              countFor: {
                $ref: "definitions#/definitions/contextualizedFieldName"
              },
              type: {
                $ref: "dataType"
              }
            },
            required: ["countFor", "type"],
            additionalProperties: false
          }
        ],
        additionalItems: false
      },
      container: {
        title: "container",
        type: "array",
        items: [
          {
            enum: ["container"]
          },
          {
            type: "array",
            items: {
              type: "object",
              properties: {
                anon: {
                  type: "boolean"
                },
                name: {
                  $ref: "definitions#/definitions/fieldName"
                },
                type: {
                  $ref: "dataType"
                }
              },
              oneOf: [
                {
                  required: ["anon"]
                },
                {
                  required: ["name"]
                }
              ],
              required: ["type"],
              additionalProperties: false
            },
            additionalItems: false
          }
        ],
        additionalItems: false
      }
    };
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/structures.js
var require_structures3 = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/structures.js"(exports2, module2) {
    var { getField, getCount, sendCount, calcCount, tryDoc } = require_utils();
    module2.exports = {
      array: [readArray, writeArray, sizeOfArray, require_structures2().array],
      count: [readCount, writeCount, sizeOfCount, require_structures2().count],
      container: [readContainer, writeContainer, sizeOfContainer, require_structures2().container]
    };
    function readArray(buffer, offset, typeArgs, rootNode) {
      const results = {
        value: [],
        size: 0
      };
      let value;
      let { count, size } = getCount.call(this, buffer, offset, typeArgs, rootNode);
      offset += size;
      results.size += size;
      for (let i = 0; i < count; i++) {
        ({ size, value } = tryDoc(() => this.read(buffer, offset, typeArgs.type, rootNode), i));
        results.size += size;
        offset += size;
        results.value.push(value);
      }
      return results;
    }
    function writeArray(value, buffer, offset, typeArgs, rootNode) {
      offset = sendCount.call(this, value.length, buffer, offset, typeArgs, rootNode);
      return value.reduce((offset2, v, index) => tryDoc(() => this.write(v, buffer, offset2, typeArgs.type, rootNode), index), offset);
    }
    function sizeOfArray(value, typeArgs, rootNode) {
      let size = calcCount.call(this, value.length, typeArgs, rootNode);
      size = value.reduce((size2, v, index) => tryDoc(() => size2 + this.sizeOf(v, typeArgs.type, rootNode), index), size);
      return size;
    }
    function readContainer(buffer, offset, typeArgs, context) {
      const results = {
        value: { "..": context },
        size: 0
      };
      typeArgs.forEach(({ type, name, anon }) => {
        tryDoc(() => {
          const readResults = this.read(buffer, offset, type, results.value);
          results.size += readResults.size;
          offset += readResults.size;
          if (anon) {
            if (readResults.value !== void 0) {
              Object.keys(readResults.value).forEach((key) => {
                results.value[key] = readResults.value[key];
              });
            }
          } else {
            results.value[name] = readResults.value;
          }
        }, name || "unknown");
      });
      delete results.value[".."];
      return results;
    }
    function writeContainer(value, buffer, offset, typeArgs, context) {
      value[".."] = context;
      offset = typeArgs.reduce((offset2, { type, name, anon }) => tryDoc(() => this.write(anon ? value : value[name], buffer, offset2, type, value), name || "unknown"), offset);
      delete value[".."];
      return offset;
    }
    function sizeOfContainer(value, typeArgs, context) {
      value[".."] = context;
      const size = typeArgs.reduce((size2, { type, name, anon }) => size2 + tryDoc(() => this.sizeOf(anon ? value : value[name], type, value), name || "unknown"), 0);
      delete value[".."];
      return size;
    }
    function readCount(buffer, offset, { type }, rootNode) {
      return this.read(buffer, offset, type, rootNode);
    }
    function writeCount(value, buffer, offset, { countFor, type }, rootNode) {
      return this.write(getField(countFor, rootNode).length, buffer, offset, type, rootNode);
    }
    function sizeOfCount(value, { countFor, type }, rootNode) {
      return this.sizeOf(getField(countFor, rootNode).length, type, rootNode);
    }
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/ProtoDef/schemas/conditional.json
var require_conditional2 = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/ProtoDef/schemas/conditional.json"(exports2, module2) {
    module2.exports = {
      switch: {
        title: "switch",
        type: "array",
        items: [
          {
            enum: ["switch"]
          },
          {
            type: "object",
            properties: {
              compareTo: {
                $ref: "definitions#/definitions/contextualizedFieldName"
              },
              compareToValue: {
                type: "string"
              },
              fields: {
                type: "object",
                patternProperties: {
                  "^[-a-zA-Z0-9 _:/]+$": {
                    $ref: "dataType"
                  }
                },
                additionalProperties: false
              },
              default: {
                $ref: "dataType"
              }
            },
            oneOf: [
              {
                required: ["compareTo", "fields"]
              },
              {
                required: ["compareToValue", "fields"]
              }
            ],
            additionalProperties: false
          }
        ],
        additionalItems: false
      },
      option: {
        title: "option",
        type: "array",
        items: [
          {
            enum: ["option"]
          },
          {
            $ref: "dataType"
          }
        ],
        additionalItems: false
      }
    };
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/conditional.js
var require_conditional3 = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/conditional.js"(exports2, module2) {
    var { getField, getFieldInfo, tryDoc, PartialReadError: PartialReadError2 } = require_utils();
    module2.exports = {
      switch: [readSwitch, writeSwitch, sizeOfSwitch, require_conditional2().switch],
      option: [readOption, writeOption, sizeOfOption, require_conditional2().option]
    };
    function readSwitch(buffer, offset, { compareTo, fields, compareToValue, default: defVal }, rootNode) {
      compareTo = compareToValue !== void 0 ? compareToValue : getField(compareTo, rootNode);
      if (typeof fields[compareTo] === "undefined" && typeof defVal === "undefined") {
        throw new Error(compareTo + " has no associated fieldInfo in switch");
      }
      for (const field in fields) {
        if (field.startsWith("/")) {
          fields[this.types[field.slice(1)]] = fields[field];
          delete fields[field];
        }
      }
      const caseDefault = typeof fields[compareTo] === "undefined";
      const resultingType = caseDefault ? defVal : fields[compareTo];
      const fieldInfo = getFieldInfo(resultingType);
      return tryDoc(() => this.read(buffer, offset, fieldInfo, rootNode), caseDefault ? "default" : compareTo);
    }
    function writeSwitch(value, buffer, offset, { compareTo, fields, compareToValue, default: defVal }, rootNode) {
      compareTo = compareToValue !== void 0 ? compareToValue : getField(compareTo, rootNode);
      if (typeof fields[compareTo] === "undefined" && typeof defVal === "undefined") {
        throw new Error(compareTo + " has no associated fieldInfo in switch");
      }
      for (const field in fields) {
        if (field.startsWith("/")) {
          fields[this.types[field.slice(1)]] = fields[field];
          delete fields[field];
        }
      }
      const caseDefault = typeof fields[compareTo] === "undefined";
      const fieldInfo = getFieldInfo(caseDefault ? defVal : fields[compareTo]);
      return tryDoc(() => this.write(value, buffer, offset, fieldInfo, rootNode), caseDefault ? "default" : compareTo);
    }
    function sizeOfSwitch(value, { compareTo, fields, compareToValue, default: defVal }, rootNode) {
      compareTo = compareToValue !== void 0 ? compareToValue : getField(compareTo, rootNode);
      if (typeof fields[compareTo] === "undefined" && typeof defVal === "undefined") {
        throw new Error(compareTo + " has no associated fieldInfo in switch");
      }
      for (const field in fields) {
        if (field.startsWith("/")) {
          fields[this.types[field.slice(1)]] = fields[field];
          delete fields[field];
        }
      }
      const caseDefault = typeof fields[compareTo] === "undefined";
      const fieldInfo = getFieldInfo(caseDefault ? defVal : fields[compareTo]);
      return tryDoc(() => this.sizeOf(value, fieldInfo, rootNode), caseDefault ? "default" : compareTo);
    }
    function readOption(buffer, offset, typeArgs, context) {
      if (buffer.length < offset + 1) {
        throw new PartialReadError2();
      }
      const val = buffer.readUInt8(offset++);
      if (val !== 0) {
        const retval = this.read(buffer, offset, typeArgs, context);
        retval.size++;
        return retval;
      } else {
        return { size: 1 };
      }
    }
    function writeOption(value, buffer, offset, typeArgs, context) {
      if (value != null) {
        buffer.writeUInt8(1, offset++);
        offset = this.write(value, buffer, offset, typeArgs, context);
      } else {
        buffer.writeUInt8(0, offset++);
      }
      return offset;
    }
    function sizeOfOption(value, typeArgs, context) {
      return value == null ? 1 : this.sizeOf(value, typeArgs, context) + 1;
    }
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/protodef.js
var require_protodef = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/protodef.js"(exports2, module2) {
    var { getFieldInfo, tryCatch: tryCatch2 } = require_utils();
    var reduce = require_lodash();
    var Validator = require_protodef_validator();
    function isFieldInfo(type) {
      return typeof type === "string" || Array.isArray(type) && typeof type[0] === "string" || type.type;
    }
    function findArgs(acc, v, k) {
      if (typeof v === "string" && v.charAt(0) === "$") {
        acc.push({ path: k, val: v.substr(1) });
      } else if (Array.isArray(v) || typeof v === "object") {
        acc = acc.concat(reduce(v, findArgs, []).map((v2) => ({ path: k + "." + v2.path, val: v2.val })));
      }
      return acc;
    }
    function setField(path, val, into) {
      const c = path.split(".").reverse();
      while (c.length > 1) {
        into = into[c.pop()];
      }
      into[c.pop()] = val;
    }
    function extendType(functions, defaultTypeArgs) {
      const json = JSON.stringify(defaultTypeArgs);
      const argPos = reduce(defaultTypeArgs, findArgs, []);
      function produceArgs(typeArgs) {
        const args = JSON.parse(json);
        argPos.forEach((v) => {
          setField(v.path, typeArgs[v.val], args);
        });
        return args;
      }
      return [function read(buffer, offset, typeArgs, context) {
        return functions[0].call(this, buffer, offset, produceArgs(typeArgs), context);
      }, function write(value, buffer, offset, typeArgs, context) {
        return functions[1].call(this, value, buffer, offset, produceArgs(typeArgs), context);
      }, function sizeOf(value, typeArgs, context) {
        if (typeof functions[2] === "function") {
          return functions[2].call(this, value, produceArgs(typeArgs), context);
        } else {
          return functions[2];
        }
      }];
    }
    var ProtoDef = class {
      constructor(validation = true) {
        this.types = {};
        this.validator = validation ? new Validator() : null;
        this.addDefaultTypes();
      }
      addDefaultTypes() {
        this.addTypes(require_numeric3());
        this.addTypes(require_utils4());
        this.addTypes(require_structures3());
        this.addTypes(require_conditional3());
      }
      addProtocol(protocolData, path) {
        const self2 = this;
        function recursiveAddTypes(protocolData2, path2) {
          if (protocolData2 === void 0) {
            return;
          }
          if (protocolData2.types) {
            self2.addTypes(protocolData2.types);
          }
          recursiveAddTypes(protocolData2?.[path2[0]], path2.slice(1));
        }
        if (this.validator) {
          this.validator.validateProtocol(protocolData);
        }
        recursiveAddTypes(protocolData, path);
      }
      addType(name, functions, validate = true) {
        if (functions === "native") {
          if (this.validator) {
            this.validator.addType(name);
          }
          return;
        }
        if (isFieldInfo(functions)) {
          if (this.validator) {
            if (validate) {
              this.validator.validateType(functions);
            }
            this.validator.addType(name);
          }
          const { type, typeArgs } = getFieldInfo(functions);
          this.types[name] = typeArgs ? extendType(this.types[type], typeArgs) : this.types[type];
        } else {
          if (this.validator) {
            if (functions[3]) {
              this.validator.addType(name, functions[3]);
            } else {
              this.validator.addType(name);
            }
          }
          this.types[name] = functions;
        }
      }
      addTypes(types) {
        Object.keys(types).forEach((name) => this.addType(name, types[name], false));
        if (this.validator) {
          Object.keys(types).forEach((name) => {
            if (isFieldInfo(types[name])) {
              this.validator.validateType(types[name]);
            }
          });
        }
      }
      setVariable(key, val) {
        this.types[key] = val;
      }
      read(buffer, cursor, _fieldInfo, rootNodes) {
        const { type, typeArgs } = getFieldInfo(_fieldInfo);
        const typeFunctions = this.types[type];
        if (!typeFunctions) {
          throw new Error("missing data type: " + type);
        }
        return typeFunctions[0].call(this, buffer, cursor, typeArgs, rootNodes);
      }
      write(value, buffer, offset, _fieldInfo, rootNode) {
        const { type, typeArgs } = getFieldInfo(_fieldInfo);
        const typeFunctions = this.types[type];
        if (!typeFunctions) {
          throw new Error("missing data type: " + type);
        }
        return typeFunctions[1].call(this, value, buffer, offset, typeArgs, rootNode);
      }
      sizeOf(value, _fieldInfo, rootNode) {
        const { type, typeArgs } = getFieldInfo(_fieldInfo);
        const typeFunctions = this.types[type];
        if (!typeFunctions) {
          throw new Error("missing data type: " + type);
        }
        if (typeof typeFunctions[2] === "function") {
          return typeFunctions[2].call(this, value, typeArgs, rootNode);
        } else {
          return typeFunctions[2];
        }
      }
      createPacketBuffer(type, packet) {
        const length = tryCatch2(
          () => this.sizeOf(packet, type, {}),
          (e) => {
            e.message = `SizeOf error for ${e.field} : ${e.message}`;
            throw e;
          }
        );
        const buffer = Buffer.allocUnsafe(length);
        tryCatch2(
          () => this.write(packet, buffer, 0, type, {}),
          (e) => {
            e.message = `Write error for ${e.field} : ${e.message}`;
            throw e;
          }
        );
        return buffer;
      }
      parsePacketBuffer(type, buffer, offset = 0) {
        const { value, size } = tryCatch2(
          () => this.read(buffer, offset, type, {}),
          (e) => {
            e.message = `Read error for ${e.field} : ${e.message}`;
            throw e;
          }
        );
        return {
          data: value,
          metadata: {
            size
          },
          buffer: buffer.slice(0, size),
          fullBuffer: buffer
        };
      }
    };
    module2.exports = ProtoDef;
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/ours/primordials.js
var require_primordials = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/ours/primordials.js"(exports2, module2) {
    "use strict";
    var AggregateError = class extends Error {
      constructor(errors) {
        if (!Array.isArray(errors)) {
          throw new TypeError(`Expected input to be an Array, got ${typeof errors}`);
        }
        let message = "";
        for (let i = 0; i < errors.length; i++) {
          message += `    ${errors[i].stack}
`;
        }
        super(message);
        this.name = "AggregateError";
        this.errors = errors;
      }
    };
    module2.exports = {
      AggregateError,
      ArrayIsArray(self2) {
        return Array.isArray(self2);
      },
      ArrayPrototypeIncludes(self2, el) {
        return self2.includes(el);
      },
      ArrayPrototypeIndexOf(self2, el) {
        return self2.indexOf(el);
      },
      ArrayPrototypeJoin(self2, sep) {
        return self2.join(sep);
      },
      ArrayPrototypeMap(self2, fn) {
        return self2.map(fn);
      },
      ArrayPrototypePop(self2, el) {
        return self2.pop(el);
      },
      ArrayPrototypePush(self2, el) {
        return self2.push(el);
      },
      ArrayPrototypeSlice(self2, start, end) {
        return self2.slice(start, end);
      },
      Error,
      FunctionPrototypeCall(fn, thisArgs, ...args) {
        return fn.call(thisArgs, ...args);
      },
      FunctionPrototypeSymbolHasInstance(self2, instance) {
        return Function.prototype[Symbol.hasInstance].call(self2, instance);
      },
      MathFloor: Math.floor,
      Number,
      NumberIsInteger: Number.isInteger,
      NumberIsNaN: Number.isNaN,
      NumberMAX_SAFE_INTEGER: Number.MAX_SAFE_INTEGER,
      NumberMIN_SAFE_INTEGER: Number.MIN_SAFE_INTEGER,
      NumberParseInt: Number.parseInt,
      ObjectDefineProperties(self2, props) {
        return Object.defineProperties(self2, props);
      },
      ObjectDefineProperty(self2, name, prop) {
        return Object.defineProperty(self2, name, prop);
      },
      ObjectGetOwnPropertyDescriptor(self2, name) {
        return Object.getOwnPropertyDescriptor(self2, name);
      },
      ObjectKeys(obj) {
        return Object.keys(obj);
      },
      ObjectSetPrototypeOf(target, proto) {
        return Object.setPrototypeOf(target, proto);
      },
      Promise,
      PromisePrototypeCatch(self2, fn) {
        return self2.catch(fn);
      },
      PromisePrototypeThen(self2, thenFn, catchFn) {
        return self2.then(thenFn, catchFn);
      },
      PromiseReject(err) {
        return Promise.reject(err);
      },
      PromiseResolve(val) {
        return Promise.resolve(val);
      },
      ReflectApply: Reflect.apply,
      RegExpPrototypeTest(self2, value) {
        return self2.test(value);
      },
      SafeSet: Set,
      String,
      StringPrototypeSlice(self2, start, end) {
        return self2.slice(start, end);
      },
      StringPrototypeToLowerCase(self2) {
        return self2.toLowerCase();
      },
      StringPrototypeToUpperCase(self2) {
        return self2.toUpperCase();
      },
      StringPrototypeTrim(self2) {
        return self2.trim();
      },
      Symbol,
      SymbolFor: Symbol.for,
      SymbolAsyncIterator: Symbol.asyncIterator,
      SymbolHasInstance: Symbol.hasInstance,
      SymbolIterator: Symbol.iterator,
      SymbolDispose: Symbol.dispose || Symbol("Symbol.dispose"),
      SymbolAsyncDispose: Symbol.asyncDispose || Symbol("Symbol.asyncDispose"),
      TypedArrayPrototypeSet(self2, buf, len) {
        return self2.set(buf, len);
      },
      Boolean,
      Uint8Array
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/ours/util/inspect.js
var require_inspect = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/ours/util/inspect.js"(exports2, module2) {
    "use strict";
    module2.exports = {
      format(format, ...args) {
        return format.replace(/%([sdifj])/g, function(...[_unused, type]) {
          const replacement = args.shift();
          if (type === "f") {
            return replacement.toFixed(6);
          } else if (type === "j") {
            return JSON.stringify(replacement);
          } else if (type === "s" && typeof replacement === "object") {
            const ctor = replacement.constructor !== Object ? replacement.constructor.name : "";
            return `${ctor} {}`.trim();
          } else {
            return replacement.toString();
          }
        });
      },
      inspect(value) {
        switch (typeof value) {
          case "string":
            if (value.includes("'")) {
              if (!value.includes('"')) {
                return `"${value}"`;
              } else if (!value.includes("`") && !value.includes("${")) {
                return `\`${value}\``;
              }
            }
            return `'${value}'`;
          case "number":
            if (isNaN(value)) {
              return "NaN";
            } else if (Object.is(value, -0)) {
              return String(value);
            }
            return value;
          case "bigint":
            return `${String(value)}n`;
          case "boolean":
          case "undefined":
            return String(value);
          case "object":
            return "{}";
        }
      }
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/ours/errors.js
var require_errors = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/ours/errors.js"(exports2, module2) {
    "use strict";
    var { format, inspect } = require_inspect();
    var { AggregateError: CustomAggregateError } = require_primordials();
    var AggregateError = globalThis.AggregateError || CustomAggregateError;
    var kIsNodeError = Symbol("kIsNodeError");
    var kTypes = [
      "string",
      "function",
      "number",
      "object",
      // Accept 'Function' and 'Object' as alternative to the lower cased version.
      "Function",
      "Object",
      "boolean",
      "bigint",
      "symbol"
    ];
    var classRegExp = /^([A-Z][a-z0-9]*)+$/;
    var nodeInternalPrefix = "__node_internal_";
    var codes = {};
    function assert(value, message) {
      if (!value) {
        throw new codes.ERR_INTERNAL_ASSERTION(message);
      }
    }
    function addNumericalSeparator(val) {
      let res = "";
      let i = val.length;
      const start = val[0] === "-" ? 1 : 0;
      for (; i >= start + 4; i -= 3) {
        res = `_${val.slice(i - 3, i)}${res}`;
      }
      return `${val.slice(0, i)}${res}`;
    }
    function getMessage(key, msg, args) {
      if (typeof msg === "function") {
        assert(
          msg.length <= args.length,
          // Default options do not count.
          `Code: ${key}; The provided arguments length (${args.length}) does not match the required ones (${msg.length}).`
        );
        return msg(...args);
      }
      const expectedLength = (msg.match(/%[dfijoOs]/g) || []).length;
      assert(
        expectedLength === args.length,
        `Code: ${key}; The provided arguments length (${args.length}) does not match the required ones (${expectedLength}).`
      );
      if (args.length === 0) {
        return msg;
      }
      return format(msg, ...args);
    }
    function E(code2, message, Base) {
      if (!Base) {
        Base = Error;
      }
      class NodeError extends Base {
        constructor(...args) {
          super(getMessage(code2, message, args));
        }
        toString() {
          return `${this.name} [${code2}]: ${this.message}`;
        }
      }
      Object.defineProperties(NodeError.prototype, {
        name: {
          value: Base.name,
          writable: true,
          enumerable: false,
          configurable: true
        },
        toString: {
          value() {
            return `${this.name} [${code2}]: ${this.message}`;
          },
          writable: true,
          enumerable: false,
          configurable: true
        }
      });
      NodeError.prototype.code = code2;
      NodeError.prototype[kIsNodeError] = true;
      codes[code2] = NodeError;
    }
    function hideStackFrames(fn) {
      const hidden = nodeInternalPrefix + fn.name;
      Object.defineProperty(fn, "name", {
        value: hidden
      });
      return fn;
    }
    function aggregateTwoErrors(innerError, outerError) {
      if (innerError && outerError && innerError !== outerError) {
        if (Array.isArray(outerError.errors)) {
          outerError.errors.push(innerError);
          return outerError;
        }
        const err = new AggregateError([outerError, innerError], outerError.message);
        err.code = outerError.code;
        return err;
      }
      return innerError || outerError;
    }
    var AbortError = class extends Error {
      constructor(message = "The operation was aborted", options = void 0) {
        if (options !== void 0 && typeof options !== "object") {
          throw new codes.ERR_INVALID_ARG_TYPE("options", "Object", options);
        }
        super(message, options);
        this.code = "ABORT_ERR";
        this.name = "AbortError";
      }
    };
    E("ERR_ASSERTION", "%s", Error);
    E(
      "ERR_INVALID_ARG_TYPE",
      (name, expected, actual) => {
        assert(typeof name === "string", "'name' must be a string");
        if (!Array.isArray(expected)) {
          expected = [expected];
        }
        let msg = "The ";
        if (name.endsWith(" argument")) {
          msg += `${name} `;
        } else {
          msg += `"${name}" ${name.includes(".") ? "property" : "argument"} `;
        }
        msg += "must be ";
        const types = [];
        const instances = [];
        const other = [];
        for (const value of expected) {
          assert(typeof value === "string", "All expected entries have to be of type string");
          if (kTypes.includes(value)) {
            types.push(value.toLowerCase());
          } else if (classRegExp.test(value)) {
            instances.push(value);
          } else {
            assert(value !== "object", 'The value "object" should be written as "Object"');
            other.push(value);
          }
        }
        if (instances.length > 0) {
          const pos = types.indexOf("object");
          if (pos !== -1) {
            types.splice(types, pos, 1);
            instances.push("Object");
          }
        }
        if (types.length > 0) {
          switch (types.length) {
            case 1:
              msg += `of type ${types[0]}`;
              break;
            case 2:
              msg += `one of type ${types[0]} or ${types[1]}`;
              break;
            default: {
              const last = types.pop();
              msg += `one of type ${types.join(", ")}, or ${last}`;
            }
          }
          if (instances.length > 0 || other.length > 0) {
            msg += " or ";
          }
        }
        if (instances.length > 0) {
          switch (instances.length) {
            case 1:
              msg += `an instance of ${instances[0]}`;
              break;
            case 2:
              msg += `an instance of ${instances[0]} or ${instances[1]}`;
              break;
            default: {
              const last = instances.pop();
              msg += `an instance of ${instances.join(", ")}, or ${last}`;
            }
          }
          if (other.length > 0) {
            msg += " or ";
          }
        }
        switch (other.length) {
          case 0:
            break;
          case 1:
            if (other[0].toLowerCase() !== other[0]) {
              msg += "an ";
            }
            msg += `${other[0]}`;
            break;
          case 2:
            msg += `one of ${other[0]} or ${other[1]}`;
            break;
          default: {
            const last = other.pop();
            msg += `one of ${other.join(", ")}, or ${last}`;
          }
        }
        if (actual == null) {
          msg += `. Received ${actual}`;
        } else if (typeof actual === "function" && actual.name) {
          msg += `. Received function ${actual.name}`;
        } else if (typeof actual === "object") {
          var _actual$constructor;
          if ((_actual$constructor = actual.constructor) !== null && _actual$constructor !== void 0 && _actual$constructor.name) {
            msg += `. Received an instance of ${actual.constructor.name}`;
          } else {
            const inspected = inspect(actual, {
              depth: -1
            });
            msg += `. Received ${inspected}`;
          }
        } else {
          let inspected = inspect(actual, {
            colors: false
          });
          if (inspected.length > 25) {
            inspected = `${inspected.slice(0, 25)}...`;
          }
          msg += `. Received type ${typeof actual} (${inspected})`;
        }
        return msg;
      },
      TypeError
    );
    E(
      "ERR_INVALID_ARG_VALUE",
      (name, value, reason = "is invalid") => {
        let inspected = inspect(value);
        if (inspected.length > 128) {
          inspected = inspected.slice(0, 128) + "...";
        }
        const type = name.includes(".") ? "property" : "argument";
        return `The ${type} '${name}' ${reason}. Received ${inspected}`;
      },
      TypeError
    );
    E(
      "ERR_INVALID_RETURN_VALUE",
      (input, name, value) => {
        var _value$constructor;
        const type = value !== null && value !== void 0 && (_value$constructor = value.constructor) !== null && _value$constructor !== void 0 && _value$constructor.name ? `instance of ${value.constructor.name}` : `type ${typeof value}`;
        return `Expected ${input} to be returned from the "${name}" function but got ${type}.`;
      },
      TypeError
    );
    E(
      "ERR_MISSING_ARGS",
      (...args) => {
        assert(args.length > 0, "At least one arg needs to be specified");
        let msg;
        const len = args.length;
        args = (Array.isArray(args) ? args : [args]).map((a) => `"${a}"`).join(" or ");
        switch (len) {
          case 1:
            msg += `The ${args[0]} argument`;
            break;
          case 2:
            msg += `The ${args[0]} and ${args[1]} arguments`;
            break;
          default:
            {
              const last = args.pop();
              msg += `The ${args.join(", ")}, and ${last} arguments`;
            }
            break;
        }
        return `${msg} must be specified`;
      },
      TypeError
    );
    E(
      "ERR_OUT_OF_RANGE",
      (str, range, input) => {
        assert(range, 'Missing "range" argument');
        let received;
        if (Number.isInteger(input) && Math.abs(input) > 2 ** 32) {
          received = addNumericalSeparator(String(input));
        } else if (typeof input === "bigint") {
          received = String(input);
          const limit = BigInt(2) ** BigInt(32);
          if (input > limit || input < -limit) {
            received = addNumericalSeparator(received);
          }
          received += "n";
        } else {
          received = inspect(input);
        }
        return `The value of "${str}" is out of range. It must be ${range}. Received ${received}`;
      },
      RangeError
    );
    E("ERR_MULTIPLE_CALLBACK", "Callback called multiple times", Error);
    E("ERR_METHOD_NOT_IMPLEMENTED", "The %s method is not implemented", Error);
    E("ERR_STREAM_ALREADY_FINISHED", "Cannot call %s after a stream was finished", Error);
    E("ERR_STREAM_CANNOT_PIPE", "Cannot pipe, not readable", Error);
    E("ERR_STREAM_DESTROYED", "Cannot call %s after a stream was destroyed", Error);
    E("ERR_STREAM_NULL_VALUES", "May not write null values to stream", TypeError);
    E("ERR_STREAM_PREMATURE_CLOSE", "Premature close", Error);
    E("ERR_STREAM_PUSH_AFTER_EOF", "stream.push() after EOF", Error);
    E("ERR_STREAM_UNSHIFT_AFTER_END_EVENT", "stream.unshift() after end event", Error);
    E("ERR_STREAM_WRITE_AFTER_END", "write after end", Error);
    E("ERR_UNKNOWN_ENCODING", "Unknown encoding: %s", TypeError);
    module2.exports = {
      AbortError,
      aggregateTwoErrors: hideStackFrames(aggregateTwoErrors),
      hideStackFrames,
      codes
    };
  }
});

// ../node_modules/.pnpm/event-target-shim@5.0.1/node_modules/event-target-shim/dist/event-target-shim.js
var require_event_target_shim = __commonJS({
  "../node_modules/.pnpm/event-target-shim@5.0.1/node_modules/event-target-shim/dist/event-target-shim.js"(exports2, module2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    var privateData = /* @__PURE__ */ new WeakMap();
    var wrappers = /* @__PURE__ */ new WeakMap();
    function pd(event) {
      const retv = privateData.get(event);
      console.assert(
        retv != null,
        "'this' is expected an Event object, but got",
        event
      );
      return retv;
    }
    function setCancelFlag(data) {
      if (data.passiveListener != null) {
        if (typeof console !== "undefined" && typeof console.error === "function") {
          console.error(
            "Unable to preventDefault inside passive event listener invocation.",
            data.passiveListener
          );
        }
        return;
      }
      if (!data.event.cancelable) {
        return;
      }
      data.canceled = true;
      if (typeof data.event.preventDefault === "function") {
        data.event.preventDefault();
      }
    }
    function Event(eventTarget, event) {
      privateData.set(this, {
        eventTarget,
        event,
        eventPhase: 2,
        currentTarget: eventTarget,
        canceled: false,
        stopped: false,
        immediateStopped: false,
        passiveListener: null,
        timeStamp: event.timeStamp || Date.now()
      });
      Object.defineProperty(this, "isTrusted", { value: false, enumerable: true });
      const keys = Object.keys(event);
      for (let i = 0; i < keys.length; ++i) {
        const key = keys[i];
        if (!(key in this)) {
          Object.defineProperty(this, key, defineRedirectDescriptor(key));
        }
      }
    }
    Event.prototype = {
      /**
       * The type of this event.
       * @type {string}
       */
      get type() {
        return pd(this).event.type;
      },
      /**
       * The target of this event.
       * @type {EventTarget}
       */
      get target() {
        return pd(this).eventTarget;
      },
      /**
       * The target of this event.
       * @type {EventTarget}
       */
      get currentTarget() {
        return pd(this).currentTarget;
      },
      /**
       * @returns {EventTarget[]} The composed path of this event.
       */
      composedPath() {
        const currentTarget = pd(this).currentTarget;
        if (currentTarget == null) {
          return [];
        }
        return [currentTarget];
      },
      /**
       * Constant of NONE.
       * @type {number}
       */
      get NONE() {
        return 0;
      },
      /**
       * Constant of CAPTURING_PHASE.
       * @type {number}
       */
      get CAPTURING_PHASE() {
        return 1;
      },
      /**
       * Constant of AT_TARGET.
       * @type {number}
       */
      get AT_TARGET() {
        return 2;
      },
      /**
       * Constant of BUBBLING_PHASE.
       * @type {number}
       */
      get BUBBLING_PHASE() {
        return 3;
      },
      /**
       * The target of this event.
       * @type {number}
       */
      get eventPhase() {
        return pd(this).eventPhase;
      },
      /**
       * Stop event bubbling.
       * @returns {void}
       */
      stopPropagation() {
        const data = pd(this);
        data.stopped = true;
        if (typeof data.event.stopPropagation === "function") {
          data.event.stopPropagation();
        }
      },
      /**
       * Stop event bubbling.
       * @returns {void}
       */
      stopImmediatePropagation() {
        const data = pd(this);
        data.stopped = true;
        data.immediateStopped = true;
        if (typeof data.event.stopImmediatePropagation === "function") {
          data.event.stopImmediatePropagation();
        }
      },
      /**
       * The flag to be bubbling.
       * @type {boolean}
       */
      get bubbles() {
        return Boolean(pd(this).event.bubbles);
      },
      /**
       * The flag to be cancelable.
       * @type {boolean}
       */
      get cancelable() {
        return Boolean(pd(this).event.cancelable);
      },
      /**
       * Cancel this event.
       * @returns {void}
       */
      preventDefault() {
        setCancelFlag(pd(this));
      },
      /**
       * The flag to indicate cancellation state.
       * @type {boolean}
       */
      get defaultPrevented() {
        return pd(this).canceled;
      },
      /**
       * The flag to be composed.
       * @type {boolean}
       */
      get composed() {
        return Boolean(pd(this).event.composed);
      },
      /**
       * The unix time of this event.
       * @type {number}
       */
      get timeStamp() {
        return pd(this).timeStamp;
      },
      /**
       * The target of this event.
       * @type {EventTarget}
       * @deprecated
       */
      get srcElement() {
        return pd(this).eventTarget;
      },
      /**
       * The flag to stop event bubbling.
       * @type {boolean}
       * @deprecated
       */
      get cancelBubble() {
        return pd(this).stopped;
      },
      set cancelBubble(value) {
        if (!value) {
          return;
        }
        const data = pd(this);
        data.stopped = true;
        if (typeof data.event.cancelBubble === "boolean") {
          data.event.cancelBubble = true;
        }
      },
      /**
       * The flag to indicate cancellation state.
       * @type {boolean}
       * @deprecated
       */
      get returnValue() {
        return !pd(this).canceled;
      },
      set returnValue(value) {
        if (!value) {
          setCancelFlag(pd(this));
        }
      },
      /**
       * Initialize this event object. But do nothing under event dispatching.
       * @param {string} type The event type.
       * @param {boolean} [bubbles=false] The flag to be possible to bubble up.
       * @param {boolean} [cancelable=false] The flag to be possible to cancel.
       * @deprecated
       */
      initEvent() {
      }
    };
    Object.defineProperty(Event.prototype, "constructor", {
      value: Event,
      configurable: true,
      writable: true
    });
    if (typeof window !== "undefined" && typeof window.Event !== "undefined") {
      Object.setPrototypeOf(Event.prototype, window.Event.prototype);
      wrappers.set(window.Event.prototype, Event);
    }
    function defineRedirectDescriptor(key) {
      return {
        get() {
          return pd(this).event[key];
        },
        set(value) {
          pd(this).event[key] = value;
        },
        configurable: true,
        enumerable: true
      };
    }
    function defineCallDescriptor(key) {
      return {
        value() {
          const event = pd(this).event;
          return event[key].apply(event, arguments);
        },
        configurable: true,
        enumerable: true
      };
    }
    function defineWrapper(BaseEvent, proto) {
      const keys = Object.keys(proto);
      if (keys.length === 0) {
        return BaseEvent;
      }
      function CustomEvent(eventTarget, event) {
        BaseEvent.call(this, eventTarget, event);
      }
      CustomEvent.prototype = Object.create(BaseEvent.prototype, {
        constructor: { value: CustomEvent, configurable: true, writable: true }
      });
      for (let i = 0; i < keys.length; ++i) {
        const key = keys[i];
        if (!(key in BaseEvent.prototype)) {
          const descriptor = Object.getOwnPropertyDescriptor(proto, key);
          const isFunc = typeof descriptor.value === "function";
          Object.defineProperty(
            CustomEvent.prototype,
            key,
            isFunc ? defineCallDescriptor(key) : defineRedirectDescriptor(key)
          );
        }
      }
      return CustomEvent;
    }
    function getWrapper(proto) {
      if (proto == null || proto === Object.prototype) {
        return Event;
      }
      let wrapper = wrappers.get(proto);
      if (wrapper == null) {
        wrapper = defineWrapper(getWrapper(Object.getPrototypeOf(proto)), proto);
        wrappers.set(proto, wrapper);
      }
      return wrapper;
    }
    function wrapEvent(eventTarget, event) {
      const Wrapper = getWrapper(Object.getPrototypeOf(event));
      return new Wrapper(eventTarget, event);
    }
    function isStopped(event) {
      return pd(event).immediateStopped;
    }
    function setEventPhase(event, eventPhase) {
      pd(event).eventPhase = eventPhase;
    }
    function setCurrentTarget(event, currentTarget) {
      pd(event).currentTarget = currentTarget;
    }
    function setPassiveListener(event, passiveListener) {
      pd(event).passiveListener = passiveListener;
    }
    var listenersMap = /* @__PURE__ */ new WeakMap();
    var CAPTURE = 1;
    var BUBBLE = 2;
    var ATTRIBUTE = 3;
    function isObject(x) {
      return x !== null && typeof x === "object";
    }
    function getListeners(eventTarget) {
      const listeners = listenersMap.get(eventTarget);
      if (listeners == null) {
        throw new TypeError(
          "'this' is expected an EventTarget object, but got another value."
        );
      }
      return listeners;
    }
    function defineEventAttributeDescriptor(eventName) {
      return {
        get() {
          const listeners = getListeners(this);
          let node = listeners.get(eventName);
          while (node != null) {
            if (node.listenerType === ATTRIBUTE) {
              return node.listener;
            }
            node = node.next;
          }
          return null;
        },
        set(listener) {
          if (typeof listener !== "function" && !isObject(listener)) {
            listener = null;
          }
          const listeners = getListeners(this);
          let prev = null;
          let node = listeners.get(eventName);
          while (node != null) {
            if (node.listenerType === ATTRIBUTE) {
              if (prev !== null) {
                prev.next = node.next;
              } else if (node.next !== null) {
                listeners.set(eventName, node.next);
              } else {
                listeners.delete(eventName);
              }
            } else {
              prev = node;
            }
            node = node.next;
          }
          if (listener !== null) {
            const newNode = {
              listener,
              listenerType: ATTRIBUTE,
              passive: false,
              once: false,
              next: null
            };
            if (prev === null) {
              listeners.set(eventName, newNode);
            } else {
              prev.next = newNode;
            }
          }
        },
        configurable: true,
        enumerable: true
      };
    }
    function defineEventAttribute(eventTargetPrototype, eventName) {
      Object.defineProperty(
        eventTargetPrototype,
        `on${eventName}`,
        defineEventAttributeDescriptor(eventName)
      );
    }
    function defineCustomEventTarget(eventNames) {
      function CustomEventTarget() {
        EventTarget.call(this);
      }
      CustomEventTarget.prototype = Object.create(EventTarget.prototype, {
        constructor: {
          value: CustomEventTarget,
          configurable: true,
          writable: true
        }
      });
      for (let i = 0; i < eventNames.length; ++i) {
        defineEventAttribute(CustomEventTarget.prototype, eventNames[i]);
      }
      return CustomEventTarget;
    }
    function EventTarget() {
      if (this instanceof EventTarget) {
        listenersMap.set(this, /* @__PURE__ */ new Map());
        return;
      }
      if (arguments.length === 1 && Array.isArray(arguments[0])) {
        return defineCustomEventTarget(arguments[0]);
      }
      if (arguments.length > 0) {
        const types = new Array(arguments.length);
        for (let i = 0; i < arguments.length; ++i) {
          types[i] = arguments[i];
        }
        return defineCustomEventTarget(types);
      }
      throw new TypeError("Cannot call a class as a function");
    }
    EventTarget.prototype = {
      /**
       * Add a given listener to this event target.
       * @param {string} eventName The event name to add.
       * @param {Function} listener The listener to add.
       * @param {boolean|{capture?:boolean,passive?:boolean,once?:boolean}} [options] The options for this listener.
       * @returns {void}
       */
      addEventListener(eventName, listener, options) {
        if (listener == null) {
          return;
        }
        if (typeof listener !== "function" && !isObject(listener)) {
          throw new TypeError("'listener' should be a function or an object.");
        }
        const listeners = getListeners(this);
        const optionsIsObj = isObject(options);
        const capture = optionsIsObj ? Boolean(options.capture) : Boolean(options);
        const listenerType = capture ? CAPTURE : BUBBLE;
        const newNode = {
          listener,
          listenerType,
          passive: optionsIsObj && Boolean(options.passive),
          once: optionsIsObj && Boolean(options.once),
          next: null
        };
        let node = listeners.get(eventName);
        if (node === void 0) {
          listeners.set(eventName, newNode);
          return;
        }
        let prev = null;
        while (node != null) {
          if (node.listener === listener && node.listenerType === listenerType) {
            return;
          }
          prev = node;
          node = node.next;
        }
        prev.next = newNode;
      },
      /**
       * Remove a given listener from this event target.
       * @param {string} eventName The event name to remove.
       * @param {Function} listener The listener to remove.
       * @param {boolean|{capture?:boolean,passive?:boolean,once?:boolean}} [options] The options for this listener.
       * @returns {void}
       */
      removeEventListener(eventName, listener, options) {
        if (listener == null) {
          return;
        }
        const listeners = getListeners(this);
        const capture = isObject(options) ? Boolean(options.capture) : Boolean(options);
        const listenerType = capture ? CAPTURE : BUBBLE;
        let prev = null;
        let node = listeners.get(eventName);
        while (node != null) {
          if (node.listener === listener && node.listenerType === listenerType) {
            if (prev !== null) {
              prev.next = node.next;
            } else if (node.next !== null) {
              listeners.set(eventName, node.next);
            } else {
              listeners.delete(eventName);
            }
            return;
          }
          prev = node;
          node = node.next;
        }
      },
      /**
       * Dispatch a given event.
       * @param {Event|{type:string}} event The event to dispatch.
       * @returns {boolean} `false` if canceled.
       */
      dispatchEvent(event) {
        if (event == null || typeof event.type !== "string") {
          throw new TypeError('"event.type" should be a string.');
        }
        const listeners = getListeners(this);
        const eventName = event.type;
        let node = listeners.get(eventName);
        if (node == null) {
          return true;
        }
        const wrappedEvent = wrapEvent(this, event);
        let prev = null;
        while (node != null) {
          if (node.once) {
            if (prev !== null) {
              prev.next = node.next;
            } else if (node.next !== null) {
              listeners.set(eventName, node.next);
            } else {
              listeners.delete(eventName);
            }
          } else {
            prev = node;
          }
          setPassiveListener(
            wrappedEvent,
            node.passive ? node.listener : null
          );
          if (typeof node.listener === "function") {
            try {
              node.listener.call(this, wrappedEvent);
            } catch (err) {
              if (typeof console !== "undefined" && typeof console.error === "function") {
                console.error(err);
              }
            }
          } else if (node.listenerType !== ATTRIBUTE && typeof node.listener.handleEvent === "function") {
            node.listener.handleEvent(wrappedEvent);
          }
          if (isStopped(wrappedEvent)) {
            break;
          }
          node = node.next;
        }
        setPassiveListener(wrappedEvent, null);
        setEventPhase(wrappedEvent, 0);
        setCurrentTarget(wrappedEvent, null);
        return !wrappedEvent.defaultPrevented;
      }
    };
    Object.defineProperty(EventTarget.prototype, "constructor", {
      value: EventTarget,
      configurable: true,
      writable: true
    });
    if (typeof window !== "undefined" && typeof window.EventTarget !== "undefined") {
      Object.setPrototypeOf(EventTarget.prototype, window.EventTarget.prototype);
    }
    exports2.defineEventAttribute = defineEventAttribute;
    exports2.EventTarget = EventTarget;
    exports2.default = EventTarget;
    module2.exports = EventTarget;
    module2.exports.EventTarget = module2.exports["default"] = EventTarget;
    module2.exports.defineEventAttribute = defineEventAttribute;
  }
});

// ../node_modules/.pnpm/abort-controller@3.0.0/node_modules/abort-controller/dist/abort-controller.js
var require_abort_controller = __commonJS({
  "../node_modules/.pnpm/abort-controller@3.0.0/node_modules/abort-controller/dist/abort-controller.js"(exports2, module2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    var eventTargetShim = require_event_target_shim();
    var AbortSignal = class extends eventTargetShim.EventTarget {
      /**
       * AbortSignal cannot be constructed directly.
       */
      constructor() {
        super();
        throw new TypeError("AbortSignal cannot be constructed directly");
      }
      /**
       * Returns `true` if this `AbortSignal`'s `AbortController` has signaled to abort, and `false` otherwise.
       */
      get aborted() {
        const aborted = abortedFlags.get(this);
        if (typeof aborted !== "boolean") {
          throw new TypeError(`Expected 'this' to be an 'AbortSignal' object, but got ${this === null ? "null" : typeof this}`);
        }
        return aborted;
      }
    };
    eventTargetShim.defineEventAttribute(AbortSignal.prototype, "abort");
    function createAbortSignal() {
      const signal = Object.create(AbortSignal.prototype);
      eventTargetShim.EventTarget.call(signal);
      abortedFlags.set(signal, false);
      return signal;
    }
    function abortSignal(signal) {
      if (abortedFlags.get(signal) !== false) {
        return;
      }
      abortedFlags.set(signal, true);
      signal.dispatchEvent({ type: "abort" });
    }
    var abortedFlags = /* @__PURE__ */ new WeakMap();
    Object.defineProperties(AbortSignal.prototype, {
      aborted: { enumerable: true }
    });
    if (typeof Symbol === "function" && typeof Symbol.toStringTag === "symbol") {
      Object.defineProperty(AbortSignal.prototype, Symbol.toStringTag, {
        configurable: true,
        value: "AbortSignal"
      });
    }
    var AbortController = class {
      /**
       * Initialize this controller.
       */
      constructor() {
        signals.set(this, createAbortSignal());
      }
      /**
       * Returns the `AbortSignal` object associated with this object.
       */
      get signal() {
        return getSignal(this);
      }
      /**
       * Abort and signal to any observers that the associated activity is to be aborted.
       */
      abort() {
        abortSignal(getSignal(this));
      }
    };
    var signals = /* @__PURE__ */ new WeakMap();
    function getSignal(controller) {
      const signal = signals.get(controller);
      if (signal == null) {
        throw new TypeError(`Expected 'this' to be an 'AbortController' object, but got ${controller === null ? "null" : typeof controller}`);
      }
      return signal;
    }
    Object.defineProperties(AbortController.prototype, {
      signal: { enumerable: true },
      abort: { enumerable: true }
    });
    if (typeof Symbol === "function" && typeof Symbol.toStringTag === "symbol") {
      Object.defineProperty(AbortController.prototype, Symbol.toStringTag, {
        configurable: true,
        value: "AbortController"
      });
    }
    exports2.AbortController = AbortController;
    exports2.AbortSignal = AbortSignal;
    exports2.default = AbortController;
    module2.exports = AbortController;
    module2.exports.AbortController = module2.exports["default"] = AbortController;
    module2.exports.AbortSignal = AbortSignal;
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/ours/util.js
var require_util2 = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/ours/util.js"(exports2, module2) {
    "use strict";
    var bufferModule = require("buffer");
    var { format, inspect } = require_inspect();
    var {
      codes: { ERR_INVALID_ARG_TYPE }
    } = require_errors();
    var { kResistStopPropagation, AggregateError, SymbolDispose } = require_primordials();
    var AbortSignal = globalThis.AbortSignal || require_abort_controller().AbortSignal;
    var AbortController = globalThis.AbortController || require_abort_controller().AbortController;
    var AsyncFunction = Object.getPrototypeOf(async function() {
    }).constructor;
    var Blob = globalThis.Blob || bufferModule.Blob;
    var isBlob = typeof Blob !== "undefined" ? function isBlob2(b) {
      return b instanceof Blob;
    } : function isBlob2(b) {
      return false;
    };
    var validateAbortSignal = (signal, name) => {
      if (signal !== void 0 && (signal === null || typeof signal !== "object" || !("aborted" in signal))) {
        throw new ERR_INVALID_ARG_TYPE(name, "AbortSignal", signal);
      }
    };
    var validateFunction = (value, name) => {
      if (typeof value !== "function") {
        throw new ERR_INVALID_ARG_TYPE(name, "Function", value);
      }
    };
    module2.exports = {
      AggregateError,
      kEmptyObject: Object.freeze({}),
      once(callback) {
        let called = false;
        return function(...args) {
          if (called) {
            return;
          }
          called = true;
          callback.apply(this, args);
        };
      },
      createDeferredPromise: function() {
        let resolve;
        let reject;
        const promise = new Promise((res, rej) => {
          resolve = res;
          reject = rej;
        });
        return {
          promise,
          resolve,
          reject
        };
      },
      promisify(fn) {
        return new Promise((resolve, reject) => {
          fn((err, ...args) => {
            if (err) {
              return reject(err);
            }
            return resolve(...args);
          });
        });
      },
      debuglog() {
        return function() {
        };
      },
      format,
      inspect,
      types: {
        isAsyncFunction(fn) {
          return fn instanceof AsyncFunction;
        },
        isArrayBufferView(arr) {
          return ArrayBuffer.isView(arr);
        }
      },
      isBlob,
      deprecate(fn, message) {
        return fn;
      },
      addAbortListener: require("events").addAbortListener || function addAbortListener(signal, listener) {
        if (signal === void 0) {
          throw new ERR_INVALID_ARG_TYPE("signal", "AbortSignal", signal);
        }
        validateAbortSignal(signal, "signal");
        validateFunction(listener, "listener");
        let removeEventListener;
        if (signal.aborted) {
          queueMicrotask(() => listener());
        } else {
          signal.addEventListener("abort", listener, {
            __proto__: null,
            once: true,
            [kResistStopPropagation]: true
          });
          removeEventListener = () => {
            signal.removeEventListener("abort", listener);
          };
        }
        return {
          __proto__: null,
          [SymbolDispose]() {
            var _removeEventListener;
            (_removeEventListener = removeEventListener) === null || _removeEventListener === void 0 ? void 0 : _removeEventListener();
          }
        };
      },
      AbortSignalAny: AbortSignal.any || function AbortSignalAny(signals) {
        if (signals.length === 1) {
          return signals[0];
        }
        const ac = new AbortController();
        const abort = () => ac.abort();
        signals.forEach((signal) => {
          validateAbortSignal(signal, "signals");
          signal.addEventListener("abort", abort, {
            once: true
          });
        });
        ac.signal.addEventListener(
          "abort",
          () => {
            signals.forEach((signal) => signal.removeEventListener("abort", abort));
          },
          {
            once: true
          }
        );
        return ac.signal;
      }
    };
    module2.exports.promisify.custom = Symbol.for("nodejs.util.promisify.custom");
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/validators.js
var require_validators = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/validators.js"(exports2, module2) {
    "use strict";
    var {
      ArrayIsArray,
      ArrayPrototypeIncludes,
      ArrayPrototypeJoin,
      ArrayPrototypeMap,
      NumberIsInteger,
      NumberIsNaN,
      NumberMAX_SAFE_INTEGER,
      NumberMIN_SAFE_INTEGER,
      NumberParseInt,
      ObjectPrototypeHasOwnProperty,
      RegExpPrototypeExec,
      String: String2,
      StringPrototypeToUpperCase,
      StringPrototypeTrim
    } = require_primordials();
    var {
      hideStackFrames,
      codes: { ERR_SOCKET_BAD_PORT, ERR_INVALID_ARG_TYPE, ERR_INVALID_ARG_VALUE, ERR_OUT_OF_RANGE, ERR_UNKNOWN_SIGNAL }
    } = require_errors();
    var { normalizeEncoding } = require_util2();
    var { isAsyncFunction, isArrayBufferView } = require_util2().types;
    var signals = {};
    function isInt32(value) {
      return value === (value | 0);
    }
    function isUint32(value) {
      return value === value >>> 0;
    }
    var octalReg = /^[0-7]+$/;
    var modeDesc = "must be a 32-bit unsigned integer or an octal string";
    function parseFileMode(value, name, def) {
      if (typeof value === "undefined") {
        value = def;
      }
      if (typeof value === "string") {
        if (RegExpPrototypeExec(octalReg, value) === null) {
          throw new ERR_INVALID_ARG_VALUE(name, value, modeDesc);
        }
        value = NumberParseInt(value, 8);
      }
      validateUint32(value, name);
      return value;
    }
    var validateInteger = hideStackFrames((value, name, min = NumberMIN_SAFE_INTEGER, max = NumberMAX_SAFE_INTEGER) => {
      if (typeof value !== "number")
        throw new ERR_INVALID_ARG_TYPE(name, "number", value);
      if (!NumberIsInteger(value))
        throw new ERR_OUT_OF_RANGE(name, "an integer", value);
      if (value < min || value > max)
        throw new ERR_OUT_OF_RANGE(name, `>= ${min} && <= ${max}`, value);
    });
    var validateInt32 = hideStackFrames((value, name, min = -2147483648, max = 2147483647) => {
      if (typeof value !== "number") {
        throw new ERR_INVALID_ARG_TYPE(name, "number", value);
      }
      if (!NumberIsInteger(value)) {
        throw new ERR_OUT_OF_RANGE(name, "an integer", value);
      }
      if (value < min || value > max) {
        throw new ERR_OUT_OF_RANGE(name, `>= ${min} && <= ${max}`, value);
      }
    });
    var validateUint32 = hideStackFrames((value, name, positive = false) => {
      if (typeof value !== "number") {
        throw new ERR_INVALID_ARG_TYPE(name, "number", value);
      }
      if (!NumberIsInteger(value)) {
        throw new ERR_OUT_OF_RANGE(name, "an integer", value);
      }
      const min = positive ? 1 : 0;
      const max = 4294967295;
      if (value < min || value > max) {
        throw new ERR_OUT_OF_RANGE(name, `>= ${min} && <= ${max}`, value);
      }
    });
    function validateString(value, name) {
      if (typeof value !== "string")
        throw new ERR_INVALID_ARG_TYPE(name, "string", value);
    }
    function validateNumber(value, name, min = void 0, max) {
      if (typeof value !== "number")
        throw new ERR_INVALID_ARG_TYPE(name, "number", value);
      if (min != null && value < min || max != null && value > max || (min != null || max != null) && NumberIsNaN(value)) {
        throw new ERR_OUT_OF_RANGE(
          name,
          `${min != null ? `>= ${min}` : ""}${min != null && max != null ? " && " : ""}${max != null ? `<= ${max}` : ""}`,
          value
        );
      }
    }
    var validateOneOf = hideStackFrames((value, name, oneOf) => {
      if (!ArrayPrototypeIncludes(oneOf, value)) {
        const allowed = ArrayPrototypeJoin(
          ArrayPrototypeMap(oneOf, (v) => typeof v === "string" ? `'${v}'` : String2(v)),
          ", "
        );
        const reason = "must be one of: " + allowed;
        throw new ERR_INVALID_ARG_VALUE(name, value, reason);
      }
    });
    function validateBoolean(value, name) {
      if (typeof value !== "boolean")
        throw new ERR_INVALID_ARG_TYPE(name, "boolean", value);
    }
    function getOwnPropertyValueOrDefault(options, key, defaultValue) {
      return options == null || !ObjectPrototypeHasOwnProperty(options, key) ? defaultValue : options[key];
    }
    var validateObject = hideStackFrames((value, name, options = null) => {
      const allowArray = getOwnPropertyValueOrDefault(options, "allowArray", false);
      const allowFunction = getOwnPropertyValueOrDefault(options, "allowFunction", false);
      const nullable = getOwnPropertyValueOrDefault(options, "nullable", false);
      if (!nullable && value === null || !allowArray && ArrayIsArray(value) || typeof value !== "object" && (!allowFunction || typeof value !== "function")) {
        throw new ERR_INVALID_ARG_TYPE(name, "Object", value);
      }
    });
    var validateDictionary = hideStackFrames((value, name) => {
      if (value != null && typeof value !== "object" && typeof value !== "function") {
        throw new ERR_INVALID_ARG_TYPE(name, "a dictionary", value);
      }
    });
    var validateArray = hideStackFrames((value, name, minLength = 0) => {
      if (!ArrayIsArray(value)) {
        throw new ERR_INVALID_ARG_TYPE(name, "Array", value);
      }
      if (value.length < minLength) {
        const reason = `must be longer than ${minLength}`;
        throw new ERR_INVALID_ARG_VALUE(name, value, reason);
      }
    });
    function validateStringArray(value, name) {
      validateArray(value, name);
      for (let i = 0; i < value.length; i++) {
        validateString(value[i], `${name}[${i}]`);
      }
    }
    function validateBooleanArray(value, name) {
      validateArray(value, name);
      for (let i = 0; i < value.length; i++) {
        validateBoolean(value[i], `${name}[${i}]`);
      }
    }
    function validateAbortSignalArray(value, name) {
      validateArray(value, name);
      for (let i = 0; i < value.length; i++) {
        const signal = value[i];
        const indexedName = `${name}[${i}]`;
        if (signal == null) {
          throw new ERR_INVALID_ARG_TYPE(indexedName, "AbortSignal", signal);
        }
        validateAbortSignal(signal, indexedName);
      }
    }
    function validateSignalName(signal, name = "signal") {
      validateString(signal, name);
      if (signals[signal] === void 0) {
        if (signals[StringPrototypeToUpperCase(signal)] !== void 0) {
          throw new ERR_UNKNOWN_SIGNAL(signal + " (signals must use all capital letters)");
        }
        throw new ERR_UNKNOWN_SIGNAL(signal);
      }
    }
    var validateBuffer = hideStackFrames((buffer, name = "buffer") => {
      if (!isArrayBufferView(buffer)) {
        throw new ERR_INVALID_ARG_TYPE(name, ["Buffer", "TypedArray", "DataView"], buffer);
      }
    });
    function validateEncoding(data, encoding) {
      const normalizedEncoding = normalizeEncoding(encoding);
      const length = data.length;
      if (normalizedEncoding === "hex" && length % 2 !== 0) {
        throw new ERR_INVALID_ARG_VALUE("encoding", encoding, `is invalid for data of length ${length}`);
      }
    }
    function validatePort(port, name = "Port", allowZero = true) {
      if (typeof port !== "number" && typeof port !== "string" || typeof port === "string" && StringPrototypeTrim(port).length === 0 || +port !== +port >>> 0 || port > 65535 || port === 0 && !allowZero) {
        throw new ERR_SOCKET_BAD_PORT(name, port, allowZero);
      }
      return port | 0;
    }
    var validateAbortSignal = hideStackFrames((signal, name) => {
      if (signal !== void 0 && (signal === null || typeof signal !== "object" || !("aborted" in signal))) {
        throw new ERR_INVALID_ARG_TYPE(name, "AbortSignal", signal);
      }
    });
    var validateFunction = hideStackFrames((value, name) => {
      if (typeof value !== "function")
        throw new ERR_INVALID_ARG_TYPE(name, "Function", value);
    });
    var validatePlainFunction = hideStackFrames((value, name) => {
      if (typeof value !== "function" || isAsyncFunction(value))
        throw new ERR_INVALID_ARG_TYPE(name, "Function", value);
    });
    var validateUndefined = hideStackFrames((value, name) => {
      if (value !== void 0)
        throw new ERR_INVALID_ARG_TYPE(name, "undefined", value);
    });
    function validateUnion(value, name, union) {
      if (!ArrayPrototypeIncludes(union, value)) {
        throw new ERR_INVALID_ARG_TYPE(name, `('${ArrayPrototypeJoin(union, "|")}')`, value);
      }
    }
    var linkValueRegExp = /^(?:<[^>]*>)(?:\s*;\s*[^;"\s]+(?:=(")?[^;"\s]*\1)?)*$/;
    function validateLinkHeaderFormat(value, name) {
      if (typeof value === "undefined" || !RegExpPrototypeExec(linkValueRegExp, value)) {
        throw new ERR_INVALID_ARG_VALUE(
          name,
          value,
          'must be an array or string of format "</styles.css>; rel=preload; as=style"'
        );
      }
    }
    function validateLinkHeaderValue(hints) {
      if (typeof hints === "string") {
        validateLinkHeaderFormat(hints, "hints");
        return hints;
      } else if (ArrayIsArray(hints)) {
        const hintsLength = hints.length;
        let result = "";
        if (hintsLength === 0) {
          return result;
        }
        for (let i = 0; i < hintsLength; i++) {
          const link = hints[i];
          validateLinkHeaderFormat(link, "hints");
          result += link;
          if (i !== hintsLength - 1) {
            result += ", ";
          }
        }
        return result;
      }
      throw new ERR_INVALID_ARG_VALUE(
        "hints",
        hints,
        'must be an array or string of format "</styles.css>; rel=preload; as=style"'
      );
    }
    module2.exports = {
      isInt32,
      isUint32,
      parseFileMode,
      validateArray,
      validateStringArray,
      validateBooleanArray,
      validateAbortSignalArray,
      validateBoolean,
      validateBuffer,
      validateDictionary,
      validateEncoding,
      validateFunction,
      validateInt32,
      validateInteger,
      validateNumber,
      validateObject,
      validateOneOf,
      validatePlainFunction,
      validatePort,
      validateSignalName,
      validateString,
      validateUint32,
      validateUndefined,
      validateUnion,
      validateAbortSignal,
      validateLinkHeaderValue
    };
  }
});

// ../node_modules/.pnpm/process@0.11.10/node_modules/process/index.js
var require_process = __commonJS({
  "../node_modules/.pnpm/process@0.11.10/node_modules/process/index.js"(exports2, module2) {
    module2.exports = global.process;
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/utils.js
var require_utils5 = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/utils.js"(exports2, module2) {
    "use strict";
    var { SymbolAsyncIterator, SymbolIterator, SymbolFor } = require_primordials();
    var kIsDestroyed = SymbolFor("nodejs.stream.destroyed");
    var kIsErrored = SymbolFor("nodejs.stream.errored");
    var kIsReadable = SymbolFor("nodejs.stream.readable");
    var kIsWritable = SymbolFor("nodejs.stream.writable");
    var kIsDisturbed = SymbolFor("nodejs.stream.disturbed");
    var kIsClosedPromise = SymbolFor("nodejs.webstream.isClosedPromise");
    var kControllerErrorFunction = SymbolFor("nodejs.webstream.controllerErrorFunction");
    function isReadableNodeStream(obj, strict = false) {
      var _obj$_readableState;
      return !!(obj && typeof obj.pipe === "function" && typeof obj.on === "function" && (!strict || typeof obj.pause === "function" && typeof obj.resume === "function") && (!obj._writableState || ((_obj$_readableState = obj._readableState) === null || _obj$_readableState === void 0 ? void 0 : _obj$_readableState.readable) !== false) && // Duplex
      (!obj._writableState || obj._readableState));
    }
    function isWritableNodeStream(obj) {
      var _obj$_writableState;
      return !!(obj && typeof obj.write === "function" && typeof obj.on === "function" && (!obj._readableState || ((_obj$_writableState = obj._writableState) === null || _obj$_writableState === void 0 ? void 0 : _obj$_writableState.writable) !== false));
    }
    function isDuplexNodeStream(obj) {
      return !!(obj && typeof obj.pipe === "function" && obj._readableState && typeof obj.on === "function" && typeof obj.write === "function");
    }
    function isNodeStream(obj) {
      return obj && (obj._readableState || obj._writableState || typeof obj.write === "function" && typeof obj.on === "function" || typeof obj.pipe === "function" && typeof obj.on === "function");
    }
    function isReadableStream(obj) {
      return !!(obj && !isNodeStream(obj) && typeof obj.pipeThrough === "function" && typeof obj.getReader === "function" && typeof obj.cancel === "function");
    }
    function isWritableStream(obj) {
      return !!(obj && !isNodeStream(obj) && typeof obj.getWriter === "function" && typeof obj.abort === "function");
    }
    function isTransformStream(obj) {
      return !!(obj && !isNodeStream(obj) && typeof obj.readable === "object" && typeof obj.writable === "object");
    }
    function isWebStream(obj) {
      return isReadableStream(obj) || isWritableStream(obj) || isTransformStream(obj);
    }
    function isIterable(obj, isAsync) {
      if (obj == null)
        return false;
      if (isAsync === true)
        return typeof obj[SymbolAsyncIterator] === "function";
      if (isAsync === false)
        return typeof obj[SymbolIterator] === "function";
      return typeof obj[SymbolAsyncIterator] === "function" || typeof obj[SymbolIterator] === "function";
    }
    function isDestroyed(stream) {
      if (!isNodeStream(stream))
        return null;
      const wState = stream._writableState;
      const rState = stream._readableState;
      const state = wState || rState;
      return !!(stream.destroyed || stream[kIsDestroyed] || state !== null && state !== void 0 && state.destroyed);
    }
    function isWritableEnded(stream) {
      if (!isWritableNodeStream(stream))
        return null;
      if (stream.writableEnded === true)
        return true;
      const wState = stream._writableState;
      if (wState !== null && wState !== void 0 && wState.errored)
        return false;
      if (typeof (wState === null || wState === void 0 ? void 0 : wState.ended) !== "boolean")
        return null;
      return wState.ended;
    }
    function isWritableFinished(stream, strict) {
      if (!isWritableNodeStream(stream))
        return null;
      if (stream.writableFinished === true)
        return true;
      const wState = stream._writableState;
      if (wState !== null && wState !== void 0 && wState.errored)
        return false;
      if (typeof (wState === null || wState === void 0 ? void 0 : wState.finished) !== "boolean")
        return null;
      return !!(wState.finished || strict === false && wState.ended === true && wState.length === 0);
    }
    function isReadableEnded(stream) {
      if (!isReadableNodeStream(stream))
        return null;
      if (stream.readableEnded === true)
        return true;
      const rState = stream._readableState;
      if (!rState || rState.errored)
        return false;
      if (typeof (rState === null || rState === void 0 ? void 0 : rState.ended) !== "boolean")
        return null;
      return rState.ended;
    }
    function isReadableFinished(stream, strict) {
      if (!isReadableNodeStream(stream))
        return null;
      const rState = stream._readableState;
      if (rState !== null && rState !== void 0 && rState.errored)
        return false;
      if (typeof (rState === null || rState === void 0 ? void 0 : rState.endEmitted) !== "boolean")
        return null;
      return !!(rState.endEmitted || strict === false && rState.ended === true && rState.length === 0);
    }
    function isReadable(stream) {
      if (stream && stream[kIsReadable] != null)
        return stream[kIsReadable];
      if (typeof (stream === null || stream === void 0 ? void 0 : stream.readable) !== "boolean")
        return null;
      if (isDestroyed(stream))
        return false;
      return isReadableNodeStream(stream) && stream.readable && !isReadableFinished(stream);
    }
    function isWritable(stream) {
      if (stream && stream[kIsWritable] != null)
        return stream[kIsWritable];
      if (typeof (stream === null || stream === void 0 ? void 0 : stream.writable) !== "boolean")
        return null;
      if (isDestroyed(stream))
        return false;
      return isWritableNodeStream(stream) && stream.writable && !isWritableEnded(stream);
    }
    function isFinished(stream, opts) {
      if (!isNodeStream(stream)) {
        return null;
      }
      if (isDestroyed(stream)) {
        return true;
      }
      if ((opts === null || opts === void 0 ? void 0 : opts.readable) !== false && isReadable(stream)) {
        return false;
      }
      if ((opts === null || opts === void 0 ? void 0 : opts.writable) !== false && isWritable(stream)) {
        return false;
      }
      return true;
    }
    function isWritableErrored(stream) {
      var _stream$_writableStat, _stream$_writableStat2;
      if (!isNodeStream(stream)) {
        return null;
      }
      if (stream.writableErrored) {
        return stream.writableErrored;
      }
      return (_stream$_writableStat = (_stream$_writableStat2 = stream._writableState) === null || _stream$_writableStat2 === void 0 ? void 0 : _stream$_writableStat2.errored) !== null && _stream$_writableStat !== void 0 ? _stream$_writableStat : null;
    }
    function isReadableErrored(stream) {
      var _stream$_readableStat, _stream$_readableStat2;
      if (!isNodeStream(stream)) {
        return null;
      }
      if (stream.readableErrored) {
        return stream.readableErrored;
      }
      return (_stream$_readableStat = (_stream$_readableStat2 = stream._readableState) === null || _stream$_readableStat2 === void 0 ? void 0 : _stream$_readableStat2.errored) !== null && _stream$_readableStat !== void 0 ? _stream$_readableStat : null;
    }
    function isClosed(stream) {
      if (!isNodeStream(stream)) {
        return null;
      }
      if (typeof stream.closed === "boolean") {
        return stream.closed;
      }
      const wState = stream._writableState;
      const rState = stream._readableState;
      if (typeof (wState === null || wState === void 0 ? void 0 : wState.closed) === "boolean" || typeof (rState === null || rState === void 0 ? void 0 : rState.closed) === "boolean") {
        return (wState === null || wState === void 0 ? void 0 : wState.closed) || (rState === null || rState === void 0 ? void 0 : rState.closed);
      }
      if (typeof stream._closed === "boolean" && isOutgoingMessage(stream)) {
        return stream._closed;
      }
      return null;
    }
    function isOutgoingMessage(stream) {
      return typeof stream._closed === "boolean" && typeof stream._defaultKeepAlive === "boolean" && typeof stream._removedConnection === "boolean" && typeof stream._removedContLen === "boolean";
    }
    function isServerResponse(stream) {
      return typeof stream._sent100 === "boolean" && isOutgoingMessage(stream);
    }
    function isServerRequest(stream) {
      var _stream$req;
      return typeof stream._consuming === "boolean" && typeof stream._dumped === "boolean" && ((_stream$req = stream.req) === null || _stream$req === void 0 ? void 0 : _stream$req.upgradeOrConnect) === void 0;
    }
    function willEmitClose(stream) {
      if (!isNodeStream(stream))
        return null;
      const wState = stream._writableState;
      const rState = stream._readableState;
      const state = wState || rState;
      return !state && isServerResponse(stream) || !!(state && state.autoDestroy && state.emitClose && state.closed === false);
    }
    function isDisturbed(stream) {
      var _stream$kIsDisturbed;
      return !!(stream && ((_stream$kIsDisturbed = stream[kIsDisturbed]) !== null && _stream$kIsDisturbed !== void 0 ? _stream$kIsDisturbed : stream.readableDidRead || stream.readableAborted));
    }
    function isErrored(stream) {
      var _ref, _ref2, _ref3, _ref4, _ref5, _stream$kIsErrored, _stream$_readableStat3, _stream$_writableStat3, _stream$_readableStat4, _stream$_writableStat4;
      return !!(stream && ((_ref = (_ref2 = (_ref3 = (_ref4 = (_ref5 = (_stream$kIsErrored = stream[kIsErrored]) !== null && _stream$kIsErrored !== void 0 ? _stream$kIsErrored : stream.readableErrored) !== null && _ref5 !== void 0 ? _ref5 : stream.writableErrored) !== null && _ref4 !== void 0 ? _ref4 : (_stream$_readableStat3 = stream._readableState) === null || _stream$_readableStat3 === void 0 ? void 0 : _stream$_readableStat3.errorEmitted) !== null && _ref3 !== void 0 ? _ref3 : (_stream$_writableStat3 = stream._writableState) === null || _stream$_writableStat3 === void 0 ? void 0 : _stream$_writableStat3.errorEmitted) !== null && _ref2 !== void 0 ? _ref2 : (_stream$_readableStat4 = stream._readableState) === null || _stream$_readableStat4 === void 0 ? void 0 : _stream$_readableStat4.errored) !== null && _ref !== void 0 ? _ref : (_stream$_writableStat4 = stream._writableState) === null || _stream$_writableStat4 === void 0 ? void 0 : _stream$_writableStat4.errored));
    }
    module2.exports = {
      isDestroyed,
      kIsDestroyed,
      isDisturbed,
      kIsDisturbed,
      isErrored,
      kIsErrored,
      isReadable,
      kIsReadable,
      kIsClosedPromise,
      kControllerErrorFunction,
      kIsWritable,
      isClosed,
      isDuplexNodeStream,
      isFinished,
      isIterable,
      isReadableNodeStream,
      isReadableStream,
      isReadableEnded,
      isReadableFinished,
      isReadableErrored,
      isNodeStream,
      isWebStream,
      isWritable,
      isWritableNodeStream,
      isWritableStream,
      isWritableEnded,
      isWritableFinished,
      isWritableErrored,
      isServerRequest,
      isServerResponse,
      willEmitClose,
      isTransformStream
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/end-of-stream.js
var require_end_of_stream = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/end-of-stream.js"(exports2, module2) {
    "use strict";
    var process2 = require_process();
    var { AbortError, codes } = require_errors();
    var { ERR_INVALID_ARG_TYPE, ERR_STREAM_PREMATURE_CLOSE } = codes;
    var { kEmptyObject, once } = require_util2();
    var { validateAbortSignal, validateFunction, validateObject, validateBoolean } = require_validators();
    var { Promise: Promise2, PromisePrototypeThen, SymbolDispose } = require_primordials();
    var {
      isClosed,
      isReadable,
      isReadableNodeStream,
      isReadableStream,
      isReadableFinished,
      isReadableErrored,
      isWritable,
      isWritableNodeStream,
      isWritableStream,
      isWritableFinished,
      isWritableErrored,
      isNodeStream,
      willEmitClose: _willEmitClose,
      kIsClosedPromise
    } = require_utils5();
    var addAbortListener;
    function isRequest(stream) {
      return stream.setHeader && typeof stream.abort === "function";
    }
    var nop = () => {
    };
    function eos(stream, options, callback) {
      var _options$readable, _options$writable;
      if (arguments.length === 2) {
        callback = options;
        options = kEmptyObject;
      } else if (options == null) {
        options = kEmptyObject;
      } else {
        validateObject(options, "options");
      }
      validateFunction(callback, "callback");
      validateAbortSignal(options.signal, "options.signal");
      callback = once(callback);
      if (isReadableStream(stream) || isWritableStream(stream)) {
        return eosWeb(stream, options, callback);
      }
      if (!isNodeStream(stream)) {
        throw new ERR_INVALID_ARG_TYPE("stream", ["ReadableStream", "WritableStream", "Stream"], stream);
      }
      const readable = (_options$readable = options.readable) !== null && _options$readable !== void 0 ? _options$readable : isReadableNodeStream(stream);
      const writable = (_options$writable = options.writable) !== null && _options$writable !== void 0 ? _options$writable : isWritableNodeStream(stream);
      const wState = stream._writableState;
      const rState = stream._readableState;
      const onlegacyfinish = () => {
        if (!stream.writable) {
          onfinish();
        }
      };
      let willEmitClose = _willEmitClose(stream) && isReadableNodeStream(stream) === readable && isWritableNodeStream(stream) === writable;
      let writableFinished = isWritableFinished(stream, false);
      const onfinish = () => {
        writableFinished = true;
        if (stream.destroyed) {
          willEmitClose = false;
        }
        if (willEmitClose && (!stream.readable || readable)) {
          return;
        }
        if (!readable || readableFinished) {
          callback.call(stream);
        }
      };
      let readableFinished = isReadableFinished(stream, false);
      const onend = () => {
        readableFinished = true;
        if (stream.destroyed) {
          willEmitClose = false;
        }
        if (willEmitClose && (!stream.writable || writable)) {
          return;
        }
        if (!writable || writableFinished) {
          callback.call(stream);
        }
      };
      const onerror = (err) => {
        callback.call(stream, err);
      };
      let closed = isClosed(stream);
      const onclose = () => {
        closed = true;
        const errored = isWritableErrored(stream) || isReadableErrored(stream);
        if (errored && typeof errored !== "boolean") {
          return callback.call(stream, errored);
        }
        if (readable && !readableFinished && isReadableNodeStream(stream, true)) {
          if (!isReadableFinished(stream, false))
            return callback.call(stream, new ERR_STREAM_PREMATURE_CLOSE());
        }
        if (writable && !writableFinished) {
          if (!isWritableFinished(stream, false))
            return callback.call(stream, new ERR_STREAM_PREMATURE_CLOSE());
        }
        callback.call(stream);
      };
      const onclosed = () => {
        closed = true;
        const errored = isWritableErrored(stream) || isReadableErrored(stream);
        if (errored && typeof errored !== "boolean") {
          return callback.call(stream, errored);
        }
        callback.call(stream);
      };
      const onrequest = () => {
        stream.req.on("finish", onfinish);
      };
      if (isRequest(stream)) {
        stream.on("complete", onfinish);
        if (!willEmitClose) {
          stream.on("abort", onclose);
        }
        if (stream.req) {
          onrequest();
        } else {
          stream.on("request", onrequest);
        }
      } else if (writable && !wState) {
        stream.on("end", onlegacyfinish);
        stream.on("close", onlegacyfinish);
      }
      if (!willEmitClose && typeof stream.aborted === "boolean") {
        stream.on("aborted", onclose);
      }
      stream.on("end", onend);
      stream.on("finish", onfinish);
      if (options.error !== false) {
        stream.on("error", onerror);
      }
      stream.on("close", onclose);
      if (closed) {
        process2.nextTick(onclose);
      } else if (wState !== null && wState !== void 0 && wState.errorEmitted || rState !== null && rState !== void 0 && rState.errorEmitted) {
        if (!willEmitClose) {
          process2.nextTick(onclosed);
        }
      } else if (!readable && (!willEmitClose || isReadable(stream)) && (writableFinished || isWritable(stream) === false)) {
        process2.nextTick(onclosed);
      } else if (!writable && (!willEmitClose || isWritable(stream)) && (readableFinished || isReadable(stream) === false)) {
        process2.nextTick(onclosed);
      } else if (rState && stream.req && stream.aborted) {
        process2.nextTick(onclosed);
      }
      const cleanup = () => {
        callback = nop;
        stream.removeListener("aborted", onclose);
        stream.removeListener("complete", onfinish);
        stream.removeListener("abort", onclose);
        stream.removeListener("request", onrequest);
        if (stream.req)
          stream.req.removeListener("finish", onfinish);
        stream.removeListener("end", onlegacyfinish);
        stream.removeListener("close", onlegacyfinish);
        stream.removeListener("finish", onfinish);
        stream.removeListener("end", onend);
        stream.removeListener("error", onerror);
        stream.removeListener("close", onclose);
      };
      if (options.signal && !closed) {
        const abort = () => {
          const endCallback = callback;
          cleanup();
          endCallback.call(
            stream,
            new AbortError(void 0, {
              cause: options.signal.reason
            })
          );
        };
        if (options.signal.aborted) {
          process2.nextTick(abort);
        } else {
          addAbortListener = addAbortListener || require_util2().addAbortListener;
          const disposable = addAbortListener(options.signal, abort);
          const originalCallback = callback;
          callback = once((...args) => {
            disposable[SymbolDispose]();
            originalCallback.apply(stream, args);
          });
        }
      }
      return cleanup;
    }
    function eosWeb(stream, options, callback) {
      let isAborted = false;
      let abort = nop;
      if (options.signal) {
        abort = () => {
          isAborted = true;
          callback.call(
            stream,
            new AbortError(void 0, {
              cause: options.signal.reason
            })
          );
        };
        if (options.signal.aborted) {
          process2.nextTick(abort);
        } else {
          addAbortListener = addAbortListener || require_util2().addAbortListener;
          const disposable = addAbortListener(options.signal, abort);
          const originalCallback = callback;
          callback = once((...args) => {
            disposable[SymbolDispose]();
            originalCallback.apply(stream, args);
          });
        }
      }
      const resolverFn = (...args) => {
        if (!isAborted) {
          process2.nextTick(() => callback.apply(stream, args));
        }
      };
      PromisePrototypeThen(stream[kIsClosedPromise].promise, resolverFn, resolverFn);
      return nop;
    }
    function finished(stream, opts) {
      var _opts;
      let autoCleanup = false;
      if (opts === null) {
        opts = kEmptyObject;
      }
      if ((_opts = opts) !== null && _opts !== void 0 && _opts.cleanup) {
        validateBoolean(opts.cleanup, "cleanup");
        autoCleanup = opts.cleanup;
      }
      return new Promise2((resolve, reject) => {
        const cleanup = eos(stream, opts, (err) => {
          if (autoCleanup) {
            cleanup();
          }
          if (err) {
            reject(err);
          } else {
            resolve();
          }
        });
      });
    }
    module2.exports = eos;
    module2.exports.finished = finished;
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/destroy.js
var require_destroy = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/destroy.js"(exports2, module2) {
    "use strict";
    var process2 = require_process();
    var {
      aggregateTwoErrors,
      codes: { ERR_MULTIPLE_CALLBACK },
      AbortError
    } = require_errors();
    var { Symbol: Symbol2 } = require_primordials();
    var { kIsDestroyed, isDestroyed, isFinished, isServerRequest } = require_utils5();
    var kDestroy = Symbol2("kDestroy");
    var kConstruct = Symbol2("kConstruct");
    function checkError(err, w, r) {
      if (err) {
        err.stack;
        if (w && !w.errored) {
          w.errored = err;
        }
        if (r && !r.errored) {
          r.errored = err;
        }
      }
    }
    function destroy(err, cb) {
      const r = this._readableState;
      const w = this._writableState;
      const s = w || r;
      if (w !== null && w !== void 0 && w.destroyed || r !== null && r !== void 0 && r.destroyed) {
        if (typeof cb === "function") {
          cb();
        }
        return this;
      }
      checkError(err, w, r);
      if (w) {
        w.destroyed = true;
      }
      if (r) {
        r.destroyed = true;
      }
      if (!s.constructed) {
        this.once(kDestroy, function(er) {
          _destroy(this, aggregateTwoErrors(er, err), cb);
        });
      } else {
        _destroy(this, err, cb);
      }
      return this;
    }
    function _destroy(self2, err, cb) {
      let called = false;
      function onDestroy(err2) {
        if (called) {
          return;
        }
        called = true;
        const r = self2._readableState;
        const w = self2._writableState;
        checkError(err2, w, r);
        if (w) {
          w.closed = true;
        }
        if (r) {
          r.closed = true;
        }
        if (typeof cb === "function") {
          cb(err2);
        }
        if (err2) {
          process2.nextTick(emitErrorCloseNT, self2, err2);
        } else {
          process2.nextTick(emitCloseNT, self2);
        }
      }
      try {
        self2._destroy(err || null, onDestroy);
      } catch (err2) {
        onDestroy(err2);
      }
    }
    function emitErrorCloseNT(self2, err) {
      emitErrorNT(self2, err);
      emitCloseNT(self2);
    }
    function emitCloseNT(self2) {
      const r = self2._readableState;
      const w = self2._writableState;
      if (w) {
        w.closeEmitted = true;
      }
      if (r) {
        r.closeEmitted = true;
      }
      if (w !== null && w !== void 0 && w.emitClose || r !== null && r !== void 0 && r.emitClose) {
        self2.emit("close");
      }
    }
    function emitErrorNT(self2, err) {
      const r = self2._readableState;
      const w = self2._writableState;
      if (w !== null && w !== void 0 && w.errorEmitted || r !== null && r !== void 0 && r.errorEmitted) {
        return;
      }
      if (w) {
        w.errorEmitted = true;
      }
      if (r) {
        r.errorEmitted = true;
      }
      self2.emit("error", err);
    }
    function undestroy() {
      const r = this._readableState;
      const w = this._writableState;
      if (r) {
        r.constructed = true;
        r.closed = false;
        r.closeEmitted = false;
        r.destroyed = false;
        r.errored = null;
        r.errorEmitted = false;
        r.reading = false;
        r.ended = r.readable === false;
        r.endEmitted = r.readable === false;
      }
      if (w) {
        w.constructed = true;
        w.destroyed = false;
        w.closed = false;
        w.closeEmitted = false;
        w.errored = null;
        w.errorEmitted = false;
        w.finalCalled = false;
        w.prefinished = false;
        w.ended = w.writable === false;
        w.ending = w.writable === false;
        w.finished = w.writable === false;
      }
    }
    function errorOrDestroy(stream, err, sync) {
      const r = stream._readableState;
      const w = stream._writableState;
      if (w !== null && w !== void 0 && w.destroyed || r !== null && r !== void 0 && r.destroyed) {
        return this;
      }
      if (r !== null && r !== void 0 && r.autoDestroy || w !== null && w !== void 0 && w.autoDestroy)
        stream.destroy(err);
      else if (err) {
        err.stack;
        if (w && !w.errored) {
          w.errored = err;
        }
        if (r && !r.errored) {
          r.errored = err;
        }
        if (sync) {
          process2.nextTick(emitErrorNT, stream, err);
        } else {
          emitErrorNT(stream, err);
        }
      }
    }
    function construct(stream, cb) {
      if (typeof stream._construct !== "function") {
        return;
      }
      const r = stream._readableState;
      const w = stream._writableState;
      if (r) {
        r.constructed = false;
      }
      if (w) {
        w.constructed = false;
      }
      stream.once(kConstruct, cb);
      if (stream.listenerCount(kConstruct) > 1) {
        return;
      }
      process2.nextTick(constructNT, stream);
    }
    function constructNT(stream) {
      let called = false;
      function onConstruct(err) {
        if (called) {
          errorOrDestroy(stream, err !== null && err !== void 0 ? err : new ERR_MULTIPLE_CALLBACK());
          return;
        }
        called = true;
        const r = stream._readableState;
        const w = stream._writableState;
        const s = w || r;
        if (r) {
          r.constructed = true;
        }
        if (w) {
          w.constructed = true;
        }
        if (s.destroyed) {
          stream.emit(kDestroy, err);
        } else if (err) {
          errorOrDestroy(stream, err, true);
        } else {
          process2.nextTick(emitConstructNT, stream);
        }
      }
      try {
        stream._construct((err) => {
          process2.nextTick(onConstruct, err);
        });
      } catch (err) {
        process2.nextTick(onConstruct, err);
      }
    }
    function emitConstructNT(stream) {
      stream.emit(kConstruct);
    }
    function isRequest(stream) {
      return (stream === null || stream === void 0 ? void 0 : stream.setHeader) && typeof stream.abort === "function";
    }
    function emitCloseLegacy(stream) {
      stream.emit("close");
    }
    function emitErrorCloseLegacy(stream, err) {
      stream.emit("error", err);
      process2.nextTick(emitCloseLegacy, stream);
    }
    function destroyer(stream, err) {
      if (!stream || isDestroyed(stream)) {
        return;
      }
      if (!err && !isFinished(stream)) {
        err = new AbortError();
      }
      if (isServerRequest(stream)) {
        stream.socket = null;
        stream.destroy(err);
      } else if (isRequest(stream)) {
        stream.abort();
      } else if (isRequest(stream.req)) {
        stream.req.abort();
      } else if (typeof stream.destroy === "function") {
        stream.destroy(err);
      } else if (typeof stream.close === "function") {
        stream.close();
      } else if (err) {
        process2.nextTick(emitErrorCloseLegacy, stream, err);
      } else {
        process2.nextTick(emitCloseLegacy, stream);
      }
      if (!stream.destroyed) {
        stream[kIsDestroyed] = true;
      }
    }
    module2.exports = {
      construct,
      destroyer,
      destroy,
      undestroy,
      errorOrDestroy
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/legacy.js
var require_legacy = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/legacy.js"(exports2, module2) {
    "use strict";
    var { ArrayIsArray, ObjectSetPrototypeOf } = require_primordials();
    var { EventEmitter: EE } = require("events");
    function Stream(opts) {
      EE.call(this, opts);
    }
    ObjectSetPrototypeOf(Stream.prototype, EE.prototype);
    ObjectSetPrototypeOf(Stream, EE);
    Stream.prototype.pipe = function(dest, options) {
      const source = this;
      function ondata(chunk) {
        if (dest.writable && dest.write(chunk) === false && source.pause) {
          source.pause();
        }
      }
      source.on("data", ondata);
      function ondrain() {
        if (source.readable && source.resume) {
          source.resume();
        }
      }
      dest.on("drain", ondrain);
      if (!dest._isStdio && (!options || options.end !== false)) {
        source.on("end", onend);
        source.on("close", onclose);
      }
      let didOnEnd = false;
      function onend() {
        if (didOnEnd)
          return;
        didOnEnd = true;
        dest.end();
      }
      function onclose() {
        if (didOnEnd)
          return;
        didOnEnd = true;
        if (typeof dest.destroy === "function")
          dest.destroy();
      }
      function onerror(er) {
        cleanup();
        if (EE.listenerCount(this, "error") === 0) {
          this.emit("error", er);
        }
      }
      prependListener(source, "error", onerror);
      prependListener(dest, "error", onerror);
      function cleanup() {
        source.removeListener("data", ondata);
        dest.removeListener("drain", ondrain);
        source.removeListener("end", onend);
        source.removeListener("close", onclose);
        source.removeListener("error", onerror);
        dest.removeListener("error", onerror);
        source.removeListener("end", cleanup);
        source.removeListener("close", cleanup);
        dest.removeListener("close", cleanup);
      }
      source.on("end", cleanup);
      source.on("close", cleanup);
      dest.on("close", cleanup);
      dest.emit("pipe", source);
      return dest;
    };
    function prependListener(emitter, event, fn) {
      if (typeof emitter.prependListener === "function")
        return emitter.prependListener(event, fn);
      if (!emitter._events || !emitter._events[event])
        emitter.on(event, fn);
      else if (ArrayIsArray(emitter._events[event]))
        emitter._events[event].unshift(fn);
      else
        emitter._events[event] = [fn, emitter._events[event]];
    }
    module2.exports = {
      Stream,
      prependListener
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/add-abort-signal.js
var require_add_abort_signal = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/add-abort-signal.js"(exports2, module2) {
    "use strict";
    var { SymbolDispose } = require_primordials();
    var { AbortError, codes } = require_errors();
    var { isNodeStream, isWebStream, kControllerErrorFunction } = require_utils5();
    var eos = require_end_of_stream();
    var { ERR_INVALID_ARG_TYPE } = codes;
    var addAbortListener;
    var validateAbortSignal = (signal, name) => {
      if (typeof signal !== "object" || !("aborted" in signal)) {
        throw new ERR_INVALID_ARG_TYPE(name, "AbortSignal", signal);
      }
    };
    module2.exports.addAbortSignal = function addAbortSignal(signal, stream) {
      validateAbortSignal(signal, "signal");
      if (!isNodeStream(stream) && !isWebStream(stream)) {
        throw new ERR_INVALID_ARG_TYPE("stream", ["ReadableStream", "WritableStream", "Stream"], stream);
      }
      return module2.exports.addAbortSignalNoValidate(signal, stream);
    };
    module2.exports.addAbortSignalNoValidate = function(signal, stream) {
      if (typeof signal !== "object" || !("aborted" in signal)) {
        return stream;
      }
      const onAbort = isNodeStream(stream) ? () => {
        stream.destroy(
          new AbortError(void 0, {
            cause: signal.reason
          })
        );
      } : () => {
        stream[kControllerErrorFunction](
          new AbortError(void 0, {
            cause: signal.reason
          })
        );
      };
      if (signal.aborted) {
        onAbort();
      } else {
        addAbortListener = addAbortListener || require_util2().addAbortListener;
        const disposable = addAbortListener(signal, onAbort);
        eos(stream, disposable[SymbolDispose]);
      }
      return stream;
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/buffer_list.js
var require_buffer_list = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/buffer_list.js"(exports2, module2) {
    "use strict";
    var { StringPrototypeSlice, SymbolIterator, TypedArrayPrototypeSet, Uint8Array: Uint8Array2 } = require_primordials();
    var { Buffer: Buffer2 } = require("buffer");
    var { inspect } = require_util2();
    module2.exports = class BufferList {
      constructor() {
        this.head = null;
        this.tail = null;
        this.length = 0;
      }
      push(v) {
        const entry = {
          data: v,
          next: null
        };
        if (this.length > 0)
          this.tail.next = entry;
        else
          this.head = entry;
        this.tail = entry;
        ++this.length;
      }
      unshift(v) {
        const entry = {
          data: v,
          next: this.head
        };
        if (this.length === 0)
          this.tail = entry;
        this.head = entry;
        ++this.length;
      }
      shift() {
        if (this.length === 0)
          return;
        const ret = this.head.data;
        if (this.length === 1)
          this.head = this.tail = null;
        else
          this.head = this.head.next;
        --this.length;
        return ret;
      }
      clear() {
        this.head = this.tail = null;
        this.length = 0;
      }
      join(s) {
        if (this.length === 0)
          return "";
        let p = this.head;
        let ret = "" + p.data;
        while ((p = p.next) !== null)
          ret += s + p.data;
        return ret;
      }
      concat(n) {
        if (this.length === 0)
          return Buffer2.alloc(0);
        const ret = Buffer2.allocUnsafe(n >>> 0);
        let p = this.head;
        let i = 0;
        while (p) {
          TypedArrayPrototypeSet(ret, p.data, i);
          i += p.data.length;
          p = p.next;
        }
        return ret;
      }
      // Consumes a specified amount of bytes or characters from the buffered data.
      consume(n, hasStrings) {
        const data = this.head.data;
        if (n < data.length) {
          const slice = data.slice(0, n);
          this.head.data = data.slice(n);
          return slice;
        }
        if (n === data.length) {
          return this.shift();
        }
        return hasStrings ? this._getString(n) : this._getBuffer(n);
      }
      first() {
        return this.head.data;
      }
      *[SymbolIterator]() {
        for (let p = this.head; p; p = p.next) {
          yield p.data;
        }
      }
      // Consumes a specified amount of characters from the buffered data.
      _getString(n) {
        let ret = "";
        let p = this.head;
        let c = 0;
        do {
          const str = p.data;
          if (n > str.length) {
            ret += str;
            n -= str.length;
          } else {
            if (n === str.length) {
              ret += str;
              ++c;
              if (p.next)
                this.head = p.next;
              else
                this.head = this.tail = null;
            } else {
              ret += StringPrototypeSlice(str, 0, n);
              this.head = p;
              p.data = StringPrototypeSlice(str, n);
            }
            break;
          }
          ++c;
        } while ((p = p.next) !== null);
        this.length -= c;
        return ret;
      }
      // Consumes a specified amount of bytes from the buffered data.
      _getBuffer(n) {
        const ret = Buffer2.allocUnsafe(n);
        const retLen = n;
        let p = this.head;
        let c = 0;
        do {
          const buf = p.data;
          if (n > buf.length) {
            TypedArrayPrototypeSet(ret, buf, retLen - n);
            n -= buf.length;
          } else {
            if (n === buf.length) {
              TypedArrayPrototypeSet(ret, buf, retLen - n);
              ++c;
              if (p.next)
                this.head = p.next;
              else
                this.head = this.tail = null;
            } else {
              TypedArrayPrototypeSet(ret, new Uint8Array2(buf.buffer, buf.byteOffset, n), retLen - n);
              this.head = p;
              p.data = buf.slice(n);
            }
            break;
          }
          ++c;
        } while ((p = p.next) !== null);
        this.length -= c;
        return ret;
      }
      // Make sure the linked list only shows the minimal necessary information.
      [Symbol.for("nodejs.util.inspect.custom")](_, options) {
        return inspect(this, {
          ...options,
          // Only inspect one level.
          depth: 0,
          // It should not recurse.
          customInspect: false
        });
      }
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/state.js
var require_state = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/state.js"(exports2, module2) {
    "use strict";
    var { MathFloor, NumberIsInteger } = require_primordials();
    var { validateInteger } = require_validators();
    var { ERR_INVALID_ARG_VALUE } = require_errors().codes;
    var defaultHighWaterMarkBytes = 16 * 1024;
    var defaultHighWaterMarkObjectMode = 16;
    function highWaterMarkFrom(options, isDuplex, duplexKey) {
      return options.highWaterMark != null ? options.highWaterMark : isDuplex ? options[duplexKey] : null;
    }
    function getDefaultHighWaterMark(objectMode) {
      return objectMode ? defaultHighWaterMarkObjectMode : defaultHighWaterMarkBytes;
    }
    function setDefaultHighWaterMark(objectMode, value) {
      validateInteger(value, "value", 0);
      if (objectMode) {
        defaultHighWaterMarkObjectMode = value;
      } else {
        defaultHighWaterMarkBytes = value;
      }
    }
    function getHighWaterMark(state, options, duplexKey, isDuplex) {
      const hwm = highWaterMarkFrom(options, isDuplex, duplexKey);
      if (hwm != null) {
        if (!NumberIsInteger(hwm) || hwm < 0) {
          const name = isDuplex ? `options.${duplexKey}` : "options.highWaterMark";
          throw new ERR_INVALID_ARG_VALUE(name, hwm);
        }
        return MathFloor(hwm);
      }
      return getDefaultHighWaterMark(state.objectMode);
    }
    module2.exports = {
      getHighWaterMark,
      getDefaultHighWaterMark,
      setDefaultHighWaterMark
    };
  }
});

// ../node_modules/.pnpm/safe-buffer@5.2.1/node_modules/safe-buffer/index.js
var require_safe_buffer = __commonJS({
  "../node_modules/.pnpm/safe-buffer@5.2.1/node_modules/safe-buffer/index.js"(exports2, module2) {
    var buffer = require("buffer");
    var Buffer2 = buffer.Buffer;
    function copyProps(src, dst) {
      for (var key in src) {
        dst[key] = src[key];
      }
    }
    if (Buffer2.from && Buffer2.alloc && Buffer2.allocUnsafe && Buffer2.allocUnsafeSlow) {
      module2.exports = buffer;
    } else {
      copyProps(buffer, exports2);
      exports2.Buffer = SafeBuffer;
    }
    function SafeBuffer(arg, encodingOrOffset, length) {
      return Buffer2(arg, encodingOrOffset, length);
    }
    SafeBuffer.prototype = Object.create(Buffer2.prototype);
    copyProps(Buffer2, SafeBuffer);
    SafeBuffer.from = function(arg, encodingOrOffset, length) {
      if (typeof arg === "number") {
        throw new TypeError("Argument must not be a number");
      }
      return Buffer2(arg, encodingOrOffset, length);
    };
    SafeBuffer.alloc = function(size, fill, encoding) {
      if (typeof size !== "number") {
        throw new TypeError("Argument must be a number");
      }
      var buf = Buffer2(size);
      if (fill !== void 0) {
        if (typeof encoding === "string") {
          buf.fill(fill, encoding);
        } else {
          buf.fill(fill);
        }
      } else {
        buf.fill(0);
      }
      return buf;
    };
    SafeBuffer.allocUnsafe = function(size) {
      if (typeof size !== "number") {
        throw new TypeError("Argument must be a number");
      }
      return Buffer2(size);
    };
    SafeBuffer.allocUnsafeSlow = function(size) {
      if (typeof size !== "number") {
        throw new TypeError("Argument must be a number");
      }
      return buffer.SlowBuffer(size);
    };
  }
});

// ../node_modules/.pnpm/string_decoder@1.3.0/node_modules/string_decoder/lib/string_decoder.js
var require_string_decoder = __commonJS({
  "../node_modules/.pnpm/string_decoder@1.3.0/node_modules/string_decoder/lib/string_decoder.js"(exports2) {
    "use strict";
    var Buffer2 = require_safe_buffer().Buffer;
    var isEncoding = Buffer2.isEncoding || function(encoding) {
      encoding = "" + encoding;
      switch (encoding && encoding.toLowerCase()) {
        case "hex":
        case "utf8":
        case "utf-8":
        case "ascii":
        case "binary":
        case "base64":
        case "ucs2":
        case "ucs-2":
        case "utf16le":
        case "utf-16le":
        case "raw":
          return true;
        default:
          return false;
      }
    };
    function _normalizeEncoding(enc) {
      if (!enc)
        return "utf8";
      var retried;
      while (true) {
        switch (enc) {
          case "utf8":
          case "utf-8":
            return "utf8";
          case "ucs2":
          case "ucs-2":
          case "utf16le":
          case "utf-16le":
            return "utf16le";
          case "latin1":
          case "binary":
            return "latin1";
          case "base64":
          case "ascii":
          case "hex":
            return enc;
          default:
            if (retried)
              return;
            enc = ("" + enc).toLowerCase();
            retried = true;
        }
      }
    }
    function normalizeEncoding(enc) {
      var nenc = _normalizeEncoding(enc);
      if (typeof nenc !== "string" && (Buffer2.isEncoding === isEncoding || !isEncoding(enc)))
        throw new Error("Unknown encoding: " + enc);
      return nenc || enc;
    }
    exports2.StringDecoder = StringDecoder;
    function StringDecoder(encoding) {
      this.encoding = normalizeEncoding(encoding);
      var nb;
      switch (this.encoding) {
        case "utf16le":
          this.text = utf16Text;
          this.end = utf16End;
          nb = 4;
          break;
        case "utf8":
          this.fillLast = utf8FillLast;
          nb = 4;
          break;
        case "base64":
          this.text = base64Text;
          this.end = base64End;
          nb = 3;
          break;
        default:
          this.write = simpleWrite;
          this.end = simpleEnd;
          return;
      }
      this.lastNeed = 0;
      this.lastTotal = 0;
      this.lastChar = Buffer2.allocUnsafe(nb);
    }
    StringDecoder.prototype.write = function(buf) {
      if (buf.length === 0)
        return "";
      var r;
      var i;
      if (this.lastNeed) {
        r = this.fillLast(buf);
        if (r === void 0)
          return "";
        i = this.lastNeed;
        this.lastNeed = 0;
      } else {
        i = 0;
      }
      if (i < buf.length)
        return r ? r + this.text(buf, i) : this.text(buf, i);
      return r || "";
    };
    StringDecoder.prototype.end = utf8End;
    StringDecoder.prototype.text = utf8Text;
    StringDecoder.prototype.fillLast = function(buf) {
      if (this.lastNeed <= buf.length) {
        buf.copy(this.lastChar, this.lastTotal - this.lastNeed, 0, this.lastNeed);
        return this.lastChar.toString(this.encoding, 0, this.lastTotal);
      }
      buf.copy(this.lastChar, this.lastTotal - this.lastNeed, 0, buf.length);
      this.lastNeed -= buf.length;
    };
    function utf8CheckByte(byte) {
      if (byte <= 127)
        return 0;
      else if (byte >> 5 === 6)
        return 2;
      else if (byte >> 4 === 14)
        return 3;
      else if (byte >> 3 === 30)
        return 4;
      return byte >> 6 === 2 ? -1 : -2;
    }
    function utf8CheckIncomplete(self2, buf, i) {
      var j = buf.length - 1;
      if (j < i)
        return 0;
      var nb = utf8CheckByte(buf[j]);
      if (nb >= 0) {
        if (nb > 0)
          self2.lastNeed = nb - 1;
        return nb;
      }
      if (--j < i || nb === -2)
        return 0;
      nb = utf8CheckByte(buf[j]);
      if (nb >= 0) {
        if (nb > 0)
          self2.lastNeed = nb - 2;
        return nb;
      }
      if (--j < i || nb === -2)
        return 0;
      nb = utf8CheckByte(buf[j]);
      if (nb >= 0) {
        if (nb > 0) {
          if (nb === 2)
            nb = 0;
          else
            self2.lastNeed = nb - 3;
        }
        return nb;
      }
      return 0;
    }
    function utf8CheckExtraBytes(self2, buf, p) {
      if ((buf[0] & 192) !== 128) {
        self2.lastNeed = 0;
        return "\uFFFD";
      }
      if (self2.lastNeed > 1 && buf.length > 1) {
        if ((buf[1] & 192) !== 128) {
          self2.lastNeed = 1;
          return "\uFFFD";
        }
        if (self2.lastNeed > 2 && buf.length > 2) {
          if ((buf[2] & 192) !== 128) {
            self2.lastNeed = 2;
            return "\uFFFD";
          }
        }
      }
    }
    function utf8FillLast(buf) {
      var p = this.lastTotal - this.lastNeed;
      var r = utf8CheckExtraBytes(this, buf, p);
      if (r !== void 0)
        return r;
      if (this.lastNeed <= buf.length) {
        buf.copy(this.lastChar, p, 0, this.lastNeed);
        return this.lastChar.toString(this.encoding, 0, this.lastTotal);
      }
      buf.copy(this.lastChar, p, 0, buf.length);
      this.lastNeed -= buf.length;
    }
    function utf8Text(buf, i) {
      var total = utf8CheckIncomplete(this, buf, i);
      if (!this.lastNeed)
        return buf.toString("utf8", i);
      this.lastTotal = total;
      var end = buf.length - (total - this.lastNeed);
      buf.copy(this.lastChar, 0, end);
      return buf.toString("utf8", i, end);
    }
    function utf8End(buf) {
      var r = buf && buf.length ? this.write(buf) : "";
      if (this.lastNeed)
        return r + "\uFFFD";
      return r;
    }
    function utf16Text(buf, i) {
      if ((buf.length - i) % 2 === 0) {
        var r = buf.toString("utf16le", i);
        if (r) {
          var c = r.charCodeAt(r.length - 1);
          if (c >= 55296 && c <= 56319) {
            this.lastNeed = 2;
            this.lastTotal = 4;
            this.lastChar[0] = buf[buf.length - 2];
            this.lastChar[1] = buf[buf.length - 1];
            return r.slice(0, -1);
          }
        }
        return r;
      }
      this.lastNeed = 1;
      this.lastTotal = 2;
      this.lastChar[0] = buf[buf.length - 1];
      return buf.toString("utf16le", i, buf.length - 1);
    }
    function utf16End(buf) {
      var r = buf && buf.length ? this.write(buf) : "";
      if (this.lastNeed) {
        var end = this.lastTotal - this.lastNeed;
        return r + this.lastChar.toString("utf16le", 0, end);
      }
      return r;
    }
    function base64Text(buf, i) {
      var n = (buf.length - i) % 3;
      if (n === 0)
        return buf.toString("base64", i);
      this.lastNeed = 3 - n;
      this.lastTotal = 3;
      if (n === 1) {
        this.lastChar[0] = buf[buf.length - 1];
      } else {
        this.lastChar[0] = buf[buf.length - 2];
        this.lastChar[1] = buf[buf.length - 1];
      }
      return buf.toString("base64", i, buf.length - n);
    }
    function base64End(buf) {
      var r = buf && buf.length ? this.write(buf) : "";
      if (this.lastNeed)
        return r + this.lastChar.toString("base64", 0, 3 - this.lastNeed);
      return r;
    }
    function simpleWrite(buf) {
      return buf.toString(this.encoding);
    }
    function simpleEnd(buf) {
      return buf && buf.length ? this.write(buf) : "";
    }
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/from.js
var require_from = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/from.js"(exports2, module2) {
    "use strict";
    var process2 = require_process();
    var { PromisePrototypeThen, SymbolAsyncIterator, SymbolIterator } = require_primordials();
    var { Buffer: Buffer2 } = require("buffer");
    var { ERR_INVALID_ARG_TYPE, ERR_STREAM_NULL_VALUES } = require_errors().codes;
    function from(Readable, iterable, opts) {
      let iterator;
      if (typeof iterable === "string" || iterable instanceof Buffer2) {
        return new Readable({
          objectMode: true,
          ...opts,
          read() {
            this.push(iterable);
            this.push(null);
          }
        });
      }
      let isAsync;
      if (iterable && iterable[SymbolAsyncIterator]) {
        isAsync = true;
        iterator = iterable[SymbolAsyncIterator]();
      } else if (iterable && iterable[SymbolIterator]) {
        isAsync = false;
        iterator = iterable[SymbolIterator]();
      } else {
        throw new ERR_INVALID_ARG_TYPE("iterable", ["Iterable"], iterable);
      }
      const readable = new Readable({
        objectMode: true,
        highWaterMark: 1,
        // TODO(ronag): What options should be allowed?
        ...opts
      });
      let reading = false;
      readable._read = function() {
        if (!reading) {
          reading = true;
          next();
        }
      };
      readable._destroy = function(error, cb) {
        PromisePrototypeThen(
          close(error),
          () => process2.nextTick(cb, error),
          // nextTick is here in case cb throws
          (e) => process2.nextTick(cb, e || error)
        );
      };
      async function close(error) {
        const hadError = error !== void 0 && error !== null;
        const hasThrow = typeof iterator.throw === "function";
        if (hadError && hasThrow) {
          const { value, done } = await iterator.throw(error);
          await value;
          if (done) {
            return;
          }
        }
        if (typeof iterator.return === "function") {
          const { value } = await iterator.return();
          await value;
        }
      }
      async function next() {
        for (; ; ) {
          try {
            const { value, done } = isAsync ? await iterator.next() : iterator.next();
            if (done) {
              readable.push(null);
            } else {
              const res = value && typeof value.then === "function" ? await value : value;
              if (res === null) {
                reading = false;
                throw new ERR_STREAM_NULL_VALUES();
              } else if (readable.push(res)) {
                continue;
              } else {
                reading = false;
              }
            }
          } catch (err) {
            readable.destroy(err);
          }
          break;
        }
      }
      return readable;
    }
    module2.exports = from;
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/readable.js
var require_readable = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/readable.js"(exports2, module2) {
    "use strict";
    var process2 = require_process();
    var {
      ArrayPrototypeIndexOf,
      NumberIsInteger,
      NumberIsNaN,
      NumberParseInt,
      ObjectDefineProperties,
      ObjectKeys,
      ObjectSetPrototypeOf,
      Promise: Promise2,
      SafeSet,
      SymbolAsyncDispose,
      SymbolAsyncIterator,
      Symbol: Symbol2
    } = require_primordials();
    module2.exports = Readable;
    Readable.ReadableState = ReadableState;
    var { EventEmitter: EE } = require("events");
    var { Stream, prependListener } = require_legacy();
    var { Buffer: Buffer2 } = require("buffer");
    var { addAbortSignal } = require_add_abort_signal();
    var eos = require_end_of_stream();
    var debug = require_util2().debuglog("stream", (fn) => {
      debug = fn;
    });
    var BufferList = require_buffer_list();
    var destroyImpl = require_destroy();
    var { getHighWaterMark, getDefaultHighWaterMark } = require_state();
    var {
      aggregateTwoErrors,
      codes: {
        ERR_INVALID_ARG_TYPE,
        ERR_METHOD_NOT_IMPLEMENTED,
        ERR_OUT_OF_RANGE,
        ERR_STREAM_PUSH_AFTER_EOF,
        ERR_STREAM_UNSHIFT_AFTER_END_EVENT
      },
      AbortError
    } = require_errors();
    var { validateObject } = require_validators();
    var kPaused = Symbol2("kPaused");
    var { StringDecoder } = require_string_decoder();
    var from = require_from();
    ObjectSetPrototypeOf(Readable.prototype, Stream.prototype);
    ObjectSetPrototypeOf(Readable, Stream);
    var nop = () => {
    };
    var { errorOrDestroy } = destroyImpl;
    var kObjectMode = 1 << 0;
    var kEnded = 1 << 1;
    var kEndEmitted = 1 << 2;
    var kReading = 1 << 3;
    var kConstructed = 1 << 4;
    var kSync = 1 << 5;
    var kNeedReadable = 1 << 6;
    var kEmittedReadable = 1 << 7;
    var kReadableListening = 1 << 8;
    var kResumeScheduled = 1 << 9;
    var kErrorEmitted = 1 << 10;
    var kEmitClose = 1 << 11;
    var kAutoDestroy = 1 << 12;
    var kDestroyed = 1 << 13;
    var kClosed = 1 << 14;
    var kCloseEmitted = 1 << 15;
    var kMultiAwaitDrain = 1 << 16;
    var kReadingMore = 1 << 17;
    var kDataEmitted = 1 << 18;
    function makeBitMapDescriptor(bit) {
      return {
        enumerable: false,
        get() {
          return (this.state & bit) !== 0;
        },
        set(value) {
          if (value)
            this.state |= bit;
          else
            this.state &= ~bit;
        }
      };
    }
    ObjectDefineProperties(ReadableState.prototype, {
      objectMode: makeBitMapDescriptor(kObjectMode),
      ended: makeBitMapDescriptor(kEnded),
      endEmitted: makeBitMapDescriptor(kEndEmitted),
      reading: makeBitMapDescriptor(kReading),
      // Stream is still being constructed and cannot be
      // destroyed until construction finished or failed.
      // Async construction is opt in, therefore we start as
      // constructed.
      constructed: makeBitMapDescriptor(kConstructed),
      // A flag to be able to tell if the event 'readable'/'data' is emitted
      // immediately, or on a later tick.  We set this to true at first, because
      // any actions that shouldn't happen until "later" should generally also
      // not happen before the first read call.
      sync: makeBitMapDescriptor(kSync),
      // Whenever we return null, then we set a flag to say
      // that we're awaiting a 'readable' event emission.
      needReadable: makeBitMapDescriptor(kNeedReadable),
      emittedReadable: makeBitMapDescriptor(kEmittedReadable),
      readableListening: makeBitMapDescriptor(kReadableListening),
      resumeScheduled: makeBitMapDescriptor(kResumeScheduled),
      // True if the error was already emitted and should not be thrown again.
      errorEmitted: makeBitMapDescriptor(kErrorEmitted),
      emitClose: makeBitMapDescriptor(kEmitClose),
      autoDestroy: makeBitMapDescriptor(kAutoDestroy),
      // Has it been destroyed.
      destroyed: makeBitMapDescriptor(kDestroyed),
      // Indicates whether the stream has finished destroying.
      closed: makeBitMapDescriptor(kClosed),
      // True if close has been emitted or would have been emitted
      // depending on emitClose.
      closeEmitted: makeBitMapDescriptor(kCloseEmitted),
      multiAwaitDrain: makeBitMapDescriptor(kMultiAwaitDrain),
      // If true, a maybeReadMore has been scheduled.
      readingMore: makeBitMapDescriptor(kReadingMore),
      dataEmitted: makeBitMapDescriptor(kDataEmitted)
    });
    function ReadableState(options, stream, isDuplex) {
      if (typeof isDuplex !== "boolean")
        isDuplex = stream instanceof require_duplex();
      this.state = kEmitClose | kAutoDestroy | kConstructed | kSync;
      if (options && options.objectMode)
        this.state |= kObjectMode;
      if (isDuplex && options && options.readableObjectMode)
        this.state |= kObjectMode;
      this.highWaterMark = options ? getHighWaterMark(this, options, "readableHighWaterMark", isDuplex) : getDefaultHighWaterMark(false);
      this.buffer = new BufferList();
      this.length = 0;
      this.pipes = [];
      this.flowing = null;
      this[kPaused] = null;
      if (options && options.emitClose === false)
        this.state &= ~kEmitClose;
      if (options && options.autoDestroy === false)
        this.state &= ~kAutoDestroy;
      this.errored = null;
      this.defaultEncoding = options && options.defaultEncoding || "utf8";
      this.awaitDrainWriters = null;
      this.decoder = null;
      this.encoding = null;
      if (options && options.encoding) {
        this.decoder = new StringDecoder(options.encoding);
        this.encoding = options.encoding;
      }
    }
    function Readable(options) {
      if (!(this instanceof Readable))
        return new Readable(options);
      const isDuplex = this instanceof require_duplex();
      this._readableState = new ReadableState(options, this, isDuplex);
      if (options) {
        if (typeof options.read === "function")
          this._read = options.read;
        if (typeof options.destroy === "function")
          this._destroy = options.destroy;
        if (typeof options.construct === "function")
          this._construct = options.construct;
        if (options.signal && !isDuplex)
          addAbortSignal(options.signal, this);
      }
      Stream.call(this, options);
      destroyImpl.construct(this, () => {
        if (this._readableState.needReadable) {
          maybeReadMore(this, this._readableState);
        }
      });
    }
    Readable.prototype.destroy = destroyImpl.destroy;
    Readable.prototype._undestroy = destroyImpl.undestroy;
    Readable.prototype._destroy = function(err, cb) {
      cb(err);
    };
    Readable.prototype[EE.captureRejectionSymbol] = function(err) {
      this.destroy(err);
    };
    Readable.prototype[SymbolAsyncDispose] = function() {
      let error;
      if (!this.destroyed) {
        error = this.readableEnded ? null : new AbortError();
        this.destroy(error);
      }
      return new Promise2((resolve, reject) => eos(this, (err) => err && err !== error ? reject(err) : resolve(null)));
    };
    Readable.prototype.push = function(chunk, encoding) {
      return readableAddChunk(this, chunk, encoding, false);
    };
    Readable.prototype.unshift = function(chunk, encoding) {
      return readableAddChunk(this, chunk, encoding, true);
    };
    function readableAddChunk(stream, chunk, encoding, addToFront) {
      debug("readableAddChunk", chunk);
      const state = stream._readableState;
      let err;
      if ((state.state & kObjectMode) === 0) {
        if (typeof chunk === "string") {
          encoding = encoding || state.defaultEncoding;
          if (state.encoding !== encoding) {
            if (addToFront && state.encoding) {
              chunk = Buffer2.from(chunk, encoding).toString(state.encoding);
            } else {
              chunk = Buffer2.from(chunk, encoding);
              encoding = "";
            }
          }
        } else if (chunk instanceof Buffer2) {
          encoding = "";
        } else if (Stream._isUint8Array(chunk)) {
          chunk = Stream._uint8ArrayToBuffer(chunk);
          encoding = "";
        } else if (chunk != null) {
          err = new ERR_INVALID_ARG_TYPE("chunk", ["string", "Buffer", "Uint8Array"], chunk);
        }
      }
      if (err) {
        errorOrDestroy(stream, err);
      } else if (chunk === null) {
        state.state &= ~kReading;
        onEofChunk(stream, state);
      } else if ((state.state & kObjectMode) !== 0 || chunk && chunk.length > 0) {
        if (addToFront) {
          if ((state.state & kEndEmitted) !== 0)
            errorOrDestroy(stream, new ERR_STREAM_UNSHIFT_AFTER_END_EVENT());
          else if (state.destroyed || state.errored)
            return false;
          else
            addChunk(stream, state, chunk, true);
        } else if (state.ended) {
          errorOrDestroy(stream, new ERR_STREAM_PUSH_AFTER_EOF());
        } else if (state.destroyed || state.errored) {
          return false;
        } else {
          state.state &= ~kReading;
          if (state.decoder && !encoding) {
            chunk = state.decoder.write(chunk);
            if (state.objectMode || chunk.length !== 0)
              addChunk(stream, state, chunk, false);
            else
              maybeReadMore(stream, state);
          } else {
            addChunk(stream, state, chunk, false);
          }
        }
      } else if (!addToFront) {
        state.state &= ~kReading;
        maybeReadMore(stream, state);
      }
      return !state.ended && (state.length < state.highWaterMark || state.length === 0);
    }
    function addChunk(stream, state, chunk, addToFront) {
      if (state.flowing && state.length === 0 && !state.sync && stream.listenerCount("data") > 0) {
        if ((state.state & kMultiAwaitDrain) !== 0) {
          state.awaitDrainWriters.clear();
        } else {
          state.awaitDrainWriters = null;
        }
        state.dataEmitted = true;
        stream.emit("data", chunk);
      } else {
        state.length += state.objectMode ? 1 : chunk.length;
        if (addToFront)
          state.buffer.unshift(chunk);
        else
          state.buffer.push(chunk);
        if ((state.state & kNeedReadable) !== 0)
          emitReadable(stream);
      }
      maybeReadMore(stream, state);
    }
    Readable.prototype.isPaused = function() {
      const state = this._readableState;
      return state[kPaused] === true || state.flowing === false;
    };
    Readable.prototype.setEncoding = function(enc) {
      const decoder = new StringDecoder(enc);
      this._readableState.decoder = decoder;
      this._readableState.encoding = this._readableState.decoder.encoding;
      const buffer = this._readableState.buffer;
      let content = "";
      for (const data of buffer) {
        content += decoder.write(data);
      }
      buffer.clear();
      if (content !== "")
        buffer.push(content);
      this._readableState.length = content.length;
      return this;
    };
    var MAX_HWM = 1073741824;
    function computeNewHighWaterMark(n) {
      if (n > MAX_HWM) {
        throw new ERR_OUT_OF_RANGE("size", "<= 1GiB", n);
      } else {
        n--;
        n |= n >>> 1;
        n |= n >>> 2;
        n |= n >>> 4;
        n |= n >>> 8;
        n |= n >>> 16;
        n++;
      }
      return n;
    }
    function howMuchToRead(n, state) {
      if (n <= 0 || state.length === 0 && state.ended)
        return 0;
      if ((state.state & kObjectMode) !== 0)
        return 1;
      if (NumberIsNaN(n)) {
        if (state.flowing && state.length)
          return state.buffer.first().length;
        return state.length;
      }
      if (n <= state.length)
        return n;
      return state.ended ? state.length : 0;
    }
    Readable.prototype.read = function(n) {
      debug("read", n);
      if (n === void 0) {
        n = NaN;
      } else if (!NumberIsInteger(n)) {
        n = NumberParseInt(n, 10);
      }
      const state = this._readableState;
      const nOrig = n;
      if (n > state.highWaterMark)
        state.highWaterMark = computeNewHighWaterMark(n);
      if (n !== 0)
        state.state &= ~kEmittedReadable;
      if (n === 0 && state.needReadable && ((state.highWaterMark !== 0 ? state.length >= state.highWaterMark : state.length > 0) || state.ended)) {
        debug("read: emitReadable", state.length, state.ended);
        if (state.length === 0 && state.ended)
          endReadable(this);
        else
          emitReadable(this);
        return null;
      }
      n = howMuchToRead(n, state);
      if (n === 0 && state.ended) {
        if (state.length === 0)
          endReadable(this);
        return null;
      }
      let doRead = (state.state & kNeedReadable) !== 0;
      debug("need readable", doRead);
      if (state.length === 0 || state.length - n < state.highWaterMark) {
        doRead = true;
        debug("length less than watermark", doRead);
      }
      if (state.ended || state.reading || state.destroyed || state.errored || !state.constructed) {
        doRead = false;
        debug("reading, ended or constructing", doRead);
      } else if (doRead) {
        debug("do read");
        state.state |= kReading | kSync;
        if (state.length === 0)
          state.state |= kNeedReadable;
        try {
          this._read(state.highWaterMark);
        } catch (err) {
          errorOrDestroy(this, err);
        }
        state.state &= ~kSync;
        if (!state.reading)
          n = howMuchToRead(nOrig, state);
      }
      let ret;
      if (n > 0)
        ret = fromList(n, state);
      else
        ret = null;
      if (ret === null) {
        state.needReadable = state.length <= state.highWaterMark;
        n = 0;
      } else {
        state.length -= n;
        if (state.multiAwaitDrain) {
          state.awaitDrainWriters.clear();
        } else {
          state.awaitDrainWriters = null;
        }
      }
      if (state.length === 0) {
        if (!state.ended)
          state.needReadable = true;
        if (nOrig !== n && state.ended)
          endReadable(this);
      }
      if (ret !== null && !state.errorEmitted && !state.closeEmitted) {
        state.dataEmitted = true;
        this.emit("data", ret);
      }
      return ret;
    };
    function onEofChunk(stream, state) {
      debug("onEofChunk");
      if (state.ended)
        return;
      if (state.decoder) {
        const chunk = state.decoder.end();
        if (chunk && chunk.length) {
          state.buffer.push(chunk);
          state.length += state.objectMode ? 1 : chunk.length;
        }
      }
      state.ended = true;
      if (state.sync) {
        emitReadable(stream);
      } else {
        state.needReadable = false;
        state.emittedReadable = true;
        emitReadable_(stream);
      }
    }
    function emitReadable(stream) {
      const state = stream._readableState;
      debug("emitReadable", state.needReadable, state.emittedReadable);
      state.needReadable = false;
      if (!state.emittedReadable) {
        debug("emitReadable", state.flowing);
        state.emittedReadable = true;
        process2.nextTick(emitReadable_, stream);
      }
    }
    function emitReadable_(stream) {
      const state = stream._readableState;
      debug("emitReadable_", state.destroyed, state.length, state.ended);
      if (!state.destroyed && !state.errored && (state.length || state.ended)) {
        stream.emit("readable");
        state.emittedReadable = false;
      }
      state.needReadable = !state.flowing && !state.ended && state.length <= state.highWaterMark;
      flow(stream);
    }
    function maybeReadMore(stream, state) {
      if (!state.readingMore && state.constructed) {
        state.readingMore = true;
        process2.nextTick(maybeReadMore_, stream, state);
      }
    }
    function maybeReadMore_(stream, state) {
      while (!state.reading && !state.ended && (state.length < state.highWaterMark || state.flowing && state.length === 0)) {
        const len = state.length;
        debug("maybeReadMore read 0");
        stream.read(0);
        if (len === state.length)
          break;
      }
      state.readingMore = false;
    }
    Readable.prototype._read = function(n) {
      throw new ERR_METHOD_NOT_IMPLEMENTED("_read()");
    };
    Readable.prototype.pipe = function(dest, pipeOpts) {
      const src = this;
      const state = this._readableState;
      if (state.pipes.length === 1) {
        if (!state.multiAwaitDrain) {
          state.multiAwaitDrain = true;
          state.awaitDrainWriters = new SafeSet(state.awaitDrainWriters ? [state.awaitDrainWriters] : []);
        }
      }
      state.pipes.push(dest);
      debug("pipe count=%d opts=%j", state.pipes.length, pipeOpts);
      const doEnd = (!pipeOpts || pipeOpts.end !== false) && dest !== process2.stdout && dest !== process2.stderr;
      const endFn = doEnd ? onend : unpipe;
      if (state.endEmitted)
        process2.nextTick(endFn);
      else
        src.once("end", endFn);
      dest.on("unpipe", onunpipe);
      function onunpipe(readable, unpipeInfo) {
        debug("onunpipe");
        if (readable === src) {
          if (unpipeInfo && unpipeInfo.hasUnpiped === false) {
            unpipeInfo.hasUnpiped = true;
            cleanup();
          }
        }
      }
      function onend() {
        debug("onend");
        dest.end();
      }
      let ondrain;
      let cleanedUp = false;
      function cleanup() {
        debug("cleanup");
        dest.removeListener("close", onclose);
        dest.removeListener("finish", onfinish);
        if (ondrain) {
          dest.removeListener("drain", ondrain);
        }
        dest.removeListener("error", onerror);
        dest.removeListener("unpipe", onunpipe);
        src.removeListener("end", onend);
        src.removeListener("end", unpipe);
        src.removeListener("data", ondata);
        cleanedUp = true;
        if (ondrain && state.awaitDrainWriters && (!dest._writableState || dest._writableState.needDrain))
          ondrain();
      }
      function pause() {
        if (!cleanedUp) {
          if (state.pipes.length === 1 && state.pipes[0] === dest) {
            debug("false write response, pause", 0);
            state.awaitDrainWriters = dest;
            state.multiAwaitDrain = false;
          } else if (state.pipes.length > 1 && state.pipes.includes(dest)) {
            debug("false write response, pause", state.awaitDrainWriters.size);
            state.awaitDrainWriters.add(dest);
          }
          src.pause();
        }
        if (!ondrain) {
          ondrain = pipeOnDrain(src, dest);
          dest.on("drain", ondrain);
        }
      }
      src.on("data", ondata);
      function ondata(chunk) {
        debug("ondata");
        const ret = dest.write(chunk);
        debug("dest.write", ret);
        if (ret === false) {
          pause();
        }
      }
      function onerror(er) {
        debug("onerror", er);
        unpipe();
        dest.removeListener("error", onerror);
        if (dest.listenerCount("error") === 0) {
          const s = dest._writableState || dest._readableState;
          if (s && !s.errorEmitted) {
            errorOrDestroy(dest, er);
          } else {
            dest.emit("error", er);
          }
        }
      }
      prependListener(dest, "error", onerror);
      function onclose() {
        dest.removeListener("finish", onfinish);
        unpipe();
      }
      dest.once("close", onclose);
      function onfinish() {
        debug("onfinish");
        dest.removeListener("close", onclose);
        unpipe();
      }
      dest.once("finish", onfinish);
      function unpipe() {
        debug("unpipe");
        src.unpipe(dest);
      }
      dest.emit("pipe", src);
      if (dest.writableNeedDrain === true) {
        pause();
      } else if (!state.flowing) {
        debug("pipe resume");
        src.resume();
      }
      return dest;
    };
    function pipeOnDrain(src, dest) {
      return function pipeOnDrainFunctionResult() {
        const state = src._readableState;
        if (state.awaitDrainWriters === dest) {
          debug("pipeOnDrain", 1);
          state.awaitDrainWriters = null;
        } else if (state.multiAwaitDrain) {
          debug("pipeOnDrain", state.awaitDrainWriters.size);
          state.awaitDrainWriters.delete(dest);
        }
        if ((!state.awaitDrainWriters || state.awaitDrainWriters.size === 0) && src.listenerCount("data")) {
          src.resume();
        }
      };
    }
    Readable.prototype.unpipe = function(dest) {
      const state = this._readableState;
      const unpipeInfo = {
        hasUnpiped: false
      };
      if (state.pipes.length === 0)
        return this;
      if (!dest) {
        const dests = state.pipes;
        state.pipes = [];
        this.pause();
        for (let i = 0; i < dests.length; i++)
          dests[i].emit("unpipe", this, {
            hasUnpiped: false
          });
        return this;
      }
      const index = ArrayPrototypeIndexOf(state.pipes, dest);
      if (index === -1)
        return this;
      state.pipes.splice(index, 1);
      if (state.pipes.length === 0)
        this.pause();
      dest.emit("unpipe", this, unpipeInfo);
      return this;
    };
    Readable.prototype.on = function(ev, fn) {
      const res = Stream.prototype.on.call(this, ev, fn);
      const state = this._readableState;
      if (ev === "data") {
        state.readableListening = this.listenerCount("readable") > 0;
        if (state.flowing !== false)
          this.resume();
      } else if (ev === "readable") {
        if (!state.endEmitted && !state.readableListening) {
          state.readableListening = state.needReadable = true;
          state.flowing = false;
          state.emittedReadable = false;
          debug("on readable", state.length, state.reading);
          if (state.length) {
            emitReadable(this);
          } else if (!state.reading) {
            process2.nextTick(nReadingNextTick, this);
          }
        }
      }
      return res;
    };
    Readable.prototype.addListener = Readable.prototype.on;
    Readable.prototype.removeListener = function(ev, fn) {
      const res = Stream.prototype.removeListener.call(this, ev, fn);
      if (ev === "readable") {
        process2.nextTick(updateReadableListening, this);
      }
      return res;
    };
    Readable.prototype.off = Readable.prototype.removeListener;
    Readable.prototype.removeAllListeners = function(ev) {
      const res = Stream.prototype.removeAllListeners.apply(this, arguments);
      if (ev === "readable" || ev === void 0) {
        process2.nextTick(updateReadableListening, this);
      }
      return res;
    };
    function updateReadableListening(self2) {
      const state = self2._readableState;
      state.readableListening = self2.listenerCount("readable") > 0;
      if (state.resumeScheduled && state[kPaused] === false) {
        state.flowing = true;
      } else if (self2.listenerCount("data") > 0) {
        self2.resume();
      } else if (!state.readableListening) {
        state.flowing = null;
      }
    }
    function nReadingNextTick(self2) {
      debug("readable nexttick read 0");
      self2.read(0);
    }
    Readable.prototype.resume = function() {
      const state = this._readableState;
      if (!state.flowing) {
        debug("resume");
        state.flowing = !state.readableListening;
        resume(this, state);
      }
      state[kPaused] = false;
      return this;
    };
    function resume(stream, state) {
      if (!state.resumeScheduled) {
        state.resumeScheduled = true;
        process2.nextTick(resume_, stream, state);
      }
    }
    function resume_(stream, state) {
      debug("resume", state.reading);
      if (!state.reading) {
        stream.read(0);
      }
      state.resumeScheduled = false;
      stream.emit("resume");
      flow(stream);
      if (state.flowing && !state.reading)
        stream.read(0);
    }
    Readable.prototype.pause = function() {
      debug("call pause flowing=%j", this._readableState.flowing);
      if (this._readableState.flowing !== false) {
        debug("pause");
        this._readableState.flowing = false;
        this.emit("pause");
      }
      this._readableState[kPaused] = true;
      return this;
    };
    function flow(stream) {
      const state = stream._readableState;
      debug("flow", state.flowing);
      while (state.flowing && stream.read() !== null)
        ;
    }
    Readable.prototype.wrap = function(stream) {
      let paused = false;
      stream.on("data", (chunk) => {
        if (!this.push(chunk) && stream.pause) {
          paused = true;
          stream.pause();
        }
      });
      stream.on("end", () => {
        this.push(null);
      });
      stream.on("error", (err) => {
        errorOrDestroy(this, err);
      });
      stream.on("close", () => {
        this.destroy();
      });
      stream.on("destroy", () => {
        this.destroy();
      });
      this._read = () => {
        if (paused && stream.resume) {
          paused = false;
          stream.resume();
        }
      };
      const streamKeys = ObjectKeys(stream);
      for (let j = 1; j < streamKeys.length; j++) {
        const i = streamKeys[j];
        if (this[i] === void 0 && typeof stream[i] === "function") {
          this[i] = stream[i].bind(stream);
        }
      }
      return this;
    };
    Readable.prototype[SymbolAsyncIterator] = function() {
      return streamToAsyncIterator(this);
    };
    Readable.prototype.iterator = function(options) {
      if (options !== void 0) {
        validateObject(options, "options");
      }
      return streamToAsyncIterator(this, options);
    };
    function streamToAsyncIterator(stream, options) {
      if (typeof stream.read !== "function") {
        stream = Readable.wrap(stream, {
          objectMode: true
        });
      }
      const iter = createAsyncIterator(stream, options);
      iter.stream = stream;
      return iter;
    }
    async function* createAsyncIterator(stream, options) {
      let callback = nop;
      function next(resolve) {
        if (this === stream) {
          callback();
          callback = nop;
        } else {
          callback = resolve;
        }
      }
      stream.on("readable", next);
      let error;
      const cleanup = eos(
        stream,
        {
          writable: false
        },
        (err) => {
          error = err ? aggregateTwoErrors(error, err) : null;
          callback();
          callback = nop;
        }
      );
      try {
        while (true) {
          const chunk = stream.destroyed ? null : stream.read();
          if (chunk !== null) {
            yield chunk;
          } else if (error) {
            throw error;
          } else if (error === null) {
            return;
          } else {
            await new Promise2(next);
          }
        }
      } catch (err) {
        error = aggregateTwoErrors(error, err);
        throw error;
      } finally {
        if ((error || (options === null || options === void 0 ? void 0 : options.destroyOnReturn) !== false) && (error === void 0 || stream._readableState.autoDestroy)) {
          destroyImpl.destroyer(stream, null);
        } else {
          stream.off("readable", next);
          cleanup();
        }
      }
    }
    ObjectDefineProperties(Readable.prototype, {
      readable: {
        __proto__: null,
        get() {
          const r = this._readableState;
          return !!r && r.readable !== false && !r.destroyed && !r.errorEmitted && !r.endEmitted;
        },
        set(val) {
          if (this._readableState) {
            this._readableState.readable = !!val;
          }
        }
      },
      readableDidRead: {
        __proto__: null,
        enumerable: false,
        get: function() {
          return this._readableState.dataEmitted;
        }
      },
      readableAborted: {
        __proto__: null,
        enumerable: false,
        get: function() {
          return !!(this._readableState.readable !== false && (this._readableState.destroyed || this._readableState.errored) && !this._readableState.endEmitted);
        }
      },
      readableHighWaterMark: {
        __proto__: null,
        enumerable: false,
        get: function() {
          return this._readableState.highWaterMark;
        }
      },
      readableBuffer: {
        __proto__: null,
        enumerable: false,
        get: function() {
          return this._readableState && this._readableState.buffer;
        }
      },
      readableFlowing: {
        __proto__: null,
        enumerable: false,
        get: function() {
          return this._readableState.flowing;
        },
        set: function(state) {
          if (this._readableState) {
            this._readableState.flowing = state;
          }
        }
      },
      readableLength: {
        __proto__: null,
        enumerable: false,
        get() {
          return this._readableState.length;
        }
      },
      readableObjectMode: {
        __proto__: null,
        enumerable: false,
        get() {
          return this._readableState ? this._readableState.objectMode : false;
        }
      },
      readableEncoding: {
        __proto__: null,
        enumerable: false,
        get() {
          return this._readableState ? this._readableState.encoding : null;
        }
      },
      errored: {
        __proto__: null,
        enumerable: false,
        get() {
          return this._readableState ? this._readableState.errored : null;
        }
      },
      closed: {
        __proto__: null,
        get() {
          return this._readableState ? this._readableState.closed : false;
        }
      },
      destroyed: {
        __proto__: null,
        enumerable: false,
        get() {
          return this._readableState ? this._readableState.destroyed : false;
        },
        set(value) {
          if (!this._readableState) {
            return;
          }
          this._readableState.destroyed = value;
        }
      },
      readableEnded: {
        __proto__: null,
        enumerable: false,
        get() {
          return this._readableState ? this._readableState.endEmitted : false;
        }
      }
    });
    ObjectDefineProperties(ReadableState.prototype, {
      // Legacy getter for `pipesCount`.
      pipesCount: {
        __proto__: null,
        get() {
          return this.pipes.length;
        }
      },
      // Legacy property for `paused`.
      paused: {
        __proto__: null,
        get() {
          return this[kPaused] !== false;
        },
        set(value) {
          this[kPaused] = !!value;
        }
      }
    });
    Readable._fromList = fromList;
    function fromList(n, state) {
      if (state.length === 0)
        return null;
      let ret;
      if (state.objectMode)
        ret = state.buffer.shift();
      else if (!n || n >= state.length) {
        if (state.decoder)
          ret = state.buffer.join("");
        else if (state.buffer.length === 1)
          ret = state.buffer.first();
        else
          ret = state.buffer.concat(state.length);
        state.buffer.clear();
      } else {
        ret = state.buffer.consume(n, state.decoder);
      }
      return ret;
    }
    function endReadable(stream) {
      const state = stream._readableState;
      debug("endReadable", state.endEmitted);
      if (!state.endEmitted) {
        state.ended = true;
        process2.nextTick(endReadableNT, state, stream);
      }
    }
    function endReadableNT(state, stream) {
      debug("endReadableNT", state.endEmitted, state.length);
      if (!state.errored && !state.closeEmitted && !state.endEmitted && state.length === 0) {
        state.endEmitted = true;
        stream.emit("end");
        if (stream.writable && stream.allowHalfOpen === false) {
          process2.nextTick(endWritableNT, stream);
        } else if (state.autoDestroy) {
          const wState = stream._writableState;
          const autoDestroy = !wState || wState.autoDestroy && // We don't expect the writable to ever 'finish'
          // if writable is explicitly set to false.
          (wState.finished || wState.writable === false);
          if (autoDestroy) {
            stream.destroy();
          }
        }
      }
    }
    function endWritableNT(stream) {
      const writable = stream.writable && !stream.writableEnded && !stream.destroyed;
      if (writable) {
        stream.end();
      }
    }
    Readable.from = function(iterable, opts) {
      return from(Readable, iterable, opts);
    };
    var webStreamsAdapters;
    function lazyWebStreams() {
      if (webStreamsAdapters === void 0)
        webStreamsAdapters = {};
      return webStreamsAdapters;
    }
    Readable.fromWeb = function(readableStream, options) {
      return lazyWebStreams().newStreamReadableFromReadableStream(readableStream, options);
    };
    Readable.toWeb = function(streamReadable, options) {
      return lazyWebStreams().newReadableStreamFromStreamReadable(streamReadable, options);
    };
    Readable.wrap = function(src, options) {
      var _ref, _src$readableObjectMo;
      return new Readable({
        objectMode: (_ref = (_src$readableObjectMo = src.readableObjectMode) !== null && _src$readableObjectMo !== void 0 ? _src$readableObjectMo : src.objectMode) !== null && _ref !== void 0 ? _ref : true,
        ...options,
        destroy(err, callback) {
          destroyImpl.destroyer(src, err);
          callback(err);
        }
      }).wrap(src);
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/writable.js
var require_writable = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/writable.js"(exports2, module2) {
    "use strict";
    var process2 = require_process();
    var {
      ArrayPrototypeSlice,
      Error: Error2,
      FunctionPrototypeSymbolHasInstance,
      ObjectDefineProperty,
      ObjectDefineProperties,
      ObjectSetPrototypeOf,
      StringPrototypeToLowerCase,
      Symbol: Symbol2,
      SymbolHasInstance
    } = require_primordials();
    module2.exports = Writable;
    Writable.WritableState = WritableState;
    var { EventEmitter: EE } = require("events");
    var Stream = require_legacy().Stream;
    var { Buffer: Buffer2 } = require("buffer");
    var destroyImpl = require_destroy();
    var { addAbortSignal } = require_add_abort_signal();
    var { getHighWaterMark, getDefaultHighWaterMark } = require_state();
    var {
      ERR_INVALID_ARG_TYPE,
      ERR_METHOD_NOT_IMPLEMENTED,
      ERR_MULTIPLE_CALLBACK,
      ERR_STREAM_CANNOT_PIPE,
      ERR_STREAM_DESTROYED,
      ERR_STREAM_ALREADY_FINISHED,
      ERR_STREAM_NULL_VALUES,
      ERR_STREAM_WRITE_AFTER_END,
      ERR_UNKNOWN_ENCODING
    } = require_errors().codes;
    var { errorOrDestroy } = destroyImpl;
    ObjectSetPrototypeOf(Writable.prototype, Stream.prototype);
    ObjectSetPrototypeOf(Writable, Stream);
    function nop() {
    }
    var kOnFinished = Symbol2("kOnFinished");
    function WritableState(options, stream, isDuplex) {
      if (typeof isDuplex !== "boolean")
        isDuplex = stream instanceof require_duplex();
      this.objectMode = !!(options && options.objectMode);
      if (isDuplex)
        this.objectMode = this.objectMode || !!(options && options.writableObjectMode);
      this.highWaterMark = options ? getHighWaterMark(this, options, "writableHighWaterMark", isDuplex) : getDefaultHighWaterMark(false);
      this.finalCalled = false;
      this.needDrain = false;
      this.ending = false;
      this.ended = false;
      this.finished = false;
      this.destroyed = false;
      const noDecode = !!(options && options.decodeStrings === false);
      this.decodeStrings = !noDecode;
      this.defaultEncoding = options && options.defaultEncoding || "utf8";
      this.length = 0;
      this.writing = false;
      this.corked = 0;
      this.sync = true;
      this.bufferProcessing = false;
      this.onwrite = onwrite.bind(void 0, stream);
      this.writecb = null;
      this.writelen = 0;
      this.afterWriteTickInfo = null;
      resetBuffer(this);
      this.pendingcb = 0;
      this.constructed = true;
      this.prefinished = false;
      this.errorEmitted = false;
      this.emitClose = !options || options.emitClose !== false;
      this.autoDestroy = !options || options.autoDestroy !== false;
      this.errored = null;
      this.closed = false;
      this.closeEmitted = false;
      this[kOnFinished] = [];
    }
    function resetBuffer(state) {
      state.buffered = [];
      state.bufferedIndex = 0;
      state.allBuffers = true;
      state.allNoop = true;
    }
    WritableState.prototype.getBuffer = function getBuffer() {
      return ArrayPrototypeSlice(this.buffered, this.bufferedIndex);
    };
    ObjectDefineProperty(WritableState.prototype, "bufferedRequestCount", {
      __proto__: null,
      get() {
        return this.buffered.length - this.bufferedIndex;
      }
    });
    function Writable(options) {
      const isDuplex = this instanceof require_duplex();
      if (!isDuplex && !FunctionPrototypeSymbolHasInstance(Writable, this))
        return new Writable(options);
      this._writableState = new WritableState(options, this, isDuplex);
      if (options) {
        if (typeof options.write === "function")
          this._write = options.write;
        if (typeof options.writev === "function")
          this._writev = options.writev;
        if (typeof options.destroy === "function")
          this._destroy = options.destroy;
        if (typeof options.final === "function")
          this._final = options.final;
        if (typeof options.construct === "function")
          this._construct = options.construct;
        if (options.signal)
          addAbortSignal(options.signal, this);
      }
      Stream.call(this, options);
      destroyImpl.construct(this, () => {
        const state = this._writableState;
        if (!state.writing) {
          clearBuffer(this, state);
        }
        finishMaybe(this, state);
      });
    }
    ObjectDefineProperty(Writable, SymbolHasInstance, {
      __proto__: null,
      value: function(object) {
        if (FunctionPrototypeSymbolHasInstance(this, object))
          return true;
        if (this !== Writable)
          return false;
        return object && object._writableState instanceof WritableState;
      }
    });
    Writable.prototype.pipe = function() {
      errorOrDestroy(this, new ERR_STREAM_CANNOT_PIPE());
    };
    function _write(stream, chunk, encoding, cb) {
      const state = stream._writableState;
      if (typeof encoding === "function") {
        cb = encoding;
        encoding = state.defaultEncoding;
      } else {
        if (!encoding)
          encoding = state.defaultEncoding;
        else if (encoding !== "buffer" && !Buffer2.isEncoding(encoding))
          throw new ERR_UNKNOWN_ENCODING(encoding);
        if (typeof cb !== "function")
          cb = nop;
      }
      if (chunk === null) {
        throw new ERR_STREAM_NULL_VALUES();
      } else if (!state.objectMode) {
        if (typeof chunk === "string") {
          if (state.decodeStrings !== false) {
            chunk = Buffer2.from(chunk, encoding);
            encoding = "buffer";
          }
        } else if (chunk instanceof Buffer2) {
          encoding = "buffer";
        } else if (Stream._isUint8Array(chunk)) {
          chunk = Stream._uint8ArrayToBuffer(chunk);
          encoding = "buffer";
        } else {
          throw new ERR_INVALID_ARG_TYPE("chunk", ["string", "Buffer", "Uint8Array"], chunk);
        }
      }
      let err;
      if (state.ending) {
        err = new ERR_STREAM_WRITE_AFTER_END();
      } else if (state.destroyed) {
        err = new ERR_STREAM_DESTROYED("write");
      }
      if (err) {
        process2.nextTick(cb, err);
        errorOrDestroy(stream, err, true);
        return err;
      }
      state.pendingcb++;
      return writeOrBuffer(stream, state, chunk, encoding, cb);
    }
    Writable.prototype.write = function(chunk, encoding, cb) {
      return _write(this, chunk, encoding, cb) === true;
    };
    Writable.prototype.cork = function() {
      this._writableState.corked++;
    };
    Writable.prototype.uncork = function() {
      const state = this._writableState;
      if (state.corked) {
        state.corked--;
        if (!state.writing)
          clearBuffer(this, state);
      }
    };
    Writable.prototype.setDefaultEncoding = function setDefaultEncoding(encoding) {
      if (typeof encoding === "string")
        encoding = StringPrototypeToLowerCase(encoding);
      if (!Buffer2.isEncoding(encoding))
        throw new ERR_UNKNOWN_ENCODING(encoding);
      this._writableState.defaultEncoding = encoding;
      return this;
    };
    function writeOrBuffer(stream, state, chunk, encoding, callback) {
      const len = state.objectMode ? 1 : chunk.length;
      state.length += len;
      const ret = state.length < state.highWaterMark;
      if (!ret)
        state.needDrain = true;
      if (state.writing || state.corked || state.errored || !state.constructed) {
        state.buffered.push({
          chunk,
          encoding,
          callback
        });
        if (state.allBuffers && encoding !== "buffer") {
          state.allBuffers = false;
        }
        if (state.allNoop && callback !== nop) {
          state.allNoop = false;
        }
      } else {
        state.writelen = len;
        state.writecb = callback;
        state.writing = true;
        state.sync = true;
        stream._write(chunk, encoding, state.onwrite);
        state.sync = false;
      }
      return ret && !state.errored && !state.destroyed;
    }
    function doWrite(stream, state, writev, len, chunk, encoding, cb) {
      state.writelen = len;
      state.writecb = cb;
      state.writing = true;
      state.sync = true;
      if (state.destroyed)
        state.onwrite(new ERR_STREAM_DESTROYED("write"));
      else if (writev)
        stream._writev(chunk, state.onwrite);
      else
        stream._write(chunk, encoding, state.onwrite);
      state.sync = false;
    }
    function onwriteError(stream, state, er, cb) {
      --state.pendingcb;
      cb(er);
      errorBuffer(state);
      errorOrDestroy(stream, er);
    }
    function onwrite(stream, er) {
      const state = stream._writableState;
      const sync = state.sync;
      const cb = state.writecb;
      if (typeof cb !== "function") {
        errorOrDestroy(stream, new ERR_MULTIPLE_CALLBACK());
        return;
      }
      state.writing = false;
      state.writecb = null;
      state.length -= state.writelen;
      state.writelen = 0;
      if (er) {
        er.stack;
        if (!state.errored) {
          state.errored = er;
        }
        if (stream._readableState && !stream._readableState.errored) {
          stream._readableState.errored = er;
        }
        if (sync) {
          process2.nextTick(onwriteError, stream, state, er, cb);
        } else {
          onwriteError(stream, state, er, cb);
        }
      } else {
        if (state.buffered.length > state.bufferedIndex) {
          clearBuffer(stream, state);
        }
        if (sync) {
          if (state.afterWriteTickInfo !== null && state.afterWriteTickInfo.cb === cb) {
            state.afterWriteTickInfo.count++;
          } else {
            state.afterWriteTickInfo = {
              count: 1,
              cb,
              stream,
              state
            };
            process2.nextTick(afterWriteTick, state.afterWriteTickInfo);
          }
        } else {
          afterWrite(stream, state, 1, cb);
        }
      }
    }
    function afterWriteTick({ stream, state, count, cb }) {
      state.afterWriteTickInfo = null;
      return afterWrite(stream, state, count, cb);
    }
    function afterWrite(stream, state, count, cb) {
      const needDrain = !state.ending && !stream.destroyed && state.length === 0 && state.needDrain;
      if (needDrain) {
        state.needDrain = false;
        stream.emit("drain");
      }
      while (count-- > 0) {
        state.pendingcb--;
        cb();
      }
      if (state.destroyed) {
        errorBuffer(state);
      }
      finishMaybe(stream, state);
    }
    function errorBuffer(state) {
      if (state.writing) {
        return;
      }
      for (let n = state.bufferedIndex; n < state.buffered.length; ++n) {
        var _state$errored;
        const { chunk, callback } = state.buffered[n];
        const len = state.objectMode ? 1 : chunk.length;
        state.length -= len;
        callback(
          (_state$errored = state.errored) !== null && _state$errored !== void 0 ? _state$errored : new ERR_STREAM_DESTROYED("write")
        );
      }
      const onfinishCallbacks = state[kOnFinished].splice(0);
      for (let i = 0; i < onfinishCallbacks.length; i++) {
        var _state$errored2;
        onfinishCallbacks[i](
          (_state$errored2 = state.errored) !== null && _state$errored2 !== void 0 ? _state$errored2 : new ERR_STREAM_DESTROYED("end")
        );
      }
      resetBuffer(state);
    }
    function clearBuffer(stream, state) {
      if (state.corked || state.bufferProcessing || state.destroyed || !state.constructed) {
        return;
      }
      const { buffered, bufferedIndex, objectMode } = state;
      const bufferedLength = buffered.length - bufferedIndex;
      if (!bufferedLength) {
        return;
      }
      let i = bufferedIndex;
      state.bufferProcessing = true;
      if (bufferedLength > 1 && stream._writev) {
        state.pendingcb -= bufferedLength - 1;
        const callback = state.allNoop ? nop : (err) => {
          for (let n = i; n < buffered.length; ++n) {
            buffered[n].callback(err);
          }
        };
        const chunks = state.allNoop && i === 0 ? buffered : ArrayPrototypeSlice(buffered, i);
        chunks.allBuffers = state.allBuffers;
        doWrite(stream, state, true, state.length, chunks, "", callback);
        resetBuffer(state);
      } else {
        do {
          const { chunk, encoding, callback } = buffered[i];
          buffered[i++] = null;
          const len = objectMode ? 1 : chunk.length;
          doWrite(stream, state, false, len, chunk, encoding, callback);
        } while (i < buffered.length && !state.writing);
        if (i === buffered.length) {
          resetBuffer(state);
        } else if (i > 256) {
          buffered.splice(0, i);
          state.bufferedIndex = 0;
        } else {
          state.bufferedIndex = i;
        }
      }
      state.bufferProcessing = false;
    }
    Writable.prototype._write = function(chunk, encoding, cb) {
      if (this._writev) {
        this._writev(
          [
            {
              chunk,
              encoding
            }
          ],
          cb
        );
      } else {
        throw new ERR_METHOD_NOT_IMPLEMENTED("_write()");
      }
    };
    Writable.prototype._writev = null;
    Writable.prototype.end = function(chunk, encoding, cb) {
      const state = this._writableState;
      if (typeof chunk === "function") {
        cb = chunk;
        chunk = null;
        encoding = null;
      } else if (typeof encoding === "function") {
        cb = encoding;
        encoding = null;
      }
      let err;
      if (chunk !== null && chunk !== void 0) {
        const ret = _write(this, chunk, encoding);
        if (ret instanceof Error2) {
          err = ret;
        }
      }
      if (state.corked) {
        state.corked = 1;
        this.uncork();
      }
      if (err) {
      } else if (!state.errored && !state.ending) {
        state.ending = true;
        finishMaybe(this, state, true);
        state.ended = true;
      } else if (state.finished) {
        err = new ERR_STREAM_ALREADY_FINISHED("end");
      } else if (state.destroyed) {
        err = new ERR_STREAM_DESTROYED("end");
      }
      if (typeof cb === "function") {
        if (err || state.finished) {
          process2.nextTick(cb, err);
        } else {
          state[kOnFinished].push(cb);
        }
      }
      return this;
    };
    function needFinish(state) {
      return state.ending && !state.destroyed && state.constructed && state.length === 0 && !state.errored && state.buffered.length === 0 && !state.finished && !state.writing && !state.errorEmitted && !state.closeEmitted;
    }
    function callFinal(stream, state) {
      let called = false;
      function onFinish(err) {
        if (called) {
          errorOrDestroy(stream, err !== null && err !== void 0 ? err : ERR_MULTIPLE_CALLBACK());
          return;
        }
        called = true;
        state.pendingcb--;
        if (err) {
          const onfinishCallbacks = state[kOnFinished].splice(0);
          for (let i = 0; i < onfinishCallbacks.length; i++) {
            onfinishCallbacks[i](err);
          }
          errorOrDestroy(stream, err, state.sync);
        } else if (needFinish(state)) {
          state.prefinished = true;
          stream.emit("prefinish");
          state.pendingcb++;
          process2.nextTick(finish, stream, state);
        }
      }
      state.sync = true;
      state.pendingcb++;
      try {
        stream._final(onFinish);
      } catch (err) {
        onFinish(err);
      }
      state.sync = false;
    }
    function prefinish(stream, state) {
      if (!state.prefinished && !state.finalCalled) {
        if (typeof stream._final === "function" && !state.destroyed) {
          state.finalCalled = true;
          callFinal(stream, state);
        } else {
          state.prefinished = true;
          stream.emit("prefinish");
        }
      }
    }
    function finishMaybe(stream, state, sync) {
      if (needFinish(state)) {
        prefinish(stream, state);
        if (state.pendingcb === 0) {
          if (sync) {
            state.pendingcb++;
            process2.nextTick(
              (stream2, state2) => {
                if (needFinish(state2)) {
                  finish(stream2, state2);
                } else {
                  state2.pendingcb--;
                }
              },
              stream,
              state
            );
          } else if (needFinish(state)) {
            state.pendingcb++;
            finish(stream, state);
          }
        }
      }
    }
    function finish(stream, state) {
      state.pendingcb--;
      state.finished = true;
      const onfinishCallbacks = state[kOnFinished].splice(0);
      for (let i = 0; i < onfinishCallbacks.length; i++) {
        onfinishCallbacks[i]();
      }
      stream.emit("finish");
      if (state.autoDestroy) {
        const rState = stream._readableState;
        const autoDestroy = !rState || rState.autoDestroy && // We don't expect the readable to ever 'end'
        // if readable is explicitly set to false.
        (rState.endEmitted || rState.readable === false);
        if (autoDestroy) {
          stream.destroy();
        }
      }
    }
    ObjectDefineProperties(Writable.prototype, {
      closed: {
        __proto__: null,
        get() {
          return this._writableState ? this._writableState.closed : false;
        }
      },
      destroyed: {
        __proto__: null,
        get() {
          return this._writableState ? this._writableState.destroyed : false;
        },
        set(value) {
          if (this._writableState) {
            this._writableState.destroyed = value;
          }
        }
      },
      writable: {
        __proto__: null,
        get() {
          const w = this._writableState;
          return !!w && w.writable !== false && !w.destroyed && !w.errored && !w.ending && !w.ended;
        },
        set(val) {
          if (this._writableState) {
            this._writableState.writable = !!val;
          }
        }
      },
      writableFinished: {
        __proto__: null,
        get() {
          return this._writableState ? this._writableState.finished : false;
        }
      },
      writableObjectMode: {
        __proto__: null,
        get() {
          return this._writableState ? this._writableState.objectMode : false;
        }
      },
      writableBuffer: {
        __proto__: null,
        get() {
          return this._writableState && this._writableState.getBuffer();
        }
      },
      writableEnded: {
        __proto__: null,
        get() {
          return this._writableState ? this._writableState.ending : false;
        }
      },
      writableNeedDrain: {
        __proto__: null,
        get() {
          const wState = this._writableState;
          if (!wState)
            return false;
          return !wState.destroyed && !wState.ending && wState.needDrain;
        }
      },
      writableHighWaterMark: {
        __proto__: null,
        get() {
          return this._writableState && this._writableState.highWaterMark;
        }
      },
      writableCorked: {
        __proto__: null,
        get() {
          return this._writableState ? this._writableState.corked : 0;
        }
      },
      writableLength: {
        __proto__: null,
        get() {
          return this._writableState && this._writableState.length;
        }
      },
      errored: {
        __proto__: null,
        enumerable: false,
        get() {
          return this._writableState ? this._writableState.errored : null;
        }
      },
      writableAborted: {
        __proto__: null,
        enumerable: false,
        get: function() {
          return !!(this._writableState.writable !== false && (this._writableState.destroyed || this._writableState.errored) && !this._writableState.finished);
        }
      }
    });
    var destroy = destroyImpl.destroy;
    Writable.prototype.destroy = function(err, cb) {
      const state = this._writableState;
      if (!state.destroyed && (state.bufferedIndex < state.buffered.length || state[kOnFinished].length)) {
        process2.nextTick(errorBuffer, state);
      }
      destroy.call(this, err, cb);
      return this;
    };
    Writable.prototype._undestroy = destroyImpl.undestroy;
    Writable.prototype._destroy = function(err, cb) {
      cb(err);
    };
    Writable.prototype[EE.captureRejectionSymbol] = function(err) {
      this.destroy(err);
    };
    var webStreamsAdapters;
    function lazyWebStreams() {
      if (webStreamsAdapters === void 0)
        webStreamsAdapters = {};
      return webStreamsAdapters;
    }
    Writable.fromWeb = function(writableStream, options) {
      return lazyWebStreams().newStreamWritableFromWritableStream(writableStream, options);
    };
    Writable.toWeb = function(streamWritable) {
      return lazyWebStreams().newWritableStreamFromStreamWritable(streamWritable);
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/duplexify.js
var require_duplexify = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/duplexify.js"(exports2, module2) {
    var process2 = require_process();
    var bufferModule = require("buffer");
    var {
      isReadable,
      isWritable,
      isIterable,
      isNodeStream,
      isReadableNodeStream,
      isWritableNodeStream,
      isDuplexNodeStream,
      isReadableStream,
      isWritableStream
    } = require_utils5();
    var eos = require_end_of_stream();
    var {
      AbortError,
      codes: { ERR_INVALID_ARG_TYPE, ERR_INVALID_RETURN_VALUE }
    } = require_errors();
    var { destroyer } = require_destroy();
    var Duplex = require_duplex();
    var Readable = require_readable();
    var Writable = require_writable();
    var { createDeferredPromise } = require_util2();
    var from = require_from();
    var Blob = globalThis.Blob || bufferModule.Blob;
    var isBlob = typeof Blob !== "undefined" ? function isBlob2(b) {
      return b instanceof Blob;
    } : function isBlob2(b) {
      return false;
    };
    var AbortController = globalThis.AbortController || require_abort_controller().AbortController;
    var { FunctionPrototypeCall } = require_primordials();
    var Duplexify = class extends Duplex {
      constructor(options) {
        super(options);
        if ((options === null || options === void 0 ? void 0 : options.readable) === false) {
          this._readableState.readable = false;
          this._readableState.ended = true;
          this._readableState.endEmitted = true;
        }
        if ((options === null || options === void 0 ? void 0 : options.writable) === false) {
          this._writableState.writable = false;
          this._writableState.ending = true;
          this._writableState.ended = true;
          this._writableState.finished = true;
        }
      }
    };
    module2.exports = function duplexify(body, name) {
      if (isDuplexNodeStream(body)) {
        return body;
      }
      if (isReadableNodeStream(body)) {
        return _duplexify({
          readable: body
        });
      }
      if (isWritableNodeStream(body)) {
        return _duplexify({
          writable: body
        });
      }
      if (isNodeStream(body)) {
        return _duplexify({
          writable: false,
          readable: false
        });
      }
      if (isReadableStream(body)) {
        return _duplexify({
          readable: Readable.fromWeb(body)
        });
      }
      if (isWritableStream(body)) {
        return _duplexify({
          writable: Writable.fromWeb(body)
        });
      }
      if (typeof body === "function") {
        const { value, write, final, destroy } = fromAsyncGen(body);
        if (isIterable(value)) {
          return from(Duplexify, value, {
            // TODO (ronag): highWaterMark?
            objectMode: true,
            write,
            final,
            destroy
          });
        }
        const then2 = value === null || value === void 0 ? void 0 : value.then;
        if (typeof then2 === "function") {
          let d;
          const promise = FunctionPrototypeCall(
            then2,
            value,
            (val) => {
              if (val != null) {
                throw new ERR_INVALID_RETURN_VALUE("nully", "body", val);
              }
            },
            (err) => {
              destroyer(d, err);
            }
          );
          return d = new Duplexify({
            // TODO (ronag): highWaterMark?
            objectMode: true,
            readable: false,
            write,
            final(cb) {
              final(async () => {
                try {
                  await promise;
                  process2.nextTick(cb, null);
                } catch (err) {
                  process2.nextTick(cb, err);
                }
              });
            },
            destroy
          });
        }
        throw new ERR_INVALID_RETURN_VALUE("Iterable, AsyncIterable or AsyncFunction", name, value);
      }
      if (isBlob(body)) {
        return duplexify(body.arrayBuffer());
      }
      if (isIterable(body)) {
        return from(Duplexify, body, {
          // TODO (ronag): highWaterMark?
          objectMode: true,
          writable: false
        });
      }
      if (isReadableStream(body === null || body === void 0 ? void 0 : body.readable) && isWritableStream(body === null || body === void 0 ? void 0 : body.writable)) {
        return Duplexify.fromWeb(body);
      }
      if (typeof (body === null || body === void 0 ? void 0 : body.writable) === "object" || typeof (body === null || body === void 0 ? void 0 : body.readable) === "object") {
        const readable = body !== null && body !== void 0 && body.readable ? isReadableNodeStream(body === null || body === void 0 ? void 0 : body.readable) ? body === null || body === void 0 ? void 0 : body.readable : duplexify(body.readable) : void 0;
        const writable = body !== null && body !== void 0 && body.writable ? isWritableNodeStream(body === null || body === void 0 ? void 0 : body.writable) ? body === null || body === void 0 ? void 0 : body.writable : duplexify(body.writable) : void 0;
        return _duplexify({
          readable,
          writable
        });
      }
      const then = body === null || body === void 0 ? void 0 : body.then;
      if (typeof then === "function") {
        let d;
        FunctionPrototypeCall(
          then,
          body,
          (val) => {
            if (val != null) {
              d.push(val);
            }
            d.push(null);
          },
          (err) => {
            destroyer(d, err);
          }
        );
        return d = new Duplexify({
          objectMode: true,
          writable: false,
          read() {
          }
        });
      }
      throw new ERR_INVALID_ARG_TYPE(
        name,
        [
          "Blob",
          "ReadableStream",
          "WritableStream",
          "Stream",
          "Iterable",
          "AsyncIterable",
          "Function",
          "{ readable, writable } pair",
          "Promise"
        ],
        body
      );
    };
    function fromAsyncGen(fn) {
      let { promise, resolve } = createDeferredPromise();
      const ac = new AbortController();
      const signal = ac.signal;
      const value = fn(
        async function* () {
          while (true) {
            const _promise = promise;
            promise = null;
            const { chunk, done, cb } = await _promise;
            process2.nextTick(cb);
            if (done)
              return;
            if (signal.aborted)
              throw new AbortError(void 0, {
                cause: signal.reason
              });
            ({ promise, resolve } = createDeferredPromise());
            yield chunk;
          }
        }(),
        {
          signal
        }
      );
      return {
        value,
        write(chunk, encoding, cb) {
          const _resolve = resolve;
          resolve = null;
          _resolve({
            chunk,
            done: false,
            cb
          });
        },
        final(cb) {
          const _resolve = resolve;
          resolve = null;
          _resolve({
            done: true,
            cb
          });
        },
        destroy(err, cb) {
          ac.abort();
          cb(err);
        }
      };
    }
    function _duplexify(pair) {
      const r = pair.readable && typeof pair.readable.read !== "function" ? Readable.wrap(pair.readable) : pair.readable;
      const w = pair.writable;
      let readable = !!isReadable(r);
      let writable = !!isWritable(w);
      let ondrain;
      let onfinish;
      let onreadable;
      let onclose;
      let d;
      function onfinished(err) {
        const cb = onclose;
        onclose = null;
        if (cb) {
          cb(err);
        } else if (err) {
          d.destroy(err);
        }
      }
      d = new Duplexify({
        // TODO (ronag): highWaterMark?
        readableObjectMode: !!(r !== null && r !== void 0 && r.readableObjectMode),
        writableObjectMode: !!(w !== null && w !== void 0 && w.writableObjectMode),
        readable,
        writable
      });
      if (writable) {
        eos(w, (err) => {
          writable = false;
          if (err) {
            destroyer(r, err);
          }
          onfinished(err);
        });
        d._write = function(chunk, encoding, callback) {
          if (w.write(chunk, encoding)) {
            callback();
          } else {
            ondrain = callback;
          }
        };
        d._final = function(callback) {
          w.end();
          onfinish = callback;
        };
        w.on("drain", function() {
          if (ondrain) {
            const cb = ondrain;
            ondrain = null;
            cb();
          }
        });
        w.on("finish", function() {
          if (onfinish) {
            const cb = onfinish;
            onfinish = null;
            cb();
          }
        });
      }
      if (readable) {
        eos(r, (err) => {
          readable = false;
          if (err) {
            destroyer(r, err);
          }
          onfinished(err);
        });
        r.on("readable", function() {
          if (onreadable) {
            const cb = onreadable;
            onreadable = null;
            cb();
          }
        });
        r.on("end", function() {
          d.push(null);
        });
        d._read = function() {
          while (true) {
            const buf = r.read();
            if (buf === null) {
              onreadable = d._read;
              return;
            }
            if (!d.push(buf)) {
              return;
            }
          }
        };
      }
      d._destroy = function(err, callback) {
        if (!err && onclose !== null) {
          err = new AbortError();
        }
        onreadable = null;
        ondrain = null;
        onfinish = null;
        if (onclose === null) {
          callback(err);
        } else {
          onclose = callback;
          destroyer(w, err);
          destroyer(r, err);
        }
      };
      return d;
    }
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/duplex.js
var require_duplex = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/duplex.js"(exports2, module2) {
    "use strict";
    var {
      ObjectDefineProperties,
      ObjectGetOwnPropertyDescriptor,
      ObjectKeys,
      ObjectSetPrototypeOf
    } = require_primordials();
    module2.exports = Duplex;
    var Readable = require_readable();
    var Writable = require_writable();
    ObjectSetPrototypeOf(Duplex.prototype, Readable.prototype);
    ObjectSetPrototypeOf(Duplex, Readable);
    {
      const keys = ObjectKeys(Writable.prototype);
      for (let i = 0; i < keys.length; i++) {
        const method = keys[i];
        if (!Duplex.prototype[method])
          Duplex.prototype[method] = Writable.prototype[method];
      }
    }
    function Duplex(options) {
      if (!(this instanceof Duplex))
        return new Duplex(options);
      Readable.call(this, options);
      Writable.call(this, options);
      if (options) {
        this.allowHalfOpen = options.allowHalfOpen !== false;
        if (options.readable === false) {
          this._readableState.readable = false;
          this._readableState.ended = true;
          this._readableState.endEmitted = true;
        }
        if (options.writable === false) {
          this._writableState.writable = false;
          this._writableState.ending = true;
          this._writableState.ended = true;
          this._writableState.finished = true;
        }
      } else {
        this.allowHalfOpen = true;
      }
    }
    ObjectDefineProperties(Duplex.prototype, {
      writable: {
        __proto__: null,
        ...ObjectGetOwnPropertyDescriptor(Writable.prototype, "writable")
      },
      writableHighWaterMark: {
        __proto__: null,
        ...ObjectGetOwnPropertyDescriptor(Writable.prototype, "writableHighWaterMark")
      },
      writableObjectMode: {
        __proto__: null,
        ...ObjectGetOwnPropertyDescriptor(Writable.prototype, "writableObjectMode")
      },
      writableBuffer: {
        __proto__: null,
        ...ObjectGetOwnPropertyDescriptor(Writable.prototype, "writableBuffer")
      },
      writableLength: {
        __proto__: null,
        ...ObjectGetOwnPropertyDescriptor(Writable.prototype, "writableLength")
      },
      writableFinished: {
        __proto__: null,
        ...ObjectGetOwnPropertyDescriptor(Writable.prototype, "writableFinished")
      },
      writableCorked: {
        __proto__: null,
        ...ObjectGetOwnPropertyDescriptor(Writable.prototype, "writableCorked")
      },
      writableEnded: {
        __proto__: null,
        ...ObjectGetOwnPropertyDescriptor(Writable.prototype, "writableEnded")
      },
      writableNeedDrain: {
        __proto__: null,
        ...ObjectGetOwnPropertyDescriptor(Writable.prototype, "writableNeedDrain")
      },
      destroyed: {
        __proto__: null,
        get() {
          if (this._readableState === void 0 || this._writableState === void 0) {
            return false;
          }
          return this._readableState.destroyed && this._writableState.destroyed;
        },
        set(value) {
          if (this._readableState && this._writableState) {
            this._readableState.destroyed = value;
            this._writableState.destroyed = value;
          }
        }
      }
    });
    var webStreamsAdapters;
    function lazyWebStreams() {
      if (webStreamsAdapters === void 0)
        webStreamsAdapters = {};
      return webStreamsAdapters;
    }
    Duplex.fromWeb = function(pair, options) {
      return lazyWebStreams().newStreamDuplexFromReadableWritablePair(pair, options);
    };
    Duplex.toWeb = function(duplex) {
      return lazyWebStreams().newReadableWritablePairFromDuplex(duplex);
    };
    var duplexify;
    Duplex.from = function(body) {
      if (!duplexify) {
        duplexify = require_duplexify();
      }
      return duplexify(body, "body");
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/transform.js
var require_transform = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/transform.js"(exports2, module2) {
    "use strict";
    var { ObjectSetPrototypeOf, Symbol: Symbol2 } = require_primordials();
    module2.exports = Transform;
    var { ERR_METHOD_NOT_IMPLEMENTED } = require_errors().codes;
    var Duplex = require_duplex();
    var { getHighWaterMark } = require_state();
    ObjectSetPrototypeOf(Transform.prototype, Duplex.prototype);
    ObjectSetPrototypeOf(Transform, Duplex);
    var kCallback = Symbol2("kCallback");
    function Transform(options) {
      if (!(this instanceof Transform))
        return new Transform(options);
      const readableHighWaterMark = options ? getHighWaterMark(this, options, "readableHighWaterMark", true) : null;
      if (readableHighWaterMark === 0) {
        options = {
          ...options,
          highWaterMark: null,
          readableHighWaterMark,
          // TODO (ronag): 0 is not optimal since we have
          // a "bug" where we check needDrain before calling _write and not after.
          // Refs: https://github.com/nodejs/node/pull/32887
          // Refs: https://github.com/nodejs/node/pull/35941
          writableHighWaterMark: options.writableHighWaterMark || 0
        };
      }
      Duplex.call(this, options);
      this._readableState.sync = false;
      this[kCallback] = null;
      if (options) {
        if (typeof options.transform === "function")
          this._transform = options.transform;
        if (typeof options.flush === "function")
          this._flush = options.flush;
      }
      this.on("prefinish", prefinish);
    }
    function final(cb) {
      if (typeof this._flush === "function" && !this.destroyed) {
        this._flush((er, data) => {
          if (er) {
            if (cb) {
              cb(er);
            } else {
              this.destroy(er);
            }
            return;
          }
          if (data != null) {
            this.push(data);
          }
          this.push(null);
          if (cb) {
            cb();
          }
        });
      } else {
        this.push(null);
        if (cb) {
          cb();
        }
      }
    }
    function prefinish() {
      if (this._final !== final) {
        final.call(this);
      }
    }
    Transform.prototype._final = final;
    Transform.prototype._transform = function(chunk, encoding, callback) {
      throw new ERR_METHOD_NOT_IMPLEMENTED("_transform()");
    };
    Transform.prototype._write = function(chunk, encoding, callback) {
      const rState = this._readableState;
      const wState = this._writableState;
      const length = rState.length;
      this._transform(chunk, encoding, (err, val) => {
        if (err) {
          callback(err);
          return;
        }
        if (val != null) {
          this.push(val);
        }
        if (wState.ended || // Backwards compat.
        length === rState.length || // Backwards compat.
        rState.length < rState.highWaterMark) {
          callback();
        } else {
          this[kCallback] = callback;
        }
      });
    };
    Transform.prototype._read = function() {
      if (this[kCallback]) {
        const callback = this[kCallback];
        this[kCallback] = null;
        callback();
      }
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/passthrough.js
var require_passthrough = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/passthrough.js"(exports2, module2) {
    "use strict";
    var { ObjectSetPrototypeOf } = require_primordials();
    module2.exports = PassThrough;
    var Transform = require_transform();
    ObjectSetPrototypeOf(PassThrough.prototype, Transform.prototype);
    ObjectSetPrototypeOf(PassThrough, Transform);
    function PassThrough(options) {
      if (!(this instanceof PassThrough))
        return new PassThrough(options);
      Transform.call(this, options);
    }
    PassThrough.prototype._transform = function(chunk, encoding, cb) {
      cb(null, chunk);
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/pipeline.js
var require_pipeline = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/pipeline.js"(exports2, module2) {
    var process2 = require_process();
    var { ArrayIsArray, Promise: Promise2, SymbolAsyncIterator, SymbolDispose } = require_primordials();
    var eos = require_end_of_stream();
    var { once } = require_util2();
    var destroyImpl = require_destroy();
    var Duplex = require_duplex();
    var {
      aggregateTwoErrors,
      codes: {
        ERR_INVALID_ARG_TYPE,
        ERR_INVALID_RETURN_VALUE,
        ERR_MISSING_ARGS,
        ERR_STREAM_DESTROYED,
        ERR_STREAM_PREMATURE_CLOSE
      },
      AbortError
    } = require_errors();
    var { validateFunction, validateAbortSignal } = require_validators();
    var {
      isIterable,
      isReadable,
      isReadableNodeStream,
      isNodeStream,
      isTransformStream,
      isWebStream,
      isReadableStream,
      isReadableFinished
    } = require_utils5();
    var AbortController = globalThis.AbortController || require_abort_controller().AbortController;
    var PassThrough;
    var Readable;
    var addAbortListener;
    function destroyer(stream, reading, writing) {
      let finished = false;
      stream.on("close", () => {
        finished = true;
      });
      const cleanup = eos(
        stream,
        {
          readable: reading,
          writable: writing
        },
        (err) => {
          finished = !err;
        }
      );
      return {
        destroy: (err) => {
          if (finished)
            return;
          finished = true;
          destroyImpl.destroyer(stream, err || new ERR_STREAM_DESTROYED("pipe"));
        },
        cleanup
      };
    }
    function popCallback(streams) {
      validateFunction(streams[streams.length - 1], "streams[stream.length - 1]");
      return streams.pop();
    }
    function makeAsyncIterable(val) {
      if (isIterable(val)) {
        return val;
      } else if (isReadableNodeStream(val)) {
        return fromReadable(val);
      }
      throw new ERR_INVALID_ARG_TYPE("val", ["Readable", "Iterable", "AsyncIterable"], val);
    }
    async function* fromReadable(val) {
      if (!Readable) {
        Readable = require_readable();
      }
      yield* Readable.prototype[SymbolAsyncIterator].call(val);
    }
    async function pumpToNode(iterable, writable, finish, { end }) {
      let error;
      let onresolve = null;
      const resume = (err) => {
        if (err) {
          error = err;
        }
        if (onresolve) {
          const callback = onresolve;
          onresolve = null;
          callback();
        }
      };
      const wait = () => new Promise2((resolve, reject) => {
        if (error) {
          reject(error);
        } else {
          onresolve = () => {
            if (error) {
              reject(error);
            } else {
              resolve();
            }
          };
        }
      });
      writable.on("drain", resume);
      const cleanup = eos(
        writable,
        {
          readable: false
        },
        resume
      );
      try {
        if (writable.writableNeedDrain) {
          await wait();
        }
        for await (const chunk of iterable) {
          if (!writable.write(chunk)) {
            await wait();
          }
        }
        if (end) {
          writable.end();
          await wait();
        }
        finish();
      } catch (err) {
        finish(error !== err ? aggregateTwoErrors(error, err) : err);
      } finally {
        cleanup();
        writable.off("drain", resume);
      }
    }
    async function pumpToWeb(readable, writable, finish, { end }) {
      if (isTransformStream(writable)) {
        writable = writable.writable;
      }
      const writer = writable.getWriter();
      try {
        for await (const chunk of readable) {
          await writer.ready;
          writer.write(chunk).catch(() => {
          });
        }
        await writer.ready;
        if (end) {
          await writer.close();
        }
        finish();
      } catch (err) {
        try {
          await writer.abort(err);
          finish(err);
        } catch (err2) {
          finish(err2);
        }
      }
    }
    function pipeline(...streams) {
      return pipelineImpl(streams, once(popCallback(streams)));
    }
    function pipelineImpl(streams, callback, opts) {
      if (streams.length === 1 && ArrayIsArray(streams[0])) {
        streams = streams[0];
      }
      if (streams.length < 2) {
        throw new ERR_MISSING_ARGS("streams");
      }
      const ac = new AbortController();
      const signal = ac.signal;
      const outerSignal = opts === null || opts === void 0 ? void 0 : opts.signal;
      const lastStreamCleanup = [];
      validateAbortSignal(outerSignal, "options.signal");
      function abort() {
        finishImpl(new AbortError());
      }
      addAbortListener = addAbortListener || require_util2().addAbortListener;
      let disposable;
      if (outerSignal) {
        disposable = addAbortListener(outerSignal, abort);
      }
      let error;
      let value;
      const destroys = [];
      let finishCount = 0;
      function finish(err) {
        finishImpl(err, --finishCount === 0);
      }
      function finishImpl(err, final) {
        var _disposable;
        if (err && (!error || error.code === "ERR_STREAM_PREMATURE_CLOSE")) {
          error = err;
        }
        if (!error && !final) {
          return;
        }
        while (destroys.length) {
          destroys.shift()(error);
        }
        ;
        (_disposable = disposable) === null || _disposable === void 0 ? void 0 : _disposable[SymbolDispose]();
        ac.abort();
        if (final) {
          if (!error) {
            lastStreamCleanup.forEach((fn) => fn());
          }
          process2.nextTick(callback, error, value);
        }
      }
      let ret;
      for (let i = 0; i < streams.length; i++) {
        const stream = streams[i];
        const reading = i < streams.length - 1;
        const writing = i > 0;
        const end = reading || (opts === null || opts === void 0 ? void 0 : opts.end) !== false;
        const isLastStream = i === streams.length - 1;
        if (isNodeStream(stream)) {
          let onError2 = function(err) {
            if (err && err.name !== "AbortError" && err.code !== "ERR_STREAM_PREMATURE_CLOSE") {
              finish(err);
            }
          };
          var onError = onError2;
          if (end) {
            const { destroy, cleanup } = destroyer(stream, reading, writing);
            destroys.push(destroy);
            if (isReadable(stream) && isLastStream) {
              lastStreamCleanup.push(cleanup);
            }
          }
          stream.on("error", onError2);
          if (isReadable(stream) && isLastStream) {
            lastStreamCleanup.push(() => {
              stream.removeListener("error", onError2);
            });
          }
        }
        if (i === 0) {
          if (typeof stream === "function") {
            ret = stream({
              signal
            });
            if (!isIterable(ret)) {
              throw new ERR_INVALID_RETURN_VALUE("Iterable, AsyncIterable or Stream", "source", ret);
            }
          } else if (isIterable(stream) || isReadableNodeStream(stream) || isTransformStream(stream)) {
            ret = stream;
          } else {
            ret = Duplex.from(stream);
          }
        } else if (typeof stream === "function") {
          if (isTransformStream(ret)) {
            var _ret;
            ret = makeAsyncIterable((_ret = ret) === null || _ret === void 0 ? void 0 : _ret.readable);
          } else {
            ret = makeAsyncIterable(ret);
          }
          ret = stream(ret, {
            signal
          });
          if (reading) {
            if (!isIterable(ret, true)) {
              throw new ERR_INVALID_RETURN_VALUE("AsyncIterable", `transform[${i - 1}]`, ret);
            }
          } else {
            var _ret2;
            if (!PassThrough) {
              PassThrough = require_passthrough();
            }
            const pt = new PassThrough({
              objectMode: true
            });
            const then = (_ret2 = ret) === null || _ret2 === void 0 ? void 0 : _ret2.then;
            if (typeof then === "function") {
              finishCount++;
              then.call(
                ret,
                (val) => {
                  value = val;
                  if (val != null) {
                    pt.write(val);
                  }
                  if (end) {
                    pt.end();
                  }
                  process2.nextTick(finish);
                },
                (err) => {
                  pt.destroy(err);
                  process2.nextTick(finish, err);
                }
              );
            } else if (isIterable(ret, true)) {
              finishCount++;
              pumpToNode(ret, pt, finish, {
                end
              });
            } else if (isReadableStream(ret) || isTransformStream(ret)) {
              const toRead = ret.readable || ret;
              finishCount++;
              pumpToNode(toRead, pt, finish, {
                end
              });
            } else {
              throw new ERR_INVALID_RETURN_VALUE("AsyncIterable or Promise", "destination", ret);
            }
            ret = pt;
            const { destroy, cleanup } = destroyer(ret, false, true);
            destroys.push(destroy);
            if (isLastStream) {
              lastStreamCleanup.push(cleanup);
            }
          }
        } else if (isNodeStream(stream)) {
          if (isReadableNodeStream(ret)) {
            finishCount += 2;
            const cleanup = pipe(ret, stream, finish, {
              end
            });
            if (isReadable(stream) && isLastStream) {
              lastStreamCleanup.push(cleanup);
            }
          } else if (isTransformStream(ret) || isReadableStream(ret)) {
            const toRead = ret.readable || ret;
            finishCount++;
            pumpToNode(toRead, stream, finish, {
              end
            });
          } else if (isIterable(ret)) {
            finishCount++;
            pumpToNode(ret, stream, finish, {
              end
            });
          } else {
            throw new ERR_INVALID_ARG_TYPE(
              "val",
              ["Readable", "Iterable", "AsyncIterable", "ReadableStream", "TransformStream"],
              ret
            );
          }
          ret = stream;
        } else if (isWebStream(stream)) {
          if (isReadableNodeStream(ret)) {
            finishCount++;
            pumpToWeb(makeAsyncIterable(ret), stream, finish, {
              end
            });
          } else if (isReadableStream(ret) || isIterable(ret)) {
            finishCount++;
            pumpToWeb(ret, stream, finish, {
              end
            });
          } else if (isTransformStream(ret)) {
            finishCount++;
            pumpToWeb(ret.readable, stream, finish, {
              end
            });
          } else {
            throw new ERR_INVALID_ARG_TYPE(
              "val",
              ["Readable", "Iterable", "AsyncIterable", "ReadableStream", "TransformStream"],
              ret
            );
          }
          ret = stream;
        } else {
          ret = Duplex.from(stream);
        }
      }
      if (signal !== null && signal !== void 0 && signal.aborted || outerSignal !== null && outerSignal !== void 0 && outerSignal.aborted) {
        process2.nextTick(abort);
      }
      return ret;
    }
    function pipe(src, dst, finish, { end }) {
      let ended = false;
      dst.on("close", () => {
        if (!ended) {
          finish(new ERR_STREAM_PREMATURE_CLOSE());
        }
      });
      src.pipe(dst, {
        end: false
      });
      if (end) {
        let endFn2 = function() {
          ended = true;
          dst.end();
        };
        var endFn = endFn2;
        if (isReadableFinished(src)) {
          process2.nextTick(endFn2);
        } else {
          src.once("end", endFn2);
        }
      } else {
        finish();
      }
      eos(
        src,
        {
          readable: true,
          writable: false
        },
        (err) => {
          const rState = src._readableState;
          if (err && err.code === "ERR_STREAM_PREMATURE_CLOSE" && rState && rState.ended && !rState.errored && !rState.errorEmitted) {
            src.once("end", finish).once("error", finish);
          } else {
            finish(err);
          }
        }
      );
      return eos(
        dst,
        {
          readable: false,
          writable: true
        },
        finish
      );
    }
    module2.exports = {
      pipelineImpl,
      pipeline
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/compose.js
var require_compose = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/compose.js"(exports2, module2) {
    "use strict";
    var { pipeline } = require_pipeline();
    var Duplex = require_duplex();
    var { destroyer } = require_destroy();
    var {
      isNodeStream,
      isReadable,
      isWritable,
      isWebStream,
      isTransformStream,
      isWritableStream,
      isReadableStream
    } = require_utils5();
    var {
      AbortError,
      codes: { ERR_INVALID_ARG_VALUE, ERR_MISSING_ARGS }
    } = require_errors();
    var eos = require_end_of_stream();
    module2.exports = function compose(...streams) {
      if (streams.length === 0) {
        throw new ERR_MISSING_ARGS("streams");
      }
      if (streams.length === 1) {
        return Duplex.from(streams[0]);
      }
      const orgStreams = [...streams];
      if (typeof streams[0] === "function") {
        streams[0] = Duplex.from(streams[0]);
      }
      if (typeof streams[streams.length - 1] === "function") {
        const idx = streams.length - 1;
        streams[idx] = Duplex.from(streams[idx]);
      }
      for (let n = 0; n < streams.length; ++n) {
        if (!isNodeStream(streams[n]) && !isWebStream(streams[n])) {
          continue;
        }
        if (n < streams.length - 1 && !(isReadable(streams[n]) || isReadableStream(streams[n]) || isTransformStream(streams[n]))) {
          throw new ERR_INVALID_ARG_VALUE(`streams[${n}]`, orgStreams[n], "must be readable");
        }
        if (n > 0 && !(isWritable(streams[n]) || isWritableStream(streams[n]) || isTransformStream(streams[n]))) {
          throw new ERR_INVALID_ARG_VALUE(`streams[${n}]`, orgStreams[n], "must be writable");
        }
      }
      let ondrain;
      let onfinish;
      let onreadable;
      let onclose;
      let d;
      function onfinished(err) {
        const cb = onclose;
        onclose = null;
        if (cb) {
          cb(err);
        } else if (err) {
          d.destroy(err);
        } else if (!readable && !writable) {
          d.destroy();
        }
      }
      const head = streams[0];
      const tail = pipeline(streams, onfinished);
      const writable = !!(isWritable(head) || isWritableStream(head) || isTransformStream(head));
      const readable = !!(isReadable(tail) || isReadableStream(tail) || isTransformStream(tail));
      d = new Duplex({
        // TODO (ronag): highWaterMark?
        writableObjectMode: !!(head !== null && head !== void 0 && head.writableObjectMode),
        readableObjectMode: !!(tail !== null && tail !== void 0 && tail.readableObjectMode),
        writable,
        readable
      });
      if (writable) {
        if (isNodeStream(head)) {
          d._write = function(chunk, encoding, callback) {
            if (head.write(chunk, encoding)) {
              callback();
            } else {
              ondrain = callback;
            }
          };
          d._final = function(callback) {
            head.end();
            onfinish = callback;
          };
          head.on("drain", function() {
            if (ondrain) {
              const cb = ondrain;
              ondrain = null;
              cb();
            }
          });
        } else if (isWebStream(head)) {
          const writable2 = isTransformStream(head) ? head.writable : head;
          const writer = writable2.getWriter();
          d._write = async function(chunk, encoding, callback) {
            try {
              await writer.ready;
              writer.write(chunk).catch(() => {
              });
              callback();
            } catch (err) {
              callback(err);
            }
          };
          d._final = async function(callback) {
            try {
              await writer.ready;
              writer.close().catch(() => {
              });
              onfinish = callback;
            } catch (err) {
              callback(err);
            }
          };
        }
        const toRead = isTransformStream(tail) ? tail.readable : tail;
        eos(toRead, () => {
          if (onfinish) {
            const cb = onfinish;
            onfinish = null;
            cb();
          }
        });
      }
      if (readable) {
        if (isNodeStream(tail)) {
          tail.on("readable", function() {
            if (onreadable) {
              const cb = onreadable;
              onreadable = null;
              cb();
            }
          });
          tail.on("end", function() {
            d.push(null);
          });
          d._read = function() {
            while (true) {
              const buf = tail.read();
              if (buf === null) {
                onreadable = d._read;
                return;
              }
              if (!d.push(buf)) {
                return;
              }
            }
          };
        } else if (isWebStream(tail)) {
          const readable2 = isTransformStream(tail) ? tail.readable : tail;
          const reader = readable2.getReader();
          d._read = async function() {
            while (true) {
              try {
                const { value, done } = await reader.read();
                if (!d.push(value)) {
                  return;
                }
                if (done) {
                  d.push(null);
                  return;
                }
              } catch {
                return;
              }
            }
          };
        }
      }
      d._destroy = function(err, callback) {
        if (!err && onclose !== null) {
          err = new AbortError();
        }
        onreadable = null;
        ondrain = null;
        onfinish = null;
        if (onclose === null) {
          callback(err);
        } else {
          onclose = callback;
          if (isNodeStream(tail)) {
            destroyer(tail, err);
          }
        }
      };
      return d;
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/operators.js
var require_operators = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/internal/streams/operators.js"(exports2, module2) {
    "use strict";
    var AbortController = globalThis.AbortController || require_abort_controller().AbortController;
    var {
      codes: { ERR_INVALID_ARG_VALUE, ERR_INVALID_ARG_TYPE, ERR_MISSING_ARGS, ERR_OUT_OF_RANGE },
      AbortError
    } = require_errors();
    var { validateAbortSignal, validateInteger, validateObject } = require_validators();
    var kWeakHandler = require_primordials().Symbol("kWeak");
    var kResistStopPropagation = require_primordials().Symbol("kResistStopPropagation");
    var { finished } = require_end_of_stream();
    var staticCompose = require_compose();
    var { addAbortSignalNoValidate } = require_add_abort_signal();
    var { isWritable, isNodeStream } = require_utils5();
    var { deprecate } = require_util2();
    var {
      ArrayPrototypePush,
      Boolean: Boolean2,
      MathFloor,
      Number: Number2,
      NumberIsNaN,
      Promise: Promise2,
      PromiseReject,
      PromiseResolve,
      PromisePrototypeThen,
      Symbol: Symbol2
    } = require_primordials();
    var kEmpty = Symbol2("kEmpty");
    var kEof = Symbol2("kEof");
    function compose(stream, options) {
      if (options != null) {
        validateObject(options, "options");
      }
      if ((options === null || options === void 0 ? void 0 : options.signal) != null) {
        validateAbortSignal(options.signal, "options.signal");
      }
      if (isNodeStream(stream) && !isWritable(stream)) {
        throw new ERR_INVALID_ARG_VALUE("stream", stream, "must be writable");
      }
      const composedStream = staticCompose(this, stream);
      if (options !== null && options !== void 0 && options.signal) {
        addAbortSignalNoValidate(options.signal, composedStream);
      }
      return composedStream;
    }
    function map(fn, options) {
      if (typeof fn !== "function") {
        throw new ERR_INVALID_ARG_TYPE("fn", ["Function", "AsyncFunction"], fn);
      }
      if (options != null) {
        validateObject(options, "options");
      }
      if ((options === null || options === void 0 ? void 0 : options.signal) != null) {
        validateAbortSignal(options.signal, "options.signal");
      }
      let concurrency = 1;
      if ((options === null || options === void 0 ? void 0 : options.concurrency) != null) {
        concurrency = MathFloor(options.concurrency);
      }
      let highWaterMark = concurrency - 1;
      if ((options === null || options === void 0 ? void 0 : options.highWaterMark) != null) {
        highWaterMark = MathFloor(options.highWaterMark);
      }
      validateInteger(concurrency, "options.concurrency", 1);
      validateInteger(highWaterMark, "options.highWaterMark", 0);
      highWaterMark += concurrency;
      return async function* map2() {
        const signal = require_util2().AbortSignalAny(
          [options === null || options === void 0 ? void 0 : options.signal].filter(Boolean2)
        );
        const stream = this;
        const queue = [];
        const signalOpt = {
          signal
        };
        let next;
        let resume;
        let done = false;
        let cnt = 0;
        function onCatch() {
          done = true;
          afterItemProcessed();
        }
        function afterItemProcessed() {
          cnt -= 1;
          maybeResume();
        }
        function maybeResume() {
          if (resume && !done && cnt < concurrency && queue.length < highWaterMark) {
            resume();
            resume = null;
          }
        }
        async function pump() {
          try {
            for await (let val of stream) {
              if (done) {
                return;
              }
              if (signal.aborted) {
                throw new AbortError();
              }
              try {
                val = fn(val, signalOpt);
                if (val === kEmpty) {
                  continue;
                }
                val = PromiseResolve(val);
              } catch (err) {
                val = PromiseReject(err);
              }
              cnt += 1;
              PromisePrototypeThen(val, afterItemProcessed, onCatch);
              queue.push(val);
              if (next) {
                next();
                next = null;
              }
              if (!done && (queue.length >= highWaterMark || cnt >= concurrency)) {
                await new Promise2((resolve) => {
                  resume = resolve;
                });
              }
            }
            queue.push(kEof);
          } catch (err) {
            const val = PromiseReject(err);
            PromisePrototypeThen(val, afterItemProcessed, onCatch);
            queue.push(val);
          } finally {
            done = true;
            if (next) {
              next();
              next = null;
            }
          }
        }
        pump();
        try {
          while (true) {
            while (queue.length > 0) {
              const val = await queue[0];
              if (val === kEof) {
                return;
              }
              if (signal.aborted) {
                throw new AbortError();
              }
              if (val !== kEmpty) {
                yield val;
              }
              queue.shift();
              maybeResume();
            }
            await new Promise2((resolve) => {
              next = resolve;
            });
          }
        } finally {
          done = true;
          if (resume) {
            resume();
            resume = null;
          }
        }
      }.call(this);
    }
    function asIndexedPairs(options = void 0) {
      if (options != null) {
        validateObject(options, "options");
      }
      if ((options === null || options === void 0 ? void 0 : options.signal) != null) {
        validateAbortSignal(options.signal, "options.signal");
      }
      return async function* asIndexedPairs2() {
        let index = 0;
        for await (const val of this) {
          var _options$signal;
          if (options !== null && options !== void 0 && (_options$signal = options.signal) !== null && _options$signal !== void 0 && _options$signal.aborted) {
            throw new AbortError({
              cause: options.signal.reason
            });
          }
          yield [index++, val];
        }
      }.call(this);
    }
    async function some(fn, options = void 0) {
      for await (const unused of filter.call(this, fn, options)) {
        return true;
      }
      return false;
    }
    async function every(fn, options = void 0) {
      if (typeof fn !== "function") {
        throw new ERR_INVALID_ARG_TYPE("fn", ["Function", "AsyncFunction"], fn);
      }
      return !await some.call(
        this,
        async (...args) => {
          return !await fn(...args);
        },
        options
      );
    }
    async function find(fn, options) {
      for await (const result of filter.call(this, fn, options)) {
        return result;
      }
      return void 0;
    }
    async function forEach(fn, options) {
      if (typeof fn !== "function") {
        throw new ERR_INVALID_ARG_TYPE("fn", ["Function", "AsyncFunction"], fn);
      }
      async function forEachFn(value, options2) {
        await fn(value, options2);
        return kEmpty;
      }
      for await (const unused of map.call(this, forEachFn, options))
        ;
    }
    function filter(fn, options) {
      if (typeof fn !== "function") {
        throw new ERR_INVALID_ARG_TYPE("fn", ["Function", "AsyncFunction"], fn);
      }
      async function filterFn(value, options2) {
        if (await fn(value, options2)) {
          return value;
        }
        return kEmpty;
      }
      return map.call(this, filterFn, options);
    }
    var ReduceAwareErrMissingArgs = class extends ERR_MISSING_ARGS {
      constructor() {
        super("reduce");
        this.message = "Reduce of an empty stream requires an initial value";
      }
    };
    async function reduce(reducer, initialValue, options) {
      var _options$signal2;
      if (typeof reducer !== "function") {
        throw new ERR_INVALID_ARG_TYPE("reducer", ["Function", "AsyncFunction"], reducer);
      }
      if (options != null) {
        validateObject(options, "options");
      }
      if ((options === null || options === void 0 ? void 0 : options.signal) != null) {
        validateAbortSignal(options.signal, "options.signal");
      }
      let hasInitialValue = arguments.length > 1;
      if (options !== null && options !== void 0 && (_options$signal2 = options.signal) !== null && _options$signal2 !== void 0 && _options$signal2.aborted) {
        const err = new AbortError(void 0, {
          cause: options.signal.reason
        });
        this.once("error", () => {
        });
        await finished(this.destroy(err));
        throw err;
      }
      const ac = new AbortController();
      const signal = ac.signal;
      if (options !== null && options !== void 0 && options.signal) {
        const opts = {
          once: true,
          [kWeakHandler]: this,
          [kResistStopPropagation]: true
        };
        options.signal.addEventListener("abort", () => ac.abort(), opts);
      }
      let gotAnyItemFromStream = false;
      try {
        for await (const value of this) {
          var _options$signal3;
          gotAnyItemFromStream = true;
          if (options !== null && options !== void 0 && (_options$signal3 = options.signal) !== null && _options$signal3 !== void 0 && _options$signal3.aborted) {
            throw new AbortError();
          }
          if (!hasInitialValue) {
            initialValue = value;
            hasInitialValue = true;
          } else {
            initialValue = await reducer(initialValue, value, {
              signal
            });
          }
        }
        if (!gotAnyItemFromStream && !hasInitialValue) {
          throw new ReduceAwareErrMissingArgs();
        }
      } finally {
        ac.abort();
      }
      return initialValue;
    }
    async function toArray(options) {
      if (options != null) {
        validateObject(options, "options");
      }
      if ((options === null || options === void 0 ? void 0 : options.signal) != null) {
        validateAbortSignal(options.signal, "options.signal");
      }
      const result = [];
      for await (const val of this) {
        var _options$signal4;
        if (options !== null && options !== void 0 && (_options$signal4 = options.signal) !== null && _options$signal4 !== void 0 && _options$signal4.aborted) {
          throw new AbortError(void 0, {
            cause: options.signal.reason
          });
        }
        ArrayPrototypePush(result, val);
      }
      return result;
    }
    function flatMap(fn, options) {
      const values = map.call(this, fn, options);
      return async function* flatMap2() {
        for await (const val of values) {
          yield* val;
        }
      }.call(this);
    }
    function toIntegerOrInfinity(number) {
      number = Number2(number);
      if (NumberIsNaN(number)) {
        return 0;
      }
      if (number < 0) {
        throw new ERR_OUT_OF_RANGE("number", ">= 0", number);
      }
      return number;
    }
    function drop(number, options = void 0) {
      if (options != null) {
        validateObject(options, "options");
      }
      if ((options === null || options === void 0 ? void 0 : options.signal) != null) {
        validateAbortSignal(options.signal, "options.signal");
      }
      number = toIntegerOrInfinity(number);
      return async function* drop2() {
        var _options$signal5;
        if (options !== null && options !== void 0 && (_options$signal5 = options.signal) !== null && _options$signal5 !== void 0 && _options$signal5.aborted) {
          throw new AbortError();
        }
        for await (const val of this) {
          var _options$signal6;
          if (options !== null && options !== void 0 && (_options$signal6 = options.signal) !== null && _options$signal6 !== void 0 && _options$signal6.aborted) {
            throw new AbortError();
          }
          if (number-- <= 0) {
            yield val;
          }
        }
      }.call(this);
    }
    function take(number, options = void 0) {
      if (options != null) {
        validateObject(options, "options");
      }
      if ((options === null || options === void 0 ? void 0 : options.signal) != null) {
        validateAbortSignal(options.signal, "options.signal");
      }
      number = toIntegerOrInfinity(number);
      return async function* take2() {
        var _options$signal7;
        if (options !== null && options !== void 0 && (_options$signal7 = options.signal) !== null && _options$signal7 !== void 0 && _options$signal7.aborted) {
          throw new AbortError();
        }
        for await (const val of this) {
          var _options$signal8;
          if (options !== null && options !== void 0 && (_options$signal8 = options.signal) !== null && _options$signal8 !== void 0 && _options$signal8.aborted) {
            throw new AbortError();
          }
          if (number-- > 0) {
            yield val;
          }
          if (number <= 0) {
            return;
          }
        }
      }.call(this);
    }
    module2.exports.streamReturningOperators = {
      asIndexedPairs: deprecate(asIndexedPairs, "readable.asIndexedPairs will be removed in a future version."),
      drop,
      filter,
      flatMap,
      map,
      take,
      compose
    };
    module2.exports.promiseReturningOperators = {
      every,
      forEach,
      reduce,
      toArray,
      some,
      find
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/stream/promises.js
var require_promises = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/stream/promises.js"(exports2, module2) {
    "use strict";
    var { ArrayPrototypePop, Promise: Promise2 } = require_primordials();
    var { isIterable, isNodeStream, isWebStream } = require_utils5();
    var { pipelineImpl: pl } = require_pipeline();
    var { finished } = require_end_of_stream();
    require_stream();
    function pipeline(...streams) {
      return new Promise2((resolve, reject) => {
        let signal;
        let end;
        const lastArg = streams[streams.length - 1];
        if (lastArg && typeof lastArg === "object" && !isNodeStream(lastArg) && !isIterable(lastArg) && !isWebStream(lastArg)) {
          const options = ArrayPrototypePop(streams);
          signal = options.signal;
          end = options.end;
        }
        pl(
          streams,
          (err, value) => {
            if (err) {
              reject(err);
            } else {
              resolve(value);
            }
          },
          {
            signal,
            end
          }
        );
      });
    }
    module2.exports = {
      finished,
      pipeline
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/stream.js
var require_stream = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/stream.js"(exports2, module2) {
    "use strict";
    var { Buffer: Buffer2 } = require("buffer");
    var { ObjectDefineProperty, ObjectKeys, ReflectApply } = require_primordials();
    var {
      promisify: { custom: customPromisify }
    } = require_util2();
    var { streamReturningOperators, promiseReturningOperators } = require_operators();
    var {
      codes: { ERR_ILLEGAL_CONSTRUCTOR }
    } = require_errors();
    var compose = require_compose();
    var { setDefaultHighWaterMark, getDefaultHighWaterMark } = require_state();
    var { pipeline } = require_pipeline();
    var { destroyer } = require_destroy();
    var eos = require_end_of_stream();
    var promises = require_promises();
    var utils2 = require_utils5();
    var Stream = module2.exports = require_legacy().Stream;
    Stream.isDestroyed = utils2.isDestroyed;
    Stream.isDisturbed = utils2.isDisturbed;
    Stream.isErrored = utils2.isErrored;
    Stream.isReadable = utils2.isReadable;
    Stream.isWritable = utils2.isWritable;
    Stream.Readable = require_readable();
    for (const key of ObjectKeys(streamReturningOperators)) {
      let fn = function(...args) {
        if (new.target) {
          throw ERR_ILLEGAL_CONSTRUCTOR();
        }
        return Stream.Readable.from(ReflectApply(op, this, args));
      };
      const op = streamReturningOperators[key];
      ObjectDefineProperty(fn, "name", {
        __proto__: null,
        value: op.name
      });
      ObjectDefineProperty(fn, "length", {
        __proto__: null,
        value: op.length
      });
      ObjectDefineProperty(Stream.Readable.prototype, key, {
        __proto__: null,
        value: fn,
        enumerable: false,
        configurable: true,
        writable: true
      });
    }
    for (const key of ObjectKeys(promiseReturningOperators)) {
      let fn = function(...args) {
        if (new.target) {
          throw ERR_ILLEGAL_CONSTRUCTOR();
        }
        return ReflectApply(op, this, args);
      };
      const op = promiseReturningOperators[key];
      ObjectDefineProperty(fn, "name", {
        __proto__: null,
        value: op.name
      });
      ObjectDefineProperty(fn, "length", {
        __proto__: null,
        value: op.length
      });
      ObjectDefineProperty(Stream.Readable.prototype, key, {
        __proto__: null,
        value: fn,
        enumerable: false,
        configurable: true,
        writable: true
      });
    }
    Stream.Writable = require_writable();
    Stream.Duplex = require_duplex();
    Stream.Transform = require_transform();
    Stream.PassThrough = require_passthrough();
    Stream.pipeline = pipeline;
    var { addAbortSignal } = require_add_abort_signal();
    Stream.addAbortSignal = addAbortSignal;
    Stream.finished = eos;
    Stream.destroy = destroyer;
    Stream.compose = compose;
    Stream.setDefaultHighWaterMark = setDefaultHighWaterMark;
    Stream.getDefaultHighWaterMark = getDefaultHighWaterMark;
    ObjectDefineProperty(Stream, "promises", {
      __proto__: null,
      configurable: true,
      enumerable: true,
      get() {
        return promises;
      }
    });
    ObjectDefineProperty(pipeline, customPromisify, {
      __proto__: null,
      enumerable: true,
      get() {
        return promises.pipeline;
      }
    });
    ObjectDefineProperty(eos, customPromisify, {
      __proto__: null,
      enumerable: true,
      get() {
        return promises.finished;
      }
    });
    Stream.Stream = Stream;
    Stream._isUint8Array = function isUint8Array(value) {
      return value instanceof Uint8Array;
    };
    Stream._uint8ArrayToBuffer = function _uint8ArrayToBuffer(chunk) {
      return Buffer2.from(chunk.buffer, chunk.byteOffset, chunk.byteLength);
    };
  }
});

// ../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/ours/index.js
var require_ours = __commonJS({
  "../node_modules/.pnpm/readable-stream@4.7.0/node_modules/readable-stream/lib/ours/index.js"(exports2, module2) {
    "use strict";
    var Stream = require("stream");
    if (Stream && process.env.READABLE_STREAM === "disable") {
      const promises = Stream.promises;
      module2.exports._uint8ArrayToBuffer = Stream._uint8ArrayToBuffer;
      module2.exports._isUint8Array = Stream._isUint8Array;
      module2.exports.isDisturbed = Stream.isDisturbed;
      module2.exports.isErrored = Stream.isErrored;
      module2.exports.isReadable = Stream.isReadable;
      module2.exports.Readable = Stream.Readable;
      module2.exports.Writable = Stream.Writable;
      module2.exports.Duplex = Stream.Duplex;
      module2.exports.Transform = Stream.Transform;
      module2.exports.PassThrough = Stream.PassThrough;
      module2.exports.addAbortSignal = Stream.addAbortSignal;
      module2.exports.finished = Stream.finished;
      module2.exports.destroy = Stream.destroy;
      module2.exports.pipeline = Stream.pipeline;
      module2.exports.compose = Stream.compose;
      Object.defineProperty(Stream, "promises", {
        configurable: true,
        enumerable: true,
        get() {
          return promises;
        }
      });
      module2.exports.Stream = Stream.Stream;
    } else {
      const CustomStream = require_stream();
      const promises = require_promises();
      const originalDestroy = CustomStream.Readable.destroy;
      module2.exports = CustomStream.Readable;
      module2.exports._uint8ArrayToBuffer = CustomStream._uint8ArrayToBuffer;
      module2.exports._isUint8Array = CustomStream._isUint8Array;
      module2.exports.isDisturbed = CustomStream.isDisturbed;
      module2.exports.isErrored = CustomStream.isErrored;
      module2.exports.isReadable = CustomStream.isReadable;
      module2.exports.Readable = CustomStream.Readable;
      module2.exports.Writable = CustomStream.Writable;
      module2.exports.Duplex = CustomStream.Duplex;
      module2.exports.Transform = CustomStream.Transform;
      module2.exports.PassThrough = CustomStream.PassThrough;
      module2.exports.addAbortSignal = CustomStream.addAbortSignal;
      module2.exports.finished = CustomStream.finished;
      module2.exports.destroy = CustomStream.destroy;
      module2.exports.destroy = originalDestroy;
      module2.exports.pipeline = CustomStream.pipeline;
      module2.exports.compose = CustomStream.compose;
      Object.defineProperty(CustomStream, "promises", {
        configurable: true,
        enumerable: true,
        get() {
          return promises;
        }
      });
      module2.exports.Stream = CustomStream.Stream;
    }
    module2.exports.default = module2.exports;
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/serializer.js
var require_serializer = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/serializer.js"(exports2, module2) {
    var Transform = require_ours().Transform;
    var Serializer = class extends Transform {
      constructor(proto, mainType) {
        super({ writableObjectMode: true });
        this.proto = proto;
        this.mainType = mainType;
        this.queue = Buffer.alloc(0);
      }
      createPacketBuffer(packet) {
        return this.proto.createPacketBuffer(this.mainType, packet);
      }
      _transform(chunk, enc, cb) {
        let buf;
        try {
          buf = this.createPacketBuffer(chunk);
        } catch (e) {
          return cb(e);
        }
        this.push(buf);
        return cb();
      }
    };
    var Parser = class extends Transform {
      constructor(proto, mainType) {
        super({ readableObjectMode: true });
        this.proto = proto;
        this.mainType = mainType;
        this.queue = Buffer.alloc(0);
      }
      parsePacketBuffer(buffer) {
        return this.proto.parsePacketBuffer(this.mainType, buffer);
      }
      _transform(chunk, enc, cb) {
        this.queue = Buffer.concat([this.queue, chunk]);
        while (true) {
          let packet;
          try {
            packet = this.parsePacketBuffer(this.queue);
          } catch (e) {
            if (e.partialReadError) {
              return cb();
            } else {
              e.buffer = this.queue;
              this.queue = Buffer.alloc(0);
              return cb(e);
            }
          }
          this.push(packet);
          this.queue = this.queue.slice(packet.metadata.size);
        }
      }
    };
    var FullPacketParser = class extends Transform {
      constructor(proto, mainType, noErrorLogging = false) {
        super({ readableObjectMode: true });
        this.proto = proto;
        this.mainType = mainType;
        this.noErrorLogging = noErrorLogging;
      }
      parsePacketBuffer(buffer) {
        return this.proto.parsePacketBuffer(this.mainType, buffer);
      }
      _transform(chunk, enc, cb) {
        let packet;
        try {
          packet = this.parsePacketBuffer(chunk);
          if (packet.metadata.size !== chunk.length && !this.noErrorLogging) {
            console.log("Chunk size is " + chunk.length + " but only " + packet.metadata.size + " was read ; partial packet : " + JSON.stringify(packet.data) + "; buffer :" + chunk.toString("hex"));
          }
        } catch (e) {
          if (e.partialReadError) {
            if (!this.noErrorLogging) {
              console.log(e.stack);
            }
            return cb();
          } else {
            return cb(e);
          }
        }
        this.push(packet);
        cb();
      }
    };
    module2.exports = {
      Serializer,
      Parser,
      FullPacketParser
    };
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/compiler-conditional.js
var require_compiler_conditional = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/compiler-conditional.js"(exports2, module2) {
    module2.exports = {
      Read: {
        switch: ["parametrizable", (compiler, struct) => {
          let compare = struct.compareTo ? struct.compareTo : struct.compareToValue;
          const args = [];
          if (compare.startsWith("$"))
            args.push(compare);
          else if (struct.compareTo) {
            compare = compiler.getField(compare, true);
          }
          let code2 = `switch (${compare}) {
`;
          for (const key in struct.fields) {
            let val = key;
            if (val.startsWith("/"))
              val = "ctx." + val.slice(1);
            else if (isNaN(val) && val !== "true" && val !== "false")
              val = `"${val}"`;
            code2 += compiler.indent(`case ${val}: return ` + compiler.callType(struct.fields[key])) + "\n";
          }
          code2 += compiler.indent("default: return " + compiler.callType(struct.default ? struct.default : "void")) + "\n";
          code2 += "}";
          return compiler.wrapCode(code2, args);
        }],
        option: ["parametrizable", (compiler, type) => {
          let code2 = "const {value} = ctx.bool(buffer, offset)\n";
          code2 += "if (value) {\n";
          code2 += "  const { value, size } = " + compiler.callType(type, "offset + 1") + "\n";
          code2 += "  return { value, size: size + 1 }\n";
          code2 += "}\n";
          code2 += "return { value: undefined, size: 1}";
          return compiler.wrapCode(code2);
        }]
      },
      Write: {
        switch: ["parametrizable", (compiler, struct) => {
          let compare = struct.compareTo ? struct.compareTo : struct.compareToValue;
          const args = [];
          if (compare.startsWith("$"))
            args.push(compare);
          else if (struct.compareTo) {
            compare = compiler.getField(compare, true);
          }
          let code2 = `switch (${compare}) {
`;
          for (const key in struct.fields) {
            let val = key;
            if (val.startsWith("/"))
              val = "ctx." + val.slice(1);
            else if (isNaN(val) && val !== "true" && val !== "false")
              val = `"${val}"`;
            code2 += compiler.indent(`case ${val}: return ` + compiler.callType("value", struct.fields[key])) + "\n";
          }
          code2 += compiler.indent("default: return " + compiler.callType("value", struct.default ? struct.default : "void")) + "\n";
          code2 += "}";
          return compiler.wrapCode(code2, args);
        }],
        option: ["parametrizable", (compiler, type) => {
          let code2 = "if (value != null) {\n";
          code2 += "  offset = ctx.bool(1, buffer, offset)\n";
          code2 += "  offset = " + compiler.callType("value", type) + "\n";
          code2 += "} else {\n";
          code2 += "  offset = ctx.bool(0, buffer, offset)\n";
          code2 += "}\n";
          code2 += "return offset";
          return compiler.wrapCode(code2);
        }]
      },
      SizeOf: {
        switch: ["parametrizable", (compiler, struct) => {
          let compare = struct.compareTo ? struct.compareTo : struct.compareToValue;
          const args = [];
          if (compare.startsWith("$"))
            args.push(compare);
          else if (struct.compareTo) {
            compare = compiler.getField(compare, true);
          }
          let code2 = `switch (${compare}) {
`;
          for (const key in struct.fields) {
            let val = key;
            if (val.startsWith("/"))
              val = "ctx." + val.slice(1);
            else if (isNaN(val) && val !== "true" && val !== "false")
              val = `"${val}"`;
            code2 += compiler.indent(`case ${val}: return ` + compiler.callType("value", struct.fields[key])) + "\n";
          }
          code2 += compiler.indent("default: return " + compiler.callType("value", struct.default ? struct.default : "void")) + "\n";
          code2 += "}";
          return compiler.wrapCode(code2, args);
        }],
        option: ["parametrizable", (compiler, type) => {
          let code2 = "if (value != null) {\n";
          code2 += "  return 1 + " + compiler.callType("value", type) + "\n";
          code2 += "}\n";
          code2 += "return 1";
          return compiler.wrapCode(code2);
        }]
      }
    };
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/compiler-structures.js
var require_compiler_structures = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/compiler-structures.js"(exports2, module2) {
    module2.exports = {
      Read: {
        array: ["parametrizable", (compiler, array) => {
          let code2 = "";
          if (array.countType) {
            code2 += "const { value: count, size: countSize } = " + compiler.callType(array.countType) + "\n";
          } else if (array.count) {
            code2 += "const count = " + array.count + "\n";
            code2 += "const countSize = 0\n";
          } else {
            throw new Error("Array must contain either count or countType");
          }
          code2 += 'if (count > 0xffffff && !ctx.noArraySizeCheck) throw new Error("array size is abnormally large, not reading: " + count)\n';
          code2 += "const data = []\n";
          code2 += "let size = countSize\n";
          code2 += "for (let i = 0; i < count; i++) {\n";
          code2 += "  const elem = " + compiler.callType(array.type, "offset + size") + "\n";
          code2 += "  data.push(elem.value)\n";
          code2 += "  size += elem.size\n";
          code2 += "}\n";
          code2 += "return { value: data, size }";
          return compiler.wrapCode(code2);
        }],
        count: ["parametrizable", (compiler, type) => {
          const code2 = "return " + compiler.callType(type.type);
          return compiler.wrapCode(code2);
        }],
        container: ["parametrizable", (compiler, values) => {
          values = containerInlining(values);
          let code2 = "";
          let offsetExpr = "offset";
          const names = [];
          for (const i in values) {
            const { type, name, anon, _shouldBeInlined } = values[i];
            let trueName;
            let sizeName;
            if (type instanceof Array && type[0] === "bitfield" && anon) {
              const subnames = [];
              for (const { name: name2 } of type[1]) {
                const trueName2 = compiler.getField(name2);
                if (name2 === trueName2) {
                  names.push(name2);
                  subnames.push(name2);
                } else {
                  names.push(`${name2}: ${trueName2}`);
                  subnames.push(`${name2}: ${trueName2}`);
                }
              }
              trueName = "{" + subnames.join(", ") + "}";
              sizeName = `anon${i}Size`;
            } else {
              trueName = compiler.getField(name);
              sizeName = `${trueName}Size`;
              if (_shouldBeInlined)
                names.push("..." + name);
              else if (name === trueName)
                names.push(name);
              else
                names.push(`${name}: ${trueName}`);
            }
            code2 += `let { value: ${trueName}, size: ${sizeName} } = ` + compiler.callType(type, offsetExpr) + "\n";
            offsetExpr += ` + ${sizeName}`;
          }
          const sizes = offsetExpr.split(" + ");
          sizes.shift();
          if (sizes.length === 0)
            sizes.push("0");
          code2 += "return { value: { " + names.join(", ") + " }, size: " + sizes.join(" + ") + "}";
          return compiler.wrapCode(code2);
        }]
      },
      Write: {
        array: ["parametrizable", (compiler, array) => {
          let code2 = "";
          if (array.countType) {
            code2 += "offset = " + compiler.callType("value.length", array.countType) + "\n";
          } else if (array.count === null) {
            throw new Error("Array must contain either count or countType");
          }
          code2 += "for (let i = 0; i < value.length; i++) {\n";
          code2 += "  offset = " + compiler.callType("value[i]", array.type) + "\n";
          code2 += "}\n";
          code2 += "return offset";
          return compiler.wrapCode(code2);
        }],
        count: ["parametrizable", (compiler, type) => {
          const code2 = "return " + compiler.callType("value", type.type);
          return compiler.wrapCode(code2);
        }],
        container: ["parametrizable", (compiler, values) => {
          values = containerInlining(values);
          let code2 = "";
          for (const i in values) {
            const { type, name, anon, _shouldBeInlined } = values[i];
            let trueName;
            if (type instanceof Array && type[0] === "bitfield" && anon) {
              const names = [];
              for (const { name: name2 } of type[1]) {
                const trueName2 = compiler.getField(name2);
                code2 += `const ${trueName2} = value.${name2}
`;
                if (name2 === trueName2)
                  names.push(name2);
                else
                  names.push(`${name2}: ${trueName2}`);
              }
              trueName = "{" + names.join(", ") + "}";
            } else {
              trueName = compiler.getField(name);
              if (_shouldBeInlined)
                code2 += `let ${name} = value
`;
              else
                code2 += `let ${trueName} = value.${name}
`;
            }
            code2 += "offset = " + compiler.callType(trueName, type) + "\n";
          }
          code2 += "return offset";
          return compiler.wrapCode(code2);
        }]
      },
      SizeOf: {
        array: ["parametrizable", (compiler, array) => {
          let code2 = "";
          if (array.countType) {
            code2 += "let size = " + compiler.callType("value.length", array.countType) + "\n";
          } else if (array.count) {
            code2 += "let size = 0\n";
          } else {
            throw new Error("Array must contain either count or countType");
          }
          if (!isNaN(compiler.callType("value[i]", array.type))) {
            code2 += "size += value.length * " + compiler.callType("value[i]", array.type) + "\n";
          } else {
            code2 += "for (let i = 0; i < value.length; i++) {\n";
            code2 += "  size += " + compiler.callType("value[i]", array.type) + "\n";
            code2 += "}\n";
          }
          code2 += "return size";
          return compiler.wrapCode(code2);
        }],
        count: ["parametrizable", (compiler, type) => {
          const code2 = "return " + compiler.callType("value", type.type);
          return compiler.wrapCode(code2);
        }],
        container: ["parametrizable", (compiler, values) => {
          values = containerInlining(values);
          let code2 = "let size = 0\n";
          for (const i in values) {
            const { type, name, anon, _shouldBeInlined } = values[i];
            let trueName;
            if (type instanceof Array && type[0] === "bitfield" && anon) {
              const names = [];
              for (const { name: name2 } of type[1]) {
                const trueName2 = compiler.getField(name2);
                code2 += `const ${trueName2} = value.${name2}
`;
                if (name2 === trueName2)
                  names.push(name2);
                else
                  names.push(`${name2}: ${trueName2}`);
              }
              trueName = "{" + names.join(", ") + "}";
            } else {
              trueName = compiler.getField(name);
              if (_shouldBeInlined)
                code2 += `let ${name} = value
`;
              else
                code2 += `let ${trueName} = value.${name}
`;
            }
            code2 += "size += " + compiler.callType(trueName, type) + "\n";
          }
          code2 += "return size";
          return compiler.wrapCode(code2);
        }]
      }
    };
    function uniqueId() {
      return "_" + Math.random().toString(36).substr(2, 9);
    }
    function containerInlining(values) {
      const newValues = [];
      for (const i in values) {
        const { type, anon } = values[i];
        if (anon && !(type instanceof Array && type[0] === "bitfield")) {
          if (type instanceof Array && type[0] === "container") {
            for (const j in type[1])
              newValues.push(type[1][j]);
          } else if (type instanceof Array && type[0] === "switch") {
            newValues.push({
              name: uniqueId(),
              _shouldBeInlined: true,
              type
            });
          } else {
            throw new Error("Cannot inline anonymous type: " + type);
          }
        } else {
          newValues.push(values[i]);
        }
      }
      return newValues;
    }
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/compiler-utils.js
var require_compiler_utils = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/datatypes/compiler-utils.js"(exports2, module2) {
    module2.exports = {
      Read: {
        pstring: ["parametrizable", (compiler, string) => {
          let code2 = "";
          if (string.countType) {
            code2 += "const { value: count, size: countSize } = " + compiler.callType(string.countType) + "\n";
          } else if (string.count) {
            code2 += "const count = " + string.count + "\n";
            code2 += "const countSize = 0\n";
          } else {
            throw new Error("pstring must contain either count or countType");
          }
          code2 += "offset += countSize\n";
          code2 += "if (offset + count > buffer.length) {\n";
          code2 += '  throw new PartialReadError("Missing characters in string, found size is " + buffer.length + " expected size was " + (offset + count))\n';
          code2 += "}\n";
          code2 += `return { value: buffer.toString("${string.encoding || "utf8"}", offset, offset + count), size: count + countSize }`;
          return compiler.wrapCode(code2);
        }],
        buffer: ["parametrizable", (compiler, buffer) => {
          let code2 = "";
          if (buffer.countType) {
            code2 += "const { value: count, size: countSize } = " + compiler.callType(buffer.countType) + "\n";
          } else if (buffer.count) {
            code2 += "const count = " + buffer.count + "\n";
            code2 += "const countSize = 0\n";
          } else {
            throw new Error("buffer must contain either count or countType");
          }
          code2 += "offset += countSize\n";
          code2 += "if (offset + count > buffer.length) {\n";
          code2 += "  throw new PartialReadError()\n";
          code2 += "}\n";
          code2 += "return { value: buffer.slice(offset, offset + count), size: count + countSize }";
          return compiler.wrapCode(code2);
        }],
        bitfield: ["parametrizable", (compiler, values) => {
          let code2 = "";
          const totalBytes = Math.ceil(values.reduce((acc, { size }) => acc + size, 0) / 8);
          code2 += `if ( offset + ${totalBytes} > buffer.length) { throw new PartialReadError() }
`;
          const names = [];
          let totalSize = 8;
          code2 += "let bits = buffer[offset++]\n";
          for (const i in values) {
            const { name, size, signed } = values[i];
            const trueName = compiler.getField(name);
            while (totalSize < size) {
              totalSize += 8;
              code2 += "bits = (bits << 8) | buffer[offset++]\n";
            }
            code2 += `let ${trueName} = (bits >> ` + (totalSize - size) + ") & 0x" + ((1 << size) - 1).toString(16) + "\n";
            if (signed)
              code2 += `${trueName} -= (${trueName} & 0x` + (1 << size - 1).toString(16) + ") << 1\n";
            totalSize -= size;
            if (name === trueName)
              names.push(name);
            else
              names.push(`${name}: ${trueName}`);
          }
          code2 += "return { value: { " + names.join(", ") + ` }, size: ${totalBytes} }`;
          return compiler.wrapCode(code2);
        }],
        bitflags: ["parametrizable", (compiler, { type, flags, shift, big }) => {
          let fstr = JSON.stringify(flags);
          if (Array.isArray(flags)) {
            fstr = "{";
            for (const [k, v] of Object.entries(flags))
              fstr += `"${v}": ${big ? 1n << BigInt(k) : 1 << k}` + (big ? "n," : ",");
            fstr += "}";
          } else if (shift) {
            fstr = "{";
            for (const key in flags)
              fstr += `"${key}": ${1 << flags[key]}${big ? "n," : ","}`;
            fstr += "}";
          }
          return compiler.wrapCode(`
const { value: _value, size } = ${compiler.callType(type, "offset")}
const value = { _value }
const flags = ${fstr}
for (const key in flags) {
  value[key] = (_value & flags[key]) == flags[key]
}
return { value, size }
      `.trim());
        }],
        mapper: ["parametrizable", (compiler, mapper) => {
          let code2 = "const { value, size } = " + compiler.callType(mapper.type) + "\n";
          code2 += "return { value: " + JSON.stringify(sanitizeMappings(mapper.mappings)) + "[value] || value, size }";
          return compiler.wrapCode(code2);
        }]
      },
      Write: {
        pstring: ["parametrizable", (compiler, string) => {
          let code2 = `const length = Buffer.byteLength(value, "${string.encoding || "utf8"}")
`;
          if (string.countType) {
            code2 += "offset = " + compiler.callType("length", string.countType) + "\n";
          } else if (string.count === null) {
            throw new Error("pstring must contain either count or countType");
          }
          code2 += `buffer.write(value, offset, length, "${string.encoding || "utf8"}")
`;
          code2 += "return offset + length";
          return compiler.wrapCode(code2);
        }],
        buffer: ["parametrizable", (compiler, buffer) => {
          let code2 = "if (!(value instanceof Buffer)) value = Buffer.from(value)\n";
          if (buffer.countType) {
            code2 += "offset = " + compiler.callType("value.length", buffer.countType) + "\n";
          } else if (buffer.count === null) {
            throw new Error("buffer must contain either count or countType");
          }
          code2 += "value.copy(buffer, offset)\n";
          code2 += "return offset + value.length";
          return compiler.wrapCode(code2);
        }],
        bitfield: ["parametrizable", (compiler, values) => {
          let toWrite = "";
          let bits = 0;
          let code2 = "";
          for (const i in values) {
            let { name, size } = values[i];
            const trueName = compiler.getField(name);
            code2 += `let ${trueName} = value.${name}
`;
            while (size > 0) {
              const writeBits = Math.min(8 - bits, size);
              const mask = (1 << writeBits) - 1;
              if (toWrite !== "")
                toWrite = `((${toWrite}) << ${writeBits}) | `;
              toWrite += `((${trueName} >> ` + (size - writeBits) + ") & 0x" + mask.toString(16) + ")";
              size -= writeBits;
              bits += writeBits;
              if (bits === 8) {
                code2 += "buffer[offset++] = " + toWrite + "\n";
                bits = 0;
                toWrite = "";
              }
            }
          }
          if (bits !== 0) {
            code2 += "buffer[offset++] = (" + toWrite + ") << " + (8 - bits) + "\n";
          }
          code2 += "return offset";
          return compiler.wrapCode(code2);
        }],
        bitflags: ["parametrizable", (compiler, { type, flags, shift, big }) => {
          let fstr = JSON.stringify(flags);
          if (Array.isArray(flags)) {
            fstr = "{";
            for (const [k, v] of Object.entries(flags))
              fstr += `"${v}": ${big ? 1n << BigInt(k) : 1 << k}` + (big ? "n," : ",");
            fstr += "}";
          } else if (shift) {
            fstr = "{";
            for (const key in flags)
              fstr += `"${key}": ${1 << flags[key]}${big ? "n," : ","}`;
            fstr += "}";
          }
          return compiler.wrapCode(`
const flags = ${fstr}
let val = value._value ${big ? "|| 0n" : ""}
for (const key in flags) {
  if (value[key]) val |= flags[key]
}
return (ctx.${type})(val, buffer, offset)
      `.trim());
        }],
        mapper: ["parametrizable", (compiler, mapper) => {
          const mappings = JSON.stringify(swapMappings(mapper.mappings));
          const code2 = "return " + compiler.callType(`${mappings}[value] || value`, mapper.type);
          return compiler.wrapCode(code2);
        }]
      },
      SizeOf: {
        pstring: ["parametrizable", (compiler, string) => {
          let code2 = `let size = Buffer.byteLength(value, "${string.encoding || "utf8"}")
`;
          if (string.countType) {
            code2 += "size += " + compiler.callType("size", string.countType) + "\n";
          } else if (string.count === null) {
            throw new Error("pstring must contain either count or countType");
          }
          code2 += "return size";
          return compiler.wrapCode(code2);
        }],
        buffer: ["parametrizable", (compiler, buffer) => {
          let code2 = "let size = value instanceof Buffer ? value.length : Buffer.from(value).length\n";
          if (buffer.countType) {
            code2 += "size += " + compiler.callType("size", buffer.countType) + "\n";
          } else if (buffer.count === null) {
            throw new Error("buffer must contain either count or countType");
          }
          code2 += "return size";
          return compiler.wrapCode(code2);
        }],
        bitfield: ["parametrizable", (compiler, values) => {
          const totalBytes = Math.ceil(values.reduce((acc, { size }) => acc + size, 0) / 8);
          return `${totalBytes}`;
        }],
        bitflags: ["parametrizable", (compiler, { type, flags, shift, big }) => {
          let fstr = JSON.stringify(flags);
          if (Array.isArray(flags)) {
            fstr = "{";
            for (const [k, v] of Object.entries(flags))
              fstr += `"${v}": ${big ? 1n << BigInt(k) : 1 << k}` + (big ? "n," : ",");
            fstr += "}";
          } else if (shift) {
            fstr = "{";
            for (const key in flags)
              fstr += `"${key}": ${1 << flags[key]}${big ? "n," : ","}`;
            fstr += "}";
          }
          return compiler.wrapCode(`
const flags = ${fstr}
let val = value._value ${big ? "|| 0n" : ""}
for (const key in flags) {
  if (value[key]) val |= flags[key]
}
return (ctx.${type})(val)
      `.trim());
        }],
        mapper: ["parametrizable", (compiler, mapper) => {
          const mappings = JSON.stringify(swapMappings(mapper.mappings));
          const code2 = "return " + compiler.callType(`${mappings}[value] || value`, mapper.type);
          return compiler.wrapCode(code2);
        }]
      }
    };
    function sanitizeMappings(json) {
      const ret = {};
      for (let key in json) {
        let val = json[key];
        key = hex2dec(key);
        if (!isNaN(val))
          val = Number(val);
        if (val === "true")
          val = true;
        if (val === "false")
          val = false;
        ret[key] = val;
      }
      return ret;
    }
    function swapMappings(json) {
      const ret = {};
      for (let key in json) {
        const val = json[key];
        key = hex2dec(key);
        ret[val] = isNaN(key) ? key : parseInt(key, 10);
      }
      return ret;
    }
    function hex2dec(num) {
      if (num.match(/^0x[0-9a-f]+$/i)) {
        return parseInt(num.substring(2), 16);
      }
      return num;
    }
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/compiler.js
var require_compiler = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/compiler.js"(exports, module) {
    var numeric = require_numeric3();
    var utils = require_utils4();
    var conditionalDatatypes = require_compiler_conditional();
    var structuresDatatypes = require_compiler_structures();
    var utilsDatatypes = require_compiler_utils();
    var { tryCatch } = require_utils();
    var ProtoDefCompiler = class {
      constructor() {
        this.readCompiler = new ReadCompiler();
        this.writeCompiler = new WriteCompiler();
        this.sizeOfCompiler = new SizeOfCompiler();
      }
      addTypes(types) {
        this.readCompiler.addTypes(types.Read);
        this.writeCompiler.addTypes(types.Write);
        this.sizeOfCompiler.addTypes(types.SizeOf);
      }
      addTypesToCompile(types) {
        this.readCompiler.addTypesToCompile(types);
        this.writeCompiler.addTypesToCompile(types);
        this.sizeOfCompiler.addTypesToCompile(types);
      }
      addProtocol(protocolData, path) {
        this.readCompiler.addProtocol(protocolData, path);
        this.writeCompiler.addProtocol(protocolData, path);
        this.sizeOfCompiler.addProtocol(protocolData, path);
      }
      addVariable(key, val) {
        this.readCompiler.addContextType(key, val);
        this.writeCompiler.addContextType(key, val);
        this.sizeOfCompiler.addContextType(key, val);
      }
      compileProtoDefSync(options = { printCode: false }) {
        const sizeOfCode = this.sizeOfCompiler.generate();
        const writeCode = this.writeCompiler.generate();
        const readCode = this.readCompiler.generate();
        if (options.printCode) {
          console.log("// SizeOf:");
          console.log(sizeOfCode);
          console.log("// Write:");
          console.log(writeCode);
          console.log("// Read:");
          console.log(readCode);
        }
        const sizeOfCtx = this.sizeOfCompiler.compile(sizeOfCode);
        const writeCtx = this.writeCompiler.compile(writeCode);
        const readCtx = this.readCompiler.compile(readCode);
        return new CompiledProtodef(sizeOfCtx, writeCtx, readCtx);
      }
    };
    var CompiledProtodef = class {
      constructor(sizeOfCtx, writeCtx, readCtx) {
        this.sizeOfCtx = sizeOfCtx;
        this.writeCtx = writeCtx;
        this.readCtx = readCtx;
      }
      read(buffer, cursor, type) {
        const readFn = this.readCtx[type];
        if (!readFn) {
          throw new Error("missing data type: " + type);
        }
        return readFn(buffer, cursor);
      }
      write(value, buffer, cursor, type) {
        const writeFn = this.writeCtx[type];
        if (!writeFn) {
          throw new Error("missing data type: " + type);
        }
        return writeFn(value, buffer, cursor);
      }
      setVariable(key, val) {
        this.sizeOfCtx[key] = val;
        this.readCtx[key] = val;
        this.writeCtx[key] = val;
      }
      sizeOf(value, type) {
        const sizeFn = this.sizeOfCtx[type];
        if (!sizeFn) {
          throw new Error("missing data type: " + type);
        }
        if (typeof sizeFn === "function") {
          return sizeFn(value);
        } else {
          return sizeFn;
        }
      }
      createPacketBuffer(type, packet) {
        const length = tryCatch(
          () => this.sizeOf(packet, type),
          (e) => {
            e.message = `SizeOf error for ${e.field} : ${e.message}`;
            throw e;
          }
        );
        const buffer = Buffer.allocUnsafe(length);
        tryCatch(
          () => this.write(packet, buffer, 0, type),
          (e) => {
            e.message = `Write error for ${e.field} : ${e.message}`;
            throw e;
          }
        );
        return buffer;
      }
      parsePacketBuffer(type, buffer, offset = 0) {
        const { value, size } = tryCatch(
          () => this.read(buffer, offset, type),
          (e) => {
            e.message = `Read error for ${e.field} : ${e.message}`;
            throw e;
          }
        );
        return {
          data: value,
          metadata: { size },
          buffer: buffer.slice(0, size),
          fullBuffer: buffer
        };
      }
    };
    var Compiler = class {
      constructor() {
        this.primitiveTypes = {};
        this.native = {};
        this.context = {};
        this.types = {};
        this.scopeStack = [];
        this.parameterizableTypes = {};
      }
      /**
       * A native type is a type read or written by a function that will be called in it's
       * original context.
       * @param {*} type
       * @param {*} fn
       */
      addNativeType(type, fn) {
        this.primitiveTypes[type] = `native.${type}`;
        this.native[type] = fn;
        this.types[type] = "native";
      }
      /**
       * A context type is a type that will be called in the protocol's context. It can refer to
       * registred native types using native.{type}() or context type (provided and generated)
       * using ctx.{type}(), but cannot access it's original context.
       * @param {*} type
       * @param {*} fn
       */
      addContextType(type, fn) {
        this.primitiveTypes[type] = `ctx.${type}`;
        this.context[type] = fn.toString();
      }
      /**
       * A parametrizable type is a function that will be generated at compile time using the
       * provided maker function
       * @param {*} type
       * @param {*} maker
       */
      addParametrizableType(type, maker) {
        this.parameterizableTypes[type] = maker;
      }
      addTypes(types) {
        for (const [type, [kind, fn]] of Object.entries(types)) {
          if (kind === "native")
            this.addNativeType(type, fn);
          else if (kind === "context")
            this.addContextType(type, fn);
          else if (kind === "parametrizable")
            this.addParametrizableType(type, fn);
        }
      }
      addTypesToCompile(types) {
        for (const [type, json] of Object.entries(types)) {
          if (!this.types[type] || this.types[type] === "native")
            this.types[type] = json;
        }
      }
      addProtocol(protocolData, path) {
        const self2 = this;
        function recursiveAddTypes(protocolData2, path2) {
          if (protocolData2 === void 0) {
            return;
          }
          if (protocolData2.types) {
            self2.addTypesToCompile(protocolData2.types);
          }
          recursiveAddTypes(protocolData2[path2.shift()], path2);
        }
        recursiveAddTypes(protocolData, path.slice(0));
      }
      indent(code2, indent = "  ") {
        return code2.split("\n").map((line) => indent + line).join("\n");
      }
      getField(name, noAssign) {
        const path = name.split("/");
        let i = this.scopeStack.length - 1;
        const reserved = ["value", "enum", "default", "size", "offset"];
        while (path.length) {
          const scope = this.scopeStack[i];
          const field = path.shift();
          if (field === "..") {
            i--;
            continue;
          }
          if (scope[field])
            return scope[field] + (path.length ? "." + path.join(".") : "");
          if (path.length !== 0) {
            throw new Error("Cannot access properties of undefined field");
          }
          let count = 0;
          if (reserved.includes(field))
            count++;
          for (let j = 0; j < i; j++) {
            if (this.scopeStack[j][field])
              count++;
          }
          if (noAssign) {
            scope[field] = field;
          } else {
            scope[field] = field + (count || "");
          }
          return scope[field];
        }
        throw new Error("Unknown field " + path);
      }
      generate() {
        this.scopeStack = [{}];
        const functions = [];
        for (const type in this.context) {
          functions[type] = this.context[type];
        }
        for (const type in this.types) {
          if (!functions[type]) {
            if (this.types[type] !== "native") {
              functions[type] = this.compileType(this.types[type]);
              if (functions[type].startsWith("ctx")) {
                functions[type] = "function () { return " + functions[type] + "(...arguments) }";
              }
              if (!isNaN(functions[type])) {
                functions[type] = this.wrapCode("  return " + functions[type]);
              }
            } else {
              functions[type] = `native.${type}`;
            }
          }
        }
        return "() => {\n" + this.indent("const ctx = {\n" + this.indent(Object.keys(functions).map((type) => {
          return type + ": " + functions[type];
        }).join(",\n")) + "\n}\nreturn ctx") + "\n}";
      }
      /**
       * Compile the given js code, providing native.{type} to the context, return the compiled types
       * @param {*} code
       */
      compile(code) {
        const native = this.native;
        const { PartialReadError } = require_utils();
        return eval(code)();
      }
    };
    var ReadCompiler = class extends Compiler {
      constructor() {
        super();
        this.addTypes(conditionalDatatypes.Read);
        this.addTypes(structuresDatatypes.Read);
        this.addTypes(utilsDatatypes.Read);
        for (const key in numeric) {
          this.addNativeType(key, numeric[key][0]);
        }
        for (const key in utils) {
          this.addNativeType(key, utils[key][0]);
        }
      }
      compileType(type) {
        if (type instanceof Array) {
          if (this.parameterizableTypes[type[0]]) {
            return this.parameterizableTypes[type[0]](this, type[1]);
          }
          if (this.types[type[0]] && this.types[type[0]] !== "native") {
            return this.wrapCode("return " + this.callType(type[0], "offset", Object.values(type[1])));
          }
          throw new Error("Unknown parametrizable type: " + JSON.stringify(type[0]));
        } else {
          if (type === "native")
            return "null";
          if (this.types[type]) {
            return "ctx." + type;
          }
          return this.primitiveTypes[type];
        }
      }
      wrapCode(code2, args = []) {
        if (args.length > 0)
          return "(buffer, offset, " + args.join(", ") + ") => {\n" + this.indent(code2) + "\n}";
        return "(buffer, offset) => {\n" + this.indent(code2) + "\n}";
      }
      callType(type, offsetExpr = "offset", args = []) {
        if (type instanceof Array) {
          if (this.types[type[0]] && this.types[type[0]] !== "native") {
            return this.callType(type[0], offsetExpr, Object.values(type[1]));
          }
        }
        if (type instanceof Array && type[0] === "container")
          this.scopeStack.push({});
        const code2 = this.compileType(type);
        if (type instanceof Array && type[0] === "container")
          this.scopeStack.pop();
        if (args.length > 0)
          return "(" + code2 + `)(buffer, ${offsetExpr}, ` + args.map((name) => this.getField(name)).join(", ") + ")";
        return "(" + code2 + `)(buffer, ${offsetExpr})`;
      }
    };
    var WriteCompiler = class extends Compiler {
      constructor() {
        super();
        this.addTypes(conditionalDatatypes.Write);
        this.addTypes(structuresDatatypes.Write);
        this.addTypes(utilsDatatypes.Write);
        for (const key in numeric) {
          this.addNativeType(key, numeric[key][1]);
        }
        for (const key in utils) {
          this.addNativeType(key, utils[key][1]);
        }
      }
      compileType(type) {
        if (type instanceof Array) {
          if (this.parameterizableTypes[type[0]]) {
            return this.parameterizableTypes[type[0]](this, type[1]);
          }
          if (this.types[type[0]] && this.types[type[0]] !== "native") {
            return this.wrapCode("return " + this.callType("value", type[0], "offset", Object.values(type[1])));
          }
          throw new Error("Unknown parametrizable type: " + type[0]);
        } else {
          if (type === "native")
            return "null";
          if (this.types[type]) {
            return "ctx." + type;
          }
          return this.primitiveTypes[type];
        }
      }
      wrapCode(code2, args = []) {
        if (args.length > 0)
          return "(value, buffer, offset, " + args.join(", ") + ") => {\n" + this.indent(code2) + "\n}";
        return "(value, buffer, offset) => {\n" + this.indent(code2) + "\n}";
      }
      callType(value, type, offsetExpr = "offset", args = []) {
        if (type instanceof Array) {
          if (this.types[type[0]] && this.types[type[0]] !== "native") {
            return this.callType(value, type[0], offsetExpr, Object.values(type[1]));
          }
        }
        if (type instanceof Array && type[0] === "container")
          this.scopeStack.push({});
        const code2 = this.compileType(type);
        if (type instanceof Array && type[0] === "container")
          this.scopeStack.pop();
        if (args.length > 0)
          return "(" + code2 + `)(${value}, buffer, ${offsetExpr}, ` + args.map((name) => this.getField(name)).join(", ") + ")";
        return "(" + code2 + `)(${value}, buffer, ${offsetExpr})`;
      }
    };
    var SizeOfCompiler = class extends Compiler {
      constructor() {
        super();
        this.addTypes(conditionalDatatypes.SizeOf);
        this.addTypes(structuresDatatypes.SizeOf);
        this.addTypes(utilsDatatypes.SizeOf);
        for (const key in numeric) {
          this.addNativeType(key, numeric[key][2]);
        }
        for (const key in utils) {
          this.addNativeType(key, utils[key][2]);
        }
      }
      /**
       * A native type is a type read or written by a function that will be called in it's
       * original context.
       * @param {*} type
       * @param {*} fn
       */
      addNativeType(type, fn) {
        this.primitiveTypes[type] = `native.${type}`;
        if (!isNaN(fn)) {
          this.native[type] = (value) => {
            return fn;
          };
        } else {
          this.native[type] = fn;
        }
        this.types[type] = "native";
      }
      compileType(type) {
        if (type instanceof Array) {
          if (this.parameterizableTypes[type[0]]) {
            return this.parameterizableTypes[type[0]](this, type[1]);
          }
          if (this.types[type[0]] && this.types[type[0]] !== "native") {
            return this.wrapCode("return " + this.callType("value", type[0], Object.values(type[1])));
          }
          throw new Error("Unknown parametrizable type: " + type[0]);
        } else {
          if (type === "native")
            return "null";
          if (!isNaN(this.primitiveTypes[type]))
            return this.primitiveTypes[type];
          if (this.types[type]) {
            return "ctx." + type;
          }
          return this.primitiveTypes[type];
        }
      }
      wrapCode(code2, args = []) {
        if (args.length > 0)
          return "(value, " + args.join(", ") + ") => {\n" + this.indent(code2) + "\n}";
        return "(value) => {\n" + this.indent(code2) + "\n}";
      }
      callType(value, type, args = []) {
        if (type instanceof Array) {
          if (this.types[type[0]] && this.types[type[0]] !== "native") {
            return this.callType(value, type[0], Object.values(type[1]));
          }
        }
        if (type instanceof Array && type[0] === "container")
          this.scopeStack.push({});
        const code2 = this.compileType(type);
        if (type instanceof Array && type[0] === "container")
          this.scopeStack.pop();
        if (!isNaN(code2))
          return code2;
        if (args.length > 0)
          return "(" + code2 + `)(${value}, ` + args.map((name) => this.getField(name)).join(", ") + ")";
        return "(" + code2 + `)(${value})`;
      }
    };
    module.exports = {
      ReadCompiler,
      WriteCompiler,
      SizeOfCompiler,
      ProtoDefCompiler,
      CompiledProtodef
    };
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/index.js
var require_src = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/src/index.js"(exports2, module2) {
    var ProtoDef = require_protodef();
    var proto = new ProtoDef();
    module2.exports = {
      ProtoDef,
      Serializer: require_serializer().Serializer,
      Parser: require_serializer().Parser,
      FullPacketParser: require_serializer().FullPacketParser,
      Compiler: require_compiler(),
      types: proto.types,
      utils: require_utils()
    };
  }
});

// ../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/index.js
var require_protodef2 = __commonJS({
  "../node_modules/.pnpm/protodef@1.19.0/node_modules/protodef/index.js"(exports2, module2) {
    module2.exports = require_src();
  }
});

// ../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/nbt.json
var require_nbt = __commonJS({
  "../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/nbt.json"(exports2, module2) {
    module2.exports = {
      void: "native",
      container: "native",
      i8: "native",
      switch: "native",
      compound: "native",
      nbtTagName: "native",
      i16: "native",
      u16: "native",
      i32: "native",
      i64: "native",
      f32: "native",
      f64: "native",
      pstring: "native",
      shortString: [
        "pstring",
        {
          countType: "u16"
        }
      ],
      byteArray: [
        "array",
        {
          countType: "i32",
          type: "i8"
        }
      ],
      list: [
        "container",
        [
          {
            name: "type",
            type: "nbtMapper"
          },
          {
            name: "value",
            type: [
              "array",
              {
                countType: "i32",
                type: [
                  "nbtSwitch",
                  {
                    type: "type"
                  }
                ]
              }
            ]
          }
        ]
      ],
      intArray: [
        "array",
        {
          countType: "i32",
          type: "i32"
        }
      ],
      longArray: [
        "array",
        {
          countType: "i32",
          type: "i64"
        }
      ],
      nbtMapper: [
        "mapper",
        {
          type: "i8",
          mappings: {
            "0": "end",
            "1": "byte",
            "2": "short",
            "3": "int",
            "4": "long",
            "5": "float",
            "6": "double",
            "7": "byteArray",
            "8": "string",
            "9": "list",
            "10": "compound",
            "11": "intArray",
            "12": "longArray"
          }
        }
      ],
      nbtSwitch: [
        "switch",
        {
          compareTo: "$type",
          fields: {
            end: "void",
            byte: "i8",
            short: "i16",
            int: "i32",
            long: "i64",
            float: "f32",
            double: "f64",
            byteArray: "byteArray",
            string: "shortString",
            list: "list",
            compound: "compound",
            intArray: "intArray",
            longArray: "longArray"
          }
        }
      ],
      nbt: [
        "container",
        [
          {
            name: "type",
            type: "nbtMapper"
          },
          {
            name: "name",
            type: "nbtTagName"
          },
          {
            name: "value",
            type: [
              "nbtSwitch",
              {
                type: "type"
              }
            ]
          }
        ]
      ],
      anonymousNbt: [
        "container",
        [
          {
            name: "type",
            type: "nbtMapper"
          },
          {
            name: "value",
            type: [
              "nbtSwitch",
              {
                type: "type"
              }
            ]
          }
        ]
      ],
      anonOptionalNbt: [
        "optionalNbtType",
        {
          tagType: "anonymousNbt"
        }
      ],
      optionalNbt: [
        "optionalNbtType",
        {
          tagType: "nbt"
        }
      ]
    };
  }
});

// ../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/nbt-varint.json
var require_nbt_varint = __commonJS({
  "../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/nbt-varint.json"(exports2, module2) {
    module2.exports = {
      void: "native",
      container: "native",
      i8: "native",
      switch: "native",
      compound: "native",
      zigzag32: "native",
      zigzag64: "native",
      i16: "native",
      i32: "native",
      i64: "native",
      f32: "native",
      f64: "native",
      pstring: "native",
      shortString: [
        "pstring",
        {
          countType: "varint"
        }
      ],
      byteArray: [
        "array",
        {
          countType: "zigzag32",
          type: "i8"
        }
      ],
      list: [
        "container",
        [
          {
            name: "type",
            type: "nbtMapper"
          },
          {
            name: "value",
            type: [
              "array",
              {
                countType: "zigzag32",
                type: [
                  "nbtSwitch",
                  {
                    type: "type"
                  }
                ]
              }
            ]
          }
        ]
      ],
      intArray: [
        "array",
        {
          countType: "zigzag32",
          type: "i32"
        }
      ],
      longArray: [
        "array",
        {
          countType: "zigzag32",
          type: "i64"
        }
      ],
      nbtMapper: [
        "mapper",
        {
          type: "i8",
          mappings: {
            "0": "end",
            "1": "byte",
            "2": "short",
            "3": "int",
            "4": "long",
            "5": "float",
            "6": "double",
            "7": "byteArray",
            "8": "string",
            "9": "list",
            "10": "compound",
            "11": "intArray",
            "12": "longArray"
          }
        }
      ],
      nbtSwitch: [
        "switch",
        {
          compareTo: "$type",
          fields: {
            end: "void",
            byte: "i8",
            short: "i16",
            int: "zigzag32",
            long: "zigzag64",
            float: "f32",
            double: "f64",
            byteArray: "byteArray",
            string: "shortString",
            list: "list",
            compound: "compound",
            intArray: "intArray",
            longArray: "longArray"
          }
        }
      ],
      nbt: [
        "container",
        [
          {
            name: "type",
            type: "nbtMapper"
          },
          {
            name: "name",
            type: "nbtTagName"
          },
          {
            name: "value",
            type: [
              "nbtSwitch",
              {
                type: "type"
              }
            ]
          }
        ]
      ]
    };
  }
});

// ../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/compiler-compound.js
var require_compiler_compound = __commonJS({
  "../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/compiler-compound.js"(exports2, module2) {
    module2.exports = {
      Read: {
        compound: ["context", (buffer, offset) => {
          const results = {
            value: {},
            size: 0
          };
          while (offset !== buffer.length) {
            const typ = ctx.i8(buffer, offset);
            if (typ.value === 0) {
              results.size += typ.size;
              break;
            }
            if (typ.value > 20) {
              throw new Error(`Invalid tag: ${typ.value} > 20`);
            }
            const readResults = ctx.nbt(buffer, offset);
            offset += readResults.size;
            results.size += readResults.size;
            results.value[readResults.value.name] = {
              type: readResults.value.type,
              value: readResults.value.value
            };
          }
          return results;
        }]
      },
      Write: {
        compound: ["context", (value, buffer, offset) => {
          for (const key in value) {
            offset = ctx.nbt({
              name: key,
              type: value[key].type,
              value: value[key].value
            }, buffer, offset);
          }
          offset = ctx.i8(0, buffer, offset);
          return offset;
        }]
      },
      SizeOf: {
        compound: ["context", (value) => {
          let size = 1;
          for (const key in value) {
            size += ctx.nbt({
              name: key,
              type: value[key].type,
              value: value[key].value
            });
          }
          return size;
        }]
      }
    };
  }
});

// ../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/compiler-tagname.js
var require_compiler_tagname = __commonJS({
  "../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/compiler-tagname.js"(exports2, module2) {
    function readPString(buffer, offset) {
      const { value, size } = ctx.shortString(buffer, offset);
      for (const c of value) {
        if (c === "\0")
          throw new Error("unexpected tag end");
      }
      return { value, size };
    }
    function writePString(...args) {
      return ctx.shortString(...args);
    }
    function sizeOfPString(...args) {
      return ctx.shortString(...args);
    }
    module2.exports = {
      Read: { nbtTagName: ["context", readPString] },
      Write: { nbtTagName: ["context", writePString] },
      SizeOf: { nbtTagName: ["context", sizeOfPString] }
    };
  }
});

// ../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/optional.js
var require_optional = __commonJS({
  "../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/optional.js"(exports2, module2) {
    function readOptionalNbt(buffer, offset, { tagType }, rootNode) {
      if (offset + 1 > buffer.length) {
        throw new Error("Read out of bounds");
      }
      if (buffer.readInt8(offset) === 0)
        return { size: 1 };
      return this.read(buffer, offset, tagType, rootNode);
    }
    function writeOptionalNbt(value, buffer, offset, { tagType }, rootNode) {
      if (value === void 0) {
        buffer.writeInt8(0, offset);
        return offset + 1;
      }
      return this.write(value, buffer, offset, tagType, rootNode);
    }
    function sizeOfOptionalNbt(value, { tagType }, rootNode) {
      if (value === void 0) {
        return 1;
      }
      return this.sizeOf(value, tagType, tagType, rootNode);
    }
    var compiler = {
      Read: {
        optionalNbtType: ["parametrizable", (compiler2, { tagType }) => {
          return compiler2.wrapCode(`
if (offset + 1 > buffer.length) { throw new PartialReadError() }
if (buffer.readInt8(offset) === 0) return { size: 1 }
return ${compiler2.callType(tagType)}
      `);
        }]
      },
      Write: {
        optionalNbtType: ["parametrizable", (compiler2, { tagType }) => {
          return compiler2.wrapCode(`
if (value === undefined) {
  buffer.writeInt8(0, offset)
  return offset + 1
}
return ${compiler2.callType("value", tagType)}
      `);
        }]
      },
      SizeOf: {
        optionalNbtType: ["parametrizable", (compiler2, { tagType }) => {
          return compiler2.wrapCode(`
if (value === undefined) { return 1 }
return ${compiler2.callType("value", tagType)}
      `);
        }]
      }
    };
    module2.exports = {
      compiler,
      interpret: { optionalNbtType: [readOptionalNbt, writeOptionalNbt, sizeOfOptionalNbt] }
    };
  }
});

// ../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/compound.js
var require_compound = __commonJS({
  "../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/compound.js"(exports2, module2) {
    module2.exports = {
      compound: [readCompound, writeCompound, sizeOfCompound]
    };
    function readCompound(buffer, offset, typeArgs, rootNode) {
      const results = {
        value: {},
        size: 0
      };
      while (true) {
        const typ = this.read(buffer, offset, "i8", rootNode);
        if (typ.value === 0) {
          offset += typ.size;
          results.size += typ.size;
          break;
        }
        const readResults = this.read(buffer, offset, "nbt", rootNode);
        offset += readResults.size;
        results.size += readResults.size;
        results.value[readResults.value.name] = {
          type: readResults.value.type,
          value: readResults.value.value
        };
      }
      return results;
    }
    function writeCompound(value, buffer, offset, typeArgs, rootNode) {
      const self2 = this;
      Object.keys(value).forEach(function(key) {
        offset = self2.write({
          name: key,
          type: value[key].type,
          value: value[key].value
        }, buffer, offset, "nbt", rootNode);
      });
      offset = this.write(0, buffer, offset, "i8", rootNode);
      return offset;
    }
    function sizeOfCompound(value, typeArgs, rootNode) {
      const self2 = this;
      const size = Object.keys(value).reduce(function(size2, key) {
        return size2 + self2.sizeOf({
          name: key,
          type: value[key].type,
          value: value[key].value
        }, "nbt", rootNode);
      }, 0);
      return 1 + size;
    }
  }
});

// ../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/typings/tag-type.js
var require_tag_type = __commonJS({
  "../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/typings/tag-type.js"(exports2, module2) {
    var TagType = {
      Byte: "byte",
      Short: "short",
      Int: "int",
      Long: "long",
      Float: "float",
      Double: "double",
      ByteArray: "byteArray",
      String: "string",
      List: "list",
      Compound: "compound",
      IntArray: "intArray",
      LongArray: "longArray"
    };
    module2.exports = TagType;
  }
});

// ../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/nbt.js
var require_nbt2 = __commonJS({
  "../node_modules/.pnpm/prismarine-nbt@2.7.0/node_modules/prismarine-nbt/nbt.js"(exports2, module2) {
    var zlib = require("zlib");
    var { ProtoDefCompiler: ProtoDefCompiler2 } = require_protodef2().Compiler;
    var beNbtJson = JSON.stringify(require_nbt());
    var leNbtJson = beNbtJson.replace(/([iuf][0-7]+)/g, "l$1");
    var varintJson = JSON.stringify(require_nbt_varint()).replace(/([if][0-7]+)/g, "l$1");
    function addTypesToCompiler(type, compiler) {
      compiler.addTypes(require_compiler_compound());
      compiler.addTypes(require_compiler_tagname());
      compiler.addTypes(require_optional().compiler);
      let proto = beNbtJson;
      if (type === "littleVarint") {
        proto = varintJson;
      } else if (type === "little") {
        proto = leNbtJson;
      }
      compiler.addTypesToCompile(JSON.parse(proto));
    }
    function addTypesToInterpreter(type, compiler) {
      compiler.addTypes(require_compound());
      compiler.addTypes(require_optional().interpret);
      let proto = beNbtJson;
      if (type === "littleVarint") {
        proto = varintJson;
      } else if (type === "little") {
        proto = leNbtJson;
      }
      compiler.addTypes(JSON.parse(proto));
      compiler.types.nbtTagName = compiler.types.shortString;
    }
    function createProto(type) {
      const compiler = new ProtoDefCompiler2();
      addTypesToCompiler(type, compiler);
      return compiler.compileProtoDefSync();
    }
    var protoBE = createProto("big");
    var protoLE = createProto("little");
    var protoVarInt = createProto("littleVarint");
    var protos = {
      big: protoBE,
      little: protoLE,
      littleVarint: protoVarInt
    };
    function writeUncompressed(value, proto = "big") {
      if (proto === true)
        proto = "little";
      return protos[proto].createPacketBuffer("nbt", value);
    }
    function parseUncompressed(data, proto = "big", options = {}) {
      if (proto === true)
        proto = "little";
      protos[proto].setVariable("noArraySizeCheck", options.noArraySizeCheck);
      return protos[proto].parsePacketBuffer("nbt", data, data.startOffset).data;
    }
    var hasGzipHeader = function(data) {
      let result = true;
      if (data[0] !== 31)
        result = false;
      if (data[1] !== 139)
        result = false;
      return result;
    };
    var hasBedrockLevelHeader = (data) => data[1] === 0 && data[2] === 0 && data[3] === 0;
    async function parseAs(data, type, options = {}) {
      if (hasGzipHeader(data)) {
        data = await new Promise((resolve, reject) => {
          zlib.gunzip(data, (error, uncompressed) => {
            if (error)
              reject(error);
            else
              resolve(uncompressed);
          });
        });
      }
      protos[type].setVariable("noArraySizeCheck", options.noArraySizeCheck);
      const parsed = protos[type].parsePacketBuffer("nbt", data, data.startOffset);
      parsed.metadata.buffer = data;
      parsed.type = type;
      return parsed;
    }
    async function parse(data, format, callback) {
      let fmt = null;
      if (typeof format === "function") {
        callback = format;
      } else if (format === true || format === "little") {
        fmt = "little";
      } else if (format === "big") {
        fmt = "big";
      } else if (format === "littleVarint") {
        fmt = "littleVarint";
      } else if (format) {
        throw new Error("Unrecognized format: " + format);
      }
      data.startOffset = data.startOffset || 0;
      if (!fmt && !data.startOffset) {
        if (hasBedrockLevelHeader(data)) {
          data.startOffset += 8;
          fmt = "little";
        }
      }
      if (fmt) {
        try {
          const res = await parseAs(data, fmt);
          if (callback)
            callback(null, res.data, res.type, res.metadata);
          return { parsed: res.data, type: res.type, metadata: res.metadata };
        } catch (e) {
          if (callback)
            return callback(e);
          else
            throw e;
        }
      }
      const verifyEOF = ({ buffer, size }) => {
        const readLen = size;
        const bufferLen = buffer.length - buffer.startOffset;
        const lastByte = buffer[readLen + buffer.startOffset];
        const nextNbtTag = lastByte === 10;
        if (readLen < bufferLen && !nextNbtTag) {
          throw new Error(`Unexpected EOF at ${readLen}: still have ${bufferLen - readLen} bytes to read !`);
        }
      };
      let ret = null;
      try {
        ret = await parseAs(data, "big");
        verifyEOF(ret.metadata);
      } catch (e) {
        try {
          ret = await parseAs(data, "little");
          verifyEOF(ret.metadata);
        } catch (e2) {
          try {
            ret = await parseAs(data, "littleVarint");
            verifyEOF(ret.metadata);
          } catch (e3) {
            if (callback)
              return callback(e);
            else
              throw e;
          }
        }
      }
      if (callback)
        callback(null, ret.data, ret.type, ret.metadata);
      return { parsed: ret.data, type: ret.type, metadata: ret.metadata };
    }
    function simplify(data) {
      function transform(value, type) {
        if (type === "compound") {
          return Object.keys(value).reduce(function(acc, key) {
            acc[key] = simplify(value[key]);
            return acc;
          }, {});
        }
        if (type === "list") {
          return value.value.map(function(v) {
            return transform(v, value.type);
          });
        }
        return value;
      }
      return transform(data.value, data.type);
    }
    function equal(nbt1, nbt2) {
      if (nbt1.type !== nbt2.type)
        return false;
      if (nbt1.type === "compound") {
        const nbt1Keys = Object.keys(nbt1.value);
        const nbt2Keys = Object.keys(nbt2.value);
        if (nbt1Keys.length !== nbt2Keys.length)
          return false;
        for (const key of nbt1Keys) {
          if (!equal(nbt1.value[key], nbt2.value[key]))
            return false;
        }
        return true;
      }
      if (nbt1.type === "list") {
        if (nbt1.value.length !== nbt2.value.length)
          return false;
        for (let i = 0; i < nbt1.value.length; i++) {
          if (!equal(nbt1.value[i], nbt2.value[i]))
            return false;
        }
        return true;
      }
      if (nbt1.type === "byteArray" || nbt1.type === "intArray" || nbt1.type === "shortArray") {
        if (nbt1.value.length !== nbt2.value.length)
          return false;
        for (let i = 0; i < nbt1.value.length; i++) {
          if (nbt1.value[i] !== nbt2.value[i])
            return false;
        }
        return true;
      }
      if (nbt1.type === "long") {
        return nbt1.value[0] === nbt2.value[0] && nbt1.value[1] === nbt2.value[1];
      }
      if (nbt1.type === "longArray") {
        if (nbt1.value.length !== nbt2.value.length)
          return false;
        for (let i = 0; i < nbt1.value.length; i++) {
          if (nbt1.value[i][0] !== nbt2.value[i][0] || nbt1.value[i][1] !== nbt2.value[i][1])
            return false;
        }
        return true;
      }
      return nbt1.value === nbt2.value;
    }
    var builder = {
      bool(value = false) {
        return { type: "bool", value };
      },
      short(value) {
        return { type: "short", value };
      },
      byte(value) {
        return { type: "byte", value };
      },
      string(value) {
        return { type: "string", value };
      },
      comp(value, name = "") {
        return { type: "compound", name, value };
      },
      int(value) {
        return { type: "int", value };
      },
      float(value) {
        return { type: "float", value };
      },
      double(value) {
        return { type: "double", value };
      },
      long(value) {
        return { type: "long", value };
      },
      list(value) {
        const type = value?.type ?? "end";
        return { type: "list", value: { type, value: value?.value ?? [] } };
      },
      byteArray(value = []) {
        return { type: "byteArray", value };
      },
      shortArray(value = []) {
        return { type: "shortArray", value };
      },
      intArray(value = []) {
        return { type: "intArray", value };
      },
      longArray(value = []) {
        return { type: "longArray", value };
      }
    };
    module2.exports = {
      addTypesToCompiler,
      addTypesToInterpreter,
      writeUncompressed,
      parseUncompressed,
      simplify,
      hasBedrockLevelHeader,
      parse,
      parseAs,
      equal,
      proto: protoBE,
      protoLE,
      protos,
      TagType: require_tag_type(),
      ...builder
    };
  }
});

// ../node_modules/.pnpm/prismarine-registry@1.11.0/node_modules/prismarine-registry/lib/loader.js
var require_loader = __commonJS({
  "../node_modules/.pnpm/prismarine-registry@1.11.0/node_modules/prismarine-registry/lib/loader.js"(exports2, module2) {
    module2.exports = (mcData2) => {
      const data = Object.assign({}, mcData2);
      return data;
    };
  }
});

// ../node_modules/.pnpm/prismarine-registry@1.11.0/node_modules/prismarine-registry/lib/indexer.js
var require_indexer = __commonJS({
  "../node_modules/.pnpm/prismarine-registry@1.11.0/node_modules/prismarine-registry/lib/indexer.js"(exports2, module2) {
    module2.exports = function buildIndexFromArray(array, fieldToIndex) {
      if (array === void 0) {
        return void 0;
      }
      return array.reduce(function(index, element) {
        index[element[fieldToIndex]] = element;
        return index;
      }, {});
    };
  }
});

// ../node_modules/.pnpm/prismarine-registry@1.11.0/node_modules/prismarine-registry/lib/bedrock/index.js
var require_bedrock = __commonJS({
  "../node_modules/.pnpm/prismarine-registry@1.11.0/node_modules/prismarine-registry/lib/bedrock/index.js"(exports2, module2) {
    var buildIndexFromArray = require_indexer();
    module2.exports = (data) => {
      function loadItemStates(itemStates) {
        const items = [];
        for (const item of itemStates) {
          const name = item.name.replace("minecraft:", "");
          items.push({ ...data.itemsByName[name], name, id: item.runtime_id });
        }
        data.itemsArray = items;
        data.items = buildIndexFromArray(data.itemsArray, "id");
        data.itemsByName = buildIndexFromArray(data.itemsArray, "name");
      }
      function loadHashedRuntimeIds(registry) {
        data.blocksByRuntimeId = {};
        const Block = require_prismarine_block()(registry);
        for (let i = 0; i < data.blockStates.length; i++) {
          const { name, states } = data.blockStates[i];
          const hash = Block.getHash(name, states);
          data.blocksByRuntimeId[hash] = { stateId: i, ...data.blocksByStateId[i] };
        }
      }
      function loadRuntimeIds() {
        data.blocksByRuntimeId = {};
        for (let i = 0; i < data.blockStates.length; i++) {
          data.blocksByRuntimeId[i] = { stateId: i, ...data.blocksByStateId[i] };
        }
      }
      return {
        handleStartGame(packet) {
          loadItemStates(packet.itemstates);
          if (this.supportFeature("blockHashes") && packet.block_network_ids_are_hashes) {
            loadHashedRuntimeIds(this);
          } else {
            loadRuntimeIds();
          }
        },
        writeItemStates() {
          const itemstates = [];
          for (const item of data.itemsArray) {
            let [ns, name] = item.name.split(":");
            if (!name) {
              name = ns;
              ns = "minecraft";
            }
            itemstates.push({ name: `${ns}:${name}`, runtime_id: item.id, component_based: ns !== "minecraft" });
          }
          return itemstates;
        }
      };
    };
  }
});

// ../node_modules/.pnpm/prismarine-registry@1.11.0/node_modules/prismarine-registry/lib/pc/transforms.js
var require_transforms = __commonJS({
  "../node_modules/.pnpm/prismarine-registry@1.11.0/node_modules/prismarine-registry/lib/pc/transforms.js"(exports2, module2) {
    var nbt = require_nbt2();
    module2.exports = {
      networkBiomesToMcDataSchema(biome, staticData) {
        const name = biome.name.replace("minecraft:", "");
        const equivalent = staticData.biomesByName[name];
        return Object.assign(biome.element, {
          ...equivalent,
          id: biome.id,
          name,
          category: biome.element.category,
          temperature: biome.element.temperature,
          depth: biome.element.depth,
          scale: biome.element.scale,
          precipitation: biome.element.precipitation,
          rainfall: biome.element.downfall
        });
      },
      mcDataSchemaToNetworkBiomes(dynBiomeData, staticData) {
        const ret = [];
        if (!dynBiomeData) {
          for (const biome of staticData.biomesArray) {
            ret.push({
              name: nbt.string("minecraft:" + biome.name),
              id: nbt.int(biome.id),
              element: nbt.comp({
                depth: nbt.float(biome.depth),
                scale: nbt.float(biome.scale),
                category: nbt.string(biome.category),
                temperature: nbt.float(biome.temperature),
                precipitation: nbt.string(biome.precipitation),
                downfall: nbt.float(biome.rainfall),
                effects: {
                  // TODO: we need to add more data to static biomes.json
                  sky_color: nbt.int(biome.color)
                }
              })
            });
          }
        } else {
          for (const biome of dynBiomeData) {
            const oldEffects = Object.entries(biome.effects).map(([k, v]) => ({
              [k]: nbt.int(v)
            })).reduce((a, b) => Object.assign(a, b), {});
            ret.push({
              name: nbt.string("minecraft:" + biome.name),
              id: nbt.int(biome.id),
              element: nbt.comp({
                depth: nbt.float(biome.depth),
                scale: nbt.float(biome.scale),
                category: nbt.string(biome.category),
                temperature: nbt.float(biome.temperature),
                precipitation: nbt.string(biome.precipitation),
                downfall: nbt.float(biome.rainfall),
                effects: nbt.comp({
                  ...oldEffects,
                  mood_sound: biome.effects?.mood_sound ? nbt.comp({
                    tick_delay: nbt.int(biome.effects.mood_sound.tick_delay),
                    offset: nbt.double(biome.effects.mood_sound.offset),
                    sound: nbt.string(biome.effects.mood_sound.sound),
                    block_search_extent: nbt.int(biome.effects.mood_sound.block_search_extent)
                  }) : void 0
                })
              })
            });
          }
        }
        return ret;
      }
    };
  }
});

// ../node_modules/.pnpm/prismarine-registry@1.11.0/node_modules/prismarine-registry/lib/pc/index.js
var require_pc = __commonJS({
  "../node_modules/.pnpm/prismarine-registry@1.11.0/node_modules/prismarine-registry/lib/pc/index.js"(exports2, module2) {
    var nbt = require_nbt2();
    var { networkBiomesToMcDataSchema, mcDataSchemaToNetworkBiomes } = require_transforms();
    module2.exports = (data, staticData) => {
      let hasDynamicDimensionData = false;
      return {
        loadDimensionCodec(codec) {
          const handlers = {
            chat_type(chat) {
              data.chatFormattingById = {};
              data.chatFormattingByName = {};
              for (const chatType of chat) {
                const d = chatType.element?.chat?.decoration ?? chatType.element?.chat;
                if (!d)
                  continue;
                const n = {
                  id: chatType.id,
                  name: chatType.name,
                  formatString: staticData.language[d.translation_key] || d.translation_key,
                  parameters: d.parameters
                };
                if (staticData.supportFeature("incrementedChatType")) {
                  data.chatFormattingById[chatType.id + 1] = n;
                } else {
                  data.chatFormattingById[chatType.id] = n;
                }
                data.chatFormattingByName[chatType.name] = n;
              }
            },
            dimension_type(dimensions) {
              data.dimensionsById = {};
              data.dimensionsByName = {};
              data.dimensionsArray = [];
              for (const { name, id, element } of dimensions) {
                const n = name.replace("minecraft:", "");
                const d = {
                  name: n,
                  minY: element.min_y,
                  height: element.height
                };
                data.dimensionsById[id] = d;
                data.dimensionsByName[n] = d;
                data.dimensionsArray.push(d);
              }
            },
            "worldgen/biome"(biomes) {
              data.biomes = [];
              data.biomesByName = {};
              data.biomesArray = [];
              biomes.map((e) => networkBiomesToMcDataSchema(e, staticData));
              const allBiomes = [];
              for (const { name, id, element } of biomes) {
                data.biomes[id] = element;
                data.biomesByName[name] = element;
                allBiomes.push(element);
              }
              data.biomesArray = allBiomes;
              hasDynamicDimensionData = true;
            }
          };
          if (staticData.supportFeature("segmentedRegistryCodecData")) {
            const entries = codec.entries.map((e, ix) => ({ id: ix, name: e.key, element: nbt.simplify(e.value) }));
            handlers[codec.id.replace("minecraft:", "")]?.(entries);
          } else {
            const dimensionCodec = nbt.simplify(codec);
            for (const codecName in dimensionCodec) {
              handlers[codecName.replace("minecraft:", "")]?.(dimensionCodec[codecName].value);
            }
          }
        },
        writeDimensionCodec() {
          const codec = {};
          if (data.version["<"]("1.16")) {
            return codec;
          } else if (data.version["<"]("1.16.2")) {
            return staticData.loginPacket.dimensionCodec;
          } else if (data.version["<"]("1.20.5")) {
            if (data.dimensionsArray) {
              codec["minecraft:dimension_type"] = nbt.comp({
                type: nbt.string("minecraft:dimension_type"),
                value: nbt.list(nbt.comp(
                  data.dimensionsArray.map((dimension) => ({
                    name: dimension.name,
                    id: dimension.id,
                    element: {
                      min_y: dimension.minY
                    }
                  }))
                ))
              });
            } else {
              codec["minecraft:dimension_type"] = staticData.loginPacket.dimensionCodec.value["minecraft:dimension_type"];
            }
            codec["minecraft:worldgen/biome"] = nbt.comp({
              type: nbt.string("minecraft:worldgen/biome"),
              value: nbt.list(nbt.comp(mcDataSchemaToNetworkBiomes(hasDynamicDimensionData ? data.biomesArray : null, staticData)))
            });
            codec["minecraft:chat_type"] = staticData.loginPacket.dimensionCodec?.value?.["minecraft:chat_type"];
            return nbt.comp(codec);
          } else {
            if (data.dimensionsArray) {
              codec["minecraft:dimension_type"] = {
                id: "minecraft:dimension_type",
                entries: data.dimensionsArray.map((dimension) => ({
                  key: dimension.name,
                  value: nbt.comp({
                    min_y: dimension.minY
                  })
                }))
              };
            } else {
              codec["minecraft:dimension_type"] = staticData.loginPacket.dimensionCodec["minecraft:dimension_type"];
            }
            codec["minecraft:worldgen/biome"] = {
              id: "minecraft:worldgen/biome",
              entries: nbt.comp(mcDataSchemaToNetworkBiomes(hasDynamicDimensionData ? data.biomesArray : null, staticData))
            };
            codec["minecraft:chat_type"] = staticData.loginPacket.dimensionCodec?.["minecraft:chat_type"];
            return codec;
          }
        }
      };
    };
  }
});

// ../node_modules/.pnpm/prismarine-registry@1.11.0/node_modules/prismarine-registry/lib/index.js
var require_lib = __commonJS({
  "../node_modules/.pnpm/prismarine-registry@1.11.0/node_modules/prismarine-registry/lib/index.js"(exports2, module2) {
    var loader = require_loader();
    module2.exports = (version) => {
      const staticData = require("minecraft-data")(version);
      if (!staticData) {
        throw new Error("Do not have data for " + version);
      }
      const data = loader(staticData);
      let registry;
      if (version.startsWith("bedrock_")) {
        registry = require_bedrock()(data, staticData);
      } else {
        registry = require_pc()(data, staticData);
      }
      return Object.assign(data, registry);
    };
  }
});

// ../node_modules/.pnpm/prismarine-biome@1.3.0_minecraft-data@3.98.0_prismarine-registry@1.11.0/node_modules/prismarine-biome/index.js
var require_prismarine_biome = __commonJS({
  "../node_modules/.pnpm/prismarine-biome@1.3.0_minecraft-data@3.98.0_prismarine-registry@1.11.0/node_modules/prismarine-biome/index.js"(exports2, module2) {
    module2.exports = loader;
    var emptyBiome = {
      color: 0,
      height: null,
      name: "",
      rainfall: 0,
      temperature: 0
    };
    function loader(registryOrVersion) {
      const registry = typeof registryOrVersion === "string" ? require_lib()(registryOrVersion) : registryOrVersion;
      const biomes = registry.biomes;
      return function Biome(id) {
        return biomes?.[id] || { ...emptyBiome, id };
      };
    }
  }
});

// ../node_modules/.pnpm/nearley@2.20.1/node_modules/nearley/lib/nearley.js
var require_nearley = __commonJS({
  "../node_modules/.pnpm/nearley@2.20.1/node_modules/nearley/lib/nearley.js"(exports2, module2) {
    (function(root, factory) {
      if (typeof module2 === "object" && module2.exports) {
        module2.exports = factory();
      } else {
        root.nearley = factory();
      }
    })(exports2, function() {
      function Rule(name, symbols, postprocess) {
        this.id = ++Rule.highestId;
        this.name = name;
        this.symbols = symbols;
        this.postprocess = postprocess;
        return this;
      }
      Rule.highestId = 0;
      Rule.prototype.toString = function(withCursorAt) {
        var symbolSequence = typeof withCursorAt === "undefined" ? this.symbols.map(getSymbolShortDisplay).join(" ") : this.symbols.slice(0, withCursorAt).map(getSymbolShortDisplay).join(" ") + " \u25CF " + this.symbols.slice(withCursorAt).map(getSymbolShortDisplay).join(" ");
        return this.name + " \u2192 " + symbolSequence;
      };
      function State(rule, dot, reference, wantedBy) {
        this.rule = rule;
        this.dot = dot;
        this.reference = reference;
        this.data = [];
        this.wantedBy = wantedBy;
        this.isComplete = this.dot === rule.symbols.length;
      }
      State.prototype.toString = function() {
        return "{" + this.rule.toString(this.dot) + "}, from: " + (this.reference || 0);
      };
      State.prototype.nextState = function(child) {
        var state = new State(this.rule, this.dot + 1, this.reference, this.wantedBy);
        state.left = this;
        state.right = child;
        if (state.isComplete) {
          state.data = state.build();
          state.right = void 0;
        }
        return state;
      };
      State.prototype.build = function() {
        var children = [];
        var node = this;
        do {
          children.push(node.right.data);
          node = node.left;
        } while (node.left);
        children.reverse();
        return children;
      };
      State.prototype.finish = function() {
        if (this.rule.postprocess) {
          this.data = this.rule.postprocess(this.data, this.reference, Parser.fail);
        }
      };
      function Column(grammar, index) {
        this.grammar = grammar;
        this.index = index;
        this.states = [];
        this.wants = {};
        this.scannable = [];
        this.completed = {};
      }
      Column.prototype.process = function(nextColumn) {
        var states = this.states;
        var wants = this.wants;
        var completed = this.completed;
        for (var w = 0; w < states.length; w++) {
          var state = states[w];
          if (state.isComplete) {
            state.finish();
            if (state.data !== Parser.fail) {
              var wantedBy = state.wantedBy;
              for (var i = wantedBy.length; i--; ) {
                var left = wantedBy[i];
                this.complete(left, state);
              }
              if (state.reference === this.index) {
                var exp = state.rule.name;
                (this.completed[exp] = this.completed[exp] || []).push(state);
              }
            }
          } else {
            var exp = state.rule.symbols[state.dot];
            if (typeof exp !== "string") {
              this.scannable.push(state);
              continue;
            }
            if (wants[exp]) {
              wants[exp].push(state);
              if (completed.hasOwnProperty(exp)) {
                var nulls = completed[exp];
                for (var i = 0; i < nulls.length; i++) {
                  var right = nulls[i];
                  this.complete(state, right);
                }
              }
            } else {
              wants[exp] = [state];
              this.predict(exp);
            }
          }
        }
      };
      Column.prototype.predict = function(exp) {
        var rules = this.grammar.byName[exp] || [];
        for (var i = 0; i < rules.length; i++) {
          var r = rules[i];
          var wantedBy = this.wants[exp];
          var s = new State(r, 0, this.index, wantedBy);
          this.states.push(s);
        }
      };
      Column.prototype.complete = function(left, right) {
        var copy = left.nextState(right);
        this.states.push(copy);
      };
      function Grammar(rules, start) {
        this.rules = rules;
        this.start = start || this.rules[0].name;
        var byName = this.byName = {};
        this.rules.forEach(function(rule) {
          if (!byName.hasOwnProperty(rule.name)) {
            byName[rule.name] = [];
          }
          byName[rule.name].push(rule);
        });
      }
      Grammar.fromCompiled = function(rules, start) {
        var lexer = rules.Lexer;
        if (rules.ParserStart) {
          start = rules.ParserStart;
          rules = rules.ParserRules;
        }
        var rules = rules.map(function(r) {
          return new Rule(r.name, r.symbols, r.postprocess);
        });
        var g = new Grammar(rules, start);
        g.lexer = lexer;
        return g;
      };
      function StreamLexer() {
        this.reset("");
      }
      StreamLexer.prototype.reset = function(data, state) {
        this.buffer = data;
        this.index = 0;
        this.line = state ? state.line : 1;
        this.lastLineBreak = state ? -state.col : 0;
      };
      StreamLexer.prototype.next = function() {
        if (this.index < this.buffer.length) {
          var ch = this.buffer[this.index++];
          if (ch === "\n") {
            this.line += 1;
            this.lastLineBreak = this.index;
          }
          return { value: ch };
        }
      };
      StreamLexer.prototype.save = function() {
        return {
          line: this.line,
          col: this.index - this.lastLineBreak
        };
      };
      StreamLexer.prototype.formatError = function(token, message) {
        var buffer = this.buffer;
        if (typeof buffer === "string") {
          var lines = buffer.split("\n").slice(
            Math.max(0, this.line - 5),
            this.line
          );
          var nextLineBreak = buffer.indexOf("\n", this.index);
          if (nextLineBreak === -1)
            nextLineBreak = buffer.length;
          var col = this.index - this.lastLineBreak;
          var lastLineDigits = String(this.line).length;
          message += " at line " + this.line + " col " + col + ":\n\n";
          message += lines.map(function(line, i) {
            return pad(this.line - lines.length + i + 1, lastLineDigits) + " " + line;
          }, this).join("\n");
          message += "\n" + pad("", lastLineDigits + col) + "^\n";
          return message;
        } else {
          return message + " at index " + (this.index - 1);
        }
        function pad(n, length) {
          var s = String(n);
          return Array(length - s.length + 1).join(" ") + s;
        }
      };
      function Parser(rules, start, options) {
        if (rules instanceof Grammar) {
          var grammar = rules;
          var options = start;
        } else {
          var grammar = Grammar.fromCompiled(rules, start);
        }
        this.grammar = grammar;
        this.options = {
          keepHistory: false,
          lexer: grammar.lexer || new StreamLexer()
        };
        for (var key in options || {}) {
          this.options[key] = options[key];
        }
        this.lexer = this.options.lexer;
        this.lexerState = void 0;
        var column = new Column(grammar, 0);
        var table = this.table = [column];
        column.wants[grammar.start] = [];
        column.predict(grammar.start);
        column.process();
        this.current = 0;
      }
      Parser.fail = {};
      Parser.prototype.feed = function(chunk) {
        var lexer = this.lexer;
        lexer.reset(chunk, this.lexerState);
        var token;
        while (true) {
          try {
            token = lexer.next();
            if (!token) {
              break;
            }
          } catch (e) {
            var nextColumn = new Column(this.grammar, this.current + 1);
            this.table.push(nextColumn);
            var err = new Error(this.reportLexerError(e));
            err.offset = this.current;
            err.token = e.token;
            throw err;
          }
          var column = this.table[this.current];
          if (!this.options.keepHistory) {
            delete this.table[this.current - 1];
          }
          var n = this.current + 1;
          var nextColumn = new Column(this.grammar, n);
          this.table.push(nextColumn);
          var literal = token.text !== void 0 ? token.text : token.value;
          var value = lexer.constructor === StreamLexer ? token.value : token;
          var scannable = column.scannable;
          for (var w = scannable.length; w--; ) {
            var state = scannable[w];
            var expect = state.rule.symbols[state.dot];
            if (expect.test ? expect.test(value) : expect.type ? expect.type === token.type : expect.literal === literal) {
              var next = state.nextState({ data: value, token, isToken: true, reference: n - 1 });
              nextColumn.states.push(next);
            }
          }
          nextColumn.process();
          if (nextColumn.states.length === 0) {
            var err = new Error(this.reportError(token));
            err.offset = this.current;
            err.token = token;
            throw err;
          }
          if (this.options.keepHistory) {
            column.lexerState = lexer.save();
          }
          this.current++;
        }
        if (column) {
          this.lexerState = lexer.save();
        }
        this.results = this.finish();
        return this;
      };
      Parser.prototype.reportLexerError = function(lexerError) {
        var tokenDisplay, lexerMessage;
        var token = lexerError.token;
        if (token) {
          tokenDisplay = "input " + JSON.stringify(token.text[0]) + " (lexer error)";
          lexerMessage = this.lexer.formatError(token, "Syntax error");
        } else {
          tokenDisplay = "input (lexer error)";
          lexerMessage = lexerError.message;
        }
        return this.reportErrorCommon(lexerMessage, tokenDisplay);
      };
      Parser.prototype.reportError = function(token) {
        var tokenDisplay = (token.type ? token.type + " token: " : "") + JSON.stringify(token.value !== void 0 ? token.value : token);
        var lexerMessage = this.lexer.formatError(token, "Syntax error");
        return this.reportErrorCommon(lexerMessage, tokenDisplay);
      };
      Parser.prototype.reportErrorCommon = function(lexerMessage, tokenDisplay) {
        var lines = [];
        lines.push(lexerMessage);
        var lastColumnIndex = this.table.length - 2;
        var lastColumn = this.table[lastColumnIndex];
        var expectantStates = lastColumn.states.filter(function(state) {
          var nextSymbol = state.rule.symbols[state.dot];
          return nextSymbol && typeof nextSymbol !== "string";
        });
        if (expectantStates.length === 0) {
          lines.push("Unexpected " + tokenDisplay + ". I did not expect any more input. Here is the state of my parse table:\n");
          this.displayStateStack(lastColumn.states, lines);
        } else {
          lines.push("Unexpected " + tokenDisplay + ". Instead, I was expecting to see one of the following:\n");
          var stateStacks = expectantStates.map(function(state) {
            return this.buildFirstStateStack(state, []) || [state];
          }, this);
          stateStacks.forEach(function(stateStack) {
            var state = stateStack[0];
            var nextSymbol = state.rule.symbols[state.dot];
            var symbolDisplay = this.getSymbolDisplay(nextSymbol);
            lines.push("A " + symbolDisplay + " based on:");
            this.displayStateStack(stateStack, lines);
          }, this);
        }
        lines.push("");
        return lines.join("\n");
      };
      Parser.prototype.displayStateStack = function(stateStack, lines) {
        var lastDisplay;
        var sameDisplayCount = 0;
        for (var j = 0; j < stateStack.length; j++) {
          var state = stateStack[j];
          var display = state.rule.toString(state.dot);
          if (display === lastDisplay) {
            sameDisplayCount++;
          } else {
            if (sameDisplayCount > 0) {
              lines.push("    ^ " + sameDisplayCount + " more lines identical to this");
            }
            sameDisplayCount = 0;
            lines.push("    " + display);
          }
          lastDisplay = display;
        }
      };
      Parser.prototype.getSymbolDisplay = function(symbol) {
        return getSymbolLongDisplay(symbol);
      };
      Parser.prototype.buildFirstStateStack = function(state, visited) {
        if (visited.indexOf(state) !== -1) {
          return null;
        }
        if (state.wantedBy.length === 0) {
          return [state];
        }
        var prevState = state.wantedBy[0];
        var childVisited = [state].concat(visited);
        var childResult = this.buildFirstStateStack(prevState, childVisited);
        if (childResult === null) {
          return null;
        }
        return [state].concat(childResult);
      };
      Parser.prototype.save = function() {
        var column = this.table[this.current];
        column.lexerState = this.lexerState;
        return column;
      };
      Parser.prototype.restore = function(column) {
        var index = column.index;
        this.current = index;
        this.table[index] = column;
        this.table.splice(index + 1);
        this.lexerState = column.lexerState;
        this.results = this.finish();
      };
      Parser.prototype.rewind = function(index) {
        if (!this.options.keepHistory) {
          throw new Error("set option `keepHistory` to enable rewinding");
        }
        this.restore(this.table[index]);
      };
      Parser.prototype.finish = function() {
        var considerations = [];
        var start = this.grammar.start;
        var column = this.table[this.table.length - 1];
        column.states.forEach(function(t) {
          if (t.rule.name === start && t.dot === t.rule.symbols.length && t.reference === 0 && t.data !== Parser.fail) {
            considerations.push(t);
          }
        });
        return considerations.map(function(c) {
          return c.data;
        });
      };
      function getSymbolLongDisplay(symbol) {
        var type = typeof symbol;
        if (type === "string") {
          return symbol;
        } else if (type === "object") {
          if (symbol.literal) {
            return JSON.stringify(symbol.literal);
          } else if (symbol instanceof RegExp) {
            return "character matching " + symbol;
          } else if (symbol.type) {
            return symbol.type + " token";
          } else if (symbol.test) {
            return "token matching " + String(symbol.test);
          } else {
            throw new Error("Unknown symbol type: " + symbol);
          }
        }
      }
      function getSymbolShortDisplay(symbol) {
        var type = typeof symbol;
        if (type === "string") {
          return symbol;
        } else if (type === "object") {
          if (symbol.literal) {
            return JSON.stringify(symbol.literal);
          } else if (symbol instanceof RegExp) {
            return symbol.toString();
          } else if (symbol.type) {
            return "%" + symbol.type;
          } else if (symbol.test) {
            return "<" + String(symbol.test) + ">";
          } else {
            throw new Error("Unknown symbol type: " + symbol);
          }
        }
      }
      return {
        Parser,
        Grammar,
        Rule
      };
    });
  }
});

// ../node_modules/.pnpm/mojangson@2.0.4/node_modules/mojangson/grammar.js
var require_grammar = __commonJS({
  "../node_modules/.pnpm/mojangson@2.0.4/node_modules/mojangson/grammar.js"(exports2, module2) {
    (function() {
      function id(x) {
        return x[0];
      }
      function parseValue(str) {
        if (str === "true")
          return { type: "boolean", value: true };
        if (str === "false")
          return { type: "boolean", value: false };
        const suffixes = "bslfdi";
        const suffixToType = { "b": "byte", "s": "short", "l": "long", "f": "float", "d": "double", "i": "int" };
        const lastC = str.charAt(str.length - 1).toLowerCase();
        if (suffixes.indexOf(lastC) !== -1) {
          const v2 = parseFloat(str.substring(0, str.length - 1));
          if (!isNaN(v2))
            return { value: v2, type: suffixToType[lastC] };
          return { value: str, type: "string" };
        }
        const v = parseFloat(str);
        const decimal = str.includes(".");
        const isInt32 = v >> 0 === v;
        if (!isNaN(str) && (decimal || isInt32))
          return { value: v, type: decimal ? "double" : "int" };
        return { value: str, type: "string" };
      }
      function extractPair(kv, output) {
        if (kv[0] !== void 0) {
          output[kv[0]] = kv[1];
        }
      }
      function extractObject(d) {
        let output = {};
        extractPair(d[2], output);
        for (let i in d[3]) {
          extractPair(d[3][i][3], output);
        }
        return { type: "compound", value: output };
      }
      function extractTypedArray(d) {
        let output = [d[6]];
        for (let i in d[7]) {
          output.push(d[7][i][3]);
        }
        const type = { "B": "byteArray", "I": "intArray", "L": "longArray" }[d[2]];
        return { type, value: { type: output[0].type, value: output.map((x) => x.value) } };
      }
      function extractArray(d) {
        let output = [d[2]];
        for (let i in d[3]) {
          output.push(d[3][i][3]);
        }
        return { type: "list", value: { type: output[0].type, value: output.map((x) => x.value) } };
      }
      function extractArrayPair(d) {
        let output = [];
        extractPair(d[2], output);
        for (let i in d[3]) {
          extractPair(d[3][i][3], output);
        }
        return { type: "list", value: { type: output[0].type, value: output.map((x) => x.value) } };
      }
      var grammar = {
        Lexer: void 0,
        ParserRules: [
          { "name": "_$ebnf$1", "symbols": [] },
          { "name": "_$ebnf$1", "symbols": ["_$ebnf$1", "wschar"], "postprocess": function arrpush(d) {
            return d[0].concat([d[1]]);
          } },
          { "name": "_", "symbols": ["_$ebnf$1"], "postprocess": function(d) {
            return null;
          } },
          { "name": "__$ebnf$1", "symbols": ["wschar"] },
          { "name": "__$ebnf$1", "symbols": ["__$ebnf$1", "wschar"], "postprocess": function arrpush(d) {
            return d[0].concat([d[1]]);
          } },
          { "name": "__", "symbols": ["__$ebnf$1"], "postprocess": function(d) {
            return null;
          } },
          { "name": "wschar", "symbols": [/[ \t\n\v\f]/], "postprocess": id },
          { "name": "MAIN", "symbols": ["_", "JVALUE", "_"], "postprocess": (d) => d[1] },
          { "name": "JVALUE", "symbols": ["JOBJECT"], "postprocess": (d) => d[0] },
          { "name": "JVALUE", "symbols": [{ "literal": "'" }, "_", "JOBJECT", "_", { "literal": "'" }], "postprocess": (d) => d[2] },
          { "name": "JVALUE", "symbols": ["JARRAY"], "postprocess": (d) => d[0] },
          { "name": "JVALUE", "symbols": ["STRING"], "postprocess": (d) => d[0] },
          { "name": "JVALUE$string$1", "symbols": [{ "literal": "n" }, { "literal": "u" }, { "literal": "l" }, { "literal": "l" }], "postprocess": function joiner(d) {
            return d.join("");
          } },
          { "name": "JVALUE", "symbols": ["JVALUE$string$1"], "postprocess": (d) => null },
          { "name": "JOBJECT", "symbols": [{ "literal": "{" }, "_", { "literal": "}" }], "postprocess": (d) => {
            return { type: "compound", value: {} };
          } },
          { "name": "JOBJECT$ebnf$1", "symbols": [] },
          { "name": "JOBJECT$ebnf$1$subexpression$1", "symbols": ["_", { "literal": "," }, "_", "PAIR"] },
          { "name": "JOBJECT$ebnf$1", "symbols": ["JOBJECT$ebnf$1", "JOBJECT$ebnf$1$subexpression$1"], "postprocess": function arrpush(d) {
            return d[0].concat([d[1]]);
          } },
          { "name": "JOBJECT$ebnf$2$subexpression$1", "symbols": ["_", { "literal": "," }] },
          { "name": "JOBJECT$ebnf$2", "symbols": ["JOBJECT$ebnf$2$subexpression$1"], "postprocess": id },
          { "name": "JOBJECT$ebnf$2", "symbols": [], "postprocess": function(d) {
            return null;
          } },
          { "name": "JOBJECT", "symbols": [{ "literal": "{" }, "_", "PAIR", "JOBJECT$ebnf$1", "JOBJECT$ebnf$2", { "literal": "}" }], "postprocess": extractObject },
          { "name": "JARRAY", "symbols": [{ "literal": "[" }, "_", { "literal": "]" }], "postprocess": (d) => {
            return { type: "list", value: {} };
          } },
          { "name": "JARRAY$ebnf$1", "symbols": [] },
          { "name": "JARRAY$ebnf$1$subexpression$1", "symbols": ["_", { "literal": "," }, "_", "JVALUE"] },
          { "name": "JARRAY$ebnf$1", "symbols": ["JARRAY$ebnf$1", "JARRAY$ebnf$1$subexpression$1"], "postprocess": function arrpush(d) {
            return d[0].concat([d[1]]);
          } },
          { "name": "JARRAY$ebnf$2$subexpression$1", "symbols": ["_", { "literal": "," }] },
          { "name": "JARRAY$ebnf$2", "symbols": ["JARRAY$ebnf$2$subexpression$1"], "postprocess": id },
          { "name": "JARRAY$ebnf$2", "symbols": [], "postprocess": function(d) {
            return null;
          } },
          { "name": "JARRAY", "symbols": [{ "literal": "[" }, "_", /[BIL]/, "_", { "literal": ";" }, "_", "JVALUE", "JARRAY$ebnf$1", "JARRAY$ebnf$2", "_", { "literal": "]" }], "postprocess": extractTypedArray },
          { "name": "JARRAY$ebnf$3", "symbols": [] },
          { "name": "JARRAY$ebnf$3$subexpression$1", "symbols": ["_", { "literal": "," }, "_", "JVALUE"] },
          { "name": "JARRAY$ebnf$3", "symbols": ["JARRAY$ebnf$3", "JARRAY$ebnf$3$subexpression$1"], "postprocess": function arrpush(d) {
            return d[0].concat([d[1]]);
          } },
          { "name": "JARRAY$ebnf$4$subexpression$1", "symbols": ["_", { "literal": "," }] },
          { "name": "JARRAY$ebnf$4", "symbols": ["JARRAY$ebnf$4$subexpression$1"], "postprocess": id },
          { "name": "JARRAY$ebnf$4", "symbols": [], "postprocess": function(d) {
            return null;
          } },
          { "name": "JARRAY", "symbols": [{ "literal": "[" }, "_", "JVALUE", "JARRAY$ebnf$3", "JARRAY$ebnf$4", "_", { "literal": "]" }], "postprocess": extractArray },
          { "name": "JARRAY$ebnf$5", "symbols": [] },
          { "name": "JARRAY$ebnf$5$subexpression$1", "symbols": ["_", { "literal": "," }, "_", "PAIR"] },
          { "name": "JARRAY$ebnf$5", "symbols": ["JARRAY$ebnf$5", "JARRAY$ebnf$5$subexpression$1"], "postprocess": function arrpush(d) {
            return d[0].concat([d[1]]);
          } },
          { "name": "JARRAY$ebnf$6$subexpression$1", "symbols": ["_", { "literal": "," }] },
          { "name": "JARRAY$ebnf$6", "symbols": ["JARRAY$ebnf$6$subexpression$1"], "postprocess": id },
          { "name": "JARRAY$ebnf$6", "symbols": [], "postprocess": function(d) {
            return null;
          } },
          { "name": "JARRAY", "symbols": [{ "literal": "[" }, "_", "PAIR", "JARRAY$ebnf$5", "JARRAY$ebnf$6", "_", { "literal": "]" }], "postprocess": extractArrayPair },
          { "name": "PAIR", "symbols": ["STRING", "_", { "literal": ":" }, "_", "JVALUE"], "postprocess": (d) => [d[0].value, d[4]] },
          { "name": "STRING$ebnf$1", "symbols": [] },
          { "name": "STRING$ebnf$1$subexpression$1", "symbols": [/[^\\"]/] },
          { "name": "STRING$ebnf$1$subexpression$1", "symbols": [{ "literal": "\\" }, /["bfnrt\/\\]/] },
          { "name": "STRING$ebnf$1$subexpression$1$string$1", "symbols": [{ "literal": "\\" }, { "literal": "u" }], "postprocess": function joiner(d) {
            return d.join("");
          } },
          { "name": "STRING$ebnf$1$subexpression$1", "symbols": ["STRING$ebnf$1$subexpression$1$string$1", /[a-fA-F0-9]/, /[a-fA-F0-9]/, /[a-fA-F0-9]/, /[a-fA-F0-9]/] },
          { "name": "STRING$ebnf$1", "symbols": ["STRING$ebnf$1", "STRING$ebnf$1$subexpression$1"], "postprocess": function arrpush(d) {
            return d[0].concat([d[1]]);
          } },
          { "name": "STRING", "symbols": [{ "literal": '"' }, "STRING$ebnf$1", { "literal": '"' }], "postprocess": (d) => parseValue(JSON.parse(d.flat(3).map((b) => b.replace("\n", "\\n")).join(""))) },
          { "name": "STRING$ebnf$2", "symbols": [/[^\"\'}\]:;,\s]/] },
          { "name": "STRING$ebnf$2", "symbols": ["STRING$ebnf$2", /[^\"\'}\]:;,\s]/], "postprocess": function arrpush(d) {
            return d[0].concat([d[1]]);
          } },
          { "name": "STRING", "symbols": ["STRING$ebnf$2"], "postprocess": (d) => parseValue(d[0].join("")) }
        ],
        ParserStart: "MAIN"
      };
      if (typeof module2 !== "undefined" && typeof module2.exports !== "undefined") {
        module2.exports = grammar;
      } else {
        window.grammar = grammar;
      }
    })();
  }
});

// ../node_modules/.pnpm/mojangson@2.0.4/node_modules/mojangson/index.js
var require_mojangson = __commonJS({
  "../node_modules/.pnpm/mojangson@2.0.4/node_modules/mojangson/index.js"(exports2, module2) {
    var nearley = require_nearley();
    var grammar = require_grammar();
    function simplify(data) {
      function transform(value, type) {
        if (type === "compound") {
          return Object.keys(value).reduce(function(acc, key) {
            acc[key] = simplify(value[key]);
            return acc;
          }, {});
        }
        if (type === "list") {
          if (!Array.isArray(value.value))
            return [];
          return value.value.map(function(v) {
            return transform(v, value.type);
          });
        }
        return value;
      }
      return transform(data.value, data.type);
    }
    function stringify({ value, type }) {
      if (type === "compound") {
        const str2 = [];
        const entries = Object.entries(value);
        for (let i = 0; i < entries.length; i++) {
          const _type = entries[i][0];
          let _value = stringify(entries[i][1]);
          if (_type === "string")
            _value = normalizeString(_value);
          str2.push(`${_type}:${_value}`);
        }
        return `{${str2.join(",")}}`;
      } else if (type === "list") {
        if (!Array.isArray(value.value))
          return "[]";
        const arrayElements = getArrayValues(value);
        return `[${arrayElements}]`;
      } else if (type === "byteArray" || type === "intArray" || type === "longArray") {
        const prefix = getArrayPrefix(type);
        const arrayElements = getArrayValues(value);
        return `[${prefix}${arrayElements}]`;
      }
      let str = value + getSuffix(value, type);
      if (type === "string")
        str = normalizeString(str);
      return str;
    }
    function normalizeString(str) {
      str = str.replace(/"/g, '\\"');
      if (/'|{|}|\[|\]|:|;|,|\(|\)|§|=/g.test(str) || str === "")
        str = `"${str}"`;
      return str;
    }
    function getArrayValues({ value: arr, type }) {
      const hasMissingEl = hasMissingElements(arr);
      const str = [];
      for (let i = 0; i < arr.length; i++) {
        let curr = arr[i];
        if (curr !== void 0) {
          curr = stringify({ value: curr, type });
          if (hasMissingEl)
            str.push(`${i}:${curr}`);
          else
            str.push(curr);
        }
      }
      return str.join(",");
    }
    function hasMissingElements(arr) {
      for (let i = 0; i < arr.length; i++) {
        if (arr[i] === void 0)
          return true;
      }
      return false;
    }
    var getArrayPrefix = (type) => type.charAt(0).toUpperCase() + ";";
    function getSuffix(val, type) {
      if (type === "double")
        return val >> 0 === val ? "d" : "";
      return { int: "", byte: "b", short: "s", float: "f", long: "l", string: "" }[type];
    }
    function normalize(str) {
      return stringify(parse(str));
    }
    function parse(text) {
      try {
        const parserNE = new nearley.Parser(nearley.Grammar.fromCompiled(grammar));
        parserNE.feed(text);
        return parserNE.results[0];
      } catch (e) {
        e.message = `Error parsing text '${text}'`;
        throw e;
      }
    }
    module2.exports = {
      parse,
      simplify,
      stringify,
      normalize
    };
  }
});

// ../node_modules/.pnpm/prismarine-chat@1.12.0/node_modules/prismarine-chat/format.js
var require_format2 = __commonJS({
  "../node_modules/.pnpm/prismarine-chat@1.12.0/node_modules/prismarine-chat/format.js"(exports2, module2) {
    module2.exports = format;
    function format(string, _with = []) {
      let i = 0;
      return string.replace(/%(?:(\d+)\$)?(s|%)/g, (g0, g1) => {
        if (g0 === "%%") {
          return "%";
        } else {
          const idx = g1 ? parseInt(g1) - 1 : i++;
          if (_with[idx] !== void 0) {
            return _with[idx];
          } else {
            return "";
          }
        }
      });
    }
  }
});

// ../node_modules/.pnpm/ms@2.1.3/node_modules/ms/index.js
var require_ms = __commonJS({
  "../node_modules/.pnpm/ms@2.1.3/node_modules/ms/index.js"(exports2, module2) {
    var s = 1e3;
    var m = s * 60;
    var h = m * 60;
    var d = h * 24;
    var w = d * 7;
    var y = d * 365.25;
    module2.exports = function(val, options) {
      options = options || {};
      var type = typeof val;
      if (type === "string" && val.length > 0) {
        return parse(val);
      } else if (type === "number" && isFinite(val)) {
        return options.long ? fmtLong(val) : fmtShort(val);
      }
      throw new Error(
        "val is not a non-empty string or a valid number. val=" + JSON.stringify(val)
      );
    };
    function parse(str) {
      str = String(str);
      if (str.length > 100) {
        return;
      }
      var match = /^(-?(?:\d+)?\.?\d+) *(milliseconds?|msecs?|ms|seconds?|secs?|s|minutes?|mins?|m|hours?|hrs?|h|days?|d|weeks?|w|years?|yrs?|y)?$/i.exec(
        str
      );
      if (!match) {
        return;
      }
      var n = parseFloat(match[1]);
      var type = (match[2] || "ms").toLowerCase();
      switch (type) {
        case "years":
        case "year":
        case "yrs":
        case "yr":
        case "y":
          return n * y;
        case "weeks":
        case "week":
        case "w":
          return n * w;
        case "days":
        case "day":
        case "d":
          return n * d;
        case "hours":
        case "hour":
        case "hrs":
        case "hr":
        case "h":
          return n * h;
        case "minutes":
        case "minute":
        case "mins":
        case "min":
        case "m":
          return n * m;
        case "seconds":
        case "second":
        case "secs":
        case "sec":
        case "s":
          return n * s;
        case "milliseconds":
        case "millisecond":
        case "msecs":
        case "msec":
        case "ms":
          return n;
        default:
          return void 0;
      }
    }
    function fmtShort(ms) {
      var msAbs = Math.abs(ms);
      if (msAbs >= d) {
        return Math.round(ms / d) + "d";
      }
      if (msAbs >= h) {
        return Math.round(ms / h) + "h";
      }
      if (msAbs >= m) {
        return Math.round(ms / m) + "m";
      }
      if (msAbs >= s) {
        return Math.round(ms / s) + "s";
      }
      return ms + "ms";
    }
    function fmtLong(ms) {
      var msAbs = Math.abs(ms);
      if (msAbs >= d) {
        return plural(ms, msAbs, d, "day");
      }
      if (msAbs >= h) {
        return plural(ms, msAbs, h, "hour");
      }
      if (msAbs >= m) {
        return plural(ms, msAbs, m, "minute");
      }
      if (msAbs >= s) {
        return plural(ms, msAbs, s, "second");
      }
      return ms + " ms";
    }
    function plural(ms, msAbs, n, name) {
      var isPlural = msAbs >= n * 1.5;
      return Math.round(ms / n) + " " + name + (isPlural ? "s" : "");
    }
  }
});

// ../node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/common.js
var require_common = __commonJS({
  "../node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/common.js"(exports2, module2) {
    function setup(env) {
      createDebug.debug = createDebug;
      createDebug.default = createDebug;
      createDebug.coerce = coerce;
      createDebug.disable = disable;
      createDebug.enable = enable;
      createDebug.enabled = enabled;
      createDebug.humanize = require_ms();
      createDebug.destroy = destroy;
      Object.keys(env).forEach((key) => {
        createDebug[key] = env[key];
      });
      createDebug.names = [];
      createDebug.skips = [];
      createDebug.formatters = {};
      function selectColor(namespace) {
        let hash = 0;
        for (let i = 0; i < namespace.length; i++) {
          hash = (hash << 5) - hash + namespace.charCodeAt(i);
          hash |= 0;
        }
        return createDebug.colors[Math.abs(hash) % createDebug.colors.length];
      }
      createDebug.selectColor = selectColor;
      function createDebug(namespace) {
        let prevTime;
        let enableOverride = null;
        let namespacesCache;
        let enabledCache;
        function debug(...args) {
          if (!debug.enabled) {
            return;
          }
          const self2 = debug;
          const curr = Number(/* @__PURE__ */ new Date());
          const ms = curr - (prevTime || curr);
          self2.diff = ms;
          self2.prev = prevTime;
          self2.curr = curr;
          prevTime = curr;
          args[0] = createDebug.coerce(args[0]);
          if (typeof args[0] !== "string") {
            args.unshift("%O");
          }
          let index = 0;
          args[0] = args[0].replace(/%([a-zA-Z%])/g, (match, format) => {
            if (match === "%%") {
              return "%";
            }
            index++;
            const formatter = createDebug.formatters[format];
            if (typeof formatter === "function") {
              const val = args[index];
              match = formatter.call(self2, val);
              args.splice(index, 1);
              index--;
            }
            return match;
          });
          createDebug.formatArgs.call(self2, args);
          const logFn = self2.log || createDebug.log;
          logFn.apply(self2, args);
        }
        debug.namespace = namespace;
        debug.useColors = createDebug.useColors();
        debug.color = createDebug.selectColor(namespace);
        debug.extend = extend;
        debug.destroy = createDebug.destroy;
        Object.defineProperty(debug, "enabled", {
          enumerable: true,
          configurable: false,
          get: () => {
            if (enableOverride !== null) {
              return enableOverride;
            }
            if (namespacesCache !== createDebug.namespaces) {
              namespacesCache = createDebug.namespaces;
              enabledCache = createDebug.enabled(namespace);
            }
            return enabledCache;
          },
          set: (v) => {
            enableOverride = v;
          }
        });
        if (typeof createDebug.init === "function") {
          createDebug.init(debug);
        }
        return debug;
      }
      function extend(namespace, delimiter) {
        const newDebug = createDebug(this.namespace + (typeof delimiter === "undefined" ? ":" : delimiter) + namespace);
        newDebug.log = this.log;
        return newDebug;
      }
      function enable(namespaces) {
        createDebug.save(namespaces);
        createDebug.namespaces = namespaces;
        createDebug.names = [];
        createDebug.skips = [];
        const split = (typeof namespaces === "string" ? namespaces : "").trim().replace(/\s+/g, ",").split(",").filter(Boolean);
        for (const ns of split) {
          if (ns[0] === "-") {
            createDebug.skips.push(ns.slice(1));
          } else {
            createDebug.names.push(ns);
          }
        }
      }
      function matchesTemplate(search, template) {
        let searchIndex = 0;
        let templateIndex = 0;
        let starIndex = -1;
        let matchIndex = 0;
        while (searchIndex < search.length) {
          if (templateIndex < template.length && (template[templateIndex] === search[searchIndex] || template[templateIndex] === "*")) {
            if (template[templateIndex] === "*") {
              starIndex = templateIndex;
              matchIndex = searchIndex;
              templateIndex++;
            } else {
              searchIndex++;
              templateIndex++;
            }
          } else if (starIndex !== -1) {
            templateIndex = starIndex + 1;
            matchIndex++;
            searchIndex = matchIndex;
          } else {
            return false;
          }
        }
        while (templateIndex < template.length && template[templateIndex] === "*") {
          templateIndex++;
        }
        return templateIndex === template.length;
      }
      function disable() {
        const namespaces = [
          ...createDebug.names,
          ...createDebug.skips.map((namespace) => "-" + namespace)
        ].join(",");
        createDebug.enable("");
        return namespaces;
      }
      function enabled(name) {
        for (const skip of createDebug.skips) {
          if (matchesTemplate(name, skip)) {
            return false;
          }
        }
        for (const ns of createDebug.names) {
          if (matchesTemplate(name, ns)) {
            return true;
          }
        }
        return false;
      }
      function coerce(val) {
        if (val instanceof Error) {
          return val.stack || val.message;
        }
        return val;
      }
      function destroy() {
        console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.");
      }
      createDebug.enable(createDebug.load());
      return createDebug;
    }
    module2.exports = setup;
  }
});

// ../node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/browser.js
var require_browser = __commonJS({
  "../node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/browser.js"(exports2, module2) {
    exports2.formatArgs = formatArgs;
    exports2.save = save;
    exports2.load = load;
    exports2.useColors = useColors;
    exports2.storage = localstorage();
    exports2.destroy = /* @__PURE__ */ (() => {
      let warned = false;
      return () => {
        if (!warned) {
          warned = true;
          console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.");
        }
      };
    })();
    exports2.colors = [
      "#0000CC",
      "#0000FF",
      "#0033CC",
      "#0033FF",
      "#0066CC",
      "#0066FF",
      "#0099CC",
      "#0099FF",
      "#00CC00",
      "#00CC33",
      "#00CC66",
      "#00CC99",
      "#00CCCC",
      "#00CCFF",
      "#3300CC",
      "#3300FF",
      "#3333CC",
      "#3333FF",
      "#3366CC",
      "#3366FF",
      "#3399CC",
      "#3399FF",
      "#33CC00",
      "#33CC33",
      "#33CC66",
      "#33CC99",
      "#33CCCC",
      "#33CCFF",
      "#6600CC",
      "#6600FF",
      "#6633CC",
      "#6633FF",
      "#66CC00",
      "#66CC33",
      "#9900CC",
      "#9900FF",
      "#9933CC",
      "#9933FF",
      "#99CC00",
      "#99CC33",
      "#CC0000",
      "#CC0033",
      "#CC0066",
      "#CC0099",
      "#CC00CC",
      "#CC00FF",
      "#CC3300",
      "#CC3333",
      "#CC3366",
      "#CC3399",
      "#CC33CC",
      "#CC33FF",
      "#CC6600",
      "#CC6633",
      "#CC9900",
      "#CC9933",
      "#CCCC00",
      "#CCCC33",
      "#FF0000",
      "#FF0033",
      "#FF0066",
      "#FF0099",
      "#FF00CC",
      "#FF00FF",
      "#FF3300",
      "#FF3333",
      "#FF3366",
      "#FF3399",
      "#FF33CC",
      "#FF33FF",
      "#FF6600",
      "#FF6633",
      "#FF9900",
      "#FF9933",
      "#FFCC00",
      "#FFCC33"
    ];
    function useColors() {
      if (typeof window !== "undefined" && window.process && (window.process.type === "renderer" || window.process.__nwjs)) {
        return true;
      }
      if (typeof navigator !== "undefined" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/(edge|trident)\/(\d+)/)) {
        return false;
      }
      let m;
      return typeof document !== "undefined" && document.documentElement && document.documentElement.style && document.documentElement.style.WebkitAppearance || // Is firebug? http://stackoverflow.com/a/398120/376773
      typeof window !== "undefined" && window.console && (window.console.firebug || window.console.exception && window.console.table) || // Is firefox >= v31?
      // https://developer.mozilla.org/en-US/docs/Tools/Web_Console#Styling_messages
      typeof navigator !== "undefined" && navigator.userAgent && (m = navigator.userAgent.toLowerCase().match(/firefox\/(\d+)/)) && parseInt(m[1], 10) >= 31 || // Double check webkit in userAgent just in case we are in a worker
      typeof navigator !== "undefined" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/applewebkit\/(\d+)/);
    }
    function formatArgs(args) {
      args[0] = (this.useColors ? "%c" : "") + this.namespace + (this.useColors ? " %c" : " ") + args[0] + (this.useColors ? "%c " : " ") + "+" + module2.exports.humanize(this.diff);
      if (!this.useColors) {
        return;
      }
      const c = "color: " + this.color;
      args.splice(1, 0, c, "color: inherit");
      let index = 0;
      let lastC = 0;
      args[0].replace(/%[a-zA-Z%]/g, (match) => {
        if (match === "%%") {
          return;
        }
        index++;
        if (match === "%c") {
          lastC = index;
        }
      });
      args.splice(lastC, 0, c);
    }
    exports2.log = console.debug || console.log || (() => {
    });
    function save(namespaces) {
      try {
        if (namespaces) {
          exports2.storage.setItem("debug", namespaces);
        } else {
          exports2.storage.removeItem("debug");
        }
      } catch (error) {
      }
    }
    function load() {
      let r;
      try {
        r = exports2.storage.getItem("debug") || exports2.storage.getItem("DEBUG");
      } catch (error) {
      }
      if (!r && typeof process !== "undefined" && "env" in process) {
        r = process.env.DEBUG;
      }
      return r;
    }
    function localstorage() {
      try {
        return localStorage;
      } catch (error) {
      }
    }
    module2.exports = require_common()(exports2);
    var { formatters } = module2.exports;
    formatters.j = function(v) {
      try {
        return JSON.stringify(v);
      } catch (error) {
        return "[UnexpectedJSONParseError]: " + error.message;
      }
    };
  }
});

// ../node_modules/.pnpm/has-flag@3.0.0/node_modules/has-flag/index.js
var require_has_flag = __commonJS({
  "../node_modules/.pnpm/has-flag@3.0.0/node_modules/has-flag/index.js"(exports2, module2) {
    "use strict";
    module2.exports = (flag, argv) => {
      argv = argv || process.argv;
      const prefix = flag.startsWith("-") ? "" : flag.length === 1 ? "-" : "--";
      const pos = argv.indexOf(prefix + flag);
      const terminatorPos = argv.indexOf("--");
      return pos !== -1 && (terminatorPos === -1 ? true : pos < terminatorPos);
    };
  }
});

// ../node_modules/.pnpm/supports-color@5.5.0/node_modules/supports-color/index.js
var require_supports_color = __commonJS({
  "../node_modules/.pnpm/supports-color@5.5.0/node_modules/supports-color/index.js"(exports2, module2) {
    "use strict";
    var os = require("os");
    var hasFlag = require_has_flag();
    var env = process.env;
    var forceColor;
    if (hasFlag("no-color") || hasFlag("no-colors") || hasFlag("color=false")) {
      forceColor = false;
    } else if (hasFlag("color") || hasFlag("colors") || hasFlag("color=true") || hasFlag("color=always")) {
      forceColor = true;
    }
    if ("FORCE_COLOR" in env) {
      forceColor = env.FORCE_COLOR.length === 0 || parseInt(env.FORCE_COLOR, 10) !== 0;
    }
    function translateLevel(level) {
      if (level === 0) {
        return false;
      }
      return {
        level,
        hasBasic: true,
        has256: level >= 2,
        has16m: level >= 3
      };
    }
    function supportsColor(stream) {
      if (forceColor === false) {
        return 0;
      }
      if (hasFlag("color=16m") || hasFlag("color=full") || hasFlag("color=truecolor")) {
        return 3;
      }
      if (hasFlag("color=256")) {
        return 2;
      }
      if (stream && !stream.isTTY && forceColor !== true) {
        return 0;
      }
      const min = forceColor ? 1 : 0;
      if (process.platform === "win32") {
        const osRelease = os.release().split(".");
        if (Number(process.versions.node.split(".")[0]) >= 8 && Number(osRelease[0]) >= 10 && Number(osRelease[2]) >= 10586) {
          return Number(osRelease[2]) >= 14931 ? 3 : 2;
        }
        return 1;
      }
      if ("CI" in env) {
        if (["TRAVIS", "CIRCLECI", "APPVEYOR", "GITLAB_CI"].some((sign) => sign in env) || env.CI_NAME === "codeship") {
          return 1;
        }
        return min;
      }
      if ("TEAMCITY_VERSION" in env) {
        return /^(9\.(0*[1-9]\d*)\.|\d{2,}\.)/.test(env.TEAMCITY_VERSION) ? 1 : 0;
      }
      if (env.COLORTERM === "truecolor") {
        return 3;
      }
      if ("TERM_PROGRAM" in env) {
        const version = parseInt((env.TERM_PROGRAM_VERSION || "").split(".")[0], 10);
        switch (env.TERM_PROGRAM) {
          case "iTerm.app":
            return version >= 3 ? 3 : 2;
          case "Apple_Terminal":
            return 2;
        }
      }
      if (/-256(color)?$/i.test(env.TERM)) {
        return 2;
      }
      if (/^screen|^xterm|^vt100|^vt220|^rxvt|color|ansi|cygwin|linux/i.test(env.TERM)) {
        return 1;
      }
      if ("COLORTERM" in env) {
        return 1;
      }
      if (env.TERM === "dumb") {
        return min;
      }
      return min;
    }
    function getSupportLevel(stream) {
      const level = supportsColor(stream);
      return translateLevel(level);
    }
    module2.exports = {
      supportsColor: getSupportLevel,
      stdout: getSupportLevel(process.stdout),
      stderr: getSupportLevel(process.stderr)
    };
  }
});

// ../node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/node.js
var require_node = __commonJS({
  "../node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/node.js"(exports2, module2) {
    var tty = require("tty");
    var util = require("util");
    exports2.init = init;
    exports2.log = log;
    exports2.formatArgs = formatArgs;
    exports2.save = save;
    exports2.load = load;
    exports2.useColors = useColors;
    exports2.destroy = util.deprecate(
      () => {
      },
      "Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`."
    );
    exports2.colors = [6, 2, 3, 4, 5, 1];
    try {
      const supportsColor = require_supports_color();
      if (supportsColor && (supportsColor.stderr || supportsColor).level >= 2) {
        exports2.colors = [
          20,
          21,
          26,
          27,
          32,
          33,
          38,
          39,
          40,
          41,
          42,
          43,
          44,
          45,
          56,
          57,
          62,
          63,
          68,
          69,
          74,
          75,
          76,
          77,
          78,
          79,
          80,
          81,
          92,
          93,
          98,
          99,
          112,
          113,
          128,
          129,
          134,
          135,
          148,
          149,
          160,
          161,
          162,
          163,
          164,
          165,
          166,
          167,
          168,
          169,
          170,
          171,
          172,
          173,
          178,
          179,
          184,
          185,
          196,
          197,
          198,
          199,
          200,
          201,
          202,
          203,
          204,
          205,
          206,
          207,
          208,
          209,
          214,
          215,
          220,
          221
        ];
      }
    } catch (error) {
    }
    exports2.inspectOpts = Object.keys(process.env).filter((key) => {
      return /^debug_/i.test(key);
    }).reduce((obj, key) => {
      const prop = key.substring(6).toLowerCase().replace(/_([a-z])/g, (_, k) => {
        return k.toUpperCase();
      });
      let val = process.env[key];
      if (/^(yes|on|true|enabled)$/i.test(val)) {
        val = true;
      } else if (/^(no|off|false|disabled)$/i.test(val)) {
        val = false;
      } else if (val === "null") {
        val = null;
      } else {
        val = Number(val);
      }
      obj[prop] = val;
      return obj;
    }, {});
    function useColors() {
      return "colors" in exports2.inspectOpts ? Boolean(exports2.inspectOpts.colors) : tty.isatty(process.stderr.fd);
    }
    function formatArgs(args) {
      const { namespace: name, useColors: useColors2 } = this;
      if (useColors2) {
        const c = this.color;
        const colorCode = "\x1B[3" + (c < 8 ? c : "8;5;" + c);
        const prefix = `  ${colorCode};1m${name} \x1B[0m`;
        args[0] = prefix + args[0].split("\n").join("\n" + prefix);
        args.push(colorCode + "m+" + module2.exports.humanize(this.diff) + "\x1B[0m");
      } else {
        args[0] = getDate() + name + " " + args[0];
      }
    }
    function getDate() {
      if (exports2.inspectOpts.hideDate) {
        return "";
      }
      return (/* @__PURE__ */ new Date()).toISOString() + " ";
    }
    function log(...args) {
      return process.stderr.write(util.formatWithOptions(exports2.inspectOpts, ...args) + "\n");
    }
    function save(namespaces) {
      if (namespaces) {
        process.env.DEBUG = namespaces;
      } else {
        delete process.env.DEBUG;
      }
    }
    function load() {
      return process.env.DEBUG;
    }
    function init(debug) {
      debug.inspectOpts = {};
      const keys = Object.keys(exports2.inspectOpts);
      for (let i = 0; i < keys.length; i++) {
        debug.inspectOpts[keys[i]] = exports2.inspectOpts[keys[i]];
      }
    }
    module2.exports = require_common()(exports2);
    var { formatters } = module2.exports;
    formatters.o = function(v) {
      this.inspectOpts.colors = this.useColors;
      return util.inspect(v, this.inspectOpts).split("\n").map((str) => str.trim()).join(" ");
    };
    formatters.O = function(v) {
      this.inspectOpts.colors = this.useColors;
      return util.inspect(v, this.inspectOpts);
    };
  }
});

// ../node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/index.js
var require_src2 = __commonJS({
  "../node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/index.js"(exports2, module2) {
    if (typeof process === "undefined" || process.type === "renderer" || process.browser === true || process.__nwjs) {
      module2.exports = require_browser();
    } else {
      module2.exports = require_node();
    }
  }
});

// ../node_modules/.pnpm/prismarine-chat@1.12.0/node_modules/prismarine-chat/MessageBuilder.js
var require_MessageBuilder = __commonJS({
  "../node_modules/.pnpm/prismarine-chat@1.12.0/node_modules/prismarine-chat/MessageBuilder.js"(exports2, module2) {
    var mojangson = require_mojangson();
    var nbt = require_nbt2();
    var supportedColors = {
      0: "black",
      1: "dark_blue",
      2: "dark_green",
      3: "dark_aqua",
      4: "dark_red",
      5: "dark_purple",
      6: "gold",
      7: "gray",
      8: "dark_gray",
      9: "blue",
      a: "green",
      b: "aqua",
      c: "red",
      d: "light_purple",
      e: "yellow",
      f: "white",
      k: "obfuscated",
      l: "bold",
      m: "strikethrough",
      n: "underlined",
      o: "italic",
      r: "reset"
    };
    function loader(registry) {
      class MessageBuilder {
        constructor() {
          this.with = [];
          this.extra = [];
        }
        /** @param {boolean} val */
        setBold(val) {
          this.bold = val;
          return this;
        }
        /** @param {boolean} val */
        setItalic(val) {
          this.italic = val;
          return this;
        }
        /** @param {boolean} val */
        setUnderlined(val) {
          this.underlined = val;
          return this;
        }
        /** @param {boolean} val */
        setStrikethrough(val) {
          this.strikethrough = val;
          return this;
        }
        /** @param {boolean} val */
        setObfuscated(val) {
          this.obfuscated = val;
          return this;
        }
        /** @param {string} val */
        setColor(val) {
          this.color = val;
          return this;
        }
        /** @param {string} val */
        setText(val) {
          this.text = val;
          return this;
        }
        /**
         * The resource location of the font for this component in the resource pack within `assets/<namespace>/font`.
         * @param {string} val Defaults to `minecraft:default`
         * @returns
         */
        setFont(val) {
          this.font = val;
          return this;
        }
        /**
         * When used, it's expected that all slots for text will be filled using .addWith()
         * @param {string} val
         * @returns
         */
        setTranslate(val) {
          this.translate = val;
          return this;
        }
        /**
         * text shown when shift clicked on message
         * @param {string} val
         */
        setInsertion(val) {
          this.insertion = val;
          return this;
        }
        /**
         * Overrode by .setText()
         * @param {string} val
         * @example
         * builder.setKeybind('key.inventory')
         */
        setKeybind(val) {
          this.keybind = val;
          return this;
        }
        /**
         * Displays a score holder's current score in an objective.
         * Displays nothing if the given score holder or the given objective do not exist, or if the score holder is not tracked in the objective.
         * @param {string} name if '*', show reader their own score. Otherwise, this is the player's score shown. Can be a selector string, but must never select more than one entity.
         * @param {string} objective The internal name of the objective to display the player's score in.
         */
        setScore(name, objective) {
          this.score = { name, objective };
          return this;
        }
        /**
         * @param {'open_url'|'run_command'|'suggest_command'|'change_page'|'copy_to_clipboard'} action
         * @param {string|number} value
         * @example
         * builder.setClickEvent('open_url', 'https://google.com')
         * builder.setClickEvent('run_command', '/say Hi!') // for signs, the slash doesn't need to be there
         * builder.setClickEvent('suggest_command', '/say ')
         * builder.setClickEvent('change_page', '/say Hi!') // Can only be used in written books
         * builder.setClickEvent('copy_to_clipboard', 'welcome to your clipboard')
         */
        setClickEvent(action, value) {
          this.clickEvent = { action, value };
          return this;
        }
        /**
         * @param {'show_text'|'show_entity'|'show_item'} action
         * @param {import('prismarine-item').Item|import('prismarine-entity').Entity|MessageBuilder} data
         * @param {'contents'|'value'} type [type='contents']
         */
        setHoverEvent(action, data, type = "contents") {
          const hoverEvent = { action };
          if (type === "contents") {
            switch (action) {
              case "show_item":
                hoverEvent.contents = {
                  id: `minecraft:${data.name}`,
                  count: data.count,
                  tag: data.nbt ? mojangson.stringify(data.nbt) : {}
                };
                break;
              case "show_entity":
                hoverEvent.contents = {
                  name: data.displayName,
                  type: `minecraft:${data.name}`,
                  id: data.uuid
                };
                break;
              case "show_text":
                hoverEvent.contents = data;
                break;
              default:
                throw Error("Not implemented");
            }
          } else if (type === "value") {
            switch (action) {
              case "show_item":
                hoverEvent.value = mojangson.stringify(nbt.comp({
                  id: nbt.string(`minecraft:${data.name}`),
                  Count: nbt.byte(data.count),
                  tag: data.nbt || nbt.comp({}),
                  Damage: nbt.short(0)
                }));
                break;
              case "show_text":
                hoverEvent.value = data.toString();
                break;
              case "show_entity":
              default:
                throw Error("Not implemented");
            }
          }
          this.hoverEvent = hoverEvent;
          return this;
        }
        /**
         * appended to the end of this message object with the existing formatting.
         * formatting can be overrode in child messagebuilder
         * @param {Array<MessageBuilder | string>} ...args
         * @returns
         */
        addExtra(...args) {
          for (const v of args) {
            const value = typeof v === "string" ? v : v.toJSON();
            this.extra.push(value);
          }
          return this;
        }
        /**
         * requires .translate to be set for this to be used
         * @param {Array<MessageBuilder | string>} ...args
         * @returns
         */
        addWith(...args) {
          for (const v of args) {
            const value = typeof v === "string" ? v : v.toJSON();
            this.with.push(value);
          }
          return this;
        }
        resetFormatting() {
          this.setBold(false);
          this.setItalic(false);
          this.setUnderlined(false);
          this.setStrikethrough(false);
          this.setObfuscated(false);
          this.setColor("reset");
        }
        toJSON() {
          const isDef = (x) => x !== void 0;
          const obj = {};
          if (isDef(this.strikethrough))
            obj.strikethrough = this.strikethrough;
          if (isDef(this.obfuscated))
            obj.obfuscated = this.obfuscated;
          if (isDef(this.underlined))
            obj.underlined = this.underlined;
          if (isDef(this.clickEvent))
            obj.clickEvent = this.clickEvent;
          if (isDef(this.hoverEvent))
            obj.hoverEvent = this.hoverEvent;
          if (isDef(this.translate))
            obj.translate = this.translate;
          if (isDef(this.insertion))
            obj.insertion = this.insertion;
          if (isDef(this.italic))
            obj.italic = this.italic;
          if (isDef(this.color))
            obj.color = this.color;
          if (isDef(this.bold))
            obj.bold = this.bold;
          if (isDef(this.font))
            obj.font = this.font;
          if (isDef(this.text)) {
            obj.text = this.text;
          } else if (isDef(this.keybind)) {
            obj.keybind = this.keybind;
          } else if (isDef(this.score)) {
            obj.score = this.score;
          }
          if (isDef(this.translate) && this.with.length > 0) {
            obj.with = this.with;
          }
          if (this.extra.length > 0) {
            obj.extra = this.extra;
          }
          return obj;
        }
        toString() {
          return JSON.stringify(this);
        }
        static fromString(str, { colorSeparator = "&" } = {}) {
          let lastObj = null;
          let currString = "";
          for (let i = str.length - 1; i > -1; i--) {
            const char = str.substring(i, i + 1);
            if (char !== colorSeparator)
              currString += char;
            else {
              const text = currString.split("").reverse();
              const color = supportedColors[text.shift()];
              const newObj = new MessageBuilder();
              if (color === "obfuscated") {
                newObj.setObfuscated(true);
              } else if (color === "bold") {
                newObj.setBold(true);
              } else if (color === "strikethrough") {
                newObj.setStrikethrough(true);
              } else if (color === "underlined") {
                newObj.setUnderlined(true);
              } else if (color === "italic") {
                newObj.setItalic(true);
              } else if (color === "reset") {
                newObj.resetFormatting();
              } else {
                newObj.setColor(color);
              }
              newObj.setText(text.join(""));
              if (lastObj === null)
                lastObj = newObj;
              else
                lastObj = newObj.addExtra(lastObj);
              currString = "";
            }
          }
          if (currString !== "") {
            const txt = currString.split("").reverse().join("");
            if (lastObj !== null)
              lastObj = new MessageBuilder().setText(txt).addExtra(lastObj);
            else
              lastObj = new MessageBuilder().setText(txt);
          }
          return lastObj;
        }
      }
      return { MessageBuilder };
    }
    module2.exports = loader;
  }
});

// ../node_modules/.pnpm/prismarine-chat@1.12.0/node_modules/prismarine-chat/index.js
var require_prismarine_chat = __commonJS({
  "../node_modules/.pnpm/prismarine-chat@1.12.0/node_modules/prismarine-chat/index.js"(exports2, module2) {
    var mojangson = require_mojangson();
    var vsprintf = require_format2();
    var debug = require_src2()("minecraft-protocol");
    var nbt = require_nbt2();
    var getValueSafely = (obj, key, def) => Object.hasOwn(obj, key) ? obj[key] : def;
    var MAX_CHAT_DEPTH = 8;
    var MAX_CHAT_LENGTH = 4096;
    function loader(registryOrVersion) {
      const registry = typeof registryOrVersion === "string" ? require_lib()(registryOrVersion) : registryOrVersion;
      const defaultLang = registry.language;
      const defaultAnsiCodes = {
        "\xA70": "\x1B[30m",
        "\xA71": "\x1B[34m",
        "\xA72": "\x1B[32m",
        "\xA73": "\x1B[36m",
        "\xA74": "\x1B[31m",
        "\xA75": "\x1B[35m",
        "\xA76": "\x1B[33m",
        "\xA77": "\x1B[37m",
        "\xA78": "\x1B[90m",
        "\xA79": "\x1B[94m",
        "\xA7a": "\x1B[92m",
        "\xA7b": "\x1B[96m",
        "\xA7c": "\x1B[91m",
        "\xA7d": "\x1B[95m",
        "\xA7e": "\x1B[93m",
        "\xA7f": "\x1B[97m",
        "\xA7l": "\x1B[1m",
        "\xA7o": "\x1B[3m",
        "\xA7n": "\x1B[4m",
        "\xA7m": "\x1B[9m",
        "\xA7k": "\x1B[6m",
        "\xA7r": "\x1B[0m"
      };
      const cssDefaultStyles = {
        black: "color:#000000",
        dark_blue: "color:#0000AA",
        dark_green: "color:#00AA00",
        dark_aqua: "color:#00AAAA",
        dark_red: "color:#AA0000",
        dark_purple: "color:#AA00AA",
        gold: "color:#FFAA00",
        gray: "color:#AAAAAA",
        dark_gray: "color:#555555",
        blue: "color:#5555FF",
        green: "color:#55FF55",
        aqua: "color:#55FFFF",
        red: "color:#FF5555",
        light_purple: "color:#FF55FF",
        yellow: "color:#FFFF55",
        white: "color:#FFFFFF",
        bold: "font-weight:900",
        strikethrough: "text-decoration:line-through",
        underlined: "text-decoration:underline",
        italic: "font-style:italic"
      };
      const formatMembers = ["color", "bold", "strikethrough", "underlined", "italic"];
      const { MessageBuilder } = require_MessageBuilder()(registry);
      class ChatMessage {
        constructor(message, displayWarning = false) {
          if (typeof message === "string") {
            if (message === "") {
              this.json = { text: "" };
            } else {
              this.json = MessageBuilder.fromString(message, { colorSeparator: "\xA7" });
            }
          } else if (typeof message === "number") {
            this.json = { text: message };
          } else if (typeof message === "object" && Array.isArray(message)) {
            this.json = { extra: message };
          } else if (typeof message === "object") {
            this.json = message;
          } else {
            throw new Error("Expected String or Object for Message argument");
          }
          this.warn = displayWarning ? console.warn : debug;
          this.parse(displayWarning);
        }
        /**
         * Parses the this.json property to decorate the properties of the ChatMessage.
         * Called by the Constructor
         * @return {void}
         */
        parse() {
          const json = this.json;
          if (typeof json.text === "string" || typeof json.text === "number") {
            this.text = json.text;
          } else if (typeof json[""] === "string" || typeof json[""] === "number") {
            this.text = json[""];
          } else if (typeof json.translate === "string") {
            this.translate = json.translate;
            if (typeof json.fallback === "string") {
              this.fallback = json.fallback;
            }
            if (typeof json.with === "object") {
              if (!Array.isArray(json.with)) {
                throw new Error("Expected with property to be an Array in ChatMessage");
              }
              this.with = json.with.map((entry) => new ChatMessage(entry));
            }
          }
          if (typeof json.extra === "object") {
            if (!Array.isArray(json.extra)) {
              throw new Error("Expected extra property to be an Array in ChatMessage");
            }
            this.extra = json.extra.map((entry) => new ChatMessage(entry));
          }
          this.bold = json.bold;
          this.italic = json.italic;
          this.underlined = json.underlined;
          this.strikethrough = json.strikethrough;
          this.obfuscated = json.obfuscated;
          const supportedColors = [
            "black",
            "dark_blue",
            "dark_green",
            "dark_aqua",
            "dark_red",
            "dark_purple",
            "gold",
            "gray",
            "dark_gray",
            "blue",
            "green",
            "aqua",
            "red",
            "light_purple",
            "yellow",
            "white",
            "obfuscated",
            "bold",
            "strikethrough",
            "underlined",
            "italic",
            "reset"
          ];
          const supportedClick = [
            "open_url",
            "open_file",
            "run_command",
            "suggest_command"
          ];
          const supportedHover = [
            "show_text",
            "show_achievement",
            "show_item",
            "show_entity"
          ];
          this.color = json.color;
          switch (this.color) {
            case "obfuscated":
              this.obfuscated = true;
              this.color = null;
              break;
            case "bold":
              this.bold = true;
              this.color = null;
              break;
            case "strikethrough":
              this.strikethrough = true;
              this.color = null;
              break;
            case "underlined":
              this.underlined = true;
              this.color = null;
              break;
            case "italic":
              this.italic = true;
              this.color = null;
              break;
            case "reset":
              this.reset = true;
              this.color = null;
              break;
          }
          if (this.color && !supportedColors.includes(this.color) && !this.color.match(/#[a-fA-F\d]{6}/)) {
            this.warn("ChatMessage parsed with unsupported color", this.color);
            this.color = null;
          }
          if (typeof json.clickEvent === "object") {
            this.clickEvent = json.clickEvent;
            if (typeof this.clickEvent.action !== "string") {
              throw new Error("ClickEvent action missing in ChatMessage");
            } else if (!supportedClick.includes(this.clickEvent.action)) {
              this.warn("ChatMessage parsed with unsupported clickEvent", this.clickEvent.action);
            }
          }
          if (typeof json.hoverEvent === "object") {
            this.hoverEvent = json.hoverEvent;
            if (typeof this.hoverEvent.action !== "string") {
              throw new Error("HoverEvent action missing in ChatMessage");
            } else if (!supportedHover.includes(this.hoverEvent.action)) {
              this.warn("ChatMessage parsed with unsupported hoverEvent", this.hoverEvent.action);
            }
            if (this.hoverEvent.action === "show_item") {
              let content;
              if (this.hoverEvent.value instanceof Array) {
                if (this.hoverEvent.value[0] instanceof Object) {
                  content = this.hoverEvent.value[0].text;
                } else {
                  content = this.hoverEvent.value[0];
                }
              } else {
                if (this.hoverEvent.value instanceof Object) {
                  content = this.hoverEvent.value.text;
                } else {
                  content = this.hoverEvent.value;
                }
              }
              try {
                this.hoverEvent.value = mojangson.parse(content);
              } catch (err) {
                debug(err);
              }
            }
          }
        }
        /**
         * Append one or more ChatMessages
         * @param {...object|string} messages ChatMessage
         * @return {void}
         */
        append(...messages) {
          if (this.extra === void 0)
            this.extra = [];
          messages.forEach((message) => {
            if (typeof message === "object" && !Array.isArray(message)) {
              this.extra.push(message);
            } else if (typeof message === "string") {
              this.extra.push(new ChatMessage(message));
            }
          });
        }
        /**
        * Returns a clone of the ChatMessage
        * @return {ChatMessage}
        */
        clone() {
          return new ChatMessage(JSON.parse(JSON.stringify(this.json)));
        }
        /**
         * Returns the count of text extras and child ChatMessages
         * Does not count recursively in to the children
         * @return {Number}
         */
        length() {
          let count = 0;
          if (this.text)
            count++;
          else if (this.with)
            count += this.with.length;
          if (this.extra)
            count += this.extra.length;
          return count;
        }
        /**
         * Returns a text part from the message
         * @param  {Number} idx Index of the part
         * @return {String}
         */
        getText(idx, lang = defaultLang) {
          if (typeof idx !== "number")
            return this.toString(lang);
          if (this.text && idx === 0)
            return this.text.replace(/§[0-9a-flnmokr]/g, "");
          else if (this.with.length > idx)
            return this.with[idx].toString(lang);
          if (this.extra && this.extra.length + (this.text ? 1 : this.with.length) > idx) {
            return this.extra[idx - (this.text ? 1 : this.with.length)].toString(lang);
          }
          return "";
        }
        /**
         * Flattens the message in to plain-text
         * @return {String}
         */
        toString(lang = defaultLang, _depth = 0) {
          if (_depth > MAX_CHAT_DEPTH)
            return "";
          let message = "";
          if (typeof this.text === "string" || typeof this.text === "number")
            message += this.text;
          else if (this.translate !== void 0) {
            const _with = this.with ?? [];
            const args = _with.map((entry) => entry.toString(lang, _depth + 1));
            let format = getValueSafely(lang, this.translate, null);
            if (format === null && this.fallback !== void 0) {
              format = this.fallback;
            }
            if (format === null) {
              format = this.translate;
            }
            message += vsprintf(format, args);
          }
          if (this.extra) {
            message += this.extra.map((entry) => entry.toString(lang, _depth + 1)).join("");
          }
          return message.replace(/§[0-9a-flnmokr]/g, "").slice(0, MAX_CHAT_LENGTH);
        }
        valueOf() {
          return this.toString();
        }
        toMotd(lang = defaultLang, parent = {}, _depth = 0) {
          if (_depth > MAX_CHAT_DEPTH)
            return "";
          const codes = {
            color: {
              black: "\xA70",
              dark_blue: "\xA71",
              dark_green: "\xA72",
              dark_aqua: "\xA73",
              dark_red: "\xA74",
              dark_purple: "\xA75",
              gold: "\xA76",
              gray: "\xA77",
              dark_gray: "\xA78",
              blue: "\xA79",
              green: "\xA7a",
              aqua: "\xA7b",
              red: "\xA7c",
              light_purple: "\xA7d",
              yellow: "\xA7e",
              white: "\xA7f",
              reset: "\xA7r"
            },
            bold: "\xA7l",
            italic: "\xA7o",
            underlined: "\xA7n",
            strikethrough: "\xA7m",
            obfuscated: "\xA7k"
          };
          let message = Object.keys(codes).map((code2) => {
            this[code2] = this[code2] || parent[code2];
            if (!this[code2] || this[code2] === "false")
              return null;
            if (code2 === "color") {
              if (this.color.startsWith("#"))
                return `\xA7${this.color}`;
              return codes.color[this.color];
            }
            return codes[code2];
          }).join("");
          if (typeof this.text === "string" || typeof this.text === "number")
            message += this.text;
          else if (this.translate !== void 0) {
            const _with = this.with ?? [];
            const args = _with.map((entry) => {
              const entryAsMotd = entry.toMotd(lang, this, _depth + 1);
              return entryAsMotd + (entryAsMotd.includes("\xA7") ? "\xA7r" + message : "");
            });
            let format = getValueSafely(lang, this.translate, null);
            if (format === null && this.fallback !== void 0) {
              format = this.fallback;
            }
            if (format === null) {
              format = this.translate;
            }
            message += vsprintf(format, args);
          }
          if (this.extra) {
            message += this.extra.map((entry) => entry.toMotd(lang, this, _depth + 1)).join("");
          }
          return message.slice(0, MAX_CHAT_LENGTH);
        }
        toAnsi(lang = defaultLang, codes = defaultAnsiCodes) {
          let message = this.toMotd(lang);
          for (const k in codes) {
            message = message.replace(new RegExp(k, "g"), codes[k]);
          }
          const hexRegex = /§#?([a-fA-F\d]{2})([a-fA-F\d]{2})([a-fA-F\d]{2})/;
          while (message.search(hexRegex) !== -1) {
            const hexCodes = hexRegex.exec(message);
            const red = parseInt(hexCodes[1], 16);
            const green = parseInt(hexCodes[2], 16);
            const blue = parseInt(hexCodes[3], 16);
            message = message.replace(hexRegex, `\x1B[38;2;${red};${green};${blue}m`);
          }
          return codes["\xA7r"] + message.slice(0, MAX_CHAT_LENGTH) + codes["\xA7r"];
        }
        // NOTE : Have to be be mindful here as bad HTML gen may lead to arbitrary code execution from server
        toHTML(lang = registry.language, styles = cssDefaultStyles, allowedFormats = formatMembers, _depth = 0) {
          if (_depth > MAX_CHAT_DEPTH)
            return "";
          let str = "";
          if (allowedFormats.some((member) => this[member])) {
            const cssProps = allowedFormats.reduce((acc, cur) => this[cur] ? acc.push(cur === "color" ? this.color.startsWith("#") ? escapeRGB(this.color.slice(1)) : styles[this.color] : styles[cur]) && acc : acc, []);
            str += `<span style="${cssProps.join(";")}">`;
          } else {
            str += "<span>";
          }
          if (this.text) {
            str += escapeHtml(this.text);
          } else if (this.translate) {
            const params = [];
            if (this.with) {
              for (const param of this.with) {
                params.push(param.toHTML(lang, styles, allowedFormats, _depth + 1));
              }
            }
            let format = getValueSafely(lang, this.translate, null);
            if (format === null && this.fallback !== void 0) {
              format = this.fallback;
            }
            if (format === null) {
              format = this.translate;
            }
            str += vsprintf(escapeHtml(format), params);
          }
          if (this.extra) {
            str += this.extra.map((entry) => entry.toHTML(lang, styles, allowedFormats, _depth + 1)).join("");
          }
          str += "</span>";
          return str.length > MAX_CHAT_LENGTH ? escapeHtml(this.toString()) : str;
        }
        static fromNotch(msg) {
          if (registry.supportFeature("chatPacketsUseNbtComponents") && msg.type) {
            const json = processNbtMessage(msg);
            return new ChatMessage(json ? JSON.parse(json) : "");
          } else {
            try {
              return new ChatMessage(JSON.parse(msg));
            } catch (e) {
              return new ChatMessage(msg);
            }
          }
        }
        // 1.19 applies chat formatting on the client side. A format string is provided like in C printf
        // syntax, including positional arguments which we poll from the supplied parameters map.
        // For example,
        //  printf("<%s> %s" /* fmt string */, [sender], [content])
        static fromNetwork(type, params) {
          const format = getValueSafely(registry.chatFormattingById, type);
          if (format == null) {
            throw new Error("unknown chat format code: " + type);
          }
          return new ChatMessage({ translate: format.formatString, with: format.parameters.map((p) => getValueSafely(params, p, "")) });
        }
      }
      ChatMessage.MessageBuilder = MessageBuilder;
      return ChatMessage;
    }
    module2.exports = loader;
    function uuidFromIntArray(arr) {
      const buf = Buffer.alloc(16);
      arr.forEach((num, index) => {
        buf.writeInt32BE(num, index * 4);
      });
      return buf.toString("hex");
    }
    function processNbtMessage(msg) {
      if (!msg || msg.type === "end")
        return null;
      const simplified = nbt.simplify(msg);
      const json = JSON.stringify(simplified, (key, val) => {
        if (key === "id" && Array.isArray(val))
          return uuidFromIntArray(val);
        return val;
      });
      return json;
    }
    module2.exports.processNbtMessage = processNbtMessage;
    var escapeHtml = (unsafe) => unsafe.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#039;");
    var escapeRGB = (unsafe) => `color:rgb(${unsafe.match(/.{2}/g).map((e) => parseInt(e, 16)).join(",")})`;
  }
});

// ../node_modules/.pnpm/prismarine-block@https+++codeload.github.com+zardoy+prismarine-block+tar.gz+853c559bff2_1105030ccd0c3eaee76528ff3bfaee3e/node_modules/prismarine-block/blockEntity.js
var require_blockEntity = __commonJS({
  "../node_modules/.pnpm/prismarine-block@https+++codeload.github.com+zardoy+prismarine-block+tar.gz+853c559bff2_1105030ccd0c3eaee76528ff3bfaee3e/node_modules/prismarine-block/blockEntity.js"(exports2, module2) {
    var nbt = require_nbt2();
    module2.exports = (registry) => {
      if (registry.version.type === "pc") {
        let signValueToJSONArray2 = function(text) {
          const texts = [];
          if (typeof text === "string") {
            texts.push(...text.split("\n").map((t) => JSON.stringify({ text: t })));
          } else if (Array.isArray(text)) {
            for (const t of text) {
              if (t.toJSON) {
                texts.push(JSON.stringify(t.toJSON()));
              } else if (typeof t === "object") {
                texts.push(JSON.stringify(t));
              } else {
                texts.push(JSON.stringify({ text: t }));
              }
            }
          }
          return texts;
        }, mergeNbt2 = function(obj1, obj2) {
          for (const [key, value] of Object.entries(obj2)) {
            if (!(key in obj1)) {
              obj1[key] = value;
              continue;
            }
            if (typeof value === "object" && !Array.isArray(value))
              mergeNbt2(obj1[key], obj2[key]);
          }
        }, setSignTextForMultiSideSign2 = function(block, side, text) {
          const texts = signValueToJSONArray2(text);
          if (!block.entity) {
            block.entity = nbt.comp({
              id: nbt.string(registry.version[">="]("1.11") ? "minecraft:sign" : "Sign")
            });
          }
          mergeNbt2(block.entity, nbt.comp({
            isWaxed: nbt.byte(0),
            back_text: nbt.comp({
              has_glowing_text: nbt.byte(0),
              color: nbt.string("black"),
              messages: nbt.list(nbt.string(['{"text":""}']))
            }),
            front_text: nbt.comp({
              has_glowing_text: nbt.byte(0),
              color: nbt.string("black"),
              messages: nbt.list(nbt.string(['{"text":""}']))
            })
          }));
          block.entity.value[side].value.messages.value.value = texts;
        }, getSignTextForMultiSideSign2 = function(block, side) {
          if (!block.entity)
            return "";
          return block.entity?.value?.[side]?.value?.messages?.value?.value?.map((text) => typeof JSON.parse(text) === "string" ? JSON.parse(text) : new ChatMessage(JSON.parse(text)).toString()).join("\n") ?? "";
        }, setSignTextForLegacySign2 = function(block, text) {
          const texts = signValueToJSONArray2(text);
          if (!block.entity) {
            block.entity = nbt.comp({
              id: nbt.string(registry.version[">="]("1.11") ? "minecraft:sign" : "Sign")
            });
          }
          Object.assign(block.entity.value, {
            Text1: nbt.string(texts[0] || '""'),
            Text2: nbt.string(texts[1] || '""'),
            Text3: nbt.string(texts[2] || '""'),
            Text4: nbt.string(texts[3] || '""')
          });
        }, getSignTextForLegacySign2 = function(block) {
          if (!block.entity)
            return "";
          const texts = [block.entity.value.Text1.value, block.entity.value.Text2.value, block.entity.value.Text3.value, block.entity.value.Text4.value].map((val) => val || '"');
          return texts.map((text) => typeof JSON.parse(text) === "string" ? JSON.parse(text) : new ChatMessage(JSON.parse(text)).toString()).join("\n").trimEnd();
        };
        var signValueToJSONArray = signValueToJSONArray2, mergeNbt = mergeNbt2, setSignTextForMultiSideSign = setSignTextForMultiSideSign2, getSignTextForMultiSideSign = getSignTextForMultiSideSign2, setSignTextForLegacySign = setSignTextForLegacySign2, getSignTextForLegacySign = getSignTextForLegacySign2;
        const ChatMessage = require_prismarine_chat()(registry.version.minecraftVersion);
        return {
          sign: {
            setSignText(front, back) {
              if (registry.supportFeature("multiSidedSigns")) {
                if (front !== void 0)
                  setSignTextForMultiSideSign2(this, "front_text", front);
                if (back !== void 0)
                  setSignTextForMultiSideSign2(this, "back_text", back);
              } else {
                if (front !== void 0)
                  setSignTextForLegacySign2(this, front);
                if (back !== void 0)
                  throw new Error(`Cannot set the back text of a sign on a version before 1.20 : ${registry.version.minecraftVersion}`);
              }
            },
            getSignText() {
              if (registry.supportFeature("multiSidedSigns")) {
                return [getSignTextForMultiSideSign2(this, "front_text"), getSignTextForMultiSideSign2(this, "back_text")];
              } else {
                return [getSignTextForLegacySign2(this)];
              }
            },
            // Deprecated APIs kept for backwards compatibility
            get signText() {
              return this.getSignText()[0];
            },
            set signText(text) {
              this.setSignText(text);
            }
          }
        };
      }
      if (registry.version.type === "bedrock") {
        return {
          sign: {
            getSignText() {
              if (!this.entity)
                return [""];
              return [this.entity.Text.value];
            },
            setSignText(text) {
              if (!this.entity) {
                this.entity = nbt.comp({
                  id: nbt.string("Sign")
                });
              }
              Object.assign(this.entity.value, {
                Text: nbt.string(Array.isArray(text) ? text.join("\n") : text)
              });
            },
            // Deprecated APIs kept for backwards compatibility
            get signText() {
              return this.getSignText()[0];
            },
            set signText(text) {
              this.setSignText(text);
            }
          }
        };
      }
    };
  }
});

// ../node_modules/.pnpm/prismarine-block@https+++codeload.github.com+zardoy+prismarine-block+tar.gz+853c559bff2_1105030ccd0c3eaee76528ff3bfaee3e/node_modules/prismarine-block/index.js
var require_prismarine_block = __commonJS({
  "../node_modules/.pnpm/prismarine-block@https+++codeload.github.com+zardoy+prismarine-block+tar.gz+853c559bff2_1105030ccd0c3eaee76528ff3bfaee3e/node_modules/prismarine-block/index.js"(exports2, module2) {
    module2.exports = loader;
    module2.exports.testedVersions = ["1.8.8", "1.9.4", "1.10.2", "1.11.2", "1.12.2", "1.13.2", "1.14.4", "1.15.2", "1.16.4", "1.17.1", "1.18.1", "bedrock_1.17.10", "bedrock_1.18.0", "1.20"];
    var nbt = require_nbt2();
    var mcData2 = require("minecraft-data");
    var legacyPcBlocksByName = Object.entries(mcData2.legacy.pc.blocks).reduce((obj, [idmeta, name]) => {
      const n = name.replace("minecraft:", "").split("[")[0];
      const s = name.split("[")[1]?.replace("]", "") ?? "";
      (obj[n] = obj[n] || {})[s] = idmeta;
      return obj;
    }, {});
    var legacyPcBlocksByIdmeta = Object.entries(mcData2.legacy.pc.blocks).reduce((obj, [idmeta, name]) => {
      const s = name.split("[")[1]?.replace("]", "");
      obj[idmeta] = s ? Object.fromEntries(s.split(",").map((s2) => {
        let [k, v] = s2.split("=");
        if (!isNaN(parseInt(v)))
          v = parseInt(v);
        return [k, v];
      })) : {};
      return obj;
    }, {});
    function parseProperties(properties) {
      if (typeof properties === "object") {
        return properties;
      }
      const json = {};
      for (const prop of properties.split(",")) {
        const [key, value] = prop.split("=");
        json[key] = value;
      }
      return json;
    }
    function matchProperties(block, properties) {
      if (!properties) {
        return true;
      }
      properties = parseProperties(properties);
      const blockProps = block.getProperties();
      if (properties.OR) {
        return properties.OR.some((or) => matchProperties(block, or));
      }
      for (const prop in blockProps) {
        if (properties[prop] === void 0)
          continue;
        const parsed = typeof properties[prop] !== "string" ? String(properties[prop]) : properties[prop];
        if (!parsed.split("|").some((value) => value === String(blockProps[prop]))) {
          return false;
        }
      }
      return true;
    }
    function loader(registryOrVersion) {
      const registry = typeof registryOrVersion === "string" ? require_lib()(registryOrVersion) : registryOrVersion;
      const version = registry.version;
      return provider(registry, { Biome: require_prismarine_biome()(version.minecraftVersion), version });
    }
    var emptyShapeReplacer = [[0, 0, 0, 1, 1, 1]];
    function provider(registry, { Biome, version }) {
      const blockMethods = require_blockEntity()(registry);
      const usesBlockStates = version.type === "pc" && registry.supportFeature("blockStateId") || version.type === "bedrock";
      const shapes = registry.blockCollisionShapes;
      if (shapes) {
        for (const id in registry.blocks) {
          const block = registry.blocks[id];
          const shapesId = shapes.blocks[block.name];
          block.shapes = shapesId instanceof Array ? shapes.shapes[shapesId[0]] : shapes.shapes[shapesId];
          if (!block.shapes) {
            console.error("No shape found for block " + block.name);
            block.shapes = [];
          }
          if (block.states || version.type === "bedrock") {
            if (shapesId instanceof Array) {
              block.stateShapes = [];
              for (const i in shapesId) {
                block.stateShapes.push(shapes.shapes[shapesId[i]]);
              }
            }
          } else {
            if ("variations" in block) {
              for (const i in block.variations) {
                const metadata = block.variations[i].metadata;
                if (shapesId instanceof Array) {
                  block.variations[i].shapes = shapes.shapes[shapesId[metadata]];
                } else {
                  block.variations[i].shapes = shapes.shapes[shapesId];
                }
              }
            }
          }
          if (!block.shapes && version.type === "bedrock") {
            block.shapes = shapes.shapes[shapes.blocks.stone[0]];
            block.stateShapes = block.shapes;
          }
        }
      }
      function getEffectLevel(effectName, effects) {
        const effectDescriptor = registry.effectsByName[effectName];
        if (!effectDescriptor) {
          return 0;
        }
        const effectInfo = effects[effectDescriptor.id];
        if (!effectInfo) {
          return 0;
        }
        return effectInfo.amplifier + 1;
      }
      function getEnchantmentLevel(enchantmentName, enchantments) {
        const enchantmentDescriptor = registry.enchantmentsByName[enchantmentName];
        if (!enchantmentDescriptor) {
          return 0;
        }
        for (const enchInfo of enchantments) {
          if (typeof enchInfo.name === "string") {
            if (enchInfo.name.includes(enchantmentName)) {
              return enchInfo.lvl;
            }
          } else if (enchInfo.name === enchantmentDescriptor.name) {
            return enchInfo.lvl;
          }
        }
        return 0;
      }
      function getMiningFatigueMultiplier(effectLevel) {
        switch (effectLevel) {
          case 0:
            return 1;
          case 1:
            return 0.3;
          case 2:
            return 0.09;
          case 3:
            return 27e-4;
          default:
            return 81e-5;
        }
      }
      return class Block {
        get interactionShapes() {
          const getShape = () => {
            const interactionShapes = globalThis.interactionShapes?.[this.name];
            if (!interactionShapes) {
              if (this.shapes?.length === 0)
                return emptyShapeReplacer[0].map((x) => x * 16);
              return [];
            }
            if (!Array.isArray(interactionShapes)) {
              if (interactionShapes.combine) {
                const shapes2 = [];
                Object.entries(interactionShapes.combine).forEach(([key, value]) => {
                  if (matchProperties(this, key)) {
                    shapes2.push(value);
                  }
                });
                return shapes2;
              }
              let shape2;
              for (const [key, value] of Object.entries(interactionShapes)) {
                if (matchProperties(this, key)) {
                  shape2 = value;
                  break;
                }
              }
              return shape2 ?? [];
            }
            return interactionShapes;
          };
          const shape = getShape();
          if (typeof shape[0] === "number")
            return [shape.map((x) => x / 16)];
          return shape.map((x) => x.map((y) => y / 16)) ?? [];
        }
        set interactionShapes(value) {
        }
        constructor(type, biomeId, metadata, stateId) {
          this.type = type;
          this.metadata = metadata ?? 0;
          this.light = 0;
          this.skyLight = 0;
          this.biome = new Biome(biomeId);
          this.position = null;
          this.entity = void 0;
          this.stateId = stateId;
          this.computedStates = {};
          if (stateId === void 0 && type !== void 0) {
            const b = registry.blocks[type];
            this.stateId = b === void 0 ? null : Math.min(b.minStateId + metadata, b.maxStateId);
          }
          const blockEnum = registry.blocksByStateId[this.stateId];
          if (blockEnum) {
            this.metadata = this.stateId - blockEnum.minStateId;
            this.type = blockEnum.id;
            this.name = blockEnum.name;
            this.hardness = blockEnum.hardness;
            this.displayName = blockEnum.displayName;
            this.shapes = blockEnum.shapes;
            this.interactionShapes = blockEnum.interactionShapes;
            if (blockEnum.stateShapes) {
              if (blockEnum.stateShapes[this.metadata] !== void 0) {
                this.shapes = blockEnum.stateShapes[this.metadata];
              } else {
                this.shapes = blockEnum.stateShapes[0];
                this.missingStateShape = true;
              }
            } else if (blockEnum.variations) {
              const variations = blockEnum.variations;
              for (const i in variations) {
                if (variations[i].metadata === metadata) {
                  this.displayName = variations[i].displayName;
                  this.shapes = variations[i].shapes;
                }
              }
            }
            this.boundingBox = blockEnum.boundingBox;
            this.transparent = blockEnum.transparent;
            this.diggable = blockEnum.diggable;
            this.material = blockEnum.material;
            this.harvestTools = blockEnum.harvestTools;
            this.drops = blockEnum.drops;
          } else {
            this.name = "";
            this.displayName = "";
            this.shapes = [];
            this.hardness = 0;
            this.boundingBox = "empty";
            this.transparent = true;
            this.diggable = false;
          }
          this._properties = {};
          if (version.type === "pc") {
            if (usesBlockStates) {
              const blockEnum2 = registry.blocksByStateId[this.stateId];
              if (blockEnum2 && blockEnum2.states) {
                let data = this.metadata;
                for (let i = blockEnum2.states.length - 1; i >= 0; i--) {
                  const prop = blockEnum2.states[i];
                  this._properties[prop.name] = propValue(prop, data % prop.num_values);
                  data = Math.floor(data / prop.num_values);
                }
              }
            } else {
              this._properties = legacyPcBlocksByIdmeta[this.type + ":" + this.metadata] || legacyPcBlocksByIdmeta[this.type + ":0"] || {};
              if (!this._properties) {
                for (let i = 0; i < 15; i++) {
                  this._properties = legacyPcBlocksByIdmeta[this.type + ":" + i];
                  if (this._properties)
                    break;
                }
              }
              this._properties ??= {};
            }
          } else if (version.type === "bedrock") {
            const states = registry.blockStates?.[this.stateId]?.states || {};
            for (const state in states) {
              this._properties[state] = states[state].value;
            }
          } else {
            throw new Error("Unknown registry type: " + version.type);
          }
          this.isWaterlogged = this._properties.waterlogged;
          if (this.name.includes("sign")) {
            mergeObject(this, blockMethods.sign);
          }
          if (blockEnum && registry.supportFeature("blockHashes")) {
            this.hash = Block.getHash(this.name, this._properties);
          }
        }
        static fromStateId(stateId, biomeId) {
          if (usesBlockStates) {
            return new Block(void 0, biomeId, 0, stateId);
          } else {
            return new Block(stateId >> 4, biomeId, stateId & 15, stateId);
          }
        }
        static fromProperties(typeId, properties, biomeId) {
          const block = typeof typeId === "string" ? registry.blocksByName[typeId] : registry.blocks[typeId];
          if (!block)
            throw new Error("No matching block id found for " + typeId + " with properties " + JSON.stringify(properties));
          if (version.type === "pc") {
            if (block.states) {
              let data = 0;
              for (const [key, value] of Object.entries(properties)) {
                data += getStateValue(block.states, key, value);
              }
              return new Block(void 0, biomeId, 0, block.minStateId + data);
            } else {
              const states = legacyPcBlocksByName[block.name];
              for (const state in states) {
                let broke;
                for (const [key, value] of Object.entries(properties)) {
                  const s = key + "=" + value;
                  if (!state.includes(s)) {
                    broke = true;
                    break;
                  }
                }
                if (!broke) {
                  const [id, meta] = states[state].split(":").map(Number);
                  return new Block(id, biomeId, meta);
                }
              }
              throw new Error("No matching block state found for " + block.name + " with properties " + JSON.stringify(properties));
            }
          } else if (version.type === "bedrock") {
            for (let stateId = block.minStateId; stateId <= block.maxStateId; stateId++) {
              const state = registry.blockStates[stateId].states;
              if (Object.entries(properties).find(([prop, val]) => state[prop]?.value !== val))
                continue;
              return new Block(void 0, biomeId, 0, stateId);
            }
            return block;
          }
        }
        static fromString(str, biomeId) {
          if (str.startsWith("minecraft:"))
            str = str.substring(10);
          const name = str.split("[", 1)[0];
          const propertiesStr = str.slice(name.length + 1, -1).split(",");
          if (!str.includes('["')) {
            return Block.fromProperties(name, Object.fromEntries(propertiesStr.map((property) => property.split("="))), biomeId);
          } else {
            return Block.fromProperties(name, Object.fromEntries(propertiesStr.map((property) => {
              const [key, value] = property.split(":");
              return [key.slice(1, -1), value.startsWith('"') ? value.slice(1, -1) : { true: 1, false: 0 }[value] ?? parseInt(value)];
            })), biomeId);
          }
        }
        get blockEntity() {
          return this.entity ? nbt.simplify(this.entity) : void 0;
        }
        getProperties() {
          return Object.assign(this._properties, this.computedStates);
        }
        changeProperties(propertiesToChange) {
          if (!usesBlockStates) {
            const newPropsMerged = Object.assign(
              {},
              this._properties,
              // normalize values
              Object.fromEntries(Object.entries(propertiesToChange).map(([key, value]) => typeof value === "boolean" ? [key, `${value}`] : [key, value]))
            );
            const propsToString = (p) => Object.entries(p).map(([k, v]) => k + "=" + v).join(",");
            const propsStringified = propsToString(newPropsMerged);
            let lastFoundState;
            const matched = Object.entries(legacyPcBlocksByIdmeta).find(([key, state]) => {
              if (+key.split(":")[0] === this.type) {
                lastFoundState = state;
              } else {
                return false;
              }
              return propsToString(state) === propsStringified;
            });
            if (!matched) {
              throw new Error("No matching block state found for " + this.name + " with properties " + JSON.stringify(propertiesToChange) + " last found state: " + JSON.stringify(lastFoundState));
            }
            return +matched[0].split(":")[1];
          } else {
            throw new Error("Not implemented");
          }
        }
        static getHash(name, states) {
          if (registry.supportFeature("blockHashes")) {
            const sortedStates = {};
            for (const key of Object.keys(states).sort()) {
              sortedStates[key] = states[key];
            }
            const tag = nbt.comp({
              name: { type: "string", value: name.includes(":") ? name : `minecraft:${name}` },
              states: nbt.comp(sortedStates)
            });
            const buf = nbt.writeUncompressed(tag, "little");
            return computeFnv1a32Hash(buf);
          }
        }
        canHarvest(heldItemType) {
          if (!this.harvestTools) {
            return true;
          }
          ;
          return heldItemType && this.harvestTools && this.harvestTools[heldItemType];
        }
        // http://minecraft.gamepedia.com/Breaking#Calculation
        // for more concrete information, look up following Minecraft methods (assuming yarn mappings):
        // AbstractBlock#calcBlockBreakingDelta, PlayerEntity#getBlockBreakingSpeed, PlayerEntity#canHarvest
        digTime(heldItemType, creative, inWater, notOnGround, enchantments = [], effects = {}) {
          if (creative)
            return 0;
          const materialToolMultipliers = registry.materials[this.material];
          const isBestTool = heldItemType && materialToolMultipliers && materialToolMultipliers[heldItemType];
          let blockBreakingSpeed = 1;
          if (isBestTool) {
            blockBreakingSpeed = materialToolMultipliers[heldItemType];
          }
          const efficiencyLevel = getEnchantmentLevel("efficiency", enchantments);
          if (efficiencyLevel > 0 && blockBreakingSpeed > 1) {
            blockBreakingSpeed += efficiencyLevel * efficiencyLevel + 1;
          }
          const hasteLevel = Math.max(
            getEffectLevel("Haste", effects),
            getEffectLevel("ConduitPower", effects)
          );
          if (hasteLevel > 0) {
            blockBreakingSpeed *= 1 + 0.2 * hasteLevel;
          }
          const miningFatigueLevel = getEffectLevel("MiningFatigue", effects);
          if (miningFatigueLevel > 0) {
            blockBreakingSpeed *= getMiningFatigueMultiplier(miningFatigueLevel);
          }
          const aquaAffinityLevel = getEnchantmentLevel("aqua_affinity", enchantments);
          if (inWater && aquaAffinityLevel === 0) {
            blockBreakingSpeed /= 5;
          }
          if (notOnGround) {
            blockBreakingSpeed /= 5;
          }
          const blockHardness = this.hardness;
          const matchingToolMultiplier = this.canHarvest(heldItemType) ? 30 : 100;
          let blockBreakingDelta = blockBreakingSpeed / blockHardness / matchingToolMultiplier;
          if (blockHardness === -1) {
            blockBreakingDelta = 0;
          }
          if (blockBreakingDelta === 0) {
            return Infinity;
          }
          if (blockBreakingDelta >= 1) {
            return 0;
          }
          const ticksToBreakBlock = Math.ceil(1 / blockBreakingDelta);
          return ticksToBreakBlock * 50;
        }
      };
      function parseValue(value, state) {
        if (state.type === "enum") {
          return state.values.indexOf(value);
        }
        if (state.type === "bool") {
          if (value === true || value === "true")
            return 0;
          if (value === false || value === "false")
            return 1;
        }
        if (state.type === "int") {
          return value;
        }
        return state.values?.indexOf(value.toString()) ?? 0;
      }
      function getStateValue(states, name, value) {
        let offset = 1;
        for (let i = states.length - 1; i >= 0; i--) {
          const state = states[i];
          if (state.name === name) {
            return offset * parseValue(value, state);
          }
          offset *= state.num_values;
        }
        return 0;
      }
      function propValue(state, value) {
        if (state.type === "enum" || state.values)
          return state.values[value];
        if (state.type === "bool")
          return !value;
        return value;
      }
    }
    function mergeObject(to, from) {
      Object.defineProperties(to, Object.getOwnPropertyDescriptors(from));
    }
    function computeFnv1a32Hash(buf) {
      const FNV1_OFFSET_32 = 2166136261;
      let h = FNV1_OFFSET_32;
      for (let i = 0; i < buf.length; i++) {
        h ^= buf[i] & 255;
        h += (h << 1) + (h << 4) + (h << 7) + (h << 8) + (h << 24);
      }
      return h & 4294967295;
    }
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.8/chunk.js
var require_chunk = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.8/chunk.js"(exports2, module2) {
    var CommonChunkColumn = require_CommonChunkColumn();
    var Section = require_section();
    var Vec32 = require_vec3().Vec3;
    var BIOME_SIZE = 256;
    var h = 256;
    var { w, l, sh } = Section;
    var sectionCount = h >> 4;
    module2.exports = (registry) => {
      const Block = require_prismarine_block()(registry);
      return class Chunk extends CommonChunkColumn {
        static get section() {
          return Section;
        }
        static get w() {
          return w;
        }
        static get l() {
          return l;
        }
        static get h() {
          return h;
        }
        static get version() {
          return registry.version;
        }
        constructor() {
          super(registry);
          this.skyLightSent = true;
          this.minY = 0;
          this.worldHeight = h;
          this.sections = new Array(sectionCount);
          for (let i = 0; i < sectionCount; i++) {
            this.sections[i] = new Section();
          }
          this.biome = Buffer.alloc(BIOME_SIZE);
        }
        toJson() {
          return JSON.stringify({
            skyLightSent: this.skyLightSent,
            biome: this.biome.toJSON(),
            blockEntities: this.blockEntities,
            sections: this.sections.map((section) => section.toJson())
          });
        }
        static fromJson(j) {
          const parsed = JSON.parse(j);
          const chunk = new Chunk();
          chunk.skyLightSent = parsed.skyLightSent;
          chunk.biome = Buffer.from(parsed.biome);
          chunk.blockEntities = parsed.blockEntities;
          chunk.sections = parsed.sections.map((s) => Section.fromJson(s));
          return chunk;
        }
        initialize(iniFunc) {
          let biome = 0;
          for (let i = 0; i < sectionCount; i++) {
            this.sections[i].initialize((x, y, z, n) => {
              const block = iniFunc(x, y % sh, z, n);
              if (block == null) {
                return;
              }
              if (y === 0 && sectionCount === 0) {
                this.biome.writeUInt8(block.biome.id || 0, biome);
                biome++;
              }
              return block;
            });
          }
        }
        getBlock(pos) {
          const block = new Block(this.getBlockType(pos), this.getBiome(pos), this.getBlockData(pos));
          block.light = this.getBlockLight(pos);
          block.skyLight = this.getSkyLight(pos);
          block.entity = this.getBlockEntity(pos);
          return block;
        }
        setBlock(pos, block) {
          if (exists(block.type)) {
            this.setBlockType(pos, block.type);
          }
          if (exists(block.metadata)) {
            this.setBlockData(pos, block.metadata);
          }
          if (exists(block.biome)) {
            this.setBiome(pos, block.biome.id);
          }
          if (exists(block.skyLight) && this.skyLightSent) {
            this.setSkyLight(pos, block.skyLight);
          }
          if (exists(block.light)) {
            this.setBlockLight(pos, block.light);
          }
          if (block.entity) {
            this.setBlockEntity(pos, block.entity);
          } else {
            this.removeBlockEntity(pos);
          }
        }
        _getSection(pos) {
          return this.sections[pos.y >> 4];
        }
        getBlockStateId(pos) {
          const section = this._getSection(pos);
          return section ? section.getBlockStateId(posInSection(pos)) : 0;
        }
        getBlockType(pos) {
          const section = this._getSection(pos);
          return section ? section.getBlockType(posInSection(pos)) : 0;
        }
        getBlockData(pos) {
          const section = this._getSection(pos);
          return section ? section.getBlockData(posInSection(pos)) : 0;
        }
        getBlockLight(pos) {
          const section = this._getSection(pos);
          return section ? section.getBlockLight(posInSection(pos)) : 0;
        }
        getSkyLight(pos) {
          if (!this.skyLightSent)
            return 0;
          const section = this._getSection(pos);
          return section ? section.getSkyLight(posInSection(pos)) : 15;
        }
        getBiome(pos) {
          const cursor = getBiomeCursor(pos);
          return this.biome.readUInt8(cursor);
        }
        setBlockStateId(pos, stateId) {
          const section = this._getSection(pos);
          return section && section.setBlockStateId(posInSection(pos), stateId);
        }
        setBlockType(pos, id) {
          const data = this.getBlockData(pos);
          this.setBlockStateId(pos, id << 4 | data);
        }
        setBlockData(pos, data) {
          const id = this.getBlockType(pos);
          this.setBlockStateId(pos, id << 4 | data);
        }
        setBlockLight(pos, light) {
          const section = this._getSection(pos);
          return section && section.setBlockLight(posInSection(pos), light);
        }
        setSkyLight(pos, light) {
          const section = this._getSection(pos);
          return section && section.setSkyLight(posInSection(pos), light);
        }
        setBiome(pos, biome) {
          const cursor = getBiomeCursor(pos);
          this.biome.writeUInt8(biome, cursor);
        }
        // These methods do nothing, and are present only for API compatibility
        dumpBiomes() {
        }
        dumpLight() {
        }
        loadLight() {
        }
        /**
         * New serializable light data dump method with consistent interface across versions
         * Returns an object that can be easily transferred between threads
         * @returns {Object} Object containing serialized light data
         */
        dumpLightNew() {
          return {
            sections: this.sections.map((section) => section.toJson())
          };
        }
        /**
         * New light data loading method with consistent interface across versions
         * Accepts a single argument that contains all necessary light data
         * @param {Object} lightData - Object containing serialized light data
         */
        loadLightNew(lightData) {
          this.sections = lightData.sections.map((s) => Section.fromJson(s));
        }
        loadBiomes() {
        }
        dump(bitMap = 65535, skyLightSent = true) {
          const SECTION_SIZE = Section.sectionSize(this.skyLightSent && skyLightSent);
          const { chunkIncluded, chunkCount } = parseBitMap(bitMap);
          const bufferLength = chunkCount * SECTION_SIZE + BIOME_SIZE;
          const buffer = Buffer.alloc(bufferLength);
          let offset = 0;
          let offsetLight = w * l * sectionCount * chunkCount * 2;
          let offsetSkyLight = this.skyLightSent && skyLightSent ? w * l * sectionCount * chunkCount / 2 * 5 : void 0;
          for (let i = 0; i < sectionCount; i++) {
            if (chunkIncluded[i]) {
              offset += this.sections[i].dump().copy(buffer, offset, 0, w * l * sh * 2);
              offsetLight += this.sections[i].dump().copy(buffer, offsetLight, w * l * sh * 2, w * l * sh * 2 + w * l * sh / 2);
              if (this.skyLightSent && skyLightSent)
                offsetSkyLight += this.sections[i].dump().copy(buffer, offsetSkyLight, w * l * sh / 2 * 5, w * l * sh / 2 * 5 + w * l * sh / 2);
            }
          }
          this.biome.copy(buffer, w * l * sectionCount * chunkCount * (this.skyLightSent && skyLightSent ? 3 : 5 / 2));
          return buffer;
        }
        load(data, bitMap = 65535, skyLightSent = true, fullChunk = true) {
          if (!Buffer.isBuffer(data)) {
            throw new Error("Data must be a buffer");
          }
          this.skyLightSent = skyLightSent;
          const SECTION_SIZE = Section.sectionSize(skyLightSent);
          const { chunkIncluded, chunkCount } = parseBitMap(bitMap);
          let offset = 0;
          let offsetLight = w * l * sectionCount * chunkCount * 2;
          let offsetSkyLight = this.skyLightSent ? w * l * sectionCount * chunkCount / 2 * 5 : void 0;
          for (let i = 0; i < sectionCount; i++) {
            if (chunkIncluded[i]) {
              const sectionBuffer = Buffer.alloc(SECTION_SIZE);
              offset += data.copy(sectionBuffer, 0, offset, offset + w * l * sh * 2);
              offsetLight += data.copy(sectionBuffer, w * l * sh * 2, offsetLight, offsetLight + w * l * sh / 2);
              if (this.skyLightSent)
                offsetSkyLight += data.copy(sectionBuffer, w * l * sh * 5 / 2, offsetSkyLight, offsetSkyLight + w * l * sh / 2);
              this.sections[i].load(sectionBuffer, skyLightSent);
            }
          }
          if (fullChunk) {
            data.copy(this.biome, 0, w * l * sectionCount * chunkCount * (skyLightSent ? 3 : 5 / 2));
          }
          const expectedSize = SECTION_SIZE * chunkCount + (fullChunk ? w * l : 0);
          if (data.length !== expectedSize) {
            throw new Error(`Data buffer not correct size (was ${data.length}, expected ${expectedSize})`);
          }
        }
        getMask() {
          return 65535;
        }
      };
    };
    var exists = function(val) {
      return val !== void 0;
    };
    var getBiomeCursor = function(pos) {
      return pos.z * w + pos.x;
    };
    function posInSection(pos) {
      return pos.modulus(new Vec32(w, l, sh));
    }
    function parseBitMap(bitMap) {
      const chunkIncluded = new Array(sectionCount);
      let chunkCount = 0;
      for (let y = 0; y < sectionCount; ++y) {
        chunkIncluded[y] = bitMap & 1 << y;
        if (chunkIncluded[y])
          chunkCount++;
      }
      return { chunkIncluded, chunkCount };
    }
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/constants.js
var require_constants = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/constants.js"(exports2, module2) {
    var CHUNK_HEIGHT = 256;
    var CHUNK_WIDTH = 16;
    var SECTION_HEIGHT = 16;
    var SECTION_WIDTH = 16;
    var BLOCK_SECTION_VOLUME = SECTION_HEIGHT * SECTION_WIDTH * SECTION_WIDTH;
    var NUM_SECTIONS = 16;
    var MIN_BITS_PER_BLOCK = 4;
    var MAX_BITS_PER_BLOCK = 8;
    var GLOBAL_BITS_PER_BLOCK = 16;
    var BIOME_SECTION_VOLUME = BLOCK_SECTION_VOLUME / (4 * 4 * 4) | 0;
    var MIN_BITS_PER_BIOME = 1;
    var MAX_BITS_PER_BIOME = 3;
    var GLOBAL_BITS_PER_BIOME = 6;
    module2.exports = {
      CHUNK_HEIGHT,
      CHUNK_WIDTH,
      SECTION_HEIGHT,
      SECTION_WIDTH,
      BLOCK_SECTION_VOLUME,
      NUM_SECTIONS,
      MIN_BITS_PER_BLOCK,
      MAX_BITS_PER_BLOCK,
      GLOBAL_BITS_PER_BLOCK,
      BIOME_SECTION_VOLUME,
      MIN_BITS_PER_BIOME,
      MAX_BITS_PER_BIOME,
      GLOBAL_BITS_PER_BIOME
    };
  }
});

// ../node_modules/.pnpm/smart-buffer@4.2.0/node_modules/smart-buffer/build/utils.js
var require_utils6 = __commonJS({
  "../node_modules/.pnpm/smart-buffer@4.2.0/node_modules/smart-buffer/build/utils.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    var buffer_1 = require("buffer");
    var ERRORS = {
      INVALID_ENCODING: "Invalid encoding provided. Please specify a valid encoding the internal Node.js Buffer supports.",
      INVALID_SMARTBUFFER_SIZE: "Invalid size provided. Size must be a valid integer greater than zero.",
      INVALID_SMARTBUFFER_BUFFER: "Invalid Buffer provided in SmartBufferOptions.",
      INVALID_SMARTBUFFER_OBJECT: "Invalid SmartBufferOptions object supplied to SmartBuffer constructor or factory methods.",
      INVALID_OFFSET: "An invalid offset value was provided.",
      INVALID_OFFSET_NON_NUMBER: "An invalid offset value was provided. A numeric value is required.",
      INVALID_LENGTH: "An invalid length value was provided.",
      INVALID_LENGTH_NON_NUMBER: "An invalid length value was provived. A numeric value is required.",
      INVALID_TARGET_OFFSET: "Target offset is beyond the bounds of the internal SmartBuffer data.",
      INVALID_TARGET_LENGTH: "Specified length value moves cursor beyong the bounds of the internal SmartBuffer data.",
      INVALID_READ_BEYOND_BOUNDS: "Attempted to read beyond the bounds of the managed data.",
      INVALID_WRITE_BEYOND_BOUNDS: "Attempted to write beyond the bounds of the managed data."
    };
    exports2.ERRORS = ERRORS;
    function checkEncoding(encoding) {
      if (!buffer_1.Buffer.isEncoding(encoding)) {
        throw new Error(ERRORS.INVALID_ENCODING);
      }
    }
    exports2.checkEncoding = checkEncoding;
    function isFiniteInteger(value) {
      return typeof value === "number" && isFinite(value) && isInteger(value);
    }
    exports2.isFiniteInteger = isFiniteInteger;
    function checkOffsetOrLengthValue(value, offset) {
      if (typeof value === "number") {
        if (!isFiniteInteger(value) || value < 0) {
          throw new Error(offset ? ERRORS.INVALID_OFFSET : ERRORS.INVALID_LENGTH);
        }
      } else {
        throw new Error(offset ? ERRORS.INVALID_OFFSET_NON_NUMBER : ERRORS.INVALID_LENGTH_NON_NUMBER);
      }
    }
    function checkLengthValue(length) {
      checkOffsetOrLengthValue(length, false);
    }
    exports2.checkLengthValue = checkLengthValue;
    function checkOffsetValue(offset) {
      checkOffsetOrLengthValue(offset, true);
    }
    exports2.checkOffsetValue = checkOffsetValue;
    function checkTargetOffset(offset, buff) {
      if (offset < 0 || offset > buff.length) {
        throw new Error(ERRORS.INVALID_TARGET_OFFSET);
      }
    }
    exports2.checkTargetOffset = checkTargetOffset;
    function isInteger(value) {
      return typeof value === "number" && isFinite(value) && Math.floor(value) === value;
    }
    function bigIntAndBufferInt64Check(bufferMethod) {
      if (typeof BigInt === "undefined") {
        throw new Error("Platform does not support JS BigInt type.");
      }
      if (typeof buffer_1.Buffer.prototype[bufferMethod] === "undefined") {
        throw new Error(`Platform does not support Buffer.prototype.${bufferMethod}.`);
      }
    }
    exports2.bigIntAndBufferInt64Check = bigIntAndBufferInt64Check;
  }
});

// ../node_modules/.pnpm/smart-buffer@4.2.0/node_modules/smart-buffer/build/smartbuffer.js
var require_smartbuffer = __commonJS({
  "../node_modules/.pnpm/smart-buffer@4.2.0/node_modules/smart-buffer/build/smartbuffer.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    var utils_1 = require_utils6();
    var DEFAULT_SMARTBUFFER_SIZE = 4096;
    var DEFAULT_SMARTBUFFER_ENCODING = "utf8";
    var SmartBuffer = class _SmartBuffer {
      /**
       * Creates a new SmartBuffer instance.
       *
       * @param options { SmartBufferOptions } The SmartBufferOptions to apply to this instance.
       */
      constructor(options) {
        this.length = 0;
        this._encoding = DEFAULT_SMARTBUFFER_ENCODING;
        this._writeOffset = 0;
        this._readOffset = 0;
        if (_SmartBuffer.isSmartBufferOptions(options)) {
          if (options.encoding) {
            utils_1.checkEncoding(options.encoding);
            this._encoding = options.encoding;
          }
          if (options.size) {
            if (utils_1.isFiniteInteger(options.size) && options.size > 0) {
              this._buff = Buffer.allocUnsafe(options.size);
            } else {
              throw new Error(utils_1.ERRORS.INVALID_SMARTBUFFER_SIZE);
            }
          } else if (options.buff) {
            if (Buffer.isBuffer(options.buff)) {
              this._buff = options.buff;
              this.length = options.buff.length;
            } else {
              throw new Error(utils_1.ERRORS.INVALID_SMARTBUFFER_BUFFER);
            }
          } else {
            this._buff = Buffer.allocUnsafe(DEFAULT_SMARTBUFFER_SIZE);
          }
        } else {
          if (typeof options !== "undefined") {
            throw new Error(utils_1.ERRORS.INVALID_SMARTBUFFER_OBJECT);
          }
          this._buff = Buffer.allocUnsafe(DEFAULT_SMARTBUFFER_SIZE);
        }
      }
      /**
       * Creates a new SmartBuffer instance with the provided internal Buffer size and optional encoding.
       *
       * @param size { Number } The size of the internal Buffer.
       * @param encoding { String } The BufferEncoding to use for strings.
       *
       * @return { SmartBuffer }
       */
      static fromSize(size, encoding) {
        return new this({
          size,
          encoding
        });
      }
      /**
       * Creates a new SmartBuffer instance with the provided Buffer and optional encoding.
       *
       * @param buffer { Buffer } The Buffer to use as the internal Buffer value.
       * @param encoding { String } The BufferEncoding to use for strings.
       *
       * @return { SmartBuffer }
       */
      static fromBuffer(buff, encoding) {
        return new this({
          buff,
          encoding
        });
      }
      /**
       * Creates a new SmartBuffer instance with the provided SmartBufferOptions options.
       *
       * @param options { SmartBufferOptions } The options to use when creating the SmartBuffer instance.
       */
      static fromOptions(options) {
        return new this(options);
      }
      /**
       * Type checking function that determines if an object is a SmartBufferOptions object.
       */
      static isSmartBufferOptions(options) {
        const castOptions = options;
        return castOptions && (castOptions.encoding !== void 0 || castOptions.size !== void 0 || castOptions.buff !== void 0);
      }
      // Signed integers
      /**
       * Reads an Int8 value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readInt8(offset) {
        return this._readNumberValue(Buffer.prototype.readInt8, 1, offset);
      }
      /**
       * Reads an Int16BE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readInt16BE(offset) {
        return this._readNumberValue(Buffer.prototype.readInt16BE, 2, offset);
      }
      /**
       * Reads an Int16LE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readInt16LE(offset) {
        return this._readNumberValue(Buffer.prototype.readInt16LE, 2, offset);
      }
      /**
       * Reads an Int32BE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readInt32BE(offset) {
        return this._readNumberValue(Buffer.prototype.readInt32BE, 4, offset);
      }
      /**
       * Reads an Int32LE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readInt32LE(offset) {
        return this._readNumberValue(Buffer.prototype.readInt32LE, 4, offset);
      }
      /**
       * Reads a BigInt64BE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { BigInt }
       */
      readBigInt64BE(offset) {
        utils_1.bigIntAndBufferInt64Check("readBigInt64BE");
        return this._readNumberValue(Buffer.prototype.readBigInt64BE, 8, offset);
      }
      /**
       * Reads a BigInt64LE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { BigInt }
       */
      readBigInt64LE(offset) {
        utils_1.bigIntAndBufferInt64Check("readBigInt64LE");
        return this._readNumberValue(Buffer.prototype.readBigInt64LE, 8, offset);
      }
      /**
       * Writes an Int8 value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeInt8(value, offset) {
        this._writeNumberValue(Buffer.prototype.writeInt8, 1, value, offset);
        return this;
      }
      /**
       * Inserts an Int8 value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertInt8(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeInt8, 1, value, offset);
      }
      /**
       * Writes an Int16BE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeInt16BE(value, offset) {
        return this._writeNumberValue(Buffer.prototype.writeInt16BE, 2, value, offset);
      }
      /**
       * Inserts an Int16BE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertInt16BE(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeInt16BE, 2, value, offset);
      }
      /**
       * Writes an Int16LE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeInt16LE(value, offset) {
        return this._writeNumberValue(Buffer.prototype.writeInt16LE, 2, value, offset);
      }
      /**
       * Inserts an Int16LE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertInt16LE(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeInt16LE, 2, value, offset);
      }
      /**
       * Writes an Int32BE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeInt32BE(value, offset) {
        return this._writeNumberValue(Buffer.prototype.writeInt32BE, 4, value, offset);
      }
      /**
       * Inserts an Int32BE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertInt32BE(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeInt32BE, 4, value, offset);
      }
      /**
       * Writes an Int32LE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeInt32LE(value, offset) {
        return this._writeNumberValue(Buffer.prototype.writeInt32LE, 4, value, offset);
      }
      /**
       * Inserts an Int32LE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertInt32LE(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeInt32LE, 4, value, offset);
      }
      /**
       * Writes a BigInt64BE value to the current write position (or at optional offset).
       *
       * @param value { BigInt } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeBigInt64BE(value, offset) {
        utils_1.bigIntAndBufferInt64Check("writeBigInt64BE");
        return this._writeNumberValue(Buffer.prototype.writeBigInt64BE, 8, value, offset);
      }
      /**
       * Inserts a BigInt64BE value at the given offset value.
       *
       * @param value { BigInt } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertBigInt64BE(value, offset) {
        utils_1.bigIntAndBufferInt64Check("writeBigInt64BE");
        return this._insertNumberValue(Buffer.prototype.writeBigInt64BE, 8, value, offset);
      }
      /**
       * Writes a BigInt64LE value to the current write position (or at optional offset).
       *
       * @param value { BigInt } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeBigInt64LE(value, offset) {
        utils_1.bigIntAndBufferInt64Check("writeBigInt64LE");
        return this._writeNumberValue(Buffer.prototype.writeBigInt64LE, 8, value, offset);
      }
      /**
       * Inserts a Int64LE value at the given offset value.
       *
       * @param value { BigInt } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertBigInt64LE(value, offset) {
        utils_1.bigIntAndBufferInt64Check("writeBigInt64LE");
        return this._insertNumberValue(Buffer.prototype.writeBigInt64LE, 8, value, offset);
      }
      // Unsigned Integers
      /**
       * Reads an UInt8 value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readUInt8(offset) {
        return this._readNumberValue(Buffer.prototype.readUInt8, 1, offset);
      }
      /**
       * Reads an UInt16BE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readUInt16BE(offset) {
        return this._readNumberValue(Buffer.prototype.readUInt16BE, 2, offset);
      }
      /**
       * Reads an UInt16LE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readUInt16LE(offset) {
        return this._readNumberValue(Buffer.prototype.readUInt16LE, 2, offset);
      }
      /**
       * Reads an UInt32BE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readUInt32BE(offset) {
        return this._readNumberValue(Buffer.prototype.readUInt32BE, 4, offset);
      }
      /**
       * Reads an UInt32LE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readUInt32LE(offset) {
        return this._readNumberValue(Buffer.prototype.readUInt32LE, 4, offset);
      }
      /**
       * Reads a BigUInt64BE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { BigInt }
       */
      readBigUInt64BE(offset) {
        utils_1.bigIntAndBufferInt64Check("readBigUInt64BE");
        return this._readNumberValue(Buffer.prototype.readBigUInt64BE, 8, offset);
      }
      /**
       * Reads a BigUInt64LE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { BigInt }
       */
      readBigUInt64LE(offset) {
        utils_1.bigIntAndBufferInt64Check("readBigUInt64LE");
        return this._readNumberValue(Buffer.prototype.readBigUInt64LE, 8, offset);
      }
      /**
       * Writes an UInt8 value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeUInt8(value, offset) {
        return this._writeNumberValue(Buffer.prototype.writeUInt8, 1, value, offset);
      }
      /**
       * Inserts an UInt8 value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertUInt8(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeUInt8, 1, value, offset);
      }
      /**
       * Writes an UInt16BE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeUInt16BE(value, offset) {
        return this._writeNumberValue(Buffer.prototype.writeUInt16BE, 2, value, offset);
      }
      /**
       * Inserts an UInt16BE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertUInt16BE(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeUInt16BE, 2, value, offset);
      }
      /**
       * Writes an UInt16LE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeUInt16LE(value, offset) {
        return this._writeNumberValue(Buffer.prototype.writeUInt16LE, 2, value, offset);
      }
      /**
       * Inserts an UInt16LE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertUInt16LE(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeUInt16LE, 2, value, offset);
      }
      /**
       * Writes an UInt32BE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeUInt32BE(value, offset) {
        return this._writeNumberValue(Buffer.prototype.writeUInt32BE, 4, value, offset);
      }
      /**
       * Inserts an UInt32BE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertUInt32BE(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeUInt32BE, 4, value, offset);
      }
      /**
       * Writes an UInt32LE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeUInt32LE(value, offset) {
        return this._writeNumberValue(Buffer.prototype.writeUInt32LE, 4, value, offset);
      }
      /**
       * Inserts an UInt32LE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertUInt32LE(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeUInt32LE, 4, value, offset);
      }
      /**
       * Writes a BigUInt64BE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeBigUInt64BE(value, offset) {
        utils_1.bigIntAndBufferInt64Check("writeBigUInt64BE");
        return this._writeNumberValue(Buffer.prototype.writeBigUInt64BE, 8, value, offset);
      }
      /**
       * Inserts a BigUInt64BE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertBigUInt64BE(value, offset) {
        utils_1.bigIntAndBufferInt64Check("writeBigUInt64BE");
        return this._insertNumberValue(Buffer.prototype.writeBigUInt64BE, 8, value, offset);
      }
      /**
       * Writes a BigUInt64LE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeBigUInt64LE(value, offset) {
        utils_1.bigIntAndBufferInt64Check("writeBigUInt64LE");
        return this._writeNumberValue(Buffer.prototype.writeBigUInt64LE, 8, value, offset);
      }
      /**
       * Inserts a BigUInt64LE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertBigUInt64LE(value, offset) {
        utils_1.bigIntAndBufferInt64Check("writeBigUInt64LE");
        return this._insertNumberValue(Buffer.prototype.writeBigUInt64LE, 8, value, offset);
      }
      // Floating Point
      /**
       * Reads an FloatBE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readFloatBE(offset) {
        return this._readNumberValue(Buffer.prototype.readFloatBE, 4, offset);
      }
      /**
       * Reads an FloatLE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readFloatLE(offset) {
        return this._readNumberValue(Buffer.prototype.readFloatLE, 4, offset);
      }
      /**
       * Writes a FloatBE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeFloatBE(value, offset) {
        return this._writeNumberValue(Buffer.prototype.writeFloatBE, 4, value, offset);
      }
      /**
       * Inserts a FloatBE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertFloatBE(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeFloatBE, 4, value, offset);
      }
      /**
       * Writes a FloatLE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeFloatLE(value, offset) {
        return this._writeNumberValue(Buffer.prototype.writeFloatLE, 4, value, offset);
      }
      /**
       * Inserts a FloatLE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertFloatLE(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeFloatLE, 4, value, offset);
      }
      // Double Floating Point
      /**
       * Reads an DoublEBE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readDoubleBE(offset) {
        return this._readNumberValue(Buffer.prototype.readDoubleBE, 8, offset);
      }
      /**
       * Reads an DoubleLE value from the current read position or an optionally provided offset.
       *
       * @param offset { Number } The offset to read data from (optional)
       * @return { Number }
       */
      readDoubleLE(offset) {
        return this._readNumberValue(Buffer.prototype.readDoubleLE, 8, offset);
      }
      /**
       * Writes a DoubleBE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeDoubleBE(value, offset) {
        return this._writeNumberValue(Buffer.prototype.writeDoubleBE, 8, value, offset);
      }
      /**
       * Inserts a DoubleBE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertDoubleBE(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeDoubleBE, 8, value, offset);
      }
      /**
       * Writes a DoubleLE value to the current write position (or at optional offset).
       *
       * @param value { Number } The value to write.
       * @param offset { Number } The offset to write the value at.
       *
       * @return this
       */
      writeDoubleLE(value, offset) {
        return this._writeNumberValue(Buffer.prototype.writeDoubleLE, 8, value, offset);
      }
      /**
       * Inserts a DoubleLE value at the given offset value.
       *
       * @param value { Number } The value to insert.
       * @param offset { Number } The offset to insert the value at.
       *
       * @return this
       */
      insertDoubleLE(value, offset) {
        return this._insertNumberValue(Buffer.prototype.writeDoubleLE, 8, value, offset);
      }
      // Strings
      /**
       * Reads a String from the current read position.
       *
       * @param arg1 { Number | String } The number of bytes to read as a String, or the BufferEncoding to use for
       *             the string (Defaults to instance level encoding).
       * @param encoding { String } The BufferEncoding to use for the string (Defaults to instance level encoding).
       *
       * @return { String }
       */
      readString(arg1, encoding) {
        let lengthVal;
        if (typeof arg1 === "number") {
          utils_1.checkLengthValue(arg1);
          lengthVal = Math.min(arg1, this.length - this._readOffset);
        } else {
          encoding = arg1;
          lengthVal = this.length - this._readOffset;
        }
        if (typeof encoding !== "undefined") {
          utils_1.checkEncoding(encoding);
        }
        const value = this._buff.slice(this._readOffset, this._readOffset + lengthVal).toString(encoding || this._encoding);
        this._readOffset += lengthVal;
        return value;
      }
      /**
       * Inserts a String
       *
       * @param value { String } The String value to insert.
       * @param offset { Number } The offset to insert the string at.
       * @param encoding { String } The BufferEncoding to use for writing strings (defaults to instance encoding).
       *
       * @return this
       */
      insertString(value, offset, encoding) {
        utils_1.checkOffsetValue(offset);
        return this._handleString(value, true, offset, encoding);
      }
      /**
       * Writes a String
       *
       * @param value { String } The String value to write.
       * @param arg2 { Number | String } The offset to write the string at, or the BufferEncoding to use.
       * @param encoding { String } The BufferEncoding to use for writing strings (defaults to instance encoding).
       *
       * @return this
       */
      writeString(value, arg2, encoding) {
        return this._handleString(value, false, arg2, encoding);
      }
      /**
       * Reads a null-terminated String from the current read position.
       *
       * @param encoding { String } The BufferEncoding to use for the string (Defaults to instance level encoding).
       *
       * @return { String }
       */
      readStringNT(encoding) {
        if (typeof encoding !== "undefined") {
          utils_1.checkEncoding(encoding);
        }
        let nullPos = this.length;
        for (let i = this._readOffset; i < this.length; i++) {
          if (this._buff[i] === 0) {
            nullPos = i;
            break;
          }
        }
        const value = this._buff.slice(this._readOffset, nullPos);
        this._readOffset = nullPos + 1;
        return value.toString(encoding || this._encoding);
      }
      /**
       * Inserts a null-terminated String.
       *
       * @param value { String } The String value to write.
       * @param arg2 { Number | String } The offset to write the string to, or the BufferEncoding to use.
       * @param encoding { String } The BufferEncoding to use for writing strings (defaults to instance encoding).
       *
       * @return this
       */
      insertStringNT(value, offset, encoding) {
        utils_1.checkOffsetValue(offset);
        this.insertString(value, offset, encoding);
        this.insertUInt8(0, offset + value.length);
        return this;
      }
      /**
       * Writes a null-terminated String.
       *
       * @param value { String } The String value to write.
       * @param arg2 { Number | String } The offset to write the string to, or the BufferEncoding to use.
       * @param encoding { String } The BufferEncoding to use for writing strings (defaults to instance encoding).
       *
       * @return this
       */
      writeStringNT(value, arg2, encoding) {
        this.writeString(value, arg2, encoding);
        this.writeUInt8(0, typeof arg2 === "number" ? arg2 + value.length : this.writeOffset);
        return this;
      }
      // Buffers
      /**
       * Reads a Buffer from the internal read position.
       *
       * @param length { Number } The length of data to read as a Buffer.
       *
       * @return { Buffer }
       */
      readBuffer(length) {
        if (typeof length !== "undefined") {
          utils_1.checkLengthValue(length);
        }
        const lengthVal = typeof length === "number" ? length : this.length;
        const endPoint = Math.min(this.length, this._readOffset + lengthVal);
        const value = this._buff.slice(this._readOffset, endPoint);
        this._readOffset = endPoint;
        return value;
      }
      /**
       * Writes a Buffer to the current write position.
       *
       * @param value { Buffer } The Buffer to write.
       * @param offset { Number } The offset to write the Buffer to.
       *
       * @return this
       */
      insertBuffer(value, offset) {
        utils_1.checkOffsetValue(offset);
        return this._handleBuffer(value, true, offset);
      }
      /**
       * Writes a Buffer to the current write position.
       *
       * @param value { Buffer } The Buffer to write.
       * @param offset { Number } The offset to write the Buffer to.
       *
       * @return this
       */
      writeBuffer(value, offset) {
        return this._handleBuffer(value, false, offset);
      }
      /**
       * Reads a null-terminated Buffer from the current read poisiton.
       *
       * @return { Buffer }
       */
      readBufferNT() {
        let nullPos = this.length;
        for (let i = this._readOffset; i < this.length; i++) {
          if (this._buff[i] === 0) {
            nullPos = i;
            break;
          }
        }
        const value = this._buff.slice(this._readOffset, nullPos);
        this._readOffset = nullPos + 1;
        return value;
      }
      /**
       * Inserts a null-terminated Buffer.
       *
       * @param value { Buffer } The Buffer to write.
       * @param offset { Number } The offset to write the Buffer to.
       *
       * @return this
       */
      insertBufferNT(value, offset) {
        utils_1.checkOffsetValue(offset);
        this.insertBuffer(value, offset);
        this.insertUInt8(0, offset + value.length);
        return this;
      }
      /**
       * Writes a null-terminated Buffer.
       *
       * @param value { Buffer } The Buffer to write.
       * @param offset { Number } The offset to write the Buffer to.
       *
       * @return this
       */
      writeBufferNT(value, offset) {
        if (typeof offset !== "undefined") {
          utils_1.checkOffsetValue(offset);
        }
        this.writeBuffer(value, offset);
        this.writeUInt8(0, typeof offset === "number" ? offset + value.length : this._writeOffset);
        return this;
      }
      /**
       * Clears the SmartBuffer instance to its original empty state.
       */
      clear() {
        this._writeOffset = 0;
        this._readOffset = 0;
        this.length = 0;
        return this;
      }
      /**
       * Gets the remaining data left to be read from the SmartBuffer instance.
       *
       * @return { Number }
       */
      remaining() {
        return this.length - this._readOffset;
      }
      /**
       * Gets the current read offset value of the SmartBuffer instance.
       *
       * @return { Number }
       */
      get readOffset() {
        return this._readOffset;
      }
      /**
       * Sets the read offset value of the SmartBuffer instance.
       *
       * @param offset { Number } - The offset value to set.
       */
      set readOffset(offset) {
        utils_1.checkOffsetValue(offset);
        utils_1.checkTargetOffset(offset, this);
        this._readOffset = offset;
      }
      /**
       * Gets the current write offset value of the SmartBuffer instance.
       *
       * @return { Number }
       */
      get writeOffset() {
        return this._writeOffset;
      }
      /**
       * Sets the write offset value of the SmartBuffer instance.
       *
       * @param offset { Number } - The offset value to set.
       */
      set writeOffset(offset) {
        utils_1.checkOffsetValue(offset);
        utils_1.checkTargetOffset(offset, this);
        this._writeOffset = offset;
      }
      /**
       * Gets the currently set string encoding of the SmartBuffer instance.
       *
       * @return { BufferEncoding } The string Buffer encoding currently set.
       */
      get encoding() {
        return this._encoding;
      }
      /**
       * Sets the string encoding of the SmartBuffer instance.
       *
       * @param encoding { BufferEncoding } The string Buffer encoding to set.
       */
      set encoding(encoding) {
        utils_1.checkEncoding(encoding);
        this._encoding = encoding;
      }
      /**
       * Gets the underlying internal Buffer. (This includes unmanaged data in the Buffer)
       *
       * @return { Buffer } The Buffer value.
       */
      get internalBuffer() {
        return this._buff;
      }
      /**
       * Gets the value of the internal managed Buffer (Includes managed data only)
       *
       * @param { Buffer }
       */
      toBuffer() {
        return this._buff.slice(0, this.length);
      }
      /**
       * Gets the String value of the internal managed Buffer
       *
       * @param encoding { String } The BufferEncoding to display the Buffer as (defaults to instance level encoding).
       */
      toString(encoding) {
        const encodingVal = typeof encoding === "string" ? encoding : this._encoding;
        utils_1.checkEncoding(encodingVal);
        return this._buff.toString(encodingVal, 0, this.length);
      }
      /**
       * Destroys the SmartBuffer instance.
       */
      destroy() {
        this.clear();
        return this;
      }
      /**
       * Handles inserting and writing strings.
       *
       * @param value { String } The String value to insert.
       * @param isInsert { Boolean } True if inserting a string, false if writing.
       * @param arg2 { Number | String } The offset to insert the string at, or the BufferEncoding to use.
       * @param encoding { String } The BufferEncoding to use for writing strings (defaults to instance encoding).
       */
      _handleString(value, isInsert, arg3, encoding) {
        let offsetVal = this._writeOffset;
        let encodingVal = this._encoding;
        if (typeof arg3 === "number") {
          offsetVal = arg3;
        } else if (typeof arg3 === "string") {
          utils_1.checkEncoding(arg3);
          encodingVal = arg3;
        }
        if (typeof encoding === "string") {
          utils_1.checkEncoding(encoding);
          encodingVal = encoding;
        }
        const byteLength = Buffer.byteLength(value, encodingVal);
        if (isInsert) {
          this.ensureInsertable(byteLength, offsetVal);
        } else {
          this._ensureWriteable(byteLength, offsetVal);
        }
        this._buff.write(value, offsetVal, byteLength, encodingVal);
        if (isInsert) {
          this._writeOffset += byteLength;
        } else {
          if (typeof arg3 === "number") {
            this._writeOffset = Math.max(this._writeOffset, offsetVal + byteLength);
          } else {
            this._writeOffset += byteLength;
          }
        }
        return this;
      }
      /**
       * Handles writing or insert of a Buffer.
       *
       * @param value { Buffer } The Buffer to write.
       * @param offset { Number } The offset to write the Buffer to.
       */
      _handleBuffer(value, isInsert, offset) {
        const offsetVal = typeof offset === "number" ? offset : this._writeOffset;
        if (isInsert) {
          this.ensureInsertable(value.length, offsetVal);
        } else {
          this._ensureWriteable(value.length, offsetVal);
        }
        value.copy(this._buff, offsetVal);
        if (isInsert) {
          this._writeOffset += value.length;
        } else {
          if (typeof offset === "number") {
            this._writeOffset = Math.max(this._writeOffset, offsetVal + value.length);
          } else {
            this._writeOffset += value.length;
          }
        }
        return this;
      }
      /**
       * Ensures that the internal Buffer is large enough to read data.
       *
       * @param length { Number } The length of the data that needs to be read.
       * @param offset { Number } The offset of the data that needs to be read.
       */
      ensureReadable(length, offset) {
        let offsetVal = this._readOffset;
        if (typeof offset !== "undefined") {
          utils_1.checkOffsetValue(offset);
          offsetVal = offset;
        }
        if (offsetVal < 0 || offsetVal + length > this.length) {
          throw new Error(utils_1.ERRORS.INVALID_READ_BEYOND_BOUNDS);
        }
      }
      /**
       * Ensures that the internal Buffer is large enough to insert data.
       *
       * @param dataLength { Number } The length of the data that needs to be written.
       * @param offset { Number } The offset of the data to be written.
       */
      ensureInsertable(dataLength, offset) {
        utils_1.checkOffsetValue(offset);
        this._ensureCapacity(this.length + dataLength);
        if (offset < this.length) {
          this._buff.copy(this._buff, offset + dataLength, offset, this._buff.length);
        }
        if (offset + dataLength > this.length) {
          this.length = offset + dataLength;
        } else {
          this.length += dataLength;
        }
      }
      /**
       * Ensures that the internal Buffer is large enough to write data.
       *
       * @param dataLength { Number } The length of the data that needs to be written.
       * @param offset { Number } The offset of the data to be written (defaults to writeOffset).
       */
      _ensureWriteable(dataLength, offset) {
        const offsetVal = typeof offset === "number" ? offset : this._writeOffset;
        this._ensureCapacity(offsetVal + dataLength);
        if (offsetVal + dataLength > this.length) {
          this.length = offsetVal + dataLength;
        }
      }
      /**
       * Ensures that the internal Buffer is large enough to write at least the given amount of data.
       *
       * @param minLength { Number } The minimum length of the data needs to be written.
       */
      _ensureCapacity(minLength) {
        const oldLength = this._buff.length;
        if (minLength > oldLength) {
          let data = this._buff;
          let newLength = oldLength * 3 / 2 + 1;
          if (newLength < minLength) {
            newLength = minLength;
          }
          this._buff = Buffer.allocUnsafe(newLength);
          data.copy(this._buff, 0, 0, oldLength);
        }
      }
      /**
       * Reads a numeric number value using the provided function.
       *
       * @typeparam T { number | bigint } The type of the value to be read
       *
       * @param func { Function(offset: number) => number } The function to read data on the internal Buffer with.
       * @param byteSize { Number } The number of bytes read.
       * @param offset { Number } The offset to read from (optional). When this is not provided, the managed readOffset is used instead.
       *
       * @returns { T } the number value
       */
      _readNumberValue(func, byteSize, offset) {
        this.ensureReadable(byteSize, offset);
        const value = func.call(this._buff, typeof offset === "number" ? offset : this._readOffset);
        if (typeof offset === "undefined") {
          this._readOffset += byteSize;
        }
        return value;
      }
      /**
       * Inserts a numeric number value based on the given offset and value.
       *
       * @typeparam T { number | bigint } The type of the value to be written
       *
       * @param func { Function(offset: T, offset?) => number} The function to write data on the internal Buffer with.
       * @param byteSize { Number } The number of bytes written.
       * @param value { T } The number value to write.
       * @param offset { Number } the offset to write the number at (REQUIRED).
       *
       * @returns SmartBuffer this buffer
       */
      _insertNumberValue(func, byteSize, value, offset) {
        utils_1.checkOffsetValue(offset);
        this.ensureInsertable(byteSize, offset);
        func.call(this._buff, value, offset);
        this._writeOffset += byteSize;
        return this;
      }
      /**
       * Writes a numeric number value based on the given offset and value.
       *
       * @typeparam T { number | bigint } The type of the value to be written
       *
       * @param func { Function(offset: T, offset?) => number} The function to write data on the internal Buffer with.
       * @param byteSize { Number } The number of bytes written.
       * @param value { T } The number value to write.
       * @param offset { Number } the offset to write the number at (REQUIRED).
       *
       * @returns SmartBuffer this buffer
       */
      _writeNumberValue(func, byteSize, value, offset) {
        if (typeof offset === "number") {
          if (offset < 0) {
            throw new Error(utils_1.ERRORS.INVALID_WRITE_BEYOND_BOUNDS);
          }
          utils_1.checkOffsetValue(offset);
        }
        const offsetVal = typeof offset === "number" ? offset : this._writeOffset;
        this._ensureWriteable(byteSize, offsetVal);
        func.call(this._buff, value, offsetVal);
        if (typeof offset === "number") {
          this._writeOffset = Math.max(this._writeOffset, offsetVal + byteSize);
        } else {
          this._writeOffset += byteSize;
        }
        return this;
      }
    };
    exports2.SmartBuffer = SmartBuffer;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/neededBits.js
var require_neededBits = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/neededBits.js"(exports2, module2) {
    function neededBits(value) {
      return 32 - Math.clz32(value);
    }
    module2.exports = neededBits;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/BitArray.js
var require_BitArray = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/BitArray.js"(exports2, module2) {
    var neededBits = require_neededBits();
    var BitArray = class _BitArray {
      constructor(options) {
        if (options === null) {
          return;
        }
        if (!options.data) {
          options.data = new Uint32Array(Math.ceil(options.capacity * options.bitsPerValue / 64) * 2);
        }
        const valueMask = (1 << options.bitsPerValue) - 1;
        this.data = options.data.buffer ? new Uint32Array(options.data.buffer) : Uint32Array.from(options.data);
        this.capacity = options.capacity;
        this.bitsPerValue = options.bitsPerValue;
        this.valueMask = valueMask;
      }
      toJson() {
        return JSON.stringify({
          data: Array.from(this.data),
          capacity: this.capacity,
          bitsPerValue: this.bitsPerValue,
          valueMask: this.valueMask
        });
      }
      static fromJson(j) {
        return new _BitArray(JSON.parse(j));
      }
      toArray() {
        const array = [];
        for (let i = 0; i < this.capacity; i++) {
          array.push(this.get(i));
        }
        return array;
      }
      static fromArray(array, bitsPerValue) {
        const data = [];
        let i = 0;
        let curLong = 0;
        let curBit = 0;
        while (i < array.length) {
          curLong |= array[i] << curBit;
          curBit += bitsPerValue;
          if (curBit > 32) {
            data.push(curLong & 4294967295);
            curBit -= 32;
            curLong = array[i] >>> bitsPerValue - curBit;
          }
          i++;
        }
        if (curBit > 0) {
          data.push(curLong);
        }
        const bitarray = new _BitArray(null);
        bitarray.data = data;
        bitarray.capacity = array.length;
        bitarray.bitsPerValue = bitsPerValue;
        bitarray.valueMask = (1 << bitsPerValue) - 1;
        return bitarray;
      }
      get(index) {
        const bitIndex = index * this.bitsPerValue;
        const startLongIndex = bitIndex >>> 5;
        const startLong = this.data[startLongIndex];
        const indexInStartLong = bitIndex & 31;
        let result = startLong >>> indexInStartLong;
        const endBitOffset = indexInStartLong + this.bitsPerValue;
        if (endBitOffset > 32) {
          const endLong = this.data[startLongIndex + 1];
          result |= endLong << 32 - indexInStartLong;
        }
        return result & this.valueMask;
      }
      set(index, value) {
        const bitIndex = index * this.bitsPerValue;
        const startLongIndex = bitIndex >>> 5;
        const indexInStartLong = bitIndex & 31;
        this.data[startLongIndex] = (this.data[startLongIndex] & ~(this.valueMask << indexInStartLong) | (value & this.valueMask) << indexInStartLong) >>> 0;
        const endBitOffset = indexInStartLong + this.bitsPerValue;
        if (endBitOffset > 32) {
          this.data[startLongIndex + 1] = (this.data[startLongIndex + 1] & ~((1 << endBitOffset - 32) - 1) | value >> 32 - indexInStartLong) >>> 0;
        }
      }
      resizeTo(newBitsPerValue) {
        const newArr = new _BitArray({
          bitsPerValue: newBitsPerValue,
          capacity: this.capacity
        });
        for (let i = 0; i < this.capacity; ++i) {
          const value = this.get(i);
          if (neededBits(value) > newBitsPerValue) {
            throw new Error(
              "existing value in BitArray can't fit in new bits per value"
            );
          }
          newArr.set(i, value);
        }
        return newArr;
      }
      length() {
        return this.data.length / 2;
      }
      readBuffer(smartBuffer, size = this.data.length) {
        if (size !== this.data.length) {
          this.data = new Uint32Array(size);
          this.readOffset = size * 4;
          return;
        }
        for (let i = 0; i < this.data.length; i += 2) {
          this.data[i + 1] = smartBuffer.readUInt32BE();
          this.data[i] = smartBuffer.readUInt32BE();
        }
        return this;
      }
      writeBuffer(smartBuffer) {
        for (let i = 0; i < this.data.length; i += 2) {
          smartBuffer.writeUInt32BE(this.data[i + 1]);
          smartBuffer.writeUInt32BE(this.data[i]);
        }
        return this;
      }
      getBitsPerValue() {
        return this.bitsPerValue;
      }
    };
    module2.exports = BitArray;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/varInt.js
var require_varInt = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/varInt.js"(exports2) {
    exports2.write = (buffer, value) => {
      do {
        let temp = value & 127;
        value >>>= 7;
        if (value !== 0) {
          temp |= 128;
        }
        buffer.writeUInt8(temp);
      } while (value !== 0);
    };
    exports2.read = (buffer) => {
      let numRead = 0;
      let result = 0;
      let read;
      do {
        read = buffer.readUInt8();
        const value = read & 127;
        result |= value << 7 * numRead;
        numRead++;
        if (numRead > 5) {
          throw new Error("varint is too big");
        }
      } while ((read & 128) !== 0);
      return result;
    };
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.9/ChunkSection.js
var require_ChunkSection = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.9/ChunkSection.js"(exports2, module2) {
    var BitArray = require_BitArray();
    var neededBits = require_neededBits();
    var constants = require_constants();
    var varInt = require_varInt();
    var GLOBAL_BITS_PER_BLOCK = 13;
    function getBlockIndex(pos) {
      return pos.y << 8 | pos.z << 4 | pos.x;
    }
    var ChunkSection = class _ChunkSection {
      constructor(options = {}) {
        if (options === null) {
          return;
        }
        if (typeof options.solidBlockCount === "undefined") {
          options.solidBlockCount = 0;
          if (options.data) {
            for (let i = 0; i < constants.BLOCK_SECTION_VOLUME; i++) {
              if (options.data.get(i) !== 0) {
                options.solidBlockCount += 1;
              }
            }
          }
        }
        if (!options.data) {
          options.data = new BitArray({
            bitsPerValue: 4,
            capacity: constants.BLOCK_SECTION_VOLUME
          });
        }
        if (options.palette === void 0) {
          options.palette = [0];
        }
        if (!options.blockLight) {
          options.blockLight = new BitArray({
            bitsPerValue: 4,
            capacity: constants.BLOCK_SECTION_VOLUME
          });
        }
        if (options.skyLight === void 0) {
          options.skyLight = new BitArray({
            bitsPerValue: 4,
            capacity: constants.BLOCK_SECTION_VOLUME
          });
        }
        this.data = options.data;
        this.palette = options.palette;
        this.isDirty = false;
        this.blockLight = options.blockLight;
        this.skyLight = options.skyLight;
        this.solidBlockCount = options.solidBlockCount;
        this.maxBitsPerBlock = options.maxBitsPerBlock || GLOBAL_BITS_PER_BLOCK;
      }
      toJson() {
        return JSON.stringify({
          data: this.data.toJson(),
          palette: this.palette,
          isDirty: this.isDirty,
          blockLight: this.blockLight.toJson(),
          skyLight: this.skyLight ? this.skyLight.toJson() : this.skyLight,
          solidBlockCount: this.solidBlockCount
        });
      }
      static fromJson(j) {
        const parsed = JSON.parse(j);
        return new _ChunkSection({
          data: BitArray.fromJson(parsed.data),
          palette: parsed.palette,
          blockLight: BitArray.fromJson(parsed.blockLight),
          skyLight: parsed.skyLight ? BitArray.fromJson(parsed.skyLight) : parsed.skyLight,
          solidBlockCount: parsed.solidBlockCount
        });
      }
      getBlock(pos) {
        let stateId = this.data.get(getBlockIndex(pos));
        if (this.palette !== null) {
          stateId = this.palette[stateId];
        }
        return stateId;
      }
      setBlock(pos, stateId) {
        const blockIndex = getBlockIndex(pos);
        let palettedIndex;
        if (this.palette !== null) {
          const indexInPalette = this.palette.indexOf(stateId);
          if (indexInPalette >= 0) {
            palettedIndex = indexInPalette;
          } else {
            this.palette.push(stateId);
            palettedIndex = this.palette.length - 1;
            const bitsPerValue = neededBits(palettedIndex);
            if (bitsPerValue > this.data.getBitsPerValue()) {
              if (bitsPerValue <= constants.MAX_BITS_PER_BLOCK) {
                this.data = this.data.resizeTo(bitsPerValue);
              } else {
                const newData = new BitArray({
                  bitsPerValue: this.maxBitsPerBlock,
                  capacity: constants.BLOCK_SECTION_VOLUME
                });
                for (let i = 0; i < constants.BLOCK_SECTION_VOLUME; i++) {
                  const stateId2 = this.palette[this.data.get(i)];
                  newData.set(i, stateId2);
                }
                this.palette = null;
                palettedIndex = stateId;
                this.data = newData;
              }
            }
          }
        } else {
          palettedIndex = stateId;
        }
        const oldBlock = this.getBlock(pos);
        if (stateId === 0 && oldBlock !== 0) {
          this.solidBlockCount -= 1;
        } else if (stateId !== 0 && oldBlock === 0) {
          this.solidBlockCount += 1;
        }
        this.data.set(blockIndex, palettedIndex);
      }
      getBlockLight(pos) {
        return this.blockLight.get(getBlockIndex(pos));
      }
      getSkyLight(pos) {
        return this.skyLight ? this.skyLight.get(getBlockIndex(pos)) : 0;
      }
      setBlockLight(pos, light) {
        return this.blockLight.set(getBlockIndex(pos), light);
      }
      setSkyLight(pos, light) {
        return this.skyLight ? this.skyLight.set(getBlockIndex(pos), light) : 0;
      }
      isEmpty() {
        return this.solidBlockCount === 0;
      }
      // writes the complete section into a smart buffer object
      write(smartBuffer) {
        smartBuffer.writeUInt8(this.data.getBitsPerValue());
        if (this.palette !== null) {
          varInt.write(smartBuffer, this.palette.length);
          this.palette.forEach((paletteElement) => {
            varInt.write(smartBuffer, paletteElement);
          });
        } else {
          varInt.write(smartBuffer, 0);
        }
        varInt.write(smartBuffer, this.data.length());
        this.data.writeBuffer(smartBuffer);
        this.blockLight.writeBuffer(smartBuffer);
        if (this.skyLight !== null) {
          this.skyLight.writeBuffer(smartBuffer);
        }
      }
    };
    module2.exports = ChunkSection;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.9/ChunkColumn.js
var require_ChunkColumn = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.9/ChunkColumn.js"(exports2, module2) {
    var SmartBuffer = require_smartbuffer().SmartBuffer;
    var ChunkSection = require_ChunkSection();
    var constants = require_constants();
    var BitArray = require_BitArray();
    var varInt = require_varInt();
    var CommonChunkColumn = require_CommonChunkColumn();
    var neededBits = require_neededBits();
    var exists = (val) => val !== void 0;
    module2.exports = (Block, mcData2) => {
      return class ChunkColumn extends CommonChunkColumn {
        static get section() {
          return ChunkSection;
        }
        constructor() {
          super(mcData2);
          this.sectionMask = 0;
          this.skyLightSent = true;
          this.sections = Array(constants.NUM_SECTIONS).fill(null);
          this.biomes = Array(
            constants.SECTION_WIDTH * constants.SECTION_WIDTH
          ).fill(1);
          this.maxBitsPerBlock = neededBits(Object.values(mcData2.blocks).reduce((high, block) => Math.max(high, block.maxStateId), 0));
        }
        toJson() {
          return JSON.stringify({
            biomes: this.biomes,
            blockEntities: this.blockEntities,
            sectionMask: this.sectionMask,
            sections: this.sections.map((section) => section === null ? null : section.toJson())
          });
        }
        static fromJson(j) {
          const parsed = JSON.parse(j);
          const chunk = new ChunkColumn();
          chunk.biomes = parsed.biomes;
          chunk.blockEntities = parsed.blockEntities;
          chunk.sectionMask = parsed.sectionMask;
          chunk.sections = parsed.sections.map((s) => s === null ? null : ChunkSection.fromJson(s));
          return chunk;
        }
        initialize(func) {
          const p = { x: 0, y: 0, z: 0 };
          for (p.y = 0; p.y < constants.CHUNK_HEIGHT; p.y++) {
            for (p.z = 0; p.z < constants.SECTION_WIDTH; p.z++) {
              for (p.x = 0; p.x < constants.SECTION_WIDTH; p.x++) {
                const block = func(p.x, p.y, p.z);
                if (block === null) {
                  continue;
                }
                this.setBlock(p, block);
              }
            }
          }
        }
        getBlock(pos) {
          const block = new Block(this.getBlockType(pos), this.getBiome(pos), this.getBlockData(pos));
          block.light = this.getBlockLight(pos);
          block.skyLight = this.getSkyLight(pos);
          block.entity = this.getBlockEntity(pos);
          return block;
        }
        setBlock(pos, block) {
          if (exists(block.type)) {
            this.setBlockType(pos, block.type);
          }
          if (exists(block.metadata)) {
            this.setBlockData(pos, block.metadata);
          }
          if (exists(block.biome)) {
            this.setBiome(pos, block.biome.id);
          }
          if (exists(block.skyLight) && this.skyLightSent) {
            this.setSkyLight(pos, block.skyLight);
          }
          if (exists(block.light)) {
            this.setBlockLight(pos, block.light);
          }
          if (block.entity) {
            this.setBlockEntity(pos, block.entity);
          } else {
            this.removeBlockEntity(pos);
          }
        }
        getBlockType(pos) {
          return this.getBlockStateId(pos) >> 4;
        }
        getBlockData(pos) {
          return this.getBlockStateId(pos) & 15;
        }
        getBlockStateId(pos) {
          const section = this.sections[pos.y >> 4];
          return section ? section.getBlock(toSectionPos(pos)) : 0;
        }
        getBlockLight(pos) {
          const section = this.sections[pos.y >> 4];
          return section ? section.getBlockLight(toSectionPos(pos)) : 15;
        }
        getSkyLight(pos) {
          const section = this.sections[pos.y >> 4];
          return section ? section.getSkyLight(toSectionPos(pos)) : 15;
        }
        getBiome(pos) {
          return this.biomes[getBiomeIndex(pos)];
        }
        setBlockType(pos, id) {
          const data = this.getBlockData(pos);
          this.setBlockStateId(pos, id << 4 | data);
        }
        setBlockData(pos, data) {
          const id = this.getBlockType(pos);
          this.setBlockStateId(pos, id << 4 | data);
        }
        setBlockStateId(pos, stateId) {
          const sectionIndex = pos.y >> 4;
          if (sectionIndex < 0 || sectionIndex >= 16)
            return;
          let section = this.sections[sectionIndex];
          if (!section) {
            if (stateId === 0) {
              return;
            }
            section = new ChunkSection({
              maxBitsPerBlock: this.maxBitsPerBlock
            });
            this.sectionMask |= 1 << sectionIndex;
            this.sections[sectionIndex] = section;
          }
          section.setBlock(toSectionPos(pos), stateId);
        }
        setBlockLight(pos, light) {
          const section = this.sections[pos.y >> 4];
          return section && section.setBlockLight(toSectionPos(pos), light);
        }
        setSkyLight(pos, light) {
          const section = this.sections[pos.y >> 4];
          return section && section.setSkyLight(toSectionPos(pos), light);
        }
        setBiome(pos, biome) {
          this.biomes[getBiomeIndex(pos)] = biome;
        }
        getMask() {
          return this.sectionMask;
        }
        // These methods do nothing, and are present only for API compatibility
        dumpBiomes() {
        }
        dumpLight() {
        }
        loadLight() {
        }
        /**
         * New serializable light data dump method with consistent interface across versions
         * Returns an object that can be easily transferred between threads
         * @returns {Object} Object containing serialized light data
         */
        dumpLightNew() {
          return {
            skyLightSections: this.sections.map((section) => section === null ? null : section.skyLight.toJson()),
            blockLightSections: this.sections.map((section) => section === null ? null : section.blockLight.toJson()),
            skyLightMask: 0,
            // Not used in 1.9, but included for consistency
            blockLightMask: 0,
            // Not used in 1.9, but included for consistency
            emptySkyLightMask: 0,
            // Not used in 1.9, but included for consistency
            emptyBlockLightMask: 0
            // Not used in 1.9, but included for consistency
          };
        }
        /**
         * New light data loading method with consistent interface across versions
         * Accepts a single argument that contains all necessary light data
         * @param {Object} lightData - Object containing serialized light data
         */
        loadLightNew(lightData) {
          const { skyLightSections, blockLightSections } = lightData;
          skyLightSections.forEach((section, i) => {
            if (this.sections[i] === null)
              return;
            this.sections[i].skyLight = BitArray.fromJson(section);
          });
          blockLightSections.forEach((section, i) => {
            if (this.sections[i] === null)
              return;
            this.sections[i].blockLight = BitArray.fromJson(section);
          });
        }
        loadBiomes() {
        }
        dump() {
          const smartBuffer = new SmartBuffer();
          this.sections.forEach((section, i) => {
            if (section !== null && !section.isEmpty()) {
              section.write(smartBuffer);
            }
          });
          this.biomes.forEach((biome) => {
            smartBuffer.writeUInt8(biome);
          });
          if (!smartBuffer.length) {
            return Buffer.alloc(4096);
          }
          return smartBuffer.toBuffer();
        }
        load(data, bitMap = 65535, skyLightSent = true, fullChunk = true) {
          const reader = SmartBuffer.fromBuffer(data);
          this.skyLightSent = skyLightSent;
          this.sectionMask |= bitMap;
          for (let y = 0; y < constants.NUM_SECTIONS; ++y) {
            if (!(bitMap >> y & 1)) {
              continue;
            }
            let palette;
            let skyLight;
            const bitsPerBlock = reader.readUInt8();
            if (bitsPerBlock <= constants.MAX_BITS_PER_BLOCK) {
              palette = [];
              const numPaletteItems = varInt.read(reader);
              for (let i = 0; i < numPaletteItems; ++i) {
                palette.push(varInt.read(reader));
              }
            } else {
              varInt.read(reader);
              palette = null;
            }
            const dataArray = new BitArray({
              bitsPerValue: bitsPerBlock > constants.MAX_BITS_PER_BLOCK ? this.maxBitsPerBlock : bitsPerBlock,
              capacity: 4096
            }).readBuffer(reader, varInt.read(reader) * 2);
            const blockLight = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            }).readBuffer(reader);
            if (skyLightSent) {
              skyLight = new BitArray({
                bitsPerValue: 4,
                capacity: 4096
              }).readBuffer(reader);
            }
            const section = new ChunkSection({
              data: dataArray,
              palette,
              blockLight,
              ...skyLightSent ? { skyLight } : { skyLight: null },
              maxBitsPerBlock: this.maxBitsPerBlock
            });
            this.sections[y] = section;
          }
          if (fullChunk) {
            const p = { x: 0, y: 0, z: 0 };
            for (p.z = 0; p.z < constants.SECTION_WIDTH; p.z++) {
              for (p.x = 0; p.x < constants.SECTION_WIDTH; p.x++) {
                this.setBiome(p, reader.readUInt8());
              }
            }
          }
        }
      };
    };
    function getBiomeIndex(pos) {
      return pos.z * 16 | pos.x;
    }
    function toSectionPos(pos) {
      return { x: pos.x, y: pos.y & 15, z: pos.z };
    }
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.9/chunk.js
var require_chunk2 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.9/chunk.js"(exports2, module2) {
    var constants = require_constants();
    function loader(registry) {
      const Block = require_prismarine_block()(registry);
      const Chunk = require_ChunkColumn()(Block, registry);
      Chunk.h = constants.CHUNK_HEIGHT;
      Chunk.version = registry.version;
      return Chunk;
    }
    module2.exports = loader;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.13/ChunkSection.js
var require_ChunkSection2 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.13/ChunkSection.js"(exports2, module2) {
    var BitArray = require_BitArray();
    var neededBits = require_neededBits();
    var constants = require_constants();
    var varInt = require_varInt();
    var GLOBAL_BITS_PER_BLOCK = 13;
    function getBlockIndex(pos) {
      return pos.y * 16 * 16 + pos.z * 16 + pos.x;
    }
    var ChunkSection = class _ChunkSection {
      constructor(options = {}) {
        if (options === null) {
          return;
        }
        if (typeof options.solidBlockCount === "undefined") {
          options.solidBlockCount = 0;
          if (options.data) {
            for (let i = 0; i < constants.BLOCK_SECTION_VOLUME; i++) {
              if (options.data.get(i) !== 0) {
                options.solidBlockCount += 1;
              }
            }
          }
        }
        if (!options.data) {
          options.data = new BitArray({
            bitsPerValue: 4,
            capacity: constants.BLOCK_SECTION_VOLUME
          });
        }
        if (options.palette === void 0) {
          options.palette = [0];
        }
        if (!options.blockLight) {
          options.blockLight = new BitArray({
            bitsPerValue: 4,
            capacity: constants.BLOCK_SECTION_VOLUME
          });
        }
        if (options.skyLight === void 0) {
          options.skyLight = new BitArray({
            bitsPerValue: 4,
            capacity: constants.BLOCK_SECTION_VOLUME
          });
        }
        this.data = options.data;
        this.palette = options.palette;
        this.isDirty = false;
        this.blockLight = options.blockLight;
        this.skyLight = options.skyLight;
        this.solidBlockCount = options.solidBlockCount;
        this.maxBitsPerBlock = options.maxBitsPerBlock || GLOBAL_BITS_PER_BLOCK;
      }
      toJson() {
        return JSON.stringify({
          data: this.data.toJson(),
          palette: this.palette,
          isDirty: this.isDirty,
          blockLight: this.blockLight.toJson(),
          skyLight: this.skyLight ? this.skyLight.toJson() : this.skyLight,
          solidBlockCount: this.solidBlockCount
        });
      }
      static fromJson(j) {
        const parsed = JSON.parse(j);
        return new _ChunkSection({
          data: BitArray.fromJson(parsed.data),
          palette: parsed.palette,
          blockLight: BitArray.fromJson(parsed.blockLight),
          skyLight: parsed.skyLight ? BitArray.fromJson(parsed.skyLight) : parsed.skyLight,
          solidBlockCount: parsed.solidBlockCount
        });
      }
      getBlock(pos) {
        let stateId = this.data.get(getBlockIndex(pos));
        if (this.palette !== null) {
          stateId = this.palette[stateId];
        }
        return stateId;
      }
      setBlock(pos, stateId) {
        const blockIndex = getBlockIndex(pos);
        let palettedIndex;
        if (this.palette !== null) {
          const indexInPalette = this.palette.indexOf(stateId);
          if (indexInPalette >= 0) {
            palettedIndex = indexInPalette;
          } else {
            this.palette.push(stateId);
            palettedIndex = this.palette.length - 1;
            const bitsPerValue = neededBits(palettedIndex);
            if (bitsPerValue > this.data.getBitsPerValue()) {
              if (bitsPerValue <= constants.MAX_BITS_PER_BLOCK) {
                this.data = this.data.resizeTo(bitsPerValue);
              } else {
                const newData = new BitArray({
                  bitsPerValue: this.maxBitsPerBlock,
                  capacity: constants.BLOCK_SECTION_VOLUME
                });
                for (let i = 0; i < constants.BLOCK_SECTION_VOLUME; i++) {
                  const stateId2 = this.palette[this.data.get(i)];
                  newData.set(i, stateId2);
                }
                this.palette = null;
                palettedIndex = stateId;
                this.data = newData;
              }
            }
          }
        } else {
          palettedIndex = stateId;
        }
        const oldBlock = this.getBlock(pos);
        if (stateId === 0 && oldBlock !== 0) {
          this.solidBlockCount -= 1;
        } else if (stateId !== 0 && oldBlock === 0) {
          this.solidBlockCount += 1;
        }
        this.data.set(blockIndex, palettedIndex);
      }
      getBlockLight(pos) {
        return this.blockLight.get(getBlockIndex(pos));
      }
      getSkyLight(pos) {
        return this.skyLight ? this.skyLight.get(getBlockIndex(pos)) : 0;
      }
      setBlockLight(pos, light) {
        return this.blockLight.set(getBlockIndex(pos), light);
      }
      setSkyLight(pos, light) {
        return this.skyLight ? this.skyLight.set(getBlockIndex(pos), light) : 0;
      }
      isEmpty() {
        return this.solidBlockCount === 0;
      }
      // writes the complete section into a smart buffer object
      write(smartBuffer) {
        smartBuffer.writeUInt8(this.data.getBitsPerValue());
        if (this.palette !== null) {
          varInt.write(smartBuffer, this.palette.length);
          this.palette.forEach((paletteElement) => {
            varInt.write(smartBuffer, paletteElement);
          });
        }
        varInt.write(smartBuffer, this.data.length());
        this.data.writeBuffer(smartBuffer);
        this.blockLight.writeBuffer(smartBuffer);
        if (this.skyLight !== null) {
          this.skyLight.writeBuffer(smartBuffer);
        }
      }
    };
    module2.exports = ChunkSection;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.13/ChunkColumn.js
var require_ChunkColumn2 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.13/ChunkColumn.js"(exports2, module2) {
    var SmartBuffer = require_smartbuffer().SmartBuffer;
    var ChunkSection = require_ChunkSection2();
    var constants = require_constants();
    var BitArray = require_BitArray();
    var varInt = require_varInt();
    var CommonChunkColumn = require_CommonChunkColumn();
    var neededBits = require_neededBits();
    module2.exports = (Block, mcData2) => {
      return class ChunkColumn extends CommonChunkColumn {
        static get section() {
          return ChunkSection;
        }
        constructor() {
          super(mcData2);
          this.sectionMask = 0;
          this.skyLightSent = true;
          this.sections = Array(constants.NUM_SECTIONS).fill(null);
          this.biomes = Array(
            constants.SECTION_WIDTH * constants.SECTION_WIDTH
          ).fill(1);
          this.maxBitsPerBlock = neededBits(Object.values(mcData2.blocks).reduce((high, block) => Math.max(high, block.maxStateId), 0));
        }
        toJson() {
          return JSON.stringify({
            biomes: this.biomes,
            blockEntities: this.blockEntities,
            sectionMask: this.sectionMask,
            sections: this.sections.map((section) => section === null ? null : section.toJson())
          });
        }
        static fromJson(j) {
          const parsed = JSON.parse(j);
          const chunk = new ChunkColumn();
          chunk.biomes = parsed.biomes;
          chunk.blockEntities = parsed.blockEntities;
          chunk.sectionMask = parsed.sectionMask;
          chunk.sections = parsed.sections.map((s) => s === null ? null : ChunkSection.fromJson(s));
          return chunk;
        }
        initialize(func) {
          const p = { x: 0, y: 0, z: 0 };
          for (p.y = 0; p.y < constants.CHUNK_HEIGHT; p.y++) {
            for (p.z = 0; p.z < constants.SECTION_WIDTH; p.z++) {
              for (p.x = 0; p.x < constants.SECTION_WIDTH; p.x++) {
                const block = func(p.x, p.y, p.z);
                if (block === null) {
                  continue;
                }
                this.setBlock(p, block);
              }
            }
          }
        }
        getBlock(pos) {
          const section = this.sections[pos.y >> 4];
          const biome = this.getBiome(pos);
          if (!section) {
            return Block.fromStateId(0, biome);
          }
          const stateId = section.getBlock(toSectionPos(pos));
          const block = Block.fromStateId(stateId, biome);
          block.light = this.getBlockLight(pos);
          block.skyLight = this.getSkyLight(pos);
          block.entity = this.getBlockEntity(pos);
          return block;
        }
        setBlock(pos, block) {
          if (typeof block.stateId !== "undefined") {
            this.setBlockStateId(pos, block.stateId);
          }
          if (typeof block.biome !== "undefined") {
            this.setBiome(pos, block.biome.id);
          }
          if (typeof block.skyLight !== "undefined" && this.skyLightSent) {
            this.setSkyLight(pos, block.skyLight);
          }
          if (typeof block.light !== "undefined") {
            this.setBlockLight(pos, block.light);
          }
          if (block.entity) {
            this.setBlockEntity(pos, block.entity);
          } else {
            this.removeBlockEntity(pos);
          }
        }
        getBlockType(pos) {
          const blockStateId = this.getBlockStateId(pos);
          return mcData2.blocksByStateId[blockStateId].id;
        }
        getBlockData(pos) {
          const blockStateId = this.getBlockStateId(pos);
          return mcData2.blocksByStateId[blockStateId].metadata;
        }
        getBlockStateId(pos) {
          const section = this.sections[pos.y >> 4];
          return section ? section.getBlock(toSectionPos(pos)) : 0;
        }
        getBlockLight(pos) {
          const section = this.sections[pos.y >> 4];
          if (!section)
            return 15;
          const sectionPos = toSectionPos(pos);
          return section.getBlockLight(sectionPos);
        }
        getSkyLight(pos) {
          const section = this.sections[pos.y >> 4];
          if (!section)
            return 15;
          const sectionPos = toSectionPos(pos);
          return section.getSkyLight(sectionPos);
        }
        getBiome(pos) {
          return this.biomes[getBiomeIndex(pos)];
        }
        getBiomeColor(pos) {
          return { r: 0, g: 0, b: 0 };
        }
        setBlockType(pos, id) {
          this.setBlockStateId(pos, mcData2.blocks[id].minStateId);
        }
        setBlockData(pos, data) {
          this.setBlockStateId(pos, mcData2.blocksByStateId[this.getBlockStateId(pos)].minStateId + data);
        }
        setBlockStateId(pos, stateId) {
          const sectionIndex = pos.y >> 4;
          if (sectionIndex < 0 || sectionIndex >= 16)
            return;
          let section = this.sections[sectionIndex];
          if (!section) {
            if (stateId === 0) {
              return;
            }
            section = new ChunkSection({
              maxBitsPerBlock: this.maxBitsPerBlock
            });
            this.sectionMask |= 1 << sectionIndex;
            this.sections[sectionIndex] = section;
          }
          section.setBlock(toSectionPos(pos), stateId);
        }
        setBlockLight(pos, light) {
          const section = this.sections[pos.y >> 4];
          return section && section.setBlockLight(toSectionPos(pos), light);
        }
        setSkyLight(pos, light) {
          const section = this.sections[pos.y >> 4];
          return section && section.setSkyLight(toSectionPos(pos), light);
        }
        setBiome(pos, biome) {
          this.biomes[getBiomeIndex(pos)] = biome;
        }
        getMask() {
          return this.sectionMask;
        }
        // These methods do nothing, and are present only for API compatibility
        dumpBiomes() {
        }
        dumpLight() {
        }
        loadLight() {
        }
        /**
         * New serializable light data dump method with consistent interface across versions
         * Returns an object that can be easily transferred between threads
         * @returns {Object} Object containing serialized light data
         */
        dumpLightNew() {
          return {
            skyLightSections: this.sections.map((section) => section === null ? null : section.skyLight.toJson()),
            blockLightSections: this.sections.map((section) => section === null ? null : section.blockLight.toJson()),
            skyLightMask: 0,
            // Not used in 1.13, but included for consistency
            blockLightMask: 0,
            // Not used in 1.13, but included for consistency
            emptySkyLightMask: 0,
            // Not used in 1.13, but included for consistency
            emptyBlockLightMask: 0
            // Not used in 1.13, but included for consistency
          };
        }
        /**
         * New light data loading method with consistent interface across versions
         * Accepts a single argument that contains all necessary light data
         * @param {Object} lightData - Object containing serialized light data
         */
        loadLightNew(lightData) {
          const { skyLightSections, blockLightSections } = lightData;
          skyLightSections.forEach((section, i) => {
            if (this.sections[i] === null)
              return;
            this.sections[i].skyLight = BitArray.fromJson(section);
          });
          blockLightSections.forEach((section, i) => {
            if (this.sections[i] === null)
              return;
            this.sections[i].blockLight = BitArray.fromJson(section);
          });
        }
        loadBiomes() {
        }
        dump() {
          const smartBuffer = new SmartBuffer();
          this.sections.forEach((section, i) => {
            if (section !== null && !section.isEmpty()) {
              section.write(smartBuffer);
            }
          });
          this.biomes.forEach((biome) => {
            smartBuffer.writeInt32BE(biome);
          });
          if (!smartBuffer.length) {
            return Buffer.alloc(4096);
          }
          return smartBuffer.toBuffer();
        }
        load(data, bitMap = 65535, skyLightSent = true, fullChunk = true) {
          const reader = SmartBuffer.fromBuffer(data);
          this.skyLightSent = skyLightSent;
          this.sectionMask |= bitMap;
          for (let y = 0; y < constants.NUM_SECTIONS; ++y) {
            if (!(bitMap >> y & 1)) {
              continue;
            }
            let palette;
            let skyLight;
            const bitsPerBlock = reader.readUInt8();
            if (bitsPerBlock <= constants.MAX_BITS_PER_BLOCK) {
              palette = [];
              const numPaletteItems = varInt.read(reader);
              for (let i = 0; i < numPaletteItems; ++i) {
                palette.push(varInt.read(reader));
              }
            } else {
              palette = null;
            }
            const dataArray = new BitArray({
              bitsPerValue: bitsPerBlock > constants.MAX_BITS_PER_BLOCK ? this.maxBitsPerBlock : bitsPerBlock,
              capacity: 4096
            }).readBuffer(reader, varInt.read(reader) * 2);
            const blockLight = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            const blockLightRaw = new Uint8Array(2048);
            for (let i = 0; i < 2048; i++) {
              blockLightRaw[i] = reader.readUInt8();
            }
            for (let i = 0; i < 2048; i++) {
              const byte = blockLightRaw[i];
              const blockIndex1 = i * 2;
              const blockIndex2 = i * 2 + 1;
              const value1 = byte & 15;
              const value2 = byte >> 4 & 15;
              const x1 = blockIndex1 % 16;
              const z1 = Math.floor(blockIndex1 / 16 % 16);
              const y1 = Math.floor(blockIndex1 / 256);
              const x2 = blockIndex2 % 16;
              const z2 = Math.floor(blockIndex2 / 16 % 16);
              const y2 = Math.floor(blockIndex2 / 256);
              const index1 = y1 * 16 * 16 + z1 * 16 + x1;
              const index2 = y2 * 16 * 16 + z2 * 16 + x2;
              blockLight.set(index1, value1);
              if (blockIndex2 < 4096) {
                blockLight.set(index2, value2);
              }
            }
            if (skyLightSent) {
              skyLight = new BitArray({
                bitsPerValue: 4,
                capacity: 4096
              });
              const skyLightRaw = new Uint8Array(2048);
              for (let i = 0; i < 2048; i++) {
                skyLightRaw[i] = reader.readUInt8();
              }
              for (let i = 0; i < 2048; i++) {
                const byte = skyLightRaw[i];
                const blockIndex1 = i * 2;
                const blockIndex2 = i * 2 + 1;
                const value1 = byte & 15;
                const value2 = byte >> 4 & 15;
                const x1 = blockIndex1 % 16;
                const z1 = Math.floor(blockIndex1 / 16 % 16);
                const y1 = Math.floor(blockIndex1 / 256);
                const x2 = blockIndex2 % 16;
                const z2 = Math.floor(blockIndex2 / 16 % 16);
                const y2 = Math.floor(blockIndex2 / 256);
                const index1 = y1 * 16 * 16 + z1 * 16 + x1;
                const index2 = y2 * 16 * 16 + z2 * 16 + x2;
                skyLight.set(index1, value1);
                if (blockIndex2 < 4096) {
                  skyLight.set(index2, value2);
                }
              }
            }
            const section = new ChunkSection({
              data: dataArray,
              palette,
              blockLight,
              maxBitsPerBlock: this.maxBitsPerBlock,
              ...skyLightSent ? { skyLight } : { skyLight: null }
            });
            this.sections[y] = section;
          }
          if (fullChunk) {
            const p = { x: 0, y: 0, z: 0 };
            for (p.z = 0; p.z < constants.SECTION_WIDTH; p.z++) {
              for (p.x = 0; p.x < constants.SECTION_WIDTH; p.x++) {
                this.setBiome(p, reader.readInt32BE());
              }
            }
          }
        }
      };
    };
    function getBiomeIndex(pos) {
      return pos.z * 16 | pos.x;
    }
    function toSectionPos(pos) {
      return { x: pos.x, y: pos.y & 15, z: pos.z };
    }
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.13/chunk.js
var require_chunk3 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.13/chunk.js"(exports2, module2) {
    var constants = require_constants();
    function loader(registry) {
      const Block = require_prismarine_block()(registry);
      const Chunk = require_ChunkColumn2()(Block, registry);
      Chunk.h = constants.CHUNK_HEIGHT;
      Chunk.version = registry.version;
      return Chunk;
    }
    module2.exports = loader;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/CommonChunkSection.js
var require_CommonChunkSection = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/CommonChunkSection.js"(exports2, module2) {
    var neededBits = require_neededBits();
    var constants = require_constants();
    var varInt = require_varInt();
    function getBlockIndex(pos) {
      return pos.y << 8 | pos.z << 4 | pos.x;
    }
    module2.exports = (BitArray) => {
      class ChunkSection {
        constructor(options = {}) {
          if (options === null) {
            return;
          }
          if (typeof options.solidBlockCount === "undefined") {
            options.solidBlockCount = 0;
            if (options.data) {
              for (let i = 0; i < constants.BLOCK_SECTION_VOLUME; i++) {
                if (options.data.get(i) !== 0) {
                  options.solidBlockCount += 1;
                }
              }
            }
          }
          if (!options.data) {
            options.data = new BitArray({
              bitsPerValue: 4,
              capacity: constants.BLOCK_SECTION_VOLUME
            });
          }
          if (options.palette === void 0) {
            options.palette = [0];
          }
          this.data = options.data;
          this.palette = options.palette;
          this.isDirty = false;
          this.solidBlockCount = options.solidBlockCount;
          this.maxBitsPerBlock = options.maxBitsPerBlock ?? constants.GLOBAL_BITS_PER_BLOCK;
        }
        toJson() {
          return JSON.stringify({
            data: this.data.toJson(),
            palette: this.palette,
            isDirty: this.isDirty,
            solidBlockCount: this.solidBlockCount
          });
        }
        static fromJson(j) {
          const parsed = JSON.parse(j);
          return new ChunkSection({
            data: BitArray.fromJson(parsed.data),
            palette: parsed.palette,
            solidBlockCount: parsed.solidBlockCount
          });
        }
        getBlock(pos) {
          let stateId = this.data.get(getBlockIndex(pos));
          if (this.palette !== null) {
            stateId = this.palette[stateId];
          }
          return stateId;
        }
        setBlock(pos, stateId) {
          const blockIndex = getBlockIndex(pos);
          let palettedIndex;
          if (this.palette !== null) {
            const indexInPalette = this.palette.indexOf(stateId);
            if (indexInPalette >= 0) {
              palettedIndex = indexInPalette;
            } else {
              this.palette.push(stateId);
              palettedIndex = this.palette.length - 1;
              const bitsPerValue = neededBits(palettedIndex);
              if (bitsPerValue > this.data.getBitsPerValue()) {
                if (bitsPerValue <= constants.MAX_BITS_PER_BLOCK) {
                  this.data = this.data.resizeTo(bitsPerValue);
                } else {
                  const newData = new BitArray({
                    bitsPerValue: this.maxBitsPerBlock,
                    capacity: constants.BLOCK_SECTION_VOLUME
                  });
                  for (let i = 0; i < constants.BLOCK_SECTION_VOLUME; i++) {
                    const stateId2 = this.palette[this.data.get(i)];
                    newData.set(i, stateId2);
                  }
                  this.palette = null;
                  palettedIndex = stateId;
                  this.data = newData;
                }
              }
            }
          } else {
            palettedIndex = stateId;
          }
          const oldBlock = this.getBlock(pos);
          if (stateId === 0 && oldBlock !== 0) {
            this.solidBlockCount -= 1;
          } else if (stateId !== 0 && oldBlock === 0) {
            this.solidBlockCount += 1;
          }
          this.data.set(blockIndex, palettedIndex);
        }
        isEmpty() {
          return this.solidBlockCount === 0;
        }
        // writes the complete section into a smart buffer object
        write(smartBuffer) {
          smartBuffer.writeInt16BE(this.solidBlockCount);
          smartBuffer.writeUInt8(this.data.getBitsPerValue());
          if (this.palette !== null) {
            varInt.write(smartBuffer, this.palette.length);
            this.palette.forEach((paletteElement) => {
              varInt.write(smartBuffer, paletteElement);
            });
          }
          varInt.write(smartBuffer, this.data.length());
          this.data.writeBuffer(smartBuffer);
        }
      }
      return ChunkSection;
    };
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.14/ChunkColumn.js
var require_ChunkColumn3 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.14/ChunkColumn.js"(exports2, module2) {
    var SmartBuffer = require_smartbuffer().SmartBuffer;
    var BitArray = require_BitArray();
    var ChunkSection = require_CommonChunkSection()(BitArray);
    var CommonChunkColumn = require_CommonChunkColumn();
    var constants = require_constants();
    var varInt = require_varInt();
    var neededBits = require_neededBits();
    module2.exports = (Block, mcData2) => {
      return class ChunkColumn extends CommonChunkColumn {
        static get section() {
          return ChunkSection;
        }
        constructor() {
          super(mcData2);
          this.sectionMask = 0;
          this.sections = Array(constants.NUM_SECTIONS).fill(null);
          this.biomes = Array(
            constants.SECTION_WIDTH * constants.SECTION_WIDTH
          ).fill(1);
          this.skyLightMask = 0;
          this.blockLightMask = 0;
          this.skyLightSections = Array(constants.NUM_SECTIONS + 2).fill(null);
          this.blockLightSections = Array(constants.NUM_SECTIONS + 2).fill(null);
          this.maxBitsPerBlock = neededBits(Object.values(mcData2.blocks).reduce((high, block) => Math.max(high, block.maxStateId), 0));
        }
        toJson() {
          return JSON.stringify({
            biomes: this.biomes,
            blockEntities: this.blockEntities,
            sectionMask: this.sectionMask,
            sections: this.sections.map((section) => section === null ? null : section.toJson()),
            skyLightMask: this.skyLightMask,
            blockLightMask: this.blockLightMask,
            skyLightSections: this.skyLightSections.map((section) => section === null ? null : section.toJson()),
            blockLightSections: this.blockLightSections.map((section) => section === null ? null : section.toJson())
          });
        }
        static fromJson(j) {
          const parsed = JSON.parse(j);
          const chunk = new ChunkColumn();
          chunk.biomes = parsed.biomes;
          chunk.blockEntities = parsed.blockEntities;
          chunk.sectionMask = parsed.sectionMask;
          chunk.sections = parsed.sections.map((s) => s === null ? null : ChunkSection.fromJson(s));
          chunk.skyLightMask = parsed.skyLightMask;
          chunk.blockLightMask = parsed.blockLightMask;
          chunk.skyLightSections = parsed.skyLightSections.map((s) => s === null ? null : BitArray.fromJson(s));
          chunk.blockLightSections = parsed.blockLightSections.map((s) => s === null ? null : BitArray.fromJson(s));
          return chunk;
        }
        initialize(func) {
          const p = { x: 0, y: 0, z: 0 };
          for (p.y = 0; p.y < constants.CHUNK_HEIGHT; p.y++) {
            for (p.z = 0; p.z < constants.SECTION_WIDTH; p.z++) {
              for (p.x = 0; p.x < constants.SECTION_WIDTH; p.x++) {
                const block = func(p.x, p.y, p.z);
                if (block === null) {
                  continue;
                }
                this.setBlock(p, block);
              }
            }
          }
        }
        getBlock(pos) {
          const section = this.sections[pos.y >> 4];
          const biome = this.getBiome(pos);
          if (!section) {
            return Block.fromStateId(0, biome);
          }
          const stateId = section.getBlock(toSectionPos(pos));
          const block = Block.fromStateId(stateId, biome);
          block.light = this.getBlockLight(pos);
          block.skyLight = this.getSkyLight(pos);
          block.entity = this.getBlockEntity(pos);
          return block;
        }
        setBlock(pos, block) {
          if (typeof block.stateId !== "undefined") {
            this.setBlockStateId(pos, block.stateId);
          }
          if (typeof block.biome !== "undefined") {
            this.setBiome(pos, block.biome.id);
          }
          if (typeof block.skyLight !== "undefined") {
            this.setSkyLight(pos, block.skyLight);
          }
          if (typeof block.light !== "undefined") {
            this.setBlockLight(pos, block.light);
          }
          if (block.entity) {
            this.setBlockEntity(pos, block.entity);
          } else {
            this.removeBlockEntity(pos);
          }
        }
        getBlockType(pos) {
          const blockStateId = this.getBlockStateId(pos);
          return mcData2.blocksByStateId[blockStateId].id;
        }
        getBlockData(pos) {
          const blockStateId = this.getBlockStateId(pos);
          return mcData2.blocksByStateId[blockStateId].metadata;
        }
        getBlockStateId(pos) {
          const section = this.sections[pos.y >> 4];
          return section ? section.getBlock(toSectionPos(pos)) : 0;
        }
        getBlockLight(pos) {
          const section = this.blockLightSections[getLightSectionIndex2(pos)];
          if (!section)
            return 0;
          const localY = pos.y % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          return section.get(index);
        }
        /**
         * Get skylight value at specific coordinates within a chunk
         * Matching C++ implementation index calculation
         */
        getSkyLight(pos) {
          const section = this.skyLightSections[getLightSectionIndex2(pos)];
          if (!section)
            return 0;
          const localY = pos.y % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          return section.get(index);
        }
        getBiome(pos) {
          return this.biomes[getBiomeIndex(pos)];
        }
        getBiomeColor(pos) {
          return { r: 0, g: 0, b: 0 };
        }
        setBlockType(pos, id) {
          this.setBlockStateId(pos, mcData2.blocks[id].minStateId);
        }
        setBlockData(pos, data) {
          this.setBlockStateId(pos, mcData2.blocksByStateId[this.getBlockStateId(pos)].minStateId + data);
        }
        setBlockStateId(pos, stateId) {
          const sectionIndex = pos.y >> 4;
          if (sectionIndex < 0 || sectionIndex >= 16)
            return;
          let section = this.sections[sectionIndex];
          if (!section) {
            if (stateId === 0) {
              return;
            }
            section = new ChunkSection({
              maxBitsPerBlock: this.maxBitsPerBlock
            });
            this.sectionMask |= 1 << sectionIndex;
            this.sections[sectionIndex] = section;
          }
          section.setBlock(toSectionPos(pos), stateId);
        }
        setBlockLight(pos, light) {
          const sectionIndex = getLightSectionIndex2(pos);
          let section = this.blockLightSections[sectionIndex];
          if (section === null) {
            if (light === 0) {
              return;
            }
            section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            this.blockLightMask |= 1 << sectionIndex;
            this.blockLightSections[sectionIndex] = section;
          }
          const localY = pos.y % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          section.set(index, light);
        }
        setSkyLight(pos, light) {
          const sectionIndex = getLightSectionIndex2(pos);
          let section = this.skyLightSections[sectionIndex];
          if (section === null) {
            if (light === 0) {
              return;
            }
            section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            this.skyLightMask |= 1 << sectionIndex;
            this.skyLightSections[sectionIndex] = section;
          }
          const localY = pos.y % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          section.set(index, light);
        }
        setBiome(pos, biome) {
          this.biomes[getBiomeIndex(pos)] = biome;
        }
        getMask() {
          return this.sectionMask;
        }
        dump() {
          const smartBuffer = new SmartBuffer();
          this.sections.forEach((section, i) => {
            if (section !== null && !section.isEmpty()) {
              section.write(smartBuffer);
            }
          });
          this.biomes.forEach((biome) => {
            smartBuffer.writeInt32BE(biome);
          });
          if (!smartBuffer.length) {
            return Buffer.alloc(4096);
          }
          return smartBuffer.toBuffer();
        }
        load(data, bitMap = 65535, skyLightSent = true, fullChunk = true) {
          const reader = SmartBuffer.fromBuffer(data);
          this.sectionMask |= bitMap;
          for (let y = 0; y < constants.NUM_SECTIONS; ++y) {
            if (!(bitMap >> y & 1)) {
              continue;
            }
            let palette;
            const solidBlockCount = reader.readInt16BE();
            const bitsPerBlock = reader.readUInt8();
            if (bitsPerBlock <= constants.MAX_BITS_PER_BLOCK) {
              palette = [];
              const numPaletteItems = varInt.read(reader);
              for (let i = 0; i < numPaletteItems; ++i) {
                palette.push(varInt.read(reader));
              }
            } else {
              palette = null;
            }
            const dataArray = new BitArray({
              bitsPerValue: bitsPerBlock > constants.MAX_BITS_PER_BLOCK ? this.maxBitsPerBlock : bitsPerBlock,
              capacity: 4096
            }).readBuffer(reader, varInt.read(reader) * 2);
            const section = new ChunkSection({
              data: dataArray,
              palette,
              solidBlockCount,
              maxBitsPerBlock: this.maxBitsPerBlock
            });
            this.sections[y] = section;
          }
          if (fullChunk) {
            const p = { x: 0, y: 0, z: 0 };
            for (p.z = 0; p.z < constants.SECTION_WIDTH; p.z++) {
              for (p.x = 0; p.x < constants.SECTION_WIDTH; p.x++) {
                this.setBiome(p, reader.readInt32BE());
              }
            }
          }
        }
        loadLight(data, skyLightMask, blockLightMask, emptySkyLightMask = 0, emptyBlockLightMask = 0) {
          const reader = SmartBuffer.fromBuffer(data);
          this.skyLightMask |= skyLightMask;
          for (let y = 0; y < constants.NUM_SECTIONS + 2; y++) {
            if (!(skyLightMask >> y & 1)) {
              continue;
            }
            const length = varInt.read(reader);
            const section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            const tempBuffer = new Uint8Array(length);
            for (let i = 0; i < length; i++) {
              tempBuffer[i] = reader.readUInt8();
            }
            for (let i = 0; i < length; i++) {
              const byte = tempBuffer[i];
              const blockIndex1 = i * 2;
              const blockIndex2 = i * 2 + 1;
              const value1 = byte & 15;
              const value2 = byte >> 4 & 15;
              const x1 = blockIndex1 % 16;
              const z1 = Math.floor(blockIndex1 / 16) % 16;
              const y1 = Math.floor(blockIndex1 / 256);
              const x2 = blockIndex2 % 16;
              const z2 = Math.floor(blockIndex2 / 16) % 16;
              const y2 = Math.floor(blockIndex2 / 256);
              const index1 = y1 * 16 * 16 + z1 * 16 + x1;
              const index2 = y2 * 16 * 16 + z2 * 16 + x2;
              section.set(index1, value1);
              if (blockIndex2 < 4096) {
                section.set(index2, value2);
              }
            }
            this.skyLightSections[y] = section;
          }
          this.blockLightMask |= blockLightMask;
          for (let y = 0; y < constants.NUM_SECTIONS + 2; y++) {
            if (!(blockLightMask >> y & 1)) {
              continue;
            }
            const length = varInt.read(reader);
            const section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            const tempBuffer = new Uint8Array(length);
            for (let i = 0; i < length; i++) {
              tempBuffer[i] = reader.readUInt8();
            }
            for (let i = 0; i < length; i++) {
              const byte = tempBuffer[i];
              const blockIndex1 = i * 2;
              const blockIndex2 = i * 2 + 1;
              const value1 = byte & 15;
              const value2 = byte >> 4 & 15;
              const x1 = blockIndex1 % 16;
              const z1 = Math.floor(blockIndex1 / 16) % 16;
              const y1 = Math.floor(blockIndex1 / 256);
              const x2 = blockIndex2 % 16;
              const z2 = Math.floor(blockIndex2 / 16) % 16;
              const y2 = Math.floor(blockIndex2 / 256);
              const index1 = y1 * 16 * 16 + z1 * 16 + x1;
              const index2 = y2 * 16 * 16 + z2 * 16 + x2;
              section.set(index1, value1);
              if (blockIndex2 < 4096) {
                section.set(index2, value2);
              }
            }
            this.blockLightSections[y] = section;
          }
        }
        // These methods do nothing, and are present only for API compatibility
        dumpBiomes() {
        }
        loadBiomes() {
        }
        dumpLight() {
          const smartBuffer = new SmartBuffer();
          this.skyLightSections.forEach((section, i) => {
            if (section !== null) {
              varInt.write(smartBuffer, 2048);
              section.writeBuffer(smartBuffer);
            }
          });
          this.blockLightSections.forEach((section, i) => {
            if (section !== null) {
              varInt.write(smartBuffer, 2048);
              section.writeBuffer(smartBuffer);
            }
          });
          return smartBuffer.toBuffer();
        }
        /**
         * New serializable light data dump method with consistent interface across versions
         * Returns an object that can be easily transferred between threads
         * @returns {Object} Object containing serialized light data
         */
        dumpLightNew() {
          return {
            skyLightSections: this.skyLightSections.map((section) => section === null ? null : section.toJson()),
            blockLightSections: this.blockLightSections.map((section) => section === null ? null : section.toJson()),
            skyLightMask: this.skyLightMask,
            blockLightMask: this.blockLightMask,
            emptySkyLightMask: 0,
            // Not used in 1.14, but included for consistency
            emptyBlockLightMask: 0
            // Not used in 1.14, but included for consistency
          };
        }
        /**
         * New light data loading method with consistent interface across versions
         * Accepts a single argument that contains all necessary light data
         * @param {Object} lightData - Object containing serialized light data
         */
        loadLightNew(lightData) {
          const { skyLightSections, blockLightSections, skyLightMask, blockLightMask } = lightData;
          this.skyLightMask = skyLightMask;
          this.skyLightSections = skyLightSections.map((section) => section === null ? null : BitArray.fromJson(section));
          this.blockLightMask = blockLightMask;
          this.blockLightSections = blockLightSections.map((section) => section === null ? null : BitArray.fromJson(section));
        }
      };
    };
    function getLightSectionIndex2(pos) {
      return Math.floor(pos.y / 16) + 1;
    }
    function getBiomeIndex(pos) {
      return pos.z * 16 | pos.x;
    }
    function toSectionPos(pos) {
      return { x: pos.x, y: pos.y & 15, z: pos.z };
    }
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.14/chunk.js
var require_chunk4 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.14/chunk.js"(exports2, module2) {
    var constants = require_constants();
    function loader(registry) {
      const Block = require_prismarine_block()(registry);
      const Chunk = require_ChunkColumn3()(Block, registry);
      Chunk.h = constants.CHUNK_HEIGHT;
      Chunk.version = registry.version;
      return Chunk;
    }
    module2.exports = loader;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.15/ChunkColumn.js
var require_ChunkColumn4 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.15/ChunkColumn.js"(exports2, module2) {
    var SmartBuffer = require_smartbuffer().SmartBuffer;
    var BitArray = require_BitArray();
    var ChunkSection = require_CommonChunkSection()(BitArray);
    var CommonChunkColumn = require_CommonChunkColumn();
    var constants = require_constants();
    var varInt = require_varInt();
    var neededBits = require_neededBits();
    module2.exports = (Block, mcData2) => {
      return class ChunkColumn extends CommonChunkColumn {
        static get section() {
          return ChunkSection;
        }
        constructor() {
          super(mcData2);
          this.sectionMask = 0;
          this.sections = Array(constants.NUM_SECTIONS).fill(null);
          this.biomes = Array(4 * 4 * 64).fill(127);
          this.skyLightMask = 0;
          this.blockLightMask = 0;
          this.skyLightSections = Array(constants.NUM_SECTIONS + 2).fill(null);
          this.blockLightSections = Array(constants.NUM_SECTIONS + 2).fill(null);
          this.maxBitsPerBlock = neededBits(Object.values(mcData2.blocks).reduce((high, block) => Math.max(high, block.maxStateId), 0));
        }
        toJson() {
          return JSON.stringify({
            biomes: this.biomes,
            blockEntities: this.blockEntities,
            sectionMask: this.sectionMask,
            sections: this.sections.map((section) => section === null ? null : section.toJson()),
            skyLightMask: this.skyLightMask,
            blockLightMask: this.blockLightMask,
            skyLightSections: this.skyLightSections.map((section) => section === null ? null : section.toJson()),
            blockLightSections: this.blockLightSections.map((section) => section === null ? null : section.toJson())
          });
        }
        static fromJson(j) {
          const parsed = JSON.parse(j);
          const chunk = new ChunkColumn();
          chunk.biomes = parsed.biomes;
          chunk.blockEntities = parsed.blockEntities;
          chunk.sectionMask = parsed.sectionMask;
          chunk.sections = parsed.sections.map((s) => s === null ? null : ChunkSection.fromJson(s));
          chunk.skyLightMask = parsed.skyLightMask;
          chunk.blockLightMask = parsed.blockLightMask;
          chunk.skyLightSections = parsed.skyLightSections.map((s) => s === null ? null : BitArray.fromJson(s));
          chunk.blockLightSections = parsed.blockLightSections.map((s) => s === null ? null : BitArray.fromJson(s));
          return chunk;
        }
        initialize(func) {
          const p = { x: 0, y: 0, z: 0 };
          for (p.y = 0; p.y < constants.CHUNK_HEIGHT; p.y++) {
            for (p.z = 0; p.z < constants.SECTION_WIDTH; p.z++) {
              for (p.x = 0; p.x < constants.SECTION_WIDTH; p.x++) {
                const block = func(p.x, p.y, p.z);
                if (block === null) {
                  continue;
                }
                this.setBlock(p, block);
              }
            }
          }
        }
        getBlock(pos) {
          const section = this.sections[pos.y >> 4];
          const biome = this.getBiome(pos);
          if (!section) {
            return Block.fromStateId(0, biome);
          }
          const stateId = section.getBlock(toSectionPos(pos));
          const block = Block.fromStateId(stateId, biome);
          block.light = this.getBlockLight(pos);
          block.skyLight = this.getSkyLight(pos);
          block.entity = this.getBlockEntity(pos);
          return block;
        }
        setBlock(pos, block) {
          if (typeof block.stateId !== "undefined") {
            this.setBlockStateId(pos, block.stateId);
          }
          if (typeof block.biome !== "undefined") {
            this.setBiome(pos, block.biome.id);
          }
          if (typeof block.skyLight !== "undefined") {
            this.setSkyLight(pos, block.skyLight);
          }
          if (typeof block.light !== "undefined") {
            this.setBlockLight(pos, block.light);
          }
          if (block.entity) {
            this.setBlockEntity(pos, block.entity);
          } else {
            this.removeBlockEntity(pos);
          }
        }
        getBlockType(pos) {
          const blockStateId = this.getBlockStateId(pos);
          return mcData2.blocksByStateId[blockStateId].id;
        }
        getBlockData(pos) {
          const blockStateId = this.getBlockStateId(pos);
          return mcData2.blocksByStateId[blockStateId].metadata;
        }
        getBlockStateId(pos) {
          const section = this.sections[pos.y >> 4];
          return section ? section.getBlock(toSectionPos(pos)) : 0;
        }
        getBlockLight(pos) {
          const section = this.blockLightSections[getLightSectionIndex2(pos)];
          if (!section)
            return 0;
          const localY = pos.y % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          return section.get(index);
        }
        /**
         * Get skylight value at specific coordinates within a chunk
         * Matching C++ implementation index calculation
         */
        getSkyLight(pos) {
          const section = this.skyLightSections[getLightSectionIndex2(pos)];
          if (!section)
            return 0;
          const localY = pos.y % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          return section.get(index);
        }
        getBiome(pos) {
          if (pos.y < 0 || pos.y >= 256)
            return 0;
          return this.biomes[getBiomeIndex(pos)];
        }
        setBlockType(pos, id) {
          this.setBlockStateId(pos, mcData2.blocks[id].minStateId);
        }
        setBlockData(pos, data) {
          this.setBlockStateId(pos, mcData2.blocksByStateId[this.getBlockStateId(pos)].minStateId + data);
        }
        setBlockStateId(pos, stateId) {
          const sectionIndex = pos.y >> 4;
          if (sectionIndex < 0 || sectionIndex >= 16)
            return;
          let section = this.sections[sectionIndex];
          if (!section) {
            if (stateId === 0) {
              return;
            }
            section = new ChunkSection({
              maxBitsPerBlock: this.maxBitsPerBlock
            });
            this.sectionMask |= 1 << sectionIndex;
            this.sections[sectionIndex] = section;
          }
          section.setBlock(toSectionPos(pos), stateId);
        }
        setBlockLight(pos, light) {
          const sectionIndex = getLightSectionIndex2(pos);
          let section = this.blockLightSections[sectionIndex];
          if (section === null) {
            if (light === 0) {
              return;
            }
            section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            this.blockLightMask |= 1 << sectionIndex;
            this.blockLightSections[sectionIndex] = section;
          }
          const localY = pos.y % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          section.set(index, light);
        }
        setSkyLight(pos, light) {
          const sectionIndex = getLightSectionIndex2(pos);
          let section = this.skyLightSections[sectionIndex];
          if (section === null) {
            if (light === 0) {
              return;
            }
            section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            this.skyLightMask |= 1 << sectionIndex;
            this.skyLightSections[sectionIndex] = section;
          }
          const localY = pos.y % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          section.set(index, light);
        }
        setBiome(pos, biome) {
          if (pos.y < 0 || pos.y >= 256)
            return;
          this.biomes[getBiomeIndex(pos)] = biome;
        }
        getMask() {
          return this.sectionMask;
        }
        dump() {
          const smartBuffer = new SmartBuffer();
          this.sections.forEach((section, i) => {
            if (section !== null && !section.isEmpty()) {
              section.write(smartBuffer);
            }
          });
          if (!smartBuffer.length) {
            return Buffer.alloc(4096);
          }
          return smartBuffer.toBuffer();
        }
        loadBiomes(biomes) {
          this.biomes = biomes;
        }
        dumpBiomes(biomes) {
          return this.biomes;
        }
        load(data, bitMap = 65535) {
          const reader = SmartBuffer.fromBuffer(data);
          this.sectionMask |= bitMap;
          for (let y = 0; y < constants.NUM_SECTIONS; ++y) {
            if (!(bitMap >> y & 1)) {
              continue;
            }
            let palette;
            const solidBlockCount = reader.readInt16BE();
            const bitsPerBlock = reader.readUInt8();
            if (bitsPerBlock <= constants.MAX_BITS_PER_BLOCK) {
              palette = [];
              const numPaletteItems = varInt.read(reader);
              for (let i = 0; i < numPaletteItems; ++i) {
                palette.push(varInt.read(reader));
              }
            } else {
              palette = null;
            }
            const dataArray = new BitArray({
              bitsPerValue: bitsPerBlock > constants.MAX_BITS_PER_BLOCK ? this.maxBitsPerBlock : bitsPerBlock,
              capacity: 4096
            }).readBuffer(reader, varInt.read(reader) * 2);
            const section = new ChunkSection({
              data: dataArray,
              palette,
              solidBlockCount,
              maxBitsPerBlock: this.maxBitsPerBlock
            });
            this.sections[y] = section;
          }
        }
        loadLight(data, skyLightMask, blockLightMask, emptySkyLightMask = 0, emptyBlockLightMask = 0) {
          const reader = SmartBuffer.fromBuffer(data);
          this.skyLightMask |= skyLightMask;
          for (let y = 0; y < constants.NUM_SECTIONS + 2; y++) {
            if (!(skyLightMask >> y & 1)) {
              continue;
            }
            const length = varInt.read(reader);
            const section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            const tempBuffer = new Uint8Array(length);
            for (let i = 0; i < length; i++) {
              tempBuffer[i] = reader.readUInt8();
            }
            for (let i = 0; i < length; i++) {
              const byte = tempBuffer[i];
              const blockIndex1 = i * 2;
              const blockIndex2 = i * 2 + 1;
              const value1 = byte & 15;
              const value2 = byte >> 4 & 15;
              const x1 = blockIndex1 % 16;
              const z1 = Math.floor(blockIndex1 / 16) % 16;
              const y1 = Math.floor(blockIndex1 / 256);
              const x2 = blockIndex2 % 16;
              const z2 = Math.floor(blockIndex2 / 16) % 16;
              const y2 = Math.floor(blockIndex2 / 256);
              const index1 = y1 * 16 * 16 + z1 * 16 + x1;
              const index2 = y2 * 16 * 16 + z2 * 16 + x2;
              section.set(index1, value1);
              if (blockIndex2 < 4096) {
                section.set(index2, value2);
              }
            }
            this.skyLightSections[y] = section;
          }
          this.blockLightMask |= blockLightMask;
          for (let y = 0; y < constants.NUM_SECTIONS + 2; y++) {
            if (!(blockLightMask >> y & 1)) {
              continue;
            }
            const length = varInt.read(reader);
            const section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            const tempBuffer = new Uint8Array(length);
            for (let i = 0; i < length; i++) {
              tempBuffer[i] = reader.readUInt8();
            }
            for (let i = 0; i < length; i++) {
              const byte = tempBuffer[i];
              const blockIndex1 = i * 2;
              const blockIndex2 = i * 2 + 1;
              const value1 = byte & 15;
              const value2 = byte >> 4 & 15;
              const x1 = blockIndex1 % 16;
              const z1 = Math.floor(blockIndex1 / 16) % 16;
              const y1 = Math.floor(blockIndex1 / 256);
              const x2 = blockIndex2 % 16;
              const z2 = Math.floor(blockIndex2 / 16) % 16;
              const y2 = Math.floor(blockIndex2 / 256);
              const index1 = y1 * 16 * 16 + z1 * 16 + x1;
              const index2 = y2 * 16 * 16 + z2 * 16 + x2;
              section.set(index1, value1);
              if (blockIndex2 < 4096) {
                section.set(index2, value2);
              }
            }
            this.blockLightSections[y] = section;
          }
        }
        dumpLight() {
          const smartBuffer = new SmartBuffer();
          this.skyLightSections.forEach((section, i) => {
            if (section !== null) {
              varInt.write(smartBuffer, 2048);
              section.writeBuffer(smartBuffer);
            }
          });
          this.blockLightSections.forEach((section, i) => {
            if (section !== null) {
              varInt.write(smartBuffer, 2048);
              section.writeBuffer(smartBuffer);
            }
          });
          return smartBuffer.toBuffer();
        }
        /**
         * New serializable light data dump method with consistent interface across versions
         * Returns an object that can be easily transferred between threads
         * @returns {Object} Object containing serialized light data
         */
        dumpLightNew() {
          return {
            skyLightSections: this.skyLightSections.map((section) => section === null ? null : section.toJson()),
            blockLightSections: this.blockLightSections.map((section) => section === null ? null : section.toJson()),
            skyLightMask: this.skyLightMask,
            blockLightMask: this.blockLightMask,
            emptySkyLightMask: 0,
            // Not used in 1.15, but included for consistency
            emptyBlockLightMask: 0
            // Not used in 1.15, but included for consistency
          };
        }
        /**
         * New light data loading method with consistent interface across versions
         * Accepts a single argument that contains all necessary light data
         * @param {Object} lightData - Object containing serialized light data
         */
        loadLightNew(lightData) {
          const { skyLightSections, blockLightSections, skyLightMask, blockLightMask } = lightData;
          this.skyLightMask = skyLightMask;
          this.skyLightSections = skyLightSections.map((section) => section === null ? null : BitArray.fromJson(section));
          this.blockLightMask = blockLightMask;
          this.blockLightSections = blockLightSections.map((section) => section === null ? null : BitArray.fromJson(section));
        }
      };
    };
    function getLightSectionIndex2(pos) {
      return Math.floor(pos.y / 16) + 1;
    }
    function getBiomeIndex(pos) {
      return (pos.y >> 2 & 63) << 4 | (pos.z >> 2 & 3) << 2 | pos.x >> 2 & 3;
    }
    function toSectionPos(pos) {
      return { x: pos.x, y: pos.y & 15, z: pos.z };
    }
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.15/chunk.js
var require_chunk5 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.15/chunk.js"(exports2, module2) {
    var constants = require_constants();
    function loader(registry) {
      const Block = require_prismarine_block()(registry);
      const Chunk = require_ChunkColumn4()(Block, registry);
      Chunk.h = constants.CHUNK_HEIGHT;
      Chunk.version = registry.version;
      return Chunk;
    }
    module2.exports = loader;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/BitArrayNoSpan.js
var require_BitArrayNoSpan = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/BitArrayNoSpan.js"(exports2, module2) {
    var neededBits = require_neededBits();
    var BitArray = class _BitArray {
      constructor(options) {
        if (options === null) {
          return;
        }
        const valuesPerLong = Math.floor(64 / options.bitsPerValue);
        const bufferSize = Math.ceil(options.capacity / valuesPerLong) * 2;
        const valueMask = (1 << options.bitsPerValue) - 1;
        this.data = new Uint32Array(options?.data ?? bufferSize);
        this.capacity = options.capacity;
        this.bitsPerValue = options.bitsPerValue;
        this.valuesPerLong = valuesPerLong;
        this.valueMask = valueMask;
      }
      toJson() {
        return JSON.stringify({
          data: Array.from(this.data),
          capacity: this.capacity,
          bitsPerValue: this.bitsPerValue,
          valuesPerLong: this.valuesPerLong,
          valueMask: this.valueMask
        });
      }
      static fromJson(j) {
        return new _BitArray(JSON.parse(j));
      }
      toArray() {
        const array = [];
        for (let i = 0; i < this.capacity; i++) {
          array.push(this.get(i));
        }
        return array;
      }
      static fromArray(array, bitsPerValue) {
        const bitarray = new _BitArray({
          capacity: array.length,
          bitsPerValue
        });
        for (let i = 0; i < array.length; i++) {
          bitarray.set(i, array[i]);
        }
        return bitarray;
      }
      // [[MSB, LSB]]
      toLongArray() {
        const array = [];
        for (let i = 0; i < this.data.length; i += 2) {
          array.push([this.data[i + 1] << 32 >> 32, this.data[i] << 32 >> 32]);
        }
        return array;
      }
      static fromLongArray(array, bitsPerValue) {
        const bitArray = new _BitArray({
          capacity: Math.floor(64 / bitsPerValue) * array.length,
          bitsPerValue
        });
        for (let i = 0; i < array.length; i++) {
          const j = i * 2;
          bitArray.data[j + 1] = array[i][0];
          bitArray.data[j] = array[i][1];
        }
        return bitArray;
      }
      static or(a, b) {
        const long = a.data.length > b.data.length ? a.data : b.data;
        const short = a.data.length > b.data.length ? b.data : a.data;
        const array = new Uint32Array(long.length);
        array.set(long);
        for (let i = 0; i < short.length; i++) {
          array[i] |= short[i];
        }
        return new _BitArray({
          data: array.buffer,
          bitsPerValue: Math.max(a.bitsPerValue, b.bitsPerValue)
        });
      }
      get(index) {
        const startLongIndex = Math.floor(index / this.valuesPerLong);
        const indexInLong = (index - startLongIndex * this.valuesPerLong) * this.bitsPerValue;
        if (indexInLong >= 32) {
          const indexInStartLong2 = indexInLong - 32;
          const startLong2 = this.data[startLongIndex * 2 + 1];
          return startLong2 >>> indexInStartLong2 & this.valueMask;
        }
        const startLong = this.data[startLongIndex * 2];
        const indexInStartLong = indexInLong;
        let result = startLong >>> indexInStartLong;
        const endBitOffset = indexInStartLong + this.bitsPerValue;
        if (endBitOffset > 32) {
          const endLong = this.data[startLongIndex * 2 + 1];
          result |= endLong << 32 - indexInStartLong;
        }
        return result & this.valueMask;
      }
      set(index, value) {
        const startLongIndex = Math.floor(index / this.valuesPerLong);
        const indexInLong = (index - startLongIndex * this.valuesPerLong) * this.bitsPerValue;
        if (indexInLong >= 32) {
          const indexInStartLong2 = indexInLong - 32;
          this.data[startLongIndex * 2 + 1] = (this.data[startLongIndex * 2 + 1] & ~(this.valueMask << indexInStartLong2) | (value & this.valueMask) << indexInStartLong2) >>> 0;
          return;
        }
        const indexInStartLong = indexInLong;
        this.data[startLongIndex * 2] = (this.data[startLongIndex * 2] & ~(this.valueMask << indexInStartLong) | (value & this.valueMask) << indexInStartLong) >>> 0;
        const endBitOffset = indexInStartLong + this.bitsPerValue;
        if (endBitOffset > 32) {
          this.data[startLongIndex * 2 + 1] = (this.data[startLongIndex * 2 + 1] & ~((1 << endBitOffset - 32) - 1) | value >> 32 - indexInStartLong) >>> 0;
        }
      }
      resize(newCapacity) {
        const newArr = new _BitArray({
          bitsPerValue: this.bitsPerValue,
          capacity: newCapacity
        });
        for (let i = 0; i < Math.min(newCapacity, this.capacity); ++i) {
          newArr.set(i, this.get(i));
        }
        return newArr;
      }
      resizeTo(newBitsPerValue) {
        const newArr = new _BitArray({
          bitsPerValue: newBitsPerValue,
          capacity: this.capacity
        });
        for (let i = 0; i < this.capacity; ++i) {
          const value = this.get(i);
          if (neededBits(value) > newBitsPerValue) {
            throw new Error(
              "existing value in BitArray can't fit in new bits per value"
            );
          }
          newArr.set(i, value);
        }
        return newArr;
      }
      length() {
        return this.data.length / 2;
      }
      readBuffer(smartBuffer, size = this.data.length) {
        if (!Number.isFinite(size) || size < 0 || size > 1e6) {
          console.warn(`Invalid size in BitArray.readBuffer: ${size}, using default size`);
          size = this.data.length;
        }
        if (size !== this.data.length) {
          this.data = new Uint32Array(size);
          for (let i = 0; i < size; i += 2) {
            this.data[i + 1] = smartBuffer.readUInt32BE();
            this.data[i] = smartBuffer.readUInt32BE();
          }
          return;
        }
        for (let i = 0; i < this.data.length; i += 2) {
          this.data[i + 1] = smartBuffer.readUInt32BE();
          this.data[i] = smartBuffer.readUInt32BE();
        }
        return this;
      }
      writeBuffer(smartBuffer) {
        for (let i = 0; i < this.data.length; i += 2) {
          smartBuffer.writeUInt32BE(this.data[i + 1]);
          smartBuffer.writeUInt32BE(this.data[i]);
        }
        return this;
      }
      getBitsPerValue() {
        return this.bitsPerValue;
      }
    };
    module2.exports = BitArray;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.16/ChunkColumn.js
var require_ChunkColumn5 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.16/ChunkColumn.js"(exports2, module2) {
    var SmartBuffer = require_smartbuffer().SmartBuffer;
    var BitArray = require_BitArrayNoSpan();
    var ChunkSection = require_CommonChunkSection()(BitArray);
    var CommonChunkColumn = require_CommonChunkColumn();
    var constants = require_constants();
    var varInt = require_varInt();
    var neededBits = require_neededBits();
    module2.exports = (Block, mcData2) => {
      return class ChunkColumn extends CommonChunkColumn {
        static get section() {
          return ChunkSection;
        }
        constructor() {
          super(mcData2);
          this.sectionMask = 0;
          this.sections = Array(constants.NUM_SECTIONS).fill(null);
          this.biomes = Array(4 * 4 * 64).fill(127);
          this.skyLightMask = 0;
          this.blockLightMask = 0;
          this.skyLightSections = Array(constants.NUM_SECTIONS + 2).fill(null);
          this.blockLightSections = Array(constants.NUM_SECTIONS + 2).fill(null);
          this.maxBitsPerBlock = neededBits(Object.values(mcData2.blocks).reduce((high, block) => Math.max(high, block.maxStateId), 0));
        }
        toJson() {
          return JSON.stringify({
            biomes: this.biomes,
            blockEntities: this.blockEntities,
            sectionMask: this.sectionMask,
            sections: this.sections.map((section) => section === null ? null : section.toJson()),
            skyLightMask: this.skyLightMask,
            blockLightMask: this.blockLightMask,
            skyLightSections: this.skyLightSections.map((section) => section === null ? null : section.toJson()),
            blockLightSections: this.blockLightSections.map((section) => section === null ? null : section.toJson())
          });
        }
        static fromJson(j) {
          const parsed = JSON.parse(j);
          const chunk = new ChunkColumn();
          chunk.biomes = parsed.biomes;
          chunk.blockEntities = parsed.blockEntities;
          chunk.sectionMask = parsed.sectionMask;
          chunk.sections = parsed.sections.map((s) => s === null ? null : ChunkSection.fromJson(s));
          chunk.skyLightMask = parsed.skyLightMask;
          chunk.blockLightMask = parsed.blockLightMask;
          chunk.skyLightSections = parsed.skyLightSections.map((s) => s === null ? null : BitArray.fromJson(s));
          chunk.blockLightSections = parsed.blockLightSections.map((s) => s === null ? null : BitArray.fromJson(s));
          return chunk;
        }
        initialize(func) {
          const p = { x: 0, y: 0, z: 0 };
          for (p.y = 0; p.y < constants.CHUNK_HEIGHT; p.y++) {
            for (p.z = 0; p.z < constants.SECTION_WIDTH; p.z++) {
              for (p.x = 0; p.x < constants.SECTION_WIDTH; p.x++) {
                const block = func(p.x, p.y, p.z);
                if (block === null) {
                  continue;
                }
                this.setBlock(p, block);
              }
            }
          }
        }
        getBlock(pos) {
          const section = this.sections[pos.y >> 4];
          const biome = this.getBiome(pos);
          if (!section) {
            return Block.fromStateId(0, biome);
          }
          const stateId = section.getBlock(toSectionPos(pos));
          const block = Block.fromStateId(stateId, biome);
          block.light = this.getBlockLight(pos);
          block.skyLight = this.getSkyLight(pos);
          block.entity = this.getBlockEntity(pos);
          return block;
        }
        setBlock(pos, block) {
          if (typeof block.stateId !== "undefined") {
            this.setBlockStateId(pos, block.stateId);
          }
          if (typeof block.biome !== "undefined") {
            this.setBiome(pos, block.biome.id);
          }
          if (typeof block.skyLight !== "undefined") {
            this.setSkyLight(pos, block.skyLight);
          }
          if (typeof block.light !== "undefined") {
            this.setBlockLight(pos, block.light);
          }
          if (block.entity) {
            this.setBlockEntity(pos, block.entity);
          } else {
            this.removeBlockEntity(pos);
          }
        }
        getBlockType(pos) {
          const blockStateId = this.getBlockStateId(pos);
          return mcData2.blocksByStateId[blockStateId].id;
        }
        getBlockData(pos) {
          const blockStateId = this.getBlockStateId(pos);
          return mcData2.blocksByStateId[blockStateId].metadata;
        }
        getBlockStateId(pos) {
          const section = this.sections[pos.y >> 4];
          return section ? section.getBlock(toSectionPos(pos)) : 0;
        }
        getBlockLight(pos) {
          const section = this.blockLightSections[getLightSectionIndex2(pos)];
          if (!section)
            return 0;
          const localY = pos.y % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          return section.get(index);
        }
        /**
         * Get skylight value at specific coordinates within a chunk
         * Matching C++ implementation index calculation
         */
        getSkyLight(pos) {
          const section = this.skyLightSections[getLightSectionIndex2(pos)];
          if (!section)
            return 0;
          const localY = pos.y % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          return section.get(index);
        }
        getBiome(pos) {
          if (pos.y < 0 || pos.y >= 256)
            return 0;
          return this.biomes[getBiomeIndex(pos)];
        }
        setBlockType(pos, id) {
          this.setBlockStateId(pos, mcData2.blocks[id].minStateId);
        }
        setBlockData(pos, data) {
          this.setBlockStateId(pos, mcData2.blocksByStateId[this.getBlockStateId(pos)].minStateId + data);
        }
        setBlockStateId(pos, stateId) {
          const sectionIndex = pos.y >> 4;
          if (sectionIndex < 0 || sectionIndex >= 16)
            return;
          let section = this.sections[sectionIndex];
          if (!section) {
            if (stateId === 0) {
              return;
            }
            section = new ChunkSection({
              maxBitsPerBlock: this.maxBitsPerBlock
            });
            this.sectionMask |= 1 << sectionIndex;
            this.sections[sectionIndex] = section;
          }
          section.setBlock(toSectionPos(pos), stateId);
        }
        setBlockLight(pos, light) {
          const sectionIndex = getLightSectionIndex2(pos);
          let section = this.blockLightSections[sectionIndex];
          if (section === null) {
            if (light === 0) {
              return;
            }
            section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            this.blockLightMask |= 1 << sectionIndex;
            this.blockLightSections[sectionIndex] = section;
          }
          const localY = pos.y % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          section.set(index, light);
        }
        setSkyLight(pos, light) {
          const sectionIndex = getLightSectionIndex2(pos);
          let section = this.skyLightSections[sectionIndex];
          if (section === null) {
            if (light === 0) {
              return;
            }
            section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            this.skyLightMask |= 1 << sectionIndex;
            this.skyLightSections[sectionIndex] = section;
          }
          const localY = pos.y % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          section.set(index, light);
        }
        setBiome(pos, biome) {
          if (pos.y < 0 || pos.y >= 256)
            return;
          this.biomes[getBiomeIndex(pos)] = biome;
        }
        getMask() {
          return this.sectionMask;
        }
        dump() {
          const smartBuffer = new SmartBuffer();
          this.sections.forEach((section, i) => {
            if (section !== null && !section.isEmpty()) {
              section.write(smartBuffer);
            }
          });
          if (!smartBuffer.length) {
            return Buffer.alloc(4096);
          }
          return smartBuffer.toBuffer();
        }
        loadBiomes(biomes) {
          this.biomes = biomes;
        }
        dumpBiomes(biomes) {
          return this.biomes;
        }
        load(data, bitMap = 65535) {
          const reader = SmartBuffer.fromBuffer(data);
          this.sectionMask |= bitMap;
          for (let y = 0; y < constants.NUM_SECTIONS; ++y) {
            if (!(bitMap >> y & 1)) {
              continue;
            }
            let palette;
            const solidBlockCount = reader.readInt16BE();
            const bitsPerBlock = reader.readUInt8();
            if (bitsPerBlock <= constants.MAX_BITS_PER_BLOCK) {
              palette = [];
              const numPaletteItems = varInt.read(reader);
              for (let i = 0; i < numPaletteItems; ++i) {
                palette.push(varInt.read(reader));
              }
            } else {
              palette = null;
            }
            const dataArray = new BitArray({
              bitsPerValue: bitsPerBlock > constants.MAX_BITS_PER_BLOCK ? this.maxBitsPerBlock : bitsPerBlock,
              capacity: 4096
            }).readBuffer(reader, varInt.read(reader) * 2);
            const section = new ChunkSection({
              data: dataArray,
              palette,
              solidBlockCount,
              maxBitsPerBlock: this.maxBitsPerBlock
            });
            this.sections[y] = section;
          }
        }
        loadLight(data, skyLightMask, blockLightMask, emptySkyLightMask = 0, emptyBlockLightMask = 0) {
          const reader = SmartBuffer.fromBuffer(data);
          this.skyLightMask |= skyLightMask;
          for (let y = 0; y < constants.NUM_SECTIONS + 2; y++) {
            if (!(skyLightMask >> y & 1)) {
              continue;
            }
            const length = varInt.read(reader);
            const section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            const tempBuffer = new Uint8Array(length);
            for (let i = 0; i < length; i++) {
              tempBuffer[i] = reader.readUInt8();
            }
            for (let i = 0; i < length; i++) {
              const byte = tempBuffer[i];
              const blockIndex1 = i * 2;
              const blockIndex2 = i * 2 + 1;
              const value1 = byte & 15;
              const value2 = byte >> 4 & 15;
              const x1 = blockIndex1 % 16;
              const z1 = Math.floor(blockIndex1 / 16) % 16;
              const y1 = Math.floor(blockIndex1 / 256);
              const x2 = blockIndex2 % 16;
              const z2 = Math.floor(blockIndex2 / 16) % 16;
              const y2 = Math.floor(blockIndex2 / 256);
              const index1 = y1 * 16 * 16 + z1 * 16 + x1;
              const index2 = y2 * 16 * 16 + z2 * 16 + x2;
              section.set(index1, value1);
              if (blockIndex2 < 4096) {
                section.set(index2, value2);
              }
            }
            this.skyLightSections[y] = section;
          }
          this.blockLightMask |= blockLightMask;
          for (let y = 0; y < constants.NUM_SECTIONS + 2; y++) {
            if (!(blockLightMask >> y & 1)) {
              continue;
            }
            const length = varInt.read(reader);
            const section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            const tempBuffer = new Uint8Array(length);
            for (let i = 0; i < length; i++) {
              tempBuffer[i] = reader.readUInt8();
            }
            for (let i = 0; i < length; i++) {
              const byte = tempBuffer[i];
              const blockIndex1 = i * 2;
              const blockIndex2 = i * 2 + 1;
              const value1 = byte & 15;
              const value2 = byte >> 4 & 15;
              const x1 = blockIndex1 % 16;
              const z1 = Math.floor(blockIndex1 / 16) % 16;
              const y1 = Math.floor(blockIndex1 / 256);
              const x2 = blockIndex2 % 16;
              const z2 = Math.floor(blockIndex2 / 16) % 16;
              const y2 = Math.floor(blockIndex2 / 256);
              const index1 = y1 * 16 * 16 + z1 * 16 + x1;
              const index2 = y2 * 16 * 16 + z2 * 16 + x2;
              section.set(index1, value1);
              if (blockIndex2 < 4096) {
                section.set(index2, value2);
              }
            }
            this.blockLightSections[y] = section;
          }
        }
        dumpLight() {
          const smartBuffer = new SmartBuffer();
          this.skyLightSections.forEach((section, i) => {
            if (section !== null) {
              varInt.write(smartBuffer, 2048);
              section.writeBuffer(smartBuffer);
            }
          });
          this.blockLightSections.forEach((section, i) => {
            if (section !== null) {
              varInt.write(smartBuffer, 2048);
              section.writeBuffer(smartBuffer);
            }
          });
          return smartBuffer.toBuffer();
        }
        /**
         * New serializable light data dump method with consistent interface across versions
         * Returns an object that can be easily transferred between threads
         * @returns {Object} Object containing serialized light data
         */
        dumpLightNew() {
          return {
            skyLightSections: this.skyLightSections.map((section) => section === null ? null : section.toJson()),
            blockLightSections: this.blockLightSections.map((section) => section === null ? null : section.toJson()),
            skyLightMask: this.skyLightMask,
            blockLightMask: this.blockLightMask,
            emptySkyLightMask: 0,
            // Not used in 1.16, but included for consistency
            emptyBlockLightMask: 0
            // Not used in 1.16, but included for consistency
          };
        }
        /**
         * New light data loading method with consistent interface across versions
         * Accepts a single argument that contains all necessary light data
         * @param {Object} lightData - Object containing serialized light data
         */
        loadLightNew(lightData) {
          const { skyLightSections, blockLightSections, skyLightMask, blockLightMask } = lightData;
          this.skyLightMask = skyLightMask;
          this.skyLightSections = skyLightSections.map((section) => section === null ? null : BitArray.fromJson(section));
          this.blockLightMask = blockLightMask;
          this.blockLightSections = blockLightSections.map((section) => section === null ? null : BitArray.fromJson(section));
        }
      };
    };
    function getLightSectionIndex2(pos) {
      return Math.floor(pos.y / 16) + 1;
    }
    function getBiomeIndex(pos) {
      return (pos.y >> 2 & 63) << 4 | (pos.z >> 2 & 3) << 2 | pos.x >> 2 & 3;
    }
    function toSectionPos(pos) {
      return { x: pos.x, y: pos.y & 15, z: pos.z };
    }
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.16/chunk.js
var require_chunk6 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.16/chunk.js"(exports2, module2) {
    var constants = require_constants();
    function loader(registry) {
      const Block = require_prismarine_block()(registry);
      const Chunk = require_ChunkColumn5()(Block, registry);
      Chunk.h = constants.CHUNK_HEIGHT;
      Chunk.version = registry.version;
      return Chunk;
    }
    module2.exports = loader;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.17/ChunkColumn.js
var require_ChunkColumn6 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.17/ChunkColumn.js"(exports2, module2) {
    var SmartBuffer = require_smartbuffer().SmartBuffer;
    var BitArray = require_BitArrayNoSpan();
    var ChunkSection = require_CommonChunkSection()(BitArray);
    var CommonChunkColumn = require_CommonChunkColumn();
    var constants = require_constants();
    var varInt = require_varInt();
    var neededBits = require_neededBits();
    module2.exports = (Block, mcData2) => {
      return class ChunkColumn extends CommonChunkColumn {
        static get section() {
          return ChunkSection;
        }
        constructor(options) {
          super(mcData2);
          this.minY = options?.minY ?? 0;
          this.worldHeight = options?.worldHeight ?? constants.CHUNK_HEIGHT;
          this.numSections = this.worldHeight >> 4;
          this.maxBitsPerBlock = neededBits(Object.values(mcData2.blocks).reduce((high, block) => Math.max(high, block.maxStateId), 0));
          this.sectionMask = new BitArray({
            bitsPerValue: 1,
            capacity: this.numSections
          });
          this.sections = Array(this.numSections).fill(null);
          this.biomes = Array(4 * 4 * (this.worldHeight >> 2)).fill(0);
          this.skyLightMask = new BitArray({
            bitsPerValue: 1,
            capacity: this.numSections + 2
          });
          this.emptySkyLightMask = new BitArray({
            bitsPerValue: 1,
            capacity: this.numSections + 2
          });
          this.skyLightSections = Array(this.numSections + 2).fill(null);
          this.blockLightMask = new BitArray({
            bitsPerValue: 1,
            capacity: this.numSections + 2
          });
          this.emptyBlockLightMask = new BitArray({
            bitsPerValue: 1,
            capacity: this.numSections + 2
          });
          this.blockLightSections = Array(this.numSections + 2).fill(null);
        }
        // Json serialization/deserialization methods
        toJson() {
          return JSON.stringify({
            biomes: this.biomes,
            blockEntities: this.blockEntities,
            sectionMask: this.sectionMask.toLongArray(),
            sections: this.sections.map((section) => section === null ? null : section.toJson()),
            skyLightMask: this.skyLightMask.toLongArray(),
            blockLightMask: this.blockLightMask.toLongArray(),
            skyLightSections: this.skyLightSections.map((section) => section === null ? null : section.toJson()),
            blockLightSections: this.blockLightSections.map((section) => section === null ? null : section.toJson()),
            emptyBlockLightMask: this.emptyBlockLightMask.toLongArray(),
            emptySkyLightMask: this.emptySkyLightMask.toLongArray()
          });
        }
        static fromJson(j) {
          const parsed = JSON.parse(j);
          const chunk = new ChunkColumn();
          chunk.biomes = parsed.biomes;
          chunk.blockEntities = parsed.blockEntities;
          chunk.sectionMask = BitArray.fromLongArray(parsed.sectionMask, 1);
          chunk.sections = parsed.sections.map((s) => s === null ? null : ChunkSection.fromJson(s));
          chunk.skyLightMask = BitArray.fromLongArray(parsed.skyLightMask, 1);
          chunk.blockLightMask = BitArray.fromLongArray(parsed.blockLightMask, 1);
          chunk.skyLightSections = parsed.skyLightSections.map((s) => s === null ? null : BitArray.fromJson(s));
          chunk.blockLightSections = parsed.blockLightSections.map((s) => s === null ? null : BitArray.fromJson(s));
          chunk.emptySkyLightMask = BitArray.fromLongArray(parsed.emptyBlockLightMask, 1);
          chunk.emptyBlockLightMask = BitArray.fromLongArray(parsed.emptySkyLightMask, 1);
          return chunk;
        }
        initialize(func) {
          const p = { x: 0, y: 0, z: 0 };
          for (p.y = 0; p.y < this.worldHeight; p.y++) {
            for (p.z = 0; p.z < constants.SECTION_WIDTH; p.z++) {
              for (p.x = 0; p.x < constants.SECTION_WIDTH; p.x++) {
                const block = func(p.x, p.y, p.z);
                if (block === null) {
                  continue;
                }
                this.setBlock(p, block);
              }
            }
          }
        }
        getBlock(pos) {
          const section = this.sections[pos.y - this.minY >> 4];
          const biome = this.getBiome(pos);
          if (!section) {
            return Block.fromStateId(0, biome);
          }
          const stateId = section.getBlock(toSectionPos(pos, this.minY));
          const block = Block.fromStateId(stateId, biome);
          block.light = this.getBlockLight(pos);
          block.skyLight = this.getSkyLight(pos);
          block.entity = this.getBlockEntity(pos);
          return block;
        }
        setBlock(pos, block) {
          if (typeof block.stateId !== "undefined") {
            this.setBlockStateId(pos, block.stateId);
          }
          if (typeof block.biome !== "undefined") {
            this.setBiome(pos, block.biome.id);
          }
          if (typeof block.skyLight !== "undefined") {
            this.setSkyLight(pos, block.skyLight);
          }
          if (typeof block.light !== "undefined") {
            this.setBlockLight(pos, block.light);
          }
          if (block.entity) {
            this.setBlockEntity(pos, block.entity);
          } else {
            this.removeBlockEntity(pos);
          }
        }
        getBlockType(pos) {
          const blockStateId = this.getBlockStateId(pos);
          return mcData2.blocksByStateId[blockStateId].id;
        }
        getBlockData(pos) {
          const blockStateId = this.getBlockStateId(pos);
          return mcData2.blocksByStateId[blockStateId].metadata;
        }
        getBlockStateId(pos) {
          const section = this.sections[pos.y - this.minY >> 4];
          return section ? section.getBlock(toSectionPos(pos, this.minY)) : 0;
        }
        getBlockLight(pos) {
          const section = this.blockLightSections[getLightSectionIndex2(pos, this.minY)];
          if (!section)
            return 0;
          const localY = (pos.y - this.minY) % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          return section.get(index);
        }
        /**
         * Get skylight value at specific coordinates within a chunk
         * Matching C++ implementation index calculation
         */
        getSkyLight(pos) {
          const section = this.skyLightSections[getLightSectionIndex2(pos, this.minY)];
          if (!section)
            return 0;
          const localY = (pos.y - this.minY) % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          return section.get(index);
        }
        getBiome(pos) {
          if (pos.y < this.minY || pos.y >= this.minY + this.worldHeight)
            return 0;
          return this.biomes[getBiomeIndex(pos, this.minY)];
        }
        setBlockType(pos, id) {
          this.setBlockStateId(pos, mcData2.blocks[id].minStateId);
        }
        setBlockData(pos, data) {
          this.setBlockStateId(pos, mcData2.blocksByStateId[this.getBlockStateId(pos)].minStateId + data);
        }
        setBlockStateId(pos, stateId) {
          const sectionIndex = pos.y - this.minY >> 4;
          if (sectionIndex < 0 || sectionIndex >= this.numSections)
            return;
          let section = this.sections[sectionIndex];
          if (!section) {
            if (stateId === 0) {
              return;
            }
            section = new ChunkSection({ maxBitsPerBlock: this.maxBitsPerBlock });
            if (sectionIndex > this.sectionMask.capacity) {
              this.sectionMask = this.sectionMask.resize(sectionIndex);
            }
            this.sectionMask.set(sectionIndex, 1);
            this.sections[sectionIndex] = section;
          }
          section.setBlock(toSectionPos(pos, this.minY), stateId);
        }
        setBlockLight(pos, light) {
          const sectionIndex = getLightSectionIndex2(pos, this.minY);
          let section = this.blockLightSections[sectionIndex];
          if (section === null) {
            if (light === 0) {
              return;
            }
            section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            if (sectionIndex > this.blockLightMask.capacity) {
              this.blockLightMask = this.blockLightMask.resize(sectionIndex);
            }
            this.blockLightMask.set(sectionIndex, 1);
            this.blockLightSections[sectionIndex] = section;
          }
          section.set(getSectionBlockIndex(pos, this.minY), light);
        }
        setSkyLight(pos, light) {
          const sectionIndex = getLightSectionIndex2(pos, this.minY);
          let section = this.skyLightSections[sectionIndex];
          if (section === null) {
            if (light === 0) {
              return;
            }
            section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            this.skyLightMask.set(sectionIndex, 1);
            this.skyLightSections[sectionIndex] = section;
          }
          section.set(getSectionBlockIndex(pos, this.minY), light);
        }
        setBiome(pos, biome) {
          if (pos.y < this.minY || pos.y >= this.minY + this.worldHeight)
            return;
          this.biomes[getBiomeIndex(pos, this.minY)] = biome;
        }
        getMask() {
          return this.sectionMask.toLongArray();
        }
        dump() {
          const smartBuffer = new SmartBuffer();
          this.sections.forEach((section) => {
            if (section !== null && !section.isEmpty()) {
              section.write(smartBuffer);
            }
          });
          if (!smartBuffer.length) {
            return Buffer.alloc(4096);
          }
          return smartBuffer.toBuffer();
        }
        loadBiomes(biomes) {
          this.biomes = biomes;
        }
        dumpBiomes(biomes) {
          return this.biomes;
        }
        load(data, bitMap = [[0, 65535]]) {
          const reader = SmartBuffer.fromBuffer(data);
          bitMap = BitArray.fromLongArray(bitMap, 1);
          this.sectionMask = BitArray.or(this.sectionMask, bitMap);
          for (let y = 0; y < this.numSections; ++y) {
            if (!bitMap.get(y)) {
              continue;
            }
            let palette;
            const solidBlockCount = reader.readInt16BE();
            const bitsPerBlock = reader.readUInt8();
            if (bitsPerBlock <= constants.MAX_BITS_PER_BLOCK) {
              palette = [];
              const numPaletteItems = varInt.read(reader);
              for (let i = 0; i < numPaletteItems; ++i) {
                palette.push(varInt.read(reader));
              }
            } else {
              palette = null;
            }
            const dataArray = new BitArray({
              bitsPerValue: bitsPerBlock > constants.MAX_BITS_PER_BLOCK ? this.maxBitsPerBlock : bitsPerBlock,
              capacity: 4096
            }).readBuffer(reader, varInt.read(reader) * 2);
            this.sections[y] = new ChunkSection({
              data: dataArray,
              palette,
              solidBlockCount,
              maxBitsPerBlock: this.maxBitsPerBlock
            });
          }
        }
        loadParsedLight(skyLight, blockLight, skyLightMask, blockLightMask, emptySkyLightMask, emptyBlockLightMask) {
          function readSection(sections, data, lightMask, pLightMask, emptyMask, pEmptyMask) {
            let currentSectionIndex = 0;
            const incomingLightMask = BitArray.fromLongArray(pLightMask, 1);
            const incomingEmptyMask = BitArray.fromLongArray(pEmptyMask, 1);
            for (let y = 0; y < sections.length; y++) {
              const isEmpty = incomingEmptyMask.get(y);
              if (!incomingLightMask.get(y) && !isEmpty) {
                continue;
              }
              emptyMask.set(y, isEmpty);
              lightMask.set(y, 1 - isEmpty);
              const bitArray = new BitArray({
                bitsPerValue: 4,
                capacity: 4096
              });
              sections[y] = bitArray;
              if (!isEmpty) {
                const sectionData = data[currentSectionIndex++];
                for (let i = 0; i < sectionData.length; i++) {
                  const byte = sectionData[i];
                  const blockIndex1 = i * 2;
                  const blockIndex2 = i * 2 + 1;
                  const value1 = byte & 15;
                  const value2 = byte >> 4 & 15;
                  const x1 = blockIndex1 % 16;
                  const z1 = Math.floor(blockIndex1 / 16) % 16;
                  const y1 = Math.floor(blockIndex1 / 256);
                  const x2 = blockIndex2 % 16;
                  const z2 = Math.floor(blockIndex2 / 16) % 16;
                  const y2 = Math.floor(blockIndex2 / 256);
                  const index1 = y1 * 16 * 16 + z1 * 16 + x1;
                  const index2 = y2 * 16 * 16 + z2 * 16 + x2;
                  bitArray.set(index1, value1);
                  if (blockIndex2 < 4096) {
                    bitArray.set(index2, value2);
                  }
                }
              }
            }
          }
          readSection(this.skyLightSections, skyLight, this.skyLightMask, skyLightMask, this.emptySkyLightMask, emptySkyLightMask);
          readSection(this.blockLightSections, blockLight, this.blockLightMask, blockLightMask, this.emptyBlockLightMask, emptyBlockLightMask);
        }
        dumpLight() {
          const skyLight = [];
          const blockLight = [];
          this.skyLightSections.forEach((section, index) => {
            if (section !== null && this.skyLightMask.get(index)) {
              const smartBuffer = new SmartBuffer();
              section.writeBuffer(smartBuffer);
              skyLight.push(Uint8Array.from(smartBuffer.toBuffer()));
            }
          });
          this.blockLightSections.forEach((section, index) => {
            if (section !== null && this.blockLightMask.get(index)) {
              const smartBuffer = new SmartBuffer();
              section.writeBuffer(smartBuffer);
              blockLight.push(Uint8Array.from(smartBuffer.toBuffer()));
            }
          });
          return {
            skyLight,
            blockLight,
            skyLightMask: this.skyLightMask.toLongArray(),
            blockLightMask: this.blockLightMask.toLongArray(),
            emptySkyLightMask: this.emptySkyLightMask.toLongArray(),
            emptyBlockLightMask: this.emptyBlockLightMask.toLongArray()
          };
        }
        /**
         * New serializable light data dump method with consistent interface across versions
         * Returns an object that can be easily transferred between threads
         * @returns {Object} Object containing serialized light data
         */
        dumpLightNew() {
          return {
            skyLightMask: this.skyLightMask.toLongArray(),
            emptySkyLightMask: this.emptySkyLightMask.toLongArray(),
            skyLightSections: this.skyLightSections.map((section) => section === null ? null : section.toJson()),
            blockLightMask: this.blockLightMask.toLongArray(),
            emptyBlockLightMask: this.emptyBlockLightMask.toLongArray(),
            blockLightSections: this.blockLightSections.map((section) => section === null ? null : section.toJson())
          };
        }
        /**
         * New light data loading method with consistent interface across versions
         * Accepts a single argument that contains all necessary light data
         * @param {Object} lightData - Object containing serialized light data
         */
        loadLightNew(lightData) {
          const { skyLightSections, blockLightSections, skyLightMask, blockLightMask, emptySkyLightMask, emptyBlockLightMask } = lightData;
          this.skyLightMask = BitArray.fromLongArray(skyLightMask, 1);
          this.emptySkyLightMask = BitArray.fromLongArray(emptySkyLightMask, 1);
          this.skyLightSections = skyLightSections.map((section) => section === null ? null : BitArray.fromJson(section));
          this.blockLightMask = BitArray.fromLongArray(blockLightMask, 1);
          this.emptyBlockLightMask = BitArray.fromLongArray(emptyBlockLightMask, 1);
          this.blockLightSections = blockLightSections.map((section) => section === null ? null : BitArray.fromJson(section));
        }
      };
    };
    function getLightSectionIndex2(pos, minY) {
      return Math.floor((pos.y - minY) / 16) + 1;
    }
    function getBiomeIndex(pos, minY) {
      return (pos.y - minY >> 2 & 63) << 4 | (pos.z >> 2 & 3) << 2 | pos.x >> 2 & 3;
    }
    function toSectionPos(pos, minY) {
      return { x: pos.x, y: pos.y - minY & 15, z: pos.z };
    }
    function getSectionBlockIndex(pos, minY) {
      return (pos.y - minY & 15) << 8 | pos.z << 4 | pos.x;
    }
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.17/chunk.js
var require_chunk7 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.17/chunk.js"(exports2, module2) {
    function loader(registry) {
      const Block = require_prismarine_block()(registry);
      return require_ChunkColumn6()(Block, registry);
    }
    module2.exports = loader;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/PaletteContainer.js
var require_PaletteContainer = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/PaletteContainer.js"(exports2, module2) {
    var BitArray = require_BitArrayNoSpan();
    var constants = require_constants();
    var neededBits = require_neededBits();
    var varInt = require_varInt();
    var DirectPaletteContainer = class _DirectPaletteContainer {
      constructor(options) {
        this.noSizePrefix = options?.noSizePrefix;
        this.data = new BitArray({
          bitsPerValue: options?.bitsPerValue ?? constants.GLOBAL_BITS_PER_BLOCK,
          capacity: options?.capacity ?? constants.BLOCK_SECTION_VOLUME
        });
      }
      get(index) {
        return this.data.get(index);
      }
      set(index, value) {
        this.data.set(index, value);
        return this;
      }
      write(smartBuffer) {
        smartBuffer.writeUInt8(this.data.bitsPerValue);
        if (!this.noSizePrefix)
          varInt.write(smartBuffer, this.data.length());
        this.data.writeBuffer(smartBuffer);
      }
      readBuffer(smartBuffer, bitsPerValue) {
        const longs = this.noSizePrefix ? Math.ceil(this.data.capacity / Math.floor(64 / bitsPerValue)) : varInt.read(smartBuffer);
        this.data.readBuffer(smartBuffer, longs * 2);
        return this;
      }
      toJson() {
        return JSON.stringify({
          type: "direct",
          data: this.data.toJson()
        });
      }
      static fromJson(j) {
        const parsed = JSON.parse(j);
        return new _DirectPaletteContainer({
          noSizePrefix: parsed.noSizePrefix,
          data: BitArray.fromJson(parsed.data),
          bitsPerValue: parsed.bitsPerValue
        });
      }
    };
    var IndirectPaletteContainer = class _IndirectPaletteContainer {
      constructor(options) {
        this.noSizePrefix = options?.noSizePrefix;
        this.data = options?.data ?? new BitArray({
          bitsPerValue: options?.bitsPerValue ?? constants.MIN_BITS_PER_BLOCK,
          capacity: options?.capacity ?? constants.BLOCK_SECTION_VOLUME
        });
        this.palette = options?.palette ?? [0];
        this.maxBits = options?.maxBits ?? constants.MAX_BITS_PER_BLOCK;
        this.maxBitsPerBlock = options?.maxBitsPerBlock ?? constants.MAX_BITS_PER_BLOCK;
      }
      get(index) {
        return this.palette[this.data.get(index)];
      }
      set(index, value) {
        let paletteIndex = this.palette.indexOf(value);
        if (paletteIndex < 0) {
          paletteIndex = this.palette.length;
          this.palette.push(value);
          const bitsPerValue = neededBits(paletteIndex);
          if (bitsPerValue > this.data.bitsPerValue) {
            if (bitsPerValue <= this.maxBits) {
              this.data = this.data.resizeTo(bitsPerValue);
            } else {
              return this.convertToDirect(this.maxBitsPerBlock).set(index, value);
            }
          }
        }
        this.data.set(index, paletteIndex);
        return this;
      }
      convertToDirect(bitsPerValue) {
        const direct = new DirectPaletteContainer({
          noSizePrefix: this.noSizePrefix,
          bitsPerValue: bitsPerValue ?? constants.GLOBAL_BITS_PER_BLOCK,
          capacity: this.data.capacity
        });
        for (let i = 0; i < this.data.capacity; ++i) {
          direct.data.set(i, this.get(i));
        }
        return direct;
      }
      write(smartBuffer) {
        smartBuffer.writeUInt8(this.data.bitsPerValue);
        varInt.write(smartBuffer, this.palette.length);
        for (const paletteElement of this.palette) {
          varInt.write(smartBuffer, paletteElement);
        }
        if (!this.noSizePrefix)
          varInt.write(smartBuffer, this.data.length());
        this.data.writeBuffer(smartBuffer);
      }
      readBuffer(smartBuffer, bitsPerValue) {
        const longs = this.noSizePrefix ? Math.ceil(this.data.capacity / Math.floor(64 / bitsPerValue)) : varInt.read(smartBuffer);
        this.data.readBuffer(smartBuffer, longs * 2);
        return this;
      }
      toJson() {
        return JSON.stringify({
          type: "indirect",
          palette: this.palette,
          maxBits: this.maxBits,
          maxBitsPerBlock: this.maxBitsPerBlock,
          data: this.data.toJson()
        });
      }
      static fromJson(j) {
        const parsed = JSON.parse(j);
        return new _IndirectPaletteContainer({
          noSizePrefix: parsed.noSizePrefix,
          palette: parsed.palette,
          maxBits: parsed.maxBits,
          maxBitsPerBlock: parsed.maxBitsPerBlock,
          data: BitArray.fromJson(parsed.data)
        });
      }
    };
    var SingleValueContainer = class _SingleValueContainer {
      constructor(options) {
        this.noSizePrefix = options?.noSizePrefix;
        this.value = options?.value ?? 0;
        this.bitsPerValue = options?.bitsPerValue ?? constants.MIN_BITS_PER_BLOCK;
        this.capacity = options?.capacity ?? constants.BLOCK_SECTION_VOLUME;
        this.maxBits = options?.maxBits ?? constants.MAX_BITS_PER_BLOCK;
        this.maxBitsPerBlock = options?.maxBitsPerBlock ?? constants.MAX_BITS_PER_BLOCK;
      }
      get(index) {
        return this.value;
      }
      set(index, value) {
        if (value === this.value) {
          return this;
        }
        const data = new BitArray({
          bitsPerValue: this.bitsPerValue,
          capacity: this.capacity
        });
        data.set(index, 1);
        return new IndirectPaletteContainer({
          noSizePrefix: this.noSizePrefix,
          data,
          palette: [this.value, value],
          capacity: this.capacity,
          bitsPerValue: this.bitsPerValue,
          maxBits: this.maxBits,
          maxBitsPerBlock: this.maxBitsPerBlock
        });
      }
      write(smartBuffer) {
        smartBuffer.writeUInt8(0);
        varInt.write(smartBuffer, this.value);
        if (!this.noSizePrefix)
          smartBuffer.writeUInt8(0);
      }
      toJson() {
        return JSON.stringify({
          type: "single",
          value: this.value,
          bitsPerValue: this.bitsPerValue,
          capacity: this.capacity,
          maxBits: this.maxBits,
          maxBitsPerBlock: this.maxBitsPerBlock
        });
      }
      static fromJson(j) {
        const parsed = JSON.parse(j);
        return new _SingleValueContainer({
          noSizePrefix: parsed.noSizePrefix,
          value: parsed.value,
          bitsPerValue: parsed.bitsPerValue,
          capacity: parsed.capacity,
          maxBits: parsed.maxBits,
          maxBitsPerBlock: parsed.maxBitsPerBlock
        });
      }
    };
    function containerFromJson(j) {
      const parsed = JSON.parse(j);
      if (parsed.type === "direct") {
        return new DirectPaletteContainer({
          noSizePrefix: parsed.noSizePrefix,
          data: BitArray.fromJson(parsed.data)
        });
      } else if (parsed.type === "indirect") {
        return new IndirectPaletteContainer({
          noSizePrefix: parsed.noSizePrefix,
          palette: parsed.palette,
          maxBits: parsed.maxBits,
          data: BitArray.fromJson(parsed.data),
          maxBitsPerBlock: parsed.maxBitsPerBlock
        });
      } else if (parsed.type === "single") {
        return new SingleValueContainer({
          noSizePrefix: parsed.noSizePrefix,
          value: parsed.value,
          bitsPerValue: parsed.bitsPerValue,
          capacity: parsed.capacity,
          maxBits: parsed.maxBits,
          maxBitsPerBlock: parsed.maxBitsPerBlock
        });
      }
      return void 0;
    }
    module2.exports = {
      SingleValueContainer,
      IndirectPaletteContainer,
      DirectPaletteContainer,
      fromJson: containerFromJson
    };
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/PaletteChunkSection.js
var require_PaletteChunkSection = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/PaletteChunkSection.js"(exports2, module2) {
    var constants = require_constants();
    var paletteContainer = require_PaletteContainer();
    var varInt = require_varInt();
    var SingleValueContainer = paletteContainer.SingleValueContainer;
    var IndirectPaletteContainer = paletteContainer.IndirectPaletteContainer;
    var DirectPaletteContainer = paletteContainer.DirectPaletteContainer;
    function getBlockIndex(pos) {
      return pos.y << 8 | pos.z << 4 | pos.x;
    }
    var ChunkSection = class _ChunkSection {
      constructor(options) {
        this.noSizePrefix = options?.noSizePrefix;
        this.data = options?.data;
        if (!this.data) {
          const value = options?.singleValue ?? 0;
          this.data = new SingleValueContainer({
            value,
            bitsPerValue: constants.MIN_BITS_PER_BLOCK,
            capacity: constants.BLOCK_SECTION_VOLUME,
            maxBits: constants.MAX_BITS_PER_BLOCK,
            maxBitsPerBlock: options?.maxBitsPerBlock ?? constants.GLOBAL_BITS_PER_BLOCK,
            noSizePrefix: this.noSizePrefix
          });
          this.solidBlockCount = value ? constants.BLOCK_SECTION_VOLUME : 0;
        } else {
          this.solidBlockCount = options?.solidBlockCount ?? 0;
          if (options?.solidBlockCount == null) {
            for (let i = 0; i < constants.BLOCK_SECTION_VOLUME; ++i) {
              if (this.data.get(i)) {
                this.solidBlockCount++;
              }
            }
          }
        }
        this.palette = this.data.palette;
      }
      toJson() {
        return JSON.stringify({
          data: this.data.toJson(),
          solidBlockCount: this.solidBlockCount
        });
      }
      static fromJson(j) {
        const parsed = JSON.parse(j);
        return new _ChunkSection({
          data: paletteContainer.fromJson(parsed.data),
          solidBlockCount: parsed.solidBlockCount
        });
      }
      get(pos) {
        return this.data.get(getBlockIndex(pos));
      }
      set(pos, stateId) {
        const blockIndex = getBlockIndex(pos);
        const oldBlock = this.get(pos);
        if (stateId === 0 && oldBlock !== 0) {
          this.solidBlockCount -= 1;
        } else if (stateId !== 0 && oldBlock === 0) {
          this.solidBlockCount += 1;
        }
        this.data = this.data.set(blockIndex, stateId);
        this.palette = this.data.palette;
      }
      isEmpty() {
        return this.solidBlockCount === 0;
      }
      write(smartBuffer) {
        smartBuffer.writeInt16BE(this.solidBlockCount);
        this.data.write(smartBuffer);
      }
      static fromLocalPalette({ data, palette, noSizePrefix }) {
        return new _ChunkSection({
          noSizePrefix,
          data: palette.length === 1 ? new SingleValueContainer({
            noSizePrefix,
            value: palette[0],
            bitsPerValue: constants.MIN_BITS_PER_BLOCK,
            capacity: constants.BIOME_SECTION_VOLUME,
            maxBits: constants.MAX_BITS_PER_BLOCK
          }) : new IndirectPaletteContainer({
            noSizePrefix,
            data,
            palette
          })
        });
      }
      static read(smartBuffer, maxBitsPerBlock = constants.GLOBAL_BITS_PER_BLOCK, noSizePrefix) {
        const solidBlockCount = smartBuffer.readInt16BE();
        const bitsPerBlock = smartBuffer.readUInt8();
        if (bitsPerBlock > 16)
          throw new Error(`Bits per block is too big: ${bitsPerBlock}`);
        if (bitsPerBlock === 0) {
          const section = new _ChunkSection({
            noSizePrefix,
            solidBlockCount,
            singleValue: varInt.read(smartBuffer),
            maxBitsPerBlock
          });
          if (!noSizePrefix)
            smartBuffer.readUInt8();
          return section;
        }
        if (bitsPerBlock > constants.MAX_BITS_PER_BLOCK) {
          return new _ChunkSection({
            noSizePrefix,
            solidBlockCount,
            data: new DirectPaletteContainer({
              noSizePrefix,
              bitsPerValue: maxBitsPerBlock,
              capacity: constants.BLOCK_SECTION_VOLUME
            }).readBuffer(smartBuffer, bitsPerBlock)
          });
        }
        const palette = [];
        const paletteLength = varInt.read(smartBuffer);
        for (let i = 0; i < paletteLength; ++i) {
          palette.push(varInt.read(smartBuffer));
        }
        return new _ChunkSection({
          noSizePrefix,
          solidBlockCount,
          data: new IndirectPaletteContainer({
            noSizePrefix,
            bitsPerValue: bitsPerBlock,
            capacity: constants.BLOCK_SECTION_VOLUME,
            maxBits: constants.MAX_BITS_PER_BLOCK,
            maxBitsPerBlock,
            palette
          }).readBuffer(smartBuffer, bitsPerBlock)
        });
      }
    };
    module2.exports = ChunkSection;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/PaletteBiome.js
var require_PaletteBiome = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/common/PaletteBiome.js"(exports2, module2) {
    var constants = require_constants();
    var paletteContainer = require_PaletteContainer();
    var varInt = require_varInt();
    var SingleValueContainer = paletteContainer.SingleValueContainer;
    var IndirectPaletteContainer = paletteContainer.IndirectPaletteContainer;
    var DirectPaletteContainer = paletteContainer.DirectPaletteContainer;
    function getBiomeIndex(pos) {
      return pos.y << 4 | pos.z << 2 | pos.x;
    }
    var BiomeSection = class _BiomeSection {
      constructor(options) {
        this.noSizePrefix = options?.noSizePrefix;
        this.data = options?.data ?? new SingleValueContainer({
          noSizePrefix: this.noSizePrefix,
          value: options?.singleValue ?? 0,
          bitsPerValue: constants.MIN_BITS_PER_BIOME,
          capacity: constants.BIOME_SECTION_VOLUME,
          maxBits: constants.MAX_BITS_PER_BIOME
        });
      }
      toJson() {
        return this.data.toJson();
      }
      static fromJson(j) {
        return new _BiomeSection({
          data: paletteContainer.fromJson(j)
        });
      }
      get(pos) {
        return this.data.get(getBiomeIndex(pos));
      }
      set(pos, biomeId) {
        this.data = this.data.set(getBiomeIndex(pos), biomeId);
      }
      static fromLocalPalette({ palette, data, noSizePrefix }) {
        return new _BiomeSection({
          noSizePrefix,
          data: palette.length === 1 ? new SingleValueContainer({
            noSizePrefix,
            value: palette[0],
            bitsPerValue: constants.MIN_BITS_PER_BIOME,
            capacity: constants.BIOME_SECTION_VOLUME,
            maxBits: constants.MAX_BITS_PER_BIOME
          }) : new IndirectPaletteContainer({
            noSizePrefix,
            palette,
            data
          })
        });
      }
      write(smartBuffer) {
        this.data.write(smartBuffer);
      }
      static read(smartBuffer, maxBitsPerBiome = constants.GLOBAL_BITS_PER_BIOME, noSizePrefix) {
        const bitsPerBiome = smartBuffer.readUInt8();
        if (bitsPerBiome > 8)
          throw new Error(`Bits per biome is too big: ${bitsPerBiome}`);
        if (bitsPerBiome === 0) {
          const section = new _BiomeSection({
            noSizePrefix,
            singleValue: varInt.read(smartBuffer)
          });
          if (!noSizePrefix)
            smartBuffer.readUInt8();
          return section;
        }
        if (bitsPerBiome > constants.MAX_BITS_PER_BIOME) {
          return new _BiomeSection({
            noSizePrefix,
            data: new DirectPaletteContainer({
              noSizePrefix,
              bitsPerValue: maxBitsPerBiome,
              capacity: constants.BIOME_SECTION_VOLUME
            }).readBuffer(smartBuffer, bitsPerBiome)
          });
        }
        const palette = [];
        const paletteLength = varInt.read(smartBuffer);
        for (let i = 0; i < paletteLength; ++i) {
          palette.push(varInt.read(smartBuffer));
        }
        return new _BiomeSection({
          data: new IndirectPaletteContainer({
            noSizePrefix,
            bitsPerValue: bitsPerBiome,
            capacity: constants.BIOME_SECTION_VOLUME,
            maxBits: constants.MAX_BITS_PER_BIOME,
            palette
          }).readBuffer(smartBuffer, bitsPerBiome)
        });
      }
    };
    module2.exports = BiomeSection;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.18/ChunkColumn.js
var require_ChunkColumn7 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.18/ChunkColumn.js"(exports2, module2) {
    var SmartBuffer = require_smartbuffer().SmartBuffer;
    var BitArray = require_BitArrayNoSpan();
    var ChunkSection = require_PaletteChunkSection();
    var BiomeSection = require_PaletteBiome();
    var CommonChunkColumn = require_CommonChunkColumn();
    var constants = require_constants();
    var neededBits = require_neededBits();
    var CAVES_UPDATE_MIN_Y = -64;
    var CAVES_UPDATE_WORLD_HEIGHT = 384;
    module2.exports = (Block, mcData2) => {
      const noSizePrefix = mcData2.version[">="]("1.21.5");
      return class ChunkColumn extends CommonChunkColumn {
        static get section() {
          return ChunkSection;
        }
        constructor(options) {
          super(mcData2);
          this.minY = options?.minY ?? CAVES_UPDATE_MIN_Y;
          this.worldHeight = options?.worldHeight ?? CAVES_UPDATE_WORLD_HEIGHT;
          this.numSections = this.worldHeight >> 4;
          this.maxBitsPerBlock = neededBits(Object.values(mcData2.blocks).reduce((high, block) => Math.max(high, block.maxStateId), 0));
          this.maxBitsPerBiome = neededBits(Object.values(mcData2.biomes).length);
          this.sections = options?.sections ?? Array.from(
            { length: this.numSections },
            (_) => new ChunkSection({ noSizePrefix, maxBitsPerBlock: this.maxBitsPerBlock })
          );
          this.biomes = options?.biomes ?? Array.from(
            { length: this.numSections },
            (_) => new BiomeSection({ noSizePrefix })
          );
          this.skyLightMask = options?.skyLightMask ?? new BitArray({
            bitsPerValue: 1,
            capacity: this.numSections + 2
          });
          this.emptySkyLightMask = options?.emptySkyLightMask ?? new BitArray({
            bitsPerValue: 1,
            capacity: this.numSections + 2
          });
          this.skyLightSections = options?.skyLightSections ?? Array(
            this.numSections + 2
          ).fill(null);
          this.blockLightMask = options?.blockLightMask ?? new BitArray({
            bitsPerValue: 1,
            capacity: this.numSections + 2
          });
          this.emptyBlockLightMask = options?.emptyBlockLightMask ?? new BitArray({
            bitsPerValue: 1,
            capacity: this.numSections + 2
          });
          this.blockLightSections = options?.blockLightSections ?? Array(
            this.numSections + 2
          ).fill(null);
          this.blockEntities = options?.blockEntities ?? {};
        }
        toJson() {
          return JSON.stringify({
            worldHeight: this.worldHeight,
            minY: this.minY,
            sections: this.sections.map((section) => section.toJson()),
            biomes: this.biomes.map((biome) => biome.toJson()),
            skyLightMask: this.skyLightMask.toLongArray(),
            emptySkyLightMask: this.emptySkyLightMask.toLongArray(),
            skyLightSections: this.skyLightSections.map((section) => section === null ? null : section.toJson()),
            blockLightMask: this.blockLightMask.toLongArray(),
            emptyBlockLightMask: this.emptyBlockLightMask.toLongArray(),
            blockLightSections: this.blockLightSections.map((section) => section === null ? null : section.toJson()),
            blockEntities: this.blockEntities
          });
        }
        static fromJson(j) {
          const parsed = JSON.parse(j);
          return new ChunkColumn({
            worldHeight: parsed.worldHeight,
            minY: parsed.minY,
            sections: parsed.sections.map((s) => ChunkSection.fromJson(s)),
            biomes: parsed.biomes.map((s) => BiomeSection.fromJson(s)),
            blockEntities: parsed.blockEntities,
            skyLightMask: BitArray.fromLongArray(parsed.skyLightMask, 1),
            emptySkyLightMask: BitArray.fromLongArray(parsed.emptyBlockLightMask, 1),
            skyLightSections: parsed.skyLightSections.map((s) => s === null ? null : BitArray.fromJson(s)),
            blockLightMask: BitArray.fromLongArray(parsed.blockLightMask, 1),
            emptyBlockLightMask: BitArray.fromLongArray(parsed.emptySkyLightMask, 1),
            blockLightSections: parsed.blockLightSections.map((s) => s === null ? null : BitArray.fromJson(s))
          });
        }
        initialize(func) {
          const p = { x: 0, y: 0, z: 0 };
          const maxY = this.worldHeight + this.minY;
          for (p.y = this.minY; p.y < maxY; p.y++) {
            for (p.z = 0; p.z < constants.SECTION_WIDTH; p.z++) {
              for (p.x = 0; p.x < constants.SECTION_WIDTH; p.x++) {
                const block = func(p.x, p.y, p.z);
                if (block !== null) {
                  this.setBlock(p, block);
                }
              }
            }
          }
        }
        getBlock(pos) {
          const stateId = this.getBlockStateId(pos);
          const biome = this.getBiome(pos);
          const block = Block.fromStateId(stateId, biome);
          block.light = this.getBlockLight(pos);
          block.skyLight = this.getSkyLight(pos);
          block.entity = this.getBlockEntity(pos);
          return block;
        }
        setBlock(pos, block) {
          if (typeof block.stateId !== "undefined") {
            this.setBlockStateId(pos, block.stateId);
          }
          if (typeof block.biome !== "undefined") {
            this.setBiome(pos, block.biome.id);
          }
          if (typeof block.skyLight !== "undefined") {
            this.setSkyLight(pos, block.skyLight);
          }
          if (typeof block.light !== "undefined") {
            this.setBlockLight(pos, block.light);
          }
          if (block.entity) {
            this.setBlockEntity(pos, block.entity);
          } else {
            this.removeBlockEntity(pos);
          }
        }
        getBlockType(pos) {
          const blockStateId = this.getBlockStateId(pos);
          return mcData2.blocksByStateId[blockStateId].id;
        }
        getBlockData(pos) {
          const blockStateId = this.getBlockStateId(pos);
          return mcData2.blocksByStateId[blockStateId].metadata;
        }
        getBlockStateId(pos) {
          const section = this.sections[pos.y - this.minY >> 4];
          return section ? section.get(toSectionPos(pos, this.minY)) : 0;
        }
        getBlockLight(pos) {
          const section = this.blockLightSections[getLightSectionIndex2(pos, this.minY)];
          if (!section)
            return 0;
          const localY = (pos.y - this.minY) % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          return section.get(index);
        }
        /**
         * Get skylight value at specific coordinates within a chunk
         * Matching C++ implementation index calculation
         */
        getSkyLight(pos) {
          const section = this.skyLightSections[getLightSectionIndex2(pos, this.minY)];
          if (!section)
            return 0;
          const localY = (pos.y - this.minY) % 16;
          const index = localY * 16 * 16 + pos.z * 16 + pos.x;
          return section.get(index);
        }
        getBiome(pos) {
          const biome = this.biomes[pos.y - this.minY >> 4];
          return biome ? biome.get(toBiomePos(pos, this.minY)) : 0;
        }
        setBlockType(pos, id) {
          this.setBlockStateId(pos, mcData2.blocks[id].minStateId);
        }
        setBlockData(pos, data) {
          this.setBlockStateId(pos, mcData2.blocksByStateId[this.getBlockStateId(pos)].minStateId + data);
        }
        setBlockStateId(pos, stateId) {
          const section = this.sections[pos.y - this.minY >> 4];
          if (section) {
            section.set(toSectionPos(pos, this.minY), stateId);
          }
        }
        setBlockLight(pos, light) {
          const sectionIndex = getLightSectionIndex2(pos, this.minY);
          let section = this.blockLightSections[sectionIndex];
          if (section === null) {
            if (light === 0) {
              return;
            }
            section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            if (sectionIndex > this.blockLightMask.capacity) {
              this.blockLightMask = this.blockLightMask.resize(sectionIndex);
            }
            this.blockLightMask.set(sectionIndex, 1);
            this.blockLightSections[sectionIndex] = section;
          }
          section.set(getSectionBlockIndex(pos, this.minY), light);
        }
        setSkyLight(pos, light) {
          const sectionIndex = getLightSectionIndex2(pos, this.minY);
          let section = this.skyLightSections[sectionIndex];
          if (section === null) {
            if (light === 0) {
              return;
            }
            section = new BitArray({
              bitsPerValue: 4,
              capacity: 4096
            });
            this.skyLightMask.set(sectionIndex, 1);
            this.skyLightSections[sectionIndex] = section;
          }
          section.set(getSectionBlockIndex(pos, this.minY), light);
        }
        setBiome(pos, biomeId) {
          const biome = this.biomes[pos.y - this.minY >> 4];
          if (biome) {
            biome.set(toBiomePos(pos, this.minY), biomeId);
          }
        }
        getMask() {
          return void 0;
        }
        dump() {
          const smartBuffer = new SmartBuffer();
          for (let i = 0; i < this.numSections; ++i) {
            this.sections[i].write(smartBuffer);
            this.biomes[i].write(smartBuffer);
          }
          if (!smartBuffer.length) {
            return Buffer.alloc(4096);
          }
          return smartBuffer.toBuffer();
        }
        loadBiomes(biomes) {
        }
        dumpBiomes(biomes) {
          return void 0;
        }
        load(data) {
          const reader = SmartBuffer.fromBuffer(data);
          for (let i = 0; i < this.numSections; ++i) {
            this.sections[i] = ChunkSection.read(reader, this.maxBitsPerBlock, noSizePrefix);
            this.biomes[i] = BiomeSection.read(reader, this.maxBitsPerBiome, noSizePrefix);
          }
        }
        loadParsedLight(skyLight, blockLight, skyLightMask, blockLightMask, emptySkyLightMask, emptyBlockLightMask) {
          function readSection(sections, data, lightMask, pLightMask, emptyMask, pEmptyMask) {
            let currentSectionIndex = 0;
            const incomingLightMask = BitArray.fromLongArray(pLightMask, 1);
            const incomingEmptyMask = BitArray.fromLongArray(pEmptyMask, 1);
            for (let y = 0; y < sections.length; y++) {
              const isEmpty = incomingEmptyMask.get(y);
              if (!incomingLightMask.get(y) && !isEmpty) {
                continue;
              }
              emptyMask.set(y, isEmpty);
              lightMask.set(y, 1 - isEmpty);
              const bitArray = new BitArray({
                bitsPerValue: 4,
                capacity: 4096
              });
              sections[y] = bitArray;
              if (!isEmpty) {
                const sectionData = data[currentSectionIndex++];
                for (let i = 0; i < sectionData.length; i++) {
                  const byte = sectionData[i];
                  const blockIndex1 = i * 2;
                  const blockIndex2 = i * 2 + 1;
                  const value1 = byte & 15;
                  const value2 = byte >> 4 & 15;
                  const x1 = blockIndex1 % 16;
                  const z1 = Math.floor(blockIndex1 / 16) % 16;
                  const y1 = Math.floor(blockIndex1 / 256);
                  const x2 = blockIndex2 % 16;
                  const z2 = Math.floor(blockIndex2 / 16) % 16;
                  const y2 = Math.floor(blockIndex2 / 256);
                  const index1 = y1 * 16 * 16 + z1 * 16 + x1;
                  const index2 = y2 * 16 * 16 + z2 * 16 + x2;
                  bitArray.set(index1, value1);
                  if (blockIndex2 < 4096) {
                    bitArray.set(index2, value2);
                  }
                }
              }
            }
          }
          readSection(this.skyLightSections, skyLight, this.skyLightMask, skyLightMask, this.emptySkyLightMask, emptySkyLightMask);
          readSection(this.blockLightSections, blockLight, this.blockLightMask, blockLightMask, this.emptyBlockLightMask, emptyBlockLightMask);
        }
        // Also update _loadBlockLightNibbles and _loadSkyLightNibbles with similar index conversion
        _loadBlockLightNibbles(y, buffer) {
          if (buffer.length !== 2048)
            throw new Error("Invalid light nibble buffer length " + buffer.length);
          const minCY = Math.abs(this.minY >> 4) + 1;
          this.blockLightMask.set(y + minCY, 1);
          const bitArray = new BitArray({
            bitsPerValue: 4,
            capacity: 4096
          });
          for (let i = 0; i < buffer.length; i++) {
            const byte = buffer[i];
            const blockIndex1 = i * 2;
            const blockIndex2 = i * 2 + 1;
            const value1 = byte & 15;
            const value2 = byte >> 4 & 15;
            const x1 = blockIndex1 % 16;
            const z1 = Math.floor(blockIndex1 / 16) % 16;
            const y1 = Math.floor(blockIndex1 / 256);
            const x2 = blockIndex2 % 16;
            const z2 = Math.floor(blockIndex2 / 16) % 16;
            const y2 = Math.floor(blockIndex2 / 256);
            const index1 = y1 * 16 * 16 + z1 * 16 + x1;
            const index2 = y2 * 16 * 16 + z2 * 16 + x2;
            bitArray.set(index1, value1);
            if (blockIndex2 < 4096) {
              bitArray.set(index2, value2);
            }
          }
          this.blockLightSections[y + minCY] = bitArray;
        }
        _loadSkyLightNibbles(y, buffer) {
          if (buffer.length !== 2048)
            throw new Error("Invalid light nibble buffer length: " + buffer.length);
          const minCY = Math.abs(this.minY >> 4) + 1;
          this.skyLightMask.set(y + minCY, 1);
          const bitArray = new BitArray({
            bitsPerValue: 4,
            capacity: 4096
          });
          for (let i = 0; i < buffer.length; i++) {
            const byte = buffer[i];
            const blockIndex1 = i * 2;
            const blockIndex2 = i * 2 + 1;
            const value1 = byte & 15;
            const value2 = byte >> 4 & 15;
            const x1 = blockIndex1 % 16;
            const z1 = Math.floor(blockIndex1 / 16) % 16;
            const y1 = Math.floor(blockIndex1 / 256);
            const x2 = blockIndex2 % 16;
            const z2 = Math.floor(blockIndex2 / 16) % 16;
            const y2 = Math.floor(blockIndex2 / 256);
            const index1 = y1 * 16 * 16 + z1 * 16 + x1;
            const index2 = y2 * 16 * 16 + z2 * 16 + x2;
            bitArray.set(index1, value1);
            if (blockIndex2 < 4096) {
              bitArray.set(index2, value2);
            }
          }
          this.skyLightSections[y + minCY] = bitArray;
        }
        // Loads an disk serialized chunk
        loadSection(y, blockStates, biomes, blockLight, skyLight) {
          const minCY = Math.abs(this.minY >> 4);
          const raiseUnknownBlock = (block) => {
            throw new Error(`Failed to map ${JSON.stringify(block)} to a block state ID`);
          };
          const raiseUnknownBiome = (biome) => {
            throw new Error(`Failed to map ${JSON.stringify(biome)} to a biome ID`);
          };
          this.sections[y + minCY] = ChunkSection.fromLocalPalette({
            noSizePrefix,
            data: BitArray.fromLongArray(blockStates.data || {}, blockStates.bitsPerBlock),
            palette: blockStates.palette.map((e) => Block.fromProperties(e.Name.replace("minecraft:", ""), e.Properties || {}) ?? raiseUnknownBlock(e)).map((e) => e.stateId)
          });
          this.biomes[y + minCY] = BiomeSection.fromLocalPalette({
            noSizePrefix,
            data: BitArray.fromLongArray(biomes.data || {}, biomes.bitsPerBiome),
            palette: biomes.palette.map((e) => mcData2.biomesByName[e.replace("minecraft:", "")] ?? raiseUnknownBiome(e)).map((e) => e.id)
          });
          if (blockLight)
            this._loadBlockLightNibbles(y, blockLight);
          if (skyLight)
            this._loadSkyLightNibbles(y, skyLight);
        }
        dumpLight() {
          const skyLight = [];
          const blockLight = [];
          this.skyLightSections.forEach((section, index) => {
            if (section !== null && this.skyLightMask.get(index)) {
              const smartBuffer = new SmartBuffer();
              section.writeBuffer(smartBuffer);
              skyLight.push(Uint8Array.from(smartBuffer.toBuffer()));
            }
          });
          this.blockLightSections.forEach((section, index) => {
            if (section !== null && this.blockLightMask.get(index)) {
              const smartBuffer = new SmartBuffer();
              section.writeBuffer(smartBuffer);
              blockLight.push(Uint8Array.from(smartBuffer.toBuffer()));
            }
          });
          return {
            skyLight,
            blockLight,
            skyLightMask: this.skyLightMask.toLongArray(),
            blockLightMask: this.blockLightMask.toLongArray(),
            emptySkyLightMask: this.emptySkyLightMask.toLongArray(),
            emptyBlockLightMask: this.emptyBlockLightMask.toLongArray()
          };
        }
        /**
         * New serializable light data dump method with consistent interface across versions
         * Returns an object that can be easily transferred between threads
         * @returns {Object} Object containing serialized light data
         */
        dumpLightNew() {
          return {
            skyLightMask: this.skyLightMask.toLongArray(),
            emptySkyLightMask: this.emptySkyLightMask.toLongArray(),
            skyLightSections: this.skyLightSections.map((section) => section === null ? null : section.toJson()),
            blockLightMask: this.blockLightMask.toLongArray(),
            emptyBlockLightMask: this.emptyBlockLightMask.toLongArray(),
            blockLightSections: this.blockLightSections.map((section) => section === null ? null : section.toJson())
          };
        }
        /**
         * New light data loading method with consistent interface across versions
         * Accepts a single argument that contains all necessary light data
         * @param {Object} lightData - Object containing serialized light data
         */
        loadLightNew(lightData) {
          this.skyLightMask = BitArray.fromLongArray(lightData.skyLightMask, 1);
          this.emptySkyLightMask = BitArray.fromLongArray(lightData.emptySkyLightMask, 1);
          this.skyLightSections = lightData.skyLightSections.map((s) => s === null ? null : BitArray.fromJson(s));
          this.blockLightMask = BitArray.fromLongArray(lightData.blockLightMask, 1);
          this.emptyBlockLightMask = BitArray.fromLongArray(lightData.emptyBlockLightMask, 1);
          this.blockLightSections = lightData.blockLightSections.map((s) => s === null ? null : BitArray.fromJson(s));
        }
      };
    };
    function getLightSectionIndex2(pos, minY) {
      return Math.floor((pos.y - minY) / 16) + 1;
    }
    function toBiomePos(pos, minY) {
      return { x: pos.x >> 2, y: (pos.y - minY & 15) >> 2, z: pos.z >> 2 };
    }
    function toSectionPos(pos, minY) {
      return { x: pos.x, y: pos.y - minY & 15, z: pos.z };
    }
    function getSectionBlockIndex(pos, minY) {
      return (pos.y - minY & 15) << 8 | pos.z << 4 | pos.x;
    }
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.18/chunk.js
var require_chunk8 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/pc/1.18/chunk.js"(exports2, module2) {
    function loader(registry) {
      const Block = require_prismarine_block()(registry);
      return require_ChunkColumn7()(Block, registry);
    }
    module2.exports = loader;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/0.14/chunk.js
var require_chunk9 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/0.14/chunk.js"(exports2, module2) {
    "use strict";
    var Vec32 = require_vec3().Vec3;
    var w = 16;
    var l = 16;
    var h = 128;
    var BLOCK_DATA_SIZE = w * l * h;
    var REGULAR_DATA_SIZE = BLOCK_DATA_SIZE / 2;
    var SKYLIGHT_DATA_SIZE = BLOCK_DATA_SIZE / 2;
    var BLOCKLIGHT_DATA_SIZE = BLOCK_DATA_SIZE / 2;
    var ADDITIONAL_DATA_SIZE_DIRTY = w * l;
    var ADDITIONAL_DATA_SIZE_COLOR = w * l * 4;
    var BUFFER_SIZE = BLOCK_DATA_SIZE + REGULAR_DATA_SIZE + SKYLIGHT_DATA_SIZE + BLOCKLIGHT_DATA_SIZE + ADDITIONAL_DATA_SIZE_COLOR + ADDITIONAL_DATA_SIZE_DIRTY;
    var readUInt4LE = require_uint4().readUInt4LE;
    var writeUInt4LE = require_uint4().writeUInt4LE;
    module2.exports = loader;
    function loader(registry) {
      Block = require_prismarine_block()(registry);
      Chunk.w = w;
      Chunk.l = l;
      Chunk.h = h;
      Chunk.BUFFER_SIZE = BUFFER_SIZE;
      Chunk.version = registry.version;
      return Chunk;
    }
    var Block;
    function exists(val) {
      return val !== void 0;
    }
    var getArrayPosition = function(pos) {
      return pos.x + w * (pos.z + l * pos.y);
    };
    var getBlockCursor = function(pos) {
      return getArrayPosition(pos);
    };
    var getBlockDataCursor = function(pos) {
      return BLOCK_DATA_SIZE + getArrayPosition(pos) * 0.5;
    };
    var getBlockLightCursor = function(pos) {
      return BLOCK_DATA_SIZE + REGULAR_DATA_SIZE + getArrayPosition(pos) * 0.5;
    };
    var getSkyLightCursor = function(pos) {
      return BLOCK_DATA_SIZE + REGULAR_DATA_SIZE + SKYLIGHT_DATA_SIZE + getArrayPosition(pos) * 0.5;
    };
    var getHeightMapCursor = function(pos) {
      return BLOCK_DATA_SIZE + REGULAR_DATA_SIZE + SKYLIGHT_DATA_SIZE + BLOCKLIGHT_DATA_SIZE + pos.z * w + pos.x;
    };
    var getBiomeCursor = function(pos) {
      return BLOCK_DATA_SIZE + REGULAR_DATA_SIZE + SKYLIGHT_DATA_SIZE + BLOCKLIGHT_DATA_SIZE + ADDITIONAL_DATA_SIZE_DIRTY + (pos.z * w + pos.x) * 4;
    };
    var Chunk = class _Chunk {
      constructor() {
        this.data = Buffer.alloc(BUFFER_SIZE);
        this.data.fill(0);
      }
      toJson() {
        return JSON.stringify({ data: this.data.toJSON() });
      }
      static fromJson(j) {
        const parsed = JSON.parse(j);
        const chunk = new _Chunk();
        chunk.data = Buffer.from(parsed.data);
        return chunk;
      }
      initialize(iniFunc) {
        const p = new Vec32(0, 0, 0);
        for (p.y = 0; p.y < h; p.y++) {
          for (p.z = 0; p.z < w; p.z++) {
            for (p.x = 0; p.x < l; p.x++) {
              const block = iniFunc(p.x, p.y, p.z);
              if (block === null) {
                continue;
              }
              this.setBlock(p, block);
            }
          }
        }
      }
      getBlock(pos) {
        const block = new Block(this.getBlockType(pos), this.getBiome(pos), this.getBlockData(pos));
        block.light = this.getBlockLight(pos);
        block.skyLight = this.getSkyLight(pos);
        return block;
      }
      setBlock(pos, block) {
        if (exists(block.type)) {
          this.setBlockType(pos, block.type);
        }
        if (exists(block.metadata)) {
          this.setBlockData(pos, block.metadata);
        }
        if (exists(block.biome)) {
          this.setBiome(pos, block.biome.id);
        }
        if (exists(block.skyLight)) {
          this.setSkyLight(pos, block.skyLight);
        }
        if (exists(block.light)) {
          this.setBlockLight(pos, block.light);
        }
      }
      getBlockType(pos) {
        if (pos.y < 0 || pos.y >= 256)
          return 0;
        return this.data.readUInt8(getBlockCursor(pos));
      }
      setBlockType(pos, id) {
        if (pos.y < 0 || pos.y >= 256)
          return;
        this.data.writeUInt8(id, getBlockCursor(pos));
      }
      getBlockData(pos) {
        if (pos.y < 0 || pos.y >= 256)
          return 0;
        return readUInt4LE(this.data, getBlockDataCursor(pos));
      }
      setBlockData(pos, data) {
        if (pos.y < 0 || pos.y >= 256)
          return;
        writeUInt4LE(this.data, data, getBlockDataCursor(pos));
      }
      getBlockLight(pos) {
        if (pos.y < 0 || pos.y >= 256)
          return 0;
        return readUInt4LE(this.data, getBlockLightCursor(pos));
      }
      setBlockLight(pos, light) {
        if (pos.y < 0 || pos.y >= 256)
          return;
        writeUInt4LE(this.data, light, getBlockLightCursor(pos));
      }
      getSkyLight(pos) {
        if (pos.y < 0 || pos.y >= 256)
          return 0;
        return readUInt4LE(this.data, getSkyLightCursor(pos));
      }
      setSkyLight(pos, light) {
        if (pos.y < 0 || pos.y >= 256)
          return;
        writeUInt4LE(this.data, light, getSkyLightCursor(pos));
      }
      getBiomeColor(pos) {
        const color = this.data.readInt32BE(getBiomeCursor(pos)) & 16777215;
        return {
          r: color >> 16,
          g: color >> 8 & 255,
          b: color & 255
        };
      }
      setBiomeColor(pos, r, g, b) {
        this.data.writeInt32BE(this.data.readInt32BE(getBiomeCursor(pos)) & 4278190080 | (r & 255) << 16 | (g & 255) << 8 | b & 255, getBiomeCursor(pos));
      }
      getBiome(pos) {
        return (this.data.readInt32BE(getBiomeCursor(pos)) & 4278190080) >> 24;
      }
      setBiome(pos, id) {
        this.data.writeInt32BE(this.data.readInt32BE(getBiomeCursor(pos)) & 16777215 | id << 24, getBiomeCursor(pos));
      }
      getHeight(pos) {
        return this.data.readUInt8(getHeightMapCursor(pos));
      }
      setHeight(pos, value) {
        this.data.writeUInt8(value, getHeightMapCursor(pos));
      }
      load(data) {
        if (!Buffer.isBuffer(data)) {
          throw new Error("Data must be a buffer");
        }
        if (data.length !== BUFFER_SIZE) {
          throw new Error(`Data buffer not correct size (was ${data.length}, expected ${BUFFER_SIZE})`);
        }
        this.data = data;
      }
      // These methods do nothing, and are present only for API compatibility
      dumpBiomes() {
      }
      dumpLight() {
      }
      loadLight() {
      }
      loadBiomes() {
      }
      dump() {
        return this.data;
      }
      getMask() {
        return 65535;
      }
    };
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.0/subchunk.js
var require_subchunk = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.0/subchunk.js"(exports2, module2) {
    "use strict";
    var readUInt4LE = require_uint4().readUInt4LE;
    var writeUInt4LE = require_uint4().writeUInt4LE;
    var BLOCK_SIZE = 16 * 16 * 16;
    var METADATA_SIZE = 16 * 16 * 16 / 2;
    var SKYLIGHT_SIZE = 16 * 16 * 16 / 2;
    var BLOCKLIGHT_SIZE = 16 * 16 * 16 / 2;
    var BUFFER_SIZE = 1 + (BLOCK_SIZE + METADATA_SIZE + BLOCKLIGHT_SIZE + SKYLIGHT_SIZE);
    function getIndex(pos) {
      return 1 + (pos.x * 256 + pos.z * 16 + pos.y);
    }
    var SubChunk = class _SubChunk {
      constructor() {
        this.data = Buffer.alloc(BUFFER_SIZE);
        this.data.fill(0);
      }
      toJson() {
        return JSON.stringify({ data: this.data.toJSON() });
      }
      static fromJson(j) {
        const parsed = JSON.parse(j);
        const chunk = new _SubChunk();
        chunk.data = Buffer.from(parsed.data);
        return chunk;
      }
      getBlockType(pos) {
        return this.data.readUInt8(getIndex(pos));
      }
      setBlockType(pos, type) {
        this.data.writeUInt8(type, getIndex(pos));
      }
      getBlockLight(pos) {
        return readUInt4LE(this.data, BLOCK_SIZE + METADATA_SIZE + SKYLIGHT_SIZE + getIndex(pos) / 2);
      }
      setBlockLight(pos, light) {
        writeUInt4LE(this.data, light, BLOCK_SIZE + METADATA_SIZE + SKYLIGHT_SIZE + getIndex(pos) / 2);
      }
      getSkyLight(pos) {
        return readUInt4LE(this.data, BLOCK_SIZE + METADATA_SIZE + getIndex(pos) / 2);
      }
      setSkyLight(pos, light) {
        writeUInt4LE(this.data, light, BLOCK_SIZE + METADATA_SIZE + getIndex(pos) / 2);
      }
      getBlockData(pos) {
        return readUInt4LE(this.data, BLOCK_SIZE + getIndex(pos) / 2);
      }
      setBlockData(pos, data) {
        writeUInt4LE(this.data, data, BLOCK_SIZE + getIndex(pos) / 2);
      }
      load(data) {
        if (!Buffer.isBuffer(data)) {
          throw new Error("Data must be a buffer");
        }
        if (data.length !== BUFFER_SIZE) {
          throw new Error(`Data buffer not correct size (was ${data.length}, expected ${BUFFER_SIZE})`);
        }
        this.data = data;
      }
      dump() {
        return this.data;
      }
    };
    module2.exports = SubChunk;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.0/chunk.js
var require_chunk10 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.0/chunk.js"(exports2, module2) {
    "use strict";
    var SubChunk = require_subchunk();
    var Vec32 = require_vec3();
    var BIOME_ID_SIZE = 256;
    var HEIGHT_SIZE = 256 * 2;
    var BUFFER_SIZE = BIOME_ID_SIZE + HEIGHT_SIZE;
    module2.exports = loader;
    function loader(registry) {
      Block = require_prismarine_block()(registry);
      Chunk.w = 16;
      Chunk.l = 16;
      Chunk.h = 256;
      Chunk.BUFFER_SIZE = 3 + 256 + 512 + 16 * 10241;
      Chunk.version = registry.version;
      return Chunk;
    }
    var Block;
    function exists(val) {
      return val !== void 0;
    }
    var Chunk = class _Chunk {
      constructor() {
        this.chunks = new Array(16);
        for (let i = 0; i < this.chunks.length; i++) {
          this.chunks[i] = new SubChunk();
        }
        this.data = Buffer.alloc(BUFFER_SIZE);
        this.data.fill(0);
        for (let j = 0; j < 256; j++) {
          this.data[j] = 1;
        }
      }
      toJson() {
        return JSON.stringify({ data: this.data, sections: this.chunks.map((section) => section.toJson()) });
      }
      static fromJson(j) {
        const parsed = JSON.parse(j);
        const chunk = new _Chunk();
        chunk.data = parsed.data;
        chunk.chunks = parsed.sections.map((s) => SubChunk.fromJson(s));
        return chunk;
      }
      initialize(iniFunc) {
        const p = new Vec32(0, 0, 0);
        for (p.y = 0; p.y < _Chunk.h; p.y++) {
          for (p.z = 0; p.z < _Chunk.w; p.z++) {
            for (p.x = 0; p.x < _Chunk.l; p.x++) {
              const block = iniFunc(p.x, p.y, p.z);
              if (block === null) {
                continue;
              }
              this.setBlock(p, block);
            }
          }
        }
      }
      getBlock(pos) {
        const block = new Block(this.getBlockType(pos), this.getBiome(pos), this.getBlockData(pos));
        block.light = this.getBlockLight(pos);
        block.skyLight = this.getSkyLight(pos);
        return block;
      }
      setBlock(pos, block) {
        if (exists(block.type)) {
          this.setBlockType(pos, block.type);
        }
        if (exists(block.metadata)) {
          this.setBlockData(pos, block.metadata);
        }
        if (exists(block.biome)) {
          this.setBiome(pos, block.biome.id);
        }
        if (exists(block.skyLight)) {
          this.setSkyLight(pos, block.skyLight);
        }
        if (exists(block.light)) {
          this.setBlockLight(pos, block.light);
        }
      }
      getBlockType(pos) {
        const chunk = this.chunks[pos.y >> 4];
        return chunk ? chunk.getBlockType(new Vec32(pos.x, pos.y - 16 * (pos.y >> 4), pos.z)) : 0;
      }
      setBlockType(pos, type) {
        const chunk = this.chunks[pos.y >> 4];
        return chunk && chunk.setBlockType(new Vec32(pos.x, pos.y - 16 * (pos.y >> 4), pos.z), type);
      }
      getBlockData(pos) {
        const chunk = this.chunks[pos.y >> 4];
        return chunk ? chunk.getBlockData(new Vec32(pos.x, pos.y - 16 * (pos.y >> 4), pos.z)) : 0;
      }
      setBlockData(pos, data) {
        const chunk = this.chunks[pos.y >> 4];
        return chunk && chunk.setBlockData(new Vec32(pos.x, pos.y - 16 * (pos.y >> 4), pos.z), data);
      }
      getBlockLight(pos) {
        const chunk = this.chunks[pos.y >> 4];
        return chunk ? chunk.getBlockLight(new Vec32(pos.x, pos.y - 16 * (pos.y >> 4), pos.z)) : 0;
      }
      setBlockLight(pos, light) {
        const chunk = this.chunks[pos.y >> 4];
        return chunk && chunk.setBlockLight(new Vec32(pos.x, pos.y - 16 * (pos.y >> 4), pos.z), light);
      }
      getSkyLight(pos) {
        const chunk = this.chunks[pos.y >> 4];
        return chunk ? chunk.getSkyLight(new Vec32(pos.x, pos.y - 16 * (pos.y >> 4), pos.z)) : 15;
      }
      setSkyLight(pos, light) {
        const chunk = this.chunks[pos.y >> 4];
        return chunk && chunk.setSkyLight(new Vec32(pos.x, pos.y - 16 * (pos.y >> 4), pos.z), light);
      }
      getBiomeColor(pos) {
        return { x: 0, y: 0, z: 0 };
      }
      setBiomeColor(pos, r, g, b) {
      }
      getBiome(pos) {
        return this.data.readUInt8((pos.z << 4) + pos.x);
      }
      setBiome(pos, id) {
        this.data.writeUInt8(id, (pos.z << 4) + pos.x);
      }
      getHeight(pos) {
        return this.data.readUInt8((pos.z << 4) + pos.x);
      }
      setHeight(pos, height) {
        this.data.writeUInt8(height, (pos.z << 4) + pos.x);
      }
      load(newData) {
        if (!Buffer.isBuffer(newData)) {
          throw new Error("Data must be a buffer");
        }
        let offset = 0;
        const numberOfChunks = newData.readUInt8(offset);
        offset += 1;
        if (numberOfChunks * 10241 + 1 > newData.length) {
          throw new Error(`Data buffer not correct size (was ${newData.length}, expected ${3 + 256 + 512 + 16 * 10241})`);
        }
        for (let i = 0; i < numberOfChunks; i++) {
          this.chunks[i].load(newData.slice(offset, offset + 10241));
          offset += 10241;
        }
      }
      size() {
        let size = 1;
        size += this.chunks.length * 10241;
        size += HEIGHT_SIZE;
        size += BIOME_ID_SIZE;
        size += 1;
        size += 1;
        return size;
      }
      // These methods do nothing, and are present only for API compatibility
      dumpBiomes() {
      }
      dumpLight() {
      }
      loadLight() {
      }
      loadBiomes() {
      }
      dump() {
        let offset = 0;
        const exportData = Buffer.alloc(this.size());
        exportData.fill(0);
        exportData.writeUInt8(this.chunks.length, offset);
        offset += 1;
        for (let i = 0; i < this.chunks.length; i++) {
          const dump = this.chunks[i].dump();
          dump.copy(exportData, offset);
          offset += dump.length;
        }
        this.data.copy(exportData, offset);
        offset += this.data.length;
        exportData.writeUInt8(0, offset);
        offset += 1;
        exportData.writeUInt8(0, offset);
        offset += 1;
        return exportData;
      }
      getMask() {
        return 65535;
      }
    };
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/common/constants.js
var require_constants2 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/common/constants.js"(exports2, module2) {
    module2.exports = {
      StorageType: {
        LocalPersistence: 0,
        NetworkPersistence: 1,
        Runtime: 2
      },
      ChunkVersion: {
        v0_9_00: 0,
        v0_9_02: 1,
        // added to fix the grass color being corrupted
        v0_9_05: 2,
        // make sure that biomes are not corrupted
        v0_17_0: 3,
        // switch to a key per subchunk + 2D data
        v0_18_0: 4,
        // made beds be block entities
        vConsole1_to_v0_18_0: 5,
        // converted from another version of the game
        v1_2_0: 6,
        // Format added in MC1.2 - for upgrading structure spawners
        v1_2_0_bis: 7,
        // second format added in MC1.2 - to remove dynamic water in oceans
        v1_4_0: 8,
        v1_8_0: 9,
        v1_9_0: 10,
        v1_10_0: 11,
        v1_11_0: 12,
        v1_11_1: 13,
        v1_11_2: 14,
        v1_12_0: 15,
        v1_15_0: 16,
        v1_15_1: 17,
        v1_16_0: 18,
        v1_16_1: 19,
        v1_16_100: 20,
        v1_16_200: 21,
        v1_16_210: 22,
        // caves and cliffs disabled
        v1_17_0: 25,
        // 1.17.0-20 caves and cliffs enabled
        v1_17_30: 29,
        // 1.17.30 caves and cliffs enabled
        v1_17_40: 31,
        v1_18_0: 39,
        v1_18_11: 39
      }
    };
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/common/Stream.js
var require_Stream = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/common/Stream.js"(exports2, module2) {
    var MAX_ALLOC_SIZE = 1024 * 1024 * 2;
    var DEFAULT_ALLOC_SIZE = 1e4;
    var ByteStream = class {
      constructor(buffer) {
        this.buffer = buffer || Buffer.allocUnsafe(DEFAULT_ALLOC_SIZE);
        this.readOffset = 0;
        this.writeOffset = 0;
        this.size = this.buffer.length;
      }
      resizeForWriteIfNeeded(bytes) {
        if (this.writeOffset + bytes > this.size) {
          this.size *= 2;
          const allocSize = this.size - this.writeOffset;
          this.buffer = Buffer.concat([this.buffer, Buffer.allocUnsafe(allocSize)]);
        }
        if (this.size > MAX_ALLOC_SIZE)
          throw new Error("Buffer size exceeded guard limit");
      }
      readByte() {
        return this.buffer[this.readOffset++];
      }
      // Write unsigned
      writeUInt8(value) {
        this.resizeForWriteIfNeeded(1);
        this.buffer.writeUInt8(value, this.writeOffset);
        this.writeOffset += 1;
      }
      writeUInt16LE(value) {
        this.resizeForWriteIfNeeded(2);
        this.buffer.writeUInt16LE(value, this.writeOffset);
        this.writeOffset += 2;
      }
      writeUInt32LE(value) {
        this.resizeForWriteIfNeeded(4);
        this.buffer.writeUInt32LE(value, this.writeOffset);
        this.writeOffset += 4;
      }
      writeUInt64LE(value) {
        this.resizeForWriteIfNeeded(8);
        switch (typeof value) {
          case "bigint":
            this.buffer.writeBigUInt64LE(value, this.writeOffset);
            break;
          case "number":
            this.buffer.writeUInt32LE(value & 4294967295, this.writeOffset);
            this.buffer.writeUInt32LE(value >>> 32, this.writeOffset + 4);
            break;
          default:
            throw new Error("Invalid value type");
        }
        this.writeOffset += 8;
      }
      writeFloatLE(value) {
        this.resizeForWriteIfNeeded(4);
        this.buffer.writeFloatLE(value, this.writeOffset);
        this.writeOffset += 4;
      }
      writeDoubleLE(value) {
        this.resizeForWriteIfNeeded(8);
        this.buffer.writeDoubleLE(value, this.writeOffset);
        this.writeOffset += 8;
      }
      writeUInt16BE(value) {
        this.resizeForWriteIfNeeded(2);
        this.buffer.writeUInt16BE(value, this.writeOffset);
        this.writeOffset += 2;
      }
      writeUInt32BE(value) {
        this.resizeForWriteIfNeeded(4);
        this.buffer.writeUInt32BE(value, this.writeOffset);
        this.writeOffset += 4;
      }
      writeUInt64BE(value) {
        this.resizeForWriteIfNeeded(8);
        switch (typeof value) {
          case "bigint":
            this.buffer.writeBigUInt64BE(value, this.writeOffset);
            break;
          case "number":
            this.buffer.writeUInt32BE(value >>> 32, this.writeOffset);
            this.buffer.writeUInt32BE(value & 4294967295, this.writeOffset + 4);
            break;
          default:
            throw new Error("Invalid value type");
        }
        this.writeOffset += 8;
      }
      // Write signed
      writeInt8(value) {
        this.resizeForWriteIfNeeded(1);
        this.buffer.writeInt8(value, this.writeOffset);
        this.writeOffset += 1;
      }
      writeInt16LE(value) {
        this.resizeForWriteIfNeeded(2);
        this.buffer.writeInt16LE(value, this.writeOffset);
        this.writeOffset += 2;
      }
      writeInt32LE(value) {
        this.resizeForWriteIfNeeded(4);
        this.buffer.writeInt32LE(value, this.writeOffset);
        this.writeOffset += 4;
      }
      writeInt64LE(value) {
        this.resizeForWriteIfNeeded(8);
        switch (typeof value) {
          case "bigint":
            this.buffer.writeBigInt64LE(value, this.writeOffset);
            break;
          case "number":
            this.buffer.writeInt32LE(value & 4294967295, this.writeOffset);
            this.buffer.writeInt32LE(value >>> 32, this.writeOffset + 4);
            break;
          default:
            throw new Error("Invalid value type");
        }
        this.writeOffset += 8;
      }
      writeInt16BE(value) {
        this.resizeForWriteIfNeeded(2);
        this.buffer.writeInt16BE(value, this.writeOffset);
        this.writeOffset += 2;
      }
      writeInt32BE(value) {
        this.resizeForWriteIfNeeded(4);
        this.buffer.writeInt32BE(value, this.writeOffset);
        this.writeOffset += 4;
      }
      writeInt64BE(value) {
        this.resizeForWriteIfNeeded(8);
        switch (typeof value) {
          case "bigint":
            this.buffer.writeBigInt64BE(value, this.writeOffset);
            break;
          case "number":
            this.buffer.writeInt32BE(value >>> 32, this.writeOffset);
            this.buffer.writeInt32BE(value & 4294967295, this.writeOffset + 4);
            break;
          default:
            throw new Error("Invalid value type");
        }
        this.writeOffset += 8;
      }
      // Write floats
      writeFloatBE(value) {
        this.resizeForWriteIfNeeded(4);
        this.buffer.writeFloatBE(value, this.writeOffset);
        this.writeOffset += 4;
      }
      writeDoubleBE(value) {
        this.resizeForWriteIfNeeded(8);
        this.buffer.writeDoubleBE(value, this.writeOffset);
        this.writeOffset += 8;
      }
      // Read
      readUInt8() {
        const value = this.buffer.readUInt8(this.readOffset);
        this.readOffset += 1;
        return value;
      }
      readUInt16LE() {
        const value = this.buffer.readUInt16LE(this.readOffset);
        this.readOffset += 2;
        return value;
      }
      readUInt32LE() {
        const value = this.buffer.readUInt32LE(this.readOffset);
        this.readOffset += 4;
        return value;
      }
      readUInt64LE() {
        const value = this.buffer.readBigUInt64LE(this.readOffset);
        this.readOffset += 8;
        return value;
      }
      readUInt16BE() {
        const value = this.buffer.readUInt16BE(this.readOffset);
        this.readOffset += 2;
        return value;
      }
      readUInt32BE() {
        const value = this.buffer.readUInt32BE(this.readOffset);
        this.readOffset += 4;
        return value;
      }
      readUInt64BE() {
        const value = this.buffer.readBigUInt64BE(this.readOffset);
        this.readOffset += 8;
        return value;
      }
      readInt8() {
        const value = this.buffer.readInt8(this.readOffset);
        this.readOffset += 1;
        return value;
      }
      readInt16LE() {
        const value = this.buffer.readInt16LE(this.readOffset);
        this.readOffset += 2;
        return value;
      }
      readInt32LE() {
        const value = this.buffer.readInt32LE(this.readOffset);
        this.readOffset += 4;
        return value;
      }
      readInt64LE() {
        const value = this.buffer.readBigInt64LE(this.readOffset);
        this.readOffset += 8;
        return value;
      }
      // Strings
      writeStringNT(value, encoding = "utf8") {
        this.resizeForWriteIfNeeded(value.length + 1);
        this.buffer.write(value, this.writeOffset, value.length, encoding);
        this.buffer[this.writeOffset + value.length] = 0;
        this.writeOffset += value.length + 1;
      }
      writeStringRaw(value, encoding = "utf8") {
        this.resizeForWriteIfNeeded(value.length);
        this.buffer.write(value, this.writeOffset, value.length, encoding);
        this.writeOffset += value.length;
      }
      writeBuffer(value) {
        this.resizeForWriteIfNeeded(value.length);
        value.copy(this.buffer, this.writeOffset);
        this.writeOffset += value.length;
      }
      readBuffer(length) {
        const value = this.buffer.slice(this.readOffset, this.readOffset + length);
        this.readOffset += length;
        return value;
      }
      // Varints
      writeVarInt(value) {
        this.resizeForWriteIfNeeded(9);
        let offset = 0;
        while (value >= 128) {
          this.buffer[this.writeOffset + offset] = value & 127 | 128;
          value = value >>> 7;
          offset += 1;
        }
        this.buffer[this.writeOffset + offset] = value;
        this.writeOffset += offset + 1;
      }
      readVarInt() {
        let value = 0;
        let offset = 0;
        let byte;
        do {
          byte = this.buffer[this.readOffset + offset];
          value |= (byte & 127) << 7 * offset;
          offset += 1;
        } while (byte & 128);
        this.readOffset += offset;
        return value;
      }
      writeVarLong(value) {
        this.resizeForWriteIfNeeded(9);
        let offset = 0;
        while (value >= 0x80n) {
          this.buffer[this.writeOffset + offset] = value & 0x7fn | 0x80n;
          value = value >>> 7n;
          offset += 1;
        }
        this.buffer[this.writeOffset + offset] = value;
        this.writeOffset += offset + 1;
      }
      readVarLong() {
        let value = 0n;
        let offset = 0n;
        let byte;
        do {
          byte = this.buffer[this.readOffset + offset];
          value |= (byte & 0x7fn) << 7n * offset;
          offset += 1n;
        } while (byte & 0x80n);
        this.readOffset += Number(offset);
        return value;
      }
      writeZigZagVarInt(value) {
        const zigzag = value << 1 ^ value >> 31;
        this.writeVarInt(zigzag);
      }
      readZigZagVarInt() {
        const value = this.readVarInt();
        return value >>> 1 ^ -(value & 1);
      }
      writeZigZagVarlong(value) {
        const zigzag = value << 1n ^ value >> 63n;
        this.writeVarlong(zigzag);
      }
      readZigZagVarlong() {
        const value = this.readVarInt();
        return value >>> 1n ^ -(value & 1n);
      }
      // Extra
      peekUInt8() {
        return this.buffer[this.readOffset];
      }
      peek() {
        return this.buffer[this.readOffset];
      }
      getBuffer() {
        return this.buffer.slice(0, this.writeOffset);
      }
    };
    module2.exports = ByteStream;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/common/CommonChunkColumn.js
var require_CommonChunkColumn2 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/common/CommonChunkColumn.js"(exports2, module2) {
    var { Vec3: Vec32 } = require_vec3();
    var Stream = require_Stream();
    var nbt = require_nbt2();
    var keyFromLocalPos = (pos) => `${pos.x},${pos.y},${pos.z}`;
    var keyFromGlobalPos = (x, y, z) => `${x & 15},${y},${z & 15}`;
    var CommonChunkColumn = class {
      constructor(options, registry) {
        this.x = options.x || 0;
        this.z = options.z || 0;
        this.registry = registry;
        this.chunkVersion = options.chunkVersion;
        this.blockEntities = options.blockEntities || {};
        this.sections = [];
        this.entities = {};
        this.compactOnSave = false;
        this.setBounds(0, 16);
      }
      setBounds(minCY, maxCY) {
        this.minCY = minCY;
        this.maxCY = maxCY;
        this.maxY = this.maxCY * 16;
        this.minY = this.minCY * 16;
        this.worldHeight = this.maxY + Math.abs(this.minY);
        this.co = Math.abs(this.minCY);
      }
      initialize(func) {
        const p = new Vec32();
        for (p.y = 0; p.y < this.worldHeight; p.y++) {
          for (p.z = 0; p.z < 16; p.z++) {
            for (p.x = 0; p.x < 16; p.x++) {
              const block = func(p.x, p.y, p.z);
              if (block)
                this.setBlock(p, block);
            }
          }
        }
      }
      // Blocks
      getBlock(vec4, full = true) {
        const Y = vec4.y >> 4;
        const sec = this.sections[this.co + Y];
        if (!sec)
          return this.Block.fromStateId(this.registry.blocksByName.air.defaultState, 0);
        const block = sec.getBlock(vec4.l, vec4.x, vec4.y & 15, vec4.z, this.getBiomeId(vec4));
        if (full) {
          block.light = sec.blockLight.get(vec4.x, vec4.y & 15, vec4.z);
          block.skyLight = sec.skyLight.get(vec4.x, vec4.y & 15, vec4.z);
          block.entity = this.getBlockEntity(vec4);
        }
        return block;
      }
      setBlock(pos, block) {
        const Y = pos.y >> 4;
        let sec = this.sections[this.co + Y];
        if (!sec) {
          sec = new this.Section(this.registry, this.Block, { y: Y, subChunkVersion: this.subChunkVersion });
          this.sections[this.co + Y] = sec;
        }
        sec.setBlock(pos.l, pos.x, pos.y & 15, pos.z, block);
        if (block.light !== void 0)
          sec.blockLight.set(pos.x, pos.y & 15, pos.z, block.light);
        if (block.skyLight !== void 0)
          sec.skyLight.set(pos.x, pos.y & 15, pos.z, block.skyLight);
        if (block.entity)
          this.setBlockEntity(pos, block.entity);
      }
      getBlockStateId(pos) {
        const Y = pos.y >> 4;
        const sec = this.sections[this.co + Y];
        if (!sec) {
          return;
        }
        return sec.getBlockStateId(pos.l, pos.x, pos.y & 15, pos.z);
      }
      setBlockStateId(pos, stateId) {
        const Y = pos.y >> 4;
        let sec = this.sections[this.co + Y];
        if (!sec) {
          sec = new this.Section(this.registry, this.Block, { y: Y, subChunkVersion: this.subChunkVersion });
          this.sections[this.co + Y] = sec;
        }
        sec.setBlockStateId(pos.l, pos.x, pos.y & 15, pos.z, stateId);
      }
      getBiomeId(pos) {
        return 0;
      }
      setBiomeId(pos, biomeId) {
      }
      getBlocks() {
        const arr = this.sections.map((sec) => sec.getPalette()).flat(2);
        const deduped = Object.values(arr.reduce((acc, block) => {
          if (!acc[block.stateId])
            acc[block.stateId] = block;
          acc[block.stateId] = block;
          return acc;
        }, {}));
        return deduped;
      }
      // Lighting
      setBlockLight(pos, light) {
        this.sections[this.co + (pos.y >> 4)].blockLight.set(pos.x, pos.y & 15, pos.z, light);
      }
      setSkyLight(pos, light) {
        this.sections[this.co + (pos.y >> 4)].skyLight.set(pos.x, pos.y & 15, pos.z, light);
      }
      getSkyLight(pos) {
        return this.sections[this.co + (pos.y >> 4)].skyLight.get(pos.x, pos.y & 15, pos.z);
      }
      getBlockLight(pos) {
        return this.sections[this.co + (pos.y >> 4)].blockLight.get(pos.x, pos.y & 15, pos.z);
      }
      // Block entities
      setBlockEntity(pos, tag) {
        this.blockEntities[keyFromLocalPos(pos)] = tag;
      }
      getBlockEntity(pos) {
        return this.blockEntities[keyFromLocalPos(pos)];
      }
      addBlockEntity(tag) {
        const lPos = keyFromGlobalPos(tag.value.x.value, tag.value.y.value, tag.value.z.value);
        this.blockEntities[lPos] = tag;
      }
      removeBlockEntity(pos) {
        delete this.blockEntities[keyFromLocalPos(pos)];
      }
      // This is only capable of moving block entities within the same chunk ... prismarine-world should implement this
      moveBlockEntity(pos, newPos) {
        const oldKey = keyFromLocalPos(pos);
        const newKey = keyFromLocalPos(newPos);
        const tag = this.blockEntities[oldKey];
        delete this.blockEntities[oldKey];
        this.blockEntities[newKey] = tag;
      }
      // Entities
      addEntity(entityTag) {
        const key = entityTag.value.UniqueID.value.toString();
        this.entities[key] = entityTag;
      }
      removeEntity(id) {
        delete this.entities[id];
      }
      // Section management
      getSection({ y }) {
        return this.sections[this.co + (y >> 4)];
      }
      getSectionAtIndex(y) {
        return this.sections[this.co + y];
      }
      setSection(y, section) {
        this.sections[this.co + y] = section;
      }
      newSection(y, storageFormat, buffer) {
        if (storageFormat === void 0) {
          const n = this.Section.create(this.registry, this.Block, { y, subChunkVersion: this.subChunkVersion });
          this.setSection(y, n);
          return n;
        } else {
          const n = new this.Section(this.registry, this.Block, { y, subChunkVersion: this.subChunkVersion });
          n.decode(storageFormat, buffer);
          this.setSection(y, n);
          return n;
        }
      }
      getSectionBlockEntities(sectionY) {
        const found = [];
        for (const key in this.blockEntities) {
          const y = parseInt(key.split(",")[1]) >> 4;
          if (y === sectionY) {
            found.push(this.blockEntities[key]);
          }
        }
        return found;
      }
      getSections() {
        return this.sections;
      }
      // Entities
      getEntities() {
        return this.entities;
      }
      loadEntities(tags) {
        this.entities = tags;
      }
      // Disk Encoding
      // Similar to network encoding, except without varints
      diskEncodeBlockEntities() {
        const tileBufs = [];
        for (const key in this.blockEntities) {
          const tile = this.blockEntities[key];
          tileBufs.push(nbt.writeUncompressed(tile, "little"));
        }
        return Buffer.concat(tileBufs);
      }
      diskEncodeEntities() {
        const entityBufs = [];
        for (const entity in this.entities) {
          const tag = this.entities[entity];
          entityBufs.push(nbt.writeUncompressed(tag, "little"));
        }
        return Buffer.concat(entityBufs);
      }
      diskDecodeBlockEntities(buffer) {
        if (!buffer)
          return;
        const stream = buffer instanceof Buffer ? new Stream(buffer) : buffer;
        let startOffset = stream.readOffset;
        while (stream.peek() === 10) {
          const { data, metadata } = nbt.protos.little.parsePacketBuffer("nbt", buffer, startOffset);
          stream.readOffset += metadata.size;
          startOffset += metadata.size;
          this.addBlockEntity(data);
        }
      }
      diskDecodeEntities(buffer) {
        if (!buffer)
          return;
        const stream = buffer instanceof Buffer ? new Stream(buffer) : buffer;
        let startOffset = stream.readOffset;
        while (stream.peek() === 10) {
          const { data, metadata } = nbt.protos.little.parsePacketBuffer("nbt", buffer, startOffset);
          stream.readOffset += metadata.size;
          startOffset += metadata.size;
          this.addEntity(data);
        }
      }
      toObject() {
        const sections = this.sections.map((sec) => sec?.toObject());
        const { x, z, chunkVersion, blockEntities } = this;
        return { x, z, chunkVersion, blockEntities, sections };
      }
    };
    module2.exports = CommonChunkColumn;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/common/PalettedStorage.js
var require_PalettedStorage = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/common/PalettedStorage.js"(exports2, module2) {
    var wordByteSize = 4;
    var wordBitSize = wordByteSize * 8;
    var storageSize = 4096;
    var BetterUint32Array = class extends Uint32Array {
      toJSON() {
        return Array.from(this);
      }
      [Symbol.for("nodejs.util.inspect.custom")]() {
        return `[Uint32Array of ${this.length} items { ${this.slice(0, 10).toString()} ${this.length > 10 ? ", ..." : ""} }]`;
      }
    };
    var PalettedStorage = class _PalettedStorage {
      constructor(bitsPerBlock) {
        this.bitsPerBlock = bitsPerBlock;
        this.blocksPerWord = Math.floor(wordBitSize / bitsPerBlock);
        this.wordsCount = Math.ceil(storageSize / this.blocksPerWord);
        this.mask = (1 << bitsPerBlock) - 1;
        this.array = new BetterUint32Array(this.wordsCount);
      }
      read(stream) {
        const buf = stream.readBuffer(this.wordsCount * wordByteSize);
        this.array = new BetterUint32Array(new Uint8Array(buf).buffer);
      }
      write(stream) {
        stream.writeBuffer(Buffer.from(this.array.buffer));
      }
      static copyFrom(other) {
        const next = new _PalettedStorage(other.bitsPerBlock);
        Object.assign(next, other);
        if (other instanceof Uint32Array) {
          next.array = new BetterUint32Array(other.array.slice());
        } else {
          next.array = new BetterUint32Array(other.array);
        }
        return next;
      }
      getBuffer() {
        return Buffer.from(this.array.buffer);
      }
      readBits(index, offset) {
        return this.array[index] >> offset & this.mask;
      }
      writeBits(index, offset, data) {
        this.array[index] &= ~(this.mask << offset);
        this.array[index] |= (data & this.mask) << offset;
      }
      getIndex(x, y, z) {
        x &= 15;
        y &= 15;
        z &= 15;
        const index = Math.floor((x << 8 | z << 4 | y) / this.blocksPerWord);
        const offset = (x << 8 | z << 4 | y) % this.blocksPerWord * this.bitsPerBlock;
        return [index, offset];
      }
      get(x, y, z) {
        const [index, offset] = this.getIndex(x, y, z);
        return this.readBits(index, offset);
      }
      set(x, y, z, data) {
        const [index, offset] = this.getIndex(x, y, z);
        this.writeBits(index, offset, data);
      }
      resize(newBitsPerBlock) {
        const storage = new _PalettedStorage(newBitsPerBlock);
        for (let x = 0; x < 16; x++) {
          for (let y = 0; y < 16; y++) {
            for (let z = 0; z < 16; z++) {
              storage.set(x, y, z, this.get(x, y, z));
            }
          }
        }
        return storage;
      }
      forEach(callback) {
        for (let i = 0; i < storageSize; i++) {
          const index = Math.floor(i / this.blocksPerWord);
          const offset = i % this.blocksPerWord * this.bitsPerBlock;
          callback(this.readBits(index, offset));
        }
      }
      incrementPalette(palette) {
        for (let i = 0; i < storageSize; i++) {
          const index = Math.floor(i / this.blocksPerWord);
          const offset = i % this.blocksPerWord * this.bitsPerBlock;
          const ix = this.readBits(index, offset);
          palette[ix].count++;
        }
      }
    };
    module2.exports = PalettedStorage;
  }
});

// ../node_modules/.pnpm/xxhash-wasm@0.4.2/node_modules/xxhash-wasm/cjs/xxhash-wasm.js
var require_xxhash_wasm = __commonJS({
  "../node_modules/.pnpm/xxhash-wasm@0.4.2/node_modules/xxhash-wasm/cjs/xxhash-wasm.js"(exports2, module2) {
    "use strict";
    var t = new Uint8Array([0, 97, 115, 109, 1, 0, 0, 0, 1, 13, 2, 96, 2, 127, 127, 0, 96, 3, 127, 127, 127, 1, 127, 3, 3, 2, 1, 0, 5, 3, 1, 0, 1, 7, 23, 3, 3, 109, 101, 109, 2, 0, 5, 120, 120, 104, 51, 50, 0, 0, 5, 120, 120, 104, 54, 52, 0, 1, 10, 152, 9, 2, 242, 2, 1, 4, 127, 32, 0, 32, 1, 106, 33, 3, 32, 1, 32, 1, 65, 16, 79, 4, 127, 32, 3, 65, 16, 107, 33, 6, 32, 2, 65, 168, 136, 141, 161, 2, 106, 33, 1, 32, 2, 65, 137, 235, 208, 208, 7, 107, 33, 4, 32, 2, 65, 207, 140, 162, 142, 6, 106, 33, 5, 3, 64, 32, 1, 32, 0, 40, 2, 0, 65, 247, 148, 175, 175, 120, 108, 106, 65, 13, 119, 65, 177, 243, 221, 241, 121, 108, 33, 1, 32, 4, 32, 0, 65, 4, 106, 34, 0, 40, 2, 0, 65, 247, 148, 175, 175, 120, 108, 106, 65, 13, 119, 65, 177, 243, 221, 241, 121, 108, 33, 4, 32, 2, 32, 0, 65, 4, 106, 34, 0, 40, 2, 0, 65, 247, 148, 175, 175, 120, 108, 106, 65, 13, 119, 65, 177, 243, 221, 241, 121, 108, 33, 2, 32, 5, 32, 0, 65, 4, 106, 34, 0, 40, 2, 0, 65, 247, 148, 175, 175, 120, 108, 106, 65, 13, 119, 65, 177, 243, 221, 241, 121, 108, 33, 5, 32, 6, 32, 0, 65, 4, 106, 34, 0, 79, 13, 0, 11, 32, 2, 65, 12, 119, 32, 5, 65, 18, 119, 106, 32, 4, 65, 7, 119, 106, 32, 1, 65, 1, 119, 106, 5, 32, 2, 65, 177, 207, 217, 178, 1, 106, 11, 106, 33, 2, 3, 64, 32, 3, 32, 0, 65, 4, 106, 79, 4, 64, 32, 2, 32, 0, 40, 2, 0, 65, 189, 220, 202, 149, 124, 108, 106, 65, 17, 119, 65, 175, 214, 211, 190, 2, 108, 33, 2, 32, 0, 65, 4, 106, 33, 0, 12, 1, 11, 11, 3, 64, 32, 0, 32, 3, 73, 4, 64, 32, 2, 32, 0, 45, 0, 0, 65, 177, 207, 217, 178, 1, 108, 106, 65, 11, 119, 65, 177, 243, 221, 241, 121, 108, 33, 2, 32, 0, 65, 1, 106, 33, 0, 12, 1, 11, 11, 32, 2, 32, 2, 65, 15, 118, 115, 65, 247, 148, 175, 175, 120, 108, 34, 0, 65, 13, 118, 32, 0, 115, 65, 189, 220, 202, 149, 124, 108, 34, 0, 65, 16, 118, 32, 0, 115, 11, 161, 6, 2, 4, 126, 3, 127, 32, 0, 65, 4, 106, 53, 2, 0, 32, 0, 53, 2, 0, 66, 32, 134, 132, 33, 2, 32, 1, 32, 0, 65, 8, 106, 34, 6, 106, 33, 7, 32, 1, 65, 32, 79, 4, 126, 32, 7, 65, 32, 107, 33, 8, 32, 2, 66, 214, 235, 130, 238, 234, 253, 137, 245, 224, 0, 124, 33, 3, 32, 2, 66, 177, 169, 172, 193, 173, 184, 212, 166, 61, 125, 33, 4, 32, 2, 66, 249, 234, 208, 208, 231, 201, 161, 228, 225, 0, 124, 33, 5, 3, 64, 32, 3, 32, 6, 41, 3, 0, 66, 207, 214, 211, 190, 210, 199, 171, 217, 66, 126, 124, 66, 31, 137, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 33, 3, 32, 4, 32, 6, 65, 8, 106, 34, 6, 41, 3, 0, 66, 207, 214, 211, 190, 210, 199, 171, 217, 66, 126, 124, 66, 31, 137, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 33, 4, 32, 2, 32, 6, 65, 8, 106, 34, 6, 41, 3, 0, 66, 207, 214, 211, 190, 210, 199, 171, 217, 66, 126, 124, 66, 31, 137, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 33, 2, 32, 5, 32, 6, 65, 8, 106, 34, 6, 41, 3, 0, 66, 207, 214, 211, 190, 210, 199, 171, 217, 66, 126, 124, 66, 31, 137, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 33, 5, 32, 8, 32, 6, 65, 8, 106, 34, 6, 79, 13, 0, 11, 32, 2, 66, 12, 137, 32, 5, 66, 18, 137, 124, 32, 4, 66, 7, 137, 124, 32, 3, 66, 1, 137, 124, 32, 3, 66, 207, 214, 211, 190, 210, 199, 171, 217, 66, 126, 66, 31, 137, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 133, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 66, 157, 163, 181, 234, 131, 177, 141, 138, 250, 0, 125, 32, 4, 66, 207, 214, 211, 190, 210, 199, 171, 217, 66, 126, 66, 31, 137, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 133, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 66, 157, 163, 181, 234, 131, 177, 141, 138, 250, 0, 125, 32, 2, 66, 207, 214, 211, 190, 210, 199, 171, 217, 66, 126, 66, 31, 137, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 133, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 66, 157, 163, 181, 234, 131, 177, 141, 138, 250, 0, 125, 32, 5, 66, 207, 214, 211, 190, 210, 199, 171, 217, 66, 126, 66, 31, 137, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 133, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 66, 157, 163, 181, 234, 131, 177, 141, 138, 250, 0, 125, 5, 32, 2, 66, 197, 207, 217, 178, 241, 229, 186, 234, 39, 124, 11, 32, 1, 173, 124, 33, 2, 3, 64, 32, 7, 32, 6, 65, 8, 106, 79, 4, 64, 32, 2, 32, 6, 41, 3, 0, 66, 207, 214, 211, 190, 210, 199, 171, 217, 66, 126, 66, 31, 137, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 133, 66, 27, 137, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 66, 157, 163, 181, 234, 131, 177, 141, 138, 250, 0, 125, 33, 2, 32, 6, 65, 8, 106, 33, 6, 12, 1, 11, 11, 32, 6, 65, 4, 106, 32, 7, 77, 4, 64, 32, 2, 32, 6, 53, 2, 0, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 133, 66, 23, 137, 66, 207, 214, 211, 190, 210, 199, 171, 217, 66, 126, 66, 249, 243, 221, 241, 153, 246, 153, 171, 22, 124, 33, 2, 32, 6, 65, 4, 106, 33, 6, 11, 3, 64, 32, 6, 32, 7, 73, 4, 64, 32, 2, 32, 6, 49, 0, 0, 66, 197, 207, 217, 178, 241, 229, 186, 234, 39, 126, 133, 66, 11, 137, 66, 135, 149, 175, 175, 152, 182, 222, 155, 158, 127, 126, 33, 2, 32, 6, 65, 1, 106, 33, 6, 12, 1, 11, 11, 32, 0, 32, 2, 32, 2, 66, 33, 136, 133, 66, 207, 214, 211, 190, 210, 199, 171, 217, 66, 126, 34, 2, 66, 29, 136, 32, 2, 133, 66, 249, 243, 221, 241, 153, 246, 153, 171, 22, 126, 34, 2, 66, 32, 136, 32, 2, 133, 34, 2, 66, 32, 136, 62, 2, 0, 32, 0, 65, 4, 106, 32, 2, 62, 2, 0, 11]);
    var e;
    function n(t2, e2, n2) {
      if (e2.buffer.byteLength < t2.byteLength + n2) {
        const r = Math.ceil((t2.byteLength + n2 - e2.buffer.byteLength) / 65536);
        e2.grow(r);
      }
      new Uint8Array(e2.buffer, n2).set(t2);
    }
    module2.exports = async function() {
      const { instance: { exports: { mem: r, xxh32: i, xxh64: u } } } = await WebAssembly.instantiate(t);
      function o(t2, e2 = 0) {
        return n(t2, r, 0), i(0, t2.byteLength, e2) >>> 0;
      }
      function c(t2, e2 = 0, i2 = 0) {
        n(t2, r, 8);
        const o2 = new DataView(r.buffer);
        return o2.setUint32(0, e2, true), o2.setUint32(4, i2, true), u(0, t2.byteLength), o2;
      }
      return { h32: function(t2, n2 = 0) {
        return e || (e = new (require("util")).TextEncoder()), o(e.encode(t2), n2).toString(16);
      }, h32Raw: o, h64: function(t2, n2 = 0, r2 = 0) {
        e || (e = new (require("util")).TextEncoder());
        const i2 = c(e.encode(t2), n2, r2);
        return i2.getUint32(0, true).toString(16) + i2.getUint32(4, true).toString(16);
      }, h64Raw: function(t2, e2 = 0, n2 = 0) {
        return new Uint8Array(c(t2, e2, n2).buffer, 0, 8);
      } };
    };
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/common/util.js
var require_util3 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/common/util.js"(exports2, module2) {
    var xxhash = require_xxhash_wasm();
    var hasher;
    module2.exports = {
      async getChecksum(buffer) {
        if (!hasher) {
          hasher = await xxhash();
        }
        const hash = hasher.h64Raw(buffer);
        const hi = BigInt(hash[0] | hash[1] << 8 | hash[2] << 16 | hash[3] << 24);
        const lo = BigInt(hash[4] | hash[5] << 8 | hash[6] << 16 | hash[7] << 24);
        return BigInt.asUintN(32, hi) << 32n | BigInt.asUintN(32, lo);
      }
    };
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.3/SubChunk.js
var require_SubChunk = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.3/SubChunk.js"(exports2, module2) {
    var PalettedStorage = require_PalettedStorage();
    var { StorageType } = require_constants2();
    var { getChecksum } = require_util3();
    var neededBits = require_neededBits();
    var Stream = require_Stream();
    var nbt = require_nbt2();
    var SubChunk = class {
      constructor(registry, Block, options = {}) {
        this.registry = registry;
        if (!registry) {
          throw new Error("registry is required");
        }
        this.Block = Block;
        this.y = options.y;
        this.palette = options.palette || [];
        this.blocks = [];
        if (options.blocks) {
          for (const block of options.blocks) {
            this.blocks.push(PalettedStorage.copyFrom(block));
          }
        }
        this.subChunkVersion = options.subChunkVersion || 8;
        this.hash = options.hash || null;
        this.updated = options.updated || true;
        this.blockLight = options.blockLight ? PalettedStorage.copyFrom(options.blockLight) : new PalettedStorage(4);
        this.skyLight = options.skyLight ? PalettedStorage.copyFrom(options.skyLight) : new PalettedStorage(4);
      }
      // Creates an air chunk
      static create(registry, Block, y) {
        const subChunk = new this(registry, Block, { y });
        subChunk.blocks.push(new PalettedStorage(1));
        subChunk.palette.push([]);
        subChunk.addToPalette(0, subChunk.registry.blocksByName.air.defaultState, 4096);
        return subChunk;
      }
      decode(format, stream) {
        if (stream instanceof Buffer)
          stream = new Stream(stream);
        this.subChunkVersion = stream.readByte();
        let storageCount = 1;
        switch (this.subChunkVersion) {
          case 1:
            this.subChunkVersion = 8;
            break;
          case 8:
          case 9:
            storageCount = stream.readByte();
            if (this.subChunkVersion >= 9) {
              this.y = stream.readByte();
            }
            if (storageCount > 2) {
              throw new Error("Expected storage count to be 1 or 2, got " + storageCount);
            }
            break;
          default:
            throw new Error("Unsupported sub chunk version: " + this.subChunkVersion);
        }
        for (let i = 0; i < storageCount; i++) {
          const paletteType = stream.readByte();
          const usingNetworkRuntimeIds = paletteType & 1;
          if (!usingNetworkRuntimeIds && format === StorageType.Runtime) {
            throw new Error(`Expected network encoding while decoding SubChunk at y=${this.y}`);
          }
          const bitsPerBlock = paletteType >> 1;
          this.loadPalettedBlocks(i, stream, bitsPerBlock, format);
        }
      }
      loadPalettedBlocks(storageLayer, stream, bitsPerBlock, format) {
        const storage = new PalettedStorage(bitsPerBlock);
        storage.read(stream);
        this.blocks[storageLayer] = storage;
        const paletteSize = format === StorageType.LocalPersistence ? stream.readUInt32LE() : stream.readZigZagVarInt();
        if (paletteSize > stream.buffer.length || paletteSize < 1)
          throw new Error(`Invalid palette size: ${paletteSize}`);
        if (format === StorageType.Runtime) {
          this.loadRuntimePalette(storageLayer, stream, paletteSize);
        } else {
          this.loadLocalPalette(storageLayer, stream, paletteSize, format === StorageType.NetworkPersistence);
        }
        this.blocks[storageLayer].incrementPalette(this.palette[storageLayer]);
      }
      loadRuntimePalette(storageLayer, stream, paletteSize) {
        this.palette[storageLayer] = [];
        for (let i = 0; i < paletteSize; i++) {
          const index = stream.readZigZagVarInt();
          const block = this.registry.blockStates[index];
          this.palette[storageLayer][i] = { stateId: index, ...block, count: 0 };
        }
      }
      loadLocalPalette(storageLayer, stream, paletteSize, overNetwork) {
        this.palette[storageLayer] = [];
        const buf = stream.buffer;
        let startOffset = stream.readOffset;
        let i;
        for (i = 0; i < paletteSize; i++) {
          const { metadata, data } = nbt.protos[overNetwork ? "littleVarint" : "little"].parsePacketBuffer("nbt", buf, startOffset);
          stream.readOffset += metadata.size;
          startOffset += metadata.size;
          const { name, states, version } = nbt.simplify(data);
          let block = this.Block.fromProperties(name.replace("minecraft:", ""), states ?? {}, 0);
          if (!block) {
            debugger;
            block = this.Block.fromProperties("air", {}, 0);
          }
          this.palette[storageLayer][i] = { stateId: block.stateId, name, states: data.value.states.value, version, count: 0 };
        }
        if (i !== paletteSize) {
          throw new Error(`Expected ${paletteSize} blocks, got ${i}`);
        }
      }
      async encode(format, checksum = false, compact = true) {
        const stream = new Stream();
        if (this.subChunkVersion >= 8) {
          this.encodeV8(stream, format, compact);
        } else {
          throw new Error("Unsupported sub chunk version");
        }
        const buf = stream.getBuffer();
        if (checksum) {
          this.hash = await getChecksum(buf);
          this.updated = false;
        }
        return buf;
      }
      // Encode sub chunk version 8+
      encodeV8(stream, format, compact) {
        stream.writeUInt8(this.subChunkVersion);
        stream.writeUInt8(this.blocks.length);
        if (this.subChunkVersion >= 9) {
          stream.writeUInt8(this.y);
        }
        for (let l = 0; l < this.blocks.length; l++) {
          if (compact)
            this.compact(l);
          this.writeStorage(stream, l, format);
        }
      }
      writeStorage(stream, storageLayer, format) {
        const storage = this.blocks[storageLayer];
        let paletteType = storage.bitsPerBlock << 1;
        if (format === StorageType.Runtime) {
          paletteType |= 1;
        }
        stream.writeUInt8(paletteType);
        storage.write(stream);
        if (format === StorageType.LocalPersistence) {
          stream.writeUInt32LE(this.palette[storageLayer].length);
        } else {
          stream.writeZigZagVarInt(this.palette[storageLayer].length);
        }
        if (format === StorageType.Runtime) {
          for (const block of this.palette[storageLayer]) {
            stream.writeZigZagVarInt(block.stateId);
          }
        } else {
          for (const block of this.palette[storageLayer]) {
            const { name, states, version } = block;
            const tag = nbt.comp({ name: nbt.string(name), states: nbt.comp(states), version: nbt.int(version) });
            const buf = nbt.writeUncompressed(tag, format === StorageType.LocalPersistence ? "little" : "littleVarint");
            stream.writeBuffer(buf);
          }
        }
      }
      // Normal block access
      getBlock(l, x, y, z, biomeId) {
        if (l !== void 0) {
          const stateId = this.getBlockStateId(l, x, y, z);
          return this.Block.fromStateId(stateId, biomeId);
        } else {
          const layer1 = this.getBlockStateId(0, x, y, z);
          const layer2 = this.getBlockStateId(1, x, y, z);
          const block = this.Block.fromStateId(layer1, biomeId);
          if (layer2) {
            block.superimposed = this.Block.fromStateId(layer2, biomeId);
            const name = block.superimposed.name;
            if (name.includes("water")) {
              block.computedStates.waterlogged = true;
            }
          }
          return block;
        }
      }
      setBlock(l, x, y, z, block) {
        if (l !== void 0) {
          this.setBlockStateId(l, x, y, z, block.stateId);
        } else {
          this.setBlockStateId(0, x, y, z, block.stateId);
          if (block.superimposed) {
            this.setBlockStateId(1, x, y, z, block.superimposed.stateId);
          }
        }
        this.updated = true;
      }
      getBlockStateId(l = 0, x, y, z) {
        const blocks = this.blocks[l];
        if (!blocks) {
          return this.registry.blocksByName.air.defaultState;
        }
        return this.palette[l][blocks.get(x, y, z)].stateId;
      }
      setBlockStateId(l = 0, x, y, z, stateId) {
        if (!this.palette[l]) {
          this.palette[l] = [];
          this.blocks[l] = new PalettedStorage(4);
          this.addToPalette(l, this.registry.blocksByName.air.defaultState, 4096 - 1);
          this.addToPalette(l, stateId, 1);
          this.blocks[l].set(x, y, z, this.palette[l].length - 1);
        } else {
          const currentIndex = this.blocks[l].get(x, y, z);
          const currentEntry = this.palette[l][currentIndex];
          if (currentEntry.stateId === stateId) {
            return;
          }
          currentEntry.count--;
          for (let i = 0; i < this.palette[l].length; i++) {
            const entry = this.palette[l][i];
            if (entry.stateId === stateId) {
              entry.count = Math.max(entry.count, 0) + 1;
              this.blocks[l].set(x, y, z, i);
              return;
            }
          }
          this.addToPalette(l, stateId, 1);
          this.blocks[l].set(x, y, z, this.palette[l].length - 1);
        }
        this.updated = true;
      }
      addToPalette(l, stateId, count = 0) {
        const block = this.registry.blockStates[stateId];
        this.palette[l].push({ stateId, name: block.name, states: block.states, count });
        const minBits = neededBits(this.palette[l].length - 1);
        if (minBits > this.blocks[l].bitsPerBlock) {
          this.blocks[l] = this.blocks[l].resize(minBits);
        }
      }
      // These compation functions reduces the size of the chunk by removing unused blocks
      // and reordering the palette to reduce the number of bits per block
      isCompactable(layer) {
        let newPaletteLength = 0;
        for (const block of this.palette[layer]) {
          if (block.count > 0) {
            newPaletteLength++;
          }
        }
        return newPaletteLength < this.palette[layer].length;
      }
      compact(layer) {
        const newPalette = [];
        const map = [];
        for (const block of this.palette[layer]) {
          if (block.count > 0) {
            newPalette.push(block);
          }
          map.push(newPalette.length - 1);
        }
        if (newPalette.length === this.palette[layer].length) {
          return;
        }
        const newStorage = new PalettedStorage(neededBits(newPalette.length - 1));
        for (let x = 0; x < 16; x++) {
          for (let y = 0; y < 16; y++) {
            for (let z = 0; z < 16; z++) {
              const ix = this.blocks[layer].get(x, y, z);
              newStorage.set(x, y, z, map[ix]);
            }
          }
        }
        this.blocks[layer] = newStorage;
        this.palette[layer] = newPalette;
      }
      /**
       * Gets the block runtime ID at the layer and position
       * @returns Global block palette (runtime) ID for the block
       */
      getPaletteEntry(l, x, y, z) {
        return this.palette[l][this.blocks[l].get(x, y, z)];
      }
      getPalette(layer = 0) {
        return this.palette[layer].filter((block) => block.count > 0);
      }
      // Lighting - Not written or read, but computed during chunk loading
      getBlockLight(x, y, z) {
        return this.blockLight.get(x, y, z);
      }
      setBlockLight(x, y, z, value) {
        this.blockLight.set(x, y, z, value);
      }
      getSkyLight(x, y, z) {
        return this.skyLight.get(x, y, z);
      }
      setSkyLight(x, y, z, value) {
        this.skyLight.set(x, y, z, value);
      }
      toObject() {
        return {
          y: this.y,
          palette: this.palette,
          blocks: this.blocks,
          subChunkVersion: this.subChunkVersion,
          hash: this.hash,
          updated: this.updated,
          blockLight: this.blockLight,
          skyLight: this.skyLight
        };
      }
    };
    module2.exports = SubChunk;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.18/BiomeSection.js
var require_BiomeSection = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.18/BiomeSection.js"(exports2, module2) {
    var { StorageType } = require_constants2();
    var PalettedStorage = require_PalettedStorage();
    var neededBits = require_neededBits();
    var BiomeSection = class {
      constructor(registry, y, options = {}) {
        this.Biome = require_prismarine_biome()(registry);
        this.y = y;
        this.biomes = options.biomes ? PalettedStorage.copyFrom(options.biomes) : new PalettedStorage(1);
        this.palette = options.palette || [0];
      }
      readLegacy2D(stream) {
        for (let x = 0; x < 16; x++) {
          for (let z = 0; z < 16; z++) {
            this.setBiomeId(x, 0, z, stream.readByte());
          }
        }
      }
      copy(other) {
        this.biomes = PalettedStorage.copyFrom(other.biomes);
        this.palette = JSON.parse(JSON.stringify(other.palette));
        this.y = other.y;
      }
      read(type, buf, previousSection) {
        this.palette = [];
        const paletteType = buf.readByte();
        const usingNetworkRuntimeIds = paletteType & 1;
        if (usingNetworkRuntimeIds !== 1)
          throw new Error("Biome palette type must be set to use runtime IDs");
        const bitsPerBlock = paletteType >> 1;
        if (bitsPerBlock === 0) {
          this.palette.push(type === StorageType.LocalPersistence ? buf.readInt32LE() : buf.readVarInt() >> 1);
          return;
        }
        this.biomes = new PalettedStorage(bitsPerBlock);
        this.biomes.read(buf);
        if (type === StorageType.Runtime) {
          const biomePaletteLength = buf.readVarInt() >> 1;
          for (let i = 0; i < biomePaletteLength; i++) {
            this.palette.push(buf.readVarInt() >> 1);
          }
        } else {
          const biomePaletteLength = buf.readInt32LE();
          for (let i = 0; i < biomePaletteLength; i++) {
            this.palette.push(buf.readInt32LE());
          }
        }
      }
      // TODO: handle downsizing
      setBiomeId(x, y, z, biomeId) {
        if (!this.palette.includes(biomeId)) {
          this.palette.push(biomeId);
        }
        const minBits = neededBits(this.palette.length - 1);
        if (minBits > this.biomes.bitsPerBlock) {
          this.biomes = this.biomes.resize(minBits);
        }
        this.biomes.set(x, y, z, this.palette.indexOf(biomeId));
      }
      getBiomeId(x, y, z) {
        return this.palette[this.biomes.get(x, y, z)];
      }
      getBiome(pos) {
        return new this.Biome(this.getBiomeId(pos.x, pos.y, pos.z));
      }
      setBiome(pos, biome) {
        this.setBiomeId(pos.x, pos.y, pos.z, biome.id);
      }
      export(type, stream) {
        const bitsPerBlock = Math.ceil(Math.log2(this.palette.length));
        const paletteType = bitsPerBlock << 1 | type === StorageType.Runtime;
        stream.writeUInt8(paletteType);
        if (bitsPerBlock === 0) {
          if (type === StorageType.LocalPersistence) {
            stream.writeInt32LE(this.palette[0]);
          } else {
            stream.writeVarInt(this.palette[0] << 1);
          }
          return;
        }
        this.biomes.write(stream);
        if (type === StorageType.Runtime) {
          stream.writeVarInt(this.palette.length << 1);
          for (const biome of this.palette) {
            stream.writeVarInt(biome << 1);
          }
        } else {
          stream.writeUInt32LE(this.palette.length);
          for (const biome of this.palette) {
            stream.writeUInt32LE(biome);
          }
        }
      }
      // Just write the top most layer biomes
      exportLegacy2D(stream) {
        for (let x = 0; x < 16; x++) {
          for (let z = 0; z < 16; z++) {
            const y = 0;
            const biome = this.getBiomeId(x, y, z);
            stream.writeUInt8(biome);
          }
        }
      }
      toObject() {
        return {
          y: this.y,
          biomes: this.biomes,
          palette: this.palette
        };
      }
    };
    module2.exports = BiomeSection;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/common/BlobCache.js
var require_BlobCache = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/common/BlobCache.js"(exports2, module2) {
    var BlobType = {
      ChunkSection: 0,
      Biomes: 1
    };
    var BlobEntry = class {
      // When the blob was put into BlobCache store
      created = Date.now();
      // The type of blob
      type;
      constructor(args) {
        Object.assign(this, args);
      }
    };
    module2.exports = { BlobType, BlobEntry };
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.3/ChunkColumn.js
var require_ChunkColumn8 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.3/ChunkColumn.js"(exports2, module2) {
    var CommonChunkColumn = require_CommonChunkColumn2();
    var SubChunk = require_SubChunk();
    var BiomeSection = require_BiomeSection();
    var { StorageType } = require_constants2();
    var Stream = require_Stream();
    var { BlobType, BlobEntry } = require_BlobCache();
    var nbt = require_nbt2();
    var { getChecksum } = require_util3();
    var ChunkColumn13 = class extends CommonChunkColumn {
      Section = SubChunk;
      constructor(options = {}, registry, Block, Biome) {
        super(options, registry);
        this.Block = Block;
        this.Biome = Biome;
        this.biomes = [];
        this.sections = [];
        this.biomesUpdated = true;
        if (options.sections?.length) {
          for (const section of options.sections) {
            if (section) {
              this.sections.push(new this.Section(registry, Block, section));
            } else {
              this.sections.push(null);
            }
          }
        }
        if (options.biomes?.length) {
          for (const biome of options.biomes) {
            this.biomes.push(biome ? new BiomeSection(registry, biome.y, biome) : null);
          }
        } else {
          this.biomes.push(new BiomeSection(registry, 0));
        }
      }
      getBiome(pos) {
        return new this.Biome(this.biomes[0].getBiomeId(pos.x, 0, pos.z));
      }
      setBiome(pos, biome) {
        this.biomes[0].setBiomeId(pos.x, 0, pos.z, biome.id);
        this.biomesUpdated = true;
      }
      getBiomeId(pos) {
        return this.biomes[0].getBiomeId(pos.x, 0, pos.z);
      }
      setBiomeId(pos, biome) {
        this.biomes[0].setBiomeId(pos.x, 0, pos.z, biome);
        this.biomesUpdated = true;
      }
      loadLegacyBiomes(buf) {
        if (buf instanceof Buffer)
          buf = new Stream(buf);
        const biome = new BiomeSection(this.registry, 0);
        biome.readLegacy2D(buf);
        this.biomes = [biome];
      }
      // Load heightmap data
      /** @type Uint16Array */
      loadHeights(heightmap) {
        this.heights = heightmap;
      }
      getHeights() {
        return this.heights;
      }
      writeLegacyBiomes(stream) {
        this.biomes[0].exportLegacy2D(stream);
      }
      writeHeightMap(stream) {
        if (!this.heights) {
          this.heights = new Uint16Array(256);
        }
        for (let i = 0; i < 256; i++) {
          stream.writeUInt16LE(this.heights[i]);
        }
      }
      async updateBiomeHash(fromBuf) {
        this.biomesUpdated = false;
        this.biomesHash = await getChecksum(fromBuf);
        return this.biomesHash;
      }
      /**
       * Encodes this chunk column for the network with no caching
       * @param buffer Full chunk buffer
       */
      async networkEncodeNoCache() {
        const tileBufs = [];
        for (const key in this.blockEntities) {
          const tile = this.blockEntities[key];
          tileBufs.push(nbt.writeUncompressed(tile, "littleVarint"));
        }
        let biomeBuf;
        const stream = new Stream(Buffer.alloc(256));
        if (this.biomes[0]) {
          this.biomes[0].exportLegacy2D(stream);
          biomeBuf = stream.buffer;
        } else {
          throw Error("No biome section");
        }
        const sectionBufs = [];
        for (const section of this.sections) {
          sectionBufs.push(await section.encode(StorageType.Runtime, false, this.compactOnSave));
        }
        return Buffer.concat([
          ...sectionBufs,
          biomeBuf,
          Buffer.from([0]),
          // border blocks count
          ...tileBufs
          // block entities
        ]);
      }
      /**
       * Encodes this chunk column for use over network with caching enabled
       *
       * @param blobStore The blob store to write chunks in this section to
       * @returns {Promise<Buffer[]>} The blob hashes for this chunk, the last one is biomes, rest are sections
       */
      async networkEncodeBlobs(blobStore) {
        const blobHashes = [];
        for (const section of this.sections) {
          if (section.updated || !blobStore.get(section.hash)) {
            const buffer = await section.encode(StorageType.NetworkPersistence, true, this.compactOnSave);
            const blob = new BlobEntry({ x: this.x, y: section.y, z: this.z, type: BlobType.ChunkSection, buffer });
            blobStore.set(section.hash, blob);
          }
          blobHashes.push({ hash: section.hash, type: BlobType.ChunkSection });
        }
        if (this.biomesUpdated || !this.biomesHash || !blobStore.get(this.biomesHash)) {
          if (this.biomes[0]) {
            const stream = new Stream();
            this.biomes[0].exportLegacy2D(stream);
            await this.updateBiomeHash(stream.getBuffer());
          } else {
            await this.updateBiomeHash(Buffer.alloc(256));
          }
          this.biomesUpdated = false;
          blobStore.set(this.biomesHash, new BlobEntry({ x: this.x, z: this.z, type: BlobType.Biomes, buffer: this.biomes }));
        }
        blobHashes.push({ hash: this.biomesHash, type: BlobType.Biomes });
        return blobHashes;
      }
      async networkEncode(blobStore) {
        const blobs = await this.networkEncodeBlobs(blobStore);
        const tileBufs = [];
        for (const key in this.blockEntities) {
          const tile = this.blockEntities[key];
          tileBufs.push(nbt.writeUncompressed(tile, "littleVarint"));
        }
        return {
          blobs,
          // cache blobs
          payload: Buffer.concat([
            // non-cached stuff
            Buffer.from([0]),
            // border blocks
            ...tileBufs
          ])
        };
      }
      networkDecodeNoCache(buffer, sectionCount) {
        const stream = buffer instanceof Buffer ? new Stream(buffer) : buffer;
        if (sectionCount === -1) {
          throw new RangeError("-1 section count not supported below 1.18");
        }
        this.sections = [];
        for (let i = 0; i < sectionCount; i++) {
          const section = new SubChunk(this.registry, this.Block, { y: i, subChunkVersion: this.subChunkVersion });
          section.decode(StorageType.Runtime, stream);
          this.setSection(i, section);
        }
        const biomes = new BiomeSection(this.registry, 0);
        biomes.readLegacy2D(stream);
        this.biomes = [biomes];
        const borderBlocksLength = stream.readZigZagVarInt();
        const borderBlocks = stream.readBuffer(borderBlocksLength);
        if (borderBlocks.length)
          throw Error(`Read ${borderBlocksLength} border blocks, expected 0`);
        let startOffset = stream.readOffset;
        while (stream.peek() === 10) {
          const { data, metadata } = nbt.protos.littleVarint.parsePacketBuffer("nbt", stream.buffer, startOffset);
          stream.readOffset += metadata.size;
          startOffset += metadata.size;
          this.addBlockEntity(data);
        }
      }
      /**
       * Decodes cached chunks sent over the network
       * @param blobs The blob hashes sent in the Chunk packet
       * @param blobStore Our blob store for cached data
       * @param {Buffer} payload The rest of the non-cached data
       * @returns {CCHash[]} A list of hashes we don't have and need. If len > 0, decode failed.
       */
      async networkDecode(blobs, blobStore, payload) {
        blobs = [...blobs];
        if (!blobs.length) {
          throw new Error("No blobs to decode");
        }
        if (payload) {
          const stream = new Stream(payload);
          const borderblocks = stream.readBuffer(stream.readByte());
          if (borderblocks.length) {
            throw new Error("cannot handle border blocks (read length: " + borderblocks.length + ")");
          }
          let startOffset = stream.readOffset;
          while (stream.peek() === 10) {
            const { metadata, data } = nbt.protos.littleVarint.parsePacketBuffer("nbt", payload, startOffset);
            stream.readOffset += metadata.size;
            startOffset += metadata.size;
            this.addBlockEntity(data);
          }
        }
        const misses = [];
        for (const blob of blobs) {
          if (!blobStore.has(blob)) {
            misses.push(blob);
          }
        }
        if (misses.length > 0) {
          return misses;
        }
        this.sections = [];
        this.sectionsLen = 0;
        const biomeBlob = blobs.pop();
        for (let i = 0; i < blobs.length; i++) {
          const blob = blobStore.get(blobs[i]);
          const section = new SubChunk(this.registry, this.Block, { y: i, subChunkVersion: this.subChunkVersion });
          section.decode(StorageType.NetworkPersistence, blob.buffer);
          this.setSection(i, section);
        }
        const biomeEntry = blobStore.get(biomeBlob);
        this.loadLegacyBiomes(biomeEntry.buffer);
        return misses;
      }
      toObject() {
        const biomes = this.biomes.map((b) => b?.toObject());
        return { ...super.toObject(), biomes, biomesUpdated: this.biomesUpdated, version: this.registry.version.minecraftVersion };
      }
      toJson() {
        return JSON.stringify(this.toObject());
      }
    };
    module2.exports = ChunkColumn13;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.3/chunk.js
var require_chunk11 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.3/chunk.js"(exports2, module2) {
    var { ChunkVersion } = require_constants2();
    var ChunkColumn = require_ChunkColumn8();
    module2.exports = (version) => {
      const registry = version.blockRegistry || version;
      const Block = require_prismarine_block()(registry);
      const Biome = require_prismarine_biome()(registry);
      return class Chunk extends ChunkColumn {
        constructor(options) {
          super(options, registry, Block, Biome);
          this.chunkVersion = this.chunkVersion || ChunkVersion.v1_16_0;
          this.subChunkVersion = 8;
        }
        static fromJson(str) {
          return new this(JSON.parse(str));
        }
      };
    };
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.18/SubChunk.js
var require_SubChunk2 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.18/SubChunk.js"(exports2, module2) {
    var SubChunk13 = require_SubChunk();
    var { StorageType } = require_constants2();
    var PalettedStorage = require_PalettedStorage();
    var SubChunk118 = class extends SubChunk13 {
      loadRuntimePalette(storageLayer, stream, paletteSize) {
        this.palette[storageLayer] = [];
        for (let i = 0; i < paletteSize; i++) {
          const runtimeId = stream.readZigZagVarInt();
          const block = this.registry.blocksByRuntimeId[runtimeId];
          this.palette[storageLayer][i] = { stateId: block.stateId, ...block, count: 0 };
        }
      }
      loadPalettedBlocks(storageLayer, stream, bitsPerBlock, format) {
        if (format === StorageType.Runtime && bitsPerBlock === 0) {
          this.palette[storageLayer] = [];
          this.blocks[storageLayer] = new PalettedStorage(1);
          const stateId = stream.readVarInt() >> 1;
          this.addToPalette(storageLayer, stateId);
          return;
        }
        return super.loadPalettedBlocks(...arguments);
      }
      writeStorage(stream, storageLayer, format) {
        if (format === StorageType.Runtime && this.palette[storageLayer].length === 1) {
          stream.writeUInt8(1);
          stream.writeZigZagVarInt(this.palette[storageLayer][0].stateId);
          return;
        }
        return super.writeStorage(...arguments);
      }
    };
    module2.exports = SubChunk118;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.18/ProxyBiomeSection.js
var require_ProxyBiomeSection = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.18/ProxyBiomeSection.js"(exports2, module2) {
    var BiomeSection = require_BiomeSection();
    var ProxyBiomeSection = class {
      constructor(registry, target) {
        this.registry = registry;
        this.target = target;
      }
      getBiome(pos) {
        return this.target.getBiome(pos);
      }
      getBiomeId(pos) {
        return this.target.getBiomeId(pos);
      }
      copy(other) {
        return this.target.copy(other);
      }
      promote(y) {
        const biome = new BiomeSection(this.registry, y);
        biome.copy(this.target);
        return biome;
      }
      export(format, stream) {
        stream.writeUInt8(255);
      }
    };
    module2.exports = ProxyBiomeSection;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.18/ChunkColumn.js
var require_ChunkColumn9 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.18/ChunkColumn.js"(exports2, module2) {
    var ChunkColumn13 = require_ChunkColumn8();
    var SubChunk = require_SubChunk2();
    var BiomeSection = require_BiomeSection();
    var ProxyBiomeSection = require_ProxyBiomeSection();
    var { BlobType, BlobEntry } = require_BlobCache();
    var { StorageType } = require_constants2();
    var Stream = require_Stream();
    var nbt = require_nbt2();
    var ChunkColumn180 = class extends ChunkColumn13 {
      Section = SubChunk;
      bounds = this.setBounds(-4, 20);
      // World height changed
      // BIOMES
      getBiome(pos) {
        return new this.Biome(this.getBiomeId(pos));
      }
      setBiome(pos, biome) {
        this.setBiomeId(pos, biome.id);
      }
      getBiomeId(pos) {
        const Y = pos.y >> 4;
        const sec = this.biomes[this.co + Y];
        return sec?.getBiomeId(pos.x, pos.y & 15, pos.z) ?? 0;
      }
      setBiomeId(pos, biomeId) {
        const Y = pos.y >> 4;
        let sec = this.biomes[this.co + Y];
        if (!sec) {
          this.biomes[this.co + Y] = sec = new BiomeSection(this.registry, Y);
        } else if (!sec.setBiomeId) {
          this.biomes[this.co + Y] = sec = sec.promote(Y);
        }
        sec.setBiomeId(pos.x, pos.y & 15, pos.z, biomeId);
        this.biomesUpdated = true;
      }
      // Load 3D biome data
      loadBiomes(buf, storageType) {
        if (buf instanceof Buffer)
          buf = new Stream(buf);
        this.biomes = [];
        let last;
        for (let y = this.minCY; buf.peek(); y++) {
          if (buf.peek() === 255) {
            if (!last)
              throw new Error("No last biome");
            const biome = new ProxyBiomeSection(this.registry, last);
            this.biomes.push(biome);
            buf.readByte();
          } else {
            const biome = new BiomeSection(this.registry, y);
            biome.read(storageType, buf);
            last = biome;
            this.biomes.push(biome);
          }
        }
      }
      writeBiomes(stream) {
        for (let i = 0; i < this.worldHeight >> 4; i++) {
          let biomeSection = this.biomes[i];
          if (!biomeSection) {
            if (this.biomes[i - 1]) {
              this.biomes[i] = biomeSection = new ProxyBiomeSection(this.registry, this.biomes[i - 1]);
            } else {
              this.biomes[i] = biomeSection = new BiomeSection(this.registry, i);
            }
          }
          biomeSection.export(StorageType.Runtime, stream);
        }
      }
      // CACHING
      /**
       * Encodes this chunk column for the network with no caching
       * @param buffer Full chunk buffer
       */
      async networkEncodeNoCache() {
        const stream = new Stream();
        for (const biomeSection of this.biomes) {
          biomeSection.export(StorageType.Runtime, stream);
        }
        const biomeBuf = stream.getBuffer();
        return Buffer.concat([
          biomeBuf,
          Buffer.from([0])
          // border blocks count
        ]);
      }
      /**
       * Encodes this chunk column for use over network with caching enabled
       *
       * @param blobStore The blob store to write chunks in this section to
       * @returns {Promise<Buffer[]>} The blob hashes for this chunk, the last one is biomes, rest are sections
       */
      async networkEncodeBlobs(blobStore, includeBiomes) {
        const blobHashes = [];
        if (includeBiomes) {
          if (this.biomesUpdated || !this.biomesHash || !blobStore.get(this.biomesHash.toString())) {
            const stream = new Stream();
            for (const biomeSection of this.biomes) {
              biomeSection.export(StorageType.Runtime, stream);
            }
            const biomeBuf = stream.getBuffer();
            await this.updateBiomeHash(biomeBuf);
            this.biomesUpdated = false;
            blobStore.set(this.biomesHash.toString(), new BlobEntry({ x: this.x, z: this.z, type: BlobType.Biomes, buffer: this.biomes }));
          }
          blobHashes.push({ hash: this.biomesHash, type: BlobType.Biomes });
        }
        return blobHashes;
      }
      async networkEncode(blobStore) {
        const blobs = await this.networkEncodeBlobs(blobStore, true, false);
        return {
          blobs,
          // cache blobs
          payload: Buffer.concat([
            // non-cached stuff
            Buffer.from([0])
            // border blocks
          ])
        };
      }
      networkDecodeNoCache(buffer, sectionCount) {
        const stream = buffer instanceof Buffer ? new Stream(buffer) : buffer;
        if (sectionCount !== -1 && sectionCount !== -2) {
          this.sections = [];
          for (let i = 0; i < sectionCount; i++) {
            const section = new SubChunk(this.registry, this.Block, { y: i, subChunkVersion: this.subChunkVersion });
            section.decode(StorageType.Runtime, stream);
            this.setSection(i, section);
          }
        }
        this.loadBiomes(stream, StorageType.Runtime);
        const borderBlocks = stream.readBuffer(stream.readZigZagVarInt());
        if (borderBlocks.length) {
          throw new Error(`Can't handle border blocks (length: ${borderBlocks.length})`);
        }
        let startOffset = stream.readOffset;
        while (stream.peek() === 10) {
          const { data, metadata } = nbt.protos.littleVarint.parsePacketBuffer("nbt", stream.buffer, startOffset);
          stream.readOffset += metadata.size;
          startOffset += metadata.size;
          this.addBlockEntity(data);
        }
      }
      /**
       * Decodes cached chunks sent over the network
       * @param blobs The blob hashes sent in the Chunk packet
       * @param blobStore Our blob store for cached data
       * @param {Buffer} payload The rest of the non-cached data
       * @returns {CCHash[]} A list of hashes we don't have and need. If len > 0, decode failed.
       */
      async networkDecode(blobs, blobStore, payload) {
        if (payload) {
          const stream = new Stream(payload);
          const borderblocks = stream.readBuffer(stream.readZigZagVarInt());
          if (borderblocks.length) {
            throw new Error(`Can't handle border blocks (length: ${borderblocks.length})`);
          }
        }
        const misses = [];
        for (const blob of blobs) {
          if (!blobStore.has(blob.toString())) {
            misses.push(blob);
          }
        }
        if (misses.length > 0) {
          return misses;
        }
        this.sections = [];
        for (const blob of blobs) {
          const entry = blobStore.get(blob.toString());
          if (entry.type === BlobType.Biomes) {
            const stream = new Stream(entry.buffer);
            this.loadBiomes(stream, StorageType.NetworkPersistence, blob);
          } else if (entry.type === BlobType.ChunkSection) {
            throw new Error("Can't accept chunk sections in networkDecode, these Blobs should be sent as individual sections");
          } else {
            throw Error("Unknown blob type: " + entry.type);
          }
        }
        return misses;
      }
      async networkDecodeSubChunkNoCache(y, buffer) {
        const stream = new Stream(buffer);
        const section = new SubChunk(this.registry, this.Block, { y, subChunkVersion: this.subChunkVersion });
        section.decode(StorageType.Runtime, stream);
        this.setSection(y, section);
        let startOffset = stream.readOffset;
        while (stream.peek() === 10) {
          const { data, metadata } = nbt.protos.littleVarint.parsePacketBuffer("nbt", buffer, startOffset);
          stream.readOffset += metadata.size;
          startOffset += metadata.size;
          this.addBlockEntity(data);
        }
      }
      async networkEncodeSubChunkNoCache(y) {
        const tiles = this.getSectionBlockEntities(y);
        const section = this.getSectionAtIndex(y);
        const subchunk = await section.encode(StorageType.Runtime, false, this.compactOnSave);
        const tileBufs = [];
        for (const tile of tiles) {
          tileBufs.push(nbt.writeUncompressed(tile, "littleVarint"));
        }
        return Buffer.concat([subchunk, ...tileBufs]);
      }
      async networkDecodeSubChunk(blobs, blobStore, payload) {
        if (payload) {
          const nbtStream = new Stream(payload);
          let startOffset = 0;
          while (nbtStream.peekUInt8() === 10) {
            const { data, metadata } = nbt.protos.littleVarint.parsePacketBuffer("nbt", payload, startOffset);
            nbtStream.readOffset += metadata.size;
            startOffset += metadata.size;
            this.addBlockEntity(data);
          }
        }
        const misses = [];
        for (const blob of blobs) {
          if (!blobStore.has(blob.toString())) {
            misses.push(blob);
          }
        }
        if (misses.length > 0) {
          return misses;
        }
        this.sections = [];
        this.sectionsLen = 0;
        for (const blob of blobs) {
          const entry = blobStore.get(blob.toString());
          const stream = new Stream(entry.buffer);
          const subchunk = new SubChunk(this.registry, this.Block, { y: blob.y, subChunkVersion: this.subChunkVersion });
          await subchunk.decode(StorageType.Runtime, stream);
          this.setSection(subchunk.y, subchunk);
        }
        return misses;
      }
      async networkEncodeSubChunk(y, blobStore) {
        const tiles = this.getSectionBlockEntities(y);
        const section = this.getSectionAtIndex(y);
        if (section.updated) {
          const terrainBuffer = await section.encode(StorageType.Runtime, true, this.compactOnSave);
          const blob = new BlobEntry({ x: this.x, y: section.y, z: this.z, type: BlobType.ChunkSection, buffer: terrainBuffer });
          blobStore.set(section.hash.toString(), blob);
        }
        const tileBufs = [];
        for (const tile of tiles) {
          tileBufs.push(nbt.writeUncompressed(tile, "littleVarint"));
        }
        return [section.hash, Buffer.concat(tileBufs)];
      }
      toObject() {
        return { ...super.toObject(), version: this.registry.version.minecraftVersion };
      }
    };
    module2.exports = ChunkColumn180;
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.18/chunk.js
var require_chunk12 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/bedrock/1.18/chunk.js"(exports2, module2) {
    var { ChunkVersion } = require_constants2();
    var ChunkColumn = require_ChunkColumn9();
    module2.exports = (version) => {
      const registry = version.blockRegistry || version;
      const Block = require_prismarine_block()(registry);
      const Biome = require_prismarine_biome()(registry);
      return class Chunk extends ChunkColumn {
        constructor(options) {
          super(options, registry, Block, Biome);
          this.chunkVersion = this.chunkVersion || ChunkVersion.v1_18_0;
          this.subChunkVersion = 9;
        }
        static fromJson(str) {
          return new this(JSON.parse(str));
        }
      };
    };
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/index.js
var require_src3 = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/src/index.js"(exports2, module2) {
    var chunkImplementations = {
      pc: {
        1.8: require_chunk(),
        1.9: require_chunk2(),
        "1.10": require_chunk2(),
        1.11: require_chunk2(),
        1.12: require_chunk2(),
        1.13: require_chunk3(),
        1.14: require_chunk4(),
        1.15: require_chunk5(),
        1.16: require_chunk6(),
        1.17: require_chunk7(),
        1.18: require_chunk8(),
        1.19: require_chunk8(),
        "1.20": require_chunk8(),
        1.21: require_chunk8()
      },
      bedrock: {
        0.14: require_chunk9(),
        "1.0": require_chunk10(),
        1.3: require_chunk11(),
        1.16: require_chunk11(),
        1.17: require_chunk11(),
        1.18: require_chunk12(),
        1.19: require_chunk12(),
        "1.20": require_chunk12(),
        1.21: require_chunk12()
      }
    };
    module2.exports = loader;
    var blobCache = require_BlobCache();
    module2.exports.BlobEntry = blobCache.BlobEntry;
    module2.exports.BlobType = blobCache.BlobType;
    function loader(registryOrVersion) {
      const registry = typeof registryOrVersion === "string" ? require_lib()(registryOrVersion) : registryOrVersion;
      const version = registry.version;
      if (!version)
        throw new Error("Specified version does not exist");
      try {
        return chunkImplementations[version.type][version.majorVersion](registry);
      } catch (e) {
        if (e instanceof TypeError) {
          console.error(e);
          throw new Error(`[Prismarine-chunk] No chunk implementation for ${version?.type} ${version?.majorVersion} found`);
        } else {
          console.log(`Error while loading ${version.type} - ${version.majorVersion}`);
          throw e;
        }
      }
    }
  }
});

// ../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/index.js
var require_prismarine_chunk = __commonJS({
  "../node_modules/.pnpm/prismarine-chunk@https+++codeload.github.com+zardoy+prismarine-chunk+tar.gz+ec44d01fbe2_3fd57bfc867a3079ae4e177de25ada11/node_modules/prismarine-chunk/index.js"(exports2, module2) {
    module2.exports = require_src3();
  }
});

// test-section-boundary.ts
var import_prismarine_chunk2 = __toESM(require_prismarine_chunk(), 1);

// ../node_modules/.pnpm/vec3@0.1.10/node_modules/vec3/wrapper.mjs
var import_index = __toESM(require_vec3(), 1);
var Vec3 = import_index.default.Vec3;

// test-section-boundary.ts
var wasm = __toESM(require("./pkg/wasm_mesher.js"), 1);

// ../src/wasm-lib/convertChunk.ts
var import_minecraft_data = __toESM(require("minecraft-data"), 1);
var import_prismarine_block = __toESM(require_prismarine_block(), 1);

// ../src/lib/moreBlockDataGenerated.json
var moreBlockDataGenerated_default = {
  lightLevels: {
    lava: 15,
    brown_mushroom: 1,
    torch: 14,
    wall_torch: 14,
    fire: 15,
    soul_fire: 10,
    soul_torch: 10,
    soul_wall_torch: 10,
    glowstone: 15,
    nether_portal: 11,
    jack_o_lantern: 15,
    enchanting_table: 7,
    brewing_stand: 1,
    lava_cauldron: 15,
    end_portal: 15,
    end_portal_frame: 1,
    dragon_egg: 1,
    ender_chest: 7,
    beacon: 15,
    sea_lantern: 15,
    end_rod: 14,
    end_gateway: 15,
    magma_block: 3,
    conduit: 15,
    lantern: 15,
    soul_lantern: 10,
    shroomlight: 15,
    crying_obsidian: 10,
    amethyst_cluster: 5,
    large_amethyst_bud: 4,
    medium_amethyst_bud: 2,
    small_amethyst_bud: 1,
    sculk_sensor: 1,
    sculk_catalyst: 6,
    ochre_froglight: 15,
    verdant_froglight: 15,
    pearlescent_froglight: 15
  },
  noOcclusions: {
    mangrove_roots: true,
    oak_leaves: true,
    spruce_leaves: true,
    birch_leaves: true,
    jungle_leaves: true,
    acacia_leaves: true,
    cherry_leaves: true,
    dark_oak_leaves: true,
    mangrove_leaves: true,
    azalea_leaves: true,
    flowering_azalea_leaves: true,
    glass: true,
    white_bed: true,
    orange_bed: true,
    magenta_bed: true,
    light_blue_bed: true,
    yellow_bed: true,
    lime_bed: true,
    pink_bed: true,
    gray_bed: true,
    light_gray_bed: true,
    cyan_bed: true,
    purple_bed: true,
    blue_bed: true,
    brown_bed: true,
    green_bed: true,
    red_bed: true,
    black_bed: true,
    moving_piston: true,
    spawner: true,
    oak_door: true,
    ladder: true,
    iron_door: true,
    ice: true,
    white_stained_glass: true,
    orange_stained_glass: true,
    magenta_stained_glass: true,
    light_blue_stained_glass: true,
    yellow_stained_glass: true,
    lime_stained_glass: true,
    pink_stained_glass: true,
    gray_stained_glass: true,
    light_gray_stained_glass: true,
    cyan_stained_glass: true,
    purple_stained_glass: true,
    blue_stained_glass: true,
    brown_stained_glass: true,
    green_stained_glass: true,
    red_stained_glass: true,
    black_stained_glass: true,
    oak_trapdoor: true,
    spruce_trapdoor: true,
    birch_trapdoor: true,
    jungle_trapdoor: true,
    acacia_trapdoor: true,
    cherry_trapdoor: true,
    dark_oak_trapdoor: true,
    mangrove_trapdoor: true,
    bamboo_trapdoor: true,
    iron_bars: true,
    chain: true,
    glass_pane: true,
    lily_pad: true,
    brewing_stand: true,
    cauldron: true,
    dragon_egg: true,
    cocoa: true,
    beacon: true,
    hopper: true,
    white_stained_glass_pane: true,
    orange_stained_glass_pane: true,
    magenta_stained_glass_pane: true,
    light_blue_stained_glass_pane: true,
    yellow_stained_glass_pane: true,
    lime_stained_glass_pane: true,
    pink_stained_glass_pane: true,
    gray_stained_glass_pane: true,
    light_gray_stained_glass_pane: true,
    cyan_stained_glass_pane: true,
    purple_stained_glass_pane: true,
    blue_stained_glass_pane: true,
    brown_stained_glass_pane: true,
    green_stained_glass_pane: true,
    red_stained_glass_pane: true,
    black_stained_glass_pane: true,
    slime_block: true,
    barrier: true,
    light: true,
    iron_trapdoor: true,
    spruce_door: true,
    birch_door: true,
    jungle_door: true,
    acacia_door: true,
    cherry_door: true,
    dark_oak_door: true,
    mangrove_door: true,
    bamboo_door: true,
    end_rod: true,
    chorus_plant: true,
    chorus_flower: true,
    frosted_ice: true,
    turtle_egg: true,
    sniffer_egg: true,
    sea_pickle: true,
    conduit: true,
    bamboo: true,
    lantern: true,
    soul_lantern: true,
    campfire: true,
    soul_campfire: true,
    crimson_trapdoor: true,
    warped_trapdoor: true,
    crimson_door: true,
    warped_door: true,
    honey_block: true,
    candle: true,
    white_candle: true,
    orange_candle: true,
    magenta_candle: true,
    light_blue_candle: true,
    yellow_candle: true,
    lime_candle: true,
    pink_candle: true,
    gray_candle: true,
    light_gray_candle: true,
    cyan_candle: true,
    purple_candle: true,
    blue_candle: true,
    brown_candle: true,
    green_candle: true,
    red_candle: true,
    black_candle: true,
    amethyst_cluster: true,
    tinted_glass: true,
    lightning_rod: true,
    pointed_dripstone: true,
    azalea: true,
    flowering_azalea: true,
    frogspawn: true,
    decorated_pot: true
  },
  colors: {
    stone: "rgb(112, 112, 112)",
    granite: "rgb(151, 109, 77)",
    polished_granite: "rgb(151, 109, 77)",
    diorite: "rgb(255, 252, 245)",
    polished_diorite: "rgb(255, 252, 245)",
    andesite: "rgb(112, 112, 112)",
    polished_andesite: "rgb(112, 112, 112)",
    grass_block: "rgb(127, 178, 56)",
    dirt: "rgb(151, 109, 77)",
    coarse_dirt: "rgb(151, 109, 77)",
    podzol: "rgb(129, 86, 49)",
    cobblestone: "rgb(112, 112, 112)",
    oak_planks: "rgb(143, 119, 72)",
    spruce_planks: "rgb(129, 86, 49)",
    birch_planks: "rgb(247, 233, 163)",
    jungle_planks: "rgb(151, 109, 77)",
    acacia_planks: "rgb(216, 127, 51)",
    cherry_planks: "rgb(209, 177, 161)",
    dark_oak_planks: "rgb(102, 76, 51)",
    mangrove_planks: "rgb(153, 51, 51)",
    bamboo_planks: "rgb(229, 229, 51)",
    bamboo_mosaic: "rgb(229, 229, 51)",
    oak_sapling: "rgb(0, 124, 0)",
    spruce_sapling: "rgb(0, 124, 0)",
    birch_sapling: "rgb(0, 124, 0)",
    jungle_sapling: "rgb(0, 124, 0)",
    acacia_sapling: "rgb(0, 124, 0)",
    cherry_sapling: "rgb(242, 127, 165)",
    dark_oak_sapling: "rgb(0, 124, 0)",
    mangrove_propagule: "rgb(0, 124, 0)",
    bedrock: "rgb(112, 112, 112)",
    water: "rgb(64, 64, 255)",
    lava: "rgb(255, 0, 0)",
    sand: "rgb(247, 233, 163)",
    suspicious_sand: "rgb(247, 233, 163)",
    red_sand: "rgb(216, 127, 51)",
    gravel: "rgb(112, 112, 112)",
    suspicious_gravel: "rgb(112, 112, 112)",
    gold_ore: "rgb(112, 112, 112)",
    deepslate_gold_ore: "rgb(100, 100, 100)",
    iron_ore: "rgb(112, 112, 112)",
    deepslate_iron_ore: "rgb(100, 100, 100)",
    coal_ore: "rgb(112, 112, 112)",
    deepslate_coal_ore: "rgb(100, 100, 100)",
    nether_gold_ore: "rgb(112, 2, 0)",
    mangrove_roots: "rgb(129, 86, 49)",
    muddy_mangrove_roots: "rgb(129, 86, 49)",
    oak_wood: "rgb(143, 119, 72)",
    spruce_wood: "rgb(129, 86, 49)",
    birch_wood: "rgb(247, 233, 163)",
    jungle_wood: "rgb(151, 109, 77)",
    acacia_wood: "rgb(76, 76, 76)",
    cherry_wood: "rgb(57, 41, 35)",
    dark_oak_wood: "rgb(102, 76, 51)",
    mangrove_wood: "rgb(153, 51, 51)",
    stripped_oak_wood: "rgb(143, 119, 72)",
    stripped_spruce_wood: "rgb(129, 86, 49)",
    stripped_birch_wood: "rgb(247, 233, 163)",
    stripped_jungle_wood: "rgb(151, 109, 77)",
    stripped_acacia_wood: "rgb(216, 127, 51)",
    stripped_cherry_wood: "rgb(160, 77, 78)",
    stripped_dark_oak_wood: "rgb(102, 76, 51)",
    oak_leaves: "rgb(0, 124, 0)",
    spruce_leaves: "rgb(0, 124, 0)",
    birch_leaves: "rgb(0, 124, 0)",
    jungle_leaves: "rgb(0, 124, 0)",
    acacia_leaves: "rgb(0, 124, 0)",
    cherry_leaves: "rgb(242, 127, 165)",
    dark_oak_leaves: "rgb(0, 124, 0)",
    mangrove_leaves: "rgb(0, 124, 0)",
    azalea_leaves: "rgb(0, 124, 0)",
    flowering_azalea_leaves: "rgb(0, 124, 0)",
    sponge: "rgb(229, 229, 51)",
    wet_sponge: "rgb(229, 229, 51)",
    lapis_ore: "rgb(112, 112, 112)",
    deepslate_lapis_ore: "rgb(100, 100, 100)",
    lapis_block: "rgb(74, 128, 255)",
    dispenser: "rgb(112, 112, 112)",
    sandstone: "rgb(247, 233, 163)",
    chiseled_sandstone: "rgb(247, 233, 163)",
    cut_sandstone: "rgb(247, 233, 163)",
    note_block: "rgb(143, 119, 72)",
    sticky_piston: "rgb(112, 112, 112)",
    cobweb: "rgb(199, 199, 199)",
    grass: "rgb(0, 124, 0)",
    fern: "rgb(0, 124, 0)",
    dead_bush: "rgb(143, 119, 72)",
    seagrass: "rgb(64, 64, 255)",
    tall_seagrass: "rgb(64, 64, 255)",
    piston: "rgb(112, 112, 112)",
    piston_head: "rgb(112, 112, 112)",
    white_wool: "rgb(255, 255, 255)",
    orange_wool: "rgb(216, 127, 51)",
    magenta_wool: "rgb(178, 76, 216)",
    light_blue_wool: "rgb(102, 153, 216)",
    yellow_wool: "rgb(229, 229, 51)",
    lime_wool: "rgb(127, 204, 25)",
    pink_wool: "rgb(242, 127, 165)",
    gray_wool: "rgb(76, 76, 76)",
    light_gray_wool: "rgb(153, 153, 153)",
    cyan_wool: "rgb(76, 127, 153)",
    purple_wool: "rgb(127, 63, 178)",
    blue_wool: "rgb(51, 76, 178)",
    brown_wool: "rgb(102, 76, 51)",
    green_wool: "rgb(102, 127, 51)",
    red_wool: "rgb(153, 51, 51)",
    black_wool: "rgb(25, 25, 25)",
    moving_piston: "rgb(112, 112, 112)",
    dandelion: "rgb(0, 124, 0)",
    torchflower: "rgb(0, 124, 0)",
    poppy: "rgb(0, 124, 0)",
    blue_orchid: "rgb(0, 124, 0)",
    allium: "rgb(0, 124, 0)",
    azure_bluet: "rgb(0, 124, 0)",
    red_tulip: "rgb(0, 124, 0)",
    orange_tulip: "rgb(0, 124, 0)",
    white_tulip: "rgb(0, 124, 0)",
    pink_tulip: "rgb(0, 124, 0)",
    oxeye_daisy: "rgb(0, 124, 0)",
    cornflower: "rgb(0, 124, 0)",
    wither_rose: "rgb(0, 124, 0)",
    lily_of_the_valley: "rgb(0, 124, 0)",
    brown_mushroom: "rgb(102, 76, 51)",
    red_mushroom: "rgb(153, 51, 51)",
    gold_block: "rgb(250, 238, 77)",
    iron_block: "rgb(167, 167, 167)",
    bricks: "rgb(153, 51, 51)",
    tnt: "rgb(255, 0, 0)",
    bookshelf: "rgb(143, 119, 72)",
    chiseled_bookshelf: "rgb(143, 119, 72)",
    mossy_cobblestone: "rgb(112, 112, 112)",
    obsidian: "rgb(25, 25, 25)",
    fire: "rgb(255, 0, 0)",
    soul_fire: "rgb(102, 153, 216)",
    spawner: "rgb(112, 112, 112)",
    chest: "rgb(143, 119, 72)",
    diamond_ore: "rgb(112, 112, 112)",
    deepslate_diamond_ore: "rgb(100, 100, 100)",
    diamond_block: "rgb(92, 219, 213)",
    crafting_table: "rgb(143, 119, 72)",
    wheat: "rgb(0, 124, 0)",
    farmland: "rgb(151, 109, 77)",
    furnace: "rgb(112, 112, 112)",
    oak_sign: "rgb(143, 119, 72)",
    birch_sign: "rgb(247, 233, 163)",
    acacia_sign: "rgb(216, 127, 51)",
    oak_wall_sign: "rgb(143, 119, 72)",
    birch_wall_sign: "rgb(247, 233, 163)",
    acacia_wall_sign: "rgb(216, 127, 51)",
    birch_hanging_sign: "rgb(247, 233, 163)",
    acacia_hanging_sign: "rgb(216, 127, 51)",
    cherry_hanging_sign: "rgb(160, 77, 78)",
    crimson_hanging_sign: "rgb(148, 63, 97)",
    warped_hanging_sign: "rgb(58, 142, 140)",
    bamboo_hanging_sign: "rgb(229, 229, 51)",
    spruce_wall_hanging_sign: "rgb(143, 119, 72)",
    birch_wall_hanging_sign: "rgb(247, 233, 163)",
    acacia_wall_hanging_sign: "rgb(216, 127, 51)",
    cherry_wall_hanging_sign: "rgb(160, 77, 78)",
    crimson_wall_hanging_sign: "rgb(148, 63, 97)",
    warped_wall_hanging_sign: "rgb(58, 142, 140)",
    bamboo_wall_hanging_sign: "rgb(229, 229, 51)",
    stone_pressure_plate: "rgb(112, 112, 112)",
    iron_door: "rgb(167, 167, 167)",
    redstone_ore: "rgb(112, 112, 112)",
    deepslate_redstone_ore: "rgb(100, 100, 100)",
    snow: "rgb(255, 255, 255)",
    ice: "rgb(160, 160, 255)",
    snow_block: "rgb(255, 255, 255)",
    cactus: "rgb(0, 124, 0)",
    clay: "rgb(164, 168, 184)",
    sugar_cane: "rgb(0, 124, 0)",
    jukebox: "rgb(151, 109, 77)",
    pumpkin: "rgb(216, 127, 51)",
    netherrack: "rgb(112, 2, 0)",
    soul_sand: "rgb(102, 76, 51)",
    soul_soil: "rgb(102, 76, 51)",
    basalt: "rgb(25, 25, 25)",
    polished_basalt: "rgb(25, 25, 25)",
    glowstone: "rgb(247, 233, 163)",
    carved_pumpkin: "rgb(216, 127, 51)",
    jack_o_lantern: "rgb(216, 127, 51)",
    oak_trapdoor: "rgb(143, 119, 72)",
    spruce_trapdoor: "rgb(129, 86, 49)",
    birch_trapdoor: "rgb(247, 233, 163)",
    jungle_trapdoor: "rgb(151, 109, 77)",
    acacia_trapdoor: "rgb(216, 127, 51)",
    cherry_trapdoor: "rgb(209, 177, 161)",
    dark_oak_trapdoor: "rgb(102, 76, 51)",
    mangrove_trapdoor: "rgb(153, 51, 51)",
    bamboo_trapdoor: "rgb(229, 229, 51)",
    stone_bricks: "rgb(112, 112, 112)",
    mossy_stone_bricks: "rgb(112, 112, 112)",
    cracked_stone_bricks: "rgb(112, 112, 112)",
    chiseled_stone_bricks: "rgb(112, 112, 112)",
    mud_bricks: "rgb(135, 107, 98)",
    infested_stone: "rgb(164, 168, 184)",
    infested_cobblestone: "rgb(164, 168, 184)",
    infested_stone_bricks: "rgb(164, 168, 184)",
    infested_mossy_stone_bricks: "rgb(164, 168, 184)",
    infested_cracked_stone_bricks: "rgb(164, 168, 184)",
    infested_chiseled_stone_bricks: "rgb(164, 168, 184)",
    brown_mushroom_block: "rgb(151, 109, 77)",
    red_mushroom_block: "rgb(153, 51, 51)",
    mushroom_stem: "rgb(199, 199, 199)",
    melon: "rgb(127, 204, 25)",
    attached_pumpkin_stem: "rgb(0, 124, 0)",
    attached_melon_stem: "rgb(0, 124, 0)",
    pumpkin_stem: "rgb(0, 124, 0)",
    melon_stem: "rgb(0, 124, 0)",
    vine: "rgb(0, 124, 0)",
    glow_lichen: "rgb(127, 167, 150)",
    mycelium: "rgb(127, 63, 178)",
    lily_pad: "rgb(0, 124, 0)",
    nether_bricks: "rgb(112, 2, 0)",
    nether_brick_fence: "rgb(112, 2, 0)",
    nether_wart: "rgb(153, 51, 51)",
    enchanting_table: "rgb(153, 51, 51)",
    brewing_stand: "rgb(167, 167, 167)",
    cauldron: "rgb(112, 112, 112)",
    end_portal: "rgb(25, 25, 25)",
    end_portal_frame: "rgb(102, 127, 51)",
    end_stone: "rgb(247, 233, 163)",
    dragon_egg: "rgb(25, 25, 25)",
    cocoa: "rgb(0, 124, 0)",
    emerald_ore: "rgb(112, 112, 112)",
    deepslate_emerald_ore: "rgb(100, 100, 100)",
    ender_chest: "rgb(112, 112, 112)",
    emerald_block: "rgb(0, 217, 58)",
    command_block: "rgb(102, 76, 51)",
    beacon: "rgb(92, 219, 213)",
    carrots: "rgb(0, 124, 0)",
    potatoes: "rgb(0, 124, 0)",
    anvil: "rgb(167, 167, 167)",
    chipped_anvil: "rgb(167, 167, 167)",
    damaged_anvil: "rgb(167, 167, 167)",
    trapped_chest: "rgb(143, 119, 72)",
    light_weighted_pressure_plate: "rgb(250, 238, 77)",
    heavy_weighted_pressure_plate: "rgb(167, 167, 167)",
    daylight_detector: "rgb(143, 119, 72)",
    redstone_block: "rgb(255, 0, 0)",
    nether_quartz_ore: "rgb(112, 2, 0)",
    hopper: "rgb(112, 112, 112)",
    quartz_block: "rgb(255, 252, 245)",
    chiseled_quartz_block: "rgb(255, 252, 245)",
    quartz_pillar: "rgb(255, 252, 245)",
    dropper: "rgb(112, 112, 112)",
    white_terracotta: "rgb(209, 177, 161)",
    orange_terracotta: "rgb(159, 82, 36)",
    magenta_terracotta: "rgb(149, 87, 108)",
    light_blue_terracotta: "rgb(112, 108, 138)",
    yellow_terracotta: "rgb(186, 133, 36)",
    lime_terracotta: "rgb(103, 117, 53)",
    pink_terracotta: "rgb(160, 77, 78)",
    gray_terracotta: "rgb(57, 41, 35)",
    light_gray_terracotta: "rgb(135, 107, 98)",
    cyan_terracotta: "rgb(87, 92, 92)",
    purple_terracotta: "rgb(122, 73, 88)",
    blue_terracotta: "rgb(76, 62, 92)",
    brown_terracotta: "rgb(76, 50, 35)",
    green_terracotta: "rgb(76, 82, 42)",
    red_terracotta: "rgb(142, 60, 46)",
    black_terracotta: "rgb(37, 22, 16)",
    slime_block: "rgb(127, 178, 56)",
    iron_trapdoor: "rgb(167, 167, 167)",
    prismarine: "rgb(76, 127, 153)",
    prismarine_bricks: "rgb(92, 219, 213)",
    dark_prismarine: "rgb(92, 219, 213)",
    prismarine_slab: "rgb(76, 127, 153)",
    prismarine_brick_slab: "rgb(92, 219, 213)",
    dark_prismarine_slab: "rgb(92, 219, 213)",
    sea_lantern: "rgb(255, 252, 245)",
    hay_block: "rgb(229, 229, 51)",
    white_carpet: "rgb(255, 255, 255)",
    orange_carpet: "rgb(216, 127, 51)",
    magenta_carpet: "rgb(178, 76, 216)",
    light_blue_carpet: "rgb(102, 153, 216)",
    yellow_carpet: "rgb(229, 229, 51)",
    lime_carpet: "rgb(127, 204, 25)",
    pink_carpet: "rgb(242, 127, 165)",
    gray_carpet: "rgb(76, 76, 76)",
    light_gray_carpet: "rgb(153, 153, 153)",
    cyan_carpet: "rgb(76, 127, 153)",
    purple_carpet: "rgb(127, 63, 178)",
    blue_carpet: "rgb(51, 76, 178)",
    brown_carpet: "rgb(102, 76, 51)",
    green_carpet: "rgb(102, 127, 51)",
    red_carpet: "rgb(153, 51, 51)",
    black_carpet: "rgb(25, 25, 25)",
    terracotta: "rgb(216, 127, 51)",
    coal_block: "rgb(25, 25, 25)",
    packed_ice: "rgb(160, 160, 255)",
    sunflower: "rgb(0, 124, 0)",
    lilac: "rgb(0, 124, 0)",
    rose_bush: "rgb(0, 124, 0)",
    peony: "rgb(0, 124, 0)",
    tall_grass: "rgb(0, 124, 0)",
    large_fern: "rgb(0, 124, 0)",
    white_banner: "rgb(143, 119, 72)",
    orange_banner: "rgb(143, 119, 72)",
    magenta_banner: "rgb(143, 119, 72)",
    light_blue_banner: "rgb(143, 119, 72)",
    yellow_banner: "rgb(143, 119, 72)",
    lime_banner: "rgb(143, 119, 72)",
    pink_banner: "rgb(143, 119, 72)",
    gray_banner: "rgb(143, 119, 72)",
    light_gray_banner: "rgb(143, 119, 72)",
    cyan_banner: "rgb(143, 119, 72)",
    purple_banner: "rgb(143, 119, 72)",
    blue_banner: "rgb(143, 119, 72)",
    brown_banner: "rgb(143, 119, 72)",
    green_banner: "rgb(143, 119, 72)",
    red_banner: "rgb(143, 119, 72)",
    black_banner: "rgb(143, 119, 72)",
    white_wall_banner: "rgb(143, 119, 72)",
    orange_wall_banner: "rgb(143, 119, 72)",
    magenta_wall_banner: "rgb(143, 119, 72)",
    light_blue_wall_banner: "rgb(143, 119, 72)",
    yellow_wall_banner: "rgb(143, 119, 72)",
    lime_wall_banner: "rgb(143, 119, 72)",
    pink_wall_banner: "rgb(143, 119, 72)",
    gray_wall_banner: "rgb(143, 119, 72)",
    light_gray_wall_banner: "rgb(143, 119, 72)",
    cyan_wall_banner: "rgb(143, 119, 72)",
    purple_wall_banner: "rgb(143, 119, 72)",
    blue_wall_banner: "rgb(143, 119, 72)",
    brown_wall_banner: "rgb(143, 119, 72)",
    green_wall_banner: "rgb(143, 119, 72)",
    red_wall_banner: "rgb(143, 119, 72)",
    black_wall_banner: "rgb(143, 119, 72)",
    red_sandstone: "rgb(216, 127, 51)",
    chiseled_red_sandstone: "rgb(216, 127, 51)",
    cut_red_sandstone: "rgb(216, 127, 51)",
    oak_slab: "rgb(143, 119, 72)",
    spruce_slab: "rgb(129, 86, 49)",
    birch_slab: "rgb(247, 233, 163)",
    jungle_slab: "rgb(151, 109, 77)",
    acacia_slab: "rgb(216, 127, 51)",
    cherry_slab: "rgb(209, 177, 161)",
    dark_oak_slab: "rgb(102, 76, 51)",
    mangrove_slab: "rgb(153, 51, 51)",
    bamboo_slab: "rgb(229, 229, 51)",
    bamboo_mosaic_slab: "rgb(229, 229, 51)",
    stone_slab: "rgb(112, 112, 112)",
    smooth_stone_slab: "rgb(112, 112, 112)",
    sandstone_slab: "rgb(247, 233, 163)",
    cut_sandstone_slab: "rgb(247, 233, 163)",
    petrified_oak_slab: "rgb(143, 119, 72)",
    cobblestone_slab: "rgb(112, 112, 112)",
    brick_slab: "rgb(153, 51, 51)",
    stone_brick_slab: "rgb(112, 112, 112)",
    mud_brick_slab: "rgb(135, 107, 98)",
    nether_brick_slab: "rgb(112, 2, 0)",
    quartz_slab: "rgb(255, 252, 245)",
    red_sandstone_slab: "rgb(216, 127, 51)",
    cut_red_sandstone_slab: "rgb(216, 127, 51)",
    purpur_slab: "rgb(178, 76, 216)",
    smooth_stone: "rgb(112, 112, 112)",
    smooth_sandstone: "rgb(247, 233, 163)",
    smooth_quartz: "rgb(255, 252, 245)",
    smooth_red_sandstone: "rgb(216, 127, 51)",
    chorus_plant: "rgb(127, 63, 178)",
    chorus_flower: "rgb(127, 63, 178)",
    purpur_block: "rgb(178, 76, 216)",
    purpur_pillar: "rgb(178, 76, 216)",
    end_stone_bricks: "rgb(247, 233, 163)",
    torchflower_crop: "rgb(0, 124, 0)",
    pitcher_crop: "rgb(0, 124, 0)",
    pitcher_plant: "rgb(0, 124, 0)",
    beetroots: "rgb(0, 124, 0)",
    dirt_path: "rgb(151, 109, 77)",
    end_gateway: "rgb(25, 25, 25)",
    repeating_command_block: "rgb(127, 63, 178)",
    chain_command_block: "rgb(102, 127, 51)",
    frosted_ice: "rgb(160, 160, 255)",
    magma_block: "rgb(112, 2, 0)",
    nether_wart_block: "rgb(153, 51, 51)",
    red_nether_bricks: "rgb(112, 2, 0)",
    bone_block: "rgb(247, 233, 163)",
    observer: "rgb(112, 112, 112)",
    kelp: "rgb(64, 64, 255)",
    kelp_plant: "rgb(64, 64, 255)",
    dried_kelp_block: "rgb(102, 127, 51)",
    turtle_egg: "rgb(247, 233, 163)",
    sniffer_egg: "rgb(153, 51, 51)",
    dead_tube_coral_block: "rgb(76, 76, 76)",
    dead_brain_coral_block: "rgb(76, 76, 76)",
    dead_bubble_coral_block: "rgb(76, 76, 76)",
    dead_fire_coral_block: "rgb(76, 76, 76)",
    dead_horn_coral_block: "rgb(76, 76, 76)",
    tube_coral_block: "rgb(51, 76, 178)",
    brain_coral_block: "rgb(242, 127, 165)",
    bubble_coral_block: "rgb(127, 63, 178)",
    fire_coral_block: "rgb(153, 51, 51)",
    horn_coral_block: "rgb(229, 229, 51)",
    dead_tube_coral: "rgb(76, 76, 76)",
    dead_brain_coral: "rgb(76, 76, 76)",
    dead_bubble_coral: "rgb(76, 76, 76)",
    dead_fire_coral: "rgb(76, 76, 76)",
    dead_horn_coral: "rgb(76, 76, 76)",
    tube_coral: "rgb(51, 76, 178)",
    brain_coral: "rgb(242, 127, 165)",
    bubble_coral: "rgb(127, 63, 178)",
    fire_coral: "rgb(153, 51, 51)",
    horn_coral: "rgb(229, 229, 51)",
    dead_tube_coral_fan: "rgb(76, 76, 76)",
    dead_brain_coral_fan: "rgb(76, 76, 76)",
    dead_bubble_coral_fan: "rgb(76, 76, 76)",
    dead_fire_coral_fan: "rgb(76, 76, 76)",
    dead_horn_coral_fan: "rgb(76, 76, 76)",
    tube_coral_fan: "rgb(51, 76, 178)",
    brain_coral_fan: "rgb(242, 127, 165)",
    bubble_coral_fan: "rgb(127, 63, 178)",
    fire_coral_fan: "rgb(153, 51, 51)",
    horn_coral_fan: "rgb(229, 229, 51)",
    dead_tube_coral_wall_fan: "rgb(76, 76, 76)",
    dead_brain_coral_wall_fan: "rgb(76, 76, 76)",
    dead_bubble_coral_wall_fan: "rgb(76, 76, 76)",
    dead_fire_coral_wall_fan: "rgb(76, 76, 76)",
    dead_horn_coral_wall_fan: "rgb(76, 76, 76)",
    tube_coral_wall_fan: "rgb(51, 76, 178)",
    brain_coral_wall_fan: "rgb(242, 127, 165)",
    bubble_coral_wall_fan: "rgb(127, 63, 178)",
    fire_coral_wall_fan: "rgb(153, 51, 51)",
    horn_coral_wall_fan: "rgb(229, 229, 51)",
    sea_pickle: "rgb(102, 127, 51)",
    blue_ice: "rgb(160, 160, 255)",
    conduit: "rgb(92, 219, 213)",
    bamboo_sapling: "rgb(143, 119, 72)",
    bamboo: "rgb(0, 124, 0)",
    bubble_column: "rgb(64, 64, 255)",
    scaffolding: "rgb(247, 233, 163)",
    loom: "rgb(143, 119, 72)",
    barrel: "rgb(143, 119, 72)",
    smoker: "rgb(112, 112, 112)",
    blast_furnace: "rgb(112, 112, 112)",
    cartography_table: "rgb(143, 119, 72)",
    fletching_table: "rgb(143, 119, 72)",
    grindstone: "rgb(167, 167, 167)",
    lectern: "rgb(143, 119, 72)",
    smithing_table: "rgb(143, 119, 72)",
    stonecutter: "rgb(112, 112, 112)",
    bell: "rgb(250, 238, 77)",
    lantern: "rgb(167, 167, 167)",
    soul_lantern: "rgb(167, 167, 167)",
    campfire: "rgb(129, 86, 49)",
    soul_campfire: "rgb(129, 86, 49)",
    sweet_berry_bush: "rgb(0, 124, 0)",
    warped_hyphae: "rgb(86, 44, 62)",
    stripped_warped_hyphae: "rgb(86, 44, 62)",
    warped_nylium: "rgb(22, 126, 134)",
    warped_fungus: "rgb(76, 127, 153)",
    warped_wart_block: "rgb(20, 180, 133)",
    warped_roots: "rgb(76, 127, 153)",
    nether_sprouts: "rgb(76, 127, 153)",
    crimson_hyphae: "rgb(92, 25, 29)",
    stripped_crimson_hyphae: "rgb(92, 25, 29)",
    crimson_nylium: "rgb(189, 48, 49)",
    crimson_fungus: "rgb(112, 2, 0)",
    shroomlight: "rgb(153, 51, 51)",
    weeping_vines: "rgb(112, 2, 0)",
    weeping_vines_plant: "rgb(112, 2, 0)",
    twisting_vines: "rgb(76, 127, 153)",
    twisting_vines_plant: "rgb(76, 127, 153)",
    crimson_roots: "rgb(112, 2, 0)",
    crimson_planks: "rgb(148, 63, 97)",
    warped_planks: "rgb(58, 142, 140)",
    structure_block: "rgb(153, 153, 153)",
    jigsaw: "rgb(153, 153, 153)",
    composter: "rgb(143, 119, 72)",
    target: "rgb(255, 252, 245)",
    bee_nest: "rgb(229, 229, 51)",
    beehive: "rgb(143, 119, 72)",
    honey_block: "rgb(216, 127, 51)",
    honeycomb_block: "rgb(216, 127, 51)",
    netherite_block: "rgb(25, 25, 25)",
    ancient_debris: "rgb(25, 25, 25)",
    crying_obsidian: "rgb(25, 25, 25)",
    respawn_anchor: "rgb(25, 25, 25)",
    lodestone: "rgb(167, 167, 167)",
    blackstone: "rgb(25, 25, 25)",
    polished_blackstone_pressure_plate: "rgb(25, 25, 25)",
    chiseled_nether_bricks: "rgb(112, 2, 0)",
    cracked_nether_bricks: "rgb(112, 2, 0)",
    amethyst_block: "rgb(127, 63, 178)",
    budding_amethyst: "rgb(127, 63, 178)",
    amethyst_cluster: "rgb(127, 63, 178)",
    tuff: "rgb(57, 41, 35)",
    calcite: "rgb(209, 177, 161)",
    tinted_glass: "rgb(76, 76, 76)",
    powder_snow: "rgb(255, 255, 255)",
    sculk_sensor: "rgb(76, 127, 153)",
    sculk: "rgb(25, 25, 25)",
    sculk_vein: "rgb(25, 25, 25)",
    sculk_catalyst: "rgb(25, 25, 25)",
    sculk_shrieker: "rgb(25, 25, 25)",
    oxidized_copper: "rgb(22, 126, 134)",
    weathered_copper: "rgb(58, 142, 140)",
    exposed_copper: "rgb(135, 107, 98)",
    copper_block: "rgb(216, 127, 51)",
    deepslate_copper_ore: "rgb(100, 100, 100)",
    lightning_rod: "rgb(216, 127, 51)",
    pointed_dripstone: "rgb(76, 50, 35)",
    dripstone_block: "rgb(76, 50, 35)",
    cave_vines: "rgb(0, 124, 0)",
    cave_vines_plant: "rgb(0, 124, 0)",
    spore_blossom: "rgb(0, 124, 0)",
    azalea: "rgb(0, 124, 0)",
    flowering_azalea: "rgb(0, 124, 0)",
    moss_carpet: "rgb(102, 127, 51)",
    pink_petals: "rgb(0, 124, 0)",
    moss_block: "rgb(102, 127, 51)",
    big_dripleaf: "rgb(0, 124, 0)",
    big_dripleaf_stem: "rgb(0, 124, 0)",
    small_dripleaf: "rgb(0, 124, 0)",
    hanging_roots: "rgb(151, 109, 77)",
    rooted_dirt: "rgb(151, 109, 77)",
    mud: "rgb(87, 92, 92)",
    deepslate: "rgb(100, 100, 100)",
    infested_deepslate: "rgb(100, 100, 100)",
    raw_iron_block: "rgb(216, 175, 147)",
    raw_copper_block: "rgb(216, 127, 51)",
    raw_gold_block: "rgb(250, 238, 77)",
    ochre_froglight: "rgb(247, 233, 163)",
    verdant_froglight: "rgb(127, 167, 150)",
    pearlescent_froglight: "rgb(242, 127, 165)",
    frogspawn: "rgb(64, 64, 255)",
    reinforced_deepslate: "rgb(100, 100, 100)",
    decorated_pot: "rgb(142, 60, 46)"
  },
  invisibleBlocks: {
    air: true,
    void_air: true,
    cave_air: true,
    barrier: true,
    light: true,
    moving_piston: true
  },
  hasSemiTransparentTextuersRegex: {
    _stained_glass$: true,
    _stained_glass_pane$: true,
    "^ice$": true,
    "^tinted_glass$": true,
    "^slime_block$": true,
    "^honey_block$": true
  }
};

// ../src/wasm-lib/convertChunk.ts
var metaCache = /* @__PURE__ */ new Map();
var blockToIds = (block) => {
  const ids = [];
  for (let i = block.minStateId; i <= block.maxStateId; i++) {
    ids.push(i);
  }
  return ids;
};
var isCube = (shapes) => {
  if (!shapes || shapes.length !== 1)
    return false;
  const s = shapes[0];
  return s[0] === 0 && s[1] === 0 && s[2] === 0 && s[3] === 1 && s[4] === 1 && s[5] === 1;
};
var getBlockMeta = (version) => {
  const cached = metaCache.get(version);
  if (cached)
    return cached;
  const mcData2 = (0, import_minecraft_data.default)(version);
  const invisibleBlocks = new Uint16Array(mcData2.blocksArray.filter((x) => moreBlockDataGenerated_default.invisibleBlocks[x.name]).flatMap(blockToIds));
  const transparentBlocks = new Uint16Array(mcData2.blocksArray.filter((x) => x.transparent).flatMap(blockToIds));
  const noAoBlocks = new Uint16Array(mcData2.blocksArray.filter((x) => moreBlockDataGenerated_default.noOcclusions[x.name]).flatMap(blockToIds));
  const cullIdenticalBlocks = new Uint16Array(mcData2.blocksArray.filter((x) => x.name.includes("glass") || x.name.includes("ice")).flatMap(blockToIds));
  const Block = (0, import_prismarine_block.default)(version);
  const noOcclusionsSet = new Set(Object.keys(moreBlockDataGenerated_default.noOcclusions));
  const occludingBlockIds = [];
  for (const idStr of Object.keys(mcData2.blocksByStateId)) {
    const id = Number(idStr);
    if (!id)
      continue;
    const b = Block.fromStateId(id, 0);
    if (!b)
      continue;
    if (b.transparent)
      continue;
    if (b.boundingBox !== "block")
      continue;
    if (noOcclusionsSet.has(b.name))
      continue;
    if (!isCube(b.shapes))
      continue;
    occludingBlockIds.push(id);
  }
  const occludingBlocks = new Uint16Array(occludingBlockIds);
  const meta = {
    invisibleBlocks,
    transparentBlocks,
    noAoBlocks,
    cullIdenticalBlocks,
    occludingBlocks
  };
  metaCache.set(version, meta);
  return meta;
};
function convertChunkToWasm(chunk, version, chunkX = 0, chunkZ = 0, worldMinY = 0, worldMaxY = 256, sectionY, sectionHeight) {
  const CHUNK_SIZE = 16;
  const startY = sectionY !== void 0 ? sectionY : worldMinY;
  const endY = sectionHeight !== void 0 ? startY + sectionHeight : worldMaxY;
  const totalBlocks = CHUNK_SIZE * CHUNK_SIZE * (endY - startY);
  const blockStates = new Uint16Array(totalBlocks);
  const blockLight = new Uint8Array(totalBlocks);
  const skyLight = new Uint8Array(totalBlocks);
  const biomesArray = new Uint8Array(totalBlocks);
  let blockCount = 0;
  for (let y = startY; y < endY; y++) {
    for (let z = 0; z < CHUNK_SIZE; z++) {
      for (let x = 0; x < CHUNK_SIZE; x++) {
        const pos = new Vec3(x, y, z);
        const idx = x + z * CHUNK_SIZE + (y - startY) * CHUNK_SIZE * CHUNK_SIZE;
        try {
          const stateId = chunk.getBlockStateId(pos);
          blockStates[idx] = stateId || 0;
          const bl = chunk.getBlockLight(pos);
          const sl = chunk.getSkyLight(pos);
          blockLight[idx] = bl !== void 0 ? bl : 0;
          skyLight[idx] = sl !== void 0 ? sl : 15;
          const biome = chunk.getBiome ? chunk.getBiome(pos) : 1;
          biomesArray[idx] = biome || 1;
          if (stateId && stateId !== 0)
            blockCount++;
        } catch (err) {
          blockStates[idx] = 0;
          blockLight[idx] = 0;
          skyLight[idx] = 15;
          biomesArray[idx] = 1;
        }
      }
    }
  }
  const {
    invisibleBlocks,
    transparentBlocks,
    noAoBlocks,
    cullIdenticalBlocks,
    occludingBlocks
  } = getBlockMeta(version);
  return {
    blockStates,
    blockLight,
    skyLight,
    biomesArray,
    invisibleBlocks,
    transparentBlocks,
    noAoBlocks,
    cullIdenticalBlocks,
    occludingBlocks,
    blockCount
  };
}

// ../src/mesher/world.ts
var import_prismarine_chunk = __toESM(require_prismarine_chunk(), 1);
var import_minecraft_data2 = __toESM(require("minecraft-data"), 1);

// ../src/lib/preflatMap.json
var preflatMap_default = {
  blocks: {
    "0:0": "air",
    "1:0": "stone",
    "1:1": "granite",
    "1:2": "polished_granite",
    "1:3": "diorite",
    "1:4": "polished_diorite",
    "1:5": "andesite",
    "1:6": "polished_andesite",
    "2:0": "grass_block[snowy=false]",
    "3:0": "dirt",
    "3:1": "coarse_dirt",
    "3:2": "podzol[snowy=false]",
    "4:0": "cobblestone",
    "5:0": "oak_planks",
    "5:1": "spruce_planks",
    "5:2": "birch_planks",
    "5:3": "jungle_planks",
    "5:4": "acacia_planks",
    "5:5": "dark_oak_planks",
    "6:0": "oak_sapling[stage=0]",
    "6:1": "spruce_sapling[stage=0]",
    "6:2": "birch_sapling[stage=0]",
    "6:3": "jungle_sapling[stage=0]",
    "6:4": "acacia_sapling[stage=0]",
    "6:5": "dark_oak_sapling[stage=0]",
    "6:8": "oak_sapling[stage=1]",
    "6:9": "spruce_sapling[stage=1]",
    "6:10": "birch_sapling[stage=1]",
    "6:11": "jungle_sapling[stage=1]",
    "6:12": "acacia_sapling[stage=1]",
    "6:13": "dark_oak_sapling[stage=1]",
    "7:0": "bedrock",
    "8:0": "water[level=0]",
    "8:1": "water[level=1]",
    "8:2": "water[level=2]",
    "8:3": "water[level=3]",
    "8:4": "water[level=4]",
    "8:5": "water[level=5]",
    "8:6": "water[level=6]",
    "8:7": "water[level=7]",
    "8:8": "water[level=8]",
    "8:9": "water[level=9]",
    "8:10": "water[level=10]",
    "8:11": "water[level=11]",
    "8:12": "water[level=12]",
    "8:13": "water[level=13]",
    "8:14": "water[level=14]",
    "8:15": "water[level=15]",
    "9:0": "water[level=0]",
    "9:1": "water[level=1]",
    "9:2": "water[level=2]",
    "9:3": "water[level=3]",
    "9:4": "water[level=4]",
    "9:5": "water[level=5]",
    "9:6": "water[level=6]",
    "9:7": "water[level=7]",
    "9:8": "water[level=8]",
    "9:9": "water[level=9]",
    "9:10": "water[level=10]",
    "9:11": "water[level=11]",
    "9:12": "water[level=12]",
    "9:13": "water[level=13]",
    "9:14": "water[level=14]",
    "9:15": "water[level=15]",
    "10:0": "lava[level=0]",
    "10:1": "lava[level=1]",
    "10:2": "lava[level=2]",
    "10:3": "lava[level=3]",
    "10:4": "lava[level=4]",
    "10:5": "lava[level=5]",
    "10:6": "lava[level=6]",
    "10:7": "lava[level=7]",
    "10:8": "lava[level=8]",
    "10:9": "lava[level=9]",
    "10:10": "lava[level=10]",
    "10:11": "lava[level=11]",
    "10:12": "lava[level=12]",
    "10:13": "lava[level=13]",
    "10:14": "lava[level=14]",
    "10:15": "lava[level=15]",
    "11:0": "lava[level=0]",
    "11:1": "lava[level=1]",
    "11:2": "lava[level=2]",
    "11:3": "lava[level=3]",
    "11:4": "lava[level=4]",
    "11:5": "lava[level=5]",
    "11:6": "lava[level=6]",
    "11:7": "lava[level=7]",
    "11:8": "lava[level=8]",
    "11:9": "lava[level=9]",
    "11:10": "lava[level=10]",
    "11:11": "lava[level=11]",
    "11:12": "lava[level=12]",
    "11:13": "lava[level=13]",
    "11:14": "lava[level=14]",
    "11:15": "lava[level=15]",
    "12:0": "sand",
    "12:1": "red_sand",
    "13:0": "gravel",
    "14:0": "gold_ore",
    "15:0": "iron_ore",
    "16:0": "coal_ore",
    "17:0": "oak_log[axis=y]",
    "17:1": "spruce_log[axis=y]",
    "17:2": "birch_log[axis=y]",
    "17:3": "jungle_log[axis=y]",
    "17:4": "oak_log[axis=x]",
    "17:5": "spruce_log[axis=x]",
    "17:6": "birch_log[axis=x]",
    "17:7": "jungle_log[axis=x]",
    "17:8": "oak_log[axis=z]",
    "17:9": "spruce_log[axis=z]",
    "17:10": "birch_log[axis=z]",
    "17:11": "jungle_log[axis=z]",
    "17:12": "oak_bark",
    "17:13": "spruce_bark",
    "17:14": "birch_bark",
    "17:15": "jungle_bark",
    "18:0": "oak_leaves[check_decay=false,decayable=true]",
    "18:1": "spruce_leaves[check_decay=false,decayable=true]",
    "18:2": "birch_leaves[check_decay=false,decayable=true]",
    "18:3": "jungle_leaves[check_decay=false,decayable=true]",
    "18:4": "oak_leaves[check_decay=false,decayable=false]",
    "18:5": "spruce_leaves[check_decay=false,decayable=false]",
    "18:6": "birch_leaves[check_decay=false,decayable=false]",
    "18:7": "jungle_leaves[check_decay=false,decayable=false]",
    "18:8": "oak_leaves[check_decay=true,decayable=true]",
    "18:9": "spruce_leaves[check_decay=true,decayable=true]",
    "18:10": "birch_leaves[check_decay=true,decayable=true]",
    "18:11": "jungle_leaves[check_decay=true,decayable=true]",
    "18:12": "oak_leaves[check_decay=true,decayable=false]",
    "18:13": "spruce_leaves[check_decay=true,decayable=false]",
    "18:14": "birch_leaves[check_decay=true,decayable=false]",
    "18:15": "jungle_leaves[check_decay=true,decayable=false]",
    "19:0": "sponge",
    "19:1": "wet_sponge",
    "20:0": "glass",
    "21:0": "lapis_ore",
    "22:0": "lapis_block",
    "23:0": "dispenser[facing=down,triggered=false]",
    "23:1": "dispenser[facing=up,triggered=false]",
    "23:2": "dispenser[facing=north,triggered=false]",
    "23:3": "dispenser[facing=south,triggered=false]",
    "23:4": "dispenser[facing=west,triggered=false]",
    "23:5": "dispenser[facing=east,triggered=false]",
    "23:8": "dispenser[facing=down,triggered=true]",
    "23:9": "dispenser[facing=up,triggered=true]",
    "23:10": "dispenser[facing=north,triggered=true]",
    "23:11": "dispenser[facing=south,triggered=true]",
    "23:12": "dispenser[facing=west,triggered=true]",
    "23:13": "dispenser[facing=east,triggered=true]",
    "24:0": "sandstone",
    "24:1": "chiseled_sandstone",
    "24:2": "cut_sandstone",
    "25:0": "note_block",
    "26:0": "red_bed[facing=south,occupied=false,part=foot]",
    "26:1": "red_bed[facing=west,occupied=false,part=foot]",
    "26:2": "red_bed[facing=north,occupied=false,part=foot]",
    "26:3": "red_bed[facing=east,occupied=false,part=foot]",
    "26:8": "red_bed[facing=south,occupied=false,part=head]",
    "26:9": "red_bed[facing=west,occupied=false,part=head]",
    "26:10": "red_bed[facing=north,occupied=false,part=head]",
    "26:11": "red_bed[facing=east,occupied=false,part=head]",
    "26:12": "red_bed[facing=south,occupied=true,part=head]",
    "26:13": "red_bed[facing=west,occupied=true,part=head]",
    "26:14": "red_bed[facing=north,occupied=true,part=head]",
    "26:15": "red_bed[facing=east,occupied=true,part=head]",
    "27:0": "powered_rail[powered=false,shape=north_south]",
    "27:1": "powered_rail[powered=false,shape=east_west]",
    "27:2": "powered_rail[powered=false,shape=ascending_east]",
    "27:3": "powered_rail[powered=false,shape=ascending_west]",
    "27:4": "powered_rail[powered=false,shape=ascending_north]",
    "27:5": "powered_rail[powered=false,shape=ascending_south]",
    "27:8": "powered_rail[powered=true,shape=north_south]",
    "27:9": "powered_rail[powered=true,shape=east_west]",
    "27:10": "powered_rail[powered=true,shape=ascending_east]",
    "27:11": "powered_rail[powered=true,shape=ascending_west]",
    "27:12": "powered_rail[powered=true,shape=ascending_north]",
    "27:13": "powered_rail[powered=true,shape=ascending_south]",
    "28:0": "detector_rail[powered=false,shape=north_south]",
    "28:1": "detector_rail[powered=false,shape=east_west]",
    "28:2": "detector_rail[powered=false,shape=ascending_east]",
    "28:3": "detector_rail[powered=false,shape=ascending_west]",
    "28:4": "detector_rail[powered=false,shape=ascending_north]",
    "28:5": "detector_rail[powered=false,shape=ascending_south]",
    "28:8": "detector_rail[powered=true,shape=north_south]",
    "28:9": "detector_rail[powered=true,shape=east_west]",
    "28:10": "detector_rail[powered=true,shape=ascending_east]",
    "28:11": "detector_rail[powered=true,shape=ascending_west]",
    "28:12": "detector_rail[powered=true,shape=ascending_north]",
    "28:13": "detector_rail[powered=true,shape=ascending_south]",
    "29:0": "sticky_piston[extended=false,facing=down]",
    "29:1": "sticky_piston[extended=false,facing=up]",
    "29:2": "sticky_piston[extended=false,facing=north]",
    "29:3": "sticky_piston[extended=false,facing=south]",
    "29:4": "sticky_piston[extended=false,facing=west]",
    "29:5": "sticky_piston[extended=false,facing=east]",
    "29:8": "sticky_piston[extended=true,facing=down]",
    "29:9": "sticky_piston[extended=true,facing=up]",
    "29:10": "sticky_piston[extended=true,facing=north]",
    "29:11": "sticky_piston[extended=true,facing=south]",
    "29:12": "sticky_piston[extended=true,facing=west]",
    "29:13": "sticky_piston[extended=true,facing=east]",
    "30:0": "cobweb",
    "31:0": "dead_bush",
    "31:1": "grass",
    "31:2": "fern",
    "32:0": "dead_bush",
    "33:0": "piston[extended=false,facing=down]",
    "33:1": "piston[extended=false,facing=up]",
    "33:2": "piston[extended=false,facing=north]",
    "33:3": "piston[extended=false,facing=south]",
    "33:4": "piston[extended=false,facing=west]",
    "33:5": "piston[extended=false,facing=east]",
    "33:8": "piston[extended=true,facing=down]",
    "33:9": "piston[extended=true,facing=up]",
    "33:10": "piston[extended=true,facing=north]",
    "33:11": "piston[extended=true,facing=south]",
    "33:12": "piston[extended=true,facing=west]",
    "33:13": "piston[extended=true,facing=east]",
    "34:0": "piston_head[facing=down,short=false,type=normal]",
    "34:1": "piston_head[facing=up,short=false,type=normal]",
    "34:2": "piston_head[facing=north,short=false,type=normal]",
    "34:3": "piston_head[facing=south,short=false,type=normal]",
    "34:4": "piston_head[facing=west,short=false,type=normal]",
    "34:5": "piston_head[facing=east,short=false,type=normal]",
    "34:8": "piston_head[facing=down,short=false,type=sticky]",
    "34:9": "piston_head[facing=up,short=false,type=sticky]",
    "34:10": "piston_head[facing=north,short=false,type=sticky]",
    "34:11": "piston_head[facing=south,short=false,type=sticky]",
    "34:12": "piston_head[facing=west,short=false,type=sticky]",
    "34:13": "piston_head[facing=east,short=false,type=sticky]",
    "35:0": "white_wool",
    "35:1": "orange_wool",
    "35:2": "magenta_wool",
    "35:3": "light_blue_wool",
    "35:4": "yellow_wool",
    "35:5": "lime_wool",
    "35:6": "pink_wool",
    "35:7": "gray_wool",
    "35:8": "light_gray_wool",
    "35:9": "cyan_wool",
    "35:10": "purple_wool",
    "35:11": "blue_wool",
    "35:12": "brown_wool",
    "35:13": "green_wool",
    "35:14": "red_wool",
    "35:15": "black_wool",
    "36:0": "moving_piston[facing=down,type=normal]",
    "36:1": "moving_piston[facing=up,type=normal]",
    "36:2": "moving_piston[facing=north,type=normal]",
    "36:3": "moving_piston[facing=south,type=normal]",
    "36:4": "moving_piston[facing=west,type=normal]",
    "36:5": "moving_piston[facing=east,type=normal]",
    "36:8": "moving_piston[facing=down,type=sticky]",
    "36:9": "moving_piston[facing=up,type=sticky]",
    "36:10": "moving_piston[facing=north,type=sticky]",
    "36:11": "moving_piston[facing=south,type=sticky]",
    "36:12": "moving_piston[facing=west,type=sticky]",
    "36:13": "moving_piston[facing=east,type=sticky]",
    "37:0": "dandelion",
    "38:0": "poppy",
    "38:1": "blue_orchid",
    "38:2": "allium",
    "38:3": "azure_bluet",
    "38:4": "red_tulip",
    "38:5": "orange_tulip",
    "38:6": "white_tulip",
    "38:7": "pink_tulip",
    "38:8": "oxeye_daisy",
    "39:0": "brown_mushroom",
    "40:0": "red_mushroom",
    "41:0": "gold_block",
    "42:0": "iron_block",
    "43:0": "stone_slab[type=double]",
    "43:1": "sandstone_slab[type=double]",
    "43:2": "petrified_oak_slab[type=double]",
    "43:3": "cobblestone_slab[type=double]",
    "43:4": "brick_slab[type=double]",
    "43:5": "stone_brick_slab[type=double]",
    "43:6": "nether_brick_slab[type=double]",
    "43:7": "quartz_slab[type=double]",
    "43:8": "smooth_stone",
    "43:9": "smooth_sandstone",
    "43:10": "petrified_oak_slab[type=double]",
    "43:11": "cobblestone_slab[type=double]",
    "43:12": "brick_slab[type=double]",
    "43:13": "stone_brick_slab[type=double]",
    "43:14": "nether_brick_slab[type=double]",
    "43:15": "smooth_quartz",
    "44:0": "stone_slab[type=bottom]",
    "44:1": "sandstone_slab[type=bottom]",
    "44:2": "petrified_oak_slab[type=bottom]",
    "44:3": "cobblestone_slab[type=bottom]",
    "44:4": "brick_slab[type=bottom]",
    "44:5": "stone_brick_slab[type=bottom]",
    "44:6": "nether_brick_slab[type=bottom]",
    "44:7": "quartz_slab[type=bottom]",
    "44:8": "stone_slab[type=top]",
    "44:9": "sandstone_slab[type=top]",
    "44:10": "petrified_oak_slab[type=top]",
    "44:11": "cobblestone_slab[type=top]",
    "44:12": "brick_slab[type=top]",
    "44:13": "stone_brick_slab[type=top]",
    "44:14": "nether_brick_slab[type=top]",
    "44:15": "quartz_slab[type=top]",
    "45:0": "bricks",
    "46:0": "tnt[unstable=false]",
    "46:1": "tnt[unstable=true]",
    "47:0": "bookshelf",
    "48:0": "mossy_cobblestone",
    "49:0": "obsidian",
    "50:1": "wall_torch[facing=east]",
    "50:2": "wall_torch[facing=west]",
    "50:3": "wall_torch[facing=south]",
    "50:4": "wall_torch[facing=north]",
    "50:5": "torch",
    "51:0": "fire[age=0,east=false,north=false,south=false,up=false,west=false]",
    "51:1": "fire[age=1,east=false,north=false,south=false,up=false,west=false]",
    "51:2": "fire[age=2,east=false,north=false,south=false,up=false,west=false]",
    "51:3": "fire[age=3,east=false,north=false,south=false,up=false,west=false]",
    "51:4": "fire[age=4,east=false,north=false,south=false,up=false,west=false]",
    "51:5": "fire[age=5,east=false,north=false,south=false,up=false,west=false]",
    "51:6": "fire[age=6,east=false,north=false,south=false,up=false,west=false]",
    "51:7": "fire[age=7,east=false,north=false,south=false,up=false,west=false]",
    "51:8": "fire[age=8,east=false,north=false,south=false,up=false,west=false]",
    "51:9": "fire[age=9,east=false,north=false,south=false,up=false,west=false]",
    "51:10": "fire[age=10,east=false,north=false,south=false,up=false,west=false]",
    "51:11": "fire[age=11,east=false,north=false,south=false,up=false,west=false]",
    "51:12": "fire[age=12,east=false,north=false,south=false,up=false,west=false]",
    "51:13": "fire[age=13,east=false,north=false,south=false,up=false,west=false]",
    "51:14": "fire[age=14,east=false,north=false,south=false,up=false,west=false]",
    "51:15": "fire[age=15,east=false,north=false,south=false,up=false,west=false]",
    "52:0": "mob_spawner",
    "53:0": "oak_stairs[facing=east,half=bottom,shape=straight]",
    "53:1": "oak_stairs[facing=west,half=bottom,shape=straight]",
    "53:2": "oak_stairs[facing=south,half=bottom,shape=straight]",
    "53:3": "oak_stairs[facing=north,half=bottom,shape=straight]",
    "53:4": "oak_stairs[facing=east,half=top,shape=straight]",
    "53:5": "oak_stairs[facing=west,half=top,shape=straight]",
    "53:6": "oak_stairs[facing=south,half=top,shape=straight]",
    "53:7": "oak_stairs[facing=north,half=top,shape=straight]",
    "54:2": "chest[facing=north,type=single]",
    "54:3": "chest[facing=south,type=single]",
    "54:4": "chest[facing=west,type=single]",
    "54:5": "chest[facing=east,type=single]",
    "55:0": "redstone_wire[east=none,north=none,power=0,south=none,west=none]",
    "55:1": "redstone_wire[east=none,north=none,power=1,south=none,west=none]",
    "55:2": "redstone_wire[east=none,north=none,power=2,south=none,west=none]",
    "55:3": "redstone_wire[east=none,north=none,power=3,south=none,west=none]",
    "55:4": "redstone_wire[east=none,north=none,power=4,south=none,west=none]",
    "55:5": "redstone_wire[east=none,north=none,power=5,south=none,west=none]",
    "55:6": "redstone_wire[east=none,north=none,power=6,south=none,west=none]",
    "55:7": "redstone_wire[east=none,north=none,power=7,south=none,west=none]",
    "55:8": "redstone_wire[east=none,north=none,power=8,south=none,west=none]",
    "55:9": "redstone_wire[east=none,north=none,power=9,south=none,west=none]",
    "55:10": "redstone_wire[east=none,north=none,power=10,south=none,west=none]",
    "55:11": "redstone_wire[east=none,north=none,power=11,south=none,west=none]",
    "55:12": "redstone_wire[east=none,north=none,power=12,south=none,west=none]",
    "55:13": "redstone_wire[east=none,north=none,power=13,south=none,west=none]",
    "55:14": "redstone_wire[east=none,north=none,power=14,south=none,west=none]",
    "55:15": "redstone_wire[east=none,north=none,power=15,south=none,west=none]",
    "56:0": "diamond_ore",
    "57:0": "diamond_block",
    "58:0": "crafting_table",
    "59:0": "wheat[age=0]",
    "59:1": "wheat[age=1]",
    "59:2": "wheat[age=2]",
    "59:3": "wheat[age=3]",
    "59:4": "wheat[age=4]",
    "59:5": "wheat[age=5]",
    "59:6": "wheat[age=6]",
    "59:7": "wheat[age=7]",
    "60:0": "farmland[moisture=0]",
    "60:1": "farmland[moisture=1]",
    "60:2": "farmland[moisture=2]",
    "60:3": "farmland[moisture=3]",
    "60:4": "farmland[moisture=4]",
    "60:5": "farmland[moisture=5]",
    "60:6": "farmland[moisture=6]",
    "60:7": "farmland[moisture=7]",
    "61:2": "furnace[facing=north,lit=false]",
    "61:3": "furnace[facing=south,lit=false]",
    "61:4": "furnace[facing=west,lit=false]",
    "61:5": "furnace[facing=east,lit=false]",
    "62:2": "furnace[facing=north,lit=true]",
    "62:3": "furnace[facing=south,lit=true]",
    "62:4": "furnace[facing=west,lit=true]",
    "62:5": "furnace[facing=east,lit=true]",
    "63:0": "sign[rotation=0]",
    "63:1": "sign[rotation=1]",
    "63:2": "sign[rotation=2]",
    "63:3": "sign[rotation=3]",
    "63:4": "sign[rotation=4]",
    "63:5": "sign[rotation=5]",
    "63:6": "sign[rotation=6]",
    "63:7": "sign[rotation=7]",
    "63:8": "sign[rotation=8]",
    "63:9": "sign[rotation=9]",
    "63:10": "sign[rotation=10]",
    "63:11": "sign[rotation=11]",
    "63:12": "sign[rotation=12]",
    "63:13": "sign[rotation=13]",
    "63:14": "sign[rotation=14]",
    "63:15": "sign[rotation=15]",
    "64:0": "oak_door[facing=east,half=lower,hinge=right,open=false,powered=false]",
    "64:1": "oak_door[facing=south,half=lower,hinge=right,open=false,powered=false]",
    "64:2": "oak_door[facing=west,half=lower,hinge=right,open=false,powered=false]",
    "64:3": "oak_door[facing=north,half=lower,hinge=right,open=false,powered=false]",
    "64:4": "oak_door[facing=east,half=lower,hinge=right,open=true,powered=false]",
    "64:5": "oak_door[facing=south,half=lower,hinge=right,open=true,powered=false]",
    "64:6": "oak_door[facing=west,half=lower,hinge=right,open=true,powered=false]",
    "64:7": "oak_door[facing=north,half=lower,hinge=right,open=true,powered=false]",
    "64:8": "oak_door[facing=east,half=upper,hinge=left,open=false,powered=false]",
    "64:9": "oak_door[facing=east,half=upper,hinge=right,open=false,powered=false]",
    "64:10": "oak_door[facing=east,half=upper,hinge=left,open=false,powered=true]",
    "64:11": "oak_door[facing=east,half=upper,hinge=right,open=false,powered=true]",
    "65:2": "ladder[facing=north]",
    "65:3": "ladder[facing=south]",
    "65:4": "ladder[facing=west]",
    "65:5": "ladder[facing=east]",
    "66:0": "rail[shape=north_south]",
    "66:1": "rail[shape=east_west]",
    "66:2": "rail[shape=ascending_east]",
    "66:3": "rail[shape=ascending_west]",
    "66:4": "rail[shape=ascending_north]",
    "66:5": "rail[shape=ascending_south]",
    "66:6": "rail[shape=south_east]",
    "66:7": "rail[shape=south_west]",
    "66:8": "rail[shape=north_west]",
    "66:9": "rail[shape=north_east]",
    "67:0": "cobblestone_stairs[facing=east,half=bottom,shape=straight]",
    "67:1": "cobblestone_stairs[facing=west,half=bottom,shape=straight]",
    "67:2": "cobblestone_stairs[facing=south,half=bottom,shape=straight]",
    "67:3": "cobblestone_stairs[facing=north,half=bottom,shape=straight]",
    "67:4": "cobblestone_stairs[facing=east,half=top,shape=straight]",
    "67:5": "cobblestone_stairs[facing=west,half=top,shape=straight]",
    "67:6": "cobblestone_stairs[facing=south,half=top,shape=straight]",
    "67:7": "cobblestone_stairs[facing=north,half=top,shape=straight]",
    "68:2": "wall_sign[facing=north]",
    "68:3": "wall_sign[facing=south]",
    "68:4": "wall_sign[facing=west]",
    "68:5": "wall_sign[facing=east]",
    "69:0": "lever[face=ceiling,facing=west,powered=false]",
    "69:1": "lever[face=wall,facing=east,powered=false]",
    "69:2": "lever[face=wall,facing=west,powered=false]",
    "69:3": "lever[face=wall,facing=south,powered=false]",
    "69:4": "lever[face=wall,facing=north,powered=false]",
    "69:5": "lever[face=floor,facing=north,powered=false]",
    "69:6": "lever[face=floor,facing=west,powered=false]",
    "69:7": "lever[face=ceiling,facing=north,powered=false]",
    "69:8": "lever[face=ceiling,facing=west,powered=true]",
    "69:9": "lever[face=wall,facing=east,powered=true]",
    "69:10": "lever[face=wall,facing=west,powered=true]",
    "69:11": "lever[face=wall,facing=south,powered=true]",
    "69:12": "lever[face=wall,facing=north,powered=true]",
    "69:13": "lever[face=floor,facing=north,powered=true]",
    "69:14": "lever[face=floor,facing=west,powered=true]",
    "69:15": "lever[face=ceiling,facing=north,powered=true]",
    "70:0": "stone_pressure_plate[powered=false]",
    "70:1": "stone_pressure_plate[powered=true]",
    "71:0": "iron_door[facing=east,half=lower,hinge=right,open=false,powered=false]",
    "71:1": "iron_door[facing=south,half=lower,hinge=right,open=false,powered=false]",
    "71:2": "iron_door[facing=west,half=lower,hinge=right,open=false,powered=false]",
    "71:3": "iron_door[facing=north,half=lower,hinge=right,open=false,powered=false]",
    "71:4": "iron_door[facing=east,half=lower,hinge=right,open=true,powered=false]",
    "71:5": "iron_door[facing=south,half=lower,hinge=right,open=true,powered=false]",
    "71:6": "iron_door[facing=west,half=lower,hinge=right,open=true,powered=false]",
    "71:7": "iron_door[facing=north,half=lower,hinge=right,open=true,powered=false]",
    "71:8": "iron_door[facing=east,half=upper,hinge=left,open=false,powered=false]",
    "71:9": "iron_door[facing=east,half=upper,hinge=right,open=false,powered=false]",
    "71:10": "iron_door[facing=east,half=upper,hinge=left,open=false,powered=true]",
    "71:11": "iron_door[facing=east,half=upper,hinge=right,open=false,powered=true]",
    "72:0": "oak_pressure_plate[powered=false]",
    "72:1": "oak_pressure_plate[powered=true]",
    "73:0": "redstone_ore[lit=false]",
    "74:0": "redstone_ore[lit=true]",
    "75:1": "redstone_wall_torch[facing=east,lit=false]",
    "75:2": "redstone_wall_torch[facing=west,lit=false]",
    "75:3": "redstone_wall_torch[facing=south,lit=false]",
    "75:4": "redstone_wall_torch[facing=north,lit=false]",
    "75:5": "redstone_torch[lit=false]",
    "76:1": "redstone_wall_torch[facing=east,lit=true]",
    "76:2": "redstone_wall_torch[facing=west,lit=true]",
    "76:3": "redstone_wall_torch[facing=south,lit=true]",
    "76:4": "redstone_wall_torch[facing=north,lit=true]",
    "76:5": "redstone_torch[lit=true]",
    "77:0": "stone_button[face=ceiling,facing=north,powered=false]",
    "77:1": "stone_button[face=wall,facing=east,powered=false]",
    "77:2": "stone_button[face=wall,facing=west,powered=false]",
    "77:3": "stone_button[face=wall,facing=south,powered=false]",
    "77:4": "stone_button[face=wall,facing=north,powered=false]",
    "77:5": "stone_button[face=floor,facing=north,powered=false]",
    "77:8": "stone_button[face=ceiling,facing=north,powered=true]",
    "77:9": "stone_button[face=wall,facing=east,powered=true]",
    "77:10": "stone_button[face=wall,facing=west,powered=true]",
    "77:11": "stone_button[face=wall,facing=south,powered=true]",
    "77:12": "stone_button[face=wall,facing=north,powered=true]",
    "77:13": "stone_button[face=floor,facing=north,powered=true]",
    "78:0": "snow[layers=1]",
    "78:1": "snow[layers=2]",
    "78:2": "snow[layers=3]",
    "78:3": "snow[layers=4]",
    "78:4": "snow[layers=5]",
    "78:5": "snow[layers=6]",
    "78:6": "snow[layers=7]",
    "78:7": "snow[layers=8]",
    "79:0": "ice",
    "80:0": "snow_block",
    "81:0": "cactus[age=0]",
    "81:1": "cactus[age=1]",
    "81:2": "cactus[age=2]",
    "81:3": "cactus[age=3]",
    "81:4": "cactus[age=4]",
    "81:5": "cactus[age=5]",
    "81:6": "cactus[age=6]",
    "81:7": "cactus[age=7]",
    "81:8": "cactus[age=8]",
    "81:9": "cactus[age=9]",
    "81:10": "cactus[age=10]",
    "81:11": "cactus[age=11]",
    "81:12": "cactus[age=12]",
    "81:13": "cactus[age=13]",
    "81:14": "cactus[age=14]",
    "81:15": "cactus[age=15]",
    "82:0": "clay",
    "83:0": "sugar_cane[age=0]",
    "83:1": "sugar_cane[age=1]",
    "83:2": "sugar_cane[age=2]",
    "83:3": "sugar_cane[age=3]",
    "83:4": "sugar_cane[age=4]",
    "83:5": "sugar_cane[age=5]",
    "83:6": "sugar_cane[age=6]",
    "83:7": "sugar_cane[age=7]",
    "83:8": "sugar_cane[age=8]",
    "83:9": "sugar_cane[age=9]",
    "83:10": "sugar_cane[age=10]",
    "83:11": "sugar_cane[age=11]",
    "83:12": "sugar_cane[age=12]",
    "83:13": "sugar_cane[age=13]",
    "83:14": "sugar_cane[age=14]",
    "83:15": "sugar_cane[age=15]",
    "84:0": "jukebox[has_record=false]",
    "84:1": "jukebox[has_record=true]",
    "85:0": "oak_fence[east=false,north=false,south=false,west=false]",
    "86:0": "carved_pumpkin[facing=south]",
    "86:1": "carved_pumpkin[facing=west]",
    "86:2": "carved_pumpkin[facing=north]",
    "86:3": "carved_pumpkin[facing=east]",
    "87:0": "netherrack",
    "88:0": "soul_sand",
    "89:0": "glowstone",
    "90:1": "portal[axis=x]",
    "90:2": "portal[axis=z]",
    "91:0": "jack_o_lantern[facing=south]",
    "91:1": "jack_o_lantern[facing=west]",
    "91:2": "jack_o_lantern[facing=north]",
    "91:3": "jack_o_lantern[facing=east]",
    "92:0": "cake[bites=0]",
    "92:1": "cake[bites=1]",
    "92:2": "cake[bites=2]",
    "92:3": "cake[bites=3]",
    "92:4": "cake[bites=4]",
    "92:5": "cake[bites=5]",
    "92:6": "cake[bites=6]",
    "93:0": "repeater[delay=1,facing=south,locked=false,powered=false]",
    "93:1": "repeater[delay=1,facing=west,locked=false,powered=false]",
    "93:2": "repeater[delay=1,facing=north,locked=false,powered=false]",
    "93:3": "repeater[delay=1,facing=east,locked=false,powered=false]",
    "93:4": "repeater[delay=2,facing=south,locked=false,powered=false]",
    "93:5": "repeater[delay=2,facing=west,locked=false,powered=false]",
    "93:6": "repeater[delay=2,facing=north,locked=false,powered=false]",
    "93:7": "repeater[delay=2,facing=east,locked=false,powered=false]",
    "93:8": "repeater[delay=3,facing=south,locked=false,powered=false]",
    "93:9": "repeater[delay=3,facing=west,locked=false,powered=false]",
    "93:10": "repeater[delay=3,facing=north,locked=false,powered=false]",
    "93:11": "repeater[delay=3,facing=east,locked=false,powered=false]",
    "93:12": "repeater[delay=4,facing=south,locked=false,powered=false]",
    "93:13": "repeater[delay=4,facing=west,locked=false,powered=false]",
    "93:14": "repeater[delay=4,facing=north,locked=false,powered=false]",
    "93:15": "repeater[delay=4,facing=east,locked=false,powered=false]",
    "94:0": "repeater[delay=1,facing=south,locked=false,powered=true]",
    "94:1": "repeater[delay=1,facing=west,locked=false,powered=true]",
    "94:2": "repeater[delay=1,facing=north,locked=false,powered=true]",
    "94:3": "repeater[delay=1,facing=east,locked=false,powered=true]",
    "94:4": "repeater[delay=2,facing=south,locked=false,powered=true]",
    "94:5": "repeater[delay=2,facing=west,locked=false,powered=true]",
    "94:6": "repeater[delay=2,facing=north,locked=false,powered=true]",
    "94:7": "repeater[delay=2,facing=east,locked=false,powered=true]",
    "94:8": "repeater[delay=3,facing=south,locked=false,powered=true]",
    "94:9": "repeater[delay=3,facing=west,locked=false,powered=true]",
    "94:10": "repeater[delay=3,facing=north,locked=false,powered=true]",
    "94:11": "repeater[delay=3,facing=east,locked=false,powered=true]",
    "94:12": "repeater[delay=4,facing=south,locked=false,powered=true]",
    "94:13": "repeater[delay=4,facing=west,locked=false,powered=true]",
    "94:14": "repeater[delay=4,facing=north,locked=false,powered=true]",
    "94:15": "repeater[delay=4,facing=east,locked=false,powered=true]",
    "95:0": "white_stained_glass",
    "95:1": "orange_stained_glass",
    "95:2": "magenta_stained_glass",
    "95:3": "light_blue_stained_glass",
    "95:4": "yellow_stained_glass",
    "95:5": "lime_stained_glass",
    "95:6": "pink_stained_glass",
    "95:7": "gray_stained_glass",
    "95:8": "light_gray_stained_glass",
    "95:9": "cyan_stained_glass",
    "95:10": "purple_stained_glass",
    "95:11": "blue_stained_glass",
    "95:12": "brown_stained_glass",
    "95:13": "green_stained_glass",
    "95:14": "red_stained_glass",
    "95:15": "black_stained_glass",
    "96:0": "oak_trapdoor[facing=north,half=bottom,open=false]",
    "96:1": "oak_trapdoor[facing=south,half=bottom,open=false]",
    "96:2": "oak_trapdoor[facing=west,half=bottom,open=false]",
    "96:3": "oak_trapdoor[facing=east,half=bottom,open=false]",
    "96:4": "oak_trapdoor[facing=north,half=bottom,open=true]",
    "96:5": "oak_trapdoor[facing=south,half=bottom,open=true]",
    "96:6": "oak_trapdoor[facing=west,half=bottom,open=true]",
    "96:7": "oak_trapdoor[facing=east,half=bottom,open=true]",
    "96:8": "oak_trapdoor[facing=north,half=top,open=false]",
    "96:9": "oak_trapdoor[facing=south,half=top,open=false]",
    "96:10": "oak_trapdoor[facing=west,half=top,open=false]",
    "96:11": "oak_trapdoor[facing=east,half=top,open=false]",
    "96:12": "oak_trapdoor[facing=north,half=top,open=true]",
    "96:13": "oak_trapdoor[facing=south,half=top,open=true]",
    "96:14": "oak_trapdoor[facing=west,half=top,open=true]",
    "96:15": "oak_trapdoor[facing=east,half=top,open=true]",
    "97:0": "infested_stone",
    "97:1": "infested_cobblestone",
    "97:2": "infested_stone_bricks",
    "97:3": "infested_mossy_stone_bricks",
    "97:4": "infested_cracked_stone_bricks",
    "97:5": "infested_chiseled_stone_bricks",
    "98:0": "stone_bricks",
    "98:1": "mossy_stone_bricks",
    "98:2": "cracked_stone_bricks",
    "98:3": "chiseled_stone_bricks",
    "99:0": "brown_mushroom_block[north=false,east=false,south=false,west=false,up=false,down=false]",
    "99:1": "brown_mushroom_block[north=true,east=false,south=false,west=true,up=true,down=false]",
    "99:2": "brown_mushroom_block[north=true,east=false,south=false,west=false,up=true,down=false]",
    "99:3": "brown_mushroom_block[north=true,east=true,south=false,west=false,up=true,down=false]",
    "99:4": "brown_mushroom_block[north=false,east=false,south=false,west=true,up=true,down=false]",
    "99:5": "brown_mushroom_block[north=false,east=false,south=false,west=false,up=true,down=false]",
    "99:6": "brown_mushroom_block[north=false,east=true,south=false,west=false,up=true,down=false]",
    "99:7": "brown_mushroom_block[north=false,east=false,south=true,west=true,up=true,down=false]",
    "99:8": "brown_mushroom_block[north=false,east=false,south=true,west=false,up=true,down=false]",
    "99:9": "brown_mushroom_block[north=false,east=true,south=true,west=false,up=true,down=false]",
    "99:10": "mushroom_stem[north=true,east=true,south=true,west=true,up=false,down=false]",
    "99:14": "brown_mushroom_block[north=true,east=true,south=true,west=true,up=true,down=true]",
    "99:15": "mushroom_stem[north=true,east=true,south=true,west=true,up=true,down=true]",
    "100:0": "red_mushroom_block[north=false,east=false,south=false,west=false,up=false,down=false]",
    "100:1": "red_mushroom_block[north=true,east=false,south=false,west=true,up=true,down=false]",
    "100:2": "red_mushroom_block[north=true,east=false,south=false,west=false,up=true,down=false]",
    "100:3": "red_mushroom_block[north=true,east=true,south=false,west=false,up=true,down=false]",
    "100:4": "red_mushroom_block[north=false,east=false,south=false,west=true,up=true,down=false]",
    "100:5": "red_mushroom_block[north=false,east=false,south=false,west=false,up=true,down=false]",
    "100:6": "red_mushroom_block[north=false,east=true,south=false,west=false,up=true,down=false]",
    "100:7": "red_mushroom_block[north=false,east=false,south=true,west=true,up=true,down=false]",
    "100:8": "red_mushroom_block[north=false,east=false,south=true,west=false,up=true,down=false]",
    "100:9": "red_mushroom_block[north=false,east=true,south=true,west=false,up=true,down=false]",
    "100:10": "mushroom_stem[north=true,east=true,south=true,west=true,up=false,down=false]",
    "100:14": "red_mushroom_block[north=true,east=true,south=true,west=true,up=true,down=true]",
    "100:15": "mushroom_stem[north=true,east=true,south=true,west=true,up=true,down=true]",
    "101:0": "iron_bars[east=false,north=false,south=false,west=false]",
    "102:0": "glass_pane[east=false,north=false,south=false,west=false]",
    "103:0": "melon_block",
    "104:0": "pumpkin_stem[age=0]",
    "104:1": "pumpkin_stem[age=1]",
    "104:2": "pumpkin_stem[age=2]",
    "104:3": "pumpkin_stem[age=3]",
    "104:4": "pumpkin_stem[age=4]",
    "104:5": "pumpkin_stem[age=5]",
    "104:6": "pumpkin_stem[age=6]",
    "104:7": "pumpkin_stem[age=7]",
    "105:0": "melon_stem[age=0]",
    "105:1": "melon_stem[age=1]",
    "105:2": "melon_stem[age=2]",
    "105:3": "melon_stem[age=3]",
    "105:4": "melon_stem[age=4]",
    "105:5": "melon_stem[age=5]",
    "105:6": "melon_stem[age=6]",
    "105:7": "melon_stem[age=7]",
    "106:0": "vine[east=false,north=false,south=false,up=true,west=false]",
    "106:1": "vine[east=false,north=false,south=true,up=true,west=false]",
    "106:2": "vine[east=false,north=false,south=false,up=true,west=true]",
    "106:3": "vine[east=false,north=false,south=true,up=true,west=true]",
    "106:4": "vine[east=false,north=true,south=false,up=true,west=false]",
    "106:5": "vine[east=false,north=true,south=true,up=true,west=false]",
    "106:6": "vine[east=false,north=true,south=false,up=true,west=true]",
    "106:7": "vine[east=false,north=true,south=true,up=true,west=true]",
    "106:8": "vine[east=true,north=false,south=false,up=true,west=false]",
    "106:9": "vine[east=true,north=false,south=true,up=true,west=false]",
    "106:10": "vine[east=true,north=false,south=false,up=true,west=true]",
    "106:11": "vine[east=true,north=false,south=true,up=true,west=true]",
    "106:12": "vine[east=true,north=true,south=false,up=true,west=false]",
    "106:13": "vine[east=true,north=true,south=true,up=true,west=false]",
    "106:14": "vine[east=true,north=true,south=false,up=true,west=true]",
    "106:15": "vine[east=true,north=true,south=true,up=true,west=true]",
    "107:0": "oak_fence_gate[facing=south,in_wall=false,open=false,powered=false]",
    "107:1": "oak_fence_gate[facing=west,in_wall=false,open=false,powered=false]",
    "107:2": "oak_fence_gate[facing=north,in_wall=false,open=false,powered=false]",
    "107:3": "oak_fence_gate[facing=east,in_wall=false,open=false,powered=false]",
    "107:4": "oak_fence_gate[facing=south,in_wall=false,open=true,powered=false]",
    "107:5": "oak_fence_gate[facing=west,in_wall=false,open=true,powered=false]",
    "107:6": "oak_fence_gate[facing=north,in_wall=false,open=true,powered=false]",
    "107:7": "oak_fence_gate[facing=east,in_wall=false,open=true,powered=false]",
    "107:8": "oak_fence_gate[facing=south,in_wall=false,open=false,powered=true]",
    "107:9": "oak_fence_gate[facing=west,in_wall=false,open=false,powered=true]",
    "107:10": "oak_fence_gate[facing=north,in_wall=false,open=false,powered=true]",
    "107:11": "oak_fence_gate[facing=east,in_wall=false,open=false,powered=true]",
    "107:12": "oak_fence_gate[facing=south,in_wall=false,open=true,powered=true]",
    "107:13": "oak_fence_gate[facing=west,in_wall=false,open=true,powered=true]",
    "107:14": "oak_fence_gate[facing=north,in_wall=false,open=true,powered=true]",
    "107:15": "oak_fence_gate[facing=east,in_wall=false,open=true,powered=true]",
    "108:0": "brick_stairs[facing=east,half=bottom,shape=straight]",
    "108:1": "brick_stairs[facing=west,half=bottom,shape=straight]",
    "108:2": "brick_stairs[facing=south,half=bottom,shape=straight]",
    "108:3": "brick_stairs[facing=north,half=bottom,shape=straight]",
    "108:4": "brick_stairs[facing=east,half=top,shape=straight]",
    "108:5": "brick_stairs[facing=west,half=top,shape=straight]",
    "108:6": "brick_stairs[facing=south,half=top,shape=straight]",
    "108:7": "brick_stairs[facing=north,half=top,shape=straight]",
    "109:0": "stone_brick_stairs[facing=east,half=bottom,shape=straight]",
    "109:1": "stone_brick_stairs[facing=west,half=bottom,shape=straight]",
    "109:2": "stone_brick_stairs[facing=south,half=bottom,shape=straight]",
    "109:3": "stone_brick_stairs[facing=north,half=bottom,shape=straight]",
    "109:4": "stone_brick_stairs[facing=east,half=top,shape=straight]",
    "109:5": "stone_brick_stairs[facing=west,half=top,shape=straight]",
    "109:6": "stone_brick_stairs[facing=south,half=top,shape=straight]",
    "109:7": "stone_brick_stairs[facing=north,half=top,shape=straight]",
    "110:0": "mycelium[snowy=false]",
    "111:0": "lily_pad",
    "112:0": "nether_bricks",
    "113:0": "nether_brick_fence[east=false,north=false,south=false,west=false]",
    "114:0": "nether_brick_stairs[facing=east,half=bottom,shape=straight]",
    "114:1": "nether_brick_stairs[facing=west,half=bottom,shape=straight]",
    "114:2": "nether_brick_stairs[facing=south,half=bottom,shape=straight]",
    "114:3": "nether_brick_stairs[facing=north,half=bottom,shape=straight]",
    "114:4": "nether_brick_stairs[facing=east,half=top,shape=straight]",
    "114:5": "nether_brick_stairs[facing=west,half=top,shape=straight]",
    "114:6": "nether_brick_stairs[facing=south,half=top,shape=straight]",
    "114:7": "nether_brick_stairs[facing=north,half=top,shape=straight]",
    "115:0": "nether_wart[age=0]",
    "115:1": "nether_wart[age=1]",
    "115:2": "nether_wart[age=2]",
    "115:3": "nether_wart[age=3]",
    "116:0": "enchanting_table",
    "117:0": "brewing_stand[has_bottle_0=false,has_bottle_1=false,has_bottle_2=false]",
    "117:1": "brewing_stand[has_bottle_0=true,has_bottle_1=false,has_bottle_2=false]",
    "117:2": "brewing_stand[has_bottle_0=false,has_bottle_1=true,has_bottle_2=false]",
    "117:3": "brewing_stand[has_bottle_0=true,has_bottle_1=true,has_bottle_2=false]",
    "117:4": "brewing_stand[has_bottle_0=false,has_bottle_1=false,has_bottle_2=true]",
    "117:5": "brewing_stand[has_bottle_0=true,has_bottle_1=false,has_bottle_2=true]",
    "117:6": "brewing_stand[has_bottle_0=false,has_bottle_1=true,has_bottle_2=true]",
    "117:7": "brewing_stand[has_bottle_0=true,has_bottle_1=true,has_bottle_2=true]",
    "118:0": "cauldron[level=0]",
    "118:1": "cauldron[level=1]",
    "118:2": "cauldron[level=2]",
    "118:3": "cauldron[level=3]",
    "119:0": "end_portal",
    "120:0": "end_portal_frame[eye=false,facing=south]",
    "120:1": "end_portal_frame[eye=false,facing=west]",
    "120:2": "end_portal_frame[eye=false,facing=north]",
    "120:3": "end_portal_frame[eye=false,facing=east]",
    "120:4": "end_portal_frame[eye=true,facing=south]",
    "120:5": "end_portal_frame[eye=true,facing=west]",
    "120:6": "end_portal_frame[eye=true,facing=north]",
    "120:7": "end_portal_frame[eye=true,facing=east]",
    "121:0": "end_stone",
    "122:0": "dragon_egg",
    "123:0": "redstone_lamp[lit=false]",
    "124:0": "redstone_lamp[lit=true]",
    "125:0": "oak_slab[type=double]",
    "125:1": "spruce_slab[type=double]",
    "125:2": "birch_slab[type=double]",
    "125:3": "jungle_slab[type=double]",
    "125:4": "acacia_slab[type=double]",
    "125:5": "dark_oak_slab[type=double]",
    "126:0": "oak_slab[type=bottom]",
    "126:1": "spruce_slab[type=bottom]",
    "126:2": "birch_slab[type=bottom]",
    "126:3": "jungle_slab[type=bottom]",
    "126:4": "acacia_slab[type=bottom]",
    "126:5": "dark_oak_slab[type=bottom]",
    "126:8": "oak_slab[type=top]",
    "126:9": "spruce_slab[type=top]",
    "126:10": "birch_slab[type=top]",
    "126:11": "jungle_slab[type=top]",
    "126:12": "acacia_slab[type=top]",
    "126:13": "dark_oak_slab[type=top]",
    "127:0": "cocoa[age=0,facing=south]",
    "127:1": "cocoa[age=0,facing=west]",
    "127:2": "cocoa[age=0,facing=north]",
    "127:3": "cocoa[age=0,facing=east]",
    "127:4": "cocoa[age=1,facing=south]",
    "127:5": "cocoa[age=1,facing=west]",
    "127:6": "cocoa[age=1,facing=north]",
    "127:7": "cocoa[age=1,facing=east]",
    "127:8": "cocoa[age=2,facing=south]",
    "127:9": "cocoa[age=2,facing=west]",
    "127:10": "cocoa[age=2,facing=north]",
    "127:11": "cocoa[age=2,facing=east]",
    "128:0": "sandstone_stairs[facing=east,half=bottom,shape=straight]",
    "128:1": "sandstone_stairs[facing=west,half=bottom,shape=straight]",
    "128:2": "sandstone_stairs[facing=south,half=bottom,shape=straight]",
    "128:3": "sandstone_stairs[facing=north,half=bottom,shape=straight]",
    "128:4": "sandstone_stairs[facing=east,half=top,shape=straight]",
    "128:5": "sandstone_stairs[facing=west,half=top,shape=straight]",
    "128:6": "sandstone_stairs[facing=south,half=top,shape=straight]",
    "128:7": "sandstone_stairs[facing=north,half=top,shape=straight]",
    "129:0": "emerald_ore",
    "130:2": "ender_chest[facing=north]",
    "130:3": "ender_chest[facing=south]",
    "130:4": "ender_chest[facing=west]",
    "130:5": "ender_chest[facing=east]",
    "131:0": "tripwire_hook[attached=false,facing=south,powered=false]",
    "131:1": "tripwire_hook[attached=false,facing=west,powered=false]",
    "131:2": "tripwire_hook[attached=false,facing=north,powered=false]",
    "131:3": "tripwire_hook[attached=false,facing=east,powered=false]",
    "131:4": "tripwire_hook[attached=true,facing=south,powered=false]",
    "131:5": "tripwire_hook[attached=true,facing=west,powered=false]",
    "131:6": "tripwire_hook[attached=true,facing=north,powered=false]",
    "131:7": "tripwire_hook[attached=true,facing=east,powered=false]",
    "131:8": "tripwire_hook[attached=false,facing=south,powered=true]",
    "131:9": "tripwire_hook[attached=false,facing=west,powered=true]",
    "131:10": "tripwire_hook[attached=false,facing=north,powered=true]",
    "131:11": "tripwire_hook[attached=false,facing=east,powered=true]",
    "131:12": "tripwire_hook[attached=true,facing=south,powered=true]",
    "131:13": "tripwire_hook[attached=true,facing=west,powered=true]",
    "131:14": "tripwire_hook[attached=true,facing=north,powered=true]",
    "131:15": "tripwire_hook[attached=true,facing=east,powered=true]",
    "132:0": "tripwire[attached=false,disarmed=false,east=false,north=false,powered=false,south=false,west=false]",
    "132:1": "tripwire[attached=false,disarmed=false,east=false,north=false,powered=true,south=false,west=false]",
    "132:4": "tripwire[attached=true,disarmed=false,east=false,north=false,powered=false,south=false,west=false]",
    "132:5": "tripwire[attached=true,disarmed=false,east=false,north=false,powered=true,south=false,west=false]",
    "132:8": "tripwire[attached=false,disarmed=true,east=false,north=false,powered=false,south=false,west=false]",
    "132:9": "tripwire[attached=false,disarmed=true,east=false,north=false,powered=true,south=false,west=false]",
    "132:12": "tripwire[attached=true,disarmed=true,east=false,north=false,powered=false,south=false,west=false]",
    "132:13": "tripwire[attached=true,disarmed=true,east=false,north=false,powered=true,south=false,west=false]",
    "133:0": "emerald_block",
    "134:0": "spruce_stairs[facing=east,half=bottom,shape=straight]",
    "134:1": "spruce_stairs[facing=west,half=bottom,shape=straight]",
    "134:2": "spruce_stairs[facing=south,half=bottom,shape=straight]",
    "134:3": "spruce_stairs[facing=north,half=bottom,shape=straight]",
    "134:4": "spruce_stairs[facing=east,half=top,shape=straight]",
    "134:5": "spruce_stairs[facing=west,half=top,shape=straight]",
    "134:6": "spruce_stairs[facing=south,half=top,shape=straight]",
    "134:7": "spruce_stairs[facing=north,half=top,shape=straight]",
    "135:0": "birch_stairs[facing=east,half=bottom,shape=straight]",
    "135:1": "birch_stairs[facing=west,half=bottom,shape=straight]",
    "135:2": "birch_stairs[facing=south,half=bottom,shape=straight]",
    "135:3": "birch_stairs[facing=north,half=bottom,shape=straight]",
    "135:4": "birch_stairs[facing=east,half=top,shape=straight]",
    "135:5": "birch_stairs[facing=west,half=top,shape=straight]",
    "135:6": "birch_stairs[facing=south,half=top,shape=straight]",
    "135:7": "birch_stairs[facing=north,half=top,shape=straight]",
    "136:0": "jungle_stairs[facing=east,half=bottom,shape=straight]",
    "136:1": "jungle_stairs[facing=west,half=bottom,shape=straight]",
    "136:2": "jungle_stairs[facing=south,half=bottom,shape=straight]",
    "136:3": "jungle_stairs[facing=north,half=bottom,shape=straight]",
    "136:4": "jungle_stairs[facing=east,half=top,shape=straight]",
    "136:5": "jungle_stairs[facing=west,half=top,shape=straight]",
    "136:6": "jungle_stairs[facing=south,half=top,shape=straight]",
    "136:7": "jungle_stairs[facing=north,half=top,shape=straight]",
    "137:0": "command_block[conditional=false,facing=down]",
    "137:1": "command_block[conditional=false,facing=up]",
    "137:2": "command_block[conditional=false,facing=north]",
    "137:3": "command_block[conditional=false,facing=south]",
    "137:4": "command_block[conditional=false,facing=west]",
    "137:5": "command_block[conditional=false,facing=east]",
    "137:8": "command_block[conditional=true,facing=down]",
    "137:9": "command_block[conditional=true,facing=up]",
    "137:10": "command_block[conditional=true,facing=north]",
    "137:11": "command_block[conditional=true,facing=south]",
    "137:12": "command_block[conditional=true,facing=west]",
    "137:13": "command_block[conditional=true,facing=east]",
    "138:0": "beacon",
    "139:0": "cobblestone_wall[east=false,north=false,south=false,up=false,west=false]",
    "139:1": "mossy_cobblestone_wall[east=false,north=false,south=false,up=false,west=false]",
    "140:0": "potted_cactus",
    "140:1": "potted_cactus",
    "140:2": "potted_cactus",
    "140:3": "potted_cactus",
    "140:4": "potted_cactus",
    "140:5": "potted_cactus",
    "140:6": "potted_cactus",
    "140:7": "potted_cactus",
    "140:8": "potted_cactus",
    "140:9": "potted_cactus",
    "140:10": "potted_cactus",
    "140:11": "potted_cactus",
    "140:12": "potted_cactus",
    "140:13": "potted_cactus",
    "140:14": "potted_cactus",
    "140:15": "potted_cactus",
    "141:0": "carrots[age=0]",
    "141:1": "carrots[age=1]",
    "141:2": "carrots[age=2]",
    "141:3": "carrots[age=3]",
    "141:4": "carrots[age=4]",
    "141:5": "carrots[age=5]",
    "141:6": "carrots[age=6]",
    "141:7": "carrots[age=7]",
    "142:0": "potatoes[age=0]",
    "142:1": "potatoes[age=1]",
    "142:2": "potatoes[age=2]",
    "142:3": "potatoes[age=3]",
    "142:4": "potatoes[age=4]",
    "142:5": "potatoes[age=5]",
    "142:6": "potatoes[age=6]",
    "142:7": "potatoes[age=7]",
    "143:0": "oak_button[face=ceiling,facing=north,powered=false]",
    "143:1": "oak_button[face=wall,facing=east,powered=false]",
    "143:2": "oak_button[face=wall,facing=west,powered=false]",
    "143:3": "oak_button[face=wall,facing=south,powered=false]",
    "143:4": "oak_button[face=wall,facing=north,powered=false]",
    "143:5": "oak_button[face=floor,facing=north,powered=false]",
    "143:8": "oak_button[face=ceiling,facing=north,powered=true]",
    "143:9": "oak_button[face=wall,facing=east,powered=true]",
    "143:10": "oak_button[face=wall,facing=west,powered=true]",
    "143:11": "oak_button[face=wall,facing=south,powered=true]",
    "143:12": "oak_button[face=wall,facing=north,powered=true]",
    "143:13": "oak_button[face=floor,facing=north,powered=true]",
    "144:0": "player_head[facing=down]",
    "144:1": "player_head[facing=up]",
    "144:2": "player_head[facing=north]",
    "144:3": "player_head[facing=south]",
    "144:4": "player_head[facing=west]",
    "144:5": "player_head[facing=east]",
    "144:8": "player_head[facing=down]",
    "144:9": "player_head[facing=up]",
    "144:10": "player_head[facing=north]",
    "144:11": "player_head[facing=south]",
    "144:12": "player_head[facing=west]",
    "144:13": "player_head[facing=east]",
    "145:0": "anvil[facing=south]",
    "145:1": "anvil[facing=west]",
    "145:2": "anvil[facing=north]",
    "145:3": "anvil[facing=east]",
    "145:4": "chipped_anvil[facing=south]",
    "145:5": "chipped_anvil[facing=west]",
    "145:6": "chipped_anvil[facing=north]",
    "145:7": "chipped_anvil[facing=east]",
    "145:8": "damaged_anvil[facing=south]",
    "145:9": "damaged_anvil[facing=west]",
    "145:10": "damaged_anvil[facing=north]",
    "145:11": "damaged_anvil[facing=east]",
    "146:2": "trapped_chest[facing=north,type=single]",
    "146:3": "trapped_chest[facing=south,type=single]",
    "146:4": "trapped_chest[facing=west,type=single]",
    "146:5": "trapped_chest[facing=east,type=single]",
    "147:0": "light_weighted_pressure_plate[power=0]",
    "147:1": "light_weighted_pressure_plate[power=1]",
    "147:2": "light_weighted_pressure_plate[power=2]",
    "147:3": "light_weighted_pressure_plate[power=3]",
    "147:4": "light_weighted_pressure_plate[power=4]",
    "147:5": "light_weighted_pressure_plate[power=5]",
    "147:6": "light_weighted_pressure_plate[power=6]",
    "147:7": "light_weighted_pressure_plate[power=7]",
    "147:8": "light_weighted_pressure_plate[power=8]",
    "147:9": "light_weighted_pressure_plate[power=9]",
    "147:10": "light_weighted_pressure_plate[power=10]",
    "147:11": "light_weighted_pressure_plate[power=11]",
    "147:12": "light_weighted_pressure_plate[power=12]",
    "147:13": "light_weighted_pressure_plate[power=13]",
    "147:14": "light_weighted_pressure_plate[power=14]",
    "147:15": "light_weighted_pressure_plate[power=15]",
    "148:0": "heavy_weighted_pressure_plate[power=0]",
    "148:1": "heavy_weighted_pressure_plate[power=1]",
    "148:2": "heavy_weighted_pressure_plate[power=2]",
    "148:3": "heavy_weighted_pressure_plate[power=3]",
    "148:4": "heavy_weighted_pressure_plate[power=4]",
    "148:5": "heavy_weighted_pressure_plate[power=5]",
    "148:6": "heavy_weighted_pressure_plate[power=6]",
    "148:7": "heavy_weighted_pressure_plate[power=7]",
    "148:8": "heavy_weighted_pressure_plate[power=8]",
    "148:9": "heavy_weighted_pressure_plate[power=9]",
    "148:10": "heavy_weighted_pressure_plate[power=10]",
    "148:11": "heavy_weighted_pressure_plate[power=11]",
    "148:12": "heavy_weighted_pressure_plate[power=12]",
    "148:13": "heavy_weighted_pressure_plate[power=13]",
    "148:14": "heavy_weighted_pressure_plate[power=14]",
    "148:15": "heavy_weighted_pressure_plate[power=15]",
    "149:0": "comparator[facing=south,mode=compare,powered=false]",
    "149:1": "comparator[facing=west,mode=compare,powered=false]",
    "149:2": "comparator[facing=north,mode=compare,powered=false]",
    "149:3": "comparator[facing=east,mode=compare,powered=false]",
    "149:4": "comparator[facing=south,mode=subtract,powered=false]",
    "149:5": "comparator[facing=west,mode=subtract,powered=false]",
    "149:6": "comparator[facing=north,mode=subtract,powered=false]",
    "149:7": "comparator[facing=east,mode=subtract,powered=false]",
    "149:8": "comparator[facing=south,mode=compare,powered=true]",
    "149:9": "comparator[facing=west,mode=compare,powered=true]",
    "149:10": "comparator[facing=north,mode=compare,powered=true]",
    "149:11": "comparator[facing=east,mode=compare,powered=true]",
    "149:12": "comparator[facing=south,mode=subtract,powered=true]",
    "149:13": "comparator[facing=west,mode=subtract,powered=true]",
    "149:14": "comparator[facing=north,mode=subtract,powered=true]",
    "149:15": "comparator[facing=east,mode=subtract,powered=true]",
    "150:0": "comparator[facing=south,mode=compare,powered=false]",
    "150:1": "comparator[facing=west,mode=compare,powered=false]",
    "150:2": "comparator[facing=north,mode=compare,powered=false]",
    "150:3": "comparator[facing=east,mode=compare,powered=false]",
    "150:4": "comparator[facing=south,mode=subtract,powered=false]",
    "150:5": "comparator[facing=west,mode=subtract,powered=false]",
    "150:6": "comparator[facing=north,mode=subtract,powered=false]",
    "150:7": "comparator[facing=east,mode=subtract,powered=false]",
    "150:8": "comparator[facing=south,mode=compare,powered=true]",
    "150:9": "comparator[facing=west,mode=compare,powered=true]",
    "150:10": "comparator[facing=north,mode=compare,powered=true]",
    "150:11": "comparator[facing=east,mode=compare,powered=true]",
    "150:12": "comparator[facing=south,mode=subtract,powered=true]",
    "150:13": "comparator[facing=west,mode=subtract,powered=true]",
    "150:14": "comparator[facing=north,mode=subtract,powered=true]",
    "150:15": "comparator[facing=east,mode=subtract,powered=true]",
    "151:0": "daylight_detector[inverted=false,power=0]",
    "151:1": "daylight_detector[inverted=false,power=1]",
    "151:2": "daylight_detector[inverted=false,power=2]",
    "151:3": "daylight_detector[inverted=false,power=3]",
    "151:4": "daylight_detector[inverted=false,power=4]",
    "151:5": "daylight_detector[inverted=false,power=5]",
    "151:6": "daylight_detector[inverted=false,power=6]",
    "151:7": "daylight_detector[inverted=false,power=7]",
    "151:8": "daylight_detector[inverted=false,power=8]",
    "151:9": "daylight_detector[inverted=false,power=9]",
    "151:10": "daylight_detector[inverted=false,power=10]",
    "151:11": "daylight_detector[inverted=false,power=11]",
    "151:12": "daylight_detector[inverted=false,power=12]",
    "151:13": "daylight_detector[inverted=false,power=13]",
    "151:14": "daylight_detector[inverted=false,power=14]",
    "151:15": "daylight_detector[inverted=false,power=15]",
    "152:0": "redstone_block",
    "153:0": "nether_quartz_ore",
    "154:0": "hopper[enabled=true,facing=down]",
    "154:2": "hopper[enabled=true,facing=north]",
    "154:3": "hopper[enabled=true,facing=south]",
    "154:4": "hopper[enabled=true,facing=west]",
    "154:5": "hopper[enabled=true,facing=east]",
    "154:8": "hopper[enabled=false,facing=down]",
    "154:10": "hopper[enabled=false,facing=north]",
    "154:11": "hopper[enabled=false,facing=south]",
    "154:12": "hopper[enabled=false,facing=west]",
    "154:13": "hopper[enabled=false,facing=east]",
    "155:0": "quartz_block",
    "155:1": "chiseled_quartz_block",
    "155:2": "quartz_pillar[axis=y]",
    "155:3": "quartz_pillar[axis=x]",
    "155:4": "quartz_pillar[axis=z]",
    "156:0": "quartz_stairs[facing=east,half=bottom,shape=straight]",
    "156:1": "quartz_stairs[facing=west,half=bottom,shape=straight]",
    "156:2": "quartz_stairs[facing=south,half=bottom,shape=straight]",
    "156:3": "quartz_stairs[facing=north,half=bottom,shape=straight]",
    "156:4": "quartz_stairs[facing=east,half=top,shape=straight]",
    "156:5": "quartz_stairs[facing=west,half=top,shape=straight]",
    "156:6": "quartz_stairs[facing=south,half=top,shape=straight]",
    "156:7": "quartz_stairs[facing=north,half=top,shape=straight]",
    "157:0": "activator_rail[powered=false,shape=north_south]",
    "157:1": "activator_rail[powered=false,shape=east_west]",
    "157:2": "activator_rail[powered=false,shape=ascending_east]",
    "157:3": "activator_rail[powered=false,shape=ascending_west]",
    "157:4": "activator_rail[powered=false,shape=ascending_north]",
    "157:5": "activator_rail[powered=false,shape=ascending_south]",
    "157:8": "activator_rail[powered=true,shape=north_south]",
    "157:9": "activator_rail[powered=true,shape=east_west]",
    "157:10": "activator_rail[powered=true,shape=ascending_east]",
    "157:11": "activator_rail[powered=true,shape=ascending_west]",
    "157:12": "activator_rail[powered=true,shape=ascending_north]",
    "157:13": "activator_rail[powered=true,shape=ascending_south]",
    "158:0": "dropper[facing=down,triggered=false]",
    "158:1": "dropper[facing=up,triggered=false]",
    "158:2": "dropper[facing=north,triggered=false]",
    "158:3": "dropper[facing=south,triggered=false]",
    "158:4": "dropper[facing=west,triggered=false]",
    "158:5": "dropper[facing=east,triggered=false]",
    "158:8": "dropper[facing=down,triggered=true]",
    "158:9": "dropper[facing=up,triggered=true]",
    "158:10": "dropper[facing=north,triggered=true]",
    "158:11": "dropper[facing=south,triggered=true]",
    "158:12": "dropper[facing=west,triggered=true]",
    "158:13": "dropper[facing=east,triggered=true]",
    "159:0": "white_terracotta",
    "159:1": "orange_terracotta",
    "159:2": "magenta_terracotta",
    "159:3": "light_blue_terracotta",
    "159:4": "yellow_terracotta",
    "159:5": "lime_terracotta",
    "159:6": "pink_terracotta",
    "159:7": "gray_terracotta",
    "159:8": "light_gray_terracotta",
    "159:9": "cyan_terracotta",
    "159:10": "purple_terracotta",
    "159:11": "blue_terracotta",
    "159:12": "brown_terracotta",
    "159:13": "green_terracotta",
    "159:14": "red_terracotta",
    "159:15": "black_terracotta",
    "160:0": "white_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:1": "orange_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:2": "magenta_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:3": "light_blue_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:4": "yellow_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:5": "lime_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:6": "pink_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:7": "gray_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:8": "light_gray_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:9": "cyan_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:10": "purple_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:11": "blue_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:12": "brown_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:13": "green_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:14": "red_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "160:15": "black_stained_glass_pane[east=false,north=false,south=false,west=false]",
    "161:0": "acacia_leaves[check_decay=false,decayable=true]",
    "161:1": "dark_oak_leaves[check_decay=false,decayable=true]",
    "161:4": "acacia_leaves[check_decay=false,decayable=false]",
    "161:5": "dark_oak_leaves[check_decay=false,decayable=false]",
    "161:8": "acacia_leaves[check_decay=true,decayable=true]",
    "161:9": "dark_oak_leaves[check_decay=true,decayable=true]",
    "161:12": "acacia_leaves[check_decay=true,decayable=false]",
    "161:13": "dark_oak_leaves[check_decay=true,decayable=false]",
    "162:0": "acacia_log[axis=y]",
    "162:1": "dark_oak_log[axis=y]",
    "162:4": "acacia_log[axis=x]",
    "162:5": "dark_oak_log[axis=x]",
    "162:8": "acacia_log[axis=z]",
    "162:9": "dark_oak_log[axis=z]",
    "162:12": "acacia_bark",
    "162:13": "dark_oak_bark",
    "163:0": "acacia_stairs[facing=east,half=bottom,shape=straight]",
    "163:1": "acacia_stairs[facing=west,half=bottom,shape=straight]",
    "163:2": "acacia_stairs[facing=south,half=bottom,shape=straight]",
    "163:3": "acacia_stairs[facing=north,half=bottom,shape=straight]",
    "163:4": "acacia_stairs[facing=east,half=top,shape=straight]",
    "163:5": "acacia_stairs[facing=west,half=top,shape=straight]",
    "163:6": "acacia_stairs[facing=south,half=top,shape=straight]",
    "163:7": "acacia_stairs[facing=north,half=top,shape=straight]",
    "164:0": "dark_oak_stairs[facing=east,half=bottom,shape=straight]",
    "164:1": "dark_oak_stairs[facing=west,half=bottom,shape=straight]",
    "164:2": "dark_oak_stairs[facing=south,half=bottom,shape=straight]",
    "164:3": "dark_oak_stairs[facing=north,half=bottom,shape=straight]",
    "164:4": "dark_oak_stairs[facing=east,half=top,shape=straight]",
    "164:5": "dark_oak_stairs[facing=west,half=top,shape=straight]",
    "164:6": "dark_oak_stairs[facing=south,half=top,shape=straight]",
    "164:7": "dark_oak_stairs[facing=north,half=top,shape=straight]",
    "165:0": "slime_block",
    "166:0": "barrier",
    "167:0": "iron_trapdoor[facing=north,half=bottom,open=false]",
    "167:1": "iron_trapdoor[facing=south,half=bottom,open=false]",
    "167:2": "iron_trapdoor[facing=west,half=bottom,open=false]",
    "167:3": "iron_trapdoor[facing=east,half=bottom,open=false]",
    "167:4": "iron_trapdoor[facing=north,half=bottom,open=true]",
    "167:5": "iron_trapdoor[facing=south,half=bottom,open=true]",
    "167:6": "iron_trapdoor[facing=west,half=bottom,open=true]",
    "167:7": "iron_trapdoor[facing=east,half=bottom,open=true]",
    "167:8": "iron_trapdoor[facing=north,half=top,open=false]",
    "167:9": "iron_trapdoor[facing=south,half=top,open=false]",
    "167:10": "iron_trapdoor[facing=west,half=top,open=false]",
    "167:11": "iron_trapdoor[facing=east,half=top,open=false]",
    "167:12": "iron_trapdoor[facing=north,half=top,open=true]",
    "167:13": "iron_trapdoor[facing=south,half=top,open=true]",
    "167:14": "iron_trapdoor[facing=west,half=top,open=true]",
    "167:15": "iron_trapdoor[facing=east,half=top,open=true]",
    "168:0": "prismarine",
    "168:1": "prismarine_bricks",
    "168:2": "dark_prismarine",
    "169:0": "sea_lantern",
    "170:0": "hay_block[axis=y]",
    "170:4": "hay_block[axis=x]",
    "170:8": "hay_block[axis=z]",
    "171:0": "white_carpet",
    "171:1": "orange_carpet",
    "171:2": "magenta_carpet",
    "171:3": "light_blue_carpet",
    "171:4": "yellow_carpet",
    "171:5": "lime_carpet",
    "171:6": "pink_carpet",
    "171:7": "gray_carpet",
    "171:8": "light_gray_carpet",
    "171:9": "cyan_carpet",
    "171:10": "purple_carpet",
    "171:11": "blue_carpet",
    "171:12": "brown_carpet",
    "171:13": "green_carpet",
    "171:14": "red_carpet",
    "171:15": "black_carpet",
    "172:0": "terracotta",
    "173:0": "coal_block",
    "174:0": "packed_ice",
    "175:0": "sunflower[half=lower]",
    "175:1": "lilac[half=lower]",
    "175:2": "tall_grass[half=lower]",
    "175:3": "large_fern[half=lower]",
    "175:4": "rose_bush[half=lower]",
    "175:5": "peony[half=lower]",
    "175:8": "peony[half=upper]",
    "175:9": "peony[half=upper]",
    "175:10": "peony[half=upper]",
    "175:11": "peony[half=upper]",
    "176:0": "white_banner[rotation=0]",
    "176:1": "white_banner[rotation=1]",
    "176:2": "white_banner[rotation=2]",
    "176:3": "white_banner[rotation=3]",
    "176:4": "white_banner[rotation=4]",
    "176:5": "white_banner[rotation=5]",
    "176:6": "white_banner[rotation=6]",
    "176:7": "white_banner[rotation=7]",
    "176:8": "white_banner[rotation=8]",
    "176:9": "white_banner[rotation=9]",
    "176:10": "white_banner[rotation=10]",
    "176:11": "white_banner[rotation=11]",
    "176:12": "white_banner[rotation=12]",
    "176:13": "white_banner[rotation=13]",
    "176:14": "white_banner[rotation=14]",
    "176:15": "white_banner[rotation=15]",
    "177:2": "white_wall_banner[facing=north]",
    "177:3": "white_wall_banner[facing=south]",
    "177:4": "white_wall_banner[facing=west]",
    "177:5": "white_wall_banner[facing=east]",
    "178:0": "daylight_detector[inverted=true,power=0]",
    "178:1": "daylight_detector[inverted=true,power=1]",
    "178:2": "daylight_detector[inverted=true,power=2]",
    "178:3": "daylight_detector[inverted=true,power=3]",
    "178:4": "daylight_detector[inverted=true,power=4]",
    "178:5": "daylight_detector[inverted=true,power=5]",
    "178:6": "daylight_detector[inverted=true,power=6]",
    "178:7": "daylight_detector[inverted=true,power=7]",
    "178:8": "daylight_detector[inverted=true,power=8]",
    "178:9": "daylight_detector[inverted=true,power=9]",
    "178:10": "daylight_detector[inverted=true,power=10]",
    "178:11": "daylight_detector[inverted=true,power=11]",
    "178:12": "daylight_detector[inverted=true,power=12]",
    "178:13": "daylight_detector[inverted=true,power=13]",
    "178:14": "daylight_detector[inverted=true,power=14]",
    "178:15": "daylight_detector[inverted=true,power=15]",
    "179:0": "red_sandstone",
    "179:1": "chiseled_red_sandstone",
    "179:2": "cut_red_sandstone",
    "180:0": "red_sandstone_stairs[facing=east,half=bottom,shape=straight]",
    "180:1": "red_sandstone_stairs[facing=west,half=bottom,shape=straight]",
    "180:2": "red_sandstone_stairs[facing=south,half=bottom,shape=straight]",
    "180:3": "red_sandstone_stairs[facing=north,half=bottom,shape=straight]",
    "180:4": "red_sandstone_stairs[facing=east,half=top,shape=straight]",
    "180:5": "red_sandstone_stairs[facing=west,half=top,shape=straight]",
    "180:6": "red_sandstone_stairs[facing=south,half=top,shape=straight]",
    "180:7": "red_sandstone_stairs[facing=north,half=top,shape=straight]",
    "181:0": "red_sandstone_slab[type=double]",
    "181:8": "smooth_red_sandstone",
    "182:0": "red_sandstone_slab[type=bottom]",
    "182:8": "red_sandstone_slab[type=top]",
    "183:0": "spruce_fence_gate[facing=south,in_wall=false,open=false,powered=false]",
    "183:1": "spruce_fence_gate[facing=west,in_wall=false,open=false,powered=false]",
    "183:2": "spruce_fence_gate[facing=north,in_wall=false,open=false,powered=false]",
    "183:3": "spruce_fence_gate[facing=east,in_wall=false,open=false,powered=false]",
    "183:4": "spruce_fence_gate[facing=south,in_wall=false,open=true,powered=false]",
    "183:5": "spruce_fence_gate[facing=west,in_wall=false,open=true,powered=false]",
    "183:6": "spruce_fence_gate[facing=north,in_wall=false,open=true,powered=false]",
    "183:7": "spruce_fence_gate[facing=east,in_wall=false,open=true,powered=false]",
    "183:8": "spruce_fence_gate[facing=south,in_wall=false,open=false,powered=true]",
    "183:9": "spruce_fence_gate[facing=west,in_wall=false,open=false,powered=true]",
    "183:10": "spruce_fence_gate[facing=north,in_wall=false,open=false,powered=true]",
    "183:11": "spruce_fence_gate[facing=east,in_wall=false,open=false,powered=true]",
    "183:12": "spruce_fence_gate[facing=south,in_wall=false,open=true,powered=true]",
    "183:13": "spruce_fence_gate[facing=west,in_wall=false,open=true,powered=true]",
    "183:14": "spruce_fence_gate[facing=north,in_wall=false,open=true,powered=true]",
    "183:15": "spruce_fence_gate[facing=east,in_wall=false,open=true,powered=true]",
    "184:0": "birch_fence_gate[facing=south,in_wall=false,open=false,powered=false]",
    "184:1": "birch_fence_gate[facing=west,in_wall=false,open=false,powered=false]",
    "184:2": "birch_fence_gate[facing=north,in_wall=false,open=false,powered=false]",
    "184:3": "birch_fence_gate[facing=east,in_wall=false,open=false,powered=false]",
    "184:4": "birch_fence_gate[facing=south,in_wall=false,open=true,powered=false]",
    "184:5": "birch_fence_gate[facing=west,in_wall=false,open=true,powered=false]",
    "184:6": "birch_fence_gate[facing=north,in_wall=false,open=true,powered=false]",
    "184:7": "birch_fence_gate[facing=east,in_wall=false,open=true,powered=false]",
    "184:8": "birch_fence_gate[facing=south,in_wall=false,open=false,powered=true]",
    "184:9": "birch_fence_gate[facing=west,in_wall=false,open=false,powered=true]",
    "184:10": "birch_fence_gate[facing=north,in_wall=false,open=false,powered=true]",
    "184:11": "birch_fence_gate[facing=east,in_wall=false,open=false,powered=true]",
    "184:12": "birch_fence_gate[facing=south,in_wall=false,open=true,powered=true]",
    "184:13": "birch_fence_gate[facing=west,in_wall=false,open=true,powered=true]",
    "184:14": "birch_fence_gate[facing=north,in_wall=false,open=true,powered=true]",
    "184:15": "birch_fence_gate[facing=east,in_wall=false,open=true,powered=true]",
    "185:0": "jungle_fence_gate[facing=south,in_wall=false,open=false,powered=false]",
    "185:1": "jungle_fence_gate[facing=west,in_wall=false,open=false,powered=false]",
    "185:2": "jungle_fence_gate[facing=north,in_wall=false,open=false,powered=false]",
    "185:3": "jungle_fence_gate[facing=east,in_wall=false,open=false,powered=false]",
    "185:4": "jungle_fence_gate[facing=south,in_wall=false,open=true,powered=false]",
    "185:5": "jungle_fence_gate[facing=west,in_wall=false,open=true,powered=false]",
    "185:6": "jungle_fence_gate[facing=north,in_wall=false,open=true,powered=false]",
    "185:7": "jungle_fence_gate[facing=east,in_wall=false,open=true,powered=false]",
    "185:8": "jungle_fence_gate[facing=south,in_wall=false,open=false,powered=true]",
    "185:9": "jungle_fence_gate[facing=west,in_wall=false,open=false,powered=true]",
    "185:10": "jungle_fence_gate[facing=north,in_wall=false,open=false,powered=true]",
    "185:11": "jungle_fence_gate[facing=east,in_wall=false,open=false,powered=true]",
    "185:12": "jungle_fence_gate[facing=south,in_wall=false,open=true,powered=true]",
    "185:13": "jungle_fence_gate[facing=west,in_wall=false,open=true,powered=true]",
    "185:14": "jungle_fence_gate[facing=north,in_wall=false,open=true,powered=true]",
    "185:15": "jungle_fence_gate[facing=east,in_wall=false,open=true,powered=true]",
    "186:0": "dark_oak_fence_gate[facing=south,in_wall=false,open=false,powered=false]",
    "186:1": "dark_oak_fence_gate[facing=west,in_wall=false,open=false,powered=false]",
    "186:2": "dark_oak_fence_gate[facing=north,in_wall=false,open=false,powered=false]",
    "186:3": "dark_oak_fence_gate[facing=east,in_wall=false,open=false,powered=false]",
    "186:4": "dark_oak_fence_gate[facing=south,in_wall=false,open=true,powered=false]",
    "186:5": "dark_oak_fence_gate[facing=west,in_wall=false,open=true,powered=false]",
    "186:6": "dark_oak_fence_gate[facing=north,in_wall=false,open=true,powered=false]",
    "186:7": "dark_oak_fence_gate[facing=east,in_wall=false,open=true,powered=false]",
    "186:8": "dark_oak_fence_gate[facing=south,in_wall=false,open=false,powered=true]",
    "186:9": "dark_oak_fence_gate[facing=west,in_wall=false,open=false,powered=true]",
    "186:10": "dark_oak_fence_gate[facing=north,in_wall=false,open=false,powered=true]",
    "186:11": "dark_oak_fence_gate[facing=east,in_wall=false,open=false,powered=true]",
    "186:12": "dark_oak_fence_gate[facing=south,in_wall=false,open=true,powered=true]",
    "186:13": "dark_oak_fence_gate[facing=west,in_wall=false,open=true,powered=true]",
    "186:14": "dark_oak_fence_gate[facing=north,in_wall=false,open=true,powered=true]",
    "186:15": "dark_oak_fence_gate[facing=east,in_wall=false,open=true,powered=true]",
    "187:0": "acacia_fence_gate[facing=south,in_wall=false,open=false,powered=false]",
    "187:1": "acacia_fence_gate[facing=west,in_wall=false,open=false,powered=false]",
    "187:2": "acacia_fence_gate[facing=north,in_wall=false,open=false,powered=false]",
    "187:3": "acacia_fence_gate[facing=east,in_wall=false,open=false,powered=false]",
    "187:4": "acacia_fence_gate[facing=south,in_wall=false,open=true,powered=false]",
    "187:5": "acacia_fence_gate[facing=west,in_wall=false,open=true,powered=false]",
    "187:6": "acacia_fence_gate[facing=north,in_wall=false,open=true,powered=false]",
    "187:7": "acacia_fence_gate[facing=east,in_wall=false,open=true,powered=false]",
    "187:8": "acacia_fence_gate[facing=south,in_wall=false,open=false,powered=true]",
    "187:9": "acacia_fence_gate[facing=west,in_wall=false,open=false,powered=true]",
    "187:10": "acacia_fence_gate[facing=north,in_wall=false,open=false,powered=true]",
    "187:11": "acacia_fence_gate[facing=east,in_wall=false,open=false,powered=true]",
    "187:12": "acacia_fence_gate[facing=south,in_wall=false,open=true,powered=true]",
    "187:13": "acacia_fence_gate[facing=west,in_wall=false,open=true,powered=true]",
    "187:14": "acacia_fence_gate[facing=north,in_wall=false,open=true,powered=true]",
    "187:15": "acacia_fence_gate[facing=east,in_wall=false,open=true,powered=true]",
    "188:0": "spruce_fence[east=false,north=false,south=false,west=false]",
    "189:0": "birch_fence[east=false,north=false,south=false,west=false]",
    "190:0": "jungle_fence[east=false,north=false,south=false,west=false]",
    "191:0": "dark_oak_fence[east=false,north=false,south=false,west=false]",
    "192:0": "acacia_fence[east=false,north=false,south=false,west=false]",
    "193:0": "spruce_door[facing=east,half=lower,hinge=right,open=false,powered=false]",
    "193:1": "spruce_door[facing=south,half=lower,hinge=right,open=false,powered=false]",
    "193:2": "spruce_door[facing=west,half=lower,hinge=right,open=false,powered=false]",
    "193:3": "spruce_door[facing=north,half=lower,hinge=right,open=false,powered=false]",
    "193:4": "spruce_door[facing=east,half=lower,hinge=right,open=true,powered=false]",
    "193:5": "spruce_door[facing=south,half=lower,hinge=right,open=true,powered=false]",
    "193:6": "spruce_door[facing=west,half=lower,hinge=right,open=true,powered=false]",
    "193:7": "spruce_door[facing=north,half=lower,hinge=right,open=true,powered=false]",
    "193:8": "spruce_door[facing=east,half=upper,hinge=left,open=false,powered=false]",
    "193:9": "spruce_door[facing=east,half=upper,hinge=right,open=false,powered=false]",
    "193:10": "spruce_door[facing=east,half=upper,hinge=left,open=false,powered=true]",
    "193:11": "spruce_door[facing=east,half=upper,hinge=right,open=false,powered=true]",
    "194:0": "birch_door[facing=east,half=lower,hinge=right,open=false,powered=false]",
    "194:1": "birch_door[facing=south,half=lower,hinge=right,open=false,powered=false]",
    "194:2": "birch_door[facing=west,half=lower,hinge=right,open=false,powered=false]",
    "194:3": "birch_door[facing=north,half=lower,hinge=right,open=false,powered=false]",
    "194:4": "birch_door[facing=east,half=lower,hinge=right,open=true,powered=false]",
    "194:5": "birch_door[facing=south,half=lower,hinge=right,open=true,powered=false]",
    "194:6": "birch_door[facing=west,half=lower,hinge=right,open=true,powered=false]",
    "194:7": "birch_door[facing=north,half=lower,hinge=right,open=true,powered=false]",
    "194:8": "birch_door[facing=east,half=upper,hinge=left,open=false,powered=false]",
    "194:9": "birch_door[facing=east,half=upper,hinge=right,open=false,powered=false]",
    "194:10": "birch_door[facing=east,half=upper,hinge=left,open=false,powered=true]",
    "194:11": "birch_door[facing=east,half=upper,hinge=right,open=false,powered=true]",
    "195:0": "jungle_door[facing=east,half=lower,hinge=right,open=false,powered=false]",
    "195:1": "jungle_door[facing=south,half=lower,hinge=right,open=false,powered=false]",
    "195:2": "jungle_door[facing=west,half=lower,hinge=right,open=false,powered=false]",
    "195:3": "jungle_door[facing=north,half=lower,hinge=right,open=false,powered=false]",
    "195:4": "jungle_door[facing=east,half=lower,hinge=right,open=true,powered=false]",
    "195:5": "jungle_door[facing=south,half=lower,hinge=right,open=true,powered=false]",
    "195:6": "jungle_door[facing=west,half=lower,hinge=right,open=true,powered=false]",
    "195:7": "jungle_door[facing=north,half=lower,hinge=right,open=true,powered=false]",
    "195:8": "jungle_door[facing=east,half=upper,hinge=left,open=false,powered=false]",
    "195:9": "jungle_door[facing=east,half=upper,hinge=right,open=false,powered=false]",
    "195:10": "jungle_door[facing=east,half=upper,hinge=left,open=false,powered=true]",
    "195:11": "jungle_door[facing=east,half=upper,hinge=right,open=false,powered=true]",
    "196:0": "acacia_door[facing=east,half=lower,hinge=right,open=false,powered=false]",
    "196:1": "acacia_door[facing=south,half=lower,hinge=right,open=false,powered=false]",
    "196:2": "acacia_door[facing=west,half=lower,hinge=right,open=false,powered=false]",
    "196:3": "acacia_door[facing=north,half=lower,hinge=right,open=false,powered=false]",
    "196:4": "acacia_door[facing=east,half=lower,hinge=right,open=true,powered=false]",
    "196:5": "acacia_door[facing=south,half=lower,hinge=right,open=true,powered=false]",
    "196:6": "acacia_door[facing=west,half=lower,hinge=right,open=true,powered=false]",
    "196:7": "acacia_door[facing=north,half=lower,hinge=right,open=true,powered=false]",
    "196:8": "acacia_door[facing=east,half=upper,hinge=left,open=false,powered=false]",
    "196:9": "acacia_door[facing=east,half=upper,hinge=right,open=false,powered=false]",
    "196:10": "acacia_door[facing=east,half=upper,hinge=left,open=false,powered=true]",
    "196:11": "acacia_door[facing=east,half=upper,hinge=right,open=false,powered=true]",
    "197:0": "dark_oak_door[facing=east,half=lower,hinge=right,open=false,powered=false]",
    "197:1": "dark_oak_door[facing=south,half=lower,hinge=right,open=false,powered=false]",
    "197:2": "dark_oak_door[facing=west,half=lower,hinge=right,open=false,powered=false]",
    "197:3": "dark_oak_door[facing=north,half=lower,hinge=right,open=false,powered=false]",
    "197:4": "dark_oak_door[facing=east,half=lower,hinge=right,open=true,powered=false]",
    "197:5": "dark_oak_door[facing=south,half=lower,hinge=right,open=true,powered=false]",
    "197:6": "dark_oak_door[facing=west,half=lower,hinge=right,open=true,powered=false]",
    "197:7": "dark_oak_door[facing=north,half=lower,hinge=right,open=true,powered=false]",
    "197:8": "dark_oak_door[facing=east,half=upper,hinge=left,open=false,powered=false]",
    "197:9": "dark_oak_door[facing=east,half=upper,hinge=right,open=false,powered=false]",
    "197:10": "dark_oak_door[facing=east,half=upper,hinge=left,open=false,powered=true]",
    "197:11": "dark_oak_door[facing=east,half=upper,hinge=right,open=false,powered=true]",
    "198:0": "end_rod[facing=down]",
    "198:1": "end_rod[facing=up]",
    "198:2": "end_rod[facing=north]",
    "198:3": "end_rod[facing=south]",
    "198:4": "end_rod[facing=west]",
    "198:5": "end_rod[facing=east]",
    "199:0": "chorus_plant[down=false,east=false,north=false,south=false,up=false,west=false]",
    "200:0": "chorus_flower[age=0]",
    "200:1": "chorus_flower[age=1]",
    "200:2": "chorus_flower[age=2]",
    "200:3": "chorus_flower[age=3]",
    "200:4": "chorus_flower[age=4]",
    "200:5": "chorus_flower[age=5]",
    "201:0": "purpur_block",
    "202:0": "purpur_pillar[axis=y]",
    "202:4": "purpur_pillar[axis=x]",
    "202:8": "purpur_pillar[axis=z]",
    "203:0": "purpur_stairs[facing=east,half=bottom,shape=straight]",
    "203:1": "purpur_stairs[facing=west,half=bottom,shape=straight]",
    "203:2": "purpur_stairs[facing=south,half=bottom,shape=straight]",
    "203:3": "purpur_stairs[facing=north,half=bottom,shape=straight]",
    "203:4": "purpur_stairs[facing=east,half=top,shape=straight]",
    "203:5": "purpur_stairs[facing=west,half=top,shape=straight]",
    "203:6": "purpur_stairs[facing=south,half=top,shape=straight]",
    "203:7": "purpur_stairs[facing=north,half=top,shape=straight]",
    "204:0": "purpur_slab[type=double]",
    "205:0": "purpur_slab[type=bottom]",
    "205:8": "purpur_slab[type=top]",
    "206:0": "end_stone_bricks",
    "207:0": "beetroots[age=0]",
    "207:1": "beetroots[age=1]",
    "207:2": "beetroots[age=2]",
    "207:3": "beetroots[age=3]",
    "208:0": "grass_path",
    "209:0": "end_gateway",
    "210:0": "repeating_command_block[conditional=false,facing=down]",
    "210:1": "repeating_command_block[conditional=false,facing=up]",
    "210:2": "repeating_command_block[conditional=false,facing=north]",
    "210:3": "repeating_command_block[conditional=false,facing=south]",
    "210:4": "repeating_command_block[conditional=false,facing=west]",
    "210:5": "repeating_command_block[conditional=false,facing=east]",
    "210:8": "repeating_command_block[conditional=true,facing=down]",
    "210:9": "repeating_command_block[conditional=true,facing=up]",
    "210:10": "repeating_command_block[conditional=true,facing=north]",
    "210:11": "repeating_command_block[conditional=true,facing=south]",
    "210:12": "repeating_command_block[conditional=true,facing=west]",
    "210:13": "repeating_command_block[conditional=true,facing=east]",
    "211:0": "chain_command_block[conditional=false,facing=down]",
    "211:1": "chain_command_block[conditional=false,facing=up]",
    "211:2": "chain_command_block[conditional=false,facing=north]",
    "211:3": "chain_command_block[conditional=false,facing=south]",
    "211:4": "chain_command_block[conditional=false,facing=west]",
    "211:5": "chain_command_block[conditional=false,facing=east]",
    "211:8": "chain_command_block[conditional=true,facing=down]",
    "211:9": "chain_command_block[conditional=true,facing=up]",
    "211:10": "chain_command_block[conditional=true,facing=north]",
    "211:11": "chain_command_block[conditional=true,facing=south]",
    "211:12": "chain_command_block[conditional=true,facing=west]",
    "211:13": "chain_command_block[conditional=true,facing=east]",
    "212:0": "frosted_ice[age=0]",
    "212:1": "frosted_ice[age=1]",
    "212:2": "frosted_ice[age=2]",
    "212:3": "frosted_ice[age=3]",
    "213:0": "magma_block",
    "214:0": "nether_wart_block",
    "215:0": "red_nether_bricks",
    "216:0": "bone_block[axis=y]",
    "216:4": "bone_block[axis=x]",
    "216:8": "bone_block[axis=z]",
    "217:0": "structure_void",
    "218:0": "observer[facing=down,powered=false]",
    "218:1": "observer[facing=up,powered=false]",
    "218:2": "observer[facing=north,powered=false]",
    "218:3": "observer[facing=south,powered=false]",
    "218:4": "observer[facing=west,powered=false]",
    "218:5": "observer[facing=east,powered=false]",
    "218:8": "observer[facing=down,powered=true]",
    "218:9": "observer[facing=up,powered=true]",
    "218:10": "observer[facing=north,powered=true]",
    "218:11": "observer[facing=south,powered=true]",
    "218:12": "observer[facing=west,powered=true]",
    "218:13": "observer[facing=east,powered=true]",
    "219:0": "white_shulker_box[facing=down]",
    "219:1": "white_shulker_box[facing=up]",
    "219:2": "white_shulker_box[facing=north]",
    "219:3": "white_shulker_box[facing=south]",
    "219:4": "white_shulker_box[facing=west]",
    "219:5": "white_shulker_box[facing=east]",
    "220:0": "orange_shulker_box[facing=down]",
    "220:1": "orange_shulker_box[facing=up]",
    "220:2": "orange_shulker_box[facing=north]",
    "220:3": "orange_shulker_box[facing=south]",
    "220:4": "orange_shulker_box[facing=west]",
    "220:5": "orange_shulker_box[facing=east]",
    "221:0": "magenta_shulker_box[facing=down]",
    "221:1": "magenta_shulker_box[facing=up]",
    "221:2": "magenta_shulker_box[facing=north]",
    "221:3": "magenta_shulker_box[facing=south]",
    "221:4": "magenta_shulker_box[facing=west]",
    "221:5": "magenta_shulker_box[facing=east]",
    "222:0": "light_blue_shulker_box[facing=down]",
    "222:1": "light_blue_shulker_box[facing=up]",
    "222:2": "light_blue_shulker_box[facing=north]",
    "222:3": "light_blue_shulker_box[facing=south]",
    "222:4": "light_blue_shulker_box[facing=west]",
    "222:5": "light_blue_shulker_box[facing=east]",
    "223:0": "yellow_shulker_box[facing=down]",
    "223:1": "yellow_shulker_box[facing=up]",
    "223:2": "yellow_shulker_box[facing=north]",
    "223:3": "yellow_shulker_box[facing=south]",
    "223:4": "yellow_shulker_box[facing=west]",
    "223:5": "yellow_shulker_box[facing=east]",
    "224:0": "lime_shulker_box[facing=down]",
    "224:1": "lime_shulker_box[facing=up]",
    "224:2": "lime_shulker_box[facing=north]",
    "224:3": "lime_shulker_box[facing=south]",
    "224:4": "lime_shulker_box[facing=west]",
    "224:5": "lime_shulker_box[facing=east]",
    "225:0": "pink_shulker_box[facing=down]",
    "225:1": "pink_shulker_box[facing=up]",
    "225:2": "pink_shulker_box[facing=north]",
    "225:3": "pink_shulker_box[facing=south]",
    "225:4": "pink_shulker_box[facing=west]",
    "225:5": "pink_shulker_box[facing=east]",
    "226:0": "gray_shulker_box[facing=down]",
    "226:1": "gray_shulker_box[facing=up]",
    "226:2": "gray_shulker_box[facing=north]",
    "226:3": "gray_shulker_box[facing=south]",
    "226:4": "gray_shulker_box[facing=west]",
    "226:5": "gray_shulker_box[facing=east]",
    "227:0": "light_gray_shulker_box[facing=down]",
    "227:1": "light_gray_shulker_box[facing=up]",
    "227:2": "light_gray_shulker_box[facing=north]",
    "227:3": "light_gray_shulker_box[facing=south]",
    "227:4": "light_gray_shulker_box[facing=west]",
    "227:5": "light_gray_shulker_box[facing=east]",
    "228:0": "cyan_shulker_box[facing=down]",
    "228:1": "cyan_shulker_box[facing=up]",
    "228:2": "cyan_shulker_box[facing=north]",
    "228:3": "cyan_shulker_box[facing=south]",
    "228:4": "cyan_shulker_box[facing=west]",
    "228:5": "cyan_shulker_box[facing=east]",
    "229:0": "purple_shulker_box[facing=down]",
    "229:1": "purple_shulker_box[facing=up]",
    "229:2": "purple_shulker_box[facing=north]",
    "229:3": "purple_shulker_box[facing=south]",
    "229:4": "purple_shulker_box[facing=west]",
    "229:5": "purple_shulker_box[facing=east]",
    "230:0": "blue_shulker_box[facing=down]",
    "230:1": "blue_shulker_box[facing=up]",
    "230:2": "blue_shulker_box[facing=north]",
    "230:3": "blue_shulker_box[facing=south]",
    "230:4": "blue_shulker_box[facing=west]",
    "230:5": "blue_shulker_box[facing=east]",
    "231:0": "brown_shulker_box[facing=down]",
    "231:1": "brown_shulker_box[facing=up]",
    "231:2": "brown_shulker_box[facing=north]",
    "231:3": "brown_shulker_box[facing=south]",
    "231:4": "brown_shulker_box[facing=west]",
    "231:5": "brown_shulker_box[facing=east]",
    "232:0": "green_shulker_box[facing=down]",
    "232:1": "green_shulker_box[facing=up]",
    "232:2": "green_shulker_box[facing=north]",
    "232:3": "green_shulker_box[facing=south]",
    "232:4": "green_shulker_box[facing=west]",
    "232:5": "green_shulker_box[facing=east]",
    "233:0": "red_shulker_box[facing=down]",
    "233:1": "red_shulker_box[facing=up]",
    "233:2": "red_shulker_box[facing=north]",
    "233:3": "red_shulker_box[facing=south]",
    "233:4": "red_shulker_box[facing=west]",
    "233:5": "red_shulker_box[facing=east]",
    "234:0": "black_shulker_box[facing=down]",
    "234:1": "black_shulker_box[facing=up]",
    "234:2": "black_shulker_box[facing=north]",
    "234:3": "black_shulker_box[facing=south]",
    "234:4": "black_shulker_box[facing=west]",
    "234:5": "black_shulker_box[facing=east]",
    "235:0": "white_glazed_terracotta[facing=south]",
    "235:1": "white_glazed_terracotta[facing=west]",
    "235:2": "white_glazed_terracotta[facing=north]",
    "235:3": "white_glazed_terracotta[facing=east]",
    "236:0": "orange_glazed_terracotta[facing=south]",
    "236:1": "orange_glazed_terracotta[facing=west]",
    "236:2": "orange_glazed_terracotta[facing=north]",
    "236:3": "orange_glazed_terracotta[facing=east]",
    "237:0": "magenta_glazed_terracotta[facing=south]",
    "237:1": "magenta_glazed_terracotta[facing=west]",
    "237:2": "magenta_glazed_terracotta[facing=north]",
    "237:3": "magenta_glazed_terracotta[facing=east]",
    "238:0": "light_blue_glazed_terracotta[facing=south]",
    "238:1": "light_blue_glazed_terracotta[facing=west]",
    "238:2": "light_blue_glazed_terracotta[facing=north]",
    "238:3": "light_blue_glazed_terracotta[facing=east]",
    "239:0": "yellow_glazed_terracotta[facing=south]",
    "239:1": "yellow_glazed_terracotta[facing=west]",
    "239:2": "yellow_glazed_terracotta[facing=north]",
    "239:3": "yellow_glazed_terracotta[facing=east]",
    "240:0": "lime_glazed_terracotta[facing=south]",
    "240:1": "lime_glazed_terracotta[facing=west]",
    "240:2": "lime_glazed_terracotta[facing=north]",
    "240:3": "lime_glazed_terracotta[facing=east]",
    "241:0": "pink_glazed_terracotta[facing=south]",
    "241:1": "pink_glazed_terracotta[facing=west]",
    "241:2": "pink_glazed_terracotta[facing=north]",
    "241:3": "pink_glazed_terracotta[facing=east]",
    "242:0": "gray_glazed_terracotta[facing=south]",
    "242:1": "gray_glazed_terracotta[facing=west]",
    "242:2": "gray_glazed_terracotta[facing=north]",
    "242:3": "gray_glazed_terracotta[facing=east]",
    "243:0": "light_gray_glazed_terracotta[facing=south]",
    "243:1": "light_gray_glazed_terracotta[facing=west]",
    "243:2": "light_gray_glazed_terracotta[facing=north]",
    "243:3": "light_gray_glazed_terracotta[facing=east]",
    "244:0": "cyan_glazed_terracotta[facing=south]",
    "244:1": "cyan_glazed_terracotta[facing=west]",
    "244:2": "cyan_glazed_terracotta[facing=north]",
    "244:3": "cyan_glazed_terracotta[facing=east]",
    "245:0": "purple_glazed_terracotta[facing=south]",
    "245:1": "purple_glazed_terracotta[facing=west]",
    "245:2": "purple_glazed_terracotta[facing=north]",
    "245:3": "purple_glazed_terracotta[facing=east]",
    "246:0": "blue_glazed_terracotta[facing=south]",
    "246:1": "blue_glazed_terracotta[facing=west]",
    "246:2": "blue_glazed_terracotta[facing=north]",
    "246:3": "blue_glazed_terracotta[facing=east]",
    "247:0": "brown_glazed_terracotta[facing=south]",
    "247:1": "brown_glazed_terracotta[facing=west]",
    "247:2": "brown_glazed_terracotta[facing=north]",
    "247:3": "brown_glazed_terracotta[facing=east]",
    "248:0": "green_glazed_terracotta[facing=south]",
    "248:1": "green_glazed_terracotta[facing=west]",
    "248:2": "green_glazed_terracotta[facing=north]",
    "248:3": "green_glazed_terracotta[facing=east]",
    "249:0": "red_glazed_terracotta[facing=south]",
    "249:1": "red_glazed_terracotta[facing=west]",
    "249:2": "red_glazed_terracotta[facing=north]",
    "249:3": "red_glazed_terracotta[facing=east]",
    "250:0": "black_glazed_terracotta[facing=south]",
    "250:1": "black_glazed_terracotta[facing=west]",
    "250:2": "black_glazed_terracotta[facing=north]",
    "250:3": "black_glazed_terracotta[facing=east]",
    "251:0": "white_concrete",
    "251:1": "orange_concrete",
    "251:2": "magenta_concrete",
    "251:3": "light_blue_concrete",
    "251:4": "yellow_concrete",
    "251:5": "lime_concrete",
    "251:6": "pink_concrete",
    "251:7": "gray_concrete",
    "251:8": "light_gray_concrete",
    "251:9": "cyan_concrete",
    "251:10": "purple_concrete",
    "251:11": "blue_concrete",
    "251:12": "brown_concrete",
    "251:13": "green_concrete",
    "251:14": "red_concrete",
    "251:15": "black_concrete",
    "252:0": "white_concrete_powder",
    "252:1": "orange_concrete_powder",
    "252:2": "magenta_concrete_powder",
    "252:3": "light_blue_concrete_powder",
    "252:4": "yellow_concrete_powder",
    "252:5": "lime_concrete_powder",
    "252:6": "pink_concrete_powder",
    "252:7": "gray_concrete_powder",
    "252:8": "light_gray_concrete_powder",
    "252:9": "cyan_concrete_powder",
    "252:10": "purple_concrete_powder",
    "252:11": "blue_concrete_powder",
    "252:12": "brown_concrete_powder",
    "252:13": "green_concrete_powder",
    "252:14": "red_concrete_powder",
    "252:15": "black_concrete_powder",
    "255:0": "structure_block[mode=save]",
    "255:1": "structure_block[mode=load]",
    "255:2": "structure_block[mode=corner]",
    "255:3": "structure_block[mode=data]"
  },
  clientCalculatedBlocks: {
    block_snowy: [
      "grass_block",
      "dirt",
      "coarse_dirt",
      "podzol",
      "mycelium"
    ],
    directional: [
      "fire",
      "redstone_wire",
      "oak_fence",
      "iron_bars",
      "glass_pane",
      "vine",
      "nether_brick_fence",
      "tripwire",
      "cobblestone_wall",
      "mossy_cobblestone_wall",
      "white_stained_glass_pane",
      "orange_stained_glass_pane",
      "magenta_stained_glass_pane",
      "light_blue_stained_glass_pane",
      "yellow_stained_glass_pane",
      "lime_stained_glass_pane",
      "pink_stained_glass_pane",
      "gray_stained_glass_pane",
      "light_gray_stained_glass_pane",
      "cyan_stained_glass_pane",
      "purple_stained_glass_pane",
      "blue_stained_glass_pane",
      "brown_stained_glass_pane",
      "green_stained_glass_pane",
      "red_stained_glass_pane",
      "black_stained_glass_pane",
      "spruce_fence",
      "birch_fence",
      "jungle_fence",
      "dark_oak_fence",
      "acacia_fence",
      "chorus_plant"
    ],
    door: [
      "oak_door",
      "iron_door",
      "spruce_door",
      "birch_door",
      "jungle_door",
      "acacia_door",
      "dark_oak_door"
    ],
    repeater_locked: [
      "repeater"
    ],
    gate_in_wall: [
      "oak_fence_gate",
      "spruce_fence_gate",
      "birch_fence_gate",
      "jungle_fence_gate",
      "dark_oak_fence_gate",
      "acacia_fence_gate"
    ]
  }
};

// ../src/lib/simpleUtils.ts
function getBlockPosKey(pos) {
  return `${pos.x},${pos.y},${pos.z}`;
}

// ../src/mesher/shared.ts
var defaultMesherConfig = {
  version: "",
  worldMaxY: 256,
  worldMinY: 0,
  enableLighting: true,
  skyLight: 15,
  smoothLighting: true,
  shadingTheme: "high-contrast",
  cardinalLight: "default",
  outputFormat: "threeJs",
  // textureSize: 1024, // for testing
  debugModelVariant: void 0,
  clipWorldBelowY: void 0,
  disableBlockEntityTextures: false
};
var getBlockAssetsCacheKey = (stateId, modelNameOverride) => {
  return modelNameOverride ? `${stateId}:${modelNameOverride}` : String(stateId);
};

// ../src/mesher/worldConstants.ts
var INVISIBLE_BLOCKS = /* @__PURE__ */ new Set(["air", "void_air", "cave_air", "barrier", "light", "moving_piston"]);

// ../src/mesher/world.ts
var ignoreAoBlocks = Object.keys(moreBlockDataGenerated_default.noOcclusions);
function worldColumnKey(x, z) {
  return `${x},${z}`;
}
function isCube2(shapes) {
  if (!shapes || shapes.length !== 1)
    return false;
  const shape = shapes[0];
  return shape[0] === 0 && shape[1] === 0 && shape[2] === 0 && shape[3] === 1 && shape[4] === 1 && shape[5] === 1;
}
var World = class {
  config = defaultMesherConfig;
  Chunk;
  columns = {};
  blockCache = {};
  biomeCache;
  preflat;
  erroredBlockModel;
  customBlockModels = /* @__PURE__ */ new Map();
  // chunkKey -> blockModels
  sentBlockStateModels = /* @__PURE__ */ new Set();
  blockStateModelInfo = /* @__PURE__ */ new Map();
  constructor(version) {
    this.Chunk = (0, import_prismarine_chunk.default)(version);
    this.biomeCache = (0, import_minecraft_data2.default)(version).biomes;
    this.preflat = !(0, import_minecraft_data2.default)(version).supportFeature("blockStateId");
    this.config.version = version;
  }
  getLight(pos, isNeighbor = false, skipMoreChecks = false, curBlockName = "") {
    if (!(pos instanceof Vec3))
      pos = new Vec3(...pos);
    const { enableLighting, skyLight } = this.config;
    if (!enableLighting)
      return 15;
    const column = this.getColumnByPos(pos);
    if (!column || !hasChunkSection(column, pos))
      return 15;
    let result = Math.min(
      15,
      Math.max(
        column.getBlockLight(posInChunk(pos)),
        Math.min(skyLight, column.getSkyLight(posInChunk(pos)))
      ) + 2
    );
    if (result === 2 && [this.getBlock(pos)?.name ?? "", curBlockName].some((x) => /_stairs|slab|glass_pane/.exec(x)) && !skipMoreChecks) {
      const lights = [
        this.getLight(pos.offset(0, 1, 0), void 0, true),
        this.getLight(pos.offset(0, -1, 0), void 0, true),
        this.getLight(pos.offset(0, 0, 1), void 0, true),
        this.getLight(pos.offset(0, 0, -1), void 0, true),
        this.getLight(pos.offset(1, 0, 0), void 0, true),
        this.getLight(pos.offset(-1, 0, 0), void 0, true)
      ].filter((x) => x !== 2);
      if (lights.length) {
        const min = Math.min(...lights);
        result = min;
      }
    }
    if (isNeighbor && result === 2)
      result = 15;
    return result;
  }
  addColumn(x, z, json) {
    const chunk = this.Chunk.fromJson(json);
    this.columns[worldColumnKey(x, z)] = chunk;
    return chunk;
  }
  removeColumn(x, z) {
    delete this.columns[worldColumnKey(x, z)];
  }
  getColumn(x, z) {
    return this.columns[worldColumnKey(x, z)];
  }
  setBlockStateId(pos, stateId) {
    if (stateId === void 0)
      throw new Error("stateId is undefined");
    const key = worldColumnKey(Math.floor(pos.x / 16) * 16, Math.floor(pos.z / 16) * 16);
    const column = this.columns[key];
    if (!column)
      return false;
    column.setBlockStateId(posInChunk(pos.floored()), stateId);
    return true;
  }
  getColumnByPos(pos) {
    return this.getColumn(Math.floor(pos.x / 16) * 16, Math.floor(pos.z / 16) * 16);
  }
  getBlock(pos, blockProvider, attr) {
    if (!(pos instanceof Vec3))
      pos = new Vec3(...pos);
    const chunkKey = worldColumnKey(Math.floor(pos.x / 16) * 16, Math.floor(pos.z / 16) * 16);
    const modelOverride = this.customBlockModels.get(chunkKey)?.[getBlockPosKey(pos)];
    const column = this.columns[chunkKey];
    if (!column)
      return null;
    const loc = pos.floored();
    const locInChunk = posInChunk(loc);
    const stateId = column.getBlockStateId(locInChunk);
    const cacheKey = getBlockAssetsCacheKey(stateId, modelOverride);
    if (!this.blockCache[cacheKey]) {
      const b = column.getBlock(locInChunk);
      if (modelOverride) {
        b.name = modelOverride;
      }
      b.isCube = isCube2(b.shapes);
      this.blockCache[cacheKey] = b;
      Object.defineProperty(b, "position", {
        get() {
          throw new Error("position is not reliable, use pos parameter instead of block.position");
        }
      });
      if (this.preflat) {
        b._properties = {};
        const namePropsStr = preflatMap_default.blocks[b.type + ":" + b.metadata] || findClosestLegacyBlockFallback(b.type, b.metadata, pos);
        if (namePropsStr) {
          b.name = namePropsStr.split("[")[0];
          const propsStr = namePropsStr.split("[")?.[1]?.split("]");
          if (propsStr) {
            const newProperties = Object.fromEntries(propsStr.join("").split(",").map((x) => {
              let [key, val] = x.split("=");
              if (!isNaN(val))
                val = parseInt(val, 10);
              return [key, val];
            }));
            b._properties = newProperties;
          }
        }
      }
    }
    const block = this.blockCache[cacheKey];
    if (block.models === void 0 && blockProvider) {
      if (!attr)
        throw new Error("attr is required");
      const props = block.getProperties();
      try {
        if (this.preflat) {
          if (block.name === "cobblestone_wall") {
            props.up = "true";
            for (const key of ["north", "south", "east", "west"]) {
              const val = props[key];
              if (val === "false" || val === "true") {
                props[key] = val === "true" ? "low" : "none";
              }
            }
          }
        }
        const useFallbackModel = !!(this.preflat || modelOverride);
        const issues = [];
        const resolvedModelNames = [];
        const resolvedConditions = [];
        block.models = blockProvider.getAllResolvedModels0_1(
          {
            name: block.name,
            properties: props
          },
          useFallbackModel,
          issues,
          resolvedModelNames,
          resolvedConditions
        );
        if (!this.sentBlockStateModels.has(cacheKey)) {
          this.blockStateModelInfo.set(cacheKey, {
            cacheKey,
            issues,
            modelNames: resolvedModelNames,
            conditions: resolvedConditions
          });
        }
        if (!block.models.length) {
          if (block.name !== "water" && block.name !== "lava" && !INVISIBLE_BLOCKS.has(block.name)) {
            console.debug("[mesher] block to render not found", block.name, props);
          }
          block.models = null;
        }
        if (block.models && modelOverride) {
          const model = block.models[0];
          block.transparent = model[0]?.["transparent"] ?? block.transparent;
        }
      } catch (err) {
        this.erroredBlockModel ??= blockProvider.getAllResolvedModels0_1({ name: "errored", properties: {} });
        block.models ??= this.erroredBlockModel;
        console.error(`Critical assets error. Unable to get block model for ${block.name}[${JSON.stringify(props)}]: ` + err.message, err.stack);
        attr.hadErrors = true;
      }
    }
    if (block.name === "flowing_water")
      block.name = "water";
    if (block.name === "flowing_lava")
      block.name = "lava";
    if (block.name === "bubble_column")
      block.name = "water";
    block.biome = this.biomeCache[column.getBiome(locInChunk)] ?? this.biomeCache[1] ?? this.biomeCache[0];
    if (block.name === "redstone_ore")
      block.transparent = false;
    return block;
  }
  shouldMakeAo(block) {
    return block?.isCube && !ignoreAoBlocks.includes(block.name) && block.boundingBox !== "empty";
  }
};
var findClosestLegacyBlockFallback = (id, metadata, pos) => {
  console.warn(`[mesher] Unknown block with ${id}:${metadata} at ${pos}, falling back`);
  for (const [key, value] of Object.entries(preflatMap_default.blocks)) {
    const [idKey, meta] = key.split(":");
    if (idKey === id)
      return value;
  }
  return null;
};
var hasChunkSection = (column, pos) => {
  if (column._getSection)
    return column._getSection(pos);
  if (column.skyLightSections) {
    return column.skyLightSections[getLightSectionIndex(pos, column.minY)] || column.blockLightSections[getLightSectionIndex(pos, column.minY)];
  }
  if (column.sections)
    return column.sections[pos.y >> 4];
};
function posInChunk(pos) {
  return new Vec3(Math.floor(pos.x) & 15, Math.floor(pos.y), Math.floor(pos.z) & 15);
}
function getLightSectionIndex(pos, minY = 0) {
  return Math.floor((pos.y - minY) / 16) + 1;
}

// ../src/mesher/computeHeightmap.ts
function computeHeightmap(world, chunkX, chunkZ) {
  const heightmap = new Int16Array(256);
  const blockPos = new Vec3(0, 0, 0);
  for (let z = 0; z < 16; z++) {
    for (let x = 0; x < 16; x++) {
      blockPos.x = x + chunkX;
      blockPos.z = z + chunkZ;
      blockPos.y = world.config.worldMaxY;
      let block = world.getBlock(blockPos);
      while (block && INVISIBLE_BLOCKS.has(block.name) && blockPos.y > world.config.worldMinY) {
        blockPos.y -= 1;
        block = world.getBlock(blockPos);
      }
      const index = z * 16 + x;
      heightmap[index] = block ? blockPos.y : -32768;
    }
  }
  return heightmap;
}
function handleGetHeightmap(world, x, z) {
  const heightmap = computeHeightmap(world, x, z);
  const key = `${Math.floor(x / 16)},${Math.floor(z / 16)}`;
  return { key, heightmap };
}

// test-section-boundary.ts
var VERSION = "1.16.5";
var WORLD_MIN_Y = 0;
var WORLD_MAX_Y = 256;
var WORLD_HEIGHT = WORLD_MAX_Y - WORLD_MIN_Y;
var STONE = 1;
var FACE_UP = 1 << 0;
var FACE_DOWN = 1 << 1;
function makeChunk() {
  const Chunk = (0, import_prismarine_chunk2.default)(VERSION);
  const chunk = new Chunk({ minY: WORLD_MIN_Y, worldHeight: WORLD_HEIGHT, x: 0, z: 0 });
  return chunk;
}
function fillLayer(chunk, y, stateId) {
  for (let z = 0; z < 16; z++) {
    for (let x = 0; x < 16; x++) {
      chunk.setBlockStateId(new Vec3(x, y, z), stateId);
    }
  }
}
function meshSection(chunk, sectionY, sectionHeight) {
  const sectionDataStartY = Math.max(sectionY - 1, WORLD_MIN_Y);
  const sectionDataEndY = Math.min(sectionY + sectionHeight + 1, WORLD_MAX_Y);
  const sectionDataHeight = sectionDataEndY - sectionDataStartY;
  const conv = convertChunkToWasm(
    chunk,
    VERSION,
    0,
    0,
    WORLD_MIN_Y,
    WORLD_MAX_Y,
    sectionDataStartY,
    sectionDataHeight
  );
  return wasm.generate_geometry(
    0,
    sectionY,
    0,
    sectionHeight,
    WORLD_MIN_Y,
    WORLD_MAX_Y,
    sectionDataStartY,
    conv.blockStates,
    conv.blockLight,
    conv.skyLight,
    conv.biomesArray,
    conv.invisibleBlocks,
    conv.transparentBlocks,
    conv.noAoBlocks,
    conv.cullIdenticalBlocks,
    conv.occludingBlocks,
    true,
    false,
    15
  );
}
function countFaces(result, predicate, faceMask) {
  let count = 0;
  for (const block of result.blocks) {
    if (!predicate(block))
      continue;
    if ((block.visible_faces & faceMask) !== 0)
      count++;
  }
  return count;
}
function testSectionBoundary() {
  console.log("\n=== Section-boundary fixture ===");
  const chunk = makeChunk();
  fillLayer(chunk, 15, STONE);
  fillLayer(chunk, 16, STONE);
  const lower = meshSection(chunk, 0, 16);
  const topFacesAtSeamLower = countFaces(lower, (b) => b.position[1] === 15, FACE_UP);
  console.log(`  lower section: top faces emitted at y=15: ${topFacesAtSeamLower} (expected 0)`);
  console.log(`  lower section: total emitted blocks: ${lower.blocks.length}`);
  if (topFacesAtSeamLower !== 0) {
    throw new Error(
      `Section-boundary regression (lower): expected 0 top faces at y=15 (occluded by y=16 stone), got ${topFacesAtSeamLower}`
    );
  }
  const upper = meshSection(chunk, 16, 16);
  const bottomFacesAtSeamUpper = countFaces(upper, (b) => b.position[1] === 16, FACE_DOWN);
  console.log(`  upper section: bottom faces emitted at y=16: ${bottomFacesAtSeamUpper} (expected 0)`);
  console.log(`  upper section: total emitted blocks: ${upper.blocks.length}`);
  if (bottomFacesAtSeamUpper !== 0) {
    throw new Error(
      `Section-boundary regression (upper): expected 0 bottom faces at y=16 (occluded by y=15 stone), got ${bottomFacesAtSeamUpper}`
    );
  }
  console.log("  \u2705 Section-boundary fixture passed");
}
function testHeightmapParity() {
  console.log("\n=== Heightmap parity fixture ===");
  const chunk = makeChunk();
  for (let z = 0; z < 16; z++) {
    for (let x = 0; x < 16; x++) {
      const topY = 64 + (x + z * 3) % 16;
      chunk.setBlockStateId(new Vec3(x, topY, z), STONE);
    }
  }
  const reference = new Int16Array(256);
  const probe = new Vec3(0, 0, 0);
  for (let z = 0; z < 16; z++) {
    for (let x = 0; x < 16; x++) {
      let foundY = -32768;
      for (let y = WORLD_MAX_Y - 1; y >= WORLD_MIN_Y; y--) {
        probe.x = x;
        probe.y = y;
        probe.z = z;
        const id = chunk.getBlockStateId(probe);
        if (id !== 0 && id !== void 0 && id !== null) {
          foundY = y;
          break;
        }
      }
      reference[z * 16 + x] = foundY;
    }
  }
  const buildWorld = () => {
    const w = new World(VERSION);
    w.config = { ...w.config, worldMinY: WORLD_MIN_Y, worldMaxY: WORLD_MAX_Y, version: VERSION };
    w.columns["0,0"] = chunk;
    return w;
  };
  const helperHeightmap = computeHeightmap(buildWorld(), 0, 0);
  for (let i = 0; i < 256; i++) {
    if (helperHeightmap[i] !== reference[i]) {
      throw new Error(
        `computeHeightmap mismatch at index ${i}: got ${helperHeightmap[i]}, expected ${reference[i]}`
      );
    }
  }
  const handlerOut = handleGetHeightmap(buildWorld(), 0, 0);
  if (handlerOut.key !== "0,0") {
    throw new Error(`handleGetHeightmap key mismatch: got "${handlerOut.key}", expected "0,0"`);
  }
  if (handlerOut.heightmap.length !== 256) {
    throw new Error(`handleGetHeightmap heightmap length: got ${handlerOut.heightmap.length}, expected 256`);
  }
  for (let i = 0; i < 256; i++) {
    if (handlerOut.heightmap[i] !== reference[i]) {
      throw new Error(
        `handleGetHeightmap mismatch at index ${i}: got ${handlerOut.heightmap[i]}, expected ${reference[i]}`
      );
    }
  }
  const fs = require("fs");
  const path = require("path");
  const checkHandler = (file) => {
    const src = fs.readFileSync(path.join(__dirname, "..", "src", "mesher", file), "utf8");
    if (!/case\s+'getHeightmap'\s*:/.test(src)) {
      throw new Error(`${file}: no 'getHeightmap' case found \u2014 handler may have been removed/renamed`);
    }
    if (!/handleGetHeightmap\s*\(\s*world\s*,/.test(src)) {
      throw new Error(
        `${file}: 'getHeightmap' handler no longer delegates to handleGetHeightmap(world, \u2026). If you intentionally inlined the logic, update test-section-boundary.ts to assert the new path matches the independent reference.`
      );
    }
  };
  checkHandler("mesher.ts");
  checkHandler("mesherWasm.ts");
  console.log(`  \u2705 256/256 entries match independent reference`);
  console.log(`  \u2705 handleGetHeightmap direct invocation matches reference (key="${handlerOut.key}")`);
  console.log(`  \u2705 both mesher.ts and mesherWasm.ts getHeightmap handlers still delegate to handleGetHeightmap`);
  console.log(`     sample: [0]=${helperHeightmap[0]} [42]=${helperHeightmap[42]} [255]=${helperHeightmap[255]}`);
}
var main = async () => {
  testSectionBoundary();
  testHeightmapParity();
  console.log("\n\u2705 All boundary / heightmap fixtures passed");
};
main().catch((err) => {
  console.error("\u274C Fixture failure:", err);
  process.exit(1);
});
/*! Bundled license information:

uri-js/dist/es5/uri.all.js:
  (** @license URI.js v4.4.1 (c) 2011 Gary Court. License: http://github.com/garycourt/uri-js *)

safe-buffer/index.js:
  (*! safe-buffer. MIT License. Feross Aboukhadijeh <https://feross.org/opensource> *)
*/
//# sourceMappingURL=wasm-mesher-boundary.cjs.map
